---
title: Curriculum-Driven 3D CT Report Generation via Language-Free Visual Grafting and Zone-Constrained Compression
title_zh: 基于无语言视觉嫁接与区域约束压缩的课程驱动型 3D CT 报告生成
authors: "V. K. Cody Bumgardner, Mitchell A. Klusty, Mahmut S. Gokmen, Evan W. Damron"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23308v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 深度学习用于3D CT放射报告生成
tldr: 针对3D CT报告生成中序列过长、类别失衡及LLM视觉忽略等挑战，本文提出Ker-VLJEPA-3B框架。该框架采用四阶段课程学习，将自监督学习的语言无关视觉编码器与Llama 3.2集成。通过区域约束交叉注意力压缩视觉特征，并结合正向发现策略和EWC技术，在CT-RATE基准上超越了现有SOTA模型，显著提升了报告生成的准确性与视觉关联度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.23308v1/fig-001.webp\", \"caption\": \"Fig. 2: Four-phase curriculum learning pipeline. Each phase progressively builds capability from visual discrimination (Phase 1) through contrastive alignment (Phase 2) to free-text generation (Phase 3) and narrative adaptation (Phase 4). Colored boxes indicate trainable components; dashed boxes indicate frozen components. The warm bridge initialization (red dashed arrow) transfers 27 converged bridge components and 392 LoRA tensors from a prior Phase 3 run, providing immediate convergence and eliminating the cold-start problem.\", \"page\": 6, \"index\": 1, \"width\": 971, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2603.23308v1/fig-002.webp\", \"caption\": \"TABLE VII: Per-Class Results (Phase 4, 2,984 Validation Volumes)\", \"page\": 8, \"index\": 2, \"width\": 442, \"height\": 413}]"
motivation: 解决3D CT影像数据量大、报告类别极度不平衡以及大语言模型在生成报告时过度依赖文本先验而忽略视觉信息的问题。
method: 提出一种四阶段课程学习框架，利用区域约束交叉注意力将切片嵌入压缩为空间定位的视觉标记，并结合PCA白化、正向发现策略及弹性权重巩固技术。
result: 在CT-RATE基准测试中，该模型达到0.429的宏F1分数，优于现有SOTA模型U-VLM，且消融实验证明其生成质量高度依赖于患者特定的视觉内容。
conclusion: 证明了通过课程学习和语言无关的视觉嫁接技术，可以有效提升3D医疗影像报告生成的质量，并缓解LLM的语言偏见问题。
---

## 摘要
从 3D 计算机断层扫描（CT）体数据自动生成放射学报告面临着极长序列、严重类别不平衡以及大语言模型（LLM）倾向于忽略视觉标记而偏向语言先验等挑战。我们提出了 Ker-VLJEPA-3B，这是一个用于从胸部 CT 体数据生成自由文本报告的四阶段课程学习框架。该分阶段训练课程逐步引导 Llama 3.2 3B 解码器将其输出建立在来自冻结自监督编码器的视觉特征之上。我们的视觉骨干网络（LeJEPA ViT-Large）通过在无标签 CT 上进行自监督联合嵌入预测训练，无需文本监督。与对比学习模型（如 CLIP、BiomedCLIP）不同，这种无语言骨干网络能产生模态纯净的表示。视觉-语言对齐被推迟到课程的桥接和生成阶段。这种模态无关的设计可以将任何自监督编码器集成到 LLM 中，而无需在基础训练期间使用配对文本。方法论创新包括：(1) 区域约束交叉注意力，将切片嵌入压缩为 32 个具有空间定位的视觉标记；(2) 对各向异性 LLM 嵌入进行 PCA 白化；(3) 仅针对阳性发现的策略，消除了后验崩溃；(4) 转移投影权重的热桥接初始化；(5) 结合弹性权重巩固（EWC）的选择性交叉注意力冻结，以防止灾难性遗忘。在 CT-RATE 基准测试（2,984 个验证体数据，18 个类别）上的评估结果显示，Ker-VLJEPA-3B 的宏 F1 达到 0.429，比现有最先进模型（U-VLM，宏 F1 = 0.414）高出 3.6%，在阈值优化后达到 0.448（+8.2%）。消融研究证实，56.6% 的生成质量源自患者特异性的视觉内容。代码和权重已公开。

## Abstract
Automated radiology report generation from 3D computed tomography (CT) volumes is challenging due to extreme sequence lengths, severe class imbalance, and the tendency of large language models (LLMs) to ignore visual tokens in favor of linguistic priors. We present Ker-VLJEPA-3B, a four-phase curriculum learning framework for free-text report generation from thoracic CT volumes. A phased training curriculum progressively adapts a Llama 3.2 3B decoder to ground its output in visual features from a frozen, self-supervised encoder. Our visual backbone (LeJEPA ViT-Large) is trained via self-supervised joint-embedding prediction on unlabeled CTs, without text supervision. Unlike contrastive models (CLIP, BiomedCLIP), this language-free backbone yields modality-pure representations. Vision-language alignment is deferred to the curriculum's bridge and generation phases. This modality-agnostic design can integrate any self-supervised encoder into an LLM without paired text during foundation training. Methodological innovations include: (1) zone-constrained cross-attention compressing slice embeddings into 32 spatially-grounded visual tokens; (2) PCA whitening of anisotropic LLM embeddings; (3) a positive-findings-only strategy eliminating posterior collapse; (4) warm bridge initialization transferring projection weights; and (5) selective cross-attention freezing with elastic weight consolidation to prevent catastrophic forgetting. Evaluated on the CT-RATE benchmark (2,984 validation volumes, 18 classes), Ker-VLJEPA-3B achieves a macro F1 of 0.429, surpassing the state-of-the-art (U-VLM, macro F1 = 0.414) by 3.6%, and reaching 0.448 (+8.2%) with threshold optimization. Ablation studies confirm 56.6% of generation quality derives from patient-specific visual content. Code and weights are available.

---

## 论文详细总结（自动生成）

这是一份关于论文《Curriculum-Driven 3D CT Report Generation via Language-Free Visual Grafting and Zone-Constrained Compression》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心挑战**：从 3D CT 影像自动生成放射科报告面临三大难题：
    1.  **数据维度极高**：单次扫描包含数百个切片，远超标准大语言模型（LLM）的上下文窗口。
    2.  **类别极度不平衡**：病理发现（如小结节）仅占极小空间，导致模型倾向于生成通用的“正常”描述。
    3.  **后验崩溃（Posterior Collapse）**：LLM 强大的语言先验使其在生成时倾向于忽略注入的视觉标记，转而依赖文本统计规律。
*   **研究背景**：现有方法（如 CT-CLIP, U-VLM）多依赖于受文本监督的视觉编码器，这会将语言偏见提前引入视觉表示中。本文旨在通过“无语言”视觉特征与“课程驱动”的对齐策略，实现更纯净、更具鲁棒性的 3D 医疗影像理解。

### 2. 核心思想与关键技术
本文提出了 **Ker-VLJEPA-3B** 框架，其核心思想是将视觉表示学习与语言对齐完全解耦。
*   **无语言视觉骨干**：使用 LeJEPA ViT-Large，仅通过自监督联合嵌入预测（JEPA）在无标签 CT 上训练，不涉及任何文本监督。
*   **区域约束交叉注意力（Zone-Constrained Cross-Attention）**：将 z 轴划分为 32 个解剖区域，每个区域由一个可学习查询负责，将变长的切片序列（最多 600 层）压缩为 32 个具有空间定位意义的视觉标记。
*   **四阶段课程学习**：
    1.  **视觉对齐阶段**：训练编码器进行 18 类异常检测。
    2.  **对比桥接阶段**：通过 PCA 白化处理 LLM 嵌入的各向异性，利用 InfoNCE 损失进行视觉-文本对齐。
    3.  **生成微调阶段**：采用**仅阳性发现（Positive-findings-only）**训练策略，强制模型关注病理特征，避免被大量“正常”文本梯度淹没。
    4.  **叙述适配阶段**：使用弹性权重巩固（EWC）和选择性冻结技术，在保持病理检测能力的同时，学习放射科医生的自然语言风格。
*   **热桥接初始化（Warm Bridge Init）**：在课程阶段转换时，通过迁移已收敛的投影层和 LoRA 权重，解决冷启动导致的性能回退问题。

### 3. 实验设计与 Benchmark
*   **数据集**：使用 **CT-RATE** 基准数据集，包含约 50,000 个训练体数据和 2,984 个验证体数据，涵盖 18 种胸部异常类别。
*   **评估协议**：生成自由文本报告 -> 使用官方 RadBERT 分类器提取标签 -> 计算宏平均 F1、精确率和召回率。
*   **对比方法**：对比了当前最先进的（SOTA）模型，包括 **CT-CLIP**、**CT-CHAT**、**BTB3D** 以及之前的记录保持者 **U-VLM**。

### 4. 资源与算力
*   **硬件环境**：所有实验均在 **8 × NVIDIA H200 GPU** 集群上运行。
*   **训练细节**：
    *   使用 HuggingFace Accelerate 进行 DDP 分布式训练。
    *   各阶段训练时长不等：阶段 1（20 epoch）、阶段 2（24 epoch）、阶段 3（约 9 epoch 达到最佳）、阶段 4（8 epoch）。
    *   模型基于 Llama 3.2 3B 构建，视觉编码器为 ViT-Large。

### 5. 实验数量与充分性
*   **实验规模**：在近 3,000 个验证体数据上进行了完整测试，涵盖了 18 类疾病。
*   **消融实验**：设计了非常充分的消融研究，包括：
    *   **线性探针实验**：验证视觉特征在各阶段的信息保留情况。
    *   **视觉标记消融**：通过置零、随机化、洗牌（Shuffled）视觉标记，量化视觉信息对生成的贡献（结论是 56.6% 的质量源于视觉内容）。
    *   **语义绑定分析**：通过负对数似然（NLL）分析，证明视觉标记在病理相关词汇上的贡献是通用词汇的 2 倍。
*   **评价**：实验设计客观、公平，特别是针对“后验崩溃”的对比实验，有力支撑了其方法论的有效性。

### 6. 主要结论与发现
*   **性能超越 SOTA**：Ker-VLJEPA-3B 在默认阈值下宏 F1 达到 **0.429**（比 U-VLM 提升 3.6%），优化阈值后达到 **0.448**（提升 8.2%）。
*   **高召回率特性**：相比 U-VLM，该模型在召回率上表现优异（+22.1%），更符合临床上“不漏诊”的初衷。
*   **视觉对齐的必要性**：证明了即使视觉编码器从未见过文本，通过合理的课程桥接，也能实现极高质量的报告生成。

### 7. 优点与亮点
*   **模态无关性**：设计高度通用，理论上可以将任何自监督编码器（如基因组、传感器数据）接入 LLM。
*   **解决梯度失衡**：通过“仅阳性发现”训练策略，巧妙解决了医疗报告中“正常描述”过剩导致的模型钝化问题。
*   **空间定位**：区域约束注意力确保了视觉标记与解剖部位的固定映射，增强了模型的可解释性。

### 8. 不足与局限
*   **精确率权衡**：虽然召回率极高，但精确率较 U-VLM 有所下降，可能存在一定的误报风险。
*   **单一基准限制**：目前仅在 CT-RATE 数据集上验证，跨中心、跨设备的泛化能力尚待进一步测试。
*   **算力门槛**：训练依赖 8 张 H200 等高端硬件，对于资源受限的机构存在部署难度。
*   **缺乏临床评估**：尚未包含放射科医生的主观盲测（Observer Study），临床实用性需进一步验证。

（完）
