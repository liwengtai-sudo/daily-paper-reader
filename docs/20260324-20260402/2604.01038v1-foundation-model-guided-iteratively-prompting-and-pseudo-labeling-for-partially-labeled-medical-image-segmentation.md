---
title: Foundation Model-guided Iteratively Prompting and Pseudo-Labeling for Partially Labeled Medical Image Segmentation
title_zh: 基础模型引导的迭代提示与伪标签方法用于部分标注医学图像分割
authors: "Qiaochu Zhao, Wei Wei, David Horowitz, Richard Bakst, Yading Yuan"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01038v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 利用基础模型和伪标签进行医学图像分割
tldr: 针对医学图像分割中由于标注成本高导致的标签缺失问题，本文提出了一种名为IPnP的迭代提示与伪标签框架。该框架通过可训练的分割网络（专家）与冻结的基础模型（通用专家）之间的协作，迭代地为未标注器官生成并优化伪标签，从而逐步恢复全器官监督。在AMOS公开数据集和真实临床头颈癌数据集上的实验表明，该方法显著提升了分割性能，接近全监督水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01038v1/fig-001.webp\", \"caption\": \"Fig. 2. Overview of the proposed IPnP framework.\", \"page\": 2, \"index\": 1, \"width\": 410, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.01038v1/fig-002.webp\", \"caption\": \"Table 1. Segmentation performance of IPnP and other methods on the AMOS dataset, reported in DSC (%) and HD95 (mm).\", \"page\": 4, \"index\": 2, \"width\": 1029, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2604.01038v1/fig-003.webp\", \"caption\": \"Fig. 5. Segmentation performance of IPnP and other methods on HnN dataset, reported in DSC (%). The numbers in parentheses indicate the counts of labeled organs in the training, validation, and testing sets, respectively, for each organ.\", \"page\": 4, \"index\": 3, \"width\": 781, \"height\": 256}]"
motivation: 临床实践中医学影像往往仅有部分器官被标注，导致模型在处理部分标注数据时性能下降。
method: 提出IPnP框架，利用基础模型引导分割网络通过迭代提示和伪标签技术来恢复缺失的器官标注。
result: 在模拟和真实的部分标注数据集上，该方法均优于现有技术，且性能逼近全标注基准。
conclusion: 该研究证明了结合基础模型与专用分割网络能有效解决部分标注问题，具有显著的临床应用价值。
---

## 摘要
自动化医学图像分割在全标注数据上取得了显著进展。然而，由于特定机构的临床优先级和高昂的人工标注成本，扫描图像往往仅标注了部分器官，导致了部分标注问题，从而降低了性能。为解决这一问题，我们提出了 IPnP，一个用于部分标注医学图像分割的迭代提示与伪标签框架。IPnP 通过可训练分割网络（专家）与冻结的基础模型（通用专家）之间的协作，为未标注器官迭代生成并优化伪标签，从而逐步恢复全器官监督。在模拟部分标注设置的公开数据集 AMOS 上，IPnP 的分割性能始终优于现有方法，并接近全标注参考基准的性能。我们进一步在包含 210 名头颈癌患者的私有部分标注数据集上进行了评估，证明了该方法在真实临床场景中的有效性。

## Abstract
Automated medical image segmentation has achieved remarkable progress with fully labeled data. However, site-specific clinical priorities and the high cost of manual annotation often yield scans with only a subset of organs labeled, leading to the partially labeled problem that degrades performance. To address this issue, we propose IPnP, an Iteratively Prompting and Pseudo-labeling framework, for partially labeled medical image segmentation. IPnP iteratively generates and refines pseudo-labels for unlabeled organs through collaboration between a trainable segmentation network (specialist) and a frozen foundation model (generalist), progressively recovering full-organ supervision. On the public dataset AMOS with the simulated partial-label setting, IPnP consistently improves segmentation performance over prior methods and approaches the performance of the fully labeled reference. We further evaluate on a private, partially labeled dataset of 210 head-and-neck cancer patients and demonstrate our effectiveness in real-world clinical settings.

---

## 论文详细总结（自动生成）

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：医学图像分割通常依赖全标注数据，但在实际临床中，由于标注成本高昂和临床关注点不同，数据集往往呈现“部分标注”（Partially Labeled）状态（即一张CT中只标注了部分器官）。
*   **研究背景**：现有的部分标注处理方法（如忽略未标注器官或将其视为背景）会导致监督信号不足或错误，进而影响模型对完整解剖结构的理解。虽然基础模型（如SAM）具有强大的泛化能力，但它们通常需要高质量的人工提示（Prompts）才能生成准确的掩码。

### 2. 方法论：IPnP 框架
论文提出了 **IPnP (Iteratively Prompting and Pseudo-labeling)** 框架，核心思想是让“专家模型”（可训练的分割网络）与“通用专家”（冻结的基础模型）进行协作。
*   **初始训练**：专家模型（基于 nnU-Net）先在部分标注数据上训练，获得初步的分割能力。
*   **交互式伪标签生成**：
    *   专家模型对未标注器官生成粗略预测。
    *   从预测中提取轴向和矢状中层切片的边界框（Bounding Box），并进行填充（Padding）处理。
    *   将这些自动生成的框作为提示（Prompts）输入给基础模型（nnInteractive），引导其生成未标注器官的候选伪标签。
*   **伪标签精炼（Refinement）**：通过三个约束过滤噪声：
    1.  **类别概率阈值**：保留高置信度体素。
    2.  **ROI 约束**：限制在提示框扩展后的感兴趣区域内。
    3.  **熵约束**：仅当当前迭代的平均熵低于前一次时才更新伪标签，确保不确定性降低。
*   **迭代训练与 VLS 损失**：引入**体素级选择损失（Voxel-level Selection Loss, VLS）**。通过掩码 $m(v)$ 屏蔽掉预测与伪标签不一致的不可靠体素，仅对可靠的伪标签和真实的地面真值（GT）计算 Dice 和交叉熵损失。

### 3. 实验设计
*   **数据集**：
    1.  **AMOS 22 腹部数据集**：模拟部分标注场景（随机保留 67% 和 33% 的标签）。
    2.  **私有头颈癌（HnN）数据集**：210 例真实临床数据，包含 13 个器官，存在天然的标注缺失。
*   **基准（Benchmark）与对比方法**：
    *   **全监督（Full Labels）**：使用完整标注训练，作为性能上限。
    *   **全监督（Partial Labels）**：将缺失标签视为背景训练，作为性能下限。
    *   **部分监督（Partial Supervision）**：使用 Sigmoid 激活并仅对已知标签计算损失。
    *   **Trans-DoDNet**：当时最先进的处理部分标注问题的动态头部网络方法。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA RTX 6000 Ada GPUs**。
*   **训练细节**：专家模型基于 nnUNetv2，训练 1000 个 epoch，批大小（Batch Size）为 2，采用 SGD 优化器。
*   **时长**：文中未明确提及具体的总训练时长，但考虑到 1000 epoch 的 3D 卷积训练及多次迭代过程，计算开销相对较大。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 AMOS 数据集上进行了两种缺失比例（67% 和 33%）的对比实验。
    *   在 210 例真实临床数据集上验证了泛化性。
    *   进行了消融实验（如验证 VLS 损失的有效性）。
*   **充分性评价**：实验设计较为充分，涵盖了模拟环境和真实临床环境。对比了多种主流处理策略，评价指标（DSC 和 HD95）兼顾了重叠度和边界精度，结果具有客观性。

### 6. 主要结论与发现
*   **性能优异**：IPnP 在 AMOS 数据集上的表现显著优于 Trans-DoDNet 等方法，在 67% 标注下 DSC 达到 88.06%，非常接近全标注上限（89.21%）。
*   **边界精度高**：相比于其他方法，IPnP 的 HD95 指标大幅下降，说明生成的伪标签有助于维持器官的结构完整性。
*   **小样本优势**：在头颈部数据集中，对于标注极少的器官（如仅 36 例标注的眼睛），IPnP 提升巨大（DSC 从 10% 左右提升至 90% 以上），证明了基础模型引导的有效性。

### 7. 优点
*   **自动化提示**：解决了基础模型在医学影像中依赖人工提示的痛点，实现了端到端的自动伪标签生成。
*   **迭代优化机制**：通过熵约束和 VLS 损失，有效抑制了伪标签中的噪声传播（Error Propagation）。
*   **临床实用性**：在真实的不完全标注数据集上表现强劲，具有很高的实际应用价值。

### 8. 不足与局限
*   **计算成本**：迭代过程需要多次重新训练专家模型并调用基础模型推理，相比单次训练方法，计算资源消耗更高。
*   **基础模型依赖**：性能高度依赖于所选基础模型（如 nnInteractive）的零样本能力，若基础模型对某些罕见病变或细小器官识别较差，可能会限制上限。
*   **对比范围**：虽然对比了 Trans-DoDNet，但未广泛对比更多近两年的半监督学习或大模型微调方法。

（完）
