---
title: Mamba-driven MRI-to-CT Synthesis for MRI-only Radiotherapy Planning
title_zh: 基于 Mamba 驱动的 MRI 到 CT 合成，用于仅 MRI 放射治疗规划
authors: "Konstantinos Barmpounakis, Theodoros P. Vagenas, Maria Vakalopoulou, George K. Matsopoulos"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23295v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 基于Mamba的MRI到CT合成用于放疗规划
tldr: 本研究探索了基于Mamba状态空间模型的MRI到CT图像合成，旨在支持仅依赖MRI的放射治疗计划。通过将U-Mamba和SegMamba架构应用于3D跨模态生成，该方法能够比传统CNN更有效地捕捉长程依赖关系。在SynthRAD2025数据集上的实验表明，该架构在保持快速推理的同时，实现了高精度的CT合成和几何一致性，为放疗流程中集成状态空间模型奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.23295v1/fig-001.webp\", \"caption\": \"Fig. 1. Illustration of the overall proposed framework. Input MRI volumes undergo Zscore normalization and feed a Mamba-based encoder–decoder architecture. The predicted output is transformed back to HU via reverse normalization on dataset-level and appropriate clipping. During training, the network is supervised using paired 3D CT volumes with a staged compound loss function\", \"page\": 4, \"index\": 1, \"width\": 725, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2603.23295v1/fig-002.webp\", \"caption\": \"Fig. 2. Visual comparison of U-Net, SwinUNETR, nnU-Net, SegMamba, and UMamba on representative cases for each anatomical site. The target CT images and corresponding MRI images are shown in the last columns\", \"page\": 7, \"index\": 2, \"width\": 725, \"height\": 292}]"
motivation: 旨在减少患者电离辐射暴露并消除模态配准误差，通过MRI-only方案提升放射治疗计划的效率和准确性。
method: 将原本用于分割的U-Mamba和SegMamba架构进行改进，使其适用于3D MRI到CT的跨模态图像生成任务。
result: 实验证明Mamba架构能有效捕获复杂的体积特征，在HU相似度指标和分割几何一致性上表现优异且推理速度快。
conclusion: Mamba等状态空间模型在医学图像合成任务中展现出超越传统CNN的潜力，是未来放疗工作流的有力竞争方案。
---

## 摘要
肿瘤患者的放射治疗工作流程日益依赖多模态医学成像，通常涉及磁共振成像 (MRI) 和计算机断层扫描 (CT)。仅 MRI 治疗规划已成为一种极具吸引力的替代方案，因为它减少了患者受到的电离辐射，并避免了模态间配准引入的误差。虽然基于 nnU-Net 的框架主要用于 MRI 到 CT 的合成，但我们探索了基于 Mamba 的架构来执行此任务，旨在展示状态空间建模在跨模态转换中相比于标准卷积神经网络的优势。具体而言，我们改进了最初为分割任务提出的 U-Mamba 和 SegMamba 架构，以执行跨模态图像生成。我们的 3D Mamba 架构能够有效捕捉复杂的体积特征和长程依赖关系，从而在保持快速推理时间的同时实现准确的 CT 合成。实验在 SynthRAD2025 数据集的子集上进行，该子集包含三个解剖区域的已配准单通道 MRI-CT 体积对。通过结合以亨氏单位 (HU) 计算的图像相似度指标和从 TotalSegmentator 获得的基于分割的指标进行定量评估，以确保几何一致性得到保持。研究结果为将状态空间模型集成到放射治疗工作流程中铺平了道路。

## Abstract
Radiotherapy workflows for oncological patients increasingly rely on multi-modal medical imaging, commonly involving both Magnetic Resonance Imaging (MRI) and Computed Tomography (CT). MRI-only treatment planning has emerged as an attractive alternative, as it reduces patient exposure to ionizing radiation and avoids errors introduced by inter-modality registration. While nnU-Net-based frameworks are predominantly used for MRI-to-CT synthesis, we explore Mamba-based architectures for this task, aiming to showcase the advantages of state-space modeling for cross-modality translation compared to standard convolutional neural networks. Specifically, we adapt both the U-Mamba and the SegMamba architecture, originally proposed for segmentation, to perform cross-modality image generation. Our 3D Mamba architecture effectively captures complex volumetric features and long-range dependencies, thus allowing accurate CT synthesis while maintaining fast inference times. Experiments were conducted on a subset of SynthRAD2025 dataset, comprising registered single-channel MRI-CT volume pairs across three anatomical regions. Quantitative evaluation is performed via a combination of image similarity metrics computed in Hounsefield Units (HU) and segmentation-based metrics obtained from TotalSegmentator to ensure geometric consistency is preserved. The findings pave the way for the integration of state-space models into radiotherapy workflows.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mamba-driven MRI-to-CT Synthesis for MRI-only Radiotherapy Planning》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在放射治疗（Radiotherapy）中，通常需要结合 MRI（提供卓越的软组织对比度用于肿瘤勾画）和 CT（提供电子密度信息用于剂量计算）。然而，重复获取 CT 会增加患者的电离辐射暴露，且跨模态配准（Registration）误差会影响治疗精度。
**核心问题**：如何通过 MRI 直接合成高质量的“合成 CT”（sCT），从而实现“仅 MRI 放射治疗规划”（MRI-only workflow）。
**研究背景**：虽然卷积神经网络（CNN，如 nnU-Net）和 Transformer 已被广泛应用，但 CNN 感受野有限，而 Transformer 计算复杂度高。本文探索了新兴的**状态空间模型（Mamba/SSM）**在 3D 跨模态图像生成任务中的潜力。

### 2. 论文提出的方法论
论文的核心思想是将原本用于医学图像分割的 Mamba 架构（U-Mamba 和 SegMamba）改进并迁移至图像生成任务。

*   **核心架构**：
    *   **U-Mamba**：保留了 nnU-Net 的自配置特性，将传统的卷积块替换为 Mamba 模块，以增强长程依赖建模。
    *   **SegMamba**：采用混合分层架构，融合了状态空间组件与卷积层，用于处理 3D 体积数据。
*   **关键技术细节：分阶段复合损失函数**：
    为了平衡全局强度重建与解剖细节，论文设计了如下损失策略：
    1.  **加权平均绝对误差（$L_{wMAE}$）**：根据 CT 的亨氏单位（HU）值分配权重。骨骼（>300 HU）权重最高（3.0），软组织权重中等（1.5），空气/背景权重最低（0.5），以确保对剂量计算至关重要的骨骼区域得到精确重建。
    2.  **结构相似性损失（$L_{SSIM}$）**：保证局部亮度、对比度和结构的连贯性。
    3.  **解剖特征优先损失（$L_{AFP}$）**：利用预训练的 TotalSegmentator 模型提取特征嵌入，在特征空间对比解剖结构的几何一致性。
    *   **流程**：前 100 个 Epoch 仅使用 $L_{wMAE}$ 稳定收敛，之后引入 $L_{SSIM}$ 和 $L_{AFP}$ 进行精细化调整。

### 3. 实验设计
*   **数据集**：使用 **SynthRAD2025** 大赛数据集的子集，包含来自 4 个欧洲医疗中心的 513 对已配准的 MRI-CT 体积数据。
*   **解剖区域**：涵盖腹部（AB）、头颈部（HN）和胸部（TH）。
*   **Benchmark（对比方法）**：
    *   **CNN 代表**：标准 U-Net、自配置的 nnU-Net。
    *   **Transformer 代表**：SwinUNETR。
    *   **Mamba 代表**：SegMamba、U-Mamba（本文改进版）。
*   **评估指标**：
    *   图像相似度：MAE（HU单位）、PSNR、MS-SSIM。
    *   几何一致性：通过 TotalSegmentator 对合成 CT 进行自动分割，计算 Dice 系数（DSC）和 95% 豪斯多夫距离（HD95）。

### 4. 资源与算力
*   **硬件**：使用了一台 **NVIDIA A100-SXM4-80GB GPU**。
*   **训练细节**：
    *   Mamba 模型、U-Net 和 SwinUNETR 训练了 **500 个 Epoch**。
    *   nnU-Net 遵循其默认设置，训练了 **1000 个 Epoch**。
    *   使用了 AdamW 优化器，初始学习率为 $5 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：总计 461 个训练案例和 52 个完全未见的测试案例。实验覆盖了三个不同的解剖部位，具有较好的泛化性验证。
*   **客观性与公平性**：
    *   作者在相同的训练框架、数据分割和随机种子下对比了 SegMamba、U-Net 和 SwinUNETR。
    *   虽然 nnU-Net 训练时长是 Mamba 的两倍且参数量更大（88M vs 67M/73M），但 Mamba 模型依然展现了极具竞争力的性能，这体现了实验的客观性。

### 6. 论文的主要结论与发现
*   **性能优异**：SegMamba 在 **MAE（平均绝对误差）** 指标上达到了最低值（103.33），优于所有对比模型（包括 nnU-Net）。
*   **效率优势**：Mamba 架构在参数量更少、训练轮数更短的情况下，性能与目前最强的 nnU-Net 相当，且显著优于 SwinUNETR。
*   **几何一致性**：虽然 nnU-Net 在 DSC 和 HD95 上略微领先，但 Mamba 模型在视觉上表现出更好的强度均匀性和更少的局部伪影。
*   **结论**：选择性状态空间模型（Selective SSM）是跨模态医学图像翻译任务中 CNN 和 Transformer 的有力替代方案。

### 7. 优点（亮点）
*   **首次尝试**：这是已知首篇将 U-Mamba 和 SegMamba 引入医学图像生成（而非仅仅是分割）任务的研究。
*   **针对性损失设计**：设计的加权 HU 损失函数紧贴放射治疗的临床需求，重点优化了对剂量计算影响最大的骨骼区域。
*   **评估全面**：不仅评估像素级相似度，还通过下游分割任务验证了合成图像的解剖准确性。

### 8. 不足与局限
*   **性能上限**：在几何一致性指标（DSC/HD95）上，Mamba 尚未全面超越经过深度优化的 nnU-Net。
*   **临床验证缺失**：论文未进行实际的**剂量分布计算（Dose Calculation）**对比，这是衡量合成 CT 是否能真正用于临床放疗规划的最终标准。
*   **数据集限制**：虽然使用了 SynthRAD2025，但样本量对于训练超大规模模型仍显不足，且排除了部分受限数据。

（完）
