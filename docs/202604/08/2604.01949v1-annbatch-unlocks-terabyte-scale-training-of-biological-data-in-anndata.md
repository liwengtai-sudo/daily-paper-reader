---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中 TB 级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于大规模生物数据分析的生物信息学工具
tldr: 随着生物数据集规模达到TB级，系统内存限制和数据访问效率成为机器学习训练的主要瓶颈。本文推出了annbatch，这是一款专为anndata设计的原生小批量加载器，支持直接在磁盘存储的数据集上进行核外训练。它在保持与scverse生态系统兼容的同时，显著提升了单细胞转录组、显微镜成像和全基因组测序等任务的加载吞吐量，将训练时间从数天缩短至数小时，为大规模生物AI研究提供了高效的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01949v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-003.webp\", \"caption\": \"\", \"page\": 18, \"index\": 3, \"width\": 1280, \"height\": 960}]"
motivation: 生物数据集规模已超出系统内存限制，导致数据访问而非模型计算成为训练大规模生物模型的主要瓶颈。
method: 开发了原生于anndata的annbatch加载器，通过优化磁盘数据的核外访问实现高效的小批量训练。
result: 在多项生物学基准测试中，annbatch将加载吞吐量提高了一个数量级，并将训练时间从数天缩短至数小时。
conclusion: annbatch为可扩展的生物AI提供了实用的数据加载方案，使用户无需放弃标准数据格式即可处理超大规模数据集。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及在既定计算生态系统内的下游分析。在此，我们推出了 annbatch，这是一种原生于 anndata 的小批量加载器，能够直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微成像和全基因组测序基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从数天缩短至数小时，同时保持与 scverse 生态系统的完全兼容。annbatch 为可扩展的生物人工智能建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的前提下，利用日益增长的大规模多样化数据集成为可能。Github: https://github.com/scverse/annbatch

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

这是一份关于论文《annbatch unlocks terabyte-scale training of biological data in anndata》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着生物数据集规模达到 TB 级（如亿级单细胞数据），**数据加载（I/O）**而非模型计算已成为机器学习训练的主要瓶颈。
*   **背景**：生物学领域广泛使用 `anndata` 格式，但现有的高性能数据加载框架（如 NVIDIA Merlin, Petastorm）通常需要将数据转换为特定格式，这破坏了与生物学常用工具链（如 `scverse` 生态系统）的兼容性。
*   **整体含义**：本文推出了 `annbatch`，这是一个原生支持 `anndata` 的小批量加载器，旨在不改变数据标准的前提下，通过优化磁盘访问模式，解决大规模生物数据训练中的 I/O 限制。

### 2. 论文提出的方法论
*   **核心思想**：将纯随机磁盘访问转变为**伪随机块访问（Pseudo-random chunked access）**，并配合**预洗牌（Pre-shuffling）**机制，在保证训练随机性的同时最大化顺序读取速度。
*   **关键技术细节**：
    *   **预洗牌器（Pre-shuffler）**：在训练前将 `anndata` 文件重新组织并写入磁盘。它采用延迟加载（Lazy loading）技术，仅需少量内存即可处理远超内存容量的数据集。
    *   **Zarr 后端优化**：利用 Zarr 存储格式替代传统的 HDF5/h5ad，并开发了 `zarrs-python`（基于 Rust 的高性能桥接库）。
    *   **高性能加载技术**：支持**直接 I/O（Direct I/O）**以绕过内核缓存、**固定内存（Pinned Memory）**读取以加速 CPU 到 GPU 的传输，以及 GPU 加速的内存内洗牌。
    *   **异步兼容性**：完全支持异步操作，适配云端训练环境。

### 3. 实验设计
*   **数据集与场景**：
    *   **单细胞转录组学**：使用 Tahoe-100M 数据集（约 1 亿个细胞）。
    *   **全基因组测序（WGS）**：基于 1000 Genomes 数据集，通过复制扩展至 50 万个体，涵盖稀疏（罕见变异）和密集（常见变异）特征。
    *   **单细胞显微成像**：处理高维度的图像切片数据（128x128x5）。
*   **Benchmark 与对比方法**：
    *   对比了 `scDataset`、`MappedCollection`（Lamin Labs 开发）以及传统的 `numpy memmap`。
    *   对比了不同存储后端（h5ad vs. Zarr）的性能差异。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   主要基准测试在 **AWS ml.g4dn.4xlarge**（配备 NVIDIA T4 GPU）上运行。
    *   分布式扩展性测试在 **NVIDIA HGX-H100** 单元上进行。
    *   部分测试使用了德国生物信息学基础设施（de.NBI）的 T4 节点。
*   **训练时长与效率**：
    *   在 Tahoe-100M 数据集上，`annbatch` 处理一个 epoch 仅需约 **17 分钟**，而 `scDataset` 需 7.5 小时，`MappedCollection` 需 21 小时。
    *   预洗牌开销（约 5.5 小时）在第一个训练 epoch 内即可通过节省的时间实现“回本”。

### 5. 实验数量与充分性
*   **实验组数**：论文涵盖了三种截然不同的生物模态（组学、基因组、图像），并针对不同硬件环境（云端 AWS vs. 本地 HPC）进行了重复验证。
*   **充分性与客观性**：
    *   实验设计考虑了操作系统缓存的干扰，确保在数据集远超内存的真实“核外”环境下进行测试。
    *   进行了消融实验，验证了 Zarr 格式、块大小、进程数量等因素对性能的影响。
    *   对比了不同模型复杂度（从逻辑回归到 10M 参数的 Transformer），证明了在模型较小时 I/O 瓶颈的严重性。

### 6. 论文的主要结论与发现
*   **性能飞跃**：`annbatch` 的吞吐量比现有工具高出**一个数量级**（最高达 40 倍加速）。
*   **格式优势**：Zarr 格式在预洗牌速度和加载吞吐量上显著优于传统的 h5ad 格式。
*   **打破瓶颈**：通过提高 I/O 效率，`annbatch` 成功将训练瓶颈从磁盘读取移回到了 GPU 计算能力上。
*   **生态兼容**：证明了高性能训练不需要放弃 `anndata` 这一标准格式，实现了科研分析与大规模生产的无缝衔接。

### 7. 优点
*   **生态系统集成度高**：完全兼容 `scverse`，用户无需转换数据格式即可享受加速。
*   **端到端方案**：不仅提供加载器，还提供了关键的预洗牌工具，解决了伪随机访问中的随机性保障问题。
*   **技术栈先进**：引入 Rust 编写的底层库和 Direct I/O 等数据库级优化技术。

### 8. 不足与局限
*   **采样策略限制**：由于采用连续块读取，目前难以直接实现复杂的加权采样（Weighted Sampling），需通过预先调整磁盘写入顺序来间接实现。
*   **多模态支持**：目前尚未直接支持多模态数据的同步加载（虽然未来可扩展）。
*   **初始成本**：预洗牌过程需要额外的磁盘空间和初始计算时间，尽管在大规模训练中这部分开销很快会被摊销。
*   **网络瓶颈**：虽然支持云端训练，但由于吞吐量极高，网络带宽可能成为新的瓶颈，建议优先使用本地磁盘。

（完）
