---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中太字节级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v2"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于大规模生物数据分析的生物信息学工具
tldr: 随着生物数据集规模达到TB级，内存限制成为机器学习训练的瓶颈。本文推出annbatch，一种原生于anndata的微批次加载器，支持直接在磁盘存储的数据集上进行核外训练。它解决了生物数据异构性与稀疏性的挑战，在单细胞、显微镜和全基因组测序任务中将加载吞吐量提升了一个数量级，将训练时间从数天缩短至数小时，且完全兼容scverse生态系统。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01949v2/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 2048, \"height\": 2048}, {\"url\": \"assets/figures/arxiv/2604.01949v2/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 2048, \"height\": 2048}, {\"url\": \"assets/figures/arxiv/2604.01949v2/fig-003.webp\", \"caption\": \"\", \"page\": 18, \"index\": 3, \"width\": 1280, \"height\": 960}]"
motivation: 生物数据集规模已超出系统内存，导致数据访问而非模型计算成为训练大规模生物AI模型的主要瓶颈。
method: 开发了原生于anndata的微批次加载器annbatch，支持对磁盘存储的异构生物数据进行高效的核外训练。
result: 在多项生物学基准测试中，annbatch将数据加载吞吐量提升了高达十倍，显著缩短了模型训练周期。
conclusion: annbatch为大规模生物AI提供了实用的数据加载基础设施，实现了在不放弃标准数据格式的前提下处理超大规模数据集。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集检测，以及在既定计算生态系统内的下游分析。在此，我们介绍了 annbatch，这是一种原生于 anndata 的小批量加载器（mini-batch loader），可直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序的基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短到几小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够使用日益庞大且多样化的数据集。Github: https://github.com/scverse/annbatch

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

这是一份关于论文《annbatch unlocks terabyte-scale training of biological data in anndata》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在生物人工智能（AI）领域，数据集规模已达到太字节（TB）级别，远超系统内存容量。目前的主要瓶颈已从“模型计算”转向“数据加载（I/O）”。
*   **背景**：现有的高性能数据加载框架（如 NVIDIA Merlin, WebDataset）通常需要转换文件格式，破坏了生物学界通用的 `anndata` 标准和 `scverse` 生态系统的兼容性。此外，现有的生物学加载工具（如 scDataset）在处理大规模数据时无法达到饱和 GPU 吞吐量的速度。
*   **整体含义**：本文旨在开发一种原生支持 `anndata` 格式的高性能加载器，在不改变数据标准的前提下，解决大规模生物数据训练中的 I/O 瓶颈。

### 2. 论文提出的方法论
*   **核心思想**：通过“预洗牌（Pre-shuffling）”和“伪随机块访问（Pseudo-random chunked access）”相结合，将随机磁盘读取转变为连续的大块读取，从而最大化 I/O 吞吐量。
*   **关键技术细节**：
    *   **预洗牌器（Preshuffler）**：在训练前将多个 `anndata` 文件合并并进行核外（out-of-core）洗牌，生成基于 Zarr 格式的 `anndata`。这保证了后续加载时，读取连续的磁盘块即可获得具有随机性的样本。
    *   **高性能加载器**：利用 `zarrs-python`（基于 Rust 的后端）实现高效读取。
    *   **优化技术**：采用 Linux 的 **Direct I/O**（绕过内核缓存）、**固定内存（Pinned Memory）** 读取、**异步 GPU 传输**以及自定义的非均匀切片索引。
    *   **架构兼容性**：完全集成于 `scverse` 生态，输入输出均为标准的 `anndata` 对象，且独立于具体的深度学习框架（PyTorch/TensorFlow 等）。

### 3. 实验设计
*   **数据集与场景**：
    *   **单细胞转录组学**：Tahoe-100M 数据集（约 1 亿个细胞）。
    *   **单细胞显微镜成像**：128x128x5 像素的裁剪图像。
    *   **全基因组测序（WGS）**：基于 1000 Genomes 项目扩展至 50 万人的数据集，涵盖稀疏（罕见变异）和密集（常见变异）两种模式。
*   **对比方法（Benchmarks）**：
    *   `scDataset`：现有的单细胞扩展加载器。
    *   `MappedCollection`：基于内存映射的加载方式。
    *   `numpy memmap`：未压缩的磁盘备份基准。
    *   `BioNemo`：NVIDIA 的生物模型框架（用于对比内存缓存效应）。

### 4. 资源与算力
*   **GPU 型号**：主要使用 NVIDIA **T4** (AWS ml.g4dn.4xlarge 实例) 和 **H100** (用于分布式训练和大型模型基准)。
*   **CPU 与内存**：16 vCPUs, 64GB RAM。
*   **训练时长/效率**：
    *   在 Tahoe-100M 上，`annbatch` 运行一个 epoch 仅需 **17 分钟**（GPU 加速），而 `scDataset` 需 7.5 小时，`MappedCollection` 需 21 小时。
    *   预洗牌开销约为 5.5 小时（非 GPU 任务），在第一个训练 epoch 内即可通过节省的时间完成成本摊销。

### 5. 实验数量与充分性
*   **实验覆盖**：论文针对三种不同的生物数据模态（序列、图像、基因组）进行了吞吐量测试，并进行了消融实验（如 Zarr vs H5AD 格式对比、不同 Batch Size 对拟合时间的影响、多进程扩展性分析）。
*   **充分性与客观性**：实验设计考虑了操作系统缓存的干扰，专门在超出内存容量的数据集上进行测试，确保了结果的真实性。对比了不同云环境（AWS vs de.NBI HPC）以验证硬件 I/O 限制对结果的影响，表现出较高的客观性。

### 6. 论文的主要结论与发现
*   **性能飞跃**：`annbatch` 的加载吞吐量比现有工具高出 **10 到 40 倍**（最高达 35,000 - 54,000 样本/秒）。
*   **格式优势**：采用 Zarr 作为后端显著优于传统的 H5AD，预洗牌时间缩短一半以上，且吞吐量更高。
*   **解决瓶颈**：证明了许多生物 AI 模型其实是“I/O 受限”而非“计算受限”，通过优化加载器，可以将训练时间从数天缩短至数小时。
*   **分布式潜力**：在多 GPU 环境下，`annbatch` 能够提供足够的吞吐量来使 H100 等高性能加速器达到饱和。

### 7. 优点
*   **生态兼容性极佳**：无需转换格式，直接服务于 `scverse` 社区，降低了科研人员的使用门槛。
*   **端到端优化**：不仅提供了加载器，还解决了大规模数据核外洗牌这一痛点。
*   **多模态支持**：设计通用，不仅限于单细胞数据，对高维基因组和图像数据同样有效。

### 8. 不足与局限
*   **采样策略限制**：由于采用连续块读取，目前难以实现复杂的加权采样（Weighted Sampling），需要通过特定的磁盘写入顺序来变通解决。
*   **多模态原生支持**：目前尚未直接支持跨文件的多模态数据关联加载（虽可扩展，但非当前版本核心）。
*   **预洗牌开销**：虽然可以摊销，但对于仅训练 1-2 个 epoch 的极小规模任务，预洗牌的时间成本仍需考虑。
*   **硬件依赖**：高性能表现依赖于磁盘 I/O 速度（如 NVMe），在受限的云存储（如 AWS EBS）上性能提升会受限于硬件带宽。

（完）
