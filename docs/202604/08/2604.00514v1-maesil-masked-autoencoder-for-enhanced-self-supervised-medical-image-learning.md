---
title: "MAESIL: Masked Autoencoder for Enhanced Self-supervised Medical Image Learning"
title_zh: MAESIL：用于增强型自监督医学图像学习的掩码自编码器
authors: "Kyeonghun Kim, Hyeonseok Jung, Youngung Han, Junsu Lim, YeonJu Jean, Seongbin Park, Eunseob Choi, Hyunsu Go, SeoYoung Ju, Seohyoung Park, Gyeongmin Kim, MinJu Kwon, KyungSeok Yuh, Soo Yong Kim, Ken Ying-Kai Liao, Nam-Joon Kim, Hyuk-Jae Lee"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00514v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 3D 医学影像的自监督学习
tldr: 针对3D医学影像标注数据稀缺及现有自监督学习方法忽视3D空间一致性的问题，本文提出MAESIL框架。该方法引入“超级块（superpatch）”作为3D输入单元，结合双重掩码策略的3D掩码自编码器，有效捕获轴向相干性和空间结构信息。实验证明，MAESIL在多个大型CT数据集上的重建性能显著优于传统自编码器模型，为3D医学影像预训练提供了高效稳健的方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00514v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative reconstruction results of MAESIL across different anatomical views. The original input and reconstruction output are compared. From left to right: Axial, Coronal and Sagittal views.\", \"page\": 4, \"index\": 1, \"width\": 508, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.00514v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed MAESIL framework, which masks a high ratio of an input 3D superpatch, feeds only the visible portions to an encoder, and uses a decoder to reconstruct the original superpatch from the encoded information and learnable mask tokens.\", \"page\": 2, \"index\": 2, \"width\": 906, \"height\": 213}]"
motivation: 现有的医学影像自监督学习方法通常将3D扫描视为独立的2D切片，导致关键的轴向相干性和3D结构上下文信息丢失。
method: 提出一种基于“超级块”输入单元的3D掩码自编码器框架，通过双重掩码策略高效学习3D空间表征。
result: 在三个大型公共CT数据集上，MAESIL在PSNR和SSIM等关键重建指标上均显著优于AE、VAE和VQ-VAE等现有方法。
conclusion: MAESIL成功平衡了3D上下文保留与计算效率，是3D医学影像任务中一种强大且实用的预训练解决方案。
---

## 摘要
为三维（3D）医学影像（如计算机断层扫描，CT）训练深度学习模型，从根本上受到标注数据稀缺的挑战。虽然在自然图像上进行预训练很常见，但这会导致显著的领域偏移，从而限制性能。在无标注医学数据上进行自监督学习（SSL）已成为一种强大的解决方案，但主流框架往往未能利用 CT 扫描固有的 3D 特性。这些方法通常将 3D 扫描处理为一系列独立的 2D 切片，这种方法从根本上丢弃了关键的轴向连贯性和 3D 结构上下文。为了解决这一局限性，我们提出了用于增强型自监督医学图像学习的自编码器（MAESIL），这是一种旨在高效捕获 3D 结构信息的新型自监督学习框架。核心创新是“超块”（superpatch），这是一种基于 3D 块的输入单元，在保留 3D 上下文与计算效率之间取得了平衡。我们的框架将体数据划分为超块，并采用具有双重掩码策略的 3D 掩码自编码器策略来学习全面的空间表示。我们在三个不同的、大规模公开 CT 数据集上验证了我们的方法。实验结果表明，MAESIL 在 PSNR 和 SSIM 等关键重建指标上，相较于 AE、VAE 和 VQ-VAE 等现有方法表现出显著改进。这确立了 MAESIL 作为 3D 医学影像任务中一种稳健且实用的预训练解决方案。

## Abstract
Training deep learning models for three-dimensional (3D) medical imaging, such as Computed Tomography (CT), is fundamentally challenged by the scarcity of labeled data. While pre-training on natural images is common, it results in a significant domain shift, limiting performance. Self-Supervised Learning (SSL) on unlabeled medical data has emerged as a powerful solution, but prominent frameworks often fail to exploit the inherent 3D nature of CT scans. These methods typically process 3D scans as a collection of independent 2D slices, an approach that fundamentally discards critical axial coherence and the 3D structural context. To address this limitation, we propose the autoencoder for enhanced self-supervised medical image learning(MAESIL), a novel self-supervised learning framework designed to capture 3D structural information efficiently. The core innovation is the 'superpatch', a 3D chunk-based input unit that balances 3D context preservation with computational efficiency. Our framework partitions the volume into superpatches and employs a 3D masked autoencoder strategy with a dual-masking strategy to learn comprehensive spatial representations. We validated our approach on three diverse large-scale public CT datasets. Our experimental results show that MAESIL demonstrates significant improvements over existing methods such as AE, VAE and VQ-VAE in key reconstruction metrics such as PSNR and SSIM. This establishes MAESIL as a robust and practical pre-training solution for 3D medical imaging tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《MAESIL: Masked Autoencoder for Enhanced Self-supervised Medical Image Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：3D 医学影像（如 CT）深度学习面临标注数据稀缺和领域偏移（Domain Shift）两大难题。
*   **研究背景**：虽然自监督学习（SSL）能利用无标注数据，但现有方法（如 MedMAE）通常将 3D CT 视为独立的 2D 切片处理。这种做法忽略了切片间的**轴向连贯性（Axial Coherence）**和 **3D 结构上下文**，导致模型无法充分理解复杂的解剖结构。
*   **整体含义**：本文旨在开发一种既能保留 3D 空间信息，又能兼顾计算效率的自监督预训练框架。

### 2. 核心方法论
*   **核心思想**：引入 **“超级块”（Superpatch）** 概念，将 3D 体数据划分为中等规模的块，在块内进行 3D 掩码重建任务。
*   **关键技术细节**：
    *   **超级块划分**：将 512×512×512 的 CT 扫描划分为 64 个非重叠的 128×128×128 超级块。
    *   **分级标记化（Tokenization）**：每个超级块进一步细分为 8×8×8 的局部标准块（Standardized Patches），产生 4,096 个 Token。
    *   **双重掩码策略（Dual-masking）**：
        1.  **平面掩码（Plane-wise）**：随机丢弃每个 2D 平面内 75% 的块。
        2.  **轴向掩码（Axis-wise）**：沿上下轴（S-I 轴）移除连续 50% 的切片块。
    *   **架构**：采用基于 Transformer 的 Encoder-Decoder 结构。Encoder 仅处理可见 Token，Decoder 结合可学习的掩码 Token 重建原始超级块。

### 3. 实验设计
*   **数据集**：使用了三个大型公共 CT 数据集进行联合预训练：
    *   **BTCV**：腹部器官分割（30 例）。
    *   **LIDC-IDRI**：胸部 CT 扫描（1,018 例）。
    *   **TotalSegmentatorV2**：全身 104 种解剖结构（1,204 例）。
*   **基准测试（Benchmark）**：图像重建质量评估。
*   **对比方法**：标准自编码器（AE）、变分自编码器（VAE）、矢量量化变分自编码器（VQ-VAE）。
*   **评估指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似度）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及训练时长。
*   **参数设置**：提到使用了 768 维的嵌入维度，总掩码率高达 75%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在统一的大规模数据集上进行了跨模型的定量对比。
    *   针对三个子数据集分别进行了 PSNR 性能分析（表 III）。
    *   提供了轴状位、冠状位、矢状位三个视角的定性重建结果对比。
*   **充分性评价**：实验涵盖了从局部器官到全身的多样化数据，对比了主流的生成式基准模型，具有较好的客观性。但缺乏在下游任务（如分割、分类）上的微调性能对比，这对于验证预训练表征的有效性至关重要。

### 6. 主要结论与发现
*   **性能领先**：MAESIL 在所有指标上均显著优于基准模型。其 PSNR 达到 30.28（比 AE 高 5dB+），SSIM 接近完美的 0.98，LPIPS 最低（0.26）。
*   **3D 上下文学习**：定性结果显示，模型能跨轴（轴、冠、矢）高保真重建脊椎和软组织，证明其成功捕获了 3D 空间关系。
*   **鲁棒性**：即使在 TotalSegmentator 这种极具多样性和挑战性的数据集上，模型依然能维持稳健的重建表现。

### 7. 优点与亮点
*   **创新的输入单元**：通过“超级块”巧妙平衡了 3D 上下文保留与计算开销，避免了全量 3D 扫描处理的内存爆炸问题。
*   **强力的掩码策略**：双重掩码（尤其是 50% 轴向掩码）极大地增加了重建难度，迫使模型学习深层的解剖结构逻辑而非简单的像素插值。
*   **跨领域泛化**：在混合了腹部、胸部、全身的数据集上训练，展现了强大的通用表征学习能力。

### 8. 不足与局限
*   **重建伪影**：作者承认重建输出中存在固定的模式伪影（Fixed-pattern artifacts），这与 Decoder 的上采样过程有关。
*   **下游验证缺失**：目前仅验证了重建（Inpainting）任务的性能，尚未展示该预训练模型在实际临床任务（如肿瘤分割）中的精度提升。
*   **算力细节不明**：缺乏训练效率和资源消耗的具体数据，难以评估其在普通实验室环境下的可迁移性。

（完）
