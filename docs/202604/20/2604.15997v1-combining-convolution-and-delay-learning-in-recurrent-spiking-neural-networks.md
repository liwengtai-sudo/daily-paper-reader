---
title: Combining Convolution and Delay Learning in Recurrent Spiking Neural Networks
title_zh: 在循环脉冲神经网络中结合卷积与延迟学习
authors: "Lúcio Folly Sanches Zebendo, Eleonora Cicciarella, Michele Rossi"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15997v1"
tags: ["query:dl"]
score: 9.0
evidence: 具有卷积连接的循环脉冲神经网络
tldr: 针对资源受限的边缘系统，本文在循环脉冲神经网络（RSNN）中结合了卷积结构与可学习的轴突延迟机制。通过在DelRec框架基础上引入卷积循环连接，研究提出了一种更高效的架构。在音频分类任务中，该方法在保持高准确率的同时，显著降低了内存占用并提升了推理速度，为高效能SNN设计提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15997v1/fig-001.webp\", \"caption\": \"Table III PERFORMANCE COMPARISON ON SHD AND SSC TEST SETS.\", \"page\": 5, \"index\": 1, \"width\": 512, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2604.15997v1/fig-002.webp\", \"caption\": \"Figure 2. Design of the convolutional recurrent delay unit. The feedforward path consists of a simple linear projection. The recurrent path replaces the dense weight matrix of [1] with 1D convolution in the neuron dimension with kernel size k = 3, while keeping the learnable delay buffer mechanism.\", \"page\": 4, \"index\": 2, \"width\": 516, \"height\": 265}]"
motivation: 旨在解决现有循环脉冲神经网络在边缘设备上部署时面临的内存占用大和推理速度慢的问题。
method: 在DelRec延迟学习机制的基础上，将全连接循环层替换为卷积循环连接。
result: "在音频分类任务中，该方法减少了约99%的循环参数，推理速度提升了52倍，且保持了原有的准确率。"
conclusion: 结合卷积与延迟学习的循环脉冲神经网络能够以极低的资源消耗实现高性能的实时处理。
---

## 摘要
脉冲神经网络 (SNNs) 作为资源受限边缘系统中传统人工神经网络的替代方案，正迅速获得关注。在这项工作中，我们延续了近期关于循环 SNN 的研究路线，其中轴突延迟与其他网络参数一起在运行时进行学习。最初提出的名为 DelRec 的方法证明了 SNN 中循环延迟学习的优势。在此，我们通过提倡将卷积循环连接与 DelRec 延迟学习机制相结合来对其进行扩展。根据我们在音频分类任务上的测试，这产生了一个精简的架构，具有更小的内存占用（在循环参数数量方面节省了约 99%）和更快的（52倍）推理时间，同时保持了 DelRec 的准确率。我们的代码可在以下网址获取：https://github.com/luciozebendo/delrec_snn/tree/conv_delays

## Abstract
Spiking neural networks (SNNs) are rapidly gaining momentum as an alternative to conventional artificial neural networks in resource constrained edge systems. In this work, we continue a recent research line on recurrent SNNs where axonal delays are learned at runtime along with the other network parameters. The first proposed approach, dubbed DelRec, demonstrated the benefit of recurrent delay learning in SNNs. Here, we extend it by advocating the use of convolutional recurrent connections in conjunction with the DelRec delay learning mechanism. According to our tests on an audio classification task, this leads to a streamlined architecture with smaller memory footprint (around 99% savings in terms of number of recurrent parameters) and a much faster (52x) inference time, while retaining DelRec's accuracy. Our code is available at: https://github.com/luciozebendo/delrec_snn/tree/conv_delays

---

## 论文详细总结（自动生成）

以下是对论文《Combining Convolution and Delay Learning in Recurrent Spiking Neural Networks》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：循环脉冲神经网络（RSNN）在处理具有复杂时间依赖性的序列数据（如语音、生物信号）方面具有潜力。近期研究（如 DelRec）证明，在 RSNN 中引入“可学习的轴突延迟”可以显著增强网络捕捉长程依赖的能力。
*   **核心问题**：现有的 DelRec 架构采用全连接（Dense）循环层，其参数量随神经元数量呈平方级增长（$N^2$）。这导致模型在扩展到更深、更宽的架构时，内存占用巨大且推理速度受限，不利于在资源受限的边缘设备上部署。
*   **研究动机**：利用音频等时序信号在空间/频率上的局部相关性，探索是否可以用轻量化的卷积连接替代冗余的全连接，同时保留延迟学习的优势。

### 2. 方法论
*   **核心思想**：将 **1D 卷积循环连接** 与 **可学习轴突延迟** 机制相结合，提出了一种卷积循环脉冲神经网络（CRSNN）变体。
*   **关键技术细节**：
    *   **卷积替代全连接**：将原本 $N \times N$ 的循环权重矩阵替换为大小为 $k$（实验中 $k=3$）的 1D 卷积核。每个神经元仅与其自身及相邻的两个神经元进行循环交互。
    *   **延迟学习机制**：保留了 DelRec 的核心，为每个神经元分配一个可学习的实数值延迟 $d_j$。
    *   **可微优化**：使用三角形展开函数（Triangular Spread Function）将脉冲的影响分布到相邻的时间步，使得离散的延迟参数变得可微，从而能利用代理梯度（Surrogate Gradient）和反向传播进行优化。
    *   **调度缓冲区**：利用循环缓冲区（Circular Buffer）管理脉冲的异步到达，实现高效的推理计算。

### 3. 实验设计
*   **数据集**：
    *   **SHD (Spiking Heidelberg Digits)**：包含 0-9 数字的语音脉冲数据集（20类）。
    *   **SSC (Spiking Speech Commands)**：包含 35 个语音命令的大型数据集。
*   **Benchmark（基准）**：原始的 **DelRec**（全连接循环延迟学习架构）。
*   **对比方法**：
    *   不同层数（2层 vs 4层）的 DelRec 与本文方法对比。
    *   消融实验：对比“可学习延迟”与“固定延迟（$d=1$ 或取学习后的统计均值/中位数）”。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。
*   **优化工具**：提到了使用 `Ray Tune` 配合 `Optuna` 算法进行超参数搜索（针对 SHD 数据集）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流神经形态音频数据集上进行了验证。
    *   针对 SHD 进行了 9 个随机种子的重复实验，针对 SSC 进行了 3 个随机种子的实验，并给出了均值和标准差。
    *   包含了针对延迟学习必要性的消融实验。
*   **充分性评价**：实验设计较为客观，通过多层架构对比验证了模型的可扩展性。虽然 SSC 上的重复次数较少（3次），但整体统计结果足以支撑其结论。

### 6. 主要结论与发现
*   **极高的参数效率**：通过将全连接改为卷积（$k=3$），循环参数量减少了约 **99.6%**（在 256 个神经元的层中，参数从 65,792 降至 259）。
*   **推理速度大幅提升**：由于计算量骤减，推理时间比原始 DelRec 快了 **52 倍**。
*   **性能保持**：在 SHD 数据集上，准确率（91.51%）与全连接版（91.72%）几乎持平；在 SSC 上虽有约 4% 的下降，但仍保持了较高的竞争力。
*   **延迟学习的必要性**：消融实验显示，若将延迟固定为 1，SHD 的准确率会下降超过 5%，证明了“学习延迟”是捕捉时序特征的关键。

### 7. 优点
*   **极度轻量化**：成功消除了 RSNN 中最耗资源的冗余部分，使模型非常适合部署在神经形态硬件或低功耗边缘芯片上。
*   **良好的扩展性**：卷积结构避免了参数随层宽平方级增长的问题，使得构建更深层的 SNN 成为可能。
*   **生物合理性**：结合了局部连接（类似皮层柱结构）和轴突延迟，更贴近生物神经系统的特性。

### 8. 不足与局限
*   **任务普适性待验证**：该方法依赖于输入信号的局部相关性（如音频频谱的连续性），对于缺乏这种空间局部性的抽象时序数据，卷积连接可能会导致性能大幅下降。
*   **SSC 性能缺口**：在较复杂的 SSC 数据集上，卷积版相比全连接版仍有明显的准确率差距，说明在复杂任务中，局部连接可能丢失了一些全局协同信息。
*   **硬件实现细节**：虽然理论推理速度快，但未在真实的神经形态硬件（如 Loihi 或 Tianjic）上进行实测。

（完）
