---
title: "TTE-CAM: Built-in Class Activation Maps for Test-Time Explainability in Pretrained Black-Box CNNs"
title_zh: TTE-CAM：预训练黑盒 CNN 中用于测试时可解释性的内置类激活映射
authors: "Kerol Djoumessi, Philipp Berens"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26885v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于医学图像分析和临床环境的可解释CNN
tldr: 针对医疗影像分析中CNN模型透明度不足的问题，本文提出TTE-CAM框架。该方法通过将预训练黑盒CNN的分类头替换为基于卷积的结构，并利用原始权重初始化，在不损失预测性能的前提下，将黑盒模型转化为具有内置可解释性的模型。TTE-CAM在保持高精度的同时，提供了比传统事后解释方法更具忠实度的解释结果，为临床应用提供了可靠的决策支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26885v1/fig-001.webp\", \"caption\": \"Figure 1: Qualitative explanations comparison. The first column shows a DR fundus with clinical annotations (green markers) and a pneumonia CXR with groundtruth bounding boxes (green). Columns 2-6 show post-hoc saliency maps; the last column shows TTE-CAM explanations. CXR scores indicate activation precision.\", \"page\": 2, \"index\": 1, \"width\": 897, \"height\": 261}]"
motivation: 现有的CNN解释方法在事后解释的忠实度与内置解释的模型性能之间存在权衡，难以满足高风险临床场景的需求。
method: 通过将预训练CNN的分类层替换为卷积层并利用原权重初始化，在测试阶段将黑盒模型转换为自解释模型。
result: 实验表明TTE-CAM在保留原始模型预测精度的同时，生成的内置解释在定性和定量指标上均优于或等同于主流的事后解释方法。
conclusion: TTE-CAM成功弥合了模型性能与可解释性之间的鸿沟，为预训练黑盒模型在医疗领域的安全应用提供了有效的测试时解释方案。
---

## 摘要
卷积神经网络（CNN）在医学图像分析中取得了最先进的性能，但其不透明性限制了其在关键临床场景中的应用。现有方法面临着一个根本性的权衡：事后（post-hoc）方法提供的是不忠实的近似解释，而固有可解释架构虽然具有忠实性，但往往会牺牲预测性能。我们引入了 TTE-CAM，这是一个测试时框架，通过使用基于卷积的分类头替换（由原始权重初始化）将预训练的黑盒 CNN 转换为自解释模型，从而弥补了这一差距。所得模型在保留黑盒预测性能的同时，提供了在定性和定量上均可与事后方法相媲美的内置忠实解释。代码可在 https://github.com/kdjoumessi/Test-Time-Explainability 获取。

## Abstract
Convolutional neural networks (CNNs) achieve state-of-the-art performance in medical image analysis yet remain opaque, limiting adoption in high-stakes clinical settings. Existing approaches face a fundamental trade-off: post-hoc methods provide unfaithful approximate explanations, while inherently interpretable architectures are faithful but often sacrifice predictive performance. We introduce TTE-CAM, a test-time framework that bridges this gap by converting pretrained black-box CNNs into self-explainable models via a convolution-based replacement of their classification head, initialized from the original weights. The resulting model preserves black-box predictive performance while delivering built-in faithful explanations competitive with post-hoc methods, both qualitatively and quantitatively. The code is available at https://github.com/kdjoumessi/Test-Time-Explainability

---

## 论文详细总结（自动生成）

这是一份关于论文《TTE-CAM: Built-in Class Activation Maps for Test-Time Explainability in Pretrained Black-Box CNNs》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：卷积神经网络（CNN）在医疗影像分析中表现卓越，但其“黑盒”性质限制了在临床高风险环境中的应用。
*   **核心问题**：现有的可解释性方法存在**“忠实度-性能”权衡**。事后解释（Post-hoc）方法（如 Grad-CAM）生成的显著图并不直接驱动模型输出，可能产生误导；而固有可解释架构虽然忠实，但通常需要复杂的重新训练，且往往会牺牲预测精度。
*   **整体含义**：本文提出 **TTE-CAM** 框架，旨在不进行重新训练、不损失预测精度的前提下，在测试阶段将预训练的黑盒 CNN 转换为具有“内置”解释能力的自解释模型。

### 2. 方法论
*   **核心思想**：利用全连接层（FCL）与 $1 \times 1$ 卷积层在数学上的等价性，重构预训练模型的分类头。
*   **关键技术细节**：
    1.  **架构重构**：移除预训练模型末端的全局平均池化（GAP）层。
    2.  **卷积替换**：将原有的全连接层替换为一个 $1 \times 1$ 的卷积层，其卷积核数量等于类别数 $C$。
    3.  **权重迁移**：直接将预训练好的全连接层权重初始化给 $1 \times 1$ 卷积核，无需任何微调或重新训练。
    4.  **内置生成**：该卷积层直接输出“内置类激活映射（Built-in CAMs）”。
    5.  **推理流程**：模型对这些内置 CAMs 进行空间平均（Spatial Averaging）以计算类得分，再通过 Softmax 得到最终预测。
*   **忠实性保证**：由于最终预测是直接从这些激活图中聚合而来的，因此解释与决策过程在机械逻辑上是完全一致的（Mechanistic Faithfulness）。

### 3. 实验设计
*   **数据集**：
    *   **Kaggle DR Dataset**：用于糖尿病视网膜病变（DR）的二分类。
    *   **RSNA Chest X-Ray (CXR)**：用于肺炎检测。
*   **Benchmark 模型**：ResNet-50。
*   **对比方法**：
    *   **无梯度类**：CAM, ScoreCAM。
    *   **基于梯度类**：GradCAM, LayerCAM。
    *   **反向传播类**：Integrated Gradients (IG)。
*   **评估指标**：
    *   **预测性能**：准确率（Acc）、曲线下面积（AUC）。
    *   **解释质量**：Top-k 灵敏度（Top-k sensitivity）、Top-k 定位精度（Top-k localization）、激活精度（Activation precision）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **资源消耗分析**：由于 TTE-CAM 是一种“测试时（Test-time）”方法，它直接复用预训练权重而无需重新训练，因此其算力需求极低。在推理阶段，它仅需一次前向传播，比需要多次前向传播的方法（如 ScoreCAM）或需要计算梯度的法（如 GradCAM）更节省资源。

### 5. 实验数量与充分性
*   **实验规模**：在两个代表性的医疗影像任务上进行了验证，涵盖了眼底图和胸部 X 光片。
*   **对比充分性**：对比了五种主流的事后解释方法，涵盖了目前最常用的技术路径。
*   **客观性**：使用了定量指标（如 Top-k 灵敏度）来评估解释的忠实度，而不仅仅依赖于主观的可视化展示。不过，DR 数据集的定量评估仅基于 65 张有临床标注的图像，样本量相对较小。

### 6. 主要结论与发现
*   **精度零损耗**：TTE-CAM 完美保留了原始黑盒模型的预测性能（在 DR 任务上 Acc=0.899，在 CXR 任务上 Acc=0.953）。
*   **解释有效性**：TTE-CAM 生成的内置解释在定性和定量上均与主流事后方法（如 CAM、GradCAM）具有竞争力。
*   **效率优势**：相比于需要为每个类别进行前向/后向传播的方法，TTE-CAM 在单次前向传播中即可完成预测与解释生成。

### 7. 优点
*   **即插即用**：无需重新训练模型，适合对已部署的医疗 AI 模型进行快速升级。
*   **机械忠实性**：解决了事后解释方法中“解释与预测脱节”的根本问题。
*   **高性能保留**：消除了传统可解释模型往往伴随的精度下降风险。

### 8. 不足与局限
*   **架构适用性受限**：该方法要求特征图通道数必须与分类器输入尺寸匹配，适用于 ResNet 和 DenseNet，但不适用于 VGG 等具有多层全连接层的架构。
*   **分辨率限制**：由于依赖于 CNN 深层的低分辨率特征图，生成的解释图较为粗糙，对于细小病灶（如 DR 中的微动脉瘤）的精确定位能力有限。
*   **模型覆盖面**：目前仅在 CNN 上进行了验证，尚未探讨在 Vision Transformers (ViT) 等新型架构上的适用性。

（完）
