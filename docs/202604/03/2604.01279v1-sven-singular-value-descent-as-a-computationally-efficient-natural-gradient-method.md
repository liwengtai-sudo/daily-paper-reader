---
title: "Sven: Singular Value Descent as a Computationally Efficient Natural Gradient Method"
title_zh: Sven：作为计算高效自然梯度法的奇异值下降
authors: "Samuel Bright-Thonney, Thomas R. Harvey, Andre Lukas, Jesse Thaler"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01279v1"
tags: ["query:dl"]
score: 9.0
evidence: 利用奇异值分解的神经网络优化算法
tldr: Sven 是一种新型神经网络优化算法，它将损失函数分解为单个数据点的残差条件，利用损失雅可比矩阵的截断奇异值分解（SVD）来近似 Moore-Penrose 伪逆，从而寻找最小范数参数更新。该方法将自然梯度法扩展到超参数化领域，计算开销仅为 SGD 的 k 倍，远低于传统自然梯度法。在回归任务中，其收敛速度和精度显著优于 Adam，且比 LBFGS 更具时间效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01279v1/fig-001.webp\", \"caption\": \"Figure 1: Validation loss as a function of epoch (top) and wall time (bottom) for 1D regression, random polynomial regression, and MNIST classification, comparing Sven against SGD, RMSProp, Adam, and LBFGS. In all tasks, Sven converges faster per epoch and to a lower final loss than all standard first-order methods, remaining competitive with LBFGS despite significantly lower wall-time cost.\", \"page\": 7, \"index\": 1, \"width\": 798, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.01279v1/fig-002.webp\", \"caption\": \"Figure 2: Training loss vs. epoch for MNIST with varying k (top) and varying rtol (bottom right). The bottom left panel shows the evolution of the singular value spectrum across epochs for all three tasks, illustrating how the rank structure of the loss Jacobian changes during training.\", \"page\": 8, \"index\": 2, \"width\": 798, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2604.01279v1/fig-003.webp\", \"caption\": \"Figure 3: Validation loss curves for the toy 1D (left) and polynomial (right) datasets using the micro-batching (top) and parameter batching (bottom) approaches to save on computational cost. Each plot shows the mean trajectory with ±1σ error bands computed over ten runs with different model initializations.\", \"page\": 17, \"index\": 3, \"width\": 806, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2604.01279v1/fig-004.webp\", \"caption\": \"Figure 4: Left, center: training and validation loss trajectories comparing Sven to baseline optimizers on MNIST classification with the cross-entropy loss. Right: Singular value spectra at uniformly sampled training batches comparing the label regression (blue) and cross-entropy (orange) objectives.\", \"page\": 18, \"index\": 4, \"width\": 796, \"height\": 189}]"
motivation: 传统自然梯度法计算复杂度过高且忽略了单样本的约束信息，难以应用于大规模神经网络优化。
method: 通过截断 SVD 近似损失雅可比矩阵的伪逆，以计算能同时满足所有样本残差条件的最小范数参数更新。
result: 在回归任务中，Sven 的收敛速度和最终损失优于 Adam 等一阶方法，且在运行时间上比 LBFGS 更具优势。
conclusion: Sven 是一种高效的广义自然梯度法，特别适用于科学计算等损失函数可分解为多个条件的场景。
---

## 摘要
我们引入了 Sven（奇异值下降，Singular Value dEsceNt），这是一种针对神经网络的新型优化算法。它利用了损失函数可自然分解为单个数据点之和的特性，而不是在计算参数更新前将总损失简化为单个标量。Sven 将每个数据点的残差视为需要同时满足的独立条件，并利用损失雅可比矩阵的 Moore-Penrose 伪逆来寻找能同时最好地满足所有条件的最小范数参数更新。在实践中，该伪逆通过截断奇异值分解进行近似，仅保留 $k$ 个最显著的方向，其计算开销仅为随机梯度下降的 $k$ 倍。相比之下，传统自然梯度法的计算量随参数数量的平方增长。我们证明了 Sven 可以被理解为推广到过参数化情形下的自然梯度法，并在欠参数化极限下退化为自然梯度下降。在回归任务中，Sven 的表现显著优于包括 Adam 在内的标准一阶方法，收敛速度更快且最终损失更低，同时在耗时仅为 LBFGS 一小部分的情况下保持了与之相当的竞争力。我们讨论了扩展该算法的主要挑战（即内存开销），并提出了缓解策略。除了标准的机器学习基准测试外，我们预计 Sven 将在科学计算场景中得到自然的应用，因为这些场景中的自定义损失函数通常会分解为多个条件。

## Abstract
We introduce Sven (Singular Value dEsceNt), a new optimization algorithm for neural networks that exploits the natural decomposition of loss functions into a sum over individual data points, rather than reducing the full loss to a single scalar before computing a parameter update. Sven treats each data point's residual as a separate condition to be satisfied simultaneously, using the Moore-Penrose pseudoinverse of the loss Jacobian to find the minimum-norm parameter update that best satisfies all conditions at once. In practice, this pseudoinverse is approximated via a truncated singular value decomposition, retaining only the $k$ most significant directions and incurring a computational overhead of only a factor of $k$ relative to stochastic gradient descent. This is in comparison to traditional natural gradient methods, which scale as the square of the number of parameters. We show that Sven can be understood as a natural gradient method generalized to the over-parametrized regime, recovering natural gradient descent in the under-parametrized limit. On regression tasks, Sven significantly outperforms standard first-order methods including Adam, converging faster and to a lower final loss, while remaining competitive with LBFGS at a fraction of the wall-time cost. We discuss the primary challenge to scaling, namely memory overhead, and propose mitigation strategies. Beyond standard machine learning benchmarks, we anticipate that Sven will find natural application in scientific computing settings where custom loss functions decompose into several conditions.

---

## 论文详细总结（自动生成）

以下是对论文《Sven: Singular Value Descent as a Computationally Efficient Natural Gradient Method》的结构化深入分析：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的深度学习优化器（如 SGD、Adam）通常将批次内所有数据点的损失求和，简化为一个标量后再计算梯度。这种做法丢弃了每个数据点残差所包含的结构化信息。
*   **核心问题**：自然梯度法（Natural Gradient Descent, NGD）虽然在理论上具有优越的收敛性，但其计算开销巨大（通常随参数数量 $N$ 的平方或立方增长），且在现代神经网络的“过参数化”（参数量远大于数据量）状态下难以直接应用。
*   **整体含义**：Sven 旨在开发一种能够利用单样本残差信息、在过参数化状态下依然有效，且计算开销可控（仅为 SGD 的 $k$ 倍）的近似自然梯度优化器。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将优化问题视为一个线性方程组求解问题。对于每个数据点 $\alpha$，其残差为 $R_\alpha(\theta)$。Sven 试图寻找一个参数更新 $\delta\theta$，使得所有数据点的残差同时趋近于零，即求解 $R_\alpha(\theta_0) + \sum_i M_{\alpha i} \delta\theta_i = 0$。
*   **关键技术细节**：
    *   **雅可比矩阵 (Jacobian)**：定义 $M_{\alpha i} = \partial R_\alpha / \partial \theta_i$，这是一个 $|D| \times N$ 的矩阵（$|D|$ 为样本数，$N$ 为参数量）。
    *   **Moore-Penrose 伪逆**：更新公式为 $\delta\theta = -\eta M^+ R$。在过参数化 regime 下，这会给出满足残差最小化的“最小范数更新”。
    *   **截断奇异值分解 (Truncated SVD)**：为了降低计算复杂度，Sven 不计算完整的伪逆，而是通过随机投影等方法进行截断 SVD，仅保留前 $k$ 个最大的奇异值。
    *   **超参数控制**：引入 $k$（保留的奇异值数量）和 $rtol$（相对于最大奇异值的截断比例）来控制近似精度和正则化。
*   **算法流程**：计算雅可比矩阵 $M$ -> 对 $M$ 进行截断 SVD -> 计算近似伪逆 $M^+$ -> 应用更新步长 $\delta\theta$。

### 3. 实验设计
*   **数据集/场景**：
    1.  **1D 回归**：拟合复杂的正弦衰减函数。
    2.  **随机多项式回归**：在 $\mathbb{R}^6$ 空间拟合四次多项式。
    3.  **MNIST 分类**：将其视为标签回归任务（Label Regression）和标准的交叉熵分类任务。
*   **对比方法 (Baselines)**：
    *   一阶优化器：SGD、PolyakSGD、RMSprop、Adam。
    *   二阶/拟牛顿法：LBFGS（Limited-memory BFGS）。
*   **评估指标**：训练/验证损失随 Epoch 的变化、随 Wall-time（实际运行时间）的变化。

### 4. 资源与算力
*   **明确说明**：论文**未明确列出**具体的 GPU 型号或总算力消耗（如 TFLOPS）。
*   **间接信息**：作者提到 Sven 的计算开销（Operations）是 SGD 的 $k$ 倍。在实验中，$k$ 通常取批次大小 $B$ 的一部分（如 $B/2$）。实验是在小型 MLP 网络上进行的，强调了 Sven 在 Wall-time 上比 LBFGS 快一个数量级，尽管单次迭代比 Adam 慢。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单 1D 到高维多项式再到图像分类的三个代表性任务。
*   **消融实验**：对关键超参数 $k$ 和 $rtol$ 进行了详细的扫描，展示了奇异值谱（Singular Value Spectra）在训练过程中的演变。
*   **充分性与公平性**：
    *   **公平性**：所有优化器使用相同的模型初始化和数据加载顺序。
    *   **充分性**：对所有对比优化器都进行了详尽的超参数搜索（Grid Search），确保对比的是各优化器的最佳性能。
    *   **局限性**：实验目前集中在小型模型（MLP）和回归任务上，尚未在大规模 Transformer 或 CNN 上验证。

### 6. 主要结论与发现
*   **性能优越**：在回归任务中，Sven 的收敛速度和最终损失显著优于 Adam 等一阶方法，且能达到接近 LBFGS 的精度。
*   **时间效率**：虽然单步计算比 Adam 慢，但由于收敛所需的 Epoch 极少，总 Wall-time 具有竞争力。
*   **理论联系**：证明了 Sven 在欠参数化极限下等价于自然梯度下降，是 NGD 在现代深度学习体制下的有效推广。
*   **奇异值谱发现**：不同任务的奇异值谱差异巨大。1D 回归谱衰减极快，而 MNIST 谱相对平坦，这解释了为什么 Sven 在回归任务上提升更明显。

### 7. 优点（亮点）
*   **理论完备性**：从泛函分析和微分几何角度为算法提供了坚实的数学支撑（见附录 A/B）。
*   **全局视角**：打破了“损失求和”的传统思维，通过雅可比矩阵捕捉样本间的几何约束。
*   **效率平衡**：通过截断 SVD 巧妙地将二阶信息的利用降低到可接受的线性开销。

### 8. 不足与局限
*   **内存瓶颈**：这是该方法最大的挑战。存储每个样本的梯度（Per-sample gradients）以构建雅可比矩阵需要巨大的内存，限制了其在大批次或超大模型上的应用。
*   **分类任务表现平平**：在 MNIST 交叉熵实验中，Sven 仅能匹配 Adam 的表现，未能展现出在回归任务中的压倒性优势。
*   **扩展性待验证**：尚未在主流的大规模视觉或语言模型基准上进行测试。
*   **超参数敏感性**：$k$ 和 $rtol$ 的选择对性能影响较大，且不同任务的最优设置不尽相同。

（完）
