---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中太字节级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于TB级生物数据分析的小批量加载器
tldr: 随着生物数据集规模突破内存限制，数据加载成为AI训练的主要瓶颈。本文推出annbatch，这是一款原生支持anndata格式的小批量加载器，支持直接在磁盘数据集上进行核外训练。它在单细胞、显微镜和全基因组测序等任务中将加载吞吐量提升了一个数量级，将训练时间从数天缩短至数小时，且完美兼容scverse生态，为大规模生物AI提供了实用的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01949v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-003.webp\", \"caption\": \"\", \"page\": 18, \"index\": 3, \"width\": 1280, \"height\": 960}]"
motivation: 生物数据集规模已超出系统内存，导致数据访问而非模型计算成为机器学习训练的主要瓶颈。
method: 开发了原生于anndata的annbatch加载器，支持直接对存储在磁盘上的异构生物数据进行高效的小批量核外训练。
result: 在多项生物学基准测试中，annbatch将加载吞吐量提升了高达一个数量级，显著缩短了训练周期。
conclusion: annbatch为大规模生物AI训练提供了高效的数据加载方案，在保持与标准生物数据格式兼容的同时实现了TB级数据的可扩展性。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及在既定计算生态系统内的下游分析。在此，我们介绍了 annbatch，这是一个 anndata 原生的迷你批次（mini-batch）加载器，能够直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序的基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短到几小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能（AI）建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够使用日益庞大且多样化的数据集。

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

这是一份关于论文《annbatch unlocks terabyte-scale training of biological data in anndata》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着生物学数据集（如单细胞测序、高分辨率成像）规模迈向太字节（TB）级别，数据量已远超服务器内存容量。在训练生物 AI 模型时，瓶颈已从“模型计算”转移到了“数据加载（I/O）”。现有的高性能加载框架（如 NVIDIA Merlin, WebDataset）通常需要转换数据格式，导致其与生物学界主流的 `anndata` 格式及 `scverse` 生态系统脱节。论文的核心目标是开发一种既能保持生态兼容性，又能实现磁盘直接读取（核外训练）的高性能数据加载器，解决大规模生物数据训练的效率问题。

### 2. 核心方法论
`annbatch` 是一个原生集成于 `anndata` 的端到端高吞吐量数据加载框架，其核心思想包括：
*   **预洗牌（Pre-shuffler）**：针对磁盘存储的 `anndata` 文件进行离线预洗牌。它通过随机读取连续数据块（Chunks）并在内存中打乱后重新写入 Zarr 格式，解决了纯随机磁盘访问极慢的问题，同时保证了训练所需的随机性。
*   **高性能加载器（Data Loader）**：
    *   **连续磁盘访问**：利用预洗牌后的布局，一次性读取大块连续观测值，最大化顺序 I/O 性能。
    *   **Zarr 后端优化**：基于 Rust 开发的 `zarrs-python` 桥接库，支持异步 I/O。
    *   **底层技术集成**：采用直接 I/O（Direct I/O，绕过 OS 缓存）、固定内存（Pinned Memory）读取以及 GPU 加速的内存内洗牌。
*   **生态兼容性**：输入和输出均为标准的 `anndata` 文件，无需改变生物学家的工作流。

### 3. 实验设计与 Benchmark
论文在三种代表性的生物学场景下进行了基准测试：
*   **单细胞转录组学**：使用 **Tahoe-100M** 数据集（约 1 亿个细胞）。对比对象包括 `scDataset` 和 `MappedCollection`。
*   **单细胞显微成像**：处理 128x128x5 尺寸的裁剪图像。对比对象为标准的 `h5ad` 随机索引读取。
*   **全基因组测序（WGS）**：使用 **1000 Genomes GRCh38** 数据集并扩增至 50 万样本。对比了稀疏格式下的 `scDataset`、`MappedCollection` 以及稠密格式下的 `numpy memmap`。
*   **分布式训练**：在多 GPU 环境下测试了视觉 Transformer（ViT）和 BERT 风格模型的吞吐量饱和度。

### 4. 资源与算力
论文详细记录了测试环境：
*   **云端环境**：AWS `ml.g4dn.4xlarge` 实例（配备标准 EBS 磁盘，带宽限制为 125MB/s）。
*   **高性能计算（HPC）**：德国 de.NBI 节点（16 vCPUs, 64GB RAM, T4 GPU）以及 Leibniz Rechenzentrum (LRZ) 的 AI 分区。
*   **顶级算力测试**：使用 NVIDIA **HGX-H100** 单元进行分布式训练吞吐量评估。
*   **训练时长示例**：在 Tahoe-100M 上，`annbatch` 跑完一个 Epoch 仅需约 17 分钟，而对比方法 `MappedCollection` 需要 21 小时。

### 5. 实验数量与充分性
实验设计非常充分且具有代表性：
*   **多维度覆盖**：涵盖了“大样本量”（1 亿细胞）、“高特征维度”（9000 万 SNPs）和“大单样本载荷”（图像）三种极端 I/O 场景。
*   **客观性**：作者特意在有磁盘限速的 AWS 环境和无限制的 HPC 环境下分别测试，证明了 `annbatch` 的性能提升并非依赖特定高端硬件，而是算法逻辑的优化。
*   **消融/对比实验**：对比了 Zarr 与 h5ad 格式的影响、预洗牌的开销摊销、以及不同 Batch Size 对 GPU 利用率的影响。

### 6. 主要结论与发现
*   **吞吐量飞跃**：在单细胞数据上，`annbatch` 达到约 35,000 样本/秒，比现有主流方法快 **20-40 倍**。
*   **训练时间缩短**：将原本需要数天的训练任务缩短至数小时。
*   **开销可摊销**：虽然预洗牌需要约 5 小时的额外 CPU 时间，但这一成本在第一个训练 Epoch 内就能通过节省的 GPU 时间完全抵消。
*   **分布式友好**：`annbatch` 的高吞吐量能够有效填充 H100 等顶级 GPU 的计算能力，避免了分布式训练中的 I/O 饥饿。

### 7. 优点与亮点
*   **无缝集成**：这是首个在不破坏 `anndata` 结构的前提下实现工业级加载性能的工具。
*   **技术栈先进**：引入了 Rust 编写的 Zarr 引擎和 Direct I/O 等底层优化技术。
*   **通用性强**：不仅限于单细胞，还证明了在基因组学和影像学中的巨大潜力。
*   **开源贡献**：提供了完整的工具链（预洗牌器+加载器）和详细的文档。

### 8. 不足与局限
*   **采样策略限制**：由于采用连续块读取，目前难以实现复杂的加权采样（Weighted Sampling），需要通过特定的磁盘写入布局来间接解决。
*   **多模态支持**：目前尚未直接支持多模态数据的同步高效加载（虽计划在未来版本中加入）。
*   **预洗牌依赖**：性能高度依赖于预洗牌步骤，对于需要频繁变动数据集组合的场景，预处理开销可能会增加。
*   **网络瓶颈**：虽然支持异步云端训练，但作者指出网络带宽仍可能成为比磁盘 I/O 更严重的瓶颈。

（完）
