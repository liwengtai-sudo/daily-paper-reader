---
title: Automated Prostate Gland Segmentation in MRI Using nnU-Net
title_zh: 基于 nnU-Net 的 MRI 前列腺自动分割
authors: "Pablo Rodriguez-Belenguer, Gloria Ribas, Javier Aquerreta Escribano, Rafael Moreno-Calatayud, Leonor Cerda-Alberich, Luis Marti-Bonmati"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01964v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 深度学习用于MRI前列腺腺体分割
tldr: 本研究针对前列腺多参数MRI（mpMRI）分割中人工标注耗时且通用工具精度不足的问题，提出了一种基于nnU-Net v2框架的深度学习方法。该方法融合了T2W、DWI和ADC多模态数据，在PI-CAI数据集上进行训练。实验结果显示，该模型在交叉验证中Dice得分达0.96，在外部验证集上达0.82，显著优于通用分割工具，为临床研究提供了可靠的自动化工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01964v1/fig-001.webp\", \"caption\": \"Table 1. Gland segmentation performance in cross-validation (PI-CAI).\", \"page\": 5, \"index\": 1, \"width\": 804, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2604.01964v1/fig-002.webp\", \"caption\": \"Table 2. Results over test set\", \"page\": 6, \"index\": 2, \"width\": 677, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2604.01964v1/fig-003.webp\", \"caption\": \"Figure 1. Qualitative comparison of prostate gland segmentation on the Hospital La Fe cohort. Each row corresponds to a different patient. From left to right: T2-weighted image, ground truth, proposed model, and TotalSegmentator. The slice was selected based on the ground truth mask (maximum voxel area). Dice scores are reported for the predicted segmentations.\", \"page\": 7, \"index\": 3, \"width\": 889, \"height\": 1146}]"
motivation: 旨在解决前列腺MRI人工分割效率低、变异性大以及通用分割工具在特定任务中准确性不足的问题。
method: 基于nnU-Net v2框架，利用T2W、DWI和ADC等多模态mpMRI数据构建并训练了针对前列腺全腺体的深度学习分割模型。
result: 模型在内部交叉验证中Dice得分达0.96，在外部独立验证集中达0.82，性能远超通用分割工具TotalSegmentator。
conclusion: 任务特定的多模态分割策略对于提高前列腺分割精度至关重要，且该容器化模型具备良好的临床应用潜力。
---

## 摘要
在多参数磁共振成像（mpMRI）中对前列腺进行准确分割是广泛临床和研究应用的基础步骤，包括图像配准、体积估算和放射组学分析。然而，手动勾画耗时且存在观察者间差异，而通用分割工具往往无法为前列腺特定任务提供足够的准确性。在这项工作中，我们提出了一种基于深度学习的专用方法，利用 nnU-Net v2 框架进行前列腺自动分割。该模型利用多模态 mpMRI 数据，包括 T2 加权成像、扩散加权成像（DWI）和表观扩散系数（ADC）图，以利用互补的组织信息。模型在来自 PI-CAI 数据集的 981 例全腺体标注病例上进行训练，并通过五折交叉验证以及在来自 La Fe 医院的 54 名患者独立队列上的外部验证来评估模型性能。所提出的模型在交叉验证中达到了 0.96 ± 0.00 的平均 Dice 分数，在外部测试集上达到了 0.82，尽管存在领域偏移，仍展现出强大的泛化能力。相比之下，通用方法（TotalSegmentator）的表现显著较低，Dice 分数为 0.15，这主要是由于对腺体的欠分割造成的。这些结果强调了特定任务、多模态分割策略的重要性，并证明了所提方法可靠集成到临床研究工作流中的潜力。为了促进可重复性和部署，该模型已完全容器化，并作为开箱即用的推理工具提供。

## Abstract
Accurate segmentation of the prostate gland in multiparametric MRI (mpMRI) is a fundamental step for a wide range of clinical and research applications, including image registration, volume estimation, and radiomic analysis. However, manual delineation is time-consuming and subject to inter-observer variability, while general-purpose segmentation tools often fail to provide sufficient accuracy for prostate-specific tasks. In this work, we propose a dedicated deep learning-based approach for automatic prostate gland segmentation using the nnU-Net v2 framework. The model leverages multimodal mpMRI data, including T2-weighted imaging, diffusion-weighted imaging (DWI), and apparent diffusion coefficient (ADC) maps, to exploit complementary tissue information. Training was performed on 981 cases from the PI-CAI dataset using whole-gland annotations, and model performance was assessed through 5-fold cross-validation and external validation on an independent cohort of 54 patients from Hospital La Fe. The proposed model achieved a mean Dice score of 0.96 +/- 0.00 in cross-validation and 0.82 on the external test set, demonstrating strong generalization despite domain shift. In comparison, a general-purpose approach (TotalSegmentator) showed substantially lower performance, with a Dice score of 0.15, primarily due to under-segmentation of the gland. These results highlight the importance of task-specific, multimodal segmentation strategies and demonstrate the potential of the proposed approach for reliable integration into clinical research workflows. To facilitate reproducibility and deployment, the model has been fully containerized and is available as a ready-to-use inference tool.

---

## 论文详细总结（自动生成）

以下是对论文《Automated Prostate Gland Segmentation in MRI Using nnU-Net》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
前列腺癌是全球男性最常见的恶性肿瘤之一。多参数磁共振成像（mpMRI）已成为其检测和定位的核心手段。然而，准确的前列腺腺体分割是体积估算、放射组学分析和图像配准等后续任务的关键前提。目前面临的主要挑战包括：
*   **人工成本高**：手动勾画耗时且存在显著的观察者间差异。
*   **通用工具精度不足**：如 TotalSegmentator 等通用多器官分割工具在处理前列腺这种小尺寸、边界模糊的结构时，往往准确性较差。
*   **信息利用不充分**：现有工具多依赖单序列（如仅 T2W），未能充分利用 mpMRI 提供的互补组织信息。

### 2. 论文提出的方法论
该研究提出了一种专门针对前列腺分割的深度学习框架：
*   **核心框架**：基于 **nnU-Net v2**，配置为 **3D 全分辨率模式**，并采用**残差编码器（Residual Encoder）**作为骨干网络，以增强特征提取能力。
*   **多模态融合策略**：模型同时输入三种 MRI 序列：**T2 加权成像（T2W）**、**扩散加权成像（DWI）**和**表观扩散系数（ADC）图**。这种多通道输入允许模型学习不同序列间的空间和组织学关联。
*   **训练细节**：模型从零开始训练，训练周期为 250 个 epoch。损失函数采用 Dice 损失与交叉熵（Cross-Entropy）损失的等权重组合。
*   **预处理流程**：包括空间重采样（以 T2W 为参考）、各向异性扩散滤波去噪、N4 偏置场校正以及序列间的刚性/仿射配准。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 PI-CAI 挑战赛的公开数据集，包含 **981 例** mpMRI 研究。
    *   **外部测试集**：来自 La Fe 医院的独立队列，包含 **54 例** 经过专家验证的病例，用于评估模型在真实临床环境下的泛化能力。
*   **Benchmark（基准对比）**：
    *   对比了目前主流的通用分割工具 **TotalSegmentator**。
*   **评价指标**：Dice 系数（DSC）、交并比（IoU）、豪斯多夫距离（HD）、精确率（Precision）和灵敏度（Sensitivity）。

### 4. 资源与算力
*   **模型规模**：文中提到生成的模型大小约为 **80 GB**。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。仅提到训练了 250 个 epoch 并采用了 5 折交叉验证方案。

### 5. 实验数量与充分性
*   **实验设置**：研究进行了 5 折交叉验证（内部验证）和独立外部验证。
*   **充分性评估**：
    *   **内部验证充分**：981 例样本在医学影像分割领域属于较大规模，5 折交叉验证确保了结果的稳定性。
    *   **外部验证客观**：使用了完全独立的医院数据，能够真实反映“领域偏移（Domain Shift）”对模型的影响。
    *   **对比公平性**：与 TotalSegmentator 的对比揭示了通用模型在特定器官上的局限性。但实验**缺乏与其他专用前列腺分割模型**（如基于其他架构的 SOTA 模型）的横向对比。

### 6. 论文的主要结论与发现
*   **性能卓越**：在 PI-CAI 内部交叉验证中，平均 Dice 达到 **0.96**；在外部测试集中，Dice 为 **0.82**。
*   **显著优于通用工具**：TotalSegmentator 在外部集上的 Dice 仅为 **0.15**，表现出严重的欠分割（灵敏度仅 0.10），证明通用工具无法直接用于高精度前列腺分析。
*   **泛化能力与挑战**：尽管存在领域偏移导致性能从 0.96 下降到 0.82，但模型在形状和空间范围上仍能与专家标注高度匹配。
*   **多模态优势**：通过融合 T2W、DWI 和 ADC，模型能够更稳健地识别腺体边界。

### 7. 优点
*   **任务特定化**：证明了针对特定解剖结构开发专用模型比使用通用工具更具临床价值。
*   **端到端自动化**：提供了从预处理到推理的完整流水线。
*   **高可重复性**：模型已完全**容器化（Docker）**并公开发布，极大地方便了其他研究者的部署和验证。
*   **临床实用性**：在外部异构数据集（不同厂商设备、不同场强）上表现稳健。

### 8. 不足与局限
*   **领域偏移影响**：外部验证性能下降明显（0.96 -> 0.82），说明模型对不同中心扫描协议的敏感性仍需进一步优化。
*   **外部样本量较小**：外部验证仅使用了 54 例样本，可能无法完全覆盖所有病理类型或极端解剖变异。
*   **对比范围有限**：未与近年来其他针对前列腺分割的深度学习改进算法进行对比。
*   **计算资源需求**：80 GB 的模型体积对于某些资源受限的临床环境可能存在部署压力。

（完）
