---
title: "NEMESIS: Noise-suppressed Efficient MAE with Enhanced Superpatch Integration Strategy"
title_zh: NEMESIS：具有增强型超块集成策略的噪声抑制高效 MAE
authors: "Kyeonghun Kim, Hyeonseok Jung, Youngung Han, Hyunsu Go, Eunseob Choi, Seongbin Park, Junsu Lim, Jiwon Yang, Sumin Lee, Insung Hwang, Ken Ying-Kai Liao, Nam-Joon Kim"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01612v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于体积CT影像诊断的自监督学习
tldr: NEMESIS是一个针对3D CT影像的高效自监督学习框架。针对全卷Transformer内存开销大和CT各向异性结构难以捕捉的问题，该方法采用128x128x128的超块策略，并引入噪声增强重建、双重掩码解剖Transformer块（MATB）以及跨尺度上下文聚合令牌（NT）。实验表明，NEMESIS在BTCV多器官分类任务上表现优异，显著降低了计算成本，且在低标注场景下具有极强的鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01612v1/fig-001.webp\", \"caption\": \"Fig. 1. The overall architecture of the NEMESIS backbone. The input superpatch is divided into volume patches and processed through the patch embedding module. The framework utilizes dual-masking and MATB-based encoders and decoders to learn high-level anatomical representations in the latent space.\", \"page\": 2, \"index\": 1, \"width\": 965, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2604.01612v1/fig-002.webp\", \"caption\": \"Fig. 3. Detailed architecture of the 3D Adaptive Patch Embedding module, integrating Linear Projection (LP) and Superpatch-Embedder (SE) pathways.\", \"page\": 2, \"index\": 2, \"width\": 529, \"height\": 297}]"
motivation: 解决3D CT影像自监督学习中全卷Transformer内存消耗过高以及传统掩码策略无法有效捕捉CT各向异性空间结构的问题。
method: 提出基于超块的MAE框架，结合噪声增强重建任务、执行平面和轴向双重掩码的MATB模块，以及用于跨尺度特征聚合的NEMESIS令牌。
result: 在BTCV基准测试中，线性分类AUROC达到0.9633，超越了全微调的SuPreM，且计算量从985.8 GFLOPs大幅降至31.0 GFLOPs。
conclusion: NEMESIS通过高效的超块设计和针对性的掩码策略，为3D医学影像提供了一个可扩展、低成本且高性能的自监督学习基础模型。
---

## 摘要
体层 CT 成像是临床诊断的基础，但 3D 体数据的标注既昂贵又耗时，这促使了利用无标签数据进行自监督学习 (SSL) 的研究。然而，由于全卷 Transformer 的高显存开销以及 CT 数据各向异性的空间结构（传统掩码策略难以有效捕捉），将 SSL 应用于 3D CT 仍具挑战性。我们提出了 NEMESIS，这是一个在局部 128x128x128 超块 (superpatches) 上运行的掩码自编码器 (MAE) 框架，在保留解剖细节的同时实现了显存高效的训练。NEMESIS 引入了三个核心组件：(i) 作为预训练任务的噪声增强重建；(ii) 通过并行的平面级和轴向级 Token 移除执行双重掩码的掩码解剖 Transformer 块 (MATB)；以及 (iii) 用于跨尺度上下文聚合的 NEMESIS Token (NT)。在 BTCV 多器官分类基准测试中，采用冻结主干网络和线性分类器的 NEMESIS 达到了 0.9633 的平均 AUROC，超过了全微调的 SuPreM (0.9493) 和 VoCo (0.9387)。在仅使用 10% 标注数据的低标签场景下，它仍保持了 0.9075 的 AUROC，展现出强大的标签效率。此外，基于超块的设计将单次前向传播的计算成本从全卷基准的 985.8 GFLOPs 降低至 31.0 GFLOPs，为 3D 医学影像提供了可扩展且稳健的基础。

## Abstract
Volumetric CT imaging is essential for clinical diagnosis, yet annotating 3D volumes is expensive and time-consuming, motivating self-supervised learning (SSL) from unlabeled data. However, applying SSL to 3D CT remains challenging due to the high memory cost of full-volume transformers and the anisotropic spatial structure of CT data, which is not well captured by conventional masking strategies. We propose NEMESIS, a masked autoencoder (MAE) framework that operates on local 128x128x128 superpatches, enabling memory-efficient training while preserving anatomical detail. NEMESIS introduces three key components: (i) noise-enhanced reconstruction as a pretext task, (ii) Masked Anatomical Transformer Blocks (MATB) that perform dual-masking through parallel plane-wise and axis-wise token removal, and (iii) NEMESIS Tokens (NT) for cross-scale context aggregation. On the BTCV multi-organ classification benchmark, NEMESIS with a frozen backbone and a linear classifier achieves a mean AUROC of 0.9633, surpassing fully fine-tuned SuPreM (0.9493) and VoCo (0.9387). Under a low-label regime with only 10% of available annotations, it retains an AUROC of 0.9075, demonstrating strong label efficiency. Furthermore, the superpatch-based design reduces computational cost to 31.0 GFLOPs per forward pass, compared to 985.8 GFLOPs for the full-volume baseline, providing a scalable and robust foundation for 3D medical imaging.

---

## 论文详细总结（自动生成）

这是一份关于论文《NEMESIS: Noise-suppressed Efficient MAE with Enhanced Superpatch Integration Strategy》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 3D CT 影像领域，自监督学习（SSL）面临两大瓶颈：一是 **计算开销巨大**，全卷 Transformer 处理高分辨率 3D 数据时显存消耗呈平方级增长；二是 **空间各向异性**，CT 数据在轴向、冠状面和矢状面的结构特性不同，传统的均匀掩码策略无法有效捕捉这些解剖学特征。
*   **整体含义**：本文旨在开发一种高效、低成本且能捕捉 3D 解剖结构的掩码自编码器（MAE）框架，通过局部“超块”（Superpatch）策略和针对性的 Transformer 架构，为 3D 医学影像提供强大的基础模型。

### 2. 方法论：核心思想与关键技术
NEMESIS 框架由以下三个核心组件构成：
*   **超块预训练（Superpatch Pretraining）**：
    *   不直接处理全卷数据，而是将 3D 体积划分为 $128^3$ 的非重叠超块。
    *   训练时随机采样超块并加入高斯噪声，执行“噪声增强重建”任务，既减少了内存占用，又增强了模型对噪声的鲁棒性。
*   **3D 自适应 Patch 嵌入与 NEMESIS Token (NT)**：
    *   采用双路径嵌入：一条是直接的线性投影（LP），另一条是基于自注意力块（SAB）的超块嵌入器（SE）。
    *   引入 **NEMESIS Token (NT)** 作为辅助上下文锚点，用于聚合跨尺度的全局上下文信息，引导细粒度的局部重建。
*   **掩码解剖 Transformer 块 (MATB)**：
    *   **双重掩码策略**：并行处理“轴向流”（捕捉 Patch 内部上下文）和“平面流”（学习 Patch 间的层间关系）。
    *   这种设计专门针对 CT 的各向异性结构，通过融合不同维度的注意力流来提取更具判别力的解剖特征。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 TotalSegmentator 及混合公共 CT 数据集（包括 HNSCC, FLARE23, TCIA-COVID19, LUNA16, BTCV 等），均为无标注数据。
    *   **下游任务**：BTCV 挑战赛数据集（30 例腹部 CT），执行 8 种器官存在性的多标签分类任务。
*   **Benchmark 与对比方法**：
    *   对比了有监督基准（ResNet3D-50）和 SOTA 级 3D SSL 方法（SuPreM, VoCo）。
    *   评估协议包括：**冻结主干+线性分类器（Linear Probe）** 以及 **全微调（Fine-tuning）**。

### 4. 资源与算力
*   **算力消耗**：论文重点强调了计算效率。NEMESIS ($128^3$ 超块配置) 的单次前向传播计算量仅为 **31.0 GFLOPs**，相比全卷基准（985.8 GFLOPs）降低了 **32 倍**。
*   **硬件细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主干性能对比实验（AUROC 和 F1 分数）。
    *   **标签效率实验**：测试了在 10%、25%、50%、100% 标注比例下的表现。
    *   **消融实验**：对比了不同掩码策略（仅轴向 vs 仅平面）对重建质量（PSNR, SSIM）的影响，以及超参数（嵌入维度、掩码比例）的敏感性分析。
*   **评价**：实验设计较为客观，通过“冻结主干”的测试有力证明了预训练特征的质量。对比了多种 SOTA 模型，且涵盖了低数据场景，实验逻辑闭环。

### 6. 主要结论与发现
*   **性能卓越**：即使在冻结主干的情况下，NEMESIS 的线性分类 AUROC (0.9633) 仍优于全微调的 SuPreM (0.9493) 和 VoCo (0.9387)。
*   **标签效率极高**：仅需 10% 的标注数据，AUROC 即可达到 0.9075，接近全监督水平。
*   **各向异性建模有效**：实验证明平面级掩码（Plane-only）的重建质量显著优于轴向掩码，验证了 MATB 捕捉 CT 结构偏置的必要性。

### 7. 优点与亮点
*   **极高的计算效率**：通过超块化处理，解决了 3D Transformer 难以在标准硬件上训练高分辨率数据的痛点。
*   **架构创新**：MATB 的双流设计和 NEMESIS Token 的引入，针对性地解决了医学影像的各向异性问题，而非简单套用自然语言处理或 2D 视觉的架构。
*   **表征能力强**：线性探测结果甚至超过了其他模型的全微调结果，说明其学习到的解剖特征具有极高的通用性。

### 8. 不足与局限
*   **任务覆盖有限**：目前的下游评估主要集中在“器官存在性分类”这一相对简单的任务上，尚未在更复杂的 3D 语义分割（如 Synapse 数据集）上进行充分验证。
*   **硬件细节缺失**：缺乏具体的训练环境描述，不便于其他研究者评估实际的训练时间成本。
*   **超块拼接风险**：虽然超块降低了开销，但在推理时独立处理超块并拼接，可能会在超块边缘产生解剖结构的不连续性（尽管文中提到重建 PSNR 较高）。

（完）
