---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中太字节级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v2"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于大规模生物数据训练的生物信息学工具
tldr: 随着生物数据集规模达到TB级，内存限制成为机器学习训练的瓶颈。本文推出annbatch，这是一款原生支持anndata的微批次加载器，支持直接在磁盘存储的数据集上进行核外训练。它在单细胞转录组、显微镜成像和全基因组测序等任务中，将加载吞吐量提升了一个数量级，使训练时间从数天缩短至数小时，同时保持了与scverse生态系统的兼容性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01949v2/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 2048, \"height\": 2048}, {\"url\": \"assets/figures/arxiv/2604.01949v2/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 2048, \"height\": 2048}, {\"url\": \"assets/figures/arxiv/2604.01949v2/fig-003.webp\", \"caption\": \"\", \"page\": 18, \"index\": 3, \"width\": 1280, \"height\": 960}]"
motivation: 生物数据集规模已超出系统内存限制，导致数据访问而非模型计算成为训练大规模生物AI模型的主要瓶颈。
method: 开发了原生于anndata的微批次加载器annbatch，支持直接对存储在磁盘上的异构、稀疏或稠密生物数据进行高效的核外训练。
result: 在多项生物学基准测试中，annbatch将数据加载吞吐量提升了高达十倍，并将模型训练周期从几天缩短到几小时。
conclusion: annbatch为大规模生物AI提供了实用的数据加载基础设施，在不放弃标准生物数据格式的前提下实现了TB级数据的可扩展训练。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及既定计算生态系统内的下游分析。在此，我们推出了 annbatch，这是一个原生于 anndata 的小批量加载器，能够直接在磁盘存储的数据集上进行离存（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短至几小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的前提下，能够利用日益增长且多样化的数据集。Github: https://github.com/scverse/annbatch

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

这篇论文介绍了 **annbatch**，一个专为生物学数据格式 `anndata` 设计的高性能微批次（mini-batch）加载器，旨在解决 TB 级生物大数据在深度学习训练中的 I/O 瓶颈问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代生物数据集（如单细胞测序、全基因组测序）的规模已达到 TB 级，远超系统内存。在训练模型时，瓶颈已从“模型计算”转移到了“数据检索（I/O）”。
*   **背景**：现有的高性能加载框架（如 NVIDIA Merlin, WebDataset）通常需要将数据转换为特定格式，破坏了生物学界通用的 `anndata` 和 `scverse` 生态兼容性。而现有的单细胞加载工具（如 `scDataset`）在处理大规模数据时速度不足以饱和现代 GPU。
*   **意义**：`annbatch` 实现了在不改变标准数据格式的前提下，通过“核外（out-of-core）”训练，将训练时间从数天缩短至数小时。

### 2. 论文提出的方法论
`annbatch` 包含两个核心组件，通过“伪随机块访问”平衡了读取速度与随机性：
*   **预洗牌器（Pre-shuffler）**：
    *   针对磁盘上的多个 `anndata` 文件，利用延迟加载（lazy loading）技术，按连续块（chunks）读取数据到内存。
    *   在内存中对这些块进行洗牌，然后以 Zarr 格式重新写入磁盘。这保证了后续读取时，大段连续的磁盘访问依然具有足够的随机性。
*   **高性能加载器（Data Loader）**：
    *   **Zarr 后端**：利用 Rust 编写的 `zarrs-python` 桥接库，实现极速数据读取。
    *   **关键技术优化**：采用 Linux 的 **直接 I/O（Direct I/O）** 绕过内核缓存、**固定内存（Pinned Memory）** 加速 GPU 传输、以及自定义的不等长切片索引。
    *   **框架无关**：支持 PyTorch 等主流深度学习框架。

### 3. 实验设计
论文在三个代表性的生物学场景中进行了基准测试：
*   **单细胞转录组学**：使用 **Tahoe-100M** 数据集（约 1 亿个细胞）。
*   **单细胞显微成像**：处理 128x128x5 尺寸的裁剪图像。
*   **全基因组测序（WGS）**：使用 1000 Genomes 数据集并扩展至 500,000 个样本，涵盖稀疏（罕见变异）和稠密（常见变异）数据。
*   **对比方法**：`scDataset`、`MappedCollection`、`numpy memmap` 以及 `BioNemo`。

### 4. 资源与算力
*   **云端测试**：AWS `ml.g4dn.4xlarge` 实例（配备 NVIDIA T4 GPU），用于模拟标准磁盘速度限制下的表现。
*   **高性能计算（HPC）**：de.NBI GPU 节点（T4）和 NVIDIA HGX-H100 单元，用于测试高带宽存储和多进程扩展性。
*   **训练时长示例**：在 Tahoe-100M 上，`annbatch` 跑完一个 epoch 仅需 **17 分钟**，而对比方法 `MappedCollection` 需要 **21 小时**。

### 5. 实验数量与充分性
*   **实验覆盖面**：涵盖了单细胞、影像、基因组学三大领域，证明了方法的通用性。
*   **消融与对比**：对比了 Zarr 与 h5ad 格式的差异、预洗牌的开销、以及不同批次大小（Batch Size）对吞吐量的影响。
*   **客观性**：实验考虑了操作系统缓存（OS Caching）的干扰，确保在数据量远超内存的真实“核外”环境下进行测试，结果较为客观公平。

### 6. 论文的主要结论与发现
*   **吞吐量提升**：`annbatch` 的加载速度比现有工具快 **10 到 40 倍**。
*   **预洗牌开销可忽略**：虽然预洗牌需要数小时（不需 GPU），但其时间成本在第一个训练 epoch 内就能通过节省的 GPU 时间完全抵消。
*   **Zarr 的优越性**：相比传统的 HDF5 (h5ad)，Zarr 格式在预洗牌速度和读取吞吐量上均有显著优势。
*   **分布式潜力**：`annbatch` 的高效率使其能够更好地支持多 GPU 分布式训练，避免 I/O 成为多卡环境下的瓶颈。

### 7. 优点
*   **生态兼容性**：完全支持 `anndata` 格式，用户无需转换数据即可直接使用 `scverse` 工具链进行下游分析。
*   **端到端方案**：提供了从磁盘预洗牌到 GPU 加载的完整流水线。
*   **极致优化**：引入 Rust 后端和 Direct I/O 等底层优化，技术深度较高。

### 8. 不足与局限
*   **采样策略限制**：由于采用连续块读取，目前难以实现复杂的加权采样（Weighted Sampling）。
*   **多模态支持**：当前版本尚未直接优化多模态数据的同步加载。
*   **预洗牌依赖**：虽然开销可抵消，但仍需额外的磁盘空间来存储洗牌后的副本。
*   **硬件依赖**：部分优化（如 Direct I/O）主要针对 Linux 系统，跨平台表现可能存在差异。

（完）
