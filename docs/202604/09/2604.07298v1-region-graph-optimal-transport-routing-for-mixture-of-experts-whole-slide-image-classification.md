---
title: Region-Graph Optimal Transport Routing for Mixture-of-Experts Whole-Slide Image Classification
title_zh: 面向混合专家模型全切片图像分类的区域图最优传输路由
authors: "Xin Tian, Jiuliu Lu, Ephraim Tsalik, Bart Wanders, Colleen Knoth, Julian Knight"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07298v1"
tags: ["query:ai-pm"]
score: 10.0
evidence: 用于病理全切片图像分类的混合专家模型
tldr: 针对全切片图像（WSI）分类中多实例学习（MIL）模型缺乏病理异质性处理能力及混合专家模型（MoE）负载不均的问题，本文提出ROAM框架。该框架利用容量受限的熵正则化最优传输（Sinkhorn）将空间区域标记路由至专家池化器，并引入图正则化机制以确保相邻区域路由的一致性，实现了均衡且空间感知的专家分配，在多个基准测试中表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07298v1/fig-001.webp\", \"caption\": \"Fig. 1. Frozen patch embeddings are pooled into M spatial region tokens. A routing GNN on the region graph parameterises region-to-expert costs, and Sinkhorn optimal transport routes region mass to E experts under per-slide capacity constraints (with optional graph smoothing). Each expert performs gated-attention pooling over its routed regions, and expert outputs are fused for slide-level prediction.\", \"page\": 3, \"index\": 1, \"width\": 702, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.07298v1/fig-002.webp\", \"caption\": \"Table 2. Ablation study on NSCLC (LUAD vs. LUSC) using UNI2-h embeddings (mean±std over 5 folds). Internal evaluation is on TCGA (int.); external evaluation is on CPTAC (ext.).\", \"page\": 7, \"index\": 2, \"width\": 714, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.07298v1/fig-003.webp\", \"caption\": \"Table 1. WSI benchmark results with UNI2 patch encoders (mean±std over 5-folds). For NSCLC (LUAD/LUSC), we report internal AUC on TCGA (int.) and external AUC on CPTAC (ext.); for BRCA and CRC we report internal AUC; for PANDA we report internal QWK. Best per column in bold; second best underlined.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 264}]"
motivation: 现有的MIL聚合器采用单一路径处理所有实例，难以应对WSI的病理异质性，且传统MoE路由易导致专家利用极度不均衡。
method: 提出ROAM框架，通过最优传输算法实现容量受限的专家路由，并结合图正则化使相邻组织区域倾向于分配给相同的专家。
result: 在四个WSI基准测试中达到了领先水平，特别是在NSCLC跨数据集泛化测试中取得了0.845的AUC。
conclusion: ROAM通过结合空间图结构与最优传输路由，有效解决了MoE在病理图像分析中的负载崩溃问题，提升了模型的专业化处理能力和泛化性能。
---

## 摘要
多实例学习（MIL）是计算病理学中千兆像素全切片图像（WSI）分类的主流框架。然而，目前的 MIL 聚合器通过共享路径路由所有实例，限制了它们针对每张切片中固有的病理异质性进行专业化处理的能力。混合专家（MoE）方法通过将实例分配到专门的专家子网络中提供了一种自然的解决方案；然而，无约束的 softmax 路由可能会导致高度不平衡的利用率，即一个或少数几个专家吸收了大部分路由权重，使混合模型退化为接近单一路径的方案。为了解决这些局限性，我们提出了 ROAM（区域图最优传输混合专家模型），这是一种空间感知的 MoE-MIL 聚合器，它通过容量受限的熵最优传输将区域标记（region tokens）路由到专家池化器，从而在结构上促进均衡的专家利用。ROAM 作用于空间区域标记，这些标记是通过将密集的补丁包（patch bags）压缩为空间分箱单元而获得的，使路由与局部组织邻域保持一致。它引入了两个关键机制：(i) 将区域到专家的分配建模为具有显式单切片容量边际的熵最优传输（Sinkhorn），在无需辅助负载均衡损失的情况下强制实现均衡的专家利用；(ii) 图正则化的 Sinkhorn 迭代，在空间区域图上扩散路由分配，鼓励相邻区域一致地路由到相同的专家。在四个 WSI 基准测试中，使用冻结的基础模型补丁嵌入进行评估，ROAM 达到了与强 MIL 和 MoE 基准线相当的性能，并在 NSCLC 泛化（TCGA-CPTAC）上达到了 0.845 ± 0.019 的外部 AUC。

## Abstract
Multiple Instance Learning (MIL) is the dominant framework for gigapixel whole-slide image (WSI) classification in computational pathology. However, current MIL aggregators route all instances through a shared pathway, constraining their capacity to specialise across the pathological heterogeneity inherent in each slide. Mixture-of-Experts (MoE) methods offer a natural remedy by partitioning instances across specialised expert subnetworks; yet unconstrained softmax routing may yield highly imbalanced utilisation, where one or a few experts absorb most routing mass, collapsing the mixture back to a near-single-pathway solution. To address these limitations, we propose ROAM (Region-graph OptimAl-transport Mixture-of-experts), a spatially aware MoE-MIL aggregator that routes region tokens to expert poolers via capacity-constrained entropic optimal transport, promoting balanced expert utilisation by construction. ROAM operates on spatial region tokens, obtained by compressing dense patch bags into spatially binned units that align routing with local tissue neighbourhoods and introduces two key mechanisms: (i) region-to-expert assignment formulated as entropic optimal transport (Sinkhorn) with explicit per slide capacity marginals, enforcing balanced expert utilisation without auxiliary load-balancing losses; and (ii) graph-regularised Sinkhorn iterations that diffuse routing assignments over the spatial region graph, encouraging neighbouring regions to coherently route to the same experts. Evaluated on four WSI benchmarks with frozen foundation-model patch embeddings, ROAM achieves performance competitive against strong MIL and MoE baselines, and on NSCLC generalisation (TCGA-CPTAC) reaches external AUC 0.845 +- 0.019.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ROAM**（Region-graph OptimAl-transport Mixture-of-experts）的新型多实例学习（MIL）聚合框架，旨在解决病理全切片图像（WSI）分类中的异质性处理和专家负载均衡问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在计算病理学中，WSI 具有千兆像素级的超大规模，且包含极高的形态学和空间异质性。目前主流的 MIL 框架（如 ABMIL、TransMIL）通常将所有图像补丁（patches）通过单一的共享路径进行处理，这限制了模型针对不同组织类型（如肿瘤、基质、炎症）进行专业化特征提取的能力。
*   **核心问题**：
    1.  **混合专家模型（MoE）的崩溃问题**：传统的 MoE 路由通常使用 Softmax 分配，容易导致“赢家通吃”，即少数专家吸收了几乎所有数据，使模型退化为单路径模型。
    2.  **空间上下文缺失**：现有的路由机制往往忽略了补丁之间的空间位置关系，导致相邻的、具有相似病理意义的组织区域被碎片化地分配给不同的专家。

### 2. 论文提出的方法论
ROAM 框架的核心思想是将专家路由建模为**容量受限的最优传输（Optimal Transport, OT）问题**，并结合图正则化。

*   **区域标记化（Region Tokenization）**：为了降低计算复杂度并保留空间局部性，ROAM 将数万个补丁通过空间网格分箱（Grid Binning）压缩为 $M$ 个区域标记（Region Tokens）。
*   **容量受限的 OT 路由**：
    *   使用 **Sinkhorn 算法** 求解熵正则化最优传输问题。
    *   **关键约束**：通过设置显式的列边际约束（Marginal Constraints），强制要求每个专家处理固定比例的区域质量。这在结构上保证了负载均衡，无需额外的辅助损失函数。
*   **图正则化 Sinkhorn 迭代**：
    *   构建区域间的 kNN 空间图。
    *   在 Sinkhorn 迭代过程中交织一个**图平滑步骤**（Graph Diffusion），利用热核权重鼓励空间相邻的区域路由到相同的专家，从而实现空间相干性。
*   **专家聚合与融合**：每个专家对其接收到的区域进行门控注意力池化（Gated-Attention Pooling），最后通过另一个门控机制融合所有专家的输出进行最终预测。

### 3. 实验设计
*   **数据集**：
    1.  **NSCLC（非小细胞肺癌）**：使用 TCGA 数据集进行训练和内部验证，使用 CPTAC 数据集进行外部泛化测试。
    2.  **BRCA（乳腺癌）**：IDC vs. non-IDC 分类。
    3.  **CRC（结直肠癌）**：COAD vs. READ 分类。
    4.  **PANDA（前列腺癌分级）**：六分类任务，包含超过 10,000 张切片。
*   **Benchmark 与对比方法**：
    *   **传统 MIL**：ABMIL, CLAM, DSMIL, TransMIL, ILRA 等。
    *   **MoE/OT 相关方法**：MAMMOTH, PAMoE, OTSurv。
*   **特征提取**：所有实验均使用预训练好的病理基础模型 **UNI2-h** 提取的冻结嵌入。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA A100 (40 GB)** GPU 上运行。
*   **训练细节**：使用 AdamW 优化器，最高训练 200 个 epoch（带早停机制），batch size 为 1。
*   **时长**：文中未明确给出具体的总训练时长，但提到区域标记化将路由复杂度从 $O(NE)$ 降低到 $O(ME)$（$M \ll N$），显著提升了计算效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了四个主流 WSI 数据集，任务类型包括二分类和多分类（PANDA）。
*   **消融实验**：针对路由 GNN、图正则化、容量约束（对比 Softmax）、OT 引导池化等关键组件进行了详细的消融研究。
*   **客观性**：采用了患者分层的 5 折交叉验证，并特别强调了**外部验证集（CPTAC）**的表现，这比单纯的内部验证更能体现模型的鲁棒性和公平性。

### 6. 主要结论与发现
*   **性能领先**：ROAM 在多个任务上达到了与最强基准线持平或更优的水平。特别是在 NSCLC 的跨中心泛化测试中，AUC 达到 0.845，优于其他 MoE 变体。
*   **稳定性**：相比于使用 Softmax 路由的 MoE 方法（如 PAMoE），ROAM 在不同折叠间的方差更小，说明容量约束路由能提供更稳定的训练过程。
*   **可视化验证**：路由图显示 ROAM 能够生成空间连续的“专家领地”，且所有专家都被有效利用，验证了图正则化和 OT 约束的有效性。

### 7. 优点
*   **结构化负载均衡**：通过 OT 理论从数学上解决了 MoE 的负载崩溃问题，避免了繁琐的辅助损失调参。
*   **空间感知**：引入图平滑机制，使模型能够捕捉病理组织的空间连续性，这符合病理学家的诊断逻辑。
*   **高效性**：区域标记化解决了大规模 WSI 在应用复杂路由算法时的计算瓶颈。

### 8. 不足与局限
*   **依赖冻结编码器**：实验主要基于冻结的基础模型特征，尚未探讨端到端微调对路由效果的影响。
*   **固定区域划分**：目前采用固定的网格分箱进行区域标记化，可能无法完美契合不规则的组织边界。
*   **超参数敏感性**：虽然减少了负载均衡损失，但引入了 OT 正则化系数 $\epsilon$ 和平滑强度 $\lambda_s$ 等新参数。

（完）
