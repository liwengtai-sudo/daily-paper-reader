---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中太字节级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于TB级生物数据训练的生物信息学工具
tldr: 随着生物数据集规模达到TB级，内存限制成为机器学习训练的瓶颈。本文推出annbatch，这是一款原生支持anndata的微批次加载器，支持直接在磁盘存储的数据集上进行核外训练。它在单细胞转录组、显微镜成像和全基因组测序等任务中，将加载吞吐量提升了一个数量级，使训练时间从数天缩短至数小时，同时保持了与scverse生态系统的兼容性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01949v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-003.webp\", \"caption\": \"\", \"page\": 18, \"index\": 3, \"width\": 1280, \"height\": 960}]"
motivation: 生物数据集规模已超出系统内存限制，导致数据访问而非模型计算成为训练大规模生物AI模型的主要瓶颈。
method: 开发了原生于anndata的微批次加载器annbatch，支持直接对存储在磁盘上的异构、稀疏或稠密生物数据进行高效的核外训练。
result: 在多项生物学基准测试中，annbatch将数据加载吞吐量提升了高达十倍，并将模型训练周期从几天缩短到几小时。
conclusion: annbatch为大规模生物AI提供了实用的数据加载基础设施，在不放弃标准生物数据格式的前提下实现了TB级数据的可扩展训练。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及在既定计算生态系统内的下游分析。在此，我们推出了 annbatch，这是一个原生于 anndata 的小批量加载器，能够直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序的基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短到几小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能建立了一个实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够利用日益增长且多样化的数据集。Github: https://github.com/scverse/annbatch

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **annbatch** 的高性能微批次（mini-batch）数据加载框架，旨在解决生物大数据（如单细胞组学、基因组学）在深度学习训练中的 I/O 瓶颈问题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着生物数据集规模达到太字节（TB）级别，系统内存已无法容纳全部数据。目前的瓶颈已从“模型计算”转移到了“数据检索（I/O）”。
*   **研究背景**：
    *   现有的高性能加载框架（如 NVIDIA Merlin, Petastorm）通常需要将数据转换为特定格式，这破坏了与生物学界主流工具生态（如 `scverse` 和 `anndata` 格式）的兼容性。
    *   现有的 `anndata` 加载器（如 `scDataset`）在处理大规模磁盘数据时，由于随机读取效率低下，无法充分利用 GPU 算力。
*   **整体含义**：`annbatch` 旨在不改变标准生物数据格式的前提下，通过优化底层读取逻辑，解锁 TB 级数据的核外（out-of-core）训练能力。

### 2. 论文提出的方法论
`annbatch` 包含两个核心组件，通过“伪随机块访问”平衡读取速度与随机性：
*   **预洗牌器 (Pre-shuffler)**：
    *   **核心思想**：在训练前对磁盘上的 `anndata` 文件进行分块洗牌。
    *   **技术细节**：利用 `anndata` 的延迟加载（lazy loading）特性，将数据读入内存中的随机连续块，洗牌后重新写入磁盘（推荐 Zarr 格式）。这保证了后续加载时可以进行大块连续读取，同时维持训练所需的随机性。
*   **高性能加载器 (Data Loader)**：
    *   **关键技术**：
        *   **Zarr 后端优化**：开发了 `zarrs-python`（基于 Rust 的 `zarrs` 库），实现比传统 HDF5 更快的并发读取。
        *   **直接 I/O (Direct I/O)**：绕过操作系统缓存，直接从磁盘读取数据。
        *   **固定内存 (Pinned Memory)**：加速数据从 CPU 内存到 GPU 显存的传输。
        *   **异步与并行**：支持多进程并行加载，并利用 GPU 加速内存中的洗牌操作。

### 3. 实验设计
论文在三种代表性的生物学场景下进行了基准测试（Benchmark）：
*   **单细胞转录组学 (scRNA-seq)**：使用 **Tahoe-100M** 数据集（约 1 亿个细胞）。
*   **全基因组测序 (WGS)**：使用 **1000 Genomes** 数据集（扩展至 50 万个体），涵盖稀疏（罕见变异）和稠密（常见变异）两种模式。
*   **单细胞显微成像**：处理高载荷的图像切片数据（128x128x5）。
*   **对比方法**：
    *   `scDataset`（现有的单细胞加载器）
    *   `MappedCollection`（scverse 生态内的现有方案）
    *   `numpy memmap`（未压缩的磁盘映射基准）
    *   `h5ad` 原生格式加载。

### 4. 资源与算力
*   **GPU 型号**：主要在 **NVIDIA T4** (AWS ml.g4dn.4xlarge 实例) 和 **NVIDIA H100** (用于扩展性测试) 上运行。
*   **算力环境**：包括 AWS 云环境、德国生物信息学基础设施 (de.NBI) 以及 LRZ AI 分区。
*   **训练时长对比**：在 Tahoe-100M 数据集上，`annbatch` 完成一个 Epoch 仅需 **17 分钟**，而 `scDataset` 需 7.5 小时，`MappedCollection` 需 21 小时。

### 5. 实验数量与充分性
*   **实验组数**：论文针对不同数据类型（稀疏 vs 稠密）、不同存储格式（Zarr vs h5ad）、不同硬件环境（AWS vs HPC）以及不同进程数进行了多组对比实验。
*   **充分性与客观性**：
    *   实验涵盖了生物数据的三大挑战：高样本量、高特征维度、大单样本载荷，具有较强的代表性。
    *   作者客观地指出了在 AWS 等云环境下受限于磁盘 I/O 速度，GPU 加速效果可能不明显，但在高性能计算（HPC）环境下优势巨大。
    *   考虑了预洗牌（Pre-shuffling）的时间成本，并证明该成本在 1-2 个 Epoch 内即可被节省的训练时间抵消。

### 6. 主要结论与发现
*   **吞吐量提升**：`annbatch` 的数据加载速度比现有工具快 **10 到 40 倍**。
*   **存储格式影响**：采用 Zarr 格式配合 Rust 底层实现，比传统的 HDF5 (h5ad) 显著提高了预洗牌速度和读取吞吐量。
*   **分布式扩展性**：在多 GPU 环境下，`annbatch` 能够提供足够的吞吐量来饱和 H100 等高性能显卡的计算能力，避免了 I/O 导致的计算闲置。
*   **生态兼容**：成功在不脱离 `anndata` 格式的前提下实现了工业级的加载性能。

### 7. 优点
*   **生态无缝集成**：完全兼容 `scverse` 生态，用户无需转换数据格式即可使用。
*   **端到端方案**：不仅提供了加载器，还解决了磁盘数据随机化（预洗牌）这一关键痛点。
*   **跨领域通用性**：证明了该框架在测序、成像、遗传学等多种生物模态下的有效性。
*   **底层优化**：引入 Rust 编写的 Zarr 库和 Direct I/O 等系统级优化，技术深度较高。

### 8. 不足与局限
*   **采样策略限制**：由于采用连续块读取，目前难以直接实现复杂的加权采样（Weighted Sampling），需要通过特定的磁盘写入顺序来变通。
*   **预洗牌开销**：虽然可以摊销，但对于仅训练 1 个 Epoch 的极小任务，预洗牌的时间可能显得较长。
*   **多模态支持**：目前尚未直接支持多模态数据的同步加载（如同时加载同一细胞的 RNA 和蛋白数据），仍需未来扩展。
*   **硬件依赖**：要达到极致性能，对磁盘 I/O 速度（如 NVMe SSD 或并行文件系统）有较高要求。

（完）
