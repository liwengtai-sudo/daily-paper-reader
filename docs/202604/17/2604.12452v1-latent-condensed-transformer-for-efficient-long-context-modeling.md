---
title: Latent-Condensed Transformer for Efficient Long Context Modeling
title_zh: 用于高效长上下文建模的潜空间压缩 Transformer
authors: "Zeng You, Yaofo Chen, Qiuwu Chen, Ying Sun, Shuhai Zhang, Yingjian Li, Yaowei Wang, Mingkui Tan"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12452v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于长上下文建模的Transformer架构
tldr: 针对大语言模型在长文本处理中面临的KV缓存增长和计算复杂度挑战，本文提出Latent-Condensed Attention (LCA)。该方法在潜在空间内将上下文解耦为语义向量和位置键，分别通过查询感知池化和锚点选择进行压缩，实现了计算开销与存储需求的同步优化。LCA无需额外参数且兼容多种架构，在保持高性能的同时显著提升了长文本处理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12452v1/fig-001.webp\", \"caption\": \"Figure 2: Efficiency comparison of different context lengths. (a) presents the first-token generation latency. (b) shows the per-token (interval) latency during decoding. (c) illustrates the GPU memory footprint of KV cache.\", \"page\": 7, \"index\": 1, \"width\": 820, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2604.12452v1/fig-002.webp\", \"caption\": \"Figure 1: An overview of Latent-Condensed Attention (LCA). The history context is condensed into a compact set of representatives via group-wise conde sation in the latent space: semantic information is aggregated through weighted pooling of CKV , while positional information is preserved via anchor selection from KR. The recent context is retained in full fidelity without condensation.\", \"page\": 4, \"index\": 2, \"width\": 948, \"height\": 478}]"
motivation: 旨在解决长文本建模中KV缓存线性增长与注意力计算平方复杂度带来的效率瓶颈，并填补现有压缩技术与稀疏注意力结合的空白。
method: 提出在潜在空间直接压缩上下文的LCA机制，通过解耦语义与位置信息并分别进行聚合与筛选，实现对上下文的高效表征。
result: "实验表明LCA在128K上下文下可实现2.5倍的预填充加速和90%的KV缓存缩减，且具有长度无关的理论误差界限。"
conclusion: LCA为高效长文本建模提供了一种架构无关且性能稳健的解决方案，成功实现了计算与存储成本的双重优化。
---

## 摘要
大语言模型 (LLM) 在处理长上下文时面临重大挑战，原因在于键值 (KV) 缓存的线性增长以及自注意力机制的平方复杂度。现有方法分别针对这些瓶颈进行处理：多头潜空间注意力 (MLA) 通过将 token 投影到低维潜空间来减少 KV 缓存，而稀疏注意力则减少了计算量。然而，稀疏方法无法原生运行在 MLA 的压缩潜空间结构上，从而错失了联合优化的机会。在本文中，我们提出了潜空间压缩注意力 (LCA)，它直接在 MLA 的潜空间内压缩上下文，其中表示被解耦为语义潜向量和位置键。LCA 通过查询感知池化分别聚合语义向量，并通过锚点选择保留位置键。该方法在不增加参数的情况下，同时降低了计算成本和 KV 缓存。除了 MLA，LCA 的设计与架构无关，可以轻松扩展到其他注意力机制（如 GQA）。在理论上，我们证明了一个与长度无关的误差界限。实验表明，在 128K 上下文下，LCA 在保持竞争性能的同时，实现了高达 2.5 倍的预填充加速和 90% 的 KV 缓存减少。

## Abstract
Large language models (LLMs) face significant challenges in processing long contexts due to the linear growth of the key-value (KV) cache and quadratic complexity of self-attention. Existing approaches address these bottlenecks separately: Multi-head Latent Attention (MLA) reduces the KV cache by projecting tokens into a low-dimensional latent space, while sparse attention reduces computation. However, sparse methods cannot operate natively on MLA's compressed latent structure, missing opportunities for joint optimization. In this paper, we propose Latent-Condensed Attention (LCA), which directly condenses context within MLA's latent space, where the representation is disentangled into semantic latent vectors and positional keys. LCA separately aggregates semantic vectors via query-aware pooling and preserves positional keys via anchor selection. This approach jointly reduces both computational cost and KV cache without adding parameters. Beyond MLA, LCA's design is architecture-agnostic and readily extends to other attention mechanisms such as GQA. Theoretically, we prove a length-independent error bound. Experiments show LCA achieves up to 2.5$\times$ prefilling speedup and 90% KV cache reduction at 128K context while maintaining competitive performance.

---

## 论文详细总结（自动生成）

### 论文总结：用于高效长上下文建模的潜空间压缩 Transformer (LCA)

#### 1. 论文的核心问题与整体含义
论文的研究动机源于大语言模型（LLM）在处理长文本时的两大瓶颈：**KV 缓存（KV Cache）的线性增长**和**自注意力机制的平方级计算复杂度**。
*   **背景**：现有的多头潜空间注意力（MLA）通过低维投影减少了单个 token 的缓存大小，但 token 总数未减，计算量依然巨大；而稀疏注意力方法通常需要重建全维 KV 矩阵，无法直接利用 MLA 的压缩结构。
*   **核心问题**：如何直接在 MLA 的潜空间内对 token 进行压缩，从而同时减少存储开销和计算量，实现长文本的高效建模。

#### 2. 论文提出的方法论：潜空间压缩注意力 (LCA)
LCA 的核心思想是**在潜空间内对冗余的上下文进行结构化压缩**。其关键技术细节包括：
*   **双路径解耦压缩策略**：针对 MLA 中解耦的语义和位置信息采取不同处理方式。
    *   **语义压缩（加权池化）**：对于语义潜向量 $C_{KV}$，采用查询感知（Query-aware）的加权池化。通过计算当前查询与历史 token 的相关性得分，将一组 token 聚合为一个代表向量。
    *   **位置保留（锚点选择）**：由于旋转位置编码（RoPE）是非线性的，无法直接池化。LCA 选择组内重要性得分最高的 token 作为“位置锚点”，保留其原始位置信息。
*   **局部窗口保留**：为了维持近距离的细粒度信息，LCA 对最近的 $w$ 个 token 不做压缩，保持全精度。
*   **算法流程**：将历史上下文划分为固定大小的组（Group），计算组内 token 的重要性得分，执行语义聚合与位置锚点选择，最后将压缩后的 KV 与局部 KV 拼接进行注意力计算。

#### 3. 实验设计
*   **数据集/场景**：
    *   **长文本评估**：LongBench-E（涵盖 21 个任务，测试综合理解）和 RULER（测试极长上下文下的检索与推理，最高达 128K）。
    *   **短文本评估**：MMLU（通用知识）、GSM8K（数学推理）、MBPP（代码生成）。
*   **对比方法**：
    *   原生 **MLA**（DeepSeek-V2-Lite）。
    *   **MInference** 与 **FlexPrefill**：代表性的动态稀疏注意力方法。
    *   **KDA**：一种基于门控线性注意力的变体。
*   **泛化性测试**：在 MiniCPM3-4B (MLA 架构) 和 DeepSeek-R1-Distill-Qwen-7B (GQA 架构) 上验证了方法的通用性。

#### 4. 资源与算力
*   **硬件设施**：实验在配备 **8 × NVIDIA H200 GPU** 的服务器上运行。
*   **训练细节**：在 SlimPajama 数据集（32K 长度）上进行了 **1000 步** 的轻量级微调，总计消耗约 2.0B tokens。
*   **工程优化**：使用 **Triton** 编写了高性能自定义内核，以实现算子级的加速。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 4B 到 16B 不同规模的模型，并在 4K 到 128K 的多种上下文长度下进行了测试。
*   **消融实验**：非常充分。作者对比了不同的池化策略（均值、最大、加权池化）、组大小 $g$、窗口大小 $w$ 以及查询摘要的数量。
*   **客观性**：通过理论证明（误差界限分析）与多维度实验相结合，验证了方法在不同架构（MLA 和 GQA）下的有效性，实验设计较为公平、客观。

#### 6. 论文的主要结论与发现
*   **极致效率**：在 128K 上下文下，LCA 实现了 **2.5 倍的预填充加速** 和 **90% 的 KV 缓存缩减**。
*   **性能稳健**：在大幅压缩的情况下，LCA 在 LongBench-E 和 RULER 上的表现与原生 MLA 相当，甚至在某些长文本任务中表现更优。
*   **理论保证**：证明了 LCA 的近似误差存在一个与序列长度无关的统一上界，解释了其在极长文本下依然稳定的原因。

#### 7. 优点
*   **联合优化**：首次实现了在潜空间内同时优化存储（KV Cache）和计算（Complexity）。
*   **架构无关**：设计思路具有普适性，不仅适用于 MLA，也能显著提升 GQA 架构的效率。
*   **无额外参数**：不需要引入新的参数，仅需极少量的微调即可迁移至现有预训练模型。
*   **工程落地性强**：通过 Triton 内核优化，展示了在实际推理系统中的巨大潜力。

#### 8. 不足与局限
*   **工程实现门槛**：高性能表现高度依赖于自定义的 Triton 内核，对于缺乏底层算子开发能力的开发者来说，部署难度较高。
*   **极端检索损失**：在需要极度精确的 token 级检索（如大海捞针任务的某些变体）中，由于采用了组内聚合，可能会存在微小的精度下降。
*   **量化兼容性未知**：论文主要基于 BF16/FP16 精度，尚未探讨该压缩机制与 Int8 或 FP4 等低比特量化技术的兼容性。

（完）
