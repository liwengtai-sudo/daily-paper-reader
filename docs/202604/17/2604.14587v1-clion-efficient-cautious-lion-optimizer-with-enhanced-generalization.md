---
title: "CLion: Efficient Cautious Lion Optimizer with Enhanced Generalization"
title_zh: CLion：具有增强泛化性能的高效谨慎 Lion 优化器
authors: "Feihu Huang, Guanyi Zhang, Songcan Chen"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14587v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于训练深度学习模型的优化算法及其泛化分析
tldr: 本文针对Lion优化器缺乏泛化性理论分析的问题，利用算法稳定性首次证明其泛化误差为O(1/Nτ^T)。为克服Lion中符号函数可能导致的泛化瓶颈，提出了一种改进的CLion（Cautious Lion）优化器。CLion通过谨慎使用符号函数，将泛化误差显著降低至O(1/N)，并在非凸随机优化中实现了快速收敛。实验结果验证了该算法在提升深度学习模型泛化性能方面的优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14587v1/fig-001.webp\", \"caption\": \"Figure 2: Language modeling at Wikitext-2 dataset.\", \"page\": 11, \"index\": 1, \"width\": 890, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.14587v1/fig-002.webp\", \"caption\": \"Figure 3: Language modeling at Wikitext-103 dataset.\", \"page\": 11, \"index\": 2, \"width\": 890, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2604.14587v1/fig-003.webp\", \"caption\": \"Figure 5: Image classification at tiny-ImageNet dataset.\", \"page\": 13, \"index\": 3, \"width\": 890, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2604.14587v1/fig-004.webp\", \"caption\": \"Figure 4: Image classification at Cifar-10 dataset.\", \"page\": 12, \"index\": 4, \"width\": 890, \"height\": 239}, {\"url\": \"assets/figures/arxiv/2604.14587v1/fig-005.webp\", \"caption\": \"Figure 1: Illustration of different active functions: (a) Lion [Chen et al., 2023b] uses h(ct) = sign(ct); (b) Lion-K [Chen et al., 2023a] uses h(ct) = ∇K(ct) = I(|ct| > e)sign(ct), where e > 0; (c) Lion-K [Chen et al., 2023a] uses h(ct) = ∇K(ct) = tanh(act), where a > 0, and RLion [Rong et al., 2025] uses h(ct) = 2 πarctan(αct), where α > 0; (d) our CLion uses h(ct) = sign(ct) when minj∈St |(ct)j | ≥ ν > 0, otherwise h(ct) = ct, where St = {j|(ct)j ̸= 0, j = 1, · · · , d}.\", \"page\": 5, \"index\": 5, \"width\": 890, \"height\": 211}]"
motivation: 旨在填补Lion优化器在泛化性理论分析方面的空白，并解决其因符号函数导致的泛化误差较大的问题。
method: 提出CLion优化器，通过谨慎应用符号函数改进梯度更新策略，并利用数学归纳法和算法稳定性进行理论证明。
result: 理论证明CLion具有比Lion更低的泛化误差O(1/N)及快速的收敛率，且实验结果支持了其在多种任务中的有效性。
conclusion: CLion是一种兼具高效收敛性与增强泛化能力的优化器，为深度学习模型的优化提供了更优的理论保障和实践选择。
---

## 摘要
Lion 优化器是机器学习中一种流行的基于学习的优化算法，在训练许多深度学习模型中展现出卓越的性能。尽管 Lion 优化器的收敛性质已得到研究，但其泛化分析仍然缺失。为了填补这一空白，我们基于数学归纳法，通过算法稳定性研究了 Lion 的泛化性质。具体而言，我们证明了 Lion 的泛化误差为 $O(\frac{1}{Nτ^T})$，其中 $N$ 是训练样本量，$τ>0$ 表示梯度估计器中非零元素的最小绝对值，$T$ 是总迭代次数。此外，我们得到了一个有趣的副产品，即 SignSGD 算法具有与 Lion 相同的泛化误差。为了增强 Lion 的泛化性能，我们通过谨慎地使用符号函数，设计了一种新型的高效谨慎 Lion（即 CLion）优化器。此外，我们证明了 CLion 的泛化误差为 $O(\frac{1}{N})$，低于 Lion 的 $O(\frac{1}{Nτ^T})$，因为参数 $τ$ 通常非常小。同时，我们研究了 CLion 优化器的收敛性质，并证明了在非凸随机优化中，CLion 在梯度的 $\ell_1$-范数下具有 $O(\frac{\sqrt{d}}{T^{1/4}})$ 的快速收敛率，其中 $d$ 表示模型维度。大量的数值实验证明了 CLion 优化器的有效性。

## Abstract
Lion optimizer is a popular learning-based optimization algorithm in machine learning, which shows impressive performance in training many deep learning models. Although convergence property of the Lion optimizer has been studied, its generalization analysis is still missing. To fill this gap, we study generalization property of the Lion via algorithmic stability based on the mathematical induction. Specifically, we prove that the Lion has a generalization error of $O(\frac{1}{Nτ^T})$, where $N$ is training sample size, and $τ>0$ denotes the smallest absolute value of non-zero element in gradient estimator, and $T$ is the total iteration number. In addition, we obtain an interesting byproduct that the SignSGD algorithm has the same generalization error as the Lion. To enhance generalization of the Lion, we design a novel efficient Cautious Lion (i.e., CLion) optimizer by cautiously using sign function. Moreover, we prove that our CLion has a lower generalization error of $O(\frac{1}{N})$ than $O(\frac{1}{Nτ^T})$ of the Lion, since the parameter $τ$ generally is very small. Meanwhile, we study convergence property of our CLion optimizer, and prove that our CLion has a fast convergence rate of $O(\frac{\sqrt{d}}{T^{1/4}})$ under $\ell_1$-norm of gradient for nonconvex stochastic optimization, where $d$ denotes the model dimension.   Extensive numerical experiments demonstrate effectiveness of our CLion optimizer.

---

## 论文详细总结（自动生成）

这是一份关于论文《CLion: Efficient Cautious Lion Optimizer with Enhanced Generalization》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：Lion 优化器是近年来通过程序搜索发现的一种高效优化算法，在训练大模型时表现出优于 AdamW 的内存效率和性能。
*   **核心问题**：尽管 Lion 的收敛性（优化误差）已有研究，但其**泛化性（Generalization）**理论分析一直处于空白。
*   **研究动机**：Lion 核心依赖的 `sign`（符号）函数在梯度较小时可能导致不稳定性，从而影响模型在未见数据上的表现。本文旨在填补 Lion 泛化性分析的空白，并针对其潜在的泛化缺陷提出改进方案。

### 2. 方法论：核心思想与算法流程
*   **理论贡献**：作者首次利用**算法稳定性（Algorithmic Stability）**和数学归纳法，证明了 Lion 的泛化误差为 $O(1/(N\tau^T))$。其中 $\tau$ 是梯度估计器中非零元素的最小绝对值。由于 $\tau$ 通常极小，这意味着 Lion 的泛化误差可能随迭代次数 $T$ 指数级放大。
*   **核心思想（CLion）**：提出“谨慎（Cautious）”策略。CLion 不再盲目对所有梯度取 `sign`，而是引入一个阈值 $\nu$。
*   **关键技术细节**：
    *   **更新规则**：在每一步迭代中，计算梯度估计器 $c_t$。
    *   **条件判断**：如果 $c_t$ 中所有非零元素的最小绝对值 $\ge \nu$，则执行 Lion 的符号更新：$w_t = w_{t-1} - \eta(\text{sign}(c_t) + \lambda w_{t-1})$。
    *   **回退机制**：如果梯度过小（$<\nu$），则回退到类似 SGD 的线性更新：$w_t = w_{t-1} - \eta(c_t + \lambda w_{t-1})$。
*   **理论优势**：证明了 CLion 的泛化误差降低至 $O(1/N)$，达到了与 SGD 相同的最优泛化水平，同时保持了 Lion $O(\sqrt{d}/T^{1/4})$ 的快速收敛率。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **语言建模**：在 WikiText-2（7层 Transformer）和 WikiText-103（21层 Transformer）上进行测试。
    *   **图像分类**：在 CIFAR-10（ResNet18）和 Tiny-ImageNet（ResNet34）上进行测试。
*   **Benchmark 与对比方法**：
    *   对比了主流手工设计优化器：**SGD、SGDM、Adam、AdamW**。
    *   对比了 Lion 及其变体：**Lion** 原型和 **RLion**（使用 arctan 代替 sign 的变体）。
*   **调参策略**：对所有算法进行了网格搜索（Grid Search）以确定最佳超参数，确保对比的公平性。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件资源（如 GPU 型号、数量）以及具体的训练总时长。
*   **实现细节**：提到了 Batch Size（语言任务为 10，图像任务为 64）以及各算法的具体学习率和动量参数设置。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了计算机视觉（CV）和自然语言处理（NLP）两大核心领域，使用了从小型（ResNet18）到中型（21层 Transformer）的不同模型。
*   **充分性评价**：实验设计较为充分。通过训练损失、测试损失、困惑度（Perplexity）和准确率等多个指标全面评估了性能。
*   **客观性**：对比了多种基准算法，并提供了训练曲线和测试曲线的对比，直观展示了 CLion 在泛化阶段（测试集）的优势。

### 6. 主要结论与发现
*   **泛化性提升**：CLion 在所有测试任务中的测试误差（Test Loss/Accuracy）均优于 Lion 和 RLion，验证了理论中 $O(1/N)$ 泛化误差的优越性。
*   **收敛速度**：CLion 保持了 Lion 的快速收敛特性，在训练初期的下降速度明显快于 AdamW 和 SGD。
*   **副产品发现**：理论推导过程中发现 SignSGD 具有与 Lion 相同的泛化误差特性。
*   **阈值作用**：通过谨慎处理小梯度，CLion 有效缓解了 Lion 可能存在的梯度爆炸或过度更新问题。

### 7. 优点
*   **理论完备**：填补了 Lion 优化器泛化性分析的理论空白，且证明过程严谨。
*   **算法简洁**：CLion 仅通过简单的阈值判断逻辑便实现了显著的泛化增强，易于在现有深度学习框架中集成。
*   **性能卓越**：在视觉和语言任务中均表现出比原始 Lion 更好的鲁棒性和测试集表现。

### 8. 不足与局限
*   **超参数敏感性**：CLion 引入了新的超参数 $\nu$（阈值），虽然文中给出了实验设置（如 $10^{-6}$ 到 $10^{-15}$），但不同任务间跨度较大，可能需要额外的调参成本。
*   **算力信息缺失**：未提供训练效率（如每步耗时）的对比，无法直观判断增加条件判断对实际训练速度的影响。
*   **超大规模验证不足**：实验主要集中在 ResNet 和中型 Transformer，尚未在千亿级参数的大语言模型（LLM）上验证其稳定性。

（完）
