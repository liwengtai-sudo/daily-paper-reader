---
title: "MedVeriSeg: Teaching MLLM-Based Medical Segmentation Models to Verify Query Validity Without Extra Training"
title_zh: MedVeriSeg：教导基于 MLLM 的医学分割模型在无需额外训练的情况下验证查询有效性
authors: "Ziqian Lu, Qinyue Tong, Jun Liu, Yunlong Yu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10242v2"
tags: ["query:ai-pm"]
score: 9.0
evidence: 基于多模态大模型的医学图像分割与验证
tldr: "针对基于多模态大模型（MLLM）的医疗影像分割模型在面对不存在的目标（虚假查询）时容易产生幻觉掩码的问题，本文提出了MedVeriSeg。这是一个无需额外训练的验证框架，通过分析[SEG]标记与图像特征之间的相似度图分布特征（强度、紧凑性、纯度），并结合GPT-4o的视觉评估，使模型能够有效识别并拒绝虚假查询，提升了临床应用的可靠性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10242v2/fig-001.webp\", \"caption\": \"Figure 1: Distribution patterns of similarity heat maps under true-query and false-query cases. We observe that when the queried\", \"page\": 2, \"index\": 1, \"width\": 1027, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2604.10242v2/fig-002.webp\", \"caption\": \"Figure 4: Qualitative analysis of the Similarity Response Quality Scoring Module.\", \"page\": 7, \"index\": 2, \"width\": 975, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2604.10242v2/fig-003.webp\", \"caption\": \"Figure 2: Overview of the MedVeriSeg framework. (a) Detailed inference pipeline for the false-query case. (b) Simplified inference\", \"page\": 4, \"index\": 3, \"width\": 975, \"height\": 619}, {\"url\": \"assets/figures/arxiv/2604.10242v2/fig-004.webp\", \"caption\": \"Figure 3: Three-dimensional point distributions from different viewpoints.\", \"page\": 5, \"index\": 4, \"width\": 1026, \"height\": 306}]"
motivation: 现有的医疗影像分割模型无法识别虚假查询，常对不存在的目标生成错误的分割掩码，限制了其实际应用的可靠性。
method: "提出一种无需训练的框架，通过相似度响应质量评分模块分析[SEG]特征图的分布，并结合GPT-4o进行最终的视觉验证。"
result: 在SA-Med2D-20M构建的基准测试上，该方法能有效拒绝虚假查询，同时保持对真实查询的可靠识别。
conclusion: MedVeriSeg在不增加训练成本的前提下，显著增强了医疗分割模型对查询有效性的验证能力，减少了模型幻觉。
---

## 摘要
尽管基于多模态大语言模型（MLLM）的医学图像分割近期取得了进展，但现有的类 LISA 方法无法可靠地拒绝错误查询，且经常为不存在的目标生成幻觉分割掩码。这一局限性降低了其在医学教育和临床应用中的实际可靠性。在这项工作中，我们提出了 MedVeriSeg，这是一个无需训练的验证框架，使类 LISA 医学分割模型具备识别并拒绝包含不存在目标的错误查询的能力。我们的关键观察是，[SEG] 标记（token）特征与 MLLM 图像特征之间的相似度图在真实查询和错误查询中表现出显著不同的分布模式。基于此，我们引入了一个相似度响应质量评分模块，从强度、紧凑性和纯度三个方面对相似度图进行表征，从而生成初步的目标存在性预测。我们进一步结合定性视觉证据，利用 GPT-4o 共同评估相似度热力图和相似度响应质量评分模块的结果，以进行最终验证。在基于 SA-Med2D-20M 构建的小规模基准测试上的实验表明，MedVeriSeg 能够有效拒绝错误查询的分割请求，同时保持对真实查询的可靠识别。

## Abstract
Despite recent advances in MLLM-based medical image segmentation, existing LISA-like methods cannot reliably reject false queries and often produce hallucinated segmentation masks for absent targets. This limitation reduces practical reliability in both medical education and clinical use. In this work, we propose MedVeriSeg, a training-free verification framework that equips LISA-like medical segmentation models with the ability to identify and reject false queries which contain non-existent targets. Our key observation is that the similarity map between the [SEG] token feature and MLLM image features exhibits markedly different distribution patterns for true and false queries. Based on this, we introduce a Similarity Response Quality Scoring Module that characterizes the similarity map from three aspects: strength, compactness, and purity, producing an initial target-existence prediction. We further incorporate qualitative visual evidence by using GPT-4o to jointly assess the similarity heatmap and the results of Similarity Response Quality Scoring Module for final verification. Experiments on a small-scale benchmark constructed from SA-Med2D-20M show that MedVeriSeg effectively rejects false-query segmentation requests while maintaining reliable recognition of true queries.

---

## 论文详细总结（自动生成）

这是一份关于论文《MedVeriSeg: Teaching MLLM-Based Medical Segmentation Models to Verify Query Validity Without Extra Training》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于多模态大模型（MLLM）的医学图像分割模型（如类 LISA 结构）存在严重的“幻觉”问题。当用户输入一个图像中并不存在的解剖结构或病灶（虚假查询）时，模型无法拒绝请求，而是会强行生成一个错误的分割掩码。
*   **研究背景**：在临床和医学教育中，这种幻觉可能导致误诊或误导学生。现有的重训练方法成本高且泛化性差。
*   **核心发现**：作者发现 MLLM 输出的 `[SEG]` 标记（用于触发分割的特殊 token）与图像特征之间的**相似度热力图**在真实查询和虚假查询下具有截然不同的分布特征：真实查询的响应集中且强烈，虚假查询的响应则弥散、微弱且杂乱。

### 2. 方法论：MedVeriSeg 框架
MedVeriSeg 是一个**无需训练（Training-free）**的验证框架，核心流程如下：

*   **相似度响应质量评分模块 (SRQSM)**：从三个定量维度分析 `[SEG]` token 与图像特征的相似度矩阵 $M$：
    1.  **响应强度 (Strength)**：计算前 $k$ 个高响应点的平均值，衡量目标信号是否显著优于背景。
    2.  **空间紧凑性 (Compactness)**：通过计算激活区域的加权中心和扩散程度，衡量响应是集中在单一区域还是散布在全图。
    3.  **区域纯度 (Purity)**：计算最大连通分量的能量占比，衡量响应是否由单一连通区域主导。
*   **GPT-4o 综合评估**：
    *   将上述三个量化得分与相似度热力图共同输入 GPT-4o。
    *   利用精心设计的 Prompt，引导 GPT-4o 结合定量分数和定性视觉特征（如热力图的形状、分布）做出最终的“存在/不存在”判定。
*   **级联决策**：只有当 SRQSM 的三个指标均超过阈值且 GPT-4o 判定为真时，才允许下游解码器（如 MedSAM）执行分割，否则直接拒绝请求。

### 3. 实验设计
*   **数据集**：基于大规模医学影像数据集 **SA-Med2D-20M** 构建了一个专门的验证集。
    *   包含 478 个正样本（目标存在）和 940 个负样本（目标不存在，通过手动修改标签生成）。
    *   划分为阈值选择集（400 样本）和测试集（1018 样本）。
*   **基准模型 (Baseline)**：以开源的医学推理分割模型 **MediSee** 为主要基准，并在 **MedPLIB** 上验证了迁移性。
*   **对比方法**：
    *   随机验证（Random Verification）。
    *   人工验证（Human Verification）。
    *   直接使用 MLLM（如 LLaVA-Med, Qwen2.5-VL, GPT-4o）进行文本判断。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A6000** GPU。
*   **显存占用**：约 19 GB。
*   **推理效率**：单张图像推理时间约 **2 秒**。
*   **成本**：每张图像消耗约 300 个 GPT token。
*   **训练时长**：由于是 **Training-free** 方法，**无需任何训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主实验（查询有效性验证准确率对比）。
    *   **消融实验**：分别针对不同的后端 MLLM（DeepSeek-Janus-Pro, Qwen2.5-VL 等）和 SRQSM 的三个评分维度进行了消融分析。
    *   **跨模型验证**：在 MediSee 之外，还测试了 MedPLIB 模型。
*   **充分性评价**：实验设计较为客观，通过定量指标（Acc）和定性热力图可视化证明了方法的有效性。虽然数据集规模属于“小规模”（约 1400 样本），但对于验证“无需训练”框架的有效性而言已具备说服力。

### 6. 主要结论与发现
*   **有效性**：MedVeriSeg 在测试集上的总准确率达到 **85.3%**，显著优于直接使用 MLLM 判断（约 76%-82%），且接近人工验证水平（90.2%）。
*   **协同效应**：SRQSM 模块与 GPT-4o 的结合至关重要，单纯依靠定量评分或单纯依靠 GPT 视觉判断的效果均不如两者结合。
*   **通用性**：该方法不依赖于特定模型参数，可直接应用于任何类 LISA 结构的分割模型。

### 7. 优点与亮点
*   **零成本部署**：无需重新训练模型，不增加可学习参数，极大地降低了解决幻觉问题的门槛。
*   **可解释性强**：通过强度、紧凑性、纯度三个物理意义明确的指标来解释模型为何拒绝某个查询。
*   **资源节省**：在识别出虚假查询后直接终止后续复杂的分割解码计算，节省了计算资源。

### 8. 不足与局限
*   **依赖外部 API**：最终判定依赖于 GPT-4o 等闭源模型，存在隐私风险和 API 调用成本。
*   **阈值敏感性**：SRQSM 的三个阈值（强度、紧凑性、纯度）需要根据特定数据集或模型进行初步校准，可能存在泛化偏差。
*   **极端情况挑战**：对于极小病灶或弥漫性病变，其相似度图可能天然不满足“紧凑性”或“纯度”要求，导致误报（False Negative）。
*   **数据集局限**：实验主要基于 2D 影像，尚未探讨在 3D 医疗影像（如 CT/MRI 序列）中的表现。

（完）
