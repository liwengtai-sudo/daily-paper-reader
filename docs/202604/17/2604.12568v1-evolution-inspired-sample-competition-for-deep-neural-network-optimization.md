---
title: Evolution-Inspired Sample Competition for Deep Neural Network Optimization
title_zh: 受进化启发的深度神经网络优化样本竞争机制
authors: "Ying Zheng, Yiyi Zhang, Yi Wang, Lap-Pui Chau"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12568v1"
tags: ["query:dl"]
score: 9.0
evidence: 受进化启发的深度网络训练优化
tldr: 针对深度学习中样本均匀处理导致的类别不平衡、难样本学习不足及噪声干扰等问题，本文提出受进化论启发的“自然选择”（NS）优化方法。该方法通过将多个样本组合成复合图像，在组内竞争语境下评估样本的竞争状态，并据此动态调整样本损失权重。实验证明，NS在多种分类任务和架构上均表现优异，具有很强的通用性和实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12568v1/fig-001.webp\", \"caption\": \"TABLE III COMPARISON OF BASIC AND NS-ENHANCED MODELS ON SIX EMOTION RECOGNITION DATASETS.\", \"page\": 7, \"index\": 1, \"width\": 1062, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2604.12568v1/fig-002.webp\", \"caption\": \"TABLE IV COMPARISON OF DIFFERENT METHODS ON THE OFFICE-HOME BENCHMARK.\", \"page\": 7, \"index\": 2, \"width\": 1058, \"height\": 384}, {\"url\": \"assets/figures/arxiv/2604.12568v1/fig-003.webp\", \"caption\": \"Fig. 4. Correlation analysis on the CIFAR-LT-10/100 and Office-Home datasets. Blue points represent the individual categories, while the red line depicts the fitted linear regression.\", \"page\": 10, \"index\": 3, \"width\": 1084, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2604.12568v1/fig-004.webp\", \"caption\": \"TABLE V PERFORMANCE COMPARISON ON THE CIFAR-LT-10/100 DATASETS.\", \"page\": 8, \"index\": 4, \"width\": 514, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2604.12568v1/fig-005.webp\", \"caption\": \"Fig. 1. (a) Natural selection in ecosystem evolution. (b) Illustration of the natural selection process in training samples. After performing image stitching and scale normalization, the resulting stitched image is fed into the classifier to obtain the softmax probability distribution. The predicted probability associated with each sample’s category is then extracted, and the NS score is subsequently computed through normalization.\", \"page\": 4, \"index\": 5, \"width\": 962, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2604.12568v1/fig-006.webp\", \"caption\": \"Fig. 3. Visualization of NS score distributions by category on the CIFAR-10 and FI datasets. Red points denote training samples, and the blue solid line within each box indicates the median NS score of all samples in that class.\", \"page\": 9, \"index\": 6, \"width\": 993, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2604.12568v1/fig-007.webp\", \"caption\": \"Fig. 2. The distribution curves of sample weights under different settings. Under the NS-WS (green dashed line) and NS-LF (purple dashed line) strategies, samples with higher-weight values are reinforced during training, while those in lower-weight regions are suppressed. This enables differentiated optimization for samples with varying competitive characteristics.\", \"page\": 5, \"index\": 7, \"width\": 522, \"height\": 273}]"
motivation: 传统训练忽略了样本间的异质竞争，导致模型在处理不平衡数据、难样本和噪声数据时表现不佳。
method: 提出NS方法，通过将多样本合成复合图像并基于预测结果计算竞争得分，从而动态调节各样本的训练权重。
result: 在12个公开数据集和4类图像分类任务上的广泛实验验证了该方法的有效性。
conclusion: NS提供了一种简单高效的非均匀样本处理机制，具有良好的架构兼容性和任务通用性。
---

## 摘要
传统的深度网络训练通常在一种基本统一的学习范式下优化所有样本，而没有显式地建模样本之间异质的竞争关系。这种过度简化的处理方式会导致几个众所周知的问题，包括类别不平衡下的偏差、对困难样本的学习不足，以及对噪声样本的错误强化。在这项工作中，我们提出了“自然选择”（Natural Selection, NS），这是一种新型的受进化启发的优化方法，它将竞争性交互显式地引入到深度网络训练中。与主要依赖预定义启发式方法或静态标准的传统样本重加权策略不同，NS 在组级上下文中估计每个样本的竞争状态，并利用该状态自适应地调节其训练贡献。具体而言，NS 首先将多个样本组合成一张复合图像，并将其缩放至原始输入尺寸进行模型推理。根据得到的预测结果，为每个样本计算一个自然选择得分，以表征其在所构建组内的相对竞争变化。随后，这些得分被用于动态地对样本损失进行重加权，从而在优化过程中引入了一种显式的竞争驱动机制。通过这种方式，NS 提供了一种简单而有效的方法，超越了统一的样本处理模式，并实现了更具自适应性和平衡性的模型优化。在四个图像分类任务的 12 个公开数据集上进行的广泛实验证明了所提方法的有效性。此外，NS 与多种网络架构兼容，且不依赖于特定任务的假设，显示出其强大的通用性和应用潜力。代码将向社会公开。

## Abstract
Conventional deep network training generally optimizes all samples under a largely uniform learning paradigm, without explicitly modeling the heterogeneous competition among them. Such an oversimplified treatment can lead to several well-known issues, including bias under class imbalance, insufficient learning of hard samples, and the erroneous reinforcement of noisy samples. In this work, we present \textit{Natural Selection} (NS), a novel evolution-inspired optimization method that explicitly incorporates competitive interactions into deep network training. Unlike conventional sample reweighting strategies that rely mainly on predefined heuristics or static criteria, NS estimates the competitive status of each sample in a group-wise context and uses it to adaptively regulate its training contribution. Specifically, NS first assembles multiple samples into a composite image and rescales it to the original input size for model inference. Based on the resulting predictions, a natural selection score is computed for each sample to characterize its relative competitive variation within the constructed group. These scores are then used to dynamically reweight the sample-wise loss, thereby introducing an explicit competition-driven mechanism into the optimization process. In this way, NS provides a simple yet effective means of moving beyond uniform sample treatment and enables more adaptive and balanced model optimization. Extensive experiments on 12 public datasets across four image classification tasks demonstrate the effectiveness of the proposed method. Moreover, NS is compatible with diverse network architectures and does not depend on task-specific assumptions, indicating its strong generality and practical potential. The code will be made publicly available.

---

## 论文详细总结（自动生成）

这篇论文提出了一种受进化论启发的深度学习优化方法——**自然选择（Natural Selection, NS）**。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的深度神经网络训练通常采用经验风险最小化（ERM），对所有训练样本施加统一的选择压力（即权重相同）。
*   **研究动机**：这种“一视同仁”的策略忽略了样本之间内在的竞争关系，导致模型在处理**类别不平衡**、**困难样本学习不足**以及**标签噪声干扰**时表现不佳。作者认为，网络优化过程可以类比为生态系统的进化，通过引入显式的样本竞争机制，可以实现更具自适应性的模型训练。

### 2. 方法论：自然选择（NS）优化
*   **核心思想**：通过将样本分组并拼接成复合图像，模拟自然界中的组内竞争，根据模型对复合图像的预测反馈来评估每个样本的“竞争状态”，进而动态调整其损失权重。
*   **关键技术流程**：
    1.  **样本分组与拼接**：从 mini-batch 中选取 $m$ 个样本（如 4 个），拼接成一个复合图像并缩放至原始输入尺寸。
    2.  **竞争得分计算**：将复合图像输入当前模型进行前向推理（不产生梯度），提取各样本对应类别的预测概率。
    3.  **NS Score 归一化**：计算每个样本在组内的相对得分 $s_i$（即该样本预测概率占组内总概率的比例）。
    4.  **动态重加权策略**：
        *   **强者强化（NS-WS）**：增加高分样本权重，适用于存在标签噪声、需要稳定决策边界的场景（如 ImageNet、域自适应）。
        *   **弱者关注（NS-LF）**：增加低分样本权重，适用于长尾分布或需要挖掘难样本的场景（如情感识别、长尾分类）。
    5.  **公式表达**：最终样本权重 $w_i = \sigma + \rho \cdot s_i$，其中 $\sigma$ 是保证训练稳定的基准值，$\rho$ 决定强化方向。

### 3. 实验设计
*   **数据集与场景**：涵盖 12 个公开数据集，包括通用分类（CIFAR-10/100, ImageNet-1K）、视觉情感识别（Twitter I/II 等 6 个）、无源域自适应（Office-Home）和长尾分类（CIFAR-LT）。
*   **Benchmark 与对比方法**：
    *   **损失函数类**：Focal Loss, Label Smoothing, PolyLoss, GCE, SCE 等 12 种。
    *   **采样策略类**：类别平衡采样（CBS）、平方根采样（SRS）等。
    *   **骨干网络**：涵盖 CNN（ResNet, VGG）、Transformer（ViT, Swin）以及状态空间模型（VMamba）。

### 4. 资源与算力
*   **硬件环境**：实验主要在 **NVIDIA RTX 4090 GPU** 上进行。
*   **算力开销**：
    *   **显存**：NS 机制仅增加约 **1.5%** 的额外显存开销。
    *   **时间**：由于每个 batch 需要额外的一次前向推理，训练时间增加了约 **10.2% 至 12.7%**。
    *   **训练时长**：ImageNet-1K 训练 90 epoch，Swin-T 和 VMamba-T 采用了 4 张 4090 并行训练。

### 5. 实验数量与充分性
*   **实验规模**：论文在 12 个数据集上进行了广泛测试，涵盖了从小型（CIFAR）到大规模（ImageNet）的规模跨度。
*   **消融实验**：对分组策略（1x2 到 4x4）、超参数 $\sigma$、策略极性 $\rho$ 进行了详细讨论。
*   **客观性与公平性**：所有实验均取 3 次独立运行的平均值，并提供了标准差分析。此外，还通过 ImageNet-Hard 数据集验证了方法对难样本的鲁棒性，并扩展到了文本分类任务（IMDB, AG News），实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能提升**：NS 机制在所有 12 个数据集上均实现了性能提升，且具有很强的架构通用性。
*   **策略选择**：在平衡但有噪声的数据集上，强化“强者”能过滤噪声；在极度不平衡的数据集上，关注“弱者”能提升少数类性能。
*   **竞争相关性**：分析发现 NS Score 与样本规模、伪标签质量呈正相关，证明了该得分能有效反映样本的竞争优势。

### 7. 优点
*   **视角新颖**：将生物进化论中的竞争机制转化为数学可计算的样本权重，理论支撑直观。
*   **即插即用**：作为一种训练辅助模块，不需要修改模型架构，也不影响推理阶段的效率。
*   **高度通用**：实验证明其在 CNN、Transformer 甚至 Mamba 架构上均有效，且能跨模态应用于文本任务。

### 8. 不足与局限
*   **训练耗时增加**：虽然显存友好，但额外的前向推理步骤确实延长了训练周期（约 10% 以上）。
*   **超参数敏感性**：基准值 $\sigma$ 在不同任务间存在差异，可能需要针对新任务进行一定的调优。
*   **任务覆盖**：目前主要集中在分类任务，对于目标检测、分割或生成任务的有效性尚待验证。

（完）
