---
title: Working Memory in a Recurrent Spiking Neural Networks With Heterogeneous Synaptic Delays
title_zh: 具有异质突触延迟的循环脉冲神经网络中的工作记忆
authors: Laurent U Perrinet
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14096v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于序列时间模式的循环脉冲神经网络
tldr: 本研究针对脉冲神经网络（SNN）在存储和召回精确时序模式方面的挑战，提出了一种具有异构突触延迟的循环SNN模型。通过为每个突触配置多个延迟参数并使用代理梯度随时间反向传播算法进行端到端训练，网络能将目标脉冲模式表示为连续的“脉冲基元”链。实验证明该方法能以极高准确率存储并召回多个复杂模式，为神经形态边缘计算提供了高效的工作记忆实现方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14096v1/fig-001.webp\", \"caption\": \"Figure 1: Working memory as sequential spike prediction via heterogeneous delays. Left: A recurrent HD-SNN of five neurons numbered 1–5, connected by two subsets of recurrent connections that define two Spiking Motifs from the presynaptic side (neurons a) to the postsynaptic side (neurons b): dark-red connections from a2, a3, a4, and a5 target b1 with delays 4, 9, 6, 3ms; green connections from a1, a2, a3, a4 target b5 with delays 1, 6, 3, 8ms. Right: Raster plot illustrating how the network predicts successive spikes from a past context window. Step 1 — first prediction (green box): the orange input spikes emitted at staggered times by a2–a5 within the first context window (dashed green box) with precise interspike intervals. These are used to define a heterogeneous Spiking Motif such that the (green) delays of all four spikes converge synchronously at b5, crossing the firing threshold and producing the green output spike. Step 2 — second prediction (dark-red box): the green spike of a5 re-enters the network as a new input and, together with the spikes now visible in the shifted context window (dashed dark-red box), is routed through the dark-red delays in order to converge synchronously on b1, generating its output spike. Because output spikes re-enter the same population, detecting one Spiking Motif provides the context for predicting the next spike in the sequence, implementing working memory as a chain of temporal predictions grounded in the preceding context window.\", \"page\": 2, \"index\": 1, \"width\": 829, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2604.14096v1/fig-002.webp\", \"caption\": \"Figure 2: Network architecture and a sample target pattern. Left: Unrolled view of the recurrent HDSNN over time (one comlumn representing one time step), illustrating delayed connections between neurons: Blue arrows indicate synaptic connections that integrate past activity across heterogeneous delays; for clarity only one delay is shown. Right: One representative target spike pattern (N = 512 neurons, T = 1000 time steps, of which we show 256 neurons and 400 time steps); each red circle marks one target spike. The goal of the network is to memorise M = 16 such patterns, initialised by clamping all neuron activities to the target during the first D = 41 time steps (as denoted by blue crosses in the region left of the dashed red line).\", \"page\": 5, \"index\": 2, \"width\": 829, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.14096v1/fig-003.webp\", \"caption\": \"Figure 3: Pattern retrieval from the recurrent HD-SNN. The raster plot shows network activity (blue crosses, N = 512 neurons) versus one of the 16 target pattern (red circles) after training. Correct recall emerges first in the time steps immediately following the clamped initialisation window (0 ≤ t < D = 41, delimited by the dashed red line) and progressively extends to the full sequence duration (T = 1000 steps).\", \"page\": 6, \"index\": 3, \"width\": 829, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.14096v1/fig-004.webp\", \"caption\": \"Figure 4: Effect of three key parameters on working-memory performance (loss L = 1− F1, mean ± s.e. over Ncv = 10 cross-validation folds). Left: Increasing the maximum delay D consistently reduces the loss, confirming that deeper delay windows provide a larger, more orthogonal context and that longer maximum delays directly increase the capacity of the recurrent HD-SNN. Middle: Loss grows slowly and monotonically with pattern duration T , consistent with the compounding-error and gradient-vanishing arguments: longer sequences require the network to maintain coherent predictions further from the clamped initialisation window. Right: Performance is optimal at low background firing rates (pA ≲ 10−3) and degrades sharply at higher rates.\", \"page\": 7, \"index\": 4, \"width\": 829, \"height\": 275}]"
motivation: 解决脉冲神经网络在存储和精确召回复杂时序神经活动模式（工作记忆）方面的难题。
method: 提出一种具有异构突触延迟的循环SNN，通过将突触建模为权重张量并利用代理梯度训练，将时序模式转化为重叠的脉冲基元链。
result: 在包含16个模式的合成基准测试中，该模型在512个神经元规模下达到了1.0的平均F1分数，实现了完美的模式召回。
conclusion: 异构突触延迟为SNN提供了高效的工作记忆基础，显著提升了神经形态硬件在边缘端处理时序任务的能力。
---

## 摘要
工作记忆——即存储和召回精确神经活动时间模式的能力——仍然是脉冲神经网络（SNN）面临的一个开放性挑战。我们提出了一种由 $N$ 个神经元组成的循环 SNN，其中每个突触都配备了 $D = 41$ 个延迟，建模为权重张量 $\mathbf{W} \in \mathbb{R}^{N \times N \times D}$，并使用代理梯度随时间反向传播进行端到端训练。该网络通过将 $M$ 个任意目标脉冲模式表示为重叠“脉冲基元”（Spiking Motifs）的序列链来存储它们：这些基元是长度为 $D$ 的连续窗口，能够唯一地预测下一个时间步的脉冲。在一个包含 $M=16$ 个模式（$N=512$ 个神经元，$T=1000$ 个时间步）的合成基准测试中，训练达到了 1.0 的平均 F1 分数，召回首先在被钳位的初始化窗口附近出现，并随时间向前传播。这一结果表明，异质延迟为 SNN 中的工作记忆提供了一种高效的基质，从而实现了高能效的类脑边缘部署。

## Abstract
Working memory -- the ability to store and recall precise temporal patterns of neural activity -- remains an open challenge for spiking neural networks (SNNs). We propose a recurrent SNN of $N$ neurons in which each synapse is equipped with $D = 41$ delays, modelled as a weight tensor $\mathbf{W} \in \mathbb{R}^{N \times N \times D}$ and trained end-to-end with surrogate-gradient backpropagation through time. The network stores $M$ arbitrary target spike patterns by representing each as a sequential chain of overlapping Spiking Motifs: contiguous windows of length $D$ that uniquely predict spikes at the next time step. On a synthetic benchmark of $M=16$ patterns ($N=512$ neurons, $T=1000$ steps), training achieves a mean F1 score of $1.0$, with recall emerging first near the clamped initialisation window and propagating forward in time. This result demonstrates that heterogeneous delays provide an efficient substrate for working memory in SNNs, enabling energy-efficient neuromorphic edge deployment.

---

## 论文详细总结（自动生成）

这篇论文提出了一种利用**异质突触延迟（Heterogeneous Synaptic Delays）**在循环脉冲神经网络（Recurrent SNN）中实现工作记忆的新方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：在脉冲神经网络（SNN）中，如何存储和精确召回跨越长时间尺度（远超单个神经元时间常数）的时序脉冲模式是一个长期存在的难题。
*   **研究动机**：传统人工神经网络（如 Transformer 或 GRU）通过特定架构解决长程依赖，但 SNN 缺乏通用的生物学合理解。作者受到生物学中“多时相同步”（Polychronization）和“脉冲基元”（Spiking Motifs）概念的启发，试图证明**突触延迟的异质性**（而非仅仅是权重）是实现高效工作记忆的关键。
*   **整体含义**：通过引入可学习的多重突触延迟，SNN 可以将复杂的时序序列转化为一系列相互衔接的预测任务，从而在极低能耗下实现高容量的序列存储。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将每个突触建模为具有 $D$ 个不同延迟通道的集合。网络不再仅仅依赖当前的脉冲，而是通过一个长度为 $D$ 的“上下文窗口”来预测下一时刻的脉冲。
*   **关键技术**：
    *   **HD-SNN 架构**：构建了一个全连接的循环网络，包含 $N$ 个 Leaky Integrate-and-Fire (LIF) 神经元。每个神经元对之间的连接由一个权重张量 $\mathbf{W} \in \mathbb{R}^{N \times N \times D}$ 表示，其中 $D$ 是最大延迟步数。
    *   **脉冲基元链（Chain of Motifs）**：将长序列分解为重叠的短窗口（长度为 $D$）。网络学习的是：给定过去 $D$ 步的脉冲分布，如何通过不同延迟的组合使电流在胞体处同步汇聚，从而触发目标神经元在精确时刻放电。
    *   **训练算法**：使用**代理梯度随时间反向传播（BPTT）**进行端到端训练。
    *   **初始化策略**：提出了一种基于**赫布学习（Hebbian）原理的解析初始化方法**（利用伪逆矩阵或互相关），为梯度下降提供良好的起点。

### 3. 实验设计
*   **数据集/场景**：使用合成的随机脉冲模式数据集。每个模式包含 $N=512$ 个神经元在 $T=1000$ 毫秒内的稀疏放电活动（放电频率约 2Hz）。
*   **Benchmark 与任务**：
    *   任务是存储 $M=16$ 个不同的任意目标模式。
    *   **记忆检索方式**：在初始的 $D$ 个时间步内“钳位”（Clamping）输入目标模式作为诱导信号，随后让网络自主循环运行，观察其能否召回剩余的序列。
*   **对比与分析**：通过扫描最大延迟 $D$、序列长度 $T$ 和背景放电率 $p_A$ 来评估模型容量和鲁棒性。

### 4. 资源与算力
*   **硬件环境**：使用了配备 Apple M3 Ultra 芯片（32核 CPU, 80核 GPU, 512GB 统一内存）的设备，并在 GENCI 的 Jean Zay 超级计算机集群（CUDA 后端）上进行了进一步测试。
*   **训练时长**：训练通常在 4096 个梯度步内收敛，单次训练耗时约 **10 分钟**。
*   **软件框架**：基于 `snnTorch` 和 `PyTorch` 开发。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了系统性的参数扫描（One-at-a-time scan），每组实验重复 10 次（10-fold cross-validation）以确保统计显著性。
*   **充分性评价**：
    *   **优点**：实验涵盖了影响 SNN 性能的核心变量（延迟深度、序列长度、稀疏度），验证了模型在不同负载下的稳定性。
    *   **局限**：实验主要集中在合成数据上，尚未在真实的大规模神经科学数据集或复杂的视觉/音频流任务上进行广泛验证。

### 6. 主要结论与发现
*   **完美召回**：在 $N=512, M=16$ 的配置下，模型达到了 **1.0 的 F1 分数**，实现了对复杂时序模式的无误差存储与召回。
*   **延迟的重要性**：增加最大延迟 $D$ 能显著降低损失并提升容量。$D$ 越大，上下文窗口的维度越高，模式之间的正交性越好，干扰越小。
*   **学习动态**：召回能力表现出“时间传播”特性，即网络先学会预测紧随初始化窗口之后的脉冲，然后逐渐向序列后期延伸。

### 7. 优点（亮点）
*   **高参数效率**：利用 $N^2 \times D$ 的参数量存储了极高维度的时序信息，证明了延迟是计算资源而非生物噪声。
*   **生物一致性**：结合了 polychronization 理论与现代深度学习优化方法，弥补了传统模型（如 Izhikevich 模型）中延迟不可学习的缺陷。
*   **硬件友好**：稀疏放电和离散延迟的设计非常适合在神经形态芯片（如 Loihi）上进行低功耗边缘部署。

### 8. 不足与局限
*   **任务单一性**：目前仅验证了“存储与召回”这一静态记忆任务，尚未涉及工作记忆中的“操纵”（Manipulation）或泛化能力。
*   **误差累积风险**：作为一种自回归式的预测模型，早期时间步的微小错误可能会随时间放大，导致长序列后期崩溃（虽然在 $T=1000$ 时表现良好，但更长序列的稳定性待考）。
*   **监督依赖**：目前的训练依赖于精确的目标脉冲标签，未来需要探索无监督或自监督的学习方案（如预测编码）。

（完）
