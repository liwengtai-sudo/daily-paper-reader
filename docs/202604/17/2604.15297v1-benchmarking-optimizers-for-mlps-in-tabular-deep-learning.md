---
title: Benchmarking Optimizers for MLPs in Tabular Deep Learning
title_zh: 表格深度学习中 MLP 优化器的基准测试
authors: "Yury Gorishniy, Ivan Rubachev, Dmitrii Feoktistov, Artem Babenko"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15297v1"
tags: ["query:dl"]
score: 9.0
evidence: 深度学习模型优化器的基准测试
tldr: 本研究针对表格数据深度学习中MLP模型的优化器选择进行了系统性基准测试。研究者在10个数据集上对比了10种优化器，旨在打破AdamW作为表格DL默认优化器的现状。实验发现Muon优化器在性能上持续优于AdamW，同时权重指数移动平均（EMA）也能有效提升基础MLP的训练效果。该研究为表格深度学习从业者提供了新的优化策略参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15297v1/fig-001.webp\", \"caption\": \"Table 7: The hyperparameter tuning space for TabM-packed. Here, (B) = {Microsoft and TabRed datasets} and (A) contains all other datasets.\", \"page\": 11, \"index\": 1, \"width\": 540, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.15297v1/fig-002.webp\", \"caption\": \"Table 5: The hyperparameter tuning space for MLP†.\", \"page\": 11, \"index\": 2, \"width\": 540, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2604.15297v1/fig-003.webp\", \"caption\": \"Table 6: The hyperparameter tuning space for TabM. Here, (B) = {Microsoft and TabRed datasets} and (A) contains all other datasets.\", \"page\": 11, \"index\": 3, \"width\": 540, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.15297v1/fig-004.webp\", \"caption\": \"Table 9: Per-dataset results for all methods evaluated in this paper. For each dataset we report the mean test score ± standard deviation over 10 random seeds.\", \"page\": 13, \"index\": 4, \"width\": 876, \"height\": 648}, {\"url\": \"assets/figures/arxiv/2604.15297v1/fig-005.webp\", \"caption\": \"Figure 1: Comparison of optimization methods for vanilla MLP across 17 datasets. Left: mean rank across datasets (lower is better). Middle: relative improvement over AdamW (higher is better). The box plots describe the percentiles of the jitter points: the boxes describe the 25th, 50th, and 75th percentiles, and the whiskers describe the 10th and 90th percentiles. Outliers are clipped. Right: pairwise win/tie/loss counts against AdamW. Muon, Schedule-Free AdamW, and AdamW with EMA consistently outperform the AdamW baseline (highlighted in bold and purple in the figure).\", \"page\": 4, \"index\": 5, \"width\": 956, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.15297v1/fig-006.webp\", \"caption\": \"Table 1: Transfer of optimizer gains across MLP-based architectures. ∆score denotes the mean relative score improvement with respect to the MLP[AdamW] baseline. The AdamW column thus shows the gain from the architecture alone. For AdamW EMA and Muon we report the combined architecture+optimizer gain, and in parentheses the additional gain over AdamW within the same architecture. W/T/L compares each method to AdamW within the same model. The last row contains summary, which is mean for the ∆score columns and sum for the W/T/L columns. Muon is the most consistent in terms of reliability of improvement across architecture variants. EMA gains are less uniform across architectures and datasets.\", \"page\": 5, \"index\": 6, \"width\": 879, \"height\": 311}]"
motivation: 尽管新优化器在其他领域不断涌现，但表格深度学习领域仍缺乏对优化器选择的系统性评估。
method: 在统一的实验协议下，针对10个表格数据集，对10种优化器在MLP及其变体上的训练表现进行了基准测试。
result: Muon优化器在各项测试中一致优于AdamW，且EMA技术被证明能有效增强AdamW在标准MLP上的性能。
conclusion: Muon是表格深度学习中比AdamW更强且实用的优化器选择，在算力允许的情况下应优先考虑。
---

## 摘要
MLP 是现代深度学习（DL）架构中用于表格数据监督学习的常用骨干网络，而 AdamW 是训练表格深度学习模型的首选优化器。然而，与架构设计不同，尽管新优化器在其他领域展现出潜力，但表格深度学习中优化器的选择尚未得到系统性的考察。为了填补这一空白，我们在统一的实验协议下，在 \Ndatasets 个表格数据集上对 \Noptimizers 种优化器进行了基准测试，用于在标准监督学习设置下训练基于 MLP 的模型。我们的主要发现是，Muon 优化器始终优于 AdamW，因此，在训练效率开销可接受的情况下，它应被视为从业者和研究人员的一个强有力且实用的选择。此外，我们发现模型权重的指数移动平均（EMA）是一种简单而有效的技术，能够提升 AdamW 在原生 MLP 上的表现，尽管其效果在不同模型变体之间的一致性略低。

## Abstract
MLP is a heavily used backbone in modern deep learning (DL) architectures for supervised learning on tabular data, and AdamW is the go-to optimizer used to train tabular DL models. Unlike architecture design, however, the choice of optimizer for tabular DL has not been examined systematically, despite new optimizers showing promise in other domains. To fill this gap, we benchmark \Noptimizers optimizers on \Ndatasets tabular datasets for training MLP-based models in the standard supervised learning setting under a shared experiment protocol. Our main finding is that the Muon optimizer consistently outperforms AdamW, and thus should be considered a strong and practical choice for practitioners and researchers, if the associated training efficiency overhead is affordable. Additionally, we find exponential moving average of model weights to be a simple yet effective technique that improves AdamW on vanilla MLPs, though its effect is less consistent across model variants.

---

## 论文详细总结（自动生成）

这是一份关于论文《Benchmarking Optimizers for MLPs in Tabular Deep Learning》的结构化深入总结：

### 1. 论文的核心问题与整体含义
在表格深度学习（Tabular DL）领域，多层感知机（MLP）及其变体是核心骨干网络。尽管架构设计层出不穷，但优化器的选择长期停留在 **AdamW** 这一默认选项上。
*   **研究动机**：近期在大型语言模型（LLM）和计算机视觉领域出现了许多性能卓越的新型优化器（如 Muon、SOAP 等），但在表格数据这一特殊场景（数据量有限、噪声大、依赖早停机制、关注测试集泛化而非训练集损失）中，这些优化器是否依然有效尚无定论。
*   **核心目标**：通过系统性的基准测试，打破 AdamW 的垄断地位，寻找更适合表格 DL 的优化方案。

### 2. 论文提出的方法论
该论文并非提出单一新算法，而是对现有 15 种优化技术进行了严谨的评估。
*   **核心思想**：在统一的超参数搜索空间和评估协议下，对比不同更新规则（自适应、符号化、二阶预处理等）对 MLP 泛化性能的影响。
*   **关键技术细节**：
    *   **Muon**：一种新型优化器，其核心在于对更新方向进行正交化处理（通过 Newton-Schulz 迭代），旨在更有效地利用参数空间的几何结构。
    *   **EMA（指数移动平均）**：在训练过程中对模型权重进行平滑处理，不改变训练逻辑，但在推理时使用平滑后的权重。
    *   **Schedule-Free**：一种无需预设训练步数（Horizon）即可获得学习率调度收益的技术，非常适合依赖“早停”的表格场景。
    *   **联合调优**：使用 Optuna 对模型超参数（层数、宽度、Dropout）和优化器参数（学习率、权重衰减等）进行**联合搜索**，确保对比的公平性。

### 3. 实验设计
*   **数据集**：涵盖 **17 个数据集**，包括 9 个经典学术数据集和 8 个来自 TabReD 基准的工业数据集（包含时间序列划分），涉及二分类、多分类和回归任务。
*   **模型架构**：
    1.  **Vanilla MLP**：标准 ReLU 网络。
    2.  **MLP†**：带有分段线性数值特征嵌入的增强型 MLP。
    3.  **TabM 系列**：包括 TabM、TabM Packed 等现代参数高效集成模型。
*   **对比方法**：共 15 种优化器，包括 AdamW, SGD, NAdamW, RAdam, AdaBelief, ADOPT, Adan, AdEMAMix, Cautious AdamW, Lion, Signum, SOAP, Muon, Schedule-Free AdamW, 以及带 EMA 的 AdamW。

### 4. 资源与算力
*   **训练时长**：论文明确记录了调优时间。在 17 个 MLP 调优任务中：
    *   **AdamW** 总计耗时约 **15.5 小时**。
    *   **Muon** 耗时约 **46.9 小时**（约为 AdamW 的 **3.03 倍**）。
    *   **AdamW + EMA** 耗时约 **20.0 小时**（约为 AdamW 的 **1.29 倍**）。
*   **GPU 信息**：文中未明确提及具体的 GPU 型号和数量，但从其代码库和 Yandex/HSE 的背景推测，通常使用主流数据中心级 GPU（如 A100 或 V100）。

### 5. 实验数量与充分性
*   **实验规模**：每个优化器在每个数据集上进行 **100 次迭代**的超参数调优（Optuna TPE 采样），选出最优配置后使用 **10 个不同的随机种子**进行重复实验并取平均值。
*   **客观性与公平性**：
    *   **统一协议**：所有优化器共享相同的调优预算和验证协议。
    *   **统计显著性**：使用 **Welch’s t-test**（显著性水平 0.05）来判定“胜/平/负”，而非仅仅对比平均分。
    *   **多指标评估**：引入了平均排名（Average Rank）和相对提升得分（$\Delta score$），实验设计非常充分且客观。

### 6. 论文的主要结论与发现
*   **Muon 是新标杆**：Muon 在原生 MLP 和先进的 MLP 变体（如 TabM）上均表现出**一致且显著**的优越性，是目前表格 DL 中最强的优化器。
*   **EMA 的价值**：对于原生 MLP，引入 EMA 能显著提升 AdamW 的表现，甚至在某些数据集上逼近 Muon。但在更复杂的架构（如 TabM）上，EMA 的提升效果不再稳定。
*   **Adam 家族的局限**：大多数 Adam 的变体（如 NAdamW, RAdam 等）在表格数据上相比标准 AdamW 并没有带来实质性的泛化提升。
*   **效率权衡**：Muon 虽然性能最强，但其计算开销（约 3 倍时间）是实际应用中需要权衡的因素。

### 7. 优点
*   **填补空白**：首次在表格 DL 领域对现代优化器进行了大规模、系统性的基准测试。
*   **工业相关性**：引入了 TabReD 工业数据集，使结论更具实战参考价值。
*   **严谨的调优**：避免了“默认参数陷阱”，通过联合调优确保了每个优化器都发挥了最大潜力。
*   **开源贡献**：提供了完整的代码库，方便社区复现和进一步研究。

### 8. 不足与局限
*   **架构覆盖有限**：研究集中在基于 MLP 的模型，未涵盖基于树的模型（如 GBDT 的深度变体）或基于 Transformer 的表格模型。
*   **未涉及基础模型**：实验主要针对从头训练（Train from scratch），未探讨这些优化器在表格基础模型（Tabular Foundation Models）预训练中的表现。
*   **计算成本**：Muon 的高昂计算开销可能限制其在超大规模数据集或资源受限环境下的普及。
*   **理论解释不足**：论文侧重于经验性观察（Empirical Study），对于“为什么 Muon 在表格数据上表现更好”缺乏深入的理论剖析。

（完）
