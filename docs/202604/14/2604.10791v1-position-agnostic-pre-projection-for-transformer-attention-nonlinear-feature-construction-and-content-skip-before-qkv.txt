Title: Position-Agnostic Pre-Projection for Transformer Attention: Nonlinear Feature Construction and Content Skip Before Q/K/V

URL Source: https://arxiv.org/pdf/2604.10791v1

Published Time: Tue, 14 Apr 2026 01:27:42 GMT

Number of Pages: 7

Markdown Content:
# Position-Agnostic Pre-Projection for Transformer Attention: Nonlinear Feature Construction and Content Skip Before Q/K/V 

Chirag Shinde 

Independent Researcher 

chirag.m.shinde@gmail.com 

Abstract 

We propose two complementary modifications to transformer attention blocks. First, a non-linear pre-projection MLP is inserted between layer norm and Q/K/V projections, constructing richer features in a position-agnostic manner before any positional encoding is applied. Second, a content skip connection routes the pre-projection’s features around the attention mechanism, allowing content information to bypass position-aware attention where beneficial. In frozen-probe experiments on Pythia-160M and 410M, the combined approach achieves the strongest results across methods: +40.6% LAMBADA accuracy and −39% perplexity at 160M scale. Learned skip connection weights reveal a consistent pattern across model sizes: later transformer layers activate the content bypass more strongly than earlier layers, suggesting that deeper layers benefit from content information that does not pass through positional attention. All modifications add no K/V cache overhead. 

# 1 Introduction 

Modern transformer blocks compute attention through a fixed pipeline: layer normalization, lin-ear Q/K/V projections, rotary positional encoding (RoPE) on Q and K, then scaled dot-product attention. This design has two limitations we address: 

Linear feature bottleneck. The Q/K/V projections are purely linear. If useful attention patterns depend on nonlinear feature combinations, the model must rely on previous layers’ FFNs to have constructed them—requiring multiple layers for what could be a single-layer operation. 

Mandatory positional filtering. All information reaching the output must pass through position-aware attention. Content information that is relevant regardless of position (semantic type, syntactic category, entity identity) is forced through the same positional routing as position-dependent information. We propose two modifications (Figure 1): 1. A pre-projection : a small nonlinear MLP before Q/K/V that constructs richer, position-agnostic features. 2. A content skip : a learned linear projection from the pre-projection output that bypasses attention and adds directly to the layer output. The pre-projection enriches features for attention; the content skip lets those features also bypass attention where the model learns it is beneficial. Both are position-agnostic and add no K/V cache overhead. 1

> arXiv:2604.10791v1 [cs.CL] 12 Apr 2026

# 2 Method 

2.1 Pre-Projection 

Between layer normalization and Q/K/V projections, we insert: ˆx = RMSNorm( x) (1) ˜x = ˆ x + Wdown · σ(Wup · ˆx) (2) where Wup ∈ Rd×ed , Wdown ∈ Red ×d, σ is SiLU, and e = 1 .25. The residual connection ensures near-identity initialization. 

2.2 Content Skip 

The same enriched features ˜ x are projected through a learned linear map Wskip ∈ Rd×d and added to the output after attention: 

Q, K, V = ˜ xW Q, ˜xW K , ˜xW V (3) attn out = Attention(RoPE( Q), RoPE( K), V ) (4) out = x + attn out + Wskip · ˜x (5) 

Wskip is initialized near-zero ( N (0 , 10 −4)) so the model starts identical to baseline. The skip learns per-layer how much content information should bypass positional attention. No separate mix-ing parameter (alpha) is needed—the skip projection’s weight magnitude serves this role naturally and trains via standard backpropagation. 

Key insight : The pre-projection does double duty. It enriches Q/K/V inputs (position-aware path) and provides content features that bypass attention (position-agnostic path). The model learns to route content information through whichever path is more useful at each layer. 

2.3 Parameter Overhead 

Table 1: Parameter breakdown for the full method (pre-projection + content skip, e = 1 .25). 

Model Base Pre-proj Skip Total new Overhead 

Pythia-160M 162M 17.7M 7.1M 24.8M 13.2% Pythia-410M 405M 62.9M 25.2M 88.1M 17.9% Neither the pre-projection nor the content skip adds K/V cache overhead. Both are fixed-cost per-token transformations that occur before and after the attention computation respectively. 

# 3 Experimental Setup 

3.1 Models, Data, and Benchmarks 

We evaluate on Pythia-160M (12 layers) and Pythia-410M (24 layers) [Biderman et al., 2023], training on WikiText-103 [Merity et al., 2016]. Benchmarks: LAMBADA [Paperno et al., 2016] (discourse comprehension), HellaSwag [Zellers et al., 2019] (commonsense reasoning), ARC-Easy [Clark et al., 2018] (science QA), and WikiText-103 perplexity. 2Standard block 

> Input
> RMS norm
> Q, K, V proj.
> RoPE (Q, K)
> Attention
> +
> RMS norm
> FFN
> +
> Proposed block
> Input
> RMS norm
> Pre-projection
> Nonlinear. No position.
> Q, K, V proj.
> RoPE (Q, K)
> Attention
> +
> RMS norm
> FFN
> +
> Wskip
> content skip

Figure 1: Standard block (left) vs. proposed block (right). The green pre-projection constructs nonlinear features before Q/K/V. The blue content skip routes those features around attention, adding directly to the output. Both paths are position-agnostic; positional encoding enters only at RoPE. 

3.2 Protocol 

All pretrained parameters are frozen. Only injected parameters train (pre-projection, skip, or LoRA). AdamW, cosine schedule, LR 10 −5, effective batch 16, bf16, 500 steps. Evaluations on 500-sample subsets. LoRA [Hu et al., 2022] baselines use rank chosen to approximately match parameter counts. 

# 4 Results 

4.1 Main Results 

Table 2 presents the full comparison. The pre-projection with content skip achieves the strongest overall results: 

At 160M scale , pre-proj + skip achieves the best LAMBADA (0.180, +40.6% over baseline), the best HellaSwag (0.338, +3.7%), and the best perplexity (47.9, −38.9% ). This is the only method that improves HellaSwag—all other methods leave it flat or slightly degrade it. 

At 410M scale , pre-proj + skip achieves the best perplexity (17.0), surpassing even standalone LoRA with 94.4M parameters (17.7). LAMBADA (0.484) exceeds all methods except standalone LoRA (0.488, which uses more parameters). 3Table 2: Frozen probe results. Base model frozen throughout. Bold = best per model per metric. Trainable parameter counts vary due to rank granularity (LoRA) and skip projection ( d2 per layer). 

Model Method Params LAMBADA ↑ HSwag ↑ ARC-E ↑ PPL ↓

Pythia 160M Baseline — 0.128 0.326 0.358 78.4 Pre-proj 17.7M 0.154 0.318 0.342 73.0 LoRA ( r=480) 26.5M 0.126 0.326 0.346 63.8 Pre-proj + LoRA 21.2M 0.162 0.324 0.352 60.8 

Pre-proj + skip 24.8M 0.180 0.338 0.348 47.9 

Pythia 410M Baseline — 0.466 0.396 0.436 29.2 Pre-proj 62.9M 0.470 0.392 0.452 25.1 LoRA ( r=640) 94.4M 0.488 0.400 0.446 17.7 Pre-proj + LoRA 72.4M 0.470 0.400 0.450 19.2 

Pre-proj + skip 88.1M 0.484 0.398 0.432 17.0 

4.2 Complementarity: Pre-Projection, LoRA, and Content Skip 

The three techniques address different limitations: 

• Pre-projection : nonlinear feature construction before attention. 

• LoRA : linear projection enrichment within attention. 

• Content skip : content bypass around attention. At 160M scale, LoRA alone decreases LAMBADA below baseline (0.126) while improving perplexity (63.8)—it optimizes surface prediction without improving comprehension. The pre-projection alone improves comprehension (LAMBADA 0.154) but modestly improves perplexity. The content skip combines both strengths: it achieves the best comprehension and perplexity simultaneously. 

4.3 Layer-Wise Content Skip Analysis 

Figure 2 reveals a consistent pattern across both model sizes: the content skip is activated most strongly in the final layers . At 160M, skip norm grows from 0.13 (layers 0–7) to 0.30 (layer 11)—a 2.3 × increase. At 410M, skip norm grows from 0.19 (layers 0–10) to 0.28 (layer 23)—a 1.5 × increase. The growth is concentrated in the final 25% of layers at both scales. This pattern has a natural interpretation. Early transformer layers are building representations through progressive refinement—positional attention is doing useful routing work, moving infor-mation between tokens based on their positions. Later layers have already formed rich semantic representations and are preparing for output prediction. At this stage, content information that by-passes positional attention is valuable: the model has already decided what matters, and additional positional routing would only dilute the signal. This finding connects to prior observations about transformer layer roles [Tenney et al., 2019]: early layers handle syntax (position-dependent), while later layers handle semantics (content-dependent). The content skip learns this division automatically—it activates where content matters more than position. 40 2 4 6 8 10 

0.1

0.15 

0.2

0.25 

0.3

Layer 

> Skip norm
> Pythia-160M (12 layers)

0 4 8 12 16 20 24 

0.2

0.25 

0.3

Layer 

> Skip norm
> Pythia-410M (24 layers)

Figure 2: Learned content skip projection weight norms by layer. Both models show the same pattern: later layers activate the content bypass more strongly. Skip weights are initialized at 

N (0 , 10 −4); the growth is entirely learned. 

# 5 Discussion 

5.1 Three Dimensions of Attention Enrichment 

Our experiments reveal three orthogonal ways to improve attention in a frozen probe setting, each addressing a different limitation: 1. Nonlinear feature construction (pre-projection): surfaces features invisible to linear Q/K/V projections. Improves comprehension (LAMBADA) most. 2. Linear projection enrichment (LoRA): makes Q/K/V projections higher-rank. Improves perplexity most. 3. Content bypass (skip): lets content information avoid positional attention where unhelpful. Improves both comprehension and perplexity. The content skip combines the benefits of (1) and (2) because the pre-projection’s enriched features serve double duty: they improve what goes into attention and provide content that bypasses it. 

5.2 Position-Agnostic Design for Multimodal Architectures 

Neither the pre-projection nor the content skip contains positional encoding. Positional information enters only at RoPE on Q/K. This separation of content preparation (modality-independent) from positional awareness (modality-specific) is particularly relevant for multimodal models where text is sequential, images are 2D spatial, video is spatiotemporal, and point clouds have no natural ordering. In joint-attention architectures like MMDiT [Esser et al., 2024], a shared pre-projection and content skip could construct modality-independent features, with each modality applying its own positional encoding only at the Q/K stage. The content skip would be especially valuable for modalities where position is undefined—content features could bypass positional attention entirely. 55.3 Limitations 

Our experiments use a frozen probe on two model sizes with 500-sample evaluations. Scaling to 1B+ models and from-scratch training remain untested. The LoRA comparison is not perfectly parameter-matched. HellaSwag and ARC-Easy differences are within sampling noise at 500 samples for some methods. The layer-wise skip pattern, while consistent across both tested sizes, should be verified at larger scale. 

# 6 Related Work 

Talking-heads attention [Shazeer et al., 2020] mixes logits across heads. LoRA [Hu et al., 2022] adds low-rank updates to projections. Adapters [Houlsby et al., 2019] insert bottleneck layers between sub-layers. Prefix tuning [Li and Liang, 2021] prepends trainable tokens. Highway net-works [Srivastava et al., 2015] use learned gating for skip connections, though not in the context of bypassing attention. Our work is distinguished by (1) placing nonlinear feature construction specifically before Q/K/V in a position-agnostic manner, (2) providing a learned content bypass around attention, and (3) demonstrating that the bypass activates preferentially in later layers. 

# 7 Conclusion 

We present a nonlinear pre-projection before Q/K/V projections with a content skip connection that bypasses attention. The pre-projection constructs richer features for attention; the skip lets those features also avoid positional routing where the model learns it is unhelpful. Across two model sizes and four benchmarks, the combined approach achieves the strongest results: +40.6% LAMBADA accuracy and −38.9% perplexity at 160M scale, with the best perplexity at 410M (17.0, surpassing LoRA’s 17.7 with fewer effective LoRA parameters). The learned skip weights reveal a consistent architectural insight: content bypass is most valuable in the final layers, where semantic representations are mature and positional routing becomes less essential. Both modifications add no K/V cache overhead and are position-agnostic by design. 

Reproducibility. Code available at https://github.com/cs-cmyk/preprojection .

# References 

Biderman, S., et al. Pythia: A suite for analyzing large language models across training and scaling. 

ICML , 2023. Clark, P., et al. Think you have solved question answering? Try ARC, the AI2 reasoning challenge. 

arXiv:1803.05457 , 2018. Esser, P., et al. Scaling rectified flow transformers for high-resolution image synthesis. ICML , 2024. Houlsby, N., et al. Parameter-efficient transfer learning for NLP. ICML , 2019. Hu, E. J., et al. LoRA: Low-rank adaptation of large language models. ICLR , 2022. Li, X. L. and Liang, P. Prefix-tuning: Optimizing continuous prompts for generation. ACL , 2021. Merity, S., et al. Pointer sentinel mixture models. arXiv:1609.07843 , 2016. 6Paperno, D., et al. The LAMBADA dataset: Word prediction requiring a broad discourse context. 

ACL , 2016. Shazeer, N., et al. Talking-heads attention. arXiv:2003.02436 , 2020. Srivastava, R. K., Greff, K., and Schmidhuber, J. Highway networks. arXiv:1505.00387 , 2015. Tenney, I., Das, D., and Pavlick, E. BERT rediscovers the classical NLP pipeline. ACL , 2019. Touvron, H., et al. LLaMA: Open and efficient foundation language models. arXiv:2302.13971 ,2023. Zellers, R., et al. HellaSwag: Can a machine really finish your sentence? ACL , 2019. 7