---
title: Orthogonal Quadratic Complements for Vision Transformer Feed-Forward Networks
title_zh: 视觉 Transformer 前馈网络的正交二次补余
authors: Wang Zixian
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.09709v1"
tags: ["query:dl"]
score: 9.0
evidence: 通过二次特征改进视觉Transformer的前馈网络
tldr: 本研究针对Vision Transformer中双线性前馈网络存在的冗余问题，提出了正交二次互补（OQC）机制。该方法通过构建低秩二次辅助分支，并将其显式投影到主分支的正交补空间，确保辅助特征仅捕捉主分支未涵盖的信息。实验表明，OQC及其变体（如低秩版OQC-LR和动态门控版）在CIFAR-100和TinyImageNet上显著提升了模型精度，同时优化了表示几何结构和类别分离度，实现了更优的速度与精度平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09709v1/fig-001.webp\", \"caption\": \"Figure 1: Architecture evolution. Left: The host FFN (e.g., standard MLP or AFBO) produces a dominant main branch. Middle: OQC adds a low-rank quadratic auxiliary feature and explicitly projects it onto the orthogonal complement of the main branch. Right: The gated OQC family maintains the orthogonal complement but dynamically or statically modulates its injection to the host branch.\", \"page\": 4, \"index\": 1, \"width\": 963, \"height\": 446}]"
motivation: 旨在解决现有ViT双线性前馈网络中二阶交互增强与特征冗余混杂的问题，提高辅助信息的有效性。
method: 提出将低秩二次辅助分支投影到主隐藏表示的正交补空间，以提取互补的非线性特征。
result: 在CIFAR-100和TinyImageNet上，OQC及其动态门控版本分别比基准提升了1.34和1.43个百分点。
conclusion: 通过正交投影消除冗余，OQC能有效增强ViT的特征表达能力并改善表示空间的几何特性。
---

## 摘要
最近用于视觉 Transformer 的双线性前馈替代方案可以显著提高准确率，但它们通常混淆了两种效应：更强的二阶交互以及相对于主分支增加的冗余。我们研究了一种互补设计原则，其中辅助二次特征仅贡献主导隐藏表示尚未捕获的信息。为此，我们提出了正交二次补余（OQC），它构建了一个低秩二次辅助分支，并在注入前将其显式投影到主分支的正交补空间上。我们进一步研究了一种高效的低秩实现（OQC-LR）以及门控扩展（OQC-static 和 OQC-dynamic）。在参数匹配的 Deep-ViT 和 CIFAR-100 协议下，并采用固定的倒数第二层残差读取，全量 OQC 将 AFBO 基准从 64.25 +/- 0.22 提升至 65.59 +/- 0.22，而 OQC-LR 达到了 65.52 +/- 0.25，且具有显著更好的速度-准确率权衡。在 TinyImageNet 上，门控扩展 OQC-dynamic 达到了 51.88 +/- 0.32，比基准（50.45 +/- 0.21）提高了 1.43 个百分点，并优于所有非门控变体。机制分析显示，投影后辅助分支与主分支的重叠接近于零，同时改善了表示几何和类别分离度。包括非门控和门控变体在内的整个系列在两个数据集上均表现出一致的泛化性。

## Abstract
Recent bilinear feed-forward replacements for vision transformers can substantially improve accuracy, but they often conflate two effects: stronger second-order interactions and increased redundancy relative to the main branch. We study a complementary design principle in which auxiliary quadratic features contribute only information not already captured by the dominant hidden representation. To this end, we propose Orthogonal Quadratic Complements (OQC), which construct a low-rank quadratic auxiliary branch and explicitly project it onto the orthogonal complement of the main branch before injection. We further study an efficient low-rank realization (OQC-LR) and gated extensions (OQC-static and OQC-dynamic).   Under a parameter-matched Deep-ViT and CIFAR-100 protocol with a fixed penultimate residual readout, full OQC improves an AFBO baseline from 64.25 +/- 0.22 to 65.59 +/- 0.22, while OQC-LR reaches 65.52 +/- 0.25 with a substantially better speed-accuracy tradeoff. On TinyImageNet, the gated extension OQC-dynamic achieves 51.88 +/- 0.32, improving the baseline (50.45 +/- 0.21) by 1.43 points and outperforming all ungated variants. Mechanism analyses show near-zero post-projection auxiliary-main overlap together with improved representation geometry and class separation. The full family, including both ungated and gated variants, generalizes consistently across both datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《Orthogonal Quadratic Complements for Vision Transformer Feed-Forward Networks》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于视觉 Transformer（ViT）中前馈网络（FFN）的效率与表达能力。虽然近年来引入双线性算子（如 AFBO）增强了二阶交互，但作者发现这些方法存在**表示冗余**问题：新增的辅助高阶分支往往会坍缩回主分支的表示空间，导致计算资源浪费且信息增量有限。
**核心问题**：如何确保辅助的二次项分支只提供主分支尚未捕获的、非冗余的互补信息？

### 2. 方法论：正交二次补余（OQC）
核心思想是将辅助分支视为希尔伯特空间中的补余成分，通过显式正交化消除冗余。
*   **核心流程**：
    1.  **构建辅助特征**：通过两个低秩投影的哈达玛积（Hadamard product）生成原始二次特征 $q(x)$。
    2.  **主分支映射**：将主分支（如 MLP 或 AFBO）的隐藏表示 $b(x)$ 投影到相同的辅助空间，得到 $m(x)$。
    3.  **正交投影**：利用施密特正交化原理，从 $q(x)$ 中减去其在 $m(x)$ 方向上的投影分量，得到正交补余项 $q_{\perp}(x)$。
    4.  **注入与混合**：将 $q_{\perp}(x)$ 重新映射回隐藏维度，并与主分支相加。
*   **变体设计**：
    *   **OQC-LR**：全过程在低秩空间完成正交化，显著提升推理速度。
    *   **OQC-static/dynamic**：引入静态标量门控或基于输入的动态空间门控，对补余信息进行自适应调制。

### 3. 实验设计
*   **数据集**：CIFAR-100 和 TinyImageNet。
*   **基准模型（Benchmark）**：Deep-ViT 架构，以 AFBO（非对称分解双线性算子）作为主要对比基准。
*   **对比方法**：
    *   标准 MLP 结构。
    *   AFBO + PR（倒数第二层残差读取）。
    *   OQC 的不同变体（Full OQC, OQC-LR, OQC-static, OQC-dynamic）。
    *   消融实验：包括不同秩（Rank）的影响、无正交化的朴素辅助分支、共享投影矩阵等。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   文中提供了推理速度的量化指标（Images/sec），例如 AFBO 基准为 9762 img/s，而 OQC-LR r56 为 7497 img/s，展示了方法对计算开销的影响。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流中小型数据集，进行了多种子（3 seeds）重复实验并报告了标准差。
*   **消融实验**：针对辅助分支的秩、正交化必要性、门控机制、以及在不同宿主（MLP vs AFBO）上的迁移性做了详细分解。
*   **充分性评价**：实验设计较为客观，通过固定“倒数第二层残差读取（PR）”这一变量，确保了性能提升确实源于 FFN 结构的改进而非读取策略。但在超大规模数据集（如 ImageNet-1K）上的验证尚缺。

### 6. 主要结论与发现
*   **性能提升**：在 CIFAR-100 上，OQC 将基准提升了 1.34%；在 TinyImageNet 上，OQC-dynamic 提升了 1.43%。
*   **冗余消除**：机制分析证实，投影后辅助与主分支的余弦重叠度从 ~0.14 降至接近机器精度的零（$10^{-8}$）。
*   **几何特性**：Full OQC 改善了表示空间的有效秩（Effective Rank），而动态门控版本则显著增强了类别分离度（Separation Score）。
*   **超叠加效应**：OQC 与 PR 读取机制结合时，表现出“1+1>2”的性能增益。

### 7. 优点（亮点）
*   **理论严谨**：从希尔伯特空间的正交补角度解决特征冗余，比单纯增加参数量更具解释性。
*   **效率平衡**：OQC-LR 变体在保持高精度的同时，通过低秩操作缓解了双线性算子常见的计算瓶颈。
*   **通用性**：证明了该模式不仅适用于先进的 AFBO，也能显著增强传统的 MLP 结构。

### 8. 不足与局限
*   **数据集规模**：实验主要集中在 CIFAR 和 TinyImageNet，缺乏在 ImageNet-1K 或更大规模视觉任务上的表现数据。
*   **算力开销**：尽管有低秩优化，OQC 变体相比原始 AFBO 仍有 20%-40% 的吞吐量下降。
*   **边际递减**：实验发现，如果宿主 FFN 本身已经具备极强的二次交互建模能力，OQC 带来的增益会相对减小。
*   **超参数敏感性**：动态门控版本虽然上限高，但实验显示其方差较大，可能对初始化和学习率调节较为敏感。

（完）
