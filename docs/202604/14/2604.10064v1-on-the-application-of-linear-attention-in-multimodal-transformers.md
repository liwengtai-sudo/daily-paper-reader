---
title: On The Application of Linear Attention in Multimodal Transformers
title_zh: 论线性注意力在多模态 Transformer 中的应用
authors: "Armin Gerami, Seyedehanita Madani, Ramani Duraiswami"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10064v1"
tags: ["query:dl"]
score: 9.0
evidence: 多模态Transformer中的线性注意力机制
tldr: 本研究针对多模态Transformer计算复杂度随序列长度平方增长的瓶颈，探索了线性注意力（LA）作为高效替代方案的可行性。通过在不同规模的ViT架构及LAION-400M数据集上进行实验，证明了LA能将计算开销降至线性级别，同时保持了与标准Softmax注意力相当的性能并遵循相同的缩放法则，为构建高效多模态模型提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10064v1/fig-001.webp\", \"caption\": \"Figure 4. Training loss scaling with model size for ViTs on LAION-400M. Our adjusted LA follows a trend similar to standard attention.\", \"page\": 4, \"index\": 1, \"width\": 485, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.10064v1/fig-002.webp\", \"caption\": \"Figure 3. Training metrics for ViT-S/16, ViT-B/16, and ViT-L/16 models trained on LAION-400M using four NVIDIA A5500 GPUs. Linear attention with our adjustments achieves performance comparable to standard attention.\", \"page\": 4, \"index\": 2, \"width\": 1021, \"height\": 683}]"
motivation: 解决多模态Transformer中标准注意力机制因平方级计算复杂度而导致的扩展性受限问题。
method: 在多模态框架中引入线性注意力机制，将计算复杂度从序列长度的平方级降低至线性级。
result: 实验表明线性注意力在显著降低计算成本的同时，在ImageNet-21K零样本准确率上表现出与标准注意力相似的竞争力和缩放特性。
conclusion: 线性注意力是处理大规模复杂多模态数据的一种高效、可扩展且性能稳健的理想解决方案。
---

## 摘要
多模态 Transformer 是最先进视觉-语言模型的核心架构，但其二次方级别的注意力复杂度仍是限制可扩展性的关键障碍。在本研究中，我们探讨了线性注意力（LA）作为多模态框架内高效替代方案的可行性。通过集成线性注意力，我们将计算开销从相对于序列长度的二次方降低到了线性级别，同时保持了具有竞争力的性能。我们在 LAION-400M 数据集上训练的 ViT-S/16、ViT-B/16 和 ViT-L/16 架构上评估了我们的方法，并重点验证了 ImageNet-21K 的零样本准确率。我们的系统性评估表明，线性注意力不仅显著节省了计算资源，而且遵循与标准 softmax 注意力相同的缩放法则。这些发现表明，线性注意力是处理日益庞大且复杂数据集的下一代多模态 Transformer 的一种稳健且可扩展的解决方案。

## Abstract
Multimodal Transformers serve as the backbone for state-of-the-art vision-language models, yet their quadratic attention complexity remains a critical barrier to scalability. In this work, we investigate the viability of Linear Attention (LA) as a high-efficiency alternative within multimodal frameworks. By integrating LA, we reduce the computational overhead from quadratic to linear relative to sequence length while preserving competitive performance. We evaluate our approach across ViT-S/16, ViT-B/16, and ViT-L/16 architectures trained on the LAION-400M dataset, with validation focused on ImageNet-21K zero-shot accuracy. Our systematic evaluation demonstrates that Linear Attention not only yields significant computational savings but also adheres to the same scaling laws as standard softmax attention. These findings position Linear Attention as a robust, scalable solution for next-generation multimodal Transformers tasked with processing increasingly large and complex datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《On The Application of Linear Attention in Multimodal Transformers》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：多模态 Transformer（如 CLIP、ViT）在处理视觉和语言信息时，其标准 Softmax 注意力机制的计算复杂度随序列长度 $N$ 呈**二次方增长** ($O(N^2)$)。这限制了模型处理高分辨率图像或长文本序列的能力。
*   **研究动机**：探索**线性注意力（Linear Attention, LA）**在多模态表征学习中的可行性，旨在将复杂度降至 $O(N)$，同时验证其在大规模预训练下的性能表现和缩放法则（Scaling Laws）。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用矩阵乘法的结合律，将注意力操作从 $O(N^2 D)$ 优化为 $O(ND^2)$。
*   **关键技术改进**：
    *   **仿射核函数**：采用 $f(x) = 1 + x$ 作为核函数，避免了指数运算。
    *   **Q/K 归一化**：为了防止仿射核产生负值（当 $q_i \cdot k_n < -1$ 时），对 Query 和 Key 向量进行 $L_2$ 归一化，确保注意力分数为非负。
    *   **移除分母归一化**：作者发现传统 LA 的分母会导致注意力分数随序列长度增加而过度平滑（Over-smoothing），从而阻碍梯度流动。论文提出**移除分母**，仅保留分子项，并配合预归一化的 Q/K，使注意力分数保持在 0 到 1 之间，增强了模型的表达能力。

### 3. 实验设计与 Benchmark
*   **数据集**：使用大规模多模态数据集 **LAION-400M** 进行预训练。
*   **模型架构**：基于 OpenCLIP 框架，评估了三种规模的 Vision Transformer：**ViT-S/16、ViT-B/16、ViT-L/16**。
*   **Benchmark**：主要评估指标为 **ImageNet-21K 的零样本（Zero-shot）分类准确率**。
*   **对比方法**：
    *   标准 Softmax 注意力（使用 FlashAttention-2 优化）。
    *   常规线性注意力（带分母归一化）。
    *   作者改进后的线性注意力（不带分母）。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 张 NVIDIA A5500 GPU** 进行模型训练。
*   **推理测试**：在 **H200 GPU** 上进行了单层注意力的运行时间（Runtime）缩放测试。
*   **训练时长**：论文展示了训练曲线（Wallclock Time），ViT-L 模型的训练时间接近 400 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 22M 到 304M 参数量的三种模型规模，实验设计较为系统。
*   **充分性评价**：
    *   **优点**：实验不仅对比了最终精度，还深入分析了推理时间随序列长度的变化趋势，并拟合了缩放法则曲线。
    *   **局限**：实验主要集中在图像分类的零样本迁移上，缺乏对下游多模态任务（如视觉问答 VQA、图像描述生成）的广泛验证。

### 6. 主要结论与发现
*   **性能对等**：改进后的 LA 在大规模预训练后，最终能达到与标准 Softmax 注意力**相当的准确率**。
*   **效率优势**：LA 在序列长度达到 $10^3$ 级别时开始展现优势，在极长序列（$4 \times 10^6$）下，计算时间比标准注意力低约 3 个数量级。
*   **缩放法则一致性**：LA 遵循与标准注意力相似的幂律缩放（Power-law scaling），这意味着增加模型参数能稳定提升 LA 模型的性能。
*   **收敛速度**：虽然 LA 最终性能达标，但其**收敛速度比标准注意力慢**，需要更多的训练轮数（Epochs）才能达到相同精度。

### 7. 优点与亮点
*   **解决了过平滑问题**：通过简单的数学调整（移除分母）显著提升了线性注意力的训练稳定性和表达力。
*   **硬件友好**：展示了 LA 在现代 GPU 上的实际加速效果，证明了其在处理超长上下文多模态任务中的潜力。
*   **理论验证**：首次在多模态大规模预训练场景下验证了 LA 的缩放法则。

### 8. 不足与局限
*   **收敛效率**：LA 需要更长的训练时间才能收敛，这在一定程度上抵消了其单步计算的效率优势。
*   **任务覆盖面**：验证任务单一（仅 ImageNet 零样本），未探讨在需要精细空间关系的复杂多模态推理任务中的表现。
*   **超参数敏感性**：移除分母后的稳定性可能依赖于特定的 Q/K 归一化方式，论文未对不同归一化策略进行详尽的消融实验。

（完）
