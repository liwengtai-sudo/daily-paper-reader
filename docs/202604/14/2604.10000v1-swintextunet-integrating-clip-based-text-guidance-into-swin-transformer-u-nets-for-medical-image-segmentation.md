---
title: "SwinTextUNet: Integrating CLIP-Based Text Guidance into Swin Transformer U-Nets for Medical Image Segmentation"
title_zh: SwinTextUNet：将基于 CLIP 的文本引导集成到 Swin Transformer U-Net 中用于医学图像分割
authors: "Ashfak Yeafi, Parthaw Goswami, Md Khairul Islam, Ashifa Islam Shamme"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10000v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于医学图像分割的Swin Transformer U-Net
tldr: 针对传统医学图像分割模型在处理低对比度或模糊图像时的局限性，本研究提出了SwinTextUNet框架。该模型创新性地将CLIP预训练的文本嵌入集成到Swin Transformer UNet骨干网络中，通过交叉注意力和卷积融合机制实现语义文本引导与分层视觉特征的深度对齐。在QaTaCOV19数据集上的实验证明，该多模态方法显著增强了分割的鲁棒性，在提升准确性的同时保持了模型复杂度的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10000v1/fig-001.webp\", \"caption\": \"Fig. 7. Representative qualitative segmentation results. Each triplet shows (a) original CXR, (b) ground-truth mask, and (c) SwinTextUNet prediction.\", \"page\": 5, \"index\": 1, \"width\": 370, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2604.10000v1/fig-002.webp\", \"caption\": \"Fig. 4. Overall architecture of SwinTextUNet. The model integrates CLIP-based text embeddings with a hierarchical Swin U-Net through cross-attention and ConvFuse blocks.\", \"page\": 4, \"index\": 2, \"width\": 702, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.10000v1/fig-003.webp\", \"caption\": \"Fig. 5. Representative samples from the QaTa-COV19 dataset across training, validation, and test sets. Each sample shows (a) chest X-ray, (b) ground-truth lesion mask, and (c) textual annotation provided by the dataset authors [23].\", \"page\": 4, \"index\": 3, \"width\": 474, \"height\": 452}]"
motivation: 传统的仅依赖视觉特征的分割模型在面对医学图像中模糊或低对比度的组织结构时往往难以获得精确结果。
method: 提出一种将CLIP文本引导集成到Swin Transformer UNet中的多模态框架，利用交叉注意力和卷积融合实现图文特征对齐。
result: "在QaTaCOV19数据集上，该模型取得了86.47%的Dice系数和78.2%的IoU，证明了文本引导对提升分割性能的有效性。"
conclusion: 视觉与语言信息的集成能显著增强医学图像分割的准确性，为开发临床辅助诊断工具提供了极具前景的方向。
---

## 摘要
精确的医学图像分割是实现计算机辅助诊断和有效治疗规划的基础。仅依赖视觉特征的传统模型在面对模糊或低对比度模式时往往表现不佳。为了克服这些局限性，我们提出了 SwinTextUNet，这是一种多模态分割框架，它将对比语言-图像预训练（CLIP）衍生的文本嵌入集成到 Swin Transformer UNet 骨干网络中。通过集成交叉注意力和卷积融合，该模型有效地将语义文本引导与分层视觉表示相对齐，从而增强了鲁棒性和准确性。我们在 QaTaCOV19 数据集上评估了我们的方法，其中所提出的四阶段变体在性能和复杂性之间达到了最佳平衡，Dice 和 IoU 分数分别达到 86.47% 和 78.2%。消融研究进一步验证了文本引导和多模态融合的重要性。这些研究结果强调了视觉-语言集成在推进医学图像分割和支持具有临床意义的诊断工具方面的潜力。

## Abstract
Precise medical image segmentation is fundamental for enabling computer aided diagnosis and effective treatment planning. Traditional models that rely solely on visual features often struggle when confronted with ambiguous or low contrast patterns. To overcome these limitations, we introduce SwinTextUNet, a multimodal segmentation framework that incorporates Contrastive Language Image Pretraining (CLIP), derived textual embeddings into a Swin Transformer UNet backbone. By integrating cross attention and convolutional fusion, the model effectively aligns semantic text guidance with hierarchical visual representations, enhancing robustness and accuracy. We evaluate our approach on the QaTaCOV19 dataset, where the proposed four stage variant achieves an optimal balance between performance and complexity, yielding Dice and IoU scores of 86.47% and 78.2%, respectively. Ablation studies further validate the importance of text guidance and multimodal fusion. These findings underscore the promise of vision language integration in advancing medical image segmentation and supporting clinically meaningful diagnostic tools.

---

## 论文详细总结（自动生成）

这是一份关于论文《SwinTextUNet: Integrating CLIP-Based Text Guidance into Swin Transformer U-Nets for Medical Image Segmentation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
医学图像分割（如病灶识别）对于临床诊断至关重要。然而，传统的卷积神经网络（CNN）或纯 Transformer 模型（如 Swin-UNet）主要依赖**单模态视觉特征**。在面对低对比度、组织边界模糊或噪声干扰的医学图像时，仅靠视觉信息往往难以实现精准分割。
**研究动机**：临床医生在诊断时通常会结合放射报告等文本信息。本文旨在通过引入**视觉-语言多模态学习**，利用 CLIP 预训练模型的语义先验知识来引导视觉特征提取，从而增强模型在复杂临床场景下的鲁棒性和准确性。

### 2. 方法论：核心思想与关键技术
论文提出了 **SwinTextUNet** 框架，其核心思想是将 CLIP 的文本嵌入集成到分层 Swin Transformer U-Net 结构中。

*   **核心架构**：采用 U 型对称结构，包含编码器、瓶颈层、解码器以及改进的跳跃连接。
*   **关键技术细节**：
    *   **CLIP 文本编码器**：使用预训练且冻结的 CLIP 文本编码器处理医疗描述（如“双肺感染，左上肺受累”），通过线性投影层将文本嵌入对齐到视觉特征空间。
    *   **Swin Transformer 编码器**：利用移位窗口自注意力（Shifted-Window Self-Attention）捕捉多尺度视觉特征，降低计算复杂度的同时获取长程依赖。
    *   **文本引导交叉注意力（Cross-Attention）**：在编码器的每个阶段，视觉 Token 作为 Query，文本 Token 作为 Key 和 Value。通过非对称的交叉注意力机制，让文本语义信息“指导”视觉特征的聚焦方向。
    *   **卷积融合模块（ConvFuse）**：在解码阶段，不只是简单的拼接，而是通过 ConvFuse 块（包含 3x3 卷积、BN 和 GELU）融合上采样特征与带文本引导的跳跃连接特征，以增强局部空间连续性和边界重建。
    *   **混合损失函数**：结合了 Dice Loss 和交叉熵损失（CE），以平衡区域重叠度和像素级分类准确性。

### 3. 实验设计
*   **数据集**：使用 **QaTa-COV19** 数据集，包含 9,258 张胸部 X 光片（CXR）及其对应的 COVID-19 病灶掩码。
*   **文本来源**：该数据集包含由医学专家编写的文本注释，描述感染模式、位置和侧性。
*   **Benchmark 与对比方法**：
    *   **传统 CNN**：U-Net, Attention U-Net。
    *   **多模态/Transformer 变体**：CLIP (用于分割的基准), LViT (Language meets Vision Transformer)。
*   **评估指标**：Dice 系数（Dice Similarity Coefficient）和交并比（IoU）。

### 4. 资源与算力
*   **软件框架**：PyTorch。
*   **超参数**：AdamW 优化器，学习率 $1 \times 10^{-4}$，权重衰减 $1 \times 10^{-2}$，Batch Size 为 8，训练 100 个 Epoch，采用余弦退火学习率调度。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 NVIDIA A100 或 RTX 3090）及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **深度探索实验**：对比了 3 阶段、4 阶段和 5 阶段的模型深度，确定 4 阶段为性能与复杂度的平衡点。
    *   **消融实验**：分别验证了“无文本引导”、“无 ConvFuse”和“无交叉注意力”的情况，数据表明文本引导对 Dice 指标的贡献高达 7.3%。
    *   **基准对比**：与 4 种主流模型进行了定量对比。
*   **充分性评价**：实验设计较为合理，涵盖了结构选择、模块有效性验证和基准测试。但实验**仅限于单一数据集**（QaTa-COV19），缺乏在其他医学影像任务（如 MRI 或 CT）上的泛化性验证。

### 6. 主要结论与发现
*   **多模态优势**：引入文本引导显著提升了分割性能，SwinTextUNet 在测试集上达到了 **86.47% 的 Dice** 和 **78.2% 的 IoU**，优于所有对比基准。
*   **结构优化**：4 阶段的 Swin 结构在保持参数量（57.6M）相对适中的同时，性能优于更浅的结构，且与更深的 5 阶段结构性能接近。
*   **模块协同**：消融实验证明，交叉注意力负责语义对齐，而 ConvFuse 负责空间细节恢复，两者缺一不可。

### 7. 优点（亮点）
*   **语义对齐**：成功将 CLIP 的强大语义理解能力引入到像素级的医学分割任务中。
*   **鲁棒性**：通过文本先验，模型在处理视觉特征模糊（如低对比度病灶）时表现出更强的抗干扰能力。
*   **端到端设计**：模型实现了从文本/图像输入到分割掩码输出的完整端到端训练。

### 8. 不足与局限
*   **数据依赖性**：临床环境下高质量的实时文本描述（放射报告）并不总是可用的，这限制了模型的实际部署。
*   **维度局限**：目前仅针对 2D 图像进行研究，未扩展到临床更常见的 3D 医疗数据（如 CT/MRI 序列）。
*   **泛化验证不足**：仅在一个 COVID-19 数据集上验证，未在多器官分割或不同疾病类型的任务中测试其通用性。
*   **计算开销**：虽然 Swin Transformer 降低了复杂度，但引入 CLIP 编码器和多级交叉注意力仍增加了推理时的计算负担。

（完）
