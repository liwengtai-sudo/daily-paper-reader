---
title: "Attend what matters: Leveraging vision foundational models for breast cancer classification using mammograms"
title_zh: 关注重点：利用视觉基础模型进行乳腺 X 线摄影的乳腺癌分类
authors: "Samyak Sanghvi, Piyush Miglani, Sarvesh Shashikumar, Kaustubh R Borgavi, Veenu Singla, Chetan Arora"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19350v1"
tags: ["query:ai-pm"]
score: 10.0
evidence: 使用视觉 Transformer 进行乳腺癌钼靶影像分类
tldr: 该研究针对视觉Transformer在乳腺癌筛查中因高分辨率图像、微小病灶及细粒度分类困难而表现受限的问题，提出了一种结合视觉基础模型的新框架。通过目标检测引导的RoI令牌缩减、基于对比学习的细粒度特征增强以及利用DINOv2预训练模型捕捉局部特征，显著提升了乳腺癌分类的准确性，为大规模临床筛查提供了有效工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19350v1/fig-001.webp\", \"caption\": \"Table 1. Performance comparison on the VinDR dataset. ViT-A and ViT-B correspond to DINOv2 [1] with a linear layer at input resolutions of 448×448 and 1024×1024 respectively. ViT-C demonstrates scores using a DeIT head instead of a linear layer at 448×448 input resolution.\", \"page\": 3, \"index\": 1, \"width\": 508, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2604.19350v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview. In our proposed method, we first use G-DINO in a zero-shot setting to localize semantically important Regions of Interest (RoIs) based on a clinically meaningful text prompt. The top-k RoIs, selected by bounding box confidence scores, are cropped and passed through a pretrained DINOv2 encoder to extract k feature vectors. These features are processed by a transformer module, enabling interaction among regions through self-attention. The global RoI (covering the full breast) is used as the final representation which is contextually enriched by fine-grained lesion-level RoIs, and is fed into a Multi-Layer Perceptron (MLP) for classification.\", \"page\": 3, \"index\": 2, \"width\": 1016, \"height\": 391}]"
motivation: 传统的ViT在处理高分辨率乳腺钼靶图像时，难以有效定位微小病灶且难以区分细微的类别差异。
method: 提出一种集成RoI令牌缩减、硬负样本对比学习以及DINOv2预训练ViT的框架，以增强模型对关键区域的关注和判别能力。
result: 在公开乳腺钼靶数据集上的实验表明，该方法在分类性能上优于现有基准模型。
conclusion: 该研究证明了结合目标检测引导的注意力机制和自监督基础模型在提升医学影像诊断精度方面的巨大潜力。
---

## 摘要
Vision Transformers (ViT) 已成为许多计算机视觉任务的首选架构，但其在计算机辅助诊断中的性能仍然有限。针对乳腺 X 线摄影中的乳腺癌检测，我们确定了导致这一不足的两个主要原因。首先，医学图像具有高分辨率且异常区域微小，导致 token 数量过多，使得基于 softmax 的注意力机制难以定位并关注相关区域。其次，医学图像分类本质上是细粒度的，具有低类间差异和高类内差异，标准的交叉熵训练不足以应对。为了克服这些挑战，我们提出了一个包含三个关键组件的框架：(1) 基于感兴趣区域 (RoI) 的 token 缩减，利用目标检测模型引导注意力；(2) 选定 RoI 之间的对比学习，通过基于难负样本 (hard-negative) 的训练增强细粒度判别能力；(3) 使用 DINOv2 预训练的 ViT，捕捉具有定位感知能力的细粒度特征，而非全局 CLIP 表示。在公开乳腺 X 线摄影数据集上的实验表明，我们的方法优于现有基准，证明了其在规模化乳腺癌筛查中的有效性和潜在临床应用价值。我们的代码可在以下地址获取以供复现：https://aih-iitd.github.io/publications/attend-what-matters

## Abstract
Vision Transformers $(\texttt{ViT})$ have become the architecture of choice for many computer vision tasks, yet their performance in computer-aided diagnostics remains limited. Focusing on breast cancer detection from mammograms, we identify two main causes for this shortfall. First, medical images are high-resolution with small abnormalities, leading to an excessive number of tokens and making it difficult for the softmax-based attention to localize and attend to relevant regions. Second, medical image classification is inherently fine-grained, with low inter-class and high intra-class variability, where standard cross-entropy training is insufficient. To overcome these challenges, we propose a framework with three key components: (1) Region of interest $(\texttt{RoI})$ based token reduction using an object detection model to guide attention; (2) contrastive learning between selected $\texttt{RoI}$ to enhance fine-grained discrimination through hard-negative based training; and (3) a $\texttt{DINOv2}$ pretrained $\texttt{ViT}$ that captures localization-aware, fine-grained features instead of global $\texttt{CLIP}$ representations. Experiments on public mammography datasets demonstrate that our method achieves superior performance over existing baselines, establishing its effectiveness and potential clinical utility for large-scale breast cancer screening. Our code is available for reproducibility here: https://aih-iitd.github.io/publications/attend-what-matters

---

## 论文详细总结（自动生成）

### 论文总结：Attend what matters: Leveraging vision foundational models for breast cancer classification using mammograms

#### 1. 核心问题与整体含义（研究动机和背景）
乳腺癌是全球女性死亡率极高的恶性肿瘤，乳腺 X 线摄影（钼靶）是早期筛查的金标准。尽管视觉 Transformer (ViT) 在通用视觉领域表现优异，但在乳腺癌分类任务中面临两大挑战：
*   **高分辨率与微小病灶的矛盾**：钼靶图像通常为 4K 分辨率，而癌变区域仅占全图约 0.1%。直接输入高分辨率图像会导致 Token 数量爆炸，使 Softmax 注意力机制被大量无关背景稀释，难以聚焦微小病灶。
*   **细粒度分类难题**：医学影像具有极高的类内差异和极低的类间差异，传统的交叉熵损失函数难以捕捉区分恶性与良性征象所需的细微特征。

#### 2. 论文提出的方法论
该研究提出了一种结合目标检测引导和自监督基础模型的框架，核心组件包括：
*   **基于 RoI 的 Token 缩减**：利用预训练的 **Grounding DINO (G-DINO)** 模型，根据临床文本提示（如“肿块”、“钙化”）在全分辨率图像中定位前 $k$ 个感兴趣区域 (RoI)。这种方法有效过滤了背景噪声，显著减少了进入分类头的 Token 数量。
*   **特征编码与聚合**：采用 **DINOv2** 预训练的 ViT 作为特征提取器（相比 CLIP，DINOv2 更擅长局部特征和定位感知）。提取的 RoI 嵌入通过带有**旋转位置编码 (RoPE)** 的 Transformer 块进行处理，以捕捉不同区域间的空间关联。
*   **损失函数设计**：
    *   **二元交叉熵 (BCE) 损失**：用于最终的分类监督。
    *   **排斥性对比损失 (Repulsive Contrastive Loss, RCL)**：计算不同 RoI 嵌入之间的归一化成对余弦相似度，并最小化其平方值。该损失旨在迫使模型学习多样化的特征，防止特征坍缩，并增强对“硬负样本”（不含肿瘤但具有干扰性的 RoI）的判别能力。

#### 3. 实验设计
*   **数据集**：主要在公开的大规模乳腺 X 线摄影数据集 **VinDR-Mammo** 上进行验证。
*   **评价指标**：AUC、F1 分数以及不同阈值下的召回率 (R@0.1, R@0.3, R@0.5)。
*   **对比方法 (Baselines)**：
    *   **纯视觉模型**：不同分辨率下的 DINOv2 (ViT-A/B/C)、MedVAE、TReg-SwinT、XFMamba。
    *   **图文多模态模型**：MMBCD、Mammo-CLIP（此前的 SOTA）。

#### 4. 资源与算力
论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。作者提到了代码已开源，通常此类基于 ViT 基础模型微调的任务需要中高端 GPU（如 A100 或 V100）支持。

#### 5. 实验数量与充分性
*   **实验规模**：作者进行了主实验对比（Table 1）和详细的消融实验（Table 2）。
*   **消融实验**：涵盖了 DINOv2 基础、G-DINO RoI 提取、锚点选择、RoPE 位置编码以及 RCL 损失函数的逐项验证，证明了每个组件的增益。
*   **充分性评价**：实验设计较为合理，对比了包括最新的多模态模型在内的多种 SOTA 方法。但由于仅在一个主要数据集 (VinDR) 上展示结果，缺乏跨中心、跨设备数据集的外部验证，其泛化性仍有待进一步证明。

#### 6. 主要结论与发现
*   **性能突破**：该方法在 AUC 上达到了 86.6%，F1 分数达到 54.5%，相比之前的 SOTA 模型 Mammo-CLIP，F1 分数显著提升了 **4%**，AUC 提升了 **1%**。
*   **纯视觉胜过多模态**：尽管 Mammo-CLIP 使用了大规模私有数据集进行图文预训练，但本研究仅通过纯图像训练和合理的 RoI 引导，就实现了更好的性能，证明了“关注重点区域”比单纯增加多模态信息更有效。
*   **RoI 策略的有效性**：消融实验显示，引入 G-DINO 提取 RoI 并结合 RCL 损失，是提升细粒度分类性能的关键。

#### 7. 优点
*   **高效的 Token 管理**：通过目标检测预筛选 RoI，解决了高分辨率医学影像在 Transformer 中的计算冗余问题。
*   **无需私有数据**：相比依赖大规模私有图文对的模型，该方法基于公开数据集和开源基础模型，更具可复现性和临床普及潜力。
*   **细粒度优化**：引入排斥性对比损失，针对性地解决了医学影像中难负样本干扰的问题。

#### 8. 不足与局限
*   **依赖前端检测器**：模型的性能高度依赖于 G-DINO 提取 RoI 的质量，如果检测器漏掉微小病灶，后续分类器将无法弥补。
*   **数据集单一**：实验仅限于 VinDR 数据集，未探讨在不同种族、不同设备（如不同厂商的钼靶机）下的鲁棒性。
*   **计算开销未详述**：虽然减少了 Token 数量，但引入 G-DINO 和 DINOv2 两个大型基础模型，其推理延迟和显存占用在实际临床部署中的可行性有待评估。

（完）
