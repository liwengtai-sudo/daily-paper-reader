---
title: Structure-Adaptive Sparse Diffusion in Voxel Space for 3D Medical Image Enhancement
title_zh: 用于3D医学图像增强的体素空间结构自适应稀疏扩散模型
authors: "Hongxu Jiang, Fei Li, Boxiao Yu, Ying Zhang, Kaleb Smith, Kuang Gong, Wei Shao"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17773v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于临床诊断中3D医学图像增强的深度学习
tldr: 针对3D医学图像增强中扩散模型计算开销巨大的挑战，本文提出一种结构自适应的稀疏体素空间扩散框架。该方法利用退化输入中的强解剖先验，通过在稀疏采样的时间步上进行训练和采样，并引入结构感知轨迹调制（STM）模块，实现了在保持精细解剖细节的同时，将训练速度提升达10倍，在CT、PET和MRI等多种模态的去噪与超分辨率任务中均达到SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17773v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison on denoising and 4× super-resolution tasks across four datasets. Best results are in red, second-best are blue.\", \"page\": 6, \"index\": 1, \"width\": 688, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative denoising results on lung CT and brain PET. Purple and blue boxes highlight lung fissures and subtle anatomical boundaries, respectively. Our model preserves sharper, more structurally consistent details than all baselines.\", \"page\": 7, \"index\": 2, \"width\": 639, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-003.webp\", \"caption\": \"Fig. 3. Qualitative 4× super-resolution results on aorta CTA and brain MRI. Highlighted regions show vessel boundaries and cortical structures. Our model recovers finer anatomical details with fewer artifacts than all baselines.\", \"page\": 7, \"index\": 3, \"width\": 639, \"height\": 397}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the proposed framework.\", \"page\": 3, \"index\": 4, \"width\": 723, \"height\": 202}]"
motivation: 传统的3D扩散模型在处理高维体数据时，由于密集的噪声调度导致计算成本极高且处理过程冗余。
method: 提出在稀疏时间步上直接预测干净数据，并利用STM模块根据局部解剖内容动态调整时间嵌入以实现结构自适应增强。
result: 在四个主流医学影像数据集上的实验表明，该方法在显著提升训练效率的同时，各项性能指标均优于现有最先进技术。
conclusion: 该研究证明了通过结合稀疏调度与结构自适应调制，可以在不进行有损压缩的情况下高效实现高质量的3D医学图像增强。
---

## 摘要
三维（3D）医学图像增强（包括去噪和超分辨率）对于CT、PET和MRI的临床诊断至关重要。尽管扩散模型在2D医学成像中取得了显著成功，但由于高维体数据上冗长的扩散轨迹，将其扩展到高分辨率3D体数据在计算上仍然难以实现。我们观察到，在条件增强任务中，退化输入中强大的解剖先验使得密集的噪声调度在很大程度上是冗余的。利用这一见解，我们提出了一种稀疏体素空间扩散框架，该框架在一组紧凑的均匀下采样时间步上进行训练和采样。网络直接在数据流形上预测干净数据，并在速度空间（velocity space）中进行监督，以实现稳定的梯度缩放。一个轻量级的结构感知轨迹调制（STM）模块根据局部解剖内容在每个网络块处重新校准时间嵌入，从而在共享的稀疏调度上实现结构自适应去噪。该框架直接在体素空间运行，在不进行有损压缩的情况下保留了精细的解剖细节，同时实现了高达10倍的训练加速。在涵盖CT、PET和MRI的四个数据集上的实验表明，该方法在去噪和超分辨率任务上均达到了最先进的性能。我们的代码公开于：https://github.com/mirthAI/sparse-3d-diffusion。

## Abstract
Three-dimensional (3D) medical image enhancement, including denoising and super-resolution, is critical for clinical diagnosis in CT, PET, and MRI. Although diffusion models have shown remarkable success in 2D medical imaging, scaling them to high-resolution 3D volumes remains computationally prohibitive due to lengthy diffusion trajectories over high-dimensional volumetric data. We observe that in conditional enhancement, strong anatomical priors in the degraded input render dense noise schedules largely redundant. Leveraging this insight, we propose a sparse voxel-space diffusion framework that trains and samples on a compact set of uniformly subsampled timesteps. The network predicts clean data directly on the data manifold, supervised in velocity space for stable gradient scaling. A lightweight Structure-aware Trajectory Modulation (STM) module recalibrates time embeddings at each network block based on local anatomical content, enabling structure-adaptive denoising over the shared sparse schedule. Operating directly in voxel space, our framework preserves fine anatomical detail without lossy compression while achieving up to $10\times$ training acceleration. Experiments on four datasets spanning CT, PET, and MRI demonstrate state-of-the-art performance on both denoising and super-resolution tasks. Our code is publicly available at: https://github.com/mirthAI/sparse-3d-diffusion.

---

## 论文详细总结（自动生成）

这是一份关于论文《Structure-Adaptive Sparse Diffusion in Voxel Space for 3D Medical Image Enhancement》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在高分辨率3D医学图像（如CT、PET、MRI）增强任务中，既能利用扩散模型（Diffusion Models）强大的生成能力，又能克服其在3D体素空间中由于高维数据和冗长采样轨迹导致的巨大计算开销。
*   **研究背景**：传统的3D扩散模型通常在压缩空间（如VAE潜空间或小波域）运行以节省算力，但这不可避免地会丢失精细的解剖细节（如肺裂、皮质褶皱）。作者观察到，在条件增强任务中，退化的输入图像本身已包含强解剖先验，因此不需要像无条件生成那样经历完整的、密集的噪声调度过程。

### 2. 论文提出的方法论
该框架的核心思想是**在体素空间直接进行稀疏扩散**，主要包含以下三个关键技术：
*   **$x_0$ 预测与速度监督（Velocity Supervision）**：
    *   不同于预测噪声（$\epsilon$），模型直接预测干净数据 $x_0$，使输出始终保持在数据流形上。
    *   为了解决 $x_0$ 预测在不同时间步梯度缩放不平衡的问题，引入了速度空间监督。通过公式 $\hat{v}_t = (\sqrt{\bar{\alpha}_t} x_t - \hat{x}_0) / \sqrt{1 - \bar{\alpha}_t}$ 将预测值转换到速度空间进行损失计算，确保训练稳定性。
*   **稀疏时间步调度（Sparse Timestep Scheduling）**：
    *   利用 $x_0$ 预测的平滑轨迹特性，从传统的1000步中均匀下采样极少数步（实验中 $K=5$）构成稀疏轨迹，大幅减少训练和推理时间。
*   **结构感知轨迹调制（STM）**：
    *   **多结构编码器**：通过3D卷积分支和两个2D平面分支（冠状面、矢状面平均投影）提取局部解剖特征。
    *   **块级调制**：将提取的结构特征 $c$ 输入到 UNet 每个残差块的 MLP 中，动态调整时间嵌入（Time Embedding）的缩放和平移。这使得模型能根据解剖复杂程度（如血管 vs 均匀组织）自适应地调整去噪强度。

### 3. 实验设计
*   **数据集与场景**：
    *   **肺部 CT 去噪**：LDCT-and-Projection 数据集。
    *   **脑部 PET 去噪**：FDG PET 数据集。
    *   **主动脉 CTA 超分辨率（4×）**：AortaSeg24 数据集。
    *   **脑部 MRI 超分辨率（4×）**：UHB FCD Lesion 数据集。
*   **Benchmark 与对比方法**：
    *   对比了四种代表性 3D 扩散模型：**3D LDM**（潜空间）、**3D WDM**（小波域）、**3D DDPM**（体素空间标准扩散）和 **3D DDIM**。
*   **评价指标**：PSNR、SSIM、MS-SSIM 和 HFEN（高频误差范数）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及具体训练总时长。
*   **效率提升**：文中强调了效率的显著提升，指出该方法相比于 3D DDIM 基准，在达到相同或更高性能的前提下，实现了**高达 10 倍的训练加速**（收敛速度更快）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同的医学影像模态和 2 种主流增强任务，样本量从 50 到 120 名患者不等，具有较好的覆盖面。
*   **消融实验**：
    *   针对扩散步数 $K$ 进行了对比（发现 $K=5$ 时性能最优，步数过多反而会导致过度平滑）。
    *   针对 $x_0$ 预测、稀疏调度、STM 模块分别进行了组件有效性验证。
*   **客观性**：实验对比了当前最先进的几种 3D 扩散变体，并提供了定量的指标分析和定性的视觉对比（切片图、局部放大图），结果较为客观、公平。

### 6. 论文的主要结论与发现
*   **性能领先**：在所有四个数据集上，该方法在 PSNR 和 SSIM 等指标上均达到或接近 SOTA（最先进）水平。
*   **稀疏性优势**：证明了在条件医学图像增强中，极少数的时间步（如 5 步）足以恢复高质量细节，密集的调度不仅浪费算力，还可能因重复的噪声注入而模糊细微结构。
*   **结构自适应的重要性**：STM 模块能有效处理解剖异质性，使模型在复杂结构处保留更多细节。

### 7. 优点（亮点）
*   **高效性**：通过稀疏调度解决了 3D 扩散模型难以训练和推理缓慢的痛点。
*   **保真度**：直接在体素空间操作，避免了 VAE 等压缩手段带来的信息损失，对临床诊断关注的细微病灶和解剖边界保护更好。
*   **即插即用**：STM 模块设计轻量，且速度空间监督为稀疏扩散提供了理论上的稳定性支持。

### 8. 不足与局限
*   **算力细节缺失**：未提供具体的硬件环境和精确的推理耗时数据，不便于其他研究者评估部署成本。
*   **超参数敏感性**：虽然 $K=5$ 在文中表现最好，但对于不同噪声水平或不同模态的图像，最优稀疏步数可能需要重新调整。
*   **应用限制**：该方法主要针对“条件增强”任务（有退化图像作为参考），对于“无条件生成”任务，这种稀疏调度可能无法捕捉完整的数据分布。

（完）
