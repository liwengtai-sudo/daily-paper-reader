---
title: "RePrompT: Recurrent Prompt Tuning for Integrating Structured EHR Encoders with Large Language Models"
title_zh: RePrompT：用于将结构化电子健康记录编码器与大语言模型集成的循环提示微调
authors: "Arya Hadizadeh Moghaddam, Drew Ross, Mohsen Nayebi Kerdabadi, Dongjie Wang, Zijun Yao"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17725v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 将结构化EHR编码器与大语言模型集成以分析患者轨迹
tldr: 本文针对大语言模型（LLM）在处理结构化电子健康档案（EHR）时存在的时序结构丢失和缺乏群体规律学习的问题，提出了RePrompT框架。该框架通过提示微调技术集成结构化EHR编码器，利用循环机制捕捉患者的纵向演变，并引入群体层面的任务对齐特征。实验证明，RePrompT在MIMIC-III和MIMIC-IV的临床预测任务中显著优于现有基准模型，有效提升了临床决策支持的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17725v1/fig-001.webp\", \"caption\": \"Table 2: Performance comparison on readmission and mortality prediction tasks using the MIMIC-III and MIMICIV datasets. Evaluation is conducted based on AUROC and PRAUC metrics.\", \"page\": 6, \"index\": 1, \"width\": 708, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2604.17725v1/fig-002.webp\", \"caption\": \"Figure 2: The framework of the proposed method. Medical codes and discharge notes are first used to generate patient summaries through Clinical Records Synthesis. Next, the structured medical codes in patient history are encoded into embeddings through a classic EHR encoder. Meanwhile, the LLM’s hidden state in the previous time step is recurrently taken as a soft prompt for the current time step to capture longitudinal dependencies. Outputs from all prompting methods are combined as the input of the predictive LLM, and the LLM’s state at the final visit is used for downstream binary classification tasks.\", \"page\": 3, \"index\": 2, \"width\": 902, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.17725v1/fig-003.webp\", \"caption\": \"Figure 3: The Prompt for clinical record synthesis.\", \"page\": 3, \"index\": 3, \"width\": 475, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2604.17725v1/fig-004.webp\", \"caption\": \"Table 6: Computational time analysis of the proposed method when processing a batch of eight patients.\", \"page\": 12, \"index\": 4, \"width\": 346, \"height\": 355}]"
motivation: LLM在处理结构化EHR时，文本化转换会破坏时序结构，且其独立推理模式难以利用群体层面的预测规律。
method: 提出一种时间感知的提示微调框架，通过循环整合历史就诊状态并注入来自群体训练编码器的可训练提示词。
result: 在MIMIC-III和MIMIC-IV数据集的多个临床预测任务中，RePrompT的性能一致优于纯EHR模型和纯LLM模型。
conclusion: RePrompT通过有效融合结构化编码器的群体表征能力与LLM的推理能力，为临床预测任务提供了一种更强大的建模方案。
---

## 摘要
大语言模型（LLMs）通过对纵向临床信息进行推理以捕捉具有丰富上下文的患者轨迹，在挖掘电子健康记录（EHR）方面展现出巨大潜力。然而，利用 LLMs 处理结构化 EHR（例如标准化的诊断和药物代码）面临两个关键挑战。首先，将带有时间戳的 EHR 序列转换为纯文本可能会模糊时间结构和代码标识，从而削弱捕捉代码共现和纵向规律的能力。其次，与学习跨患者共享且任务对齐的表示空间的队列训练预测模型不同，LLMs 通常应用于病例隔离的推理场景，即独立处理每个患者，而未能利用群体层面的模式。为了应对这些挑战，我们提出了 RePrompT，这是一个具有时间感知能力的 LLM 框架，它通过提示微调集成结构化 EHR 编码器，且无需修改底层架构。具体而言，RePrompT 循环地整合来自先前就诊的隐藏状态以保留纵向信息，并通过源自队列训练、任务对齐的 EHR 编码器的可训练提示标记注入群体层面的信息。在 MIMIC-III 和 MIMIC-IV 上的实验表明，RePrompT 在多个临床预测任务中始终优于基于 EHR 和基于 LLM 的基准模型。

## Abstract
Large Language Models (LLMs) have shown strong promise for mining Electronic Health Records (EHRs) by reasoning over longitudinal clinical information to capture context-rich patient trajectories. However, leveraging LLMs for structured EHRs (e.g., standardized diagnosis and medication codes) presents two key challenges. First, translating time-stamped EHR sequences into plain text can obscure both temporal structure and code identities, weakening the ability to capture code co-occurrence and longitudinal regularities. Second, unlike cohort-trained predictive models that learn a shared, task-aligned representation space across patients, LLMs are often applied in a case-isolated inference setting where each patient is processed independently without leveraging population-level patterns. To address these challenges, we introduce RePrompT, a time-aware LLM framework that integrates structured EHR encoders through prompt tuning, without modifying underlying architectures. Specifically, RePrompT recurrently incorporates latent states from prior visits to preserve longitudinal information, and injects population-level information through trainable prompt tokens derived from a cohort-trained, task-aligned EHR encoder. Experiments on MIMIC-III and MIMIC-IV demonstrate that RePrompT consistently outperforms both EHR-based and LLM-based baselines across multiple clinical prediction tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《RePrompT: Recurrent Prompt Tuning for Integrating Structured EHR Encoders with Large Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是如何更有效地利用大语言模型（LLM）处理结构化电子健康记录（EHR）。尽管 LLM 擅长处理非结构化文本，但在处理结构化 EHR（如诊断代码、药物代码）时面临两大挑战：
*   **时序结构丢失**：将纵向 EHR 序列简单线性化为纯文本会模糊时间依赖性和代码间的共现规律。
*   **缺乏群体知识**：LLM 通常在“病例隔离”模式下推理，无法像传统临床预测模型那样通过群体训练（Cohort-trained）学习跨患者的、任务对齐的共享表示空间。
**研究目标**：开发一个既能保留时序演变规律，又能整合群体层面任务特征的 LLM 框架。

### 2. 方法论：RePrompT 框架
RePrompT 的核心思想是通过**提示微调（Prompt Tuning）**将结构化编码器的能力注入冻结的 LLM 中。其主要包含三个模块：
*   **临床记录综合（Clinical Record Synthesis）**：利用强大的通用 LLM（如 DeepSeek-V3）将冗长、多噪声的住院摘要和结构化代码去噪并压缩为简洁的文本摘要。
*   **状态循环提示微调（State-Recurrent Prompt Tuning）**：为了捕捉纵向依赖，模型逐次处理就诊记录。将上一次就诊时 LLM 的隐藏状态（Hidden State）通过线性变换转化为当前步骤的“软提示（Soft Prompt）”，实现跨时间步的信息传递。
*   **结构编码提示微调（Struct-Encoded Prompt Tuning）**：引入一个预训练的结构化 EHR 编码器（如 RETAIN）。该编码器在群体数据上训练，能捕捉疾病共现和群体规律。其生成的患者嵌入向量被作为另一组“软提示”注入 LLM。
*   **预测与优化**：在 LLM 之上添加一个分类头，利用最后一次就诊的隐藏状态进行再入院、死亡率或药物预测。训练过程中 LLM 保持冻结，仅微调提示词相关的参数和分类头。

### 3. 实验设计
*   **数据集**：使用了两个大规模真实世界数据集：**MIMIC-III** 和 **MIMIC-IV**。
*   **任务场景**：再入院预测（Readmission）、死亡率预测（Mortality）以及药物推荐（Medication Recommendation）。
*   **基准方法（Benchmarks）**：
    *   **传统深度学习模型**：Deepr, RETAIN, GRAM, GRASP, AdaCare, StageNet, Adore, ARCI。
    *   **LLM 相关方法**：Zero-Shot（GPT-5/DeepSeek）、标准 Prompt-Tuning、COCONUT（连续思维链）。

### 4. 资源与算力
*   **硬件环境**：使用了高性能服务器，配备 **3 张 NVIDIA A6000 GPU**，256 GB RAM 和 48 核 CPU。
*   **模型配置**：预测 LLM 采用 Llama 3.1 1B（基于 LLM2Vec 框架），合成摘要采用 DeepSeek-V3。
*   **推理效率**：文中提供了计算时间分析，处理 8 个患者的批次大约需要 1.44 秒，虽然慢于传统模型（如 RETAIN 的 0.05s），但在临床实时应用中仍属可行范围。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了三类任务的对比，每项实验运行三次取均值。
*   **消融实验**：详细测试了“无循环状态”、“无结构编码”、“无 DeepSeek 摘要”等多种变体，验证了各模块的贡献。
*   **编码器鲁棒性**：测试了集成不同 EHR 编码器（LSTM, Transformer, RETAIN）对结果的影响。
*   **评价**：实验设计较为充分，涵盖了从性能对比到效率分析的多个维度，对比基准包含了最新的时序模型和 LLM 方案，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：RePrompT 在所有任务和数据集上均显著优于传统 EHR 模型和现有的 LLM 提示方法。
*   **协同效应**：结构化编码器提供的“群体知识”与 LLM 的“推理能力”具有互补性，结合后效果最佳。
*   **时序建模的重要性**：消融实验显示，状态循环模块对性能提升贡献最大，证明了显式建模纵向轨迹的必要性。
*   **小模型胜大模型**：经过特定任务提示微调的小型 LLM（1B）在临床预测任务上优于零样本的大型模型。

### 7. 优点与亮点
*   **非侵入式集成**：无需修改 LLM 内部架构，通过软提示技术实现了结构化数据与自然语言的深度融合。
*   **双重感知**：同时具备了时间感知（通过循环状态）和群体感知（通过结构化编码器）。
*   **实用性强**：利用 LLM 对医疗文本进行去噪合成，解决了 EHR 文本冗长且重复的问题。

### 8. 不足与局限
*   **泛化风险**：实验仅基于 MIMIC 系列数据集，不同医疗机构的 EHR 编码标准和记录习惯存在差异，跨系统泛化能力有待验证。
*   **计算开销**：相比于纯轻量级深度学习模型，引入 LLM 显著增加了推理延迟和显存占用。
*   **任务覆盖**：目前主要集中在二分类和多标签推荐任务，尚未探讨更复杂的生成式临床决策支持。

（完）
