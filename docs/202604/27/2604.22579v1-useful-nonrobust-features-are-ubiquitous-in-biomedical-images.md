---
title: Useful nonrobust features are ubiquitous in biomedical images
title_zh: 有用的非鲁棒特征在生物医学图像中普遍存在
authors: "Coenraad Mouton, Randle Rabe, Niklas C. Koser, Nicolai Krekiehn, Christopher Hansen, Jan-Bernd Hövener, Claus-C. Glüer"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22579v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于医学影像的深度网络及MedMNIST中的非鲁棒特征
tldr: 本研究探讨了医疗影像深度学习模型中广泛存在的“非鲁棒特征”——即具有预测性但人类不可理解且易受扰动的输入模式。通过在 MedMNIST 数据集上的实验，研究发现这些特征能显著提升模型在分布内的准确率，但会导致模型在面对分布偏移（如 MedMNIST-C）时表现不佳。研究揭示了医疗影像分类中准确性与鲁棒性之间的权衡，强调在实际部署中应根据具体需求调整模型对特征的依赖。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22579v1/fig-001.webp\", \"caption\": \"Fig. 3. The same as Fig. 2 using AUC as the performance metric.\", \"page\": 6, \"index\": 1, \"width\": 740, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2604.22579v1/fig-002.webp\", \"caption\": \"Fig. 2. Test accuracy comparison of DNNs using different features for 5 MedMNIST datasets. Solid bars: in-distribution performance. Dashed bars: average out-of-distribution performance.\", \"page\": 3, \"index\": 2, \"width\": 740, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2604.22579v1/fig-003.webp\", \"caption\": \"Fig. 4. Test accuracy comparison of robust and nonrobust models trained with different values of ϵ. Solid bars: ϵ = 4 255 . Dashed bars: ϵ = 8 255\", \"page\": 7, \"index\": 3, \"width\": 740, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2604.22579v1/fig-004.webp\", \"caption\": \"Fig. 1. Saliency map comparison between robust and nonrobust organ-classification models for a right-kidney CT slice (top) and a slightly perturbed adversarial version (bottom). The robust model’s attributions remain stable between the original and adversarial images and concentrate on kidneyrelevant anatomy, whereas the nonrobust model’s attributions change markedly under perturbation and appear almost noiselike. Overlays are normalized to [0, 1]; brighter colors indicate larger attribution.\", \"page\": 1, \"index\": 4, \"width\": 460, \"height\": 323}]"
motivation: 探究医疗影像深度学习模型是否依赖非鲁棒特征，以及这些特征如何影响模型在不同分布下的性能表现。
method: 在五个 MedMNIST 任务上，对比仅利用非鲁棒特征训练的模型与对抗训练模型在分布内及分布偏移下的表现。
result: 非鲁棒特征在分布内具有极高的预测价值，但对抗训练通过牺牲部分准确率显著提升了模型在受控分布偏移下的鲁棒性。
conclusion: 医疗影像分类中存在准确性与鲁棒性的实际权衡，非鲁棒特征虽能增强标准性能，却会削弱模型的泛化能力。
---

## 摘要
我们研究了用于医学成像的深度网络是否学习了有用的非鲁棒特征——即那些人类不可解释且对微小对抗扰动高度敏感的预测性输入模式——以及这些特征如何影响测试性能。我们表明，仅在非鲁棒特征上训练的模型在五个 MedMNIST 分类任务中实现了远高于随机概率的准确率，证实了它们在分布内（in-distribution）的预测价值。相反，主要依赖鲁棒特征的对抗训练模型牺牲了分布内准确率，但在受控的分布偏移（MedMNIST-C）下表现出显著更好的性能。总体而言，非鲁棒特征提升了标准准确率，但降低了分布外（out-of-distribution）性能，揭示了医学影像分类任务中实际存在的鲁棒性与准确率之间的权衡，这应根据部署环境的具体要求进行调整。

## Abstract
We study whether deep networks for medical imaging learn useful nonrobust features - predictive input patterns that are not human interpretable and highly susceptible to small adversarial perturbations - and how these features impact test performance. We show that models trained only on nonrobust features achieve well above chance accuracy across five MedMNIST classification tasks, confirming their predictive value in-distribution. Conversely, adversarially trained models that primarily rely on robust features sacrifice in-distribution accuracy but yield markedly better performance under controlled distribution shifts (MedMNIST-C). Overall, nonrobust features boost standard accuracy yet degrade out-of-distribution performance, revealing a practical robustness-accuracy trade-off in medical imaging classification tasks that should be tailored to the requirements of the deployment setting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Useful nonrobust features are ubiquitous in biomedical images》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度学习模型在处理医疗影像时，是否像处理自然图像一样，依赖于“非鲁棒特征”（Nonrobust Features）？这些特征对模型的性能、泛化能力和可解释性有何影响？
*   **背景**：非鲁棒特征是指那些对人类不可理解、极易受微小扰动影响，但在特定数据集内具有高度预测性的模式。在医疗这种高风险领域，如果模型依赖这些“脆弱”的特征而非解剖学意义上的“鲁棒”特征，可能会导致模型在实际临床部署（存在分布偏移时）中失效，并引发信任危机。

### 2. 方法论
*   **核心思想**：通过构建仅包含非鲁棒特征的特殊数据集，验证模型是否能仅凭这些特征完成分类；同时通过对抗训练强制模型仅依赖鲁棒特征，观察性能变化。
*   **关键技术细节**：
    *   **分离非鲁棒特征**：
        1.  训练一个标准模型 $f$。
        2.  对训练集中的样本 $x$ 进行微小的对抗扰动（$L_\infty$ 范数限制），使其被模型 $f$ 误分类为随机目标标签 $t$。
        3.  构建新数据集 $\hat{D}_{train}$，其输入是扰动后的图像，标签是目标标签 $t$。此时，图像中的人类可理解特征（鲁棒特征）与标签 $t$ 无关，只有被修改的微小扰动（非鲁棒特征）与 $t$ 相关。
        4.  在 $\hat{D}_{train}$ 上训练新模型，并在原始未改动的测试集上评估。
    *   **提取鲁棒特征**：采用 **TRADES 损失函数** 进行对抗训练，通过平衡清洁数据损失和对抗样本的 KL 散度，迫使模型忽略非鲁棒诱因。
    *   **扰动约束**：使用 $L_\infty$ 扰动范围 $\epsilon = 4/255$，通过 100 步 PGD（投影梯度下降）生成对抗样本。

### 3. 实验设计
*   **数据集**：选取了 **MedMNIST** 集合中的 5 个代表性任务，涵盖不同模态：
    *   PathMNIST（病理学）、PneumoniaMNIST（胸部 X 光）、OctMNIST（光学相干断层扫描）、OrganSMNIST（CT 扫描）、BreastMNIST（超声）。
*   **Benchmark 与对比方法**：
    *   **Base Model**：在原始数据上训练，利用所有特征。
    *   **Nonrobust-only Model**：仅在非鲁棒特征数据集上训练。
    *   **Robust Model**：通过对抗训练（TRADES）获得，主要依赖鲁棒特征。
*   **评估场景**：
    *   **分布内（In-distribution）**：原始测试集。
    *   **分布外（OOD）**：使用 **MedMNIST-C**（包含高斯噪声、模糊、压缩等 15 种常见图像损坏）。
    *   **对抗攻击**：使用 **AutoAttack** 强力攻击框架。

### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号和数量。
*   **训练细节**：提到了对抗训练的计算成本极高，大约是标准训练的 **10 倍**（因为使用了 10 步 PGD 内部循环）。
*   **优化策略**：进行了广泛的超参数搜索（每个模型 12 到 32 组配置），使用了标签平滑、指数权重平均（EMA）、Swish 激活函数和余弦退火学习率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同的医疗模态，每种模态都进行了标准训练、非鲁棒特征提取训练和对抗训练。
*   **充分性**：
    *   **多指标验证**：同时使用了平衡准确率（Balanced Accuracy）和 AUC 指标，结果一致。
    *   **消融/敏感性分析**：在附录中测试了不同扰动强度（$\epsilon = 4/255$ vs $8/255$）的影响。
    *   **客观性**：使用了预定义的训练/验证/测试拆分，且未使用预训练权重以避免引入外部特征，实验设计严谨、公平。

### 6. 主要结论与发现
*   **非鲁棒特征普遍存在**：仅靠非鲁棒特征训练的模型，其表现远超随机猜测（例如在 PathMNIST 上达到 59.24% 准确率，随机仅为 11%）。
*   **准确性与鲁棒性的权衡**：
    *   在分布内，非鲁棒特征能显著提升性能（提升 2%-12%）。
    *   在分布外（OOD），依赖非鲁棒特征的模型表现剧降；而鲁棒模型虽然在标准测试中稍逊，但在面对图像损坏时表现远优于前者（性能提升 8%-33%）。
*   **安全性风险**：非鲁棒模型在面对对抗攻击时几乎完全失效（准确率接近 0%）。

### 7. 优点
*   **系统性强**：首次在医疗影像领域系统性地量化了非鲁棒特征的泛化属性。
*   **视觉化直观**：通过显著性图（Saliency Map）清晰展示了鲁棒模型关注解剖结构，而非鲁棒模型关注类似噪声的模式。
*   **临床启示**：为医疗 AI 的部署提供了指导——追求极致准确率可能会牺牲模型在真实世界复杂环境下的可靠性。

### 8. 不足与局限
*   **图像分辨率较低**：实验基于 32x32 的低分辨率图像，尚不确定结论是否能完美推广到高分辨率或 3D 医疗影像。
*   **人工损坏 vs 自然偏移**：OOD 测试使用的是人工合成的损坏（MedMNIST-C），而非来自不同医院或设备的真实自然分布偏移。
*   **任务局限**：研究仅限于分类任务，未涉及分割或检测等其他重要的医疗影像任务。
*   **模型单一**：主要使用了 WideResNet-16-8，未探讨 Vision Transformers (ViT) 等新型架构在特征选择上的差异。

（完）
