---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中 TB 级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v2"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于anndata中生物数据分析的小批量加载器
tldr: 随着生物数据集规模突破内存限制，数据加载成为AI训练的主要瓶颈。本文推出annbatch，这是一款原生支持anndata格式的小批量加载器，支持直接在磁盘数据集上进行核外训练。它在单细胞、显微镜和全基因组测序等任务中将加载吞吐量提升了一个数量级，将训练时间从数天缩短至数小时，且完美兼容scverse生态，为大规模生物AI提供了实用的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01949v2/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 2048, \"height\": 2048}, {\"url\": \"assets/figures/arxiv/2604.01949v2/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 2048, \"height\": 2048}, {\"url\": \"assets/figures/arxiv/2604.01949v2/fig-003.webp\", \"caption\": \"\", \"page\": 18, \"index\": 3, \"width\": 1280, \"height\": 960}]"
motivation: 生物数据集规模已超出系统内存，导致数据访问而非模型计算成为机器学习训练的主要瓶颈。
method: 开发了原生于anndata的annbatch加载器，支持直接对存储在磁盘上的异构生物数据进行高效的小批量核外训练。
result: 在多项生物学基准测试中，annbatch将加载吞吐量提升了高达一个数量级，显著缩短了模型训练时间。
conclusion: annbatch为大规模生物AI训练提供了高效的数据加载方案，在保持与标准生物数据格式兼容的同时实现了TB级数据的可扩展性。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及在已建立的计算生态系统内进行下游分析。在此，我们推出了 annbatch，这是一个原生于 anndata 的小批量加载器，能够直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序的基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短到几小时，同时保持与 scverse 生态系统的完全兼容。annbatch 为可扩展的生物人工智能建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够利用日益增长且多样化的数据集。

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

这是一份关于论文《annbatch unlocks terabyte-scale training of biological data in anndata》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在生物人工智能（AI）领域，数据集规模已达到 TB 级（如亿级单细胞数据），远超系统内存容量。目前训练的瓶颈已从“模型计算”转移到了“数据加载（I/O）”。
*   **研究背景**：现有的高性能数据加载框架（如 NVIDIA Merlin、Uber Petastorm）通常需要将数据转换为特定格式，这破坏了生物学界通用的 `anndata` 格式及其生态系统（如 `scverse`）的兼容性。
*   **整体含义**：本文推出了 **annbatch**，这是一个原生支持 `anndata` 的小批量加载器。它通过优化磁盘访问模式，在不改变数据标准的前提下，实现了核外（out-of-core）高效训练，解决了大规模生物数据训练中的 I/O 瓶颈。

### 2. 论文提出的方法论
*   **核心思想**：将“纯随机磁盘访问”转变为“伪随机块访问（Pseudo-random chunked access）”。通过预先对磁盘数据进行洗牌，使得加载器可以连续读取大块数据，从而充分利用磁盘的顺序 I/O 性能。
*   **关键技术细节**：
    *   **Pre-shuffler（预洗牌器）**：在训练前将多个 `anndata` 文件合并并进行块级随机化，生成 Zarr 格式的存储。它利用延迟加载（lazy loading）技术，内存占用仅为 $O(m)$（$m$ 为用户定义的洗牌块大小），而非总数据量 $O(N)$。
    *   **Zarr 存储后端**：采用 Zarr 格式替代传统的 HDF5/h5ad，并开发了基于 Rust 的 `zarrs-python` 桥接工具，显著提升读取速度。
    *   **高性能 I/O 优化**：支持 **Direct I/O**（绕过内核缓存）、**固定内存（Pinned Memory）** 读取以及 **异步 GPU 传输**。
    *   **框架无关性**：数据加载器独立于具体的深度学习框架（如 PyTorch 或 TensorFlow）。

### 3. 实验设计
*   **数据集与场景**：
    *   **单细胞转录组学**：使用 Tahoe-100M 数据集（约 1 亿个细胞）。
    *   **单细胞显微成像**：使用 128x128x5 像素的裁剪图像数据。
    *   **全基因组测序（WGS）**：使用 1000 Genomes 数据集，并扩展至 50 万个样本，涵盖稀疏（罕见变异）和稠密（常见变异）场景。
*   **Benchmark 与对比方法**：
    *   对比了 **scDataset**、**MappedCollection**、**BioNemo** 以及传统的 **numpy memmap**。
    *   评估指标包括：每秒处理样本数（samples/sec）、单轮训练（epoch）耗时、磁盘占用空间及预洗牌开销。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   主要基准测试在 **AWS ml.g4dn.4xlarge** 实例（配备 NVIDIA T4 GPU）上运行。
    *   高性能测试在 **de.NBI GPU T4** 节点和 **NVIDIA HGX-H100** 单元上运行。
*   **训练时长与算力细节**：
    *   在 Tahoe-100M 数据集上，annbatch 单轮训练仅需 **17 分钟**，而对比方法 scDataset 需 7.5 小时，MappedCollection 需 21 小时。
    *   预洗牌过程（不需 GPU）在 1 亿细胞规模下耗时约 5.5 小时。

### 5. 实验数量与充分性
*   **实验组数**：论文针对三种完全不同的生物数据模态（测序、影像、基因组）分别进行了详细的吞吐量对比实验。此外，还进行了关于存储格式（Zarr vs h5ad）、批大小（Batch Size）影响、多进程扩展性等消融和敏感性分析。
*   **客观性与公平性**：
    *   作者特别强调了**避免操作系统缓存干扰**，在超过内存容量的大规模数据集上进行测试，以反映真实的核外训练性能。
    *   在 AWS 标准实例上运行以确保结果的可重复性，并指出了磁盘限速对结果的影响。实验设计较为严谨、客观。

### 6. 论文的主要结论与发现
*   **性能飞跃**：annbatch 的数据吞吐量比现有工具（如 MappedCollection）高出 **10 到 40 倍**。
*   **训练效率**：将大规模模型的训练时间从“天”级缩短至“小时”级。
*   **成本摊销**：虽然预洗牌需要数小时的额外开销，但由于加载速度的巨大提升，这种开销通常在训练的**第一个 epoch 内**就能通过节省的 GPU 时间完全抵消。
*   **生态兼容**：成功证明了在不放弃 `anndata` 这一社区标准格式的情况下，可以实现工业级的训练性能。

### 7. 优点
*   **端到端集成**：提供了从预洗牌到高性能加载的完整工具链，且完美融入 `scverse` 生态。
*   **技术栈先进**：引入 Rust 编写的 Zarr 后端和 Direct I/O 等底层优化，技术深度较高。
*   **通用性强**：不仅适用于单细胞数据，还证明了在基因组学和影像学中的广泛适用性。

### 8. 不足与局限
*   **采样策略受限**：由于采用连续块读取，目前难以直接实现复杂的加权采样（Weighted Sampling），需要通过特定的磁盘写入顺序来间接解决。
*   **多模态支持**：目前尚未直接支持多模态数据的同步加载（虽然作者认为这在当前规模下优先级较低）。
*   **预洗牌开销**：对于仅训练 1-2 个 epoch 的极小任务，预洗牌的时间成本可能显得较高。
*   **硬件依赖**：部分高性能特性（如 Direct I/O）主要在 Linux 环境下表现最佳，且对磁盘 I/O 带宽有较高要求。

（完）
