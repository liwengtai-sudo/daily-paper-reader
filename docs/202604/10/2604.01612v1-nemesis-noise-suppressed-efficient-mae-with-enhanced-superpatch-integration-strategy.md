---
title: "NEMESIS: Noise-suppressed Efficient MAE with Enhanced Superpatch Integration Strategy"
title_zh: NEMESIS：具有增强型超块集成策略的噪声抑制高效 MAE
authors: "Kyeonghun Kim, Hyeonseok Jung, Youngung Han, Hyunsu Go, Eunseob Choi, Seongbin Park, Junsu Lim, Jiwon Yang, Sumin Lee, Insung Hwang, Ken Ying-Kai Liao, Nam-Joon Kim"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01612v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于体积CT影像诊断的自监督深度学习
tldr: 针对3D CT影像标注成本高及Transformer内存消耗大的问题，本文提出NEMESIS框架。该框架基于局部超块进行掩码自编码器预训练，引入噪声增强重建、双重掩码策略及跨尺度上下文聚合令牌。实验表明，NEMESIS在显著降低计算开销的同时，在多器官分类任务上超越了现有主流模型，且在低标签场景下表现出极强的鲁棒性，为3D医学影像提供了高效的自监督学习方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01612v1/fig-001.webp\", \"caption\": \"Fig. 1. The overall architecture of the NEMESIS backbone. The input superpatch is divided into volume patches and processed through the patch embedding module. The framework utilizes dual-masking and MATB-based encoders and decoders to learn high-level anatomical representations in the latent space.\", \"page\": 2, \"index\": 1, \"width\": 965, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2604.01612v1/fig-002.webp\", \"caption\": \"Fig. 3. Detailed architecture of the 3D Adaptive Patch Embedding module, integrating Linear Projection (LP) and Superpatch-Embedder (SE) pathways.\", \"page\": 2, \"index\": 2, \"width\": 529, \"height\": 297}]"
motivation: 旨在解决3D CT全量数据训练中Transformer内存开销过大以及传统掩码策略难以捕捉各向异性空间结构的问题。
method: 提出基于超块的MAE框架，包含噪声增强重建任务、双重掩码Transformer块以及用于跨尺度上下文聚合的NEMESIS令牌。
result: "在BTCV基准测试中，线性分类AUROC达到0.9633，优于SuPreM等模型，且计算开销较全量基准降低了约97%。"
conclusion: NEMESIS证明了通过局部超块设计和改进的掩码策略，可以在大幅提升计算效率的同时实现卓越的3D医学影像表征学习。
---

## 摘要
体层 CT 成像是临床诊断的基础，但 3D 体数据的标注既昂贵又耗时，这促使了利用无标签数据进行自监督学习 (SSL) 的研究。然而，由于全卷 Transformer 的高内存开销以及 CT 数据各向异性的空间结构（传统掩码策略难以捕捉），将 SSL 应用于 3D CT 仍具挑战性。我们提出了 NEMESIS，这是一个在局部 128x128x128 超块 (superpatches) 上运行的掩码自编码器 (MAE) 框架，在保留解剖细节的同时实现了内存高效的训练。NEMESIS 引入了三个核心组件：(i) 作为预训练任务的噪声增强重建，(ii) 通过并行的平面级和轴级 Token 移除执行双重掩码的掩码解剖 Transformer 块 (MATB)，以及 (iii) 用于跨尺度上下文聚合的 NEMESIS Token (NT)。在 BTCV 多器官分类基准测试中，采用冻结骨干网络和线性分类器的 NEMESIS 达到了 0.9633 的平均 AUROC，超过了全微调的 SuPreM (0.9493) 和 VoCo (0.9387)。在仅有 10% 可用标注的低标签场景下，它仍保持了 0.9075 的 AUROC，展示了强大的标签效率。此外，基于超块的设计将单次前向传播的计算成本从全卷基准的 985.8 GFLOPs 降低至 31.0 GFLOPs，为 3D 医学影像提供了可扩展且稳健的基础。

## Abstract
Volumetric CT imaging is essential for clinical diagnosis, yet annotating 3D volumes is expensive and time-consuming, motivating self-supervised learning (SSL) from unlabeled data. However, applying SSL to 3D CT remains challenging due to the high memory cost of full-volume transformers and the anisotropic spatial structure of CT data, which is not well captured by conventional masking strategies. We propose NEMESIS, a masked autoencoder (MAE) framework that operates on local 128x128x128 superpatches, enabling memory-efficient training while preserving anatomical detail. NEMESIS introduces three key components: (i) noise-enhanced reconstruction as a pretext task, (ii) Masked Anatomical Transformer Blocks (MATB) that perform dual-masking through parallel plane-wise and axis-wise token removal, and (iii) NEMESIS Tokens (NT) for cross-scale context aggregation. On the BTCV multi-organ classification benchmark, NEMESIS with a frozen backbone and a linear classifier achieves a mean AUROC of 0.9633, surpassing fully fine-tuned SuPreM (0.9493) and VoCo (0.9387). Under a low-label regime with only 10% of available annotations, it retains an AUROC of 0.9075, demonstrating strong label efficiency. Furthermore, the superpatch-based design reduces computational cost to 31.0 GFLOPs per forward pass, compared to 985.8 GFLOPs for the full-volume baseline, providing a scalable and robust foundation for 3D medical imaging.

---

## 论文详细总结（自动生成）

### NEMESIS：基于增强型超块集成策略的高效噪声抑制 MAE 论文总结

#### 1. 研究动机与核心问题
*   **核心问题**：3D CT 影像的自监督学习（SSL）面临两大挑战：一是全卷（Full-volume）Transformer 的内存开销巨大，导致高分辨率处理在标准硬件上不可行；二是 CT 数据具有显著的**各向异性**（轴向、冠状、矢状面结构特征不同），传统均匀掩码策略难以捕捉这种空间结构。
*   **研究背景**：医学影像标注成本极高，SSL 虽能利用无标签数据，但现有方法（如 SuPreM、VoCo）未能同时兼顾计算效率与 CT 特有的结构感应偏差。

#### 2. 核心方法论
NEMESIS 提出了一种基于**超块（Superpatch）**的掩码自编码器框架，核心组件包括：
*   **超块预训练流水线**：将 3D 体数据划分为 $128^3$ 的非重叠超块，训练时随机采样并加入高斯噪声，通过重建任务学习局部解剖结构，显著降低了内存需求。
*   **3D 自适应嵌入与 NEMESIS Token (NT)**：
    *   采用双路径嵌入：直接线性投影（LP）路径和基于自注意力块（SAB）的超块嵌入（SE）路径。
    *   引入可学习的 **NEMESIS Token (NT)** 作为辅助上下文锚点，通过自适应门控融合两路特征。
*   **掩码解剖 Transformer 块 (MATB)**：
    *   针对各向异性设计了**双重掩码（Dual-masking）**流：轴向流（Axis-wise）捕捉块内上下文，平面流（Plane-wise）学习块间关系。
*   **训练目标**：最小化掩码区域 $M$ 内原始像素与重建像素之间的均方误差（MSE）。

#### 3. 实验设计与基准测试
*   **数据集**：
    *   **预训练**：使用 TotalSegmentator 及 HNSCC、FLARE23、LUNA16 等混合公共 CT 数据集（无标签）。
    *   **下游任务**：在 **BTCV**（30 例腹部 CT）上进行 8 类多器官存在性分类。
*   **对比方法**：包括监督学习基准 ResNet3D-50，以及 SOTA 自监督方法 SuPreM 和 VoCo。
*   **评估协议**：主要采用“冻结骨干网络 + 线性分类器（Linear Probe）”模式，以验证表征质量。

#### 4. 资源与算力
*   **计算效率**：论文明确对比了计算量。NEMESIS（$128^3$ 超块）单次前向传播仅需 **31.0 GFLOPs**，而全卷基准模型（VAE Baseline）需 **985.8 GFLOPs**，计算开销降低了约 **32 倍**。
*   **硬件细节**：文中未明确提及具体的 GPU 型号、数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主干性能对比（冻结 vs 全微调）。
    *   **标签效率实验**：测试了 10%、25%、50%、100% 标签比例下的表现。
    *   **消融实验**：对比了不同掩码策略（仅轴向 vs 仅平面）对重建质量（PSNR/SSIM）的影响，以及嵌入维度和掩码率的敏感性分析。
*   **充分性评价**：实验设计较为客观，通过线性探测（Linear Probe）证明了特征的本质优越性，而非单纯依靠微调。对比了多种 SOTA 模型，实验覆盖了性能、效率和数据稀缺性三个维度，论证充分。

#### 6. 主要结论与发现
*   **性能卓越**：冻结骨干的 NEMESIS 线性分类 AUROC 达 **0.9633**，优于全微调的 SuPreM (0.9493) 和 VoCo (0.9387)。
*   **标签高效**：在仅 10% 标注数据下，AUROC 仍达 0.9075，展现出极强的实用价值。
*   **结构敏感**：实验证明平面级掩码（Plane-only）比轴向掩码对重建质量贡献更大，验证了 MATB 处理各向异性的有效性。

#### 7. 论文亮点
*   **超块策略的成功应用**：在不损失解剖细节的前提下，通过局部化处理解决了 3D Transformer 的扩展性难题。
*   **双重掩码机制**：专门针对医学 CT 的物理特性（各向异性）设计架构，而非生搬硬套自然图像的 MAE。
*   **极高的计算性价比**：在计算量缩减 97% 的情况下，性能反而超越了全量训练的模型。

#### 8. 不足与局限
*   **任务覆盖有限**：目前主要在器官分类任务上验证，尚未展示在更复杂的 3D 分割（如 Synapse 数据集）或检测任务中的表现。
*   **算力细节缺失**：未提供具体的训练环境和收敛时间，不便于其他研究者评估复现成本。
*   **超参数依赖**：超块大小（如 $64^3$ vs $128^3$）对性能和效率的平衡影响显著，在不同分辨率的 CT 数据上可能需要重新调优。

（完）
