---
title: "DAT-CFTNet: Speech Enhancement for Cochlear Implant Recipients using Attention-based Dual-Path Recurrent Neural Network"
title_zh: DAT-CFTNet：基于注意力双路径循环神经网络的人工耳蜗植入者语音增强
authors: "Nursadul Mamun, John H. L. Hansen"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06744v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于序列音频数据的注意力双路径循环神经网络
tldr: 本研究针对人工耳蜗（CI）植入者在噪声环境下听力受限的问题，提出了一种名为DAT-CFTNet的语音增强模型。该模型结合了改进的复数频率变换网络（CFTNet）和基于注意力机制的双路径循环神经网络（DAT-RNN），通过模拟人类听觉系统的注意力机制，在时频域精确区分语音与噪声。实验证明，该模型在语音清晰度和质量上优于现有模型，能有效抑制非平稳噪声并减少伪影，显著提升了CI用户的听力体验。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06744v1/fig-001.webp\", \"caption\": \"Fig. 1: (a) Basic block diagram of the proposed DAT-CFTNet and (b) the dual-path attention RNN module\", \"page\": 2, \"index\": 1, \"width\": 528, \"height\": 422}, {\"url\": \"assets/figures/arxiv/2604.06744v1/fig-002.webp\", \"caption\": \"Fig. 2: CI electrode stimulation response (electrodogram) with spectrograms with original and vocoded speech in car noise at SNR 0 dB for clean, noisy, DAT-CFTNet-F processed speech.\", \"page\": 4, \"index\": 2, \"width\": 477, \"height\": 390}]"
motivation: 旨在解决人工耳蜗植入者在复杂噪声环境下时频听力恢复受限、难以区分语音与背景噪声的问题。
method: 提出一种结合注意力机制的双路径RNN（DAT-RNN）与改进型复数频率变换网络（CFTNet）的架构，优化局部与全局上下文信息的处理。
result: 实验结果显示DAT-CFTNet在语音清晰度和质量指标上均优于CFTNet和DCCRN，并能有效消除音乐伪影。
conclusion: 该模型通过注意力机制增强了对非平稳噪声的抑制能力，为提升人工耳蜗用户的语音感知提供了有效的技术方案。
---

## 摘要
人类听觉系统能够选择性地关注音频流中的关键语音元素，同时将次要注意力分配给背景中的噪声或失真等不太相关的区域，并随时间动态调整其注意力。受近期注意力模型成功的启发，本研究在并行语音增强网络的瓶颈层中引入了一个双路径注意力模块。本研究提出了一种基于注意力的双路径循环神经网络（DAT-RNN），将其与改进的复数值频率变换网络（CFTNet）相结合，构成了 DAT-CFTNet。这种注意力机制能够精确区分语谱图时频（T-F）区域中的语音和噪声，优化了 CFTNet 中的局部和全局上下文信息处理。实验表明，DAT-CFTNet 在语音可懂度和质量方面，相较于包括 CFTNet 和 DCCRN 在内的现有模型，性能得到了持续提升。此外，所提出的模型在提高人工耳蜗（CI）植入者的语音可懂度方面表现出优异的性能，而 CI 植入者在噪声环境下的时频听觉恢复通常严重受限。针对 CI 听者的研究表明，所提出的方案能够抑制非平稳噪声，避免了传统语音增强方法中常见的音乐伪影。该模型的实现代码将公开。

## Abstract
The human auditory system has the ability to selectively focus on key speech elements in an audio stream while giving secondary attention to less relevant areas such as noise or distortion within the background, dynamically adjusting its attention over time. Inspired by the recent success of attention models, this study introduces a dual-path attention module in the bottleneck layer of a concurrent speech enhancement network. Our study proposes an attention-based dual-path RNN (DAT-RNN), which, when combined with the modified complex-valued frequency transformation network (CFTNet), forms the DAT-CFTNet. This attention mechanism allows for precise differentiation between speech and noise in time-frequency (T-F) regions of spectrograms, optimizing both local and global context information processing in the CFTNet. Our experiments suggest that the DAT-CFTNet leads to consistently improved performance over the existing models, including CFTNet and DCCRN, in terms of speech intelligibility and quality. Moreover, the proposed model exhibits superior performance in enhancing speech intelligibility for cochlear implant (CI) recipients, who are known to have severely limited T-F hearing restoration (e.g., >10%) in CI listener studies in noisy settings show the proposed solution is capable of suppressing non-stationary noise, avoiding the musical artifacts often seen in traditional speech enhancement methods. The implementation of the proposed model will be publicly available.

---

## 论文详细总结（自动生成）

这是一份关于论文《DAT-CFTNet: Speech Enhancement for Cochlear Implant Recipients using Attention-based Dual-Path Recurrent Neural Network》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：针对人工耳蜗（CI）植入者在噪声环境下语音感知能力极差的问题，开发一种高效的语音增强（SE）算法。
*   **研究背景**：CI用户仅能获得正常听力者约10%的时频（T-F）内容。传统的语音增强方法往往无法在抑制噪声的同时保留足够的语音细节，且容易产生“音乐伪影”。
*   **动机**：模拟人类听觉系统的选择性注意机制，通过深度学习模型在受限的时频空间内精确区分语音与噪声，提升CI用户的语音可懂度和质量。

### 2. 方法论：DAT-CFTNet
*   **核心思想**：将**双路径循环神经网络（DPRNN）**与**注意力机制（Attention）**相结合，集成到**复数频率变换网络（CFTNet）**的瓶颈层中。
*   **关键技术细节**：
    *   **DAT-RNN 模块**：由“块内（Intra-chunk）”和“块间（Inter-chunk）”两个子模块组成。
        *   **块内 RNN**：使用双向 LSTM（Bi-LSTM）处理局部时频段，捕捉细微的光谱特征。
        *   **块间 RNN**：使用标准 LSTM 处理跨块信息，捕捉长距离的时间依赖关系。
    *   **注意力机制**：在每个 RNN 后加入注意力模块，通过计算 Key 和 Query 得到动态掩码向量（Mask Vector），从而对显著特征进行加权，优化内存使用并提升性能。
    *   **CFTNet 架构**：采用复数卷积编码器-解码器结构，并引入频率变换块（FTB）来捕捉全局频率相关性。
    *   **轻量化设计（DAT-CFTNet-L）**：引入深度可分离卷积（DSC）替代标准卷积，将参数量从 12.4M 压缩至 4.7M。

### 3. 实验设计
*   **数据集**：
    *   **语音**：IEEE 数据库（1040 条训练，140 条验证，400 条测试）。
    *   **噪声**：AURORA 数据集中的 9 种噪声（包括大群人声、车内噪声、SSN、白噪声等）。
    *   **场景**：涵盖了“见过”的噪声和“未见过”的噪声（餐厅、火车），信噪比（SNR）设为 -5dB, 0dB, 5dB。
*   **Benchmark 与对比方法**：
    *   对比了经典的 **DCCRN**（深度复数卷积循环网络）和基础的 **CFTNet**。
    *   评估指标包括：PESQ（语音质量）、STOI（可懂度）、SISDR（信号失真比）、LSD（对数谱距离）等。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **模型规模**：标准版 DAT-CFTNet 参数量为 12.4M，轻量版 DAT-CFTNet-L 为 4.7M。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多信噪比（-5, 0, 5 dB）下的性能测试。
    *   包含了针对“未见噪声”的泛化性测试。
    *   **消融实验**：详细对比了 CFTNet、CFTNet+DPRNN、DAT-CFTNet 以及不同 FTB 布局的效果，验证了每个组件的增益。
*   **充分性评价**：实验设计较为客观，涵盖了质量、可懂度和失真度等多个维度。特别地，论文还通过**电极刺激图（Electrodogram）**模拟了 CI 用户的实际听觉输入，这在针对 CI 的研究中非常具有说服力。

### 6. 主要结论与发现
*   **性能领先**：DAT-CFTNet 在所有测试条件下均优于 DCCRN 和 CFTNet。相比基准模型，其 SISDR 提升了约 34.3%，PESQ 提升了约 6%。
*   **CI 适用性**：通过电极刺激图分析发现，该模型能有效消除非平稳噪声并保留语音谐波结构，且不产生音乐伪影，非常适合 CI 植入者。
*   **轻量化可行性**：DAT-CFTNet-L 虽然性能略有下降，但仍优于 DCCRN，且参数量减少了 3 倍，适合嵌入式 CI 设备。

### 7. 优点与亮点
*   **结构创新**：将双路径 RNN 与注意力机制结合，解决了传统 RNN 在处理长序列语音特征时的局限性。
*   **针对性强**：专门针对 CI 用户 10% 的极低时频信息恢复进行优化，而非通用的语音增强。
*   **频率建模**：利用 FTB 块重建缺失的频率组件，增强了复数域的相位恢复能力。

### 8. 不足与局限
*   **实时性验证不足**：虽然提出了轻量化模型，但未给出在实际硬件上的推理延迟（Latency）数据。
*   **主观测试缺失**：实验主要依赖客观指标（PESQ/STOI），缺乏真实人工耳蜗植入者的主观听力测试反馈。
*   **环境多样性**：虽然测试了未见噪声，但样本量（400条）相对较小，在更复杂的真实生活场景（如极度嘈杂的鸡尾酒会环境）下的表现仍待验证。

（完）
