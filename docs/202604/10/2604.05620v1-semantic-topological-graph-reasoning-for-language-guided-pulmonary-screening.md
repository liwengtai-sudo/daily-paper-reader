---
title: Semantic-Topological Graph Reasoning for Language-Guided Pulmonary Screening
title_zh: 面向语言引导肺部筛查的语义-拓扑图推理
authors: "Chenyu Xue, Yiran Liu, Mian Zhou, Jionglong Su, Zhixiang Lu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05620v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 语言引导的肺部筛查与医学图像分割
tldr: 本研究提出语义-拓扑图推理（STGR）框架，旨在解决语言引导肺部筛查中临床报告语义模糊、解剖结构重叠及模型过拟合等挑战。该框架协同LLaMA-3-V的推理能力与MedSAM的分割能力，通过意图蒸馏提取诊断指导，利用动态图推理优化掩码选择，并采用选择性非对称微调（SAFT）策略，在极低参数更新下实现了卓越的分割精度与稳定性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05620v1/fig-001.webp\", \"caption\": \"Figure 1: The overall framework of our proposed language-guided pulmonary segmentation model. The framework consists of three stages: (A) LLaMA-3-V-driven Diagnostic Semantic Generation, (B) MedSAM-based Candidate Mask Generation and (C) Graph-Structured Collaborative Reasoning.\", \"page\": 3, \"index\": 1, \"width\": 883, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2604.05620v1/fig-002.webp\", \"caption\": \"Table 1: Quantitative evaluation on pulmonary screening datasets across 5-fold cross-validation. Results are reported as mean ± standard deviation. Best results are highlighted in bold.\", \"page\": 6, \"index\": 2, \"width\": 901, \"height\": 560}, {\"url\": \"assets/figures/arxiv/2604.05620v1/fig-003.webp\", \"caption\": \"Figure 2: Qualitative comparison of segmentation results across different baseline methods. The green boxes indicate the Dice Similarity Coefficient (DSC) for each mask.\", \"page\": 7, \"index\": 3, \"width\": 883, \"height\": 642}]"
motivation: 针对临床报告语义歧义、低对比度影像中解剖结构重叠以及大规模模型在有限医疗数据上易过拟合的问题。
method: "提出STGR框架，结合意图蒸馏模块（TVID）、动态图推理掩码选择机制以及仅更新不到1%参数的选择性非对称微调（SAFT）策略。"
result: "在LIDC-IDRI数据集上达到81.5%的DSC，超越LISA模型5%以上，并表现出极高的跨折稳定性（0.6%方差）。"
conclusion: 该框架通过整合大语言模型的推理与视觉基础模型的分割能力，为稳健、上下文感知的临床肺部筛查提供了高效且领先的解决方案。
---

## 摘要
由自由文本临床指令驱动的医学图像分割是计算机辅助诊断领域的一个关键前沿。然而，现有的多模态模型和基础模型在处理临床报告的语义歧义方面存在困难，且无法消除低对比度扫描中复杂解剖重叠的歧义。此外，在有限的医学数据集上对这些庞大架构进行全量微调，不可避免地会导致严重的过拟合。为了应对这些挑战，我们提出了一种用于语言引导肺部筛查的新型语义-拓扑图推理（STGR）框架。我们的方法优雅地协同了大语言模型（LLaMA-3-V）的推理能力与视觉基础模型（MedSAM）的零样本勾画能力。具体而言，我们引入了一个文本到视觉意图蒸馏（TVID）模块来提取精确的诊断指导。为了解决解剖歧义，我们将掩码选择建模为一个动态图推理问题，其中候选病灶被建模为节点，而边则捕捉空间和语义亲和力。为了确保部署的可行性，我们引入了一种选择性非对称微调（SAFT）策略，该策略仅更新不到 1% 的参数。在 LIDC-IDRI 和 LNDb 数据集上进行的严格五折交叉验证表明，我们的框架达到了新的最先进水平。值得注意的是，它在 LIDC-IDRI 上实现了 81.5% 的 Dice 相似系数（DSC），比 LISA 等领先的基于大语言模型的工具高出 5% 以上。至关重要的是，我们的 SAFT 策略起到了强大的正则化作用，产生了卓越的跨折稳定性（0.6% 的 DSC 方差），并为稳健、上下文感知的临床部署铺平了道路。

## Abstract
Medical image segmentation driven by free-text clinical instructions is a critical frontier in computer-aided diagnosis. However, existing multimodal and foundation models struggle with the semantic ambiguity of clinical reports and fail to disambiguate complex anatomical overlaps in low-contrast scans. Furthermore, fully fine-tuning these massive architectures on limited medical datasets invariably leads to severe overfitting. To address these challenges, we propose a novel Semantic-Topological Graph Reasoning (STGR) framework for language-guided pulmonary screening. Our approach elegantly synergizes the reasoning capabilities of large language models (LLaMA-3-V) with the zero-shot delineation of vision foundation models (MedSAM). Specifically, we introduce a Text-to-Vision Intent Distillation (TVID) module to extract precise diagnostic guidance. To resolve anatomical ambiguity, we formulate mask selection as a dynamic graph reasoning problem, where candidate lesions are modeled as nodes and edges capture spatial and semantic affinities. To ensure deployment feasibility, we introduce a Selective Asymmetric Fine-Tuning (SAFT) strategy that updates less than 1% of the parameters. Rigorous 5-fold cross-validation on the LIDC-IDRI and LNDb datasets demonstrates that our framework establishes a new state-of-the-art. Notably, it achieves an 81.5% Dice Similarity Coefficient (DSC) on LIDC-IDRI, outperforming leading LLM-based tools like LISA by over 5%. Crucially, our SAFT strategy acts as a powerful regularizer, yielding exceptional cross-fold stability (0.6% DSC variance) and paving the way for robust, context-aware clinical deployment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Semantic-Topological Graph Reasoning for Language-Guided Pulmonary Screening》（面向语言引导肺部筛查的语义-拓扑图推理）的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该研究针对的是**语言引导的医学图像分割（Referring Expression Segmentation, RES）**在肺部筛查中的应用。
*   **研究动机**：传统的计算机辅助诊断（CAD）系统（如 U-Net）依赖固定标签，无法处理放射科医生灵活的自由文本报告。
*   **现有挑战**：
    1.  **语义歧义**：多模态大模型（LMMs）难以精确理解复杂的临床术语。
    2.  **解剖重叠**：在低对比度的 X 光片中，病灶常与肋骨、血管等正常组织重叠，视觉基础模型（如 MedSAM）容易产生误判。
    3.  **过拟合风险**：在有限的医疗数据集上全量微调庞大的基础模型会导致严重的灾难性遗忘和过拟合。

### 2. 方法论：核心思想与关键技术
论文提出了 **STGR 框架**，其核心思想是协同大语言模型的推理能力、视觉基础模型的分割能力以及图神经网络的拓扑建模能力。

*   **文本到视觉意图蒸馏 (TVID)**：
    *   利用 LLaMA-3-V 将复杂的临床文本分解为 $K$ 个病理属性向量。
    *   通过非线性映射网络将这些语义向量投影到视觉特征空间，生成对下游任务的“语义引导向量”。
*   **语义-拓扑图推理 (STGR)**：
    *   **候选生成**：使用 GroundingDINO 生成边界框，再由 MedSAM 生成候选掩码池。
    *   **动态图构建**：将每个候选掩码视为节点。边权重的计算结合了**空间交并比 (IoU)** 和 **语义余弦相似度**，公式化表示为：$E_{ij} = \alpha \cdot S_{spatial} + (1-\alpha) \cdot S_{semantic}$。
    *   **协同推理**：通过多层 Graph Transformer 进行消息传递，利用交叉注意力机制将诊断意图注入图节点，从而在重叠组织中精准识别目标。
*   **选择性非对称微调 (SAFT)**：
    *   冻结所有重型骨干网络（LLaMA, MedSAM, DINOv2）。
    *   仅对 LLaMA 的注意力层使用 QLoRA，并训练轻量级的映射网络和 STGR 模块。**可训练参数量仅占总量的 0.6%**。

### 3. 实验设计
*   **数据集**：
    *   **LIDC-IDRI**：用于细粒度肺结节检测。
    *   **LNDb**：用于广泛的病理浸润分析。
*   **Benchmark 与对比方法**：
    *   **标准分割模型**：U-Net, TransUNet, SegNet。
    *   **基础/指称分割模型**：MedSAM (Text-prompt), SEEM, LISA。
*   **评价指标**：交并比 (IoU) 和 Dice 相似系数 (DSC)。

### 4. 资源与算力
*   **硬件环境**：使用了由 **4 台 NVIDIA A100 GPU** 组成的集群。
*   **训练细节**：Batch size 为 16，初始学习率为 $1 \times 10^{-4}$，采用余弦退火调度，共训练 50 个 Epoch。
*   **模型规模**：使用了 LLaMA-3-V (8B 版本) 和 DINOv2 (ViT-L/14)。

### 5. 实验数量与充分性
*   **实验规模**：采用了严格的 **5 折交叉验证**，并报告了均值和标准差。
*   **消融实验**：系统地验证了 TVID 模块（+2.8% DSC）和 STGR 模块（+4.8% DSC）的增益。
*   **微调策略对比**：对比了全量微调与 SAFT 策略，证明了 SAFT 在医疗小样本场景下不仅节省资源，且性能更优（81.5% vs 79.5% DSC）。
*   **充分性评价**：实验设计涵盖了定量分析、定性可视化及消融研究，数据量和对比维度足够，结果具有统计学意义。

### 6. 主要结论与发现
*   **性能领先**：STGR 在 LIDC-IDRI 数据集上达到了 **81.5% 的 DSC**，比目前最强的 LLM 分割工具 LISA 高出 **5.3%**。
*   **稳定性极佳**：跨折验证的标准差仅为 **±0.6%**，远低于其他模型，证明了其在不同患者群体间的鲁棒性。
*   **参数高效**：通过 SAFT 策略，模型在极低计算开销下克服了过拟合问题，且性能优于全量微调。

### 7. 优点（亮点）
*   **结构化推理**：不同于以往模型简单的线性投影，STGR 通过图网络显式建模了解剖结构间的空间和语义关系，有效解决了“重叠组织误诊”问题。
*   **正则化效应**：SAFT 策略不仅是出于效率考虑，更在医疗数据稀缺的情况下起到了强大的正则化作用，防止模型“学死”。
*   **端到端协同**：成功将 LLM 的高层逻辑与视觉模型的底层像素分割无缝结合。

### 8. 不足与局限
*   **文本依赖性**：模型高度依赖人类专家提供的高质量临床文本。在缺乏详细报告的急诊或初步筛查场景下，性能可能受限。
*   **维度限制**：目前主要针对 2D 影像进行处理。虽然肺部筛查常用 CT，但论文尚未展示其在 3D 拓扑分析上的扩展效果。
*   **实时性未详述**：虽然参数量少，但推理过程中涉及多个大型基础模型的串联（LLaMA + GroundingDINO + MedSAM），其实际推理延迟（Latency）有待进一步评估。

（完）
