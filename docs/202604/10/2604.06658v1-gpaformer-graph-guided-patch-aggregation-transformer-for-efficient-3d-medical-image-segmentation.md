---
title: "GPAFormer: Graph-guided Patch Aggregation Transformer for Efficient 3D Medical Image Segmentation"
title_zh: GPAFormer：用于高效三维医学图像分割的图引导补丁聚合 Transformer
authors: "Chung-Ming Lo, I-Yun Liu, Wei-Yang Lin"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06658v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于3D医学图像分割的Transformer架构
tldr: GPAFormer是一种专为3D医学图像分割设计的轻量化Transformer架构，旨在解决多器官分割中精度与效率难以兼得的挑战。该模型通过多尺度注意力引导堆叠聚合（MASA）和互感知块图聚合器（MPGA）模块，实现了对不同尺寸结构的高效建模与特征聚合。在仅有1.81M参数量的情况下，GPAFormer在BTCV、Synapse等多个数据集上取得了领先的分割精度，且推理速度极快，显著提升了在资源受限临床环境下的实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06658v1/fig-001.webp\", \"caption\": \"Fig. 8. Segmentation result on ACDC dataset (a) the original MRI image (b) ground truth (c) GPAFormer (Ours) (d) nnUNet (e) nnFormer (f) Swin-UNETR (g) UNETR (h) UNETR++ (i) SegFormer3D\", \"page\": 11, \"index\": 1, \"width\": 1073, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2604.06658v1/fig-002.webp\", \"caption\": \"TABLE IV The DSC (%) results of ACDC dataset TABLE V The DSC(%) result of BraTS dataset\", \"page\": 10, \"index\": 2, \"width\": 1098, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2604.06658v1/fig-003.webp\", \"caption\": \"TABLE III The DSC (%) results of Synapse dataset\", \"page\": 10, \"index\": 3, \"width\": 1027, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2604.06658v1/fig-004.webp\", \"caption\": \"Fig. 7. Segmentation results on Synapse dataset (a) the original CT image (b) ground truth (c) GPAFormer (Ours) (d) nnUNet (e) nnFormer (f) Swin-UNETR (g) UNETR (h) UNETR++ (i) SegFormer3D\", \"page\": 10, \"index\": 4, \"width\": 1073, \"height\": 180}, {\"url\": \"assets/figures/arxiv/2604.06658v1/fig-005.webp\", \"caption\": \"Fig. 5. Visualization comparison between the MPGA module introduced in GPAFormer (top row) and fixed aggregation methods (bottom row).\", \"page\": 8, \"index\": 5, \"width\": 690, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.06658v1/fig-006.webp\", \"caption\": \"Fig. 1. The architecture of GPAFormer\", \"page\": 4, \"index\": 6, \"width\": 1062, \"height\": 417}]"
motivation: 针对3D医学图像分割中由于数据高维性和结构异质性导致的计算效率与分割精度之间的平衡难题。
method: 提出了结合多尺度特征聚合（MASA）与基于图引导的动态块聚合（MPGA）的轻量化Transformer架构。
result: 在BTCV、Synapse等四个主流数据集上以仅1.81M的参数量实现了最高的DSC评分，且单例推理时间不足一秒。
conclusion: GPAFormer在多模态3D分割任务中实现了精度与效率的卓越平衡，具有极高的临床应用潜力。
---

## 摘要
深度学习已广泛应用于三维医学图像分割任务。然而，由于成像模态的多样性、数据的高维特性以及解剖结构的异质性，在多器官分割中同时实现分割精度和计算效率仍是一个挑战。本研究提出了 GPAFormer，这是一种专门为三维医学图像分割设计的轻量级网络架构，在保持高精度的同时强调效率。GPAFormer 包含两个核心模块：多尺度注意力引导堆叠聚合（MASA）和互感知补丁图聚合器（MPGA）。MASA 利用具有不同感受野的三条并行路径，通过平面聚合进行组合，以增强网络处理不同尺寸结构的能力。MPGA 采用图引导方法，根据补丁间的特征相似性和空间邻接性，动态聚合具有相似特征分布的区域，从而提高对器官内部和边界结构的辨别力。实验在包括 BTCV、Synapse、ACDC 和 BraTS 在内的公开全身 CT 和 MRI 数据集上进行。与现有的三维分割网络相比，仅使用 1.81 M 参数的 GPAFormer 在 BTCV (75.70%)、Synapse (81.20%)、ACDC (89.32%) 和 BraTS (82.74%) 上均实现了最高的 DSC。使用消费级 GPU，BTCV 一个验证病例的推理时间不到一秒。结果表明，GPAFormer 在各种临床场景下的多器官、多模态三维分割任务中平衡了准确性和效率，特别适用于资源受限和时间敏感的临床环境。

## Abstract
Deep learning has been widely applied to 3D medical image segmentation tasks. However, due to the diversity of imaging modalities, the high-dimensional nature of the data, and the heterogeneity of anatomical structures, achieving both segmentation accuracy and computational efficiency in multi-organ segmentation remains a challenge. This study proposed GPAFormer, a lightweight network architecture specifically designed for 3D medical image segmentation, emphasizing efficiency while keeping high accuracy. GPAFormer incorporated two core modules: the multi-scale attention-guided stacked aggregation (MASA) and the mutual-aware patch graph aggregator (MPGA). MASA utilized three parallel paths with different receptive fields, combined through planar aggregation, to enhance the network's capability in handling structures of varying sizes. MPGA employed a graph-guided approach to dynamically aggregate regions with similar feature distributions based on inter-patch feature similarity and spatial adjacency, thereby improving the discrimination of both internal and boundary structures of organs. Experiments were performed on public whole-body CT and MRI datasets including BTCV, Synapse, ACDC, and BraTS. Compared to the existed 3D segmentation networkd, GPAFormer using only 1.81 M parameters achieved overall highest DSC on BTCV (75.70%), Synapse (81.20%), ACDC (89.32%), and BraTS (82.74%). Using consumer level GPU, the inference time for one validation case of BTCV spent less than one second. The results demonstrated that GPAFormer balanced accuracy and efficiency in multi-organ, multi-modality 3D segmentation tasks across various clinical scenarios especially for resource-constrained and time-sensitive clinical environments.

---

## 论文详细总结（自动生成）

以下是对论文《GPAFormer: Graph-guided Patch Aggregation Transformer for Efficient 3D Medical Image Segmentation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
医学影像（CT/MRI）在现代医疗中需求激增，但人工勾画器官耗时且存在个体差异。虽然深度学习（尤其是 Transformer 架构）在 3D 分割中表现优异，但面临两大痛点：
*   **计算开销巨大**：Transformer 的自注意力机制计算复杂度随序列长度呈平方增长，导致模型参数量大、推理慢，难以部署在资源受限的临床边缘设备（如手术机器人、扫描仪）或实时场景（如术中导航）。
*   **特征聚合粗糙**：现有的轻量化手段（如固定步长的卷积或池化）在下采样时会无差别地合并不同组织的补丁，容易模糊器官边界。
**研究动机**：开发一种既能保持高精度，又能实现极低参数量和极快推理速度的轻量化 3D 分割网络。

### 2. 方法论：核心思想与关键技术
GPAFormer 采用三阶段的 Transformer 编码器结构，核心由两个创新模块驱动：
*   **MASA（多尺度注意力引导堆叠聚合模块）**：
    *   **核心思想**：通过并行路径捕捉不同尺度的解剖结构。
    *   **技术细节**：设计了三条并行卷积路径，分别使用 $3\times3\times3$（精细尺度，捕捉边缘）、$5\times5\times5$（中等尺度）和 $7\times7\times7$（粗糙尺度，捕捉整体轮廓）的卷积核。
    *   **融合机制**：将三路特征进行元素级相加（Element-wise Summation），随后引入自注意力机制分配权重，实现全局上下文与多尺度局部特征的深度融合。
*   **MPGA（互感知补丁图聚合器模块）**：
    *   **核心思想**：利用图论动态聚合语义相似的补丁，而非固定区域聚合。
    *   **算法流程**：
        1.  **建图**：将图像补丁视为节点，在 $3\times3\times3$ 空间邻域内建立边。
        2.  **相似度计算**：利用余弦相似度计算相邻补丁间的特征一致性。
        3.  **动态聚合**：通过可学习的分配矩阵（Assignment Matrix），将语义相似（如同属一个器官内部）的补丁合并为代表性节点，而对异质区域（如器官边界）保持独立。
    *   **作用**：显著缩短了 Transformer 的输入序列长度，降低了计算量，同时强化了边界辨识力。

### 3. 实验设计
*   **数据集**：涵盖了 CT 和 MRI 两种模态的四个主流公开数据集：
    *   **BTCV & Synapse**：腹部多器官 CT 分割（13类/8类）。
    *   **ACDC**：心脏 MRI 分割（右心室、心肌、左心室）。
    *   **BraTS**：脑肿瘤 MRI 分割（水肿、肿瘤核心、增强肿瘤）。
*   **Benchmark 与对比方法**：对比了 nnUNet（CNN 标杆）、nnFormer、UNETR、UNETR++、SegFormer3D（轻量化标杆）和 Swin-UNETR。
*   **评价指标**：Dice 相似系数（DSC）、参数量（Parameters）、计算量（FLOPs）及推理时间。

### 4. 资源与算力
*   **硬件环境**：Intel Core i7-12700 CPU，64GB RAM，**NVIDIA GeForce RTX 4090 GPU (24GB)**。
*   **软件框架**：PyTorch v1.12.1，MONAI 库。
*   **训练细节**：使用 AdamW 优化器，初始学习率 0.0001，采用 DiceCE 混合损失函数，设置了早停机制（10个验证步未提升则停止），每 500 次迭代进行一次验证。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同任务的数据集上进行了测试，并采用了 **5 折交叉验证**，确保了结果的统计可靠性。
*   **消融实验**：专门针对 MASA 和 MPGA 模块进行了组合测试。结果显示，仅加入 MPGA 提升了 1.32% DSC，仅加入 MASA 提升了 1.95% DSC，两者结合则提升了 4.48% DSC（在 BTCV 上从 71.22% 升至 75.70%）。
*   **客观性**：对比方法涵盖了从经典 CNN 到最新 Transformer 的多种架构，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **极致轻量化**：GPAFormer 的参数量仅为 **1.81 M**，约为 SegFormer3D 的 40%，远低于 Swin-UNETR（62.19 M）和 nnFormer（149.32 M）。
*   **精度领先**：在所有四个数据集上均取得了最高的平均 DSC 分数（BTCV: 75.70%, Synapse: 81.20%, ACDC: 89.32%, BraTS: 82.74%）。
*   **推理高效**：在消费级 GPU 上，单个病例的推理时间不足 1 秒，满足临床实时性要求。
*   **边界处理**：可视化结果证明，基于图引导的动态聚合能比固定步长方法更精准地还原器官轮廓，减少了边界处的误分类。

### 7. 优点（亮点）
*   **动态聚合机制**：MPGA 模块打破了传统 Transformer 固定 Patch 划分的局限，通过图结构实现了“语义感知”的特征压缩。
*   **多尺度融合**：MASA 模块通过并行异质感受野设计，有效解决了医学图像中器官大小差异极大的问题。
*   **普适性强**：在 CT 和 MRI 两种模态、多个解剖部位（腹部、心脏、大脑）均表现稳健，证明了模型的泛化能力。

### 8. 不足与局限
*   **小器官挑战**：尽管整体精度最高，但在某些极小结构（如肾上腺 RAG/LAG）上的表现仍有提升空间，这受限于轻量化模型对极细微特征的捕捉能力。
*   **数据规模限制**：实验主要基于公开数据集，尚未在更大规模、包含更多病理多样性的私有临床数据上进行广泛验证。
*   **先验知识利用**：目前模型主要依赖数据驱动，未显式引入解剖学先验约束（如器官间的相对位置关系）。

（完）
