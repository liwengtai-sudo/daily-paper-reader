---
title: "Tensor-Augmented Convolutional Neural Networks: Enhancing Expressivity with Generic Tensor Kernels"
title_zh: 张量增强卷积神经网络：利用通用张量核提升表达能力
authors: "Chia-Wei Hsing, Wei-Lin Tu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08072v1"
tags: ["query:dl"]
score: 9.0
evidence: 使用通用张量核增强卷积神经网络的表达能力
tldr: 针对传统卷积神经网络（CNN）依赖深层架构捕获复杂特征导致计算开销大且可解释性差的问题，本文提出张量增强卷积神经网络（TACNN）。该模型受量子物理启发，利用高阶张量替换传统卷积核，通过多线性形式捕捉高阶特征相关性。实验表明，仅需两层卷积的TACNN在Fashion-MNIST上的表现即可媲美VGG-16等深层模型，实现了高表达能力与简洁架构的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08072v1/fig-001.webp\", \"caption\": \"FIG. 2. Test accuracies of the Fashion-MNIST dataset given by TACNN (red) and comparable CNN (blue) with one convolution layer for varying kernel count 2m, where m = 0, 1, . . . , 11. For TACNN with two convolution layers (orange) the kernel counts of the second layer are 16×16, 32×32 and 64×64. Comparison with very deep CNNs VGG-16 (green) and GoogLeNet (purple) given by Ref. [25] further demonstrates the supremacy of TACNN in terms of parameter efficiency.\", \"page\": 4, \"index\": 1, \"width\": 510, \"height\": 314}]"
motivation: 传统CNN需要极深的网络才能捕捉复杂相关性，导致计算成本高且模型难以解释。
method: 提出TACNN模型，将传统卷积核替换为能编码量子叠加态的高阶张量，以多线性形式提取高阶特征。
result: "在Fashion-MNIST数据集上，仅含两层卷积的TACNN准确率达到93.7%，优于或等同于VGG-16和GoogLeNet等深层模型。"
conclusion: TACNN在保持架构简洁的同时显著增强了模型表达能力，为构建高效且可解释的深度学习模型提供了新路径。
---

## 摘要
卷积神经网络 (CNN) 擅长分层提取局部特征，但其捕获复杂相关性的性能严重依赖于深层架构，而这些架构通常计算量大且难以解释。为了解决这些问题，我们提出了一种物理引导的浅层模型：张量增强卷积神经网络 (TACNN)，它利用通用张量取代传统的卷积核，以增强表示能力。这一选择的动机在于，$N$ 阶张量能够自然地编码 $d^N$ 维希尔伯特空间（其中 $d$ 为局部物理维度）中的任意量子叠加态，从而提供显著更丰富的表达能力。此外，在我们的设计中，每一层的卷积输出都变成了一个能够捕获高阶特征相关性的多线性形式，从而使浅层多层架构具备了可与深层 CNN 媲美的表达能力。在 Fashion-MNIST 基准测试中，TACNN 展现出优于传统 CNN 的明显优势，仅需少量层数即可达到卓越的准确率。特别地，仅包含两个卷积层的 TACNN 达到了 93.7% 的测试准确率，超越或持平了 VGG-16 (93.5%) 和 GoogLeNet (93.7%) 等深度显著增加的模型。这些研究结果表明，TACNN 是一个极具前景的框架，它在保持架构简洁的同时增强了模型的表达能力，为开发更具可解释性和高效的深度学习模型铺平了道路。

## Abstract
Convolutional Neural Networks (CNNs) excel at extracting local features hierarchically, but their performance in capturing complex correlations hinges heavily on deep architectures, which are usually computationally demanding and difficult to interpret. To address these issues, we propose a physically-guided shallow model: tensor-augmented CNN (TACNN), which replaces conventional convolution kernels with generic tensors to enhance representational capacity. This choice is motivated by the fact that an order-$N$ tensor naturally encodes an arbitrary quantum superposition state in the Hilbert space of dimension $d^N$, where $d$ is the local physical dimension, thus offering substantially richer expressivity. Furthermore, in our design the convolution output of each layer becomes a multilinear form capable of capturing high-order feature correlations, thereby equipping a shallow multilayer architecture with an expressive power competitive to that of deep CNNs. On the Fashion-MNIST benchmark, TACNN demonstrates clear advantages over conventional CNNs, achieving remarkable accuracies with only a few layers. In particular, a TACNN with only two convolution layers attains a test accuracy of 93.7$\%$, surpassing or matching considerably deeper models such as VGG-16 (93.5$\%$) and GoogLeNet (93.7$\%$). These findings highlight TACNN as a promising framework that strengthens model expressivity while preserving architectural simplicity, paving the way towards more interpretable and efficient deep learning models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tensor-Augmented Convolutional Neural Networks: Enhancing Expressivity with Generic Tensor Kernels》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统卷积神经网络（CNN）依赖增加网络深度来捕获复杂的高阶特征相关性，这导致了巨大的计算开销、参数冗余以及模型可解释性的缺失。
*   **研究背景**：虽然张量网络（TN）在处理量子多体系统长程关联方面表现出色，但在处理以局部统计规律为主的经典图像数据时，其表现往往逊于深层CNN。
*   **整体含义**：本文旨在结合物理启发式的张量表示与CNN的局部特征提取优势，通过将卷积核“张量化”，在浅层架构下实现深层网络的表达能力。

### 2. 方法论：TACNN 核心思想与技术细节
*   **核心思想**：将传统CNN中的线性卷积核替换为**通用高阶张量（Generic Tensor）**。每个张量核被视为希尔伯特空间中的一个量子叠加态，能够自然地编码局部像素间的所有可能相关性。
*   **关键技术流程**：
    1.  **特征编码**：将归一化后的像素值 $x$ 映射到二维希尔伯特空间，形成量子态 $|x\rangle = [x, 1-x]^T$。一个局部补丁（Patch）被表示为 $N$ 个像素的张量积态 $|\phi\rangle$。
    2.  **张量卷积运算**：卷积操作定义为补丁态 $|\phi\rangle$ 与张量核态 $|\psi\rangle$ 的内积。输出结果是一个关于输入像素的**多线性形式**，这使得单层网络就能捕捉高阶非线性特征。
    3.  **多层扩展与归一化**：为了堆叠多层，作者设计了一种平滑归一化方案（均值/标准差归一化 + Sigmoid 激活），确保层间信息传递的稳定性并引入额外的非线性。
    4.  **参数化**：张量核的振幅 $c(s)$ 是可训练参数。对于 $3 \times 3$ 的补丁，张量核拥有 $2^9 = 512$ 个参数，远多于传统卷积核的 9 个，但这种设计显著提升了单核的表达效率。

### 3. 实验设计与 Benchmark
*   **数据集**：主要使用 **Fashion-MNIST**。该数据集比标准 MNIST 更具挑战性，包含 70,000 张 28x28 的灰度图。
*   **对比方法**：
    *   **传统 CNN**：包括 Vanilla CNN（同等层数）、VGG-16、GoogLeNet、AlexNet。
    *   **张量网络模型**：MPS（矩阵乘积态）、PEPS（投影纠缠对态）、TTN（树张量网络）等。
    *   **传统机器学习**：K-SVM、XGBoost。
*   **评估指标**：测试准确率（Test Accuracy）与参数效率。

### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA GPUs**（具体型号未注明）。
*   **软件框架**：基于 **PyTorch** 实现。
*   **训练细节**：采用 Adam 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 100。单层模型训练 400 轮，双层模型训练 800 轮。
*   **算力说明**：文中未明确给出具体的训练总时长或 GPU 数量，但强调了 TACNN 在参数量更少的情况下达到了极高的准确率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对卷积核数量（从 1 到 2048）进行了详尽的消融实验。
    *   所有数据均基于 **5 个随机种子**的平均值和标准差，保证了结果的统计可靠性。
    *   对比了超过 10 种不同的先进模型（包括深度学习和量子启发式模型）。
*   **客观性**：实验对比了在不使用数据增强（DA）情况下的模型表现，确保了与基准模型（如 VGG-16）对比的公平性。

### 6. 主要结论与发现
*   **性能卓越**：仅有 **2 层卷积** 的 TACNN 在 Fashion-MNIST 上达到了 **93.7%** 的准确率，持平了深度极大的 GoogLeNet，并超越了 VGG-16。
*   **单核表达力极强**：在仅使用 1 个卷积核的极端情况下，TACNN 的表现远超传统 CNN，且数值稳定性更高。
*   **参数效率高**：TACNN 虽然在卷积层参数较多，但由于其强大的表达力允许使用更浅的网络和更少的隐藏层神经元，整体参数量显著低于同性能的深层 CNN（如比 GoogLeNet 节省约 33.6% 的参数）。
*   **优于纯 TN 模型**：TACNN 克服了传统张量网络模型在经典图像任务上准确率不高的瓶颈。

### 7. 优点与亮点
*   **物理与 AI 的深度融合**：利用量子叠加态的概念增强卷积核，理论基础扎实且直观。
*   **架构简洁性**：通过提升“单层”的质量来规避“深度”带来的复杂性，提高了模型的可解释性。
*   **抗过拟合能力**：尽管张量核参数较多，但在实验中未表现出明显的过拟合，显示出良好的泛化性能。
*   **NISQ 友好性**：由于张量核规模较小（如 9 个量子比特），该算法未来易于在近期的量子硬件上实现。

### 8. 不足与局限
*   **计算复杂度风险**：随着卷积核尺寸（Kernel Size）增大，张量参数量呈指数级增长（$2^{L \times L}$），这限制了 TACNN 使用大尺寸卷积核的能力。
*   **数据集覆盖面**：目前仅在 Fashion-MNIST 等小型基准数据集上验证，尚未在 ImageNet 等大规模高分辨率彩色数据集上进行测试。
*   **优化挑战**：文中提到，在没有目标哈密顿量引导的情况下，如何进一步优化张量网络结构以实现更高程度的信息压缩仍是一个开放问题。

（完）
