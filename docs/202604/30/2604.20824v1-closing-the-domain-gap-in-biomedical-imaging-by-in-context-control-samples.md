---
title: Closing the Domain Gap in Biomedical Imaging by In-Context Control Samples
title_zh: 通过上下文控制样本缩小生物医学成像中的领域差距
authors: "Ana Sanchez-Fernandez, Thomas Pinetz, Werner Zellinger, Günter Klambauer"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20824v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 利用深度学习自适应消除生物医学影像中的领域差异
tldr: 生物医学成像中的批次效应会导致深度学习模型在处理新实验数据时性能大幅下降。本研究提出了一种名为CS-ARM-BN的元学习自适应方法，利用实验中自带的阴性对照样本作为稳定上下文进行模型调整。在JUMP-CP数据集的药物作用机制分类任务中，该方法成功将新批次的准确率从0.862提升至0.935，首次证明了元学习能有效消除生物成像中的领域差异，显著提升了模型的实用性和鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20824v1/fig-001.webp\", \"caption\": \"Figure 2. Representation of batch effects in microscopy imaging data considered as a multi-source domain adaptation (MSDA) problem. In this setting, each source consists of different experimental conditions, e.g. different plates. In each domain, an image of the same class is depicted, which represents a particular mechanism-of-action (MoA). Control samples are unperturbed samples, that are present in every domain, and which we represent with the c symbol.\", \"page\": 2, \"index\": 1, \"width\": 813, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.20824v1/fig-002.webp\", \"caption\": \"Table 1. MoA prediction accuracy on in-domain and unseen experimental batches from a new domain. Column \\\"TTA\\\" indicates that the method can adapt itself given samples from a new domain at test time, i.e. test time adaptation. “In-context” means that the model adapts its parameters based on the test samples through in-context learning. The accuracy values denote mean and standard deviation over 5-fold cross-validation, thus five training re-runs of each method. \\\"FM\\\" indicates a foundation model.\", \"page\": 7, \"index\": 2, \"width\": 1008, \"height\": 423}, {\"url\": \"assets/figures/arxiv/2604.20824v1/fig-003.webp\", \"caption\": \"Table 4. Comparison of MoA classification accuracy of different methods across target sources (S8, S3, and S6) under different degrees of label shift. A lower α denotes a higher degree of label shift. The accuracy values denote mean and standard deviation over re-runs with three different seeds.\", \"page\": 10, \"index\": 3, \"width\": 1017, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.20824v1/fig-004.webp\", \"caption\": \"Table 2. Comparison of MoA classification accuracy of different methods across target sources. The element before the arrow indicates the training domain, the element after the arrow indicates the evaluation domain. The accuracy values denote the mean and standard deviation over 3 re-runs with different seeds.\", \"page\": 8, \"index\": 4, \"width\": 825, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2604.20824v1/fig-005.webp\", \"caption\": \"Figure 3. Graphic representation of our method, CS-ARM-BN, and comparison to ARM-BN (Zhang et al., 2021). Both are meta-learning methods that are be modified at test-time by using the BN statistics from the target domain (lilac). CS-ARM-BN uses control samples both at training and at inference time, which provides stability when (b) the number of perturbed samples is small or (c) the label distribution is shifted.\", \"page\": 4, \"index\": 5, \"width\": 995, \"height\": 324}]"
motivation: 批次效应引起的系统性技术偏差严重损害了实验的可重复性，并导致深度学习模型在处理新实验批次时因领域偏移而失效。
method: 提出CS-ARM-BN元学习方法，利用生物实验中固有的阴性对照样本作为上下文信息，通过批归一化进行自适应风险最小化。
result: 在大规模JUMP-CP数据集上，该方法将模型在新批次上的准确率从0.862恢复至0.935，基本消除了与训练域之间的性能差距。
conclusion: 通过原则性的上下文自适应方法可以有效中和生物成像中的批次效应，使深度学习模型在实际跨实验室应用中更加高效可靠。
---

## 摘要
生物医学成像中的核心问题是批次效应（batch effects）：即与感兴趣的生物信号无关的系统性技术变异。这些批次效应严重损害了实验的可重复性，是深度学习系统在新的实验批次上失效的主要原因，阻碍了其在现实世界中的实际应用。尽管经过多年的研究，尚无方法能成功消除深度学习模型的这种性能差距。我们提出了通过批归一化实现的控制稳定自适应风险最小化（CS-ARM-BN），这是一种利用阴性控制样本（negative control samples）的元学习自适应方法。这种未经处理的参考图像在设计上存在于每个实验批次中，并作为自适应的稳定上下文。我们在大规模 JUMP-CP 数据集上，针对药物研发中的关键任务——作用机制（MoA）分类，验证了我们的新方法。标准 ResNet 的准确率从训练域的 0.939 ± 0.005 下降到新实验批次数据上的 0.862 ± 0.060。基础模型即使在经过典型变异归一化（Typical Variation Normalization）后，也无法消除这一差距。我们首次证明元学习方法通过达到 0.935 ± 0.018 的准确率缩小了领域差距。如果新的实验批次表现出强烈的领域偏移（例如在不同的实验室生成），元学习方法可以通过生物医学实验中始终可用的控制样本来保持稳定。我们的工作表明，生物成像数据中的批次效应可以通过有原则的上下文自适应（in-context adaptation）得到有效中和，从而使其具有实际可用性和高效性。

## Abstract
The central problem in biomedical imaging are batch effects: systematic technical variations unrelated to the biological signal of interest. These batch effects critically undermine experimental reproducibility and are the primary cause of failure of deep learning systems on new experimental batches, preventing their practical use in the real world. Despite years of research, no method has succeeded in closing this performance gap for deep learning models. We propose Control-Stabilized Adaptive Risk Minimization via Batch Normalization (CS-ARM-BN), a meta-learning adaptation method that exploits negative control samples. Such unperturbed reference images are present in every experimental batch by design and serve as stable context for adaptation. We validate our novel method on Mechanism-of-Action (MoA) classification, a crucial task for drug discovery, on the large-scale JUMP-CP dataset. The accuracy of standard ResNets drops from 0.939 $\pm$ 0.005, on the training domain, to 0.862 $\pm$ 0.060 on data from new experimental batches. Foundation models, even after Typical Variation Normalization, fail to close this gap. We are the first to show that meta-learning approaches close the domain gap by achieving 0.935 $\pm$ 0.018. If the new experimental batches exhibit strong domain shifts, such as being generated in a different lab, meta-learning approaches can be stabilized with control samples, which are always available in biomedical experiments. Our work shows that batch effects in bioimaging data can be effectively neutralized through principled in-context adaptation, which also makes them practically usable and efficient.

---

## 论文详细总结（自动生成）

以下是对论文《Closing the Domain Gap in Biomedical Imaging by In-Context Control Samples》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：生物医学成像中的**批次效应（Batch Effects）**。这是指由于实验条件（如试剂、光照、仪器设置、操作员等）的微小变化导致的系统性技术变异。
*   **研究动机**：批次效应往往比感兴趣的生物信号更显著，导致深度学习模型在训练集之外的新实验批次上性能大幅下降（领域偏移）。
*   **背景**：传统的领域自适应（UDA）方法通常需要针对每个新领域重新训练，且假设源域较少、样本较多，这与生物成像中“多源、小样本、序列化产生”的特点不符。

### 2. 论文提出的方法论：CS-ARM-BN
*   **核心思想**：提出**控制稳定自适应风险最小化（CS-ARM-BN）**。该方法利用生物实验设计中天然存在的“阴性对照样本”（Negative Controls，即未受药物处理的参考样本）作为稳定的上下文信息，在推理阶段动态调整模型的批归一化（Batch Normalization, BN）统计量。
*   **关键技术细节**：
    *   **元学习框架**：基于 ARM（Adaptive Risk Minimization），在训练阶段就模拟“根据上下文进行自适应”的过程，使模型学会如何利用批次统计量。
    *   **上下文构建**：不同于以往仅利用测试样本（可能存在标签偏移或样本量不足）的方法，CS-ARM-BN 将**对照样本**与**受扰动样本**结合，共同计算 BN 层的均值和方差。
    *   **偏差-方差权衡**：通过数学证明（见附录 F），引入对照样本可以显著降低在标签偏移（Label Shift）情况下的估计偏差，并减小在小样本情况下的估计方差。
*   **算法流程**：在推理时，模型接收一个新批次的数据（包含少量待预测样本和若干对照样本），计算这些样本在各 BN 层的实时统计量，替换掉训练时存储的全局统计量，从而实现“即插即用”的自适应。

### 3. 实验设计
*   **数据集**：使用大规模 **JUMP-CP** 数据集（细胞绘画图像），包含来自不同药企和机构的多个来源（Source 3, 8, 6）。
*   **任务场景**：
    1.  **轻微领域偏移**：同一来源内的新实验批次。
    2.  **强领域偏移**：跨实验室/跨机构的数据迁移（如 Source 3 迁移至 Source 8）。
    3.  **标签偏移（Label Shift）**：测试集中各类别的比例与训练集严重不一致（模拟实际药物筛选场景）。
    4.  **小样本场景**：测试批次中仅有极少量样本。
*   **Benchmark 与对比方法**：
    *   **基准**：ResNet50（BN/IN）、基础模型（CA-MAE）、CellProfiler 手工特征。
    *   **领域自适应**：DANN（对抗）、CORAL（相关性对齐）、TVN（典型变异归一化）。
    *   **测试时自适应（TTA）**：AdaBN、TENT、StyleID（基于扩散模型的风格迁移）。
    *   **元学习**：ARM-BN、ARM-CML。

### 4. 资源与算力
*   **硬件**：使用了单张 **Nvidia A40 或 A100 GPU**。
*   **训练时长**：
    *   实验 5.1/5.3：最大 400 个 epoch，早停策略（patience=80）。
    *   实验 5.2/5.4：最大 200 个 epoch，早停策略（patience=20）。
*   **实现细节**：图像分辨率下采样至 512x512，训练时进一步缩放至 256x256。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从同源新批次到跨机构迁移的多种复杂场景，并专门设计了针对标签偏移（通过 Dirichlet 分布采样模拟不同程度的 skew）和不同批次大小的消融实验。
*   **充分性与公平性**：
    *   采用了 **5 折交叉验证**，并报告了均值和标准差。
    *   对比了包括最新基础模型（Foundation Models）在内的 10 余种方法。
    *   **客观性**：作者特别对比了在相同样本量下（给对比方法也加上对照样本）的表现，证明了 CS-ARM-BN 的优势不仅来自样本量增加，更来自元学习机制。

### 6. 论文的主要结论与发现
*   **消除领域差距**：CS-ARM-BN 在新批次上的准确率（0.935）几乎追平了训练域内的表现（0.939），这是首次证明元学习能有效中和生物成像中的批次效应。
*   **鲁棒性极强**：在极端标签偏移（$\alpha=0.01$）下，传统 TTA 方法（如 TENT, AdaBN）准确率会从 ~0.8 崩塌至 ~0.15，而 CS-ARM-BN 仍能保持在 0.89 以上。
*   **架构通用性**：该方法不仅适用于 ResNet，在基于 Vision Transformer 的 LeViT 架构上也同样有效。

### 7. 优点（亮点）
*   **利用领域知识**：巧妙地将生物实验中必不可少的“阴性对照”转化为机器学习中的“自适应上下文”，具有极强的实际应用价值。
*   **计算高效**：无需在测试时进行昂贵的梯度更新（不同于 TENT 或其他 TTT 方法），仅需一次前向传播即可完成自适应。
*   **理论支撑**：提供了详细的偏差-方差分析，解释了为什么引入对照样本能稳定 BN 统计量。

### 8. 不足与局限
*   **依赖 BN 层**：该方法核心依赖于 Batch Normalization 架构。对于使用 LayerNorm 或其他归一化方式的模型（如标准 ViT），需要调整架构才能应用。
*   **对照样本假设**：假设每个批次都必须包含对照样本。虽然这在标准高通量筛选中是常态，但在某些非标准或回顾性数据集中可能缺失。
*   **类别覆盖**：实验主要集中在 8 类明确的 MoA 分类上，对于类别数量极多或存在未知类别的开放集识别场景，其表现尚待验证。

（完）
