---
title: "DiffuSAM: Diffusion-Based Prompt-Free SAM2 for Few-Shot and Source-Free Medical Image Segmentation"
title_zh: DiffuSAM：基于扩散模型的无提示 SAM2，用于少样本和无源医学图像分割
authors: "Tal Grossman, Noa Cahan, Lev Ayzenberg, Hayit Greenspan"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24719v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 基于扩散模型的SAM2医疗图像分割适配
tldr: 针对SAM2在医疗影像中依赖专家提示且跨域泛化差的问题，本文提出DiffuSAM。该框架通过轻量化扩散先验从冻结的SAM2特征中合成掩码嵌入，实现了无需人工提示的自动分割。通过引入空间一致性约束并应用于少样本及无源域自适应场景，DiffuSAM在CT和MRI数据集上表现出色，显著提升了医疗分割的效率与自动化水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24719v1/fig-001.webp\", \"caption\": \"Fig. 3: Diffusion inference steps. Top: CT; Bottom: MRI.\", \"page\": 3, \"index\": 1, \"width\": 486, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.24719v1/fig-002.webp\", \"caption\": \"Table 1: Few-shot settings: (a) model requirements and (b) Dice results per organ on BTCV dataset.\", \"page\": 4, \"index\": 2, \"width\": 502, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2604.24719v1/fig-003.webp\", \"caption\": \"Fig. 1: An outline of our proposed method. (a) In our proposed network, we add a diffusion prior (in Green) to generate memory embeddings from the image embeddings, thereby removing the need for a user prompt (in Red). During training, the SAM2 components all remain frozen. (b) Diffusion Prior model. During training, a diffusion model takes a memory embedding to which noise has been added, the matching image embedding, an embedding of the current time step, and a label. The system learns to use the image embedding to remove the noise in successive time steps. In inference, it generates a memory embedding by starting with pure noise and an image embedding and removing noise iteratively according to that embedding.\", \"page\": 2, \"index\": 3, \"width\": 477, \"height\": 513}]"
motivation: 旨在解决SAM2在医疗影像应用中需要复杂专家提示以及在自然图像上预训练导致的领域偏移问题。
method: 采用扩散先验模型从SAM2图像特征中生成掩码嵌入，并结合历史切片信息以增强三维空间一致性。
result: 在BTCV和CHAOS数据集的少样本及无源无监督域自适应任务中，该方法取得了具有竞争力的分割精度和高效的推理速度。
conclusion: DiffuSAM通过扩散模型与SAM2的结合，成功实现了医疗影像的无提示自动分割，为低资源医学图像处理提供了新方案。
---

## 摘要
Segment Anything Model (SAM) 和 SAM2 等分割模型实现了强大的提示驱动零样本性能。然而，它们在自然图像上的训练限制了向医学数据的领域迁移。因此，准确的分割通常需要大量的微调和专家设计的提示。我们提出了 DiffuSAM，这是一种基于扩散模型的 SAM2 改进版，用于无提示的医学图像分割。我们的框架通过轻量级扩散先验，从现成的冻结 SAM2 图像特征中合成与 SAM2 兼容的类掩码嵌入。生成的嵌入被集成到 SAM2 的掩码解码器中以产生准确的分割，从而消除了对用户提示的需求。扩散先验进一步以先前分割的切片为条件，从而增强了跨体数据的空间一致性。在 BTCV 和 CHAOS 数据集的 CT 和 MRI 图像上，在无源无监督领域自适应 (SF-UDA) 和少样本设置下进行评估，DiffuSAM 以高效的训练和推理实现了具有竞争力的性能。代码可根据要求向通讯作者索取。

## Abstract
Segmentation models such as Segment Anything Model (SAM) and SAM2 achieve strong prompt-driven zero-shot performance. However, their training on natural images limits domain transfer to medical data. Consequently, accurate segmentation typically requires extensive fine-tuning and expert-designed prompts. We propose DiffuSAM, a diffusion-based adaptation of SAM2 for prompt-free medical image segmentation. Our framework synthesizes SAM2-compatible segmentation mask-like embeddings via a lightweight diffusion-prior from off-the-shelf frozen SAM2 image features. The generated embeddings are integrated into SAM2's mask decoder to produce accurate segmentations, thereby eliminating the need for user prompts. The diffusion prior is further conditioned on previously segmented slices, enforcing spatial consistency across volumes. Evaluated on the BTCV and CHAOS datasets for CT and MRI under Source-Free Unsupervised Domain Adaptation (SF-UDA) and Few-Shot settings, DiffuSAM achieves competitive performance with efficient training and inference. Code is available upon request from the corresponding author.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DiffuSAM** 的新框架，旨在将 SAM2（Segment Anything Model 2）适配于医疗影像分割任务，特别是在少样本（Few-Shot）和无源域自适应（SF-UDA）场景下实现“无提示”（Prompt-Free）的自动化分割。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：SAM 和 SAM2 虽然在自然图像上表现优异，但在医疗影像中面临两大挑战：
    1.  **领域偏移**：医疗影像的纹理、对比度和解剖结构与自然图像差异巨大。
    2.  **提示依赖**：SAM2 依赖人工提示（如点、框），这在临床工作流中效率低下且依赖专家经验。
*   **研究背景**：现有的医疗适配方法通常需要昂贵的微调或复杂的提示工程。本文旨在利用扩散模型（Diffusion Models）的生成能力，自动生成 SAM2 所需的嵌入表示，从而摆脱对人工提示的依赖。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：在冻结的 SAM2 骨干网络基础上，引入一个轻量级的**扩散先验（Diffusion Prior）**。该先验学习如何从 SAM2 的图像特征中生成对应的“记忆嵌入”（Memory Embeddings），这些嵌入随后被注入 SAM2 的解码器以生成分割掩码。
*   **关键技术细节**：
    *   **扩散先验训练**：使用 UNet2D 结构的扩散模型。在训练阶段，它学习在给定图像嵌入（$z_{img}$）和类别标签（$l$）的条件下，从噪声中恢复出真实的 SAM2 记忆嵌入（$z_{mem}$）。
    *   **损失函数**：结合了先验损失（$L_{prior}$，用于对齐嵌入空间）和分割损失（$L_{seg}$，用于确保生成的嵌入能产生准确的掩码）。
    *   **跨切片一致性（3D 约束）**：为了保证 3D 体数据的空间连续性，模型在生成当前切片的嵌入时，会参考相邻已分割切片的记忆嵌入，实现体素级别的解剖一致性。
    *   **无源域自适应（SF-UDA）**：利用 SAM2 特征的“特征亲和性”（Feature Affinity），即不同领域但同类别的特征在空间上接近，使得在源域（如 CT）训练的扩散先验能直接应用于目标域（如 MRI）。

### 3. 实验设计
*   **数据集**：
    *   **BTCV**：包含 30 个腹部 CT 体数据。
    *   **CHAOS**：包含 20 个 T2-SPIR MRI 体数据。
*   **任务场景**：
    *   **少样本（Few-Shot）**：在 BTCV 上使用 10% 的数据训练，90% 测试。
    *   **SF-UDA**：在 BTCV (CT) 上训练，直接在 CHAOS (MRI) 上测试，不接触目标域标签。
*   **对比方法**：
    *   监督学习：UNet, UNETR。
    *   基础模型：SAM, SAM2（带随机点提示）。
    *   适配模型：FATE-SAM, DPL, DFG。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX A5000 GPU**。
*   **显存占用**：训练和推理阶段均**小于 5 GB**，体现了极高的轻量化特性。
*   **训练参数**：迭代 7800 次，Batch Size 为 4，采用 Adam 优化器。
*   **推理效率**：推理时仅需 2 步扩散迭代即可生成记忆嵌入，速度极快。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流腹部器官分割数据集，针对四种主要器官（脾脏、左右肾、肝脏）进行了评估。
*   **消融实验**：专门对比了有无“3D 空间一致性（Volumetric Consistency）”的效果，证明了跨切片条件约束对 Dice 指标有显著提升（约 5%）。
*   **客观性**：对比了多种类型的基准模型（从传统 CNN 到最新的 Transformer 和基础模型适配版），实验设计较为公平，且在 SF-UDA 这种极具挑战性的任务下验证了泛化性。

### 6. 主要结论与发现
*   **性能领先**：在少样本设置下，DiffuSAM 的平均 Dice 达到 **87.24%**，优于需要提示的原始 SAM2 和其他适配模型。
*   **跨模态有效性**：在 CT 到 MRI 的无源迁移中，Dice 达到 **85.2%**，证明了扩散先验在捕捉高层解剖分布方面的强大能力。
*   **无提示可行性**：证明了通过生成模型合成内部嵌入，可以完全替代人工交互提示，且不损失精度。

### 7. 优点
*   **高效轻量**：无需微调庞大的 SAM2 骨干网络，仅训练一个轻量级扩散模块，显存需求极低。
*   **自动化程度高**：实现了真正的 Prompt-free，解决了 SAM2 进入临床路径的核心痛点。
*   **空间感知**：通过跨切片条件机制，巧妙地将 2D 的 SAM2 扩展到了具有 3D 一致性的医疗体数据处理中。

### 8. 不足与局限
*   **依赖类别标签**：虽然不需要坐标提示，但训练和推理仍需要类别标签（Label）作为扩散条件，尚未实现完全盲目的全类别自动分割。
*   **对比范围有限**：未与专门针对医疗影像进行大规模端到端微调的模型（如 MedSAM）在同等数据量下进行深度对比（作者在讨论中承认了这一点）。
*   **扩散步数权衡**：虽然推理仅需 2 步，但扩散模型的随机性是否会影响极小病灶分割的稳定性仍需进一步验证。

（完）
