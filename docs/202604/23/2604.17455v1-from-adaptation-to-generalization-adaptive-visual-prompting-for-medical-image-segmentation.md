---
title: "From Adaptation to Generalization: Adaptive Visual Prompting for Medical Image Segmentation"
title_zh: 从自适应到泛化：用于医学图像分割的自适应视觉提示
authors: "Evren Çetinkaya, Sangmin Lee, Jung Uk Kim, Hong Joo Lee, Nassir Navab"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17455v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于医学图像分割的自适应视觉提示
tldr: 针对医疗图像分割中跨领域变异性大的挑战，本文提出APEX框架。该框架通过可学习的提示词记忆库，利用傅里叶频谱提取的领域特征，为每个输入动态检索特定提示词。结合低频特征对比学习（LFC），APEX显著提升了预训练模型在已知和未知领域上的泛化能力，是一种即插即用的高效视觉提示方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17455v1/fig-001.webp\", \"caption\": \"Table 1. DICE (%) and IoU (%) comparison with recent VP methods across four backbone models on both seen and unseen domains for the polyp segmentation task. Domain A: CVC-ClinicDB, Domain B: Kvasir-Seg, Domain C: ETIS-LaribPolypDB, and Domain D: CVC-ColonDB. Source Only indicates the model before adaptation; even seen domains are treated as unseen in this case.\", \"page\": 6, \"index\": 1, \"width\": 798, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2604.17455v1/fig-002.webp\", \"caption\": \"Table 2. DICE (%) and IoU (%) comparison with recent VP methods across four backbone models on both seen and unseen domains for the OC/OD segmentation task. Domain A: REFUGE Test, Domain B: REFUGE Val, Domain C: Drishti-GS, and Domain D: RIM-ONE-r3. Source Only indicates the model before adaptation; even seen domains are treated as unseen in this case.\", \"page\": 6, \"index\": 2, \"width\": 798, \"height\": 565}, {\"url\": \"assets/figures/arxiv/2604.17455v1/fig-003.webp\", \"caption\": \"Figure 1. Graphical comparison with (a) conventional prompting approaches and (b) proposed adaptive prompting.\", \"page\": 2, \"index\": 3, \"width\": 481, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.17455v1/fig-004.webp\", \"caption\": \"Figure 4. Qualitative comparison between before and after prompting. *In case of polyp images, we visualized normalized images since we prompt into the normalized images with ImageNet statistics.\", \"page\": 7, \"index\": 4, \"width\": 413, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.17455v1/fig-005.webp\", \"caption\": \"Figure 2. Overview of the proposed adaptive prompting framework. During optimization of APEX, the parameters of the segmentation model are frozen and only APEX is optimized.\", \"page\": 3, \"index\": 5, \"width\": 772, \"height\": 459}, {\"url\": \"assets/figures/arxiv/2604.17455v1/fig-006.webp\", \"caption\": \"Figure 6. Visualization of the top 10% activated memory slots for unseen domain samples on the OC/OD segmentation dataset.\", \"page\": 8, \"index\": 6, \"width\": 319, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2604.17455v1/fig-007.webp\", \"caption\": \"Figure 3. Visual descriptions about the LFC learning framework.\", \"page\": 4, \"index\": 7, \"width\": 431, \"height\": 373}]"
motivation: 传统的单一视觉提示难以应对医疗影像中复杂的域内和域间差异，限制了模型在不同场景下的泛化性能。
method: 提出APEX框架，利用傅里叶频谱提取领域特征，并通过低频特征对比学习（LFC）从记忆库中检索输入特定的自适应提示词。
result: 在两个医疗分割任务上的实验证明，APEX显著提升了模型在可见和不可见领域上的表现，且能与多种骨干网络兼容。
conclusion: APEX作为一种即插即用的视觉提示方法，有效解决了医疗图像分割中的领域泛化问题，增强了预训练模型的适应能力。
---

## 摘要
视觉提示已成为一种强大的方法，可以在不更新模型参数的情况下将预训练模型适配到新领域。然而，现有的提示方法通常为每个领域优化单个提示，并将其统一应用于所有输入，这限制了它们在域内和域间变异性下的泛化能力，而这在医学领域尤为关键。为了解决这一问题，我们提出了 APEX，一个自适应提示提取（Adaptive Prompt EXtraction）框架，它从可学习的提示存储器中检索特定于输入的提示。该存储器存储了多样化且具有领域辨别力的提示表示，并通过从傅里叶频谱中提取的领域特征进行查询。为了学习鲁棒且具有辨别力的领域特征，我们引入了一种新颖的低频特征对比（LFC）学习框架，该框架将来自同一领域的表示聚类，同时分离来自不同领域的表示。在两个医学分割任务上的广泛实验表明，APEX 显著提高了在已知和未知领域上的泛化能力。此外，它与任何现有的骨干网络互补，并持续提升性能，证实了其作为医学领域即插即用提示解决方案的有效性。代码可在 https://github.com/cetinkayaevren/apex/ 获取。

## Abstract
Visual prompting has emerged as a powerful method for adapting pre-trained models to new domains without updating model parameters. However, existing prompting methods typically optimize a single prompt per domain and apply it uniformly to all inputs, limiting their ability to generalize under intra and inter-domain variability, which is especially critical in the medical field. To address this, we propose APEX, an Adaptive Prompt EXtraction framework that retrieves input-specific prompts from a learnable prompt memory. The memory stores diverse, domain-discriminative prompt representations and is queried via domain features extracted from the Fourier spectrum. To learn robust and discriminative domain features, we introduce a novel Low-Frequency Feature Contrastive (LFC) learning framework that clusters representations from the same domain while separating those from different domains. Extensive experiments on two medical segmentation tasks demonstrate that APEX significantly improves generalization across both seen and unseen domains. Furthermore, it complements any existing backbones and consistently enhances performance, confirming its effectiveness as a plug-and-play prompting solution in medical fields. The code is available at https://github.com/cetinkayaevren/apex/

---

## 论文详细总结（自动生成）

这是一份关于论文《From Adaptation to Generalization: Adaptive Visual Prompting for Medical Image Segmentation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：医学图像分割中普遍存在的**领域偏移（Domain Shift）**问题。由于不同医疗机构的成像设备、采集设置不同，模型在源域表现良好但在目标域往往性能下降。
*   **研究背景**：
    *   传统的微调（Fine-tuning）容易导致“灾难性遗忘”，且涉及隐私数据访问限制。
    *   现有的**视觉提示（Visual Prompting, VP）**方法通常为每个领域优化一个“一刀切”的静态提示词，忽略了**域内变异性**（同一设备下不同患者/病理特征的差异）和**域间变异性**（不同设备间的差异），导致其在未知领域（Unseen Domains）的泛化能力有限。

### 2. 方法论：APEX 框架
APEX（Adaptive Prompt EXtraction）的核心思想是**根据输入图像动态生成特定的提示词**，而非使用固定提示。
*   **核心组件**：
    1.  **领域特征编码器 ($E_D$)**：利用傅里叶变换（FFT）提取图像的**低频振幅分量**。研究认为医学图像的领域差异主要集中在低频（对比度、亮度等），而高频保留了解剖结构。
    2.  **提示词记忆库 (Prompt Memory, $B$)**：一个可学习的矩阵，存储了 $J$ 个正交初始化的提示向量，代表多样化的领域特征模式。
    3.  **自适应提取机制**：通过计算输入特征与记忆库向量的余弦相似度，生成“寻址向量”，以加权求和的方式从记忆库中提取最匹配的提示特征，再经解码器生成最终的视觉提示。
*   **关键技术：低频特征对比学习 (LFC)**：
    *   引入对比损失函数，强制拉近同一领域图像的低频特征，推开不同领域的特征。这增强了编码器对领域差异的辨别力，确保从记忆库中检索到更精准的提示。

### 3. 实验设计
*   **数据集**：
    *   **息肉分割（Polyp Segmentation）**：使用 BKAI 作为源域；CVC-ClinicDB 和 Kvasir-Seg 作为可见域（Seen）；ETIS-LaribPolypDB 和 CVC-ColonDB 作为不可见域（Unseen）。
    *   **视盘/视杯分割（OC/OD Segmentation）**：使用 ORIGA 作为源域；REFUGE 的不同子集作为可见域；Drishti-GS 和 RIM-ONE-r3 作为不可见域。
*   **基准模型（Backbones）**：涵盖了主流的 CNN 和 Transformer 架构，包括 UNet, ResUNet, SwinUNet, TransUNet 和 PraNet。
*   **对比方法**：对比了多种前沿的视觉提示方法，如 VPT (ECCV'22), FVP (TMI'23), A2XP (CVPR'24), VPAD (TPAMI'25) 以及测试时自适应方法 VPTTA (CVPR'24)。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **效率分析**：文中提到 APEX 模块非常轻量，仅包含 0.036M 参数。在推理时，相比于需要逐样本优化的测试时自适应（TTA）方法（耗时约 3000ms），APEX 仅需增加 20ms 的额外开销，实现了超过 150 倍的加速。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 2 个核心任务、8 个不同来源的数据集上进行了验证。
    *   针对 5 种不同的骨干网络进行了交叉验证，证明了其“即插即用”的通用性。
    *   进行了详尽的**消融实验**，验证了提示词记忆库、LFC 损失函数以及记忆库槽位数量（Slot Number）对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。通过区分“可见领域”和“不可见领域”，严谨地评估了模型的泛化能力，而非仅仅是过拟合已知分布。

### 6. 主要结论与发现
*   **泛化性能卓越**：APEX 在所有任务和骨干网络上均优于现有的视觉提示方法。特别是在未知领域，APEX 的提升幅度显著（例如在 OC/OD 任务的 Domain D 上，Dice 分数比基准提升了 41.44%）。
*   **解决域内差异**：可视化结果显示，APEX 能根据不同输入激活记忆库中不同的槽位，证明了其处理个体差异的能力。
*   **高效性**：APEX 证明了无需在测试阶段进行耗时的梯度优化，即可达到甚至超过测试时自适应（TTA）方法的性能。

### 7. 优点与亮点
*   **动态性**：打破了传统 VP 方法“一域一提示”的局限，实现了“一图一提示”。
*   **频域视角**：巧妙利用傅里叶低频分量捕捉领域信息，既实现了适配又保护了解剖结构不被破坏。
*   **即插即用**：不改变预训练模型参数，兼容性极强，适合医疗场景下的隐私保护需求。

### 8. 不足与局限
*   **解剖结构偏移限制**：目前的方法主要针对成像设备引起的“风格”偏移。如果遇到解剖结构发生巨大变化的场景（如不同器官间的迁移），仅靠低频振幅提示可能不足，未来可能需要探索高频或相位信息的自适应。
*   **超参数敏感性**：记忆库槽位的数量（J）需要根据任务复杂度预先设定，虽然实验给出了推荐值（150），但在极端多样化的数据集上可能需要进一步调整。
*   **算力细节缺失**：未提供具体的训练环境参数，不便于其他研究者评估复现时的硬件门槛。

（完）
