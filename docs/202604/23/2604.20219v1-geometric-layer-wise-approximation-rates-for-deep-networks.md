---
title: Geometric Layer-wise Approximation Rates for Deep Networks
title_zh: 深度网络的几何级逐层逼近率
authors: "Shijun Zhang, Zuowei Shen, Yuesheng Xu"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20219v1"
tags: ["query:dl"]
score: 9.0
evidence: 神经网络深度和逐层逼近的理论框架
tldr: 本研究针对深度神经网络中中间层作用不透明的问题，提出了一种定量分析框架。通过设计一种固定宽度且具有混合激活函数的嵌套架构，使得每一层中间输出都能作为目标函数的近似。研究证明，随着深度增加，逼近误差以几何速率衰减，具体取决于层数对应的空间尺度。这一成果为“深度”提供了精确的尺度解释，揭示了深度如何通过逐层修正残差来实现多级精细化逼近，为理解深层架构的优势提供了理论支撑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20219v1/fig-001.webp\", \"caption\": \"Figure 4: Interior cubes Qℓ,β and transition regions Ωℓ for N = 2 at the levels ℓ = 0, 1, 2.\", \"page\": 14, \"index\": 1, \"width\": 758, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2604.20219v1/fig-002.webp\", \"caption\": \"Figure 7: Conceptual overview of the multigrade construction, showing how the encoder and decoder are combined (here illustrated for ℓ = 2 and N = 2).\", \"page\": 18, \"index\": 2, \"width\": 692, \"height\": 225}]"
motivation: 传统逼近理论仅关注网络最终输出，无法解释中间层在逐步逼近目标函数过程中的具体作用。
method: 提出一种基于多级学习思想的固定宽度混合激活架构，使每一层都能针对更细微尺度的残差进行修正。
result: "理论证明了中间层的逼近误差随深度呈几何级数下降，对于 Lipschitz 函数其误差缩放比例为 $N^{-\\ell}$。"
conclusion: 该研究确立了深度与逼近精度之间的几何对应关系，证明了深度是实现函数自适应精细化逼近的关键机制。
---

## 摘要
深度被广泛认为是深度神经网络成功的核心因素，而标准的神经网络逼近理论通常仅为最终输出提供保证，对中间层的作用在很大程度上尚不明确。我们通过开发一个定量框架来填补这一空白，在该框架中，深度具有精确的尺度依赖性解释。具体而言，我们设计了一个具有固定宽度 $2dN+d+2$ 和任意指定有限深度的单一共享混合激活架构，使得每个中间读取值 $Φ_\ell$ 本身就是目标函数 $f$ 的一个逼近器。对于 $p\in [1,\infty)$ 的 $f\in L^p([0,1]^d)$，对于所有 $\ell$，$Φ_\ell$ 的逼近误差受几何尺度 $N^{-\ell}$ 下 $L^p$ 连续模的 $(2d+1)$ 倍控制。如果 $f$ 是 $1$-Lipschitz 的，则该估计简化为几何速率 $(2d+1)N^{-\ell}$。我们的网络设计灵感源自多级深度学习，其中深度充当渐进式细化机制：每个新的修正都针对更细尺度上的残差信息，而早期的修正项仍作为后续读取值的一部分，从而产生一种嵌套架构，支持自适应细化而无需重新设计之前的网络。

## Abstract
Depth is widely viewed as a central contributor to the success of deep neural networks, whereas standard neural network approximation theory typically provides guarantees only for the final output and leaves the role of intermediate layers largely unclear. We address this gap by developing a quantitative framework in which depth admits a precise scale-dependent interpretation. Specifically, we design a single shared mixed-activation architecture of fixed width $2dN+d+2$ and any prescribed finite depth such that each intermediate readout $Φ_\ell$ is itself an approximant to the target function $f$. For $f\in L^p([0,1]^d)$ with $p\in [1,\infty)$, the approximation error of $Φ_\ell$ is controlled by $(2d+1)$ times the $L^p$ modulus of continuity at the geometric scale $N^{-\ell}$ for all $\ell$. The estimate reduces to the geometric rate $(2d+1)N^{-\ell}$ if $f$ is $1$-Lipschitz. Our network design is inspired by multigrade deep learning, where depth serves as a progressive refinement mechanism: each new correction targets residual information at a finer scale while the earlier correction terms remain part of the later readouts, yielding a nested architecture that supports adaptive refinement without redesigning the preceding network.

---

## 论文详细总结（自动生成）

这是一份关于论文《Geometric Layer-wise Approximation Rates for Deep Networks》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管深度神经网络（DNN）在实践中取得了巨大成功，但现有的逼近理论大多将网络视为一个“黑盒”，仅关注最终输出层的逼近精度。关于**中间层（Intermediate Layers）**具体代表了什么、深度在每一层如何贡献于逼近过程，缺乏定量的理论解释。
*   **核心问题**：能否设计一种架构，使得网络的每一个中间层输出都能对目标函数提供明确的、随深度增加而逐步精细化的逼近保证？
*   **整体含义**：本研究为“深度”提供了一个精确的**尺度依赖（Scale-dependent）**解释。它证明了深度不仅是模型复杂度的度量，更是逼近分辨率的度量。

### 2. 论文提出的方法论
*   **核心思想**：借鉴**多级深度学习（Multigrade Deep Learning, MGDL）**的思路，将逼近过程分解为一系列渐进的修正阶段。每一层都在前一层残差的基础上，在更细微的几何尺度上进行修正。
*   **关键技术细节**：
    *   **混合激活架构（Mixed-activation）**：设计了一个固定宽度为 $2dN + d + 2$ 的网络。其中 ReLU 通道负责**几何定位（Encoder）**，将输入空间划分为细小的立方体单元；Sine 通道负责**数值实现（Decoder）**，在每个单元上拟合目标函数的平均值。
    *   **嵌套读取机制**：定义层 $\ell$ 的读取值为 $\Phi_\ell = \Phi_{\ell-1} + \Gamma_\ell$，其中 $\Gamma_\ell$ 是当前层的修正项。这种结构允许在不改变前层参数的情况下，通过增加深度来提升精度。
*   **核心公式**：
    对于 $f \in L^p([0, 1]^d)$，第 $\ell$ 层的逼近误差满足：
    $$\|f - \Phi_\ell\|_{L^p} \leq (2d + 1) \omega_{f,p}(N^{-\ell})$$
    其中 $\omega_{f,p}$ 是 $L^p$ 连续模，$N$ 是基数尺度。对于 Lipschitz 函数，误差以几何速率 $N^{-\ell}$ 衰减。

### 3. 实验设计
*   **研究性质**：本论文是一篇**纯理论证明（Constructive Proof）**论文。
*   **场景与 Benchmark**：作者通过数学构造法，在 $L^p$ 空间（$p \in [1, \infty)$）和具有不同正则性（Hölders, Lipschitz）的函数类上进行了理论推导。
*   **对比方法**：论文在理论层面对比了传统的“单次整体逼近”方法与本文提出的“逐层嵌套细化”方法，强调了后者在自适应深度和多尺度解析上的优势。

### 4. 资源与算力
*   **算力说明**：由于本文属于数学理论证明，**不涉及任何数值实验或模型训练**，因此文中未提及 GPU 型号、数量或训练时长等算力资源。

### 5. 实验数量与充分性
*   **充分性评价**：作为理论论文，其“实验”体现在数学证明的严密性上。作者提供了详尽的引理证明（如几何划分、编码器-解码器实现、振荡估计等），涵盖了从单位立方体到一般矩形区域的推广。
*   **客观性**：证明过程逻辑严密，结论具有普适性，不依赖于特定的随机初始化或优化算法。

### 6. 论文的主要结论与发现
*   **深度的几何意义**：深度被量化为逼近分辨率。增加一层网络相当于将逼近尺度从 $N^{-(\ell-1)}$ 精细化到 $N^{-\ell}$。
*   **固定宽度的有效性**：证明了即使宽度固定（仅随维度 $d$ 线性增长），仅靠增加深度也能实现任意精度的逼近，且误差随深度呈几何级数下降。
*   **自适应细化**：提出的架构支持“即停即用”，即如果当前深度的精度已达标，则无需继续增加层数；若需更高精度，只需追加新层而无需重新训练旧层。

### 7. 优点（亮点）
*   **理论创新**：首次为 DNN 提供了逐层（Layer-wise）的定量逼近率保证，填补了中间层功能解释的空白。
*   **架构简洁**：混合激活函数（Sine + ReLU）的设计非常巧妙，利用 ReLU 处理空间逻辑，利用 Sine 处理数值拟合，实现了极高的参数效率。
*   **构造性强**：给出了明确的参数构造方案，为开发新型多级优化算法提供了理论基础。

### 8. 不足与局限
*   **缺乏实证验证**：论文完全基于理论构造，未在真实数据集（如图像识别或自然语言处理）上验证这种逐层修正机制的实际表现。
*   **优化与泛化缺失**：研究仅讨论了**逼近误差（Representation Error）**，未涉及实际训练中的**优化误差**和**泛化误差**。在实际 SGD 训练中，能否达到这种理论构造的参数状态尚存疑问。
*   **数值稳定性风险**：利用 Sine 函数进行高精度拟合（如 Proposition 3.2 提到的点拟合）可能需要极高的参数精度，这在有限精度的计算机硬件上可能会遇到数值稳定性问题。

（完）
