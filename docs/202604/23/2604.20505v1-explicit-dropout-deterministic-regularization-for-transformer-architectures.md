---
title: "Explicit Dropout: Deterministic Regularization for Transformer Architectures"
title_zh: 显式 Dropout：Transformer 架构的确定性正则化
authors: "Vidhi Agrawal, Illia Oleksiienko, Alexandros Iosifidis"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20505v1"
tags: ["query:dl"]
score: 9.0
evidence: Transformer架构的确定性正则化，取代随机Dropout
tldr: 针对传统Dropout依赖随机掩码且缺乏显式优化目标的局限，本文提出了一种显式Dropout框架。该方法将Dropout重新表述为直接加入训练损失函数的确定性正则化项，实现了对Transformer中注意力机制和前馈网络组件的精细化控制。实验证明，该方法在图像、动作检测和音频分类等任务中表现优于传统随机Dropout，提供了更具解释性和稳定性的正则化方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.20505v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 317}]"
motivation: 传统Dropout依赖随机扰动，缺乏明确的优化目标且难以对模型组件进行精细的正则化控制。
method: 将Dropout转化为确定性的加性正则化项并集成到损失函数中，针对Transformer的Q、K、V及FFN组件设计了独立的正则化系数。
result: 在图像分类、时序动作检测和音频分类等多种任务上，显式Dropout的效果达到或超过了传统的随机Dropout。
conclusion: 显式Dropout为Transformer架构提供了一种可解释、可控且无需随机性的高效正则化替代方案。
---

## 摘要
Dropout 是深度学习中广泛使用的正则化技术，但其效果通常是通过随机掩码而非显式优化目标实现的。我们提出了一种确定性公式，将 Dropout 表示为直接纳入训练损失的加性正则化项。该框架推导了针对 Transformer 架构的显式正则化项，涵盖了注意力机制中的查询（query）、键（key）、值（value）以及前馈网络组件，且各组件的正则化强度可独立控制。这种表述消除了对随机扰动的依赖，同时提供了对正则化强度更清晰、更细粒度的控制。在图像分类、时序动作检测和音频分类方面的实验表明，显式 Dropout 的表现与传统的隐式方法相当或更优，且在应用于注意力层和前馈网络层时具有一致的增益。消融研究证明了其性能的稳定性，并展示了通过正则化系数和 Dropout 率实现的可控正则化。总的来说，显式 Dropout 为随机正则化提供了一种实用且可解释的替代方案，同时在不同任务中保持了架构的灵活性。

## Abstract
Dropout is a widely used regularization technique in deep learning, but its effects are typically realized through stochastic masking rather than explicit optimization objectives. We propose a deterministic formulation that expresses dropout as an additive regularizer directly incorporated into the training loss. The framework derives explicit regularization terms for Transformer architectures, covering attention query, key, value, and feed-forward components with independently controllable strengths. This formulation removes reliance on stochastic perturbations while providing clearer and fine-grained control over regularization strength. Experiments across image classification, temporal action detection, and audio classification show that explicit dropout matches or outperforms conventional implicit methods, with consistent gains when applied to attention and feed-forward network layers. Ablation studies demonstrate stable performance and controllable regularization through regularization coefficients and dropout rates. Overall, explicit dropout offers a practical and interpretable alternative to stochastic regularization while maintaining architectural flexibility across diverse tasks.

---

## 论文详细总结（自动生成）

这篇论文《Explicit Dropout: Deterministic Regularization for Transformer Architectures》提出了一种将 Transformer 架构中的随机 Dropout 转化为确定性显式正则化项的方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的 Dropout 依赖于训练过程中的随机掩码（Stochastic Masking），这虽然能有效防止过拟合，但也带来了训练不稳定性、难以解释以及无法对模型特定组件进行精细化控制的问题。
*   **核心目标**：作者试图回答是否可以将 Dropout 重新表述为一个确定性的、加性的正则化项（类似于 $L_2$ 正则化），直接整合进损失函数中，从而消除随机性并提供更清晰的优化目标。

### 2. 方法论
*   **核心思想**：利用数学推导，将带有随机 Dropout 的期望损失函数分解为“标准任务损失”和“显式正则化项”。通过计算 Bernoulli 掩码在期望意义下的贡献，将随机扰动转化为与输入特征协方差和权重矩阵相关的确定性函数。
*   **关键技术细节**：
    *   **组件化推导**：针对 Transformer 的核心组件——查询（Query, Q）、键（Key, K）、值（Value, V）以及前馈网络（FFN）分别推导了显式正则化公式。
    *   **公式特征**：推导出的正则化项通常表现为迹函数（Trace）的形式，例如 $J_q = \frac{1}{2} \text{Tr} [((X^T X) \odot p^2) \Lambda_q]$，其中 $X$ 是输入，$p$ 是 Dropout 率，$\Lambda$ 是与权重矩阵相关的项。
    *   **细粒度控制**：引入了独立的正则化系数 $\lambda$，允许开发者针对 Q、K、V 或 FFN 设置不同的正则化强度，而不再受限于统一的随机概率。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：CIFAR-10 和 CIFAR-100（使用 7 层 ViT 架构）。
    *   **时序动作检测**：THUMOS14（基于预提取的 TSN 特征）。
    *   **音频分类**：GTZAN 音乐流派数据集（基于 VGGish 特征）。
*   **对比基准（Benchmark）**：
    *   **隐式方法**：标准 Dropout、DropAttention、DropKey。
    *   **显式方法**：Arora 等人先前提出的显式 Dropout 变体。
    *   **消融实验**：针对不同组件（Q, K, V, AV）、不同学习率和不同正则化系数 $\lambda$ 的组合。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练总时长。但从模型规模（7 层 ViT 和 2 层 Transformer Encoder）来看，属于中等规模的计算需求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了视觉、视频、音频三种模态，实验设计较为全面。
*   **充分性与公平性**：
    *   每组实验均进行了 **5 次独立运行**并报告均值和标准差，增强了结果的可信度。
    *   进行了详尽的消融研究，探讨了 $\lambda$ 系数与学习率对性能的影响。
    *   对比了多种最先进的注意力正则化技术，实验对比具有客观性。

### 6. 主要结论与发现
*   **性能优越性**：显式 Dropout 在多个任务上达到或超过了传统随机 Dropout 的表现。
*   **组件敏感性**：实验发现，对 **Value (V) 向量**和 **FFN 层**应用显式正则化最为有效且稳定；而对 Query 和 Key 的正则化则较为敏感，过大的系数可能导致性能下降。
*   **稳定性**：显式方法消除了随机采样带来的方差，使训练过程更加确定和可控。

### 7. 优点
*   **数学严谨性**：提供了完备的数学推导，将 Dropout 从一种“启发式技巧”提升为“原则性优化目标”。
*   **高可控性**：通过调节 $\lambda$ 系数，研究者可以精确平衡模型的泛化能力与拟合能力。
*   **通用性**：虽然针对 Transformer 推导，但该方法可以自然延伸至卷积神经网络（CNN）等其他架构。

### 8. 不足与局限
*   **联合推导难题**：论文提到，当多个组件同时应用 Dropout 时，推导一个统一的、非独立的联合正则化表达式在数学上仍具有挑战性。
*   **超参数调优**：引入了额外的正则化系数 $\lambda$，可能需要更多的超参数搜索工作。
*   **大规模验证缺失**：实验主要集中在中小规模数据集（如 CIFAR），在超大规模预训练模型（如 GPT-4 或大型 ViT）上的表现尚待进一步验证。

（完）
