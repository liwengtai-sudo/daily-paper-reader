---
title: "RF-HiT: Rectified Flow Hierarchical Transformer for General Medical Image Segmentation"
title_zh: RF-HiT：用于通用医学图像分割的修正流层级 Transformer
authors: "Ahmed Marouane Djouama, Abir Belaala, Abdellah Zakaria Sellam, Salah Eddine Bekhouche, Cosimo Distante, Abdenour Hadid"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19570v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于医学图像分割的分层Transformer
tldr: 针对医学图像分割中Transformer和扩散模型计算复杂度高、推理延迟大的问题，本文提出RF-HiT。该模型结合了整流流（Rectified Flow）技术与沙漏型Transformer架构，通过多尺度分层编码器进行解剖引导。RF-HiT实现了线性计算复杂度，仅需3步推理即可完成，在保持轻量化（13.6M参数）的同时，在ACDC和BraTS数据集上达到了领先的分割精度，为实时临床应用提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19570v1/fig-001.webp\", \"caption\": \"Fig. 2. Visualization of RF-HiT segmentation results on the ACDC dataset. Error maps highlight false positives (FP), false negatives (FN), and misclassifications (Mc).\", \"page\": 5, \"index\": 1, \"width\": 529, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2604.19570v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed RF-HiT: a rectified-flow segmentation framework composed of main flow model that follows an encoder-decoder structure, where neighborhood attention is applied at high-resolution levels, and global attention at the bottleneck. a Hierarchical Feature Encoder provides multi-scale features that are fused into corresponding main model levels via learnable linear interpolation (lerp) to guide velocity prediction.\", \"page\": 3, \"index\": 2, \"width\": 1076, \"height\": 504}]"
motivation: 现有Transformer和扩散模型在医学图像分割中面临二次计算复杂度和高推理延迟的瓶颈。
method: 提出一种结合整流流与沙漏型Transformer的架构，利用多尺度分层编码器和可学习插值实现高效的特征融合与线性复杂度推理。
result: "在ACDC和BraTS 2021数据集上分别达到91.27%和87.40%的Dice值，且仅需10.14 GFLOPs和3步推理。"
conclusion: RF-HiT在性能与效率之间取得了极佳平衡，是具有实时临床应用潜力的鲁棒性医学图像分割基础模型。
---

## 摘要
精确的医学图像分割需要长程上下文推理和精确的边界描绘，而现有的基于 Transformer 和扩散模型的范式通常受限于二次计算复杂度和极高的推理延迟。我们提出了 RF-HiT，一种修正流层级 Transformer，它集成了一个沙漏型 Transformer 骨干网络和一个多尺度层级编码器，用于解剖学引导的特征调节。与以往基于扩散的方法不同，RF-HiT 利用修正流和高效 Transformer 模块实现了线性复杂度，且仅需极少数离散化步骤。该模型通过可学习插值进一步融合了跨分辨率的调节特征，从而以极小的计算开销实现了有效的多尺度表示。因此，RF-HiT 实现了极佳的效率与性能权衡，仅需 10.14 GFLOPs 的计算量和 13.6M 的参数量，且推理最快仅需三步。尽管设计紧凑，RF-HiT 在 ACDC 数据集上达到了 91.27% 的平均 Dice 系数，在 BraTS 2021 上达到了 87.40%，其性能与计算密集型架构相当甚至更优。这证明了其作为一种鲁棒且计算高效的基础模型，在实时临床分割中具有巨大的潜力。

## Abstract
Accurate medical image segmentation requires both long-range contextual reasoning and precise boundary delineation, a task where existing transformer- and diffusion-based paradigms are frequently bottlenecked by quadratic computational complexity and prohibitive inference latency. We propose RF-HiT, a Rectified Flow Hierarchical Transformer that integrates an hourglass transformer backbone with a multi-scale hierarchical encoder for anatomically guided feature conditioning. Unlike prior diffusion-based approaches, RF-HiT leverages rectified flow with efficient transformer blocks to achieve linear complexity while requiring only a few discretization steps. The model further fuses conditioning features across resolutions via learnable interpolation, enabling effective multi-scale representation with minimal computational overhead. As a result, RF-HiT achieves a strong efficiency-performance trade-off, requiring only 10.14 GFLOPs, 13.6M parameters, and inference in as few as three steps. Despite its compact design, RF-HiT attains 91.27% mean Dice on ACDC and 87.40% on BraTS 2021, achieving performance comparable to or exceeding that of significantly more intensive architectures. This demonstrates its strong potential as a robust, computationally efficient foundation for real-time clinical segmentation.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《RF-HiT: Rectified Flow Hierarchical Transformer for General Medical Image Segmentation》** 的结构化深入总结：

### 1. 核心问题与研究背景
*   **核心问题**：医学图像分割要求同时具备长程上下文建模能力和精确的边界定位能力。
*   **研究背景**：
    *   **Transformer 的瓶颈**：标准 Transformer 的全局自注意力机制具有二次计算复杂度，处理高分辨率医学图像时开销巨大。
    *   **扩散模型的局限**：虽然生成式扩散模型能提供不确定性估计，但其推理过程通常需要数百步迭代，导致临床部署延迟过高。
*   **整体含义**：本文旨在开发一种既能保持生成模型优势（如处理复杂分布），又能实现线性计算复杂度和极速推理（仅需 3 步）的高效医学图像分割框架。

### 2. 方法论
RF-HiT 的核心思想是将**修正流（Rectified Flow）**与**沙漏型层级 Transformer（HDiT）**相结合。
*   **修正流 (Rectified Flow)**：不同于传统扩散模型学习复杂的随机微分方程，RF 学习的是将噪声分布转化为数据分布的直线轨迹（ODE）。其目标函数是最小化预测速度与真实直线位移之间的均方误差，这使得模型可以用极少的离散步数（如 1-3 步）完成高质量采样。
*   **沙漏型 Transformer 骨干 (HDiT)**：
    *   **线性复杂度**：在高分辨率层级使用**邻域注意力 (Neighborhood Attention)** 提取局部细节，仅在低分辨率瓶颈层使用全局自注意力，从而将复杂度降至 $O(n)$。
    *   **层级结构**：采用类似 U-Net 的编码器-解码器对称结构。
*   **层级特征编码器 (HFE)**：采用双路径设计。除了主流动模型外，增加一个独立的 HFE 路径提取原始图像的多尺度解剖特征。
*   **可学习线性插值融合 (lerp)**：通过可学习的标量参数 $\alpha$，将 HFE 提取的解剖特征与主模型的特征在对应分辨率下进行融合，增强边界定位的准确性。

### 3. 实验设计
*   **数据集**：
    *   **ACDC**：心脏 MRI 影像，涉及左心室、右心室和心肌三个类别的分割。
    *   **BraTS 2021**：脑胶质瘤 MRI 影像，涉及增强肿瘤 (ET)、肿瘤核心 (TC) 和全肿瘤 (WT) 的分割。
*   **Benchmark 与对比方法**：
    *   对比了确定性模型（如 TransUNet, SwinUnet, UNETR, nnFormer, MISSFormer）。
    *   对比了生成式/扩散模型（如 MedSegDiff-V2, Diff-UNet, DiffBTS）。
*   **评价指标**：Dice 系数 (DSC) 和 95% 豪斯多夫距离 (HD95)。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA TITAN V GPU**。
*   **软件环境**：Python 3.10, PyTorch 2.9.0。
*   **训练细节**：
    *   ACDC 数据集训练 1000 epochs，BraTS 2021 训练 300 epochs。
    *   Batch size 为 32，使用 AdamW 优化器，学习率 $1 \times 10^{-4}$。
*   **模型规模**：参数量仅 **13.6M**，计算量为 **10.14 GFLOPs**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个具有代表性的医学影像数据集（心脏和脑部）上进行了验证。
    *   **消融实验**：专门验证了层级特征编码器 (HFE) 的有效性（提升了约 0.6% 的 Dice 值），以及“可学习插值”对比“直接相加”融合方式的优越性。
*   **充分性评价**：实验设计较为客观，涵盖了参数量、计算量、推理步数与分割精度的多维度对比。通过与 MedSegDiff-V2（983 GFLOPs）等重型模型的对比，突出了其效率优势。

### 6. 主要结论与发现
*   **性能卓越**：在 ACDC 上达到 91.27% Mean Dice，在 BraTS 2021 上达到 87.40% Mean Dice，性能逼近或超过了参数量大得多的模型（如 nnFormer）。
*   **极高效率**：RF-HiT 仅需 3 步推理即可获得稳定结果，而传统的扩散模型通常需要数十甚至上百步。
*   **轻量化**：以不到 14M 的参数量实现了与重型 Transformer 架构相当的上下文建模能力。

### 7. 优点（亮点）
*   **推理速度突破**：利用修正流技术解决了生成式模型在医学影像领域推理慢的痛点。
*   **架构优化**：巧妙结合了邻域注意力和全局注意力，在保持 $O(n)$ 复杂度的同时不丢失全局视野。
*   **解剖引导**：通过 HFE 路径显式注入多尺度解剖先验，有效提升了复杂边界（如心肌边缘）的分割精度。

### 8. 不足与局限
*   **维度限制**：目前模型主要基于 **2D 架构** 处理 3D 卷轴数据（逐切片处理后堆叠），虽然效率高，但可能损失了部分层间（Inter-slice）的 3D 空间连续性信息。
*   **边缘挑战**：可视化结果显示，在组织对比度极低或结构极其复杂的边缘区域，模型仍存在轻微的过分割或类别混淆现象。
*   **应用范围**：论文主要验证了 MRI 模态，对于 CT、超声等其他模态的泛化能力尚待进一步大规模验证。

（完）
