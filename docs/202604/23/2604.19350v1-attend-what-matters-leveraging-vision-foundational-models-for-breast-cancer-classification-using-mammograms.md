---
title: "Attend what matters: Leveraging vision foundational models for breast cancer classification using mammograms"
title_zh: 关注重点：利用视觉基础模型进行乳房 X 线摄影乳腺癌分类
authors: "Samyak Sanghvi, Piyush Miglani, Sarvesh Shashikumar, Kaustubh R Borgavi, Veenu Singla, Chetan Arora"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19350v1"
tags: ["query:ai-pm"]
score: 10.0
evidence: 使用乳腺钼靶影像进行乳腺癌分类的视觉Transformer
tldr: 针对乳腺癌筛查中视觉Transformer（ViT）因高分辨率图像和细微病变导致注意力分散及分类困难的问题，本文提出一种新框架。通过目标检测引导的RoI令牌缩减、基于对比学习的细粒度特征增强，并结合DINOv2预训练模型，显著提升了乳腺癌分类性能，为大规模临床筛查提供了有效工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19350v1/fig-001.webp\", \"caption\": \"Table 1. Performance comparison on the VinDR dataset. ViT-A and ViT-B correspond to DINOv2 [1] with a linear layer at input resolutions of 448×448 and 1024×1024 respectively. ViT-C demonstrates scores using a DeIT head instead of a linear layer at 448×448 input resolution.\", \"page\": 3, \"index\": 1, \"width\": 508, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2604.19350v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview. In our proposed method, we first use G-DINO in a zero-shot setting to localize semantically important Regions of Interest (RoIs) based on a clinically meaningful text prompt. The top-k RoIs, selected by bounding box confidence scores, are cropped and passed through a pretrained DINOv2 encoder to extract k feature vectors. These features are processed by a transformer module, enabling interaction among regions through self-attention. The global RoI (covering the full breast) is used as the final representation which is contextually enriched by fine-grained lesion-level RoIs, and is fed into a Multi-Layer Perceptron (MLP) for classification.\", \"page\": 3, \"index\": 2, \"width\": 1016, \"height\": 391}]"
motivation: 传统ViT在处理高分辨率乳腺钼靶图像时，难以在海量令牌中精准定位微小病变，且标准交叉熵损失难以应对细粒度的类别差异。
method: 提出一种结合目标检测引导的RoI令牌缩减、RoI间对比学习以及DINOv2预训练特征提取的综合框架。
result: 在公开乳腺钼靶数据集上的实验表明，该方法在分类性能上显著优于现有基准模型。
conclusion: 该研究证明了结合局部关注与基础视觉模型在提升医学影像诊断准确性方面的巨大潜力。
---

## 摘要
Vision Transformer (ViT) 已成为许多计算机视觉任务的首选架构，但其在计算机辅助诊断中的表现仍然有限。针对乳房 X 线摄影中的乳腺癌检测，我们确定了导致这一不足的两个主要原因。首先，医学图像具有高分辨率且病变细小，导致 token 数量过多，使得基于 softmax 的注意力机制难以定位并关注相关区域。其次，医学图像分类本质上是细粒度的，具有低类间差异和高类内差异，标准的交叉熵训练不足以应对。为了克服这些挑战，我们提出了一个包含三个关键组件的框架：(1) 使用目标检测模型引导注意力，进行基于感兴趣区域 (RoI) 的 token 缩减；(2) 在选定的 RoI 之间进行对比学习，通过基于难负样本 (hard-negative) 的训练来增强细粒度辨别能力；(3) 使用 DINOv2 预训练的 ViT，以捕获具有定位感知能力的细粒度特征，而非全局 CLIP 表示。在公开乳房 X 线摄影数据集上的实验表明，我们的方法优于现有的基准模型，证明了其在规模化乳腺癌筛查中的有效性和潜在临床应用价值。我们的代码已开源以供复现：https://aih-iitd.github.io/publications/attend-what-matters

## Abstract
Vision Transformers $(\texttt{ViT})$ have become the architecture of choice for many computer vision tasks, yet their performance in computer-aided diagnostics remains limited. Focusing on breast cancer detection from mammograms, we identify two main causes for this shortfall. First, medical images are high-resolution with small abnormalities, leading to an excessive number of tokens and making it difficult for the softmax-based attention to localize and attend to relevant regions. Second, medical image classification is inherently fine-grained, with low inter-class and high intra-class variability, where standard cross-entropy training is insufficient. To overcome these challenges, we propose a framework with three key components: (1) Region of interest $(\texttt{RoI})$ based token reduction using an object detection model to guide attention; (2) contrastive learning between selected $\texttt{RoI}$ to enhance fine-grained discrimination through hard-negative based training; and (3) a $\texttt{DINOv2}$ pretrained $\texttt{ViT}$ that captures localization-aware, fine-grained features instead of global $\texttt{CLIP}$ representations. Experiments on public mammography datasets demonstrate that our method achieves superior performance over existing baselines, establishing its effectiveness and potential clinical utility for large-scale breast cancer screening. Our code is available for reproducibility here: https://aih-iitd.github.io/publications/attend-what-matters

---

## 论文详细总结（自动生成）

以下是对论文《Attend what matters: Leveraging vision foundational models for breast cancer classification using mammograms》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：乳腺癌是全球女性死亡率极高的恶性肿瘤，乳房 X 线摄影（钼靶）是早期筛查的金标准。
*   **核心问题**：尽管 Vision Transformer (ViT) 在通用视觉领域表现优异，但在乳腺癌诊断中效果受限。主要原因有二：
    1.  **注意力稀释**：钼靶图像分辨率极高（4K），而病变区域极小（仅占约 0.1%）。标准 ViT 处理海量 token 时，注意力会被大量背景噪声稀释，难以聚焦微小病变。
    2.  **细粒度分类难题**：乳腺癌分类属于典型的细粒度任务，类间差异极小且类内差异大，传统的交叉熵损失函数难以学习到足够鲁棒的判别性特征。

### 2. 论文提出的方法论
该论文提出了一种结合目标检测引导和对比学习的新型框架，核心步骤如下：
*   **RoI 引导的 Token 缩减**：使用 **Grounding DINO (G-DINO)** 模型在零样本（Zero-shot）设置下，根据临床文本提示（如“肿块”、“病变”）预先定位出前 $k$ 个感兴趣区域 (RoI)。这有效地过滤了背景，将模型注意力集中在关键区域。
*   **特征编码**：采用 **DINOv2** 预训练的 ViT 作为骨干网络。相比于侧重全局语义的 CLIP，DINOv2 在自监督预训练中学习到了更强的局部感知和定位特征。
*   **空间关系建模**：将提取的 RoI 嵌入向量输入 Transformer 块，并引入 **旋转位置嵌入 (RoPE)**。RoPE 能够隐式编码 RoI 之间的相对空间位置关系，帮助模型理解病变与周围组织架构扭曲之间的联系。
*   **损失函数设计**：
    *   **BCE Loss**：用于最终的二分类任务。
    *   **排斥对比损失 (Repulsive Contrastive Loss, RCL)**：这是本文的创新点。通过最小化不同 RoI 嵌入之间的余弦相似度平方，强制模型让不同的 RoI 提案在特征空间中占据不同位置，防止特征坍缩，增强判别力。

### 3. 实验设计
*   **数据集**：主要在大型公开乳腺钼靶数据集 **VinDR-Mammo** 上进行验证。
*   **Benchmark（基准）**：
    *   **纯视觉模型**：ViT 变体（A/B/C）、MedVAE、TransReg-SwinT、XFMamba。
    *   **图文多模态模型**：MMBCD、Mammo-CLIP。
*   **对比实验**：将提出的方法与上述 SOTA（当前最佳）模型在 AUC、F1-score 以及不同召回率（R@0.1 等）指标下进行全面对比。

### 4. 资源与算力
*   论文中**未明确说明**具体的算力资源（如 GPU 型号、数量）以及具体的训练时长。
*   不过，作者提到该方法通过 RoI 缩减了输入 token 数量，且使用了冻结或部分微调的基础模型，暗示了其在推理和训练效率上具有一定优势。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了 8 种不同的基准模型，涵盖了从传统的 CNN-Transformer 混合架构到最新的状态空间模型（Mamba）以及多模态大模型。
*   **消融实验**：作者进行了详细的消融实验（见表 2），逐一验证了 DINOv2 嵌入、G-DINO 裁剪、Anchor 机制、RoPE 和 RCL 损失函数对性能的贡献。
*   **充分性评价**：实验设计较为严谨，通过消融实验证明了每个组件的有效性。对比实验选择了最新的 SOTA 模型（如 2024 年的 Mammo-CLIP），具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能突破**：该方法在 VinDR 数据集上取得了 **86.6% 的 AUC** 和 **54.5% 的 F1 分数**。
*   **超越 SOTA**：相比于之前的最佳模型 Mammo-CLIP，F1 分数提升了 **4%**，AUC 提升了 **1%**。
*   **数据效率**：该方法仅使用公开数据集进行图像训练，就超过了那些在大型私有图文数据集上预训练的模型，证明了其架构的优越性和可访问性。

### 7. 优点
*   **精准聚焦**：通过目标检测预筛选 RoI，解决了高分辨率医学影像中“大海捞针”的注意力分散问题。
*   **特征判别性强**：引入排斥对比损失（RCL），有效应对了医学影像细粒度分类中特征容易坍缩的痛点。
*   **临床实用性**：不依赖昂贵的私有数据，代码开源，便于临床大规模筛查任务的复现和部署。

### 8. 不足与局限
*   **两阶段依赖**：分类性能高度依赖于第一阶段 G-DINO 的检测质量。如果检测器未能覆盖真实的病灶区域，后续分类器将无法纠正。
*   **泛化性验证**：实验主要集中在 VinDR 单一数据集上，尚未在更多样化的跨医院、跨设备数据集上验证其鲁棒性。
*   **计算细节缺失**：缺乏训练成本和推理延迟的具体数据，对于实际临床部署的资源评估略显不足。

（完）
