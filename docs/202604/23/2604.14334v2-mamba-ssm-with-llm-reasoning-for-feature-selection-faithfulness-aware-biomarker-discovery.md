---
title: "Mamba-SSM with LLM Reasoning for Feature Selection: Faithfulness-Aware Biomarker Discovery"
title_zh: 结合大语言模型推理与 Mamba-SSM 的特征选择：忠实度感知的生物标志物发现
authors: "Pushpa Kumar Balan, Aijing Feng"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14334v2"
tags: ["query:ai-pm"]
score: 10.0
evidence: Mamba SSM和LLM推理用于乳腺癌RNA-seq的生物标志物发现
tldr: 本研究针对深度序列模型在生物标志物发现中易受组织成分混杂因素干扰的问题，提出了一种结合Mamba-SSM与大语言模型（LLM）推理的新型特征选择框架。通过Mamba模型提取梯度显著性基因，并利用DeepSeek-R1的思维链（CoT）推理进行过滤。实验证明，该方法在TCGA-BRCA数据集上仅需17个基因即可实现0.927的AUC，显著优于传统高方差基准和未过滤的显著性集合，展示了LLM在提升模型预测性能和去除混杂因素方面的强大潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14334v2/fig-001.webp\", \"caption\": \"Figure 1: End-to-end neuro-symbolic pipeline. High-dimensional TCGA-BRCA RNA-seq is processed by Mamba SSM to produce gradient saliency scores (top-50 candidate genes), then filtered by structured DeepSeek-R1 CoT reasoning to form a 17-gene BRCA-specific subset used in downstream comparison.\", \"page\": 2, \"index\": 1, \"width\": 790, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.14334v2/fig-002.webp\", \"caption\": \"Figure 2: Performance Comparison: Accuracy, F1, and AUC metrics across experimental conditions. B3 utilizes 250x fewer genes than B1 but achieves higher predictive stability.\", \"page\": 7, \"index\": 2, \"width\": 706, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.14334v2/fig-003.webp\", \"caption\": \"Figure 3: Raw Gradient Saliency Heatmap for the top-50 genes in TCGA-BRCA samples, highlighting the noisy feature space prior to LLM intervention.\", \"page\": 8, \"index\": 3, \"width\": 706, \"height\": 1096}, {\"url\": \"assets/figures/arxiv/2604.14334v2/fig-004.webp\", \"caption\": \"Figure 4: Visualization of the Agentic Chain-of-Thought (CoT) process, mapping Mamba saliency to biological rationale.\", \"page\": 9, \"index\": 4, \"width\": 706, \"height\": 515}]"
motivation: 深度学习模型通过梯度显著性提取的生物标志物常包含组织成分混杂因素，导致下游分类器性能下降，需探索LLM推理能否有效过滤这些干扰。
method: 在乳腺癌RNA-seq数据上训练Mamba模型并提取前50个显著基因，随后利用DeepSeek-R1的结构化思维链筛选出最终的17个关键基因。
result: LLM过滤后的17个基因集以极少的特征数实现了0.927的AUC，远超5000个高方差基因基准（0.903）及未过滤的显著性集合（0.832）。
conclusion: 针对性的混杂因素去除能显著提升预测性能，即便LLM推理在全面召回已知标志物方面存在局限，其选择性忠实度仍对生物发现具有重要价值。
---

## 摘要
深度序列模型的梯度显著性可以高效地筛选出候选生物标志物，但生成的基因列表可能会受到组织成分混杂因素的污染，从而降低下游分类器的性能。我们研究了大语言模型（LLM）的思维链（CoT）推理是否能过滤这些混杂因素，以及推理质量是否与下游性能相关。我们在 TCGA-BRCA RNA-seq 数据集上训练了一个 Mamba SSM，并通过梯度显著性提取了前 50 个基因；DeepSeek-R1 使用结构化思维链对每个候选基因进行评估，最终生成了一个包含 17 个基因的集合。在留出测试集上，原始的 50 基因显著性集合（无 LLM）的表现差于 5000 基因方差基准（AUC 0.832 对比 0.903），而经 LLM 过滤后的集合则超越了基准（AUC 0.927），且使用的特征数量减少了 294 倍。忠实度审计（参考 COSMIC CGC、OncoKB、PAM50）显示，选出的 17 个基因中有 6 个（35.3%）是经过验证的 BRCA 生物标志物，而输入中存在的 16 个已知 BRCA 基因中有 10 个被遗漏了——包括 FOXA1。下游性能与推理忠实度之间的这种分歧表明，在这种情境下存在“选择性忠实度”：有针对性地消除混杂因素可以在不具备全面召回率的情况下提高预测性能。

## Abstract
Gradient saliency from deep sequence models surfaces candidate biomarkers efficiently, but the resulting gene lists can be contaminated by tissue-composition confounders that degrade downstream classifiers. We study whether LLM chain-of-thought (CoT) reasoning can filter these confounders, and whether reasoning quality is associated with downstream performance. We train a Mamba SSM on TCGA-BRCA RNA-seq and extract the top-50 genes by gradient saliency; DeepSeek-R1 evaluates every candidate with structured CoT to produce a final 17-gene set. On the held-out test split, the raw 50-gene saliency set (no LLM) performs worse than a 5,000-gene variance baseline (AUC 0.832 vs. 0.903), while the LLM-filtered set surpasses it (AUC 0.927), using 294x fewer features. A faithfulness audit (COSMIC CGC, OncoKB, PAM50) shows that 6 of 17 selected genes (35.3%) are validated BRCA biomarkers, while 10 of 16 known BRCA genes present in the input were missed - including FOXA1. This divergence between downstream performance and reasoning faithfulness suggests selective faithfulness in this setting: targeted confounder removal can improve predictive performance without comprehensive recall.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mamba-SSM with LLM Reasoning for Feature Selection: Faithfulness-Aware Biomarker Discovery》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在高维 RNA-seq 数据（>20,000 基因）中，如何精准识别真正的疾病驱动生物标志物，并剔除“混杂因素”（如免疫浸润、组织纯度、批次效应等）。
*   **研究动机**：传统的深度学习模型（如 Mamba-SSM）虽然能通过梯度显著性（Gradient Saliency）识别重要特征，但这些特征往往只是与结果共变，而非生物学上的因果驱动因子。
*   **整体含义**：本研究探索了将大语言模型（LLM）的生物医学知识与神经序列模型的计算能力相结合的“神经符号”路径，旨在验证 LLM 的思维链（CoT）推理能否有效过滤噪声特征，并探讨 LLM 推理的生物学“忠实度”与下游预测性能之间的复杂关系。

### 2. 论文提出的方法论
该研究提出了一个四阶段的端到端流水线：
*   **阶段 1：Mamba-SSM 训练**：在 TCGA-BRCA 数据集上训练一个 Mamba 状态空间模型分类器，将基因表达值映射到高维空间，利用其线性缩放特性处理长序列。
*   **阶段 2：梯度显著性提取**：通过反向传播计算每个基因对分类损失的贡献，提取平均梯度显著性最高的 Top-50 基因作为候选池。
*   **阶段 3：结构化 CoT 推理过滤**：使用 **DeepSeek-R1 (7B)** 模型。通过结构化提示词（包含 5 个拒绝标准和 3 个保留标准），要求 LLM 对 50 个候选基因逐一进行生物学评估，最终筛选出 17 个基因。
*   **阶段 4：忠实度审计**：将 LLM 选择的基因与权威数据库（COSMIC CGC, OncoKB, PAM50）进行交叉比对，评估其生物学准确性。

### 3. 实验设计
*   **数据集**：TCGA-BRCA（乳腺癌）RNA-seq 数据，包含 1,095 个肿瘤样本和 113 个正常样本，约 20,000 个蛋白编码和非编码基因。
*   **Benchmark（基准）与对比方法**：
    1.  **B1 (方差基准)**：选取方差最高的 5,000 个基因。
    2.  **B2 (原始显著性)**：仅使用 Mamba 提取的 Top-50 显著性基因（无 LLM 过滤）。
    3.  **B3 (LLM 过滤后)**：使用经 LLM 推理筛选后的 17 个基因。
*   **评估指标**：准确率（Accuracy）、F1 分数、AUC。

### 4. 资源与算力
*   **算力说明**：文中提到使用了 **Cohere Labs** 的研究资助和 **中央密苏里大学 (UCM)** 的计算资源。
*   **具体细节**：DeepSeek-R1 (7B) 是通过 **Ollama** 在本地运行的（温度设定为 0.3）。
*   **缺失信息**：论文未明确列出具体的 GPU 型号、数量以及 Mamba 模型的具体训练时长。

### 5. 实验数量与充分性
*   **实验规模**：主要针对乳腺癌（BRCA）这一单一癌种进行了深入研究。
*   **充分性评估**：
    *   **优点**：对比了高维基准和低维显著性集合，实验逻辑闭环。
    *   **局限**：实验仅基于单一的随机种子和单一的训练/测试划分（80/20 比例），缺乏多中心数据集验证或交叉验证的统计显著性分析。
    *   **客观性**：作者诚实地报告了 LLM 的失败案例（如漏掉关键基因 FOXA1），体现了研究的客观性。

### 6. 论文的主要结论与发现
*   **性能提升**：LLM 过滤后的 17 基因集（B3）达到了 **0.927 的 AUC**，优于 5,000 基因基准（0.903）和 50 基因原始显著性集合（0.832）。
*   **选择性忠实度（Selective Faithfulness）**：这是一个重要发现。尽管 LLM 的生物学召回率较低（仅识别出输入中 16 个已知基因中的 6 个，召回率 0.375），但它通过精准剔除混杂因素（如肌肉特异性基因、通用免疫标志物），显著提升了模型的泛化能力。
*   **推理缺陷**：LLM 在推理中仍存在“自信地犯错”，例如拒绝了乳腺癌最关键的转录因子 **FOXA1**，并可能使用幻觉的通路语言来辩解。

### 7. 优点
*   **神经符号结合**：成功将 Mamba 的高效特征提取与 LLM 的领域知识推理结合，实现了极高倍率（294 倍）的特征压缩。
*   **深度审计**：不仅关注模型指标，还通过权威生物数据库对 LLM 的推理过程进行了“忠实度”审计，揭示了 LLM 在科学发现中的行为模式。
*   **实用性**：证明了在小样本/高噪声的生物医学领域，LLM 的“精准剔除”能力比“全面召回”对提升模型性能更有贡献。

### 8. 不足与局限
*   **泛化性验证不足**：研究仅限于 TCGA-BRCA 数据集，未在其他癌症或疾病类型上验证。
*   **基准对比缺失**：未与传统的特征选择方法（如 LASSO、ElasticNet 或随机森林重要性评分）进行直接对比。
*   **LLM 局限性**：7B 规模的 LLM 在处理复杂生物医学知识时仍有幻觉风险，且对显著性排名较低的关键基因存在偏见（如 FOXA1 的遗漏）。
*   **统计稳健性**：由于依赖单一划分，结果可能受到数据分布偶然性的影响。

（完）
