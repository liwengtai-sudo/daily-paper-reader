---
title: "MambaBack: Bridging Local Features and Global Contexts in Whole Slide Image Analysis"
title_zh: MambaBack：在全切片图像分析中连接局部特征与全局上下文
authors: "Sicheng Chen, Chad Wong, Tianyi Zhang, Enhui Chai, Zeyu Liu, Fei Xia"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15729v1"
tags: ["query:ai-pm"]
score: 10.0
evidence: 使用 Mamba 骨干网络进行癌症诊断的全切片图像分析
tldr: 针对全扫描病理图像（WSI）分析中多实例学习面临的2D空间局部性缺失、局部特征建模不足及推理内存占用高等挑战，本文提出MambaBack。该架构结合了MambaOut的Gated CNN以提取局部细胞特征，并利用BiMamba2建模全局上下文，同时引入希尔伯特采样和异步分块设计，在五个数据集上超越了现有SOTA方法，实现了高效且精准的病理诊断。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15729v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of MambaBack. a. MIL pipeline with MambaBack. b. MambaBack structure. c. Heatmap visualization. d. Inference memory usage comparison.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 802}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-002.webp\", \"caption\": \"Table 1: The performance comparison with SOTA methods.\", \"page\": 5, \"index\": 2, \"width\": 1064, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-003.webp\", \"caption\": \"Figure 2: Analysis of WSI tile distributions and ablation studies on key components. a. Distribution of tile counts across datasets. b. Ablation study on sampling strategies on PANDA dataset. c. Ablation study on model architectures on PANDA dataset.\", \"page\": 5, \"index\": 3, \"width\": 1068, \"height\": 463}]"
motivation: 现有的Mamba模型在处理WSI时难以兼顾2D空间局部性保持、细粒度局部特征提取以及边缘设备的内存效率。
method: 提出一种混合架构，利用希尔伯特采样保持空间感知，结合Gated CNN与BiMamba2构建分层结构，并采用异步分块设计优化内存。
result: 在五个公开数据集上的实验结果表明，MambaBack的性能优于包括Transformer和纯Mamba在内的七种最先进方法。
conclusion: MambaBack通过融合局部卷积与全局状态空间模型，为高效、低内存消耗的大规模病理图像分析提供了新的有效方案。
---

## 摘要
全切片图像（WSI）分析在计算病理学中至关重要，它通过整合不同放大倍率下的形态学和结构线索来实现癌症诊断。多实例学习（MIL）是 WSI 分析的标准框架。最近，Mamba 因其源自自然语言处理（NLP）的高效性和全局上下文建模能力，超越了 Transformer，成为 MIL 中极具前景的主干网络。然而，现有的基于 Mamba 的 MIL 方法面临三个关键挑战：(1) 在 1D 序列展平过程中破坏了 2D 空间局部性；(2) 对细粒度局部细胞结构的建模效果欠佳；(3) 在资源受限的边缘设备上推理时存在高内存峰值。诸如 MambaOut 等研究表明，Mamba 的 SSM 组件在局部特征提取方面是冗余的，而门控卷积神经网络（Gated CNNs）已足够。认识到 WSI 分析既需要类似于自然图像的细粒度局部特征提取，又需要类似于 NLP 的全局上下文建模，我们提出了 MambaBack，这是一种融合了 Mamba 和 MambaOut 优势的新型混合架构。首先，我们提出了希尔伯特（Hilbert）采样策略，以保留 1D 序列中图块（tiles）的 2D 空间局部性，从而增强模型的空间感知能力。其次，我们设计了一个层次化结构，包含一个基于 MambaOut 的 1D 门控 CNN 模块用于捕捉局部细胞特征，以及一个 BiMamba2 模块用于聚合全局上下文，共同增强多尺度表示。最后，我们实现了一种非对称分块设计，允许在训练期间进行并行处理，并在推理期间进行分块流式累加，从而最大限度地降低部署时的峰值内存占用。在五个数据集上的实验结果表明，MambaBack 的性能优于七种最先进的方法。源代码和数据集已公开。

## Abstract
Whole Slide Image (WSI) analysis is pivotal in computational pathology, enabling cancer diagnosis by integrating morphological and architectural cues across magnifications. Multiple Instance Learning (MIL) serves as the standard framework for WSI analysis. Recently, Mamba has become a promising backbone for MIL, overtaking Transformers due to its efficiency and global context modeling capabilities originating from Natural Language Processing (NLP). However, existing Mamba-based MIL approaches face three critical challenges: (1) disruption of 2D spatial locality during 1D sequence flattening; (2) sub-optimal modeling of fine-grained local cellular structures; and (3) high memory peaks during inference on resource-constrained edge devices. Studies like MambaOut reveal that Mamba's SSM component is redundant for local feature extraction, where Gated CNNs suffice. Recognizing that WSI analysis demands both fine-grained local feature extraction akin to natural images, and global context modeling akin to NLP, we propose MambaBack, a novel hybrid architecture that harmonizes the strengths of Mamba and MambaOut. First, we propose the Hilbert sampling strategy to preserve the 2D spatial locality of tiles within 1D sequences, enhancing the model's spatial perception. Second, we design a hierarchical structure comprising a 1D Gated CNN block based on MambaOut to capture local cellular features, and a BiMamba2 block to aggregate global context, jointly enhancing multi-scale representation. Finally, we implement an asymmetric chunking design, allowing parallel processing during training and chunking-streaming accumulation during inference, minimizing peak memory usage for deployment. Experimental results on five datasets demonstrate that MambaBack outperforms seven state-of-the-art methods. Source code and datasets are publicly available.

---

## 论文详细总结（自动生成）

这是一份关于论文《MambaBack: Bridging Local Features and Global Contexts in Whole Slide Image Analysis》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
全切片图像（WSI）分析是计算病理学的核心，但其“十亿像素”级的超大规模给计算带来了巨大挑战。目前主流的**多实例学习（MIL）**框架在引入 Mamba（状态空间模型 SSM）后虽然提升了效率，但仍面临三个痛点：
*   **空间局部性受损**：将 2D 图像块展平为 1D 序列时，传统的扫描方式（如行优先）破坏了组织结构的拓扑连续性。
*   **局部特征建模不足**：Mamba 的 SSM 组件在处理细粒度局部细胞结构时不如卷积神经网络（CNN）有效。
*   **推理内存瓶颈**：在资源受限的临床边缘设备上，处理超长序列时容易出现内存峰值（OOM）。

**MambaBack** 的提出旨在通过结合 MambaOut（门控 CNN）和 Mamba2 的优势，在保持全局上下文建模的同时，增强局部特征提取并降低推理成本。

### 2. 方法论：核心思想与关键技术
MambaBack 采用了一种分层的混合架构，核心技术包括：
*   **希尔伯特采样策略（Hilbert Sampling）**：利用希尔伯特空间填充曲线的特性，将 2D 坐标映射为 1D 索引。相比 Z-order 或随机采样，它能最大限度地让 2D 空间中的邻居在 1D 序列中也保持邻近，从而保留组织的微环境感知。
*   **分层混合结构**：
    *   **局部阶段（1D Gated CNN）**：基于 MambaOut 设计，利用深度可分离卷积（DWConv）和门控机制捕捉高倍率下的细胞形态特征。
    *   **全局阶段（BiMamba2）**：将局部块压缩为代表性 Token 后，利用双向 Mamba2 模块进行长序列建模，聚合全片水平的组织上下文。
*   **非对称分块设计（Asymmetric Chunking）**：
    *   **训练时**：全序列并行处理，利用 GPU 吞吐量。
    *   **推理时**：采用“分块与累加”模式，将序列切分为小块逐个处理并丢弃中间特征图，使内存占用不随 WSI 尺寸线性增长，实现常数级的内存稳定性。

### 3. 实验设计
*   **数据集**：涵盖 5 个公开数据集，涉及 4 种任务：
    *   **CAMELYON16/17**：乳腺癌淋巴结转移检测（二分类/多分类）。
    *   **PANDA**：前列腺癌 ISUP 分级。
    *   **TCGA-NSCLC**：肺癌亚型分类。
    *   **TCGA-BRCA**：乳腺癌生存分析（生存预测任务）。
*   **Benchmark 与对比方法**：对比了 7 种 SOTA 方法，包括传统的 **ABMIL、CLAM、DTFD-MIL**，基于 Transformer 的 **TransMIL、Prov-GigaPath**，以及基于 SSM 的 **MambaMIL、PathRWKV**。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX 4090 GPU**。
*   **软件环境**：Python 3.12, PyTorch 2.9, CUDA 12.8。
*   **训练细节**：使用 AdamW 优化器，余弦退火调度，学习率在 $1 \times 10^{-4}$ 到 $1 \times 10^{-2}$ 之间搜索。
*   **时长说明**：文中未明确给出具体的训练总时长，但提到在训练阶段利用了并行化以提高效率。

### 5. 实验数量与充分性
*   **实验规模**：共涉及 **17,828 张 WSI**，样本量巨大，覆盖了分类和生存分析等多种临床场景。
*   **消融实验**：针对“希尔伯特采样”、“混合模型架构”以及“非对称设计”分别做了详细的对比实验（如对比随机采样、Z-order、纯 CNN 或纯 Mamba 架构）。
*   **客观性**：采用了 5 折交叉验证并报告了平均值和标准差，指标包括 AUC、Accuracy、F1-score 和 C-Index，评估维度全面且客观。

### 6. 主要结论与发现
*   **性能领先**：MambaBack 在所有 5 个数据集上均达到了 SOTA 性能，尤其在 F1 分数上提升显著，证明了其在处理类别不平衡和复杂形态时的鲁棒性。
*   **空间感知的重要性**：希尔伯特采样在所有采样策略中表现最优且方差最低，证明保持 2D 局部性对病理诊断至关重要。
*   **内存效率**：非对称设计使 MambaBack 的推理内存占用接近轻量级模型（如 CLAM），远低于 Transformer 模型，解决了大尺寸 WSI 的部署难题。

### 7. 优点（亮点）
*   **架构创新**：巧妙地将 MambaOut（证明了视觉任务中 SSM 的冗余部分）与 Mamba2 结合，实现了“局部卷积+全局 SSM”的互补。
*   **部署友好**：通过非对称分块设计，在不牺牲性能的前提下，极大地降低了边缘设备的准入门槛。
*   **数学支撑**：引入希尔伯特曲线解决病理组织非凸、不规则形状的序列化问题，比传统的行列扫描更符合生物学逻辑。

### 8. 不足与局限
*   **计算复杂度**：虽然推理内存降低了，但分层结构和双向 Mamba2 的引入在训练阶段可能仍需要较高的计算开销。
*   **超参数敏感性**：分块大小（Chunk Size $L$）和采样长度等超参数可能需要针对不同器官或放大倍率进行微调，论文中未深入讨论这些参数的普适性。
*   **实时性验证**：虽然提到了边缘设备部署，但未给出在具体 FPGA 或嵌入式设备上的实际推理延迟（Latency）数据。

（完）
