---
title: Efficient Search of Implantable Adaptive Cells for Medical Image Segmentation
title_zh: 用于医学图像分割的可植入自适应单元的高效搜索
authors: "Emil Benedykciuk, Marcin Denkowski, Grzegorz M. Wójcik"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14849v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于医学图像分割的自适应跳跃模块
tldr: 针对医学图像分割中自适应跳跃连接模块（IAC）搜索成本高的问题，本文提出了一种高效的搜索框架IAC-LTH。研究发现IAC单元在搜索早期即可表现出操作稳定性，据此引入了基于JS散度的稳定性准则来动态剪枝低重要性操作。在多个公开数据集和U-Net骨干网络上的实验表明，该方法在保持甚至提升分割性能的同时，将搜索速度提升了3.7至16倍，显著降低了计算开销。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14849v1/fig-001.webp\", \"caption\": \"Figure 6: When does the final genotype first emerge during search? For each dataset (ACDC, AMOS, BraTS, KiTS) we record,\", \"page\": 11, \"index\": 1, \"width\": 804, \"height\": 537}, {\"url\": \"assets/figures/arxiv/2604.14849v1/fig-002.webp\", \"caption\": \"Figure 7: Per-edge difficulty of convergence. For each dataset (ACDC, AMOS, BraTS, KiTS) we chart, for every edge of the\", \"page\": 11, \"index\": 2, \"width\": 825, \"height\": 508}, {\"url\": \"assets/figures/arxiv/2604.14849v1/fig-003.webp\", \"caption\": \"Figure 5: Global convergence of architecture parameters. Cosine similarity between the current 𝛼 ⋅ 𝛽 vector and its final value is\", \"page\": 10, \"index\": 3, \"width\": 825, \"height\": 552}, {\"url\": \"assets/figures/arxiv/2604.14849v1/fig-004.webp\", \"caption\": \"Figure 3: Evolution of the importance of operations during the search process (epochs). The importance of an operation on edge (𝑖, 𝑗) is defined as the product of the operation-specific logit 𝛼𝑜 𝑖,𝑗 and the edge-specific logit 𝛽𝑖,𝑗 .\", \"page\": 8, \"index\": 4, \"width\": 825, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2604.14849v1/fig-005.webp\", \"caption\": \"Figure 1: Overview of the proposed IAC search algorithm. (1) Search-space definition: each iteration starts from the current set\", \"page\": 4, \"index\": 5, \"width\": 792, \"height\": 580}, {\"url\": \"assets/figures/arxiv/2604.14849v1/fig-006.webp\", \"caption\": \"Figure 4: Similarity matrices between IAC genotypes sampled at different epochs. Each matrix entry compares two epochs using\", \"page\": 9, \"index\": 6, \"width\": 929, \"height\": 1021}]"
motivation: 传统的IAC搜索框架在每个数据集和骨干网络上都需要长达200轮的微分搜索，计算成本过于高昂。
method: 提出基于Jensen-Shannon散度的稳定性准则，实时监测操作重要性分布并提前剪枝低贡献操作，从而加速搜索过程。
result: 在ACDC、BraTS等四个基准数据集上，IAC-LTH在显著缩短搜索时间（最高16倍）的同时，达到了与全量搜索相当的分割精度。
conclusion: 通过识别早期稳定的操作，可以在极低的计算成本下设计出高性能的自适应跳跃模块，使NAS在医学影像领域更具实用性。
---

## 摘要
目的：自适应跳跃模块可以提高医学图像分割性能，但其搜索过程计算成本高昂。可植入自适应单元（IACs）是插入 U-Net 跳跃连接中的紧凑型神经架构搜索（NAS）模块，与全网络 NAS 相比，它缩小了搜索空间。然而，原始 IAC 框架针对每个骨干网络和数据集仍需进行 200 个轮次的可微分搜索。方法：我们在公开的医学图像分割基准数据集上，分析了可微分搜索过程中 IAC 单元内操作和边的时间行为。我们发现，在最终离散单元中选中的操作通常在训练早期就已成为最强的候选者，且其架构参数在最终轮次之前就已趋于稳定。基于此，我们提出了一种基于 Jensen-Shannon 散度的稳定性准则，用于追踪每条边的操作重要性分布，并在搜索过程中逐步剪枝低重要性的操作。该加速框架被称为 IAC-LTH。结果：在四个公开基准数据集（ACDC、BraTS、KiTS、AMOS）、多个 2D U-Net 骨干网络以及 2D nnU-Net 流水线上，IAC-LTH 发现的 IAC 单元在患者级分割性能上与原始全长搜索发现的单元相当，有时甚至略有超出，同时在不同数据集和骨干网络上将 NAS 的实际耗时降低了 3.7 倍至 16 倍。这些结果在不同架构、基准数据集以及非增强和增强训练设置下均保持一致，同时保留了配备 IAC 的 U-Net 相对于强注意力机制和密集跳跃连接基准模型的性能增益。结论：无需运行完整搜索，即可从早期稳定的操作中识别出具有竞争力的 IAC 架构，这使得自适应跳跃模块设计在现实计算限制下的医学图像分割中更具实用性。

## Abstract
Purpose: Adaptive skip modules can improve medical image segmentation, but searching for them is computationally costly. Implantable Adaptive Cells (IACs) are compact NAS modules inserted into U-Net skip connections, reducing the search space compared with full-network NAS. However, the original IAC framework still requires a 200-epoch differentiable search for each backbone and dataset. Methods: We analyzed the temporal behavior of operations and edges within IAC cells during differentiable search on public medical image segmentation benchmarks. We found that operations selected in the final discrete cell typically emerge among the strongest candidates early in training, and their architecture parameters stabilize well before the final epoch. Based on this, we propose a Jensen--Shannon-divergence-based stability criterion that tracks per-edge operation-importance distributions and progressively prunes low-importance operations during search. The accelerated framework is called IAC-LTH. Results: Across four public benchmarks (ACDC, BraTS, KiTS, AMOS), several 2-D U-Net backbones, and a 2-D nnU-Net pipeline, IAC-LTH discovers IAC cells whose patient-level segmentation performance matches and sometimes slightly exceeds that of cells found by the original full-length search, while reducing wall-clock NAS cost by 3.7x to 16x across datasets and backbones. These results are consistent across architectures, benchmarks, and both non-augmented and augmented training settings, while preserving the gains of IAC-equipped U-Nets over strong attention-based and dense-skip baselines. Conclusion: Competitive IAC architectures can be identified from early-stabilizing operations without running the full search, making adaptive skip-module design more practical for medical image segmentation under realistic computational constraints.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Search of Implantable Adaptive Cells for Medical Image Segmentation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保证医学图像分割精度的前提下，大幅降低自适应跳跃连接模块（IAC）的搜索计算成本？
*   **背景**：传统的 U-Net 架构在跳跃连接处通常使用简单的恒等映射。虽然“可植入自适应单元（IAC）”作为一种 NAS 插件能显著提升性能，但其原始的可微分搜索过程（DNAS）在每个数据集和骨干网络上都需要长达 200 轮的训练，这在临床和资源受限的环境中极具挑战性。
*   **研究动机**：作者观察到，在搜索过程中，最终胜出的操作往往在早期就已显现出优势。受“彩票假设（LTH）”启发，作者希望通过识别早期稳定的操作来提前终止搜索或剪枝，从而实现高效搜索。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：引入 **IAC-LTH** 框架，利用 **Jensen-Shannon (JS) 散度** 监测操作重要性分布的稳定性，实现动态剪枝和提前停止。
*   **关键技术细节**：
    *   **操作重要性度量**：定义操作重要性为架构参数 $\alpha$（操作概率）与 $\beta$（边权重）的乘积。
    *   **JS 散度稳定性准则**：在每个 Epoch 结束时，计算每条边上操作分布与前一 Epoch 的 JS 散度。若散度低于阈值，则认为该边已稳定。
    *   **渐进式剪枝策略**：对已稳定的边进行剪枝。首先移除低于均值两个标准差的“离群负向操作”；若无离群值，则移除重要性最低的一个操作。
    *   **单级优化（Single-level Optimization）**：不同于原始 IAC 的双级优化（在搜索集和验证集交替更新），IAC-LTH 在全量训练集上同时更新权重和架构参数，进一步简化流程。
    *   **权重重置**：在搜索结束后，将选定单元的权重重置为初始随机状态（遵循 LTH 协议）进行最终微调。

### 3. 实验设计
*   **数据集**：涵盖四种不同模态和部位的公开医学影像数据集：**ACDC**（心脏 MRI）、**BraTS**（脑肿瘤 MRI）、**KiTS**（肾脏 CT）和 **AMOS**（多器官 CT）。
*   **骨干网络（Backbones）**：包括 2D U-Net 配合多种编码器（VGG-16, ResNet-50, MobileNetV3, EfficientNetV2-S/M）以及 2D nnU-Net 框架。
*   **对比方法（Benchmarks）**：
    *   **基础模型**：标准 U-Net (Ref)。
    *   **经典改进**：Attention U-Net (Att)、U-Net++ (Nested)、Residual Attention (ResAtt)。
    *   **原始 NAS 方法**：原始 200 轮全量搜索的 IAC。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 Ti GPU**。
*   **软件环境**：Python 3.9, TensorFlow 2.17。
*   **耗时对比**：
    *   **原始 IAC**：在 AMOS 数据集（带增强）上搜索耗时约 **2635 分钟（约 44 小时）**。
    *   **IAC-LTH**：在相同设置下仅需 **162 分钟（约 2.7 小时）**，实现了 **16.3 倍** 的加速。
    *   **平均表现**：在不同任务中，搜索轮数从 200 轮降至约 45-55 轮。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个数据集上测试了 6 种不同的骨干网络，并分别在“无数据增强”和“有数据增强”两种环境下运行，还对比了 4 种主流的跳跃连接变体。
*   **充分性与公平性**：
    *   使用了患者级（Patient-level）的 Dice 系数评估，而非简单的切片级，更符合临床实际。
    *   引入了 Fisher 组合 <code>p-value</code> 检验，证明了性能提升在统计学上的显著性。
    *   所有对比方法均在相同的预处理和训练协议下运行，实验设计客观、公平。

### 6. 主要结论与发现
*   **性能保持**：IAC-LTH 发现的架构在分割精度上与全量搜索的 IAC 持平，在某些 CT 场景（如 AMOS 和 KiTS）下甚至略有超越。
*   **大幅加速**：搜索成本在实际耗时上降低了 **3.7 倍至 16 倍**。
*   **早期稳定性**：实验证实，IAC 单元内的操作在 40-70 轮左右即可确定最终的“胜出者”，无需跑满 200 轮。
*   **通用性**：该方法在不同的 U-Net 变体和 nnU-Net 框架中均表现稳健。

### 7. 优点（亮点）
*   **实用性极强**：将原本需要数天的 NAS 搜索缩短至数小时，极大地提升了 NAS 在医学影像领域的落地可能性。
*   **理论支撑扎实**：巧妙地将信息论（JS 散度）与深度学习剪枝理论（LTH）结合，解决了 DNAS 搜索过程中的冗余计算问题。
*   **即插即用**：作为一种“改造（Retrofit）”策略，它不需要重新设计整个网络，只需替换跳跃连接。

### 8. 不足与局限
*   **搜索空间限制**：目前仅针对 2D U-Net 的跳跃连接进行搜索，尚未扩展到 3D 网络或更复杂的全网络拓扑搜索。
*   **超参数依赖**：JS 散度的初始阈值和增长因子（$\kappa$）仍属于启发式设置，虽然在本文实验中表现稳健，但在极端新场景下可能需要微调。
*   **硬件评估缺失**：虽然减少了搜索时间，但未详细讨论 IAC 模块在嵌入式医疗设备上的推理延迟（Latency）影响。

（完）
