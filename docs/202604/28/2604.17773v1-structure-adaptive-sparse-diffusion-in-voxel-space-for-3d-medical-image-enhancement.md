---
title: Structure-Adaptive Sparse Diffusion in Voxel Space for 3D Medical Image Enhancement
title_zh: 用于三维医学图像增强的体素空间结构自适应稀疏扩散模型
authors: "Hongxu Jiang, Fei Li, Boxiao Yu, Ying Zhang, Kaleb Smith, Kuang Gong, Wei Shao"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17773v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于3D医学图像增强的深度学习
tldr: 针对3D医学图像增强中扩散模型计算开销巨大的问题，本文提出一种结构自适应的稀疏体素空间扩散框架。该方法利用退化输入中的解剖先验，通过稀疏时间步采样和速度空间监督实现高效训练，并引入结构感知轨迹调制（STM）模块以适应局部解剖内容。在保持解剖细节的同时，实现了10倍的训练加速，并在CT、PET和MRI等多个任务上达到SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17773v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison on denoising and 4× super-resolution tasks across four datasets. Best results are in red, second-best are blue.\", \"page\": 6, \"index\": 1, \"width\": 688, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative denoising results on lung CT and brain PET. Purple and blue boxes highlight lung fissures and subtle anatomical boundaries, respectively. Our model preserves sharper, more structurally consistent details than all baselines.\", \"page\": 7, \"index\": 2, \"width\": 639, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-003.webp\", \"caption\": \"Fig. 3. Qualitative 4× super-resolution results on aorta CTA and brain MRI. Highlighted regions show vessel boundaries and cortical structures. Our model recovers finer anatomical details with fewer artifacts than all baselines.\", \"page\": 7, \"index\": 3, \"width\": 639, \"height\": 397}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the proposed framework.\", \"page\": 3, \"index\": 4, \"width\": 723, \"height\": 202}]"
motivation: 传统的3D扩散模型在处理高维体数据时，由于密集的噪声调度导致计算成本极高且存在大量冗余。
method: 提出一种在稀疏时间步上运行的体素空间扩散框架，并结合结构感知轨迹调制（STM）模块进行局部解剖特征校准。
result: 在四个涵盖CT、PET和MRI的数据集上，该方法在去噪和超分辨率任务中均取得了领先性能，且训练速度提升达10倍。
conclusion: 该研究证明了通过稀疏调度和结构自适应调制，可以在不进行有损压缩的前提下显著提升3D医学图像增强的效率与精度。
---

## 摘要
三维（3D）医学图像增强，包括去噪和超分辨率，对于 CT、PET 和 MRI 的临床诊断至关重要。尽管扩散模型在二维医学成像中取得了显著成功，但由于高维体积数据上冗长的扩散轨迹，将其扩展到高分辨率三维体积在计算上仍然难以实现。我们观察到，在条件增强任务中，退化输入中强大的解剖先验使得密集的加噪计划在很大程度上是冗余的。利用这一见解，我们提出了一种稀疏体素空间扩散框架，该框架在均匀下采样的紧凑时间步集合上进行训练和采样。网络直接在数据流形上预测干净数据，并在速度空间中进行监督，以实现稳定的梯度缩放。轻量级的结构感知轨迹调制（STM）模块根据局部解剖内容在每个网络块处重新校准时间嵌入，从而在共享的稀疏计划上实现结构自适应去噪。该框架直接在体素空间运行，在不进行有损压缩的情况下保留了精细的解剖细节，同时实现了高达 10 倍的训练加速。在涵盖 CT、PET 和 MRI 的四个数据集上的实验表明，该方法在去噪和超分辨率任务上均达到了最先进的性能。我们的代码公开于：https://github.com/mirthAI/sparse-3d-diffusion。

## Abstract
Three-dimensional (3D) medical image enhancement, including denoising and super-resolution, is critical for clinical diagnosis in CT, PET, and MRI. Although diffusion models have shown remarkable success in 2D medical imaging, scaling them to high-resolution 3D volumes remains computationally prohibitive due to lengthy diffusion trajectories over high-dimensional volumetric data. We observe that in conditional enhancement, strong anatomical priors in the degraded input render dense noise schedules largely redundant. Leveraging this insight, we propose a sparse voxel-space diffusion framework that trains and samples on a compact set of uniformly subsampled timesteps. The network predicts clean data directly on the data manifold, supervised in velocity space for stable gradient scaling. A lightweight Structure-aware Trajectory Modulation (STM) module recalibrates time embeddings at each network block based on local anatomical content, enabling structure-adaptive denoising over the shared sparse schedule. Operating directly in voxel space, our framework preserves fine anatomical detail without lossy compression while achieving up to $10\times$ training acceleration. Experiments on four datasets spanning CT, PET, and MRI demonstrate state-of-the-art performance on both denoising and super-resolution tasks. Our code is publicly available at: https://github.com/mirthAI/sparse-3d-diffusion.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Structure-Adaptive Sparse Diffusion (结构自适应稀疏扩散)** 的框架，旨在解决 3D 医学图像增强（去噪与超分辨率）中扩散模型计算开销巨大的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：3D 医学影像（如 CT、PET、MRI）的质量受限于辐射剂量、扫描时间和硬件，常存在噪声和分辨率不足的问题。
*   **核心问题**：虽然扩散模型（Diffusion Models）在图像生成上表现优异，但将其应用于 3D 体数据时面临巨大挑战：
    *   **计算成本极高**：高维体数据上的长扩散轨迹导致训练和推理极慢。
    *   **表示损失**：为了降速，现有方法常在压缩空间（如 Latent 空间或小波域）操作，这会丢失关键的解剖细节（如肺裂、皮质褶皱）。
*   **核心观察**：在条件增强任务中，退化的输入图像本身已包含强解剖先验，因此传统的密集噪声调度（如 1000 步）存在大量冗余。

### 2. 方法论：核心思想与关键技术
该框架直接在**体素空间（Voxel Space）**运行，主要包含三个核心组件：
*   **$x_0$ 预测与速度监督（Velocity Supervision）**：
    *   不同于预测噪声（$\epsilon$），模型直接预测干净图像 $x_0$。这使网络输出始终保持在数据流形上。
    *   为了解决 $x_0$ 预测在不同时间步梯度不平衡的问题，引入了**速度空间监督**。通过将预测值转换为速度 $v_t$，实现跨时间步的均匀梯度缩放，稳定训练。
*   **稀疏时间步调度（Sparse Timestep Scheduling）**：
    *   利用 $x_0$ 预测的平滑性，从 1000 个时间步中均匀下采样极少数步（实验中仅用 **5 步**）进行训练和采样，大幅减少计算量。
*   **结构感知轨迹调制（STM）**：
    *   **多结构编码器**：通过 3D 卷积分支和两个正交平面（冠状面、矢状面）的 2D 卷积分支提取局部解剖特征。
    *   **块级调制**：将提取的结构特征通过 MLP 映射为仿射参数（Scale, Shift），在 UNet 的每个残差块中动态调整时间嵌入（Time Embedding）。
    *   **目的**：使模型能根据局部解剖复杂度（如复杂的血管 vs 平滑的软组织）自适应地调整去噪强度。

### 3. 实验设计
*   **数据集**：涵盖了四种主流医学影像任务：
    1.  **肺部 CT 去噪**：LDCT 数据集（50 名患者）。
    2.  **脑部 PET 去噪**：FDG PET 数据集（88 名患者）。
    3.  **主动脉 CTA 超分辨率（4x）**：AortaSeg24 数据集（60 名患者）。
    4.  **脑部 MRI 超分辨率（4x）**：UHB FCD 数据集（120 次扫描）。
*   **基准方法（Benchmarks）**：对比了四种代表性的 3D 扩散模型范式：
    *   **3D LDM**（潜在空间扩散）、**3D WDM**（小波域扩散）、**3D DDPM**（标准像素空间扩散）、**3D DDIM**（隐式采样扩散）。
*   **评价指标**：PSNR, SSIM, MS-SSIM, HFEN（高频误差范数，用于衡量细节保留）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号和数量。
*   **效率数据**：文中强调了其方法在训练收敛速度上比 3D DDIM 快 **10 倍**，且在推理时由于仅需 5 步采样，效率显著提升。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同模态、不同任务的数据集上进行了验证，每项任务均提供了定量指标和定性视觉对比。
*   **消融实验**：
    *   验证了扩散步数 $K$ 的影响（发现 $K=5$ 时性能达到顶峰，步数过多反而会导致过度平滑）。
    *   验证了 $x_0$ 预测、稀疏调度、STM 模块各自的贡献。
*   **充分性评价**：实验设计较为全面，跨模态的验证增强了结论的说服力。对比实验涵盖了当前主流的 3D 扩散模型变体，具有较高的客观性。

### 6. 主要结论与发现
*   **性能领先**：在所有四个数据集上，该方法在 PSNR 和 SSIM 等指标上均达到 SOTA 或次优。
*   **细节保留**：相比于潜在空间或小波域方法，直接在体素空间操作能更好地恢复精细的解剖结构（如肺裂线、血管边界）。
*   **效率飞跃**：通过稀疏调度，在极短的训练时间内即可达到甚至超过传统密集扩散模型的最终性能。
*   **自适应性**：STM 模块能有效处理 3D 图像中的解剖异质性，使不同复杂度的区域获得最优的去噪效果。

### 7. 优点与亮点
*   **高效性**：将 3D 扩散模型的训练和推理成本降低了一个数量级，使其在临床实际应用中更具可行性。
*   **无损处理**：坚持在体素空间操作，避免了 VAE 压缩带来的不可逆信息损失。
*   **结构自适应**：STM 模块的设计巧妙地结合了 2D 平面先验和 3D 体积特征，提升了模型对复杂解剖结构的建模能力。

### 8. 不足与局限
*   **硬件需求**：虽然训练加速了，但由于直接在体素空间处理 3D Patch，对显存（GPU Memory）的要求可能依然高于潜在空间模型。
*   **超参数敏感性**：稀疏步数 $K$ 的选择可能需要针对不同模态进行微调（虽然文中建议 $K=5$）。
*   **泛化风险**：实验主要集中在去噪和超分辨率，对于更复杂的图像翻译（如 MR-to-CT）或其他病理情况的泛化能力尚待进一步验证。

（完）
