---
title: "Attend what matters: Leveraging vision foundational models for breast cancer classification using mammograms"
title_zh: 关注重点：利用视觉基础模型进行乳腺 X 线摄影的乳腺癌分类
authors: "Samyak Sanghvi, Piyush Miglani, Sarvesh Shashikumar, Kaustubh R Borgavi, Veenu Singla, Chetan Arora"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19350v1"
tags: ["query:ai-pm"]
score: 10.0
evidence: 用于乳腺癌钼靶影像分类的视觉Transformer
tldr: 针对乳腺癌筛查中视觉Transformer（ViT）因高分辨率图像和细微病变导致注意力分散及分类困难的问题，本文提出了一种结合视觉基础模型的新框架。该框架通过目标检测模型引导RoI区域的Token缩减，利用对比学习增强细粒度判别力，并采用DINOv2预训练模型提取局部感知特征。实验证明，该方法在公开乳腺钼靶数据集上表现优异，具有显著的临床应用潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19350v1/fig-001.webp\", \"caption\": \"Table 1. Performance comparison on the VinDR dataset. ViT-A and ViT-B correspond to DINOv2 [1] with a linear layer at input resolutions of 448×448 and 1024×1024 respectively. ViT-C demonstrates scores using a DeIT head instead of a linear layer at 448×448 input resolution.\", \"page\": 3, \"index\": 1, \"width\": 508, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2604.19350v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview. In our proposed method, we first use G-DINO in a zero-shot setting to localize semantically important Regions of Interest (RoIs) based on a clinically meaningful text prompt. The top-k RoIs, selected by bounding box confidence scores, are cropped and passed through a pretrained DINOv2 encoder to extract k feature vectors. These features are processed by a transformer module, enabling interaction among regions through self-attention. The global RoI (covering the full breast) is used as the final representation which is contextually enriched by fine-grained lesion-level RoIs, and is fed into a Multi-Layer Perceptron (MLP) for classification.\", \"page\": 3, \"index\": 2, \"width\": 1016, \"height\": 391}]"
motivation: 解决ViT在处理高分辨率乳腺钼靶图像时因Token过多导致注意力难以聚焦，以及医学图像细粒度分类特征不明显的问题。
method: 提出一种集成RoI引导的Token缩减、基于难负样本的对比学习以及DINOv2预训练ViT的分类框架。
result: 在多个公开乳腺钼靶数据集上的实验结果表明，该方法在分类性能上显著超越了现有的基准模型。
conclusion: 通过结合基础模型与针对性的注意力优化策略，可以有效提升乳腺癌自动诊断的准确性与临床实用性。
---

## 摘要
视觉 Transformer (\texttt{ViT}) 已成为许多计算机视觉任务的首选架构，但其在计算机辅助诊断中的性能仍然有限。针对乳腺 X 线摄影中的乳腺癌检测，我们确定了导致这一不足的两个主要原因。首先，医学图像具有高分辨率且病变细微，导致 token 数量过多，使得基于 softmax 的注意力机制难以定位并关注相关区域。其次，医学图像分类本质上是细粒度的，具有低类间差异和高类内差异，标准的交叉熵训练难以满足需求。为了克服这些挑战，我们提出了一个包含三个关键组件的框架：(1) 基于感兴趣区域 (\texttt{RoI}) 的 token 削减，利用目标检测模型引导注意力；(2) 在选定的 \texttt{RoI} 之间进行对比学习，通过基于难负样本 (hard-negative) 的训练增强细粒度判别能力；(3) 使用 \texttt{DINOv2} 预训练的 \texttt{ViT}，以捕获感知定位的细粒度特征，而非全局 \texttt{CLIP} 表示。在公开乳腺 X 线摄影数据集上的实验表明，我们的方法优于现有的基准模型，证明了其在大规模乳腺癌筛查中的有效性和潜在临床应用价值。我们的代码可在以下地址获取以供复现：https://aih-iitd.github.io/publications/attend-what-matters

## Abstract
Vision Transformers $(\texttt{ViT})$ have become the architecture of choice for many computer vision tasks, yet their performance in computer-aided diagnostics remains limited. Focusing on breast cancer detection from mammograms, we identify two main causes for this shortfall. First, medical images are high-resolution with small abnormalities, leading to an excessive number of tokens and making it difficult for the softmax-based attention to localize and attend to relevant regions. Second, medical image classification is inherently fine-grained, with low inter-class and high intra-class variability, where standard cross-entropy training is insufficient. To overcome these challenges, we propose a framework with three key components: (1) Region of interest $(\texttt{RoI})$ based token reduction using an object detection model to guide attention; (2) contrastive learning between selected $\texttt{RoI}$ to enhance fine-grained discrimination through hard-negative based training; and (3) a $\texttt{DINOv2}$ pretrained $\texttt{ViT}$ that captures localization-aware, fine-grained features instead of global $\texttt{CLIP}$ representations. Experiments on public mammography datasets demonstrate that our method achieves superior performance over existing baselines, establishing its effectiveness and potential clinical utility for large-scale breast cancer screening. Our code is available for reproducibility here: https://aih-iitd.github.io/publications/attend-what-matters

---

## 论文详细总结（自动生成）

这是一份关于论文《Attend what matters: Leveraging vision foundational models for breast cancer classification using mammograms》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
乳腺癌是全球女性死亡率极高的恶性肿瘤，乳腺 X 线摄影（钼靶）是早期筛查的金标准。尽管视觉 Transformer (ViT) 在通用视觉领域表现优异，但在乳腺癌诊断中面临两大挑战：
*   **高分辨率与微小病灶的矛盾**：钼靶图像分辨率极高（如 4K），而癌变区域仅占约 0.1%。直接输入会导致 Token 数量爆炸，使 Softmax 注意力机制被大量背景噪声稀释，难以聚焦于关键病灶。
*   **细粒度分类难题**：医学影像具有极高的类内差异和极低的类间差异，传统的交叉熵损失函数难以捕捉微小的判别性特征。

### 2. 方法论：核心思想与关键技术
本文提出了一种结合视觉基础模型（Vision Foundational Models）的新型分类框架，核心流程如下：
*   **RoI 引导的 Token 削减**：使用 **Grounding DINO (G-DINO)** 目标检测模型，根据临床文本提示（如“肿块”、“钙化”）在全分辨率图像中识别出前 $k$ 个感兴趣区域 (RoI)。这有效过滤了背景，显著减少了后续 Transformer 处理的 Token 数量。
*   **特征提取与上下文建模**：采用 **DINOv2** 预训练的 ViT 作为编码器。相比于侧重全局语义的 CLIP，DINOv2 在预训练中包含多种定位任务，更擅长提取局部细粒度特征。
*   **空间关系编码**：利用 **旋转位置嵌入 (RoPE)** 处理非连续、非相邻的 RoI，在注意力机制中隐式注入相对位置信息。
*   **排斥性对比损失 (Repulsive Contrastive Loss, RCL)**：
    *   公式思想：$L_{rep} = \frac{1}{B} \sum \frac{1}{K(K-1)} \sum_{i \neq j} \text{sim}(e_i, e_j)^2$。
    *   核心逻辑：通过惩罚不同 RoI 嵌入之间的相似度，强制模型学习多样化的特征，防止特征坍缩，并利用“通常只有一个 RoI 包含肿瘤”的特性进行难负样本（Hard-negative）训练。
*   **聚合机制**：将覆盖整个乳腺的最大 RoI 作为“锚点”，通过 Transformer 块吸收其他细粒度 RoI 的上下文信息，最后通过 MLP 输出分类得分。

### 3. 实验设计
*   **数据集**：主要在公开的大型乳腺钼靶基准数据集 **VinDR-Mammo** 上进行验证。
*   **基准模型 (Benchmarks)**：
    *   **纯视觉模型**：不同分辨率下的 ViT (A/B/C)、MedVAE、TReg-SwinT、XFMamba。
    *   **图文多模态模型**：MMBCD、Mammo-CLIP（之前的 SOTA）。
*   **对比维度**：AUC、F1 分数以及不同召回率下的性能（R@0.1, R@0.3, R@0.5）。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量及训练时长。
*   文中提到该方法通过 RoI 过滤减少了 Token 数量，从而降低了计算开销，且不需要像 Mammo-CLIP 那样在大型私有数据集上进行昂贵的图文预训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了主实验对比（与 8 种主流模型对比）和详细的消融实验。
*   **消融实验**：系统验证了 DINOv2 编码器、G-DINO RoI 提取、锚点机制、RoPE 位置编码以及 RCL 损失函数对性能的增益。
*   **充分性评价**：实验设计较为客观，涵盖了从纯视觉到多模态的多种前沿基线。通过消融实验清晰地展示了每个组件的贡献（如 RCL 提升了约 1% 的 AUC 和 F1）。但由于仅在 VinDR 一个主要数据集上展示详细结果，跨数据集的泛化性验证略显不足。

### 6. 主要结论与发现
*   **性能超越 SOTA**：该方法在 AUC 上比之前的 SOTA (Mammo-CLIP) 提升了 1%，在 F1 分数上显著提升了 4%。
*   **基础模型的优势**：DINOv2 在处理医学影像的局部定位任务上优于 CLIP。
*   **Token 效率**：通过目标检测预筛选 RoI，可以在不损失关键信息的前提下，解决高分辨率图像带来的计算负担和注意力分散问题。

### 7. 优点与亮点
*   **架构创新**：巧妙地将目标检测（G-DINO）作为 Transformer 的预处理器，解决了医学影像“大海捞针”的注意力聚焦难题。
*   **无需私有数据**：相比于依赖大规模私有图文对训练的模型，该框架仅使用公开数据即可达到更高性能，具有更好的可复现性和民主化潜力。
*   **细粒度优化**：引入排斥性对比损失，针对性地解决了医学影像中难负样本辨析的问题。

### 8. 不足与局限
*   **依赖检测器质量**：如果第一步的 G-DINO 未能准确覆盖病灶区域，后续分类模型将无法修复该错误（存在单点故障风险）。
*   **计算流程复杂**：虽然减少了 Transformer 的 Token，但增加了目标检测和多 RoI 裁剪的预处理步骤，推理流水线较长。
*   **泛化性验证有限**：论文主要集中在 VinDR 数据集，缺乏在不同设备、不同种族人群数据集上的广泛外部验证。

（完）
