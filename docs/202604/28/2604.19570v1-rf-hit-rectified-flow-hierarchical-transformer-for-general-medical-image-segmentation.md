---
title: "RF-HiT: Rectified Flow Hierarchical Transformer for General Medical Image Segmentation"
title_zh: RF-HiT：用于通用医学图像分割的修正流分层 Transformer
authors: "Ahmed Marouane Djouama, Abir Belaala, Abdellah Zakaria Sellam, Salah Eddine Bekhouche, Cosimo Distante, Abdenour Hadid"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19570v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于通用医学图像分割的分层Transformer
tldr: 针对医学图像分割中Transformer和扩散模型计算复杂度高、推理慢的问题，本文提出RF-HiT。该模型结合了修正流（Rectified Flow）与沙漏型Transformer架构，通过多尺度分层编码器进行解剖引导。RF-HiT实现了线性计算复杂度，仅需3步推理即可完成，在保持极低参数量（13.6M）和计算量（10.14 GFLOPs）的同时，在ACDC和BraTS数据集上达到了领先的分割精度，为实时临床应用提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19570v1/fig-001.webp\", \"caption\": \"Fig. 2. Visualization of RF-HiT segmentation results on the ACDC dataset. Error maps highlight false positives (FP), false negatives (FN), and misclassifications (Mc).\", \"page\": 5, \"index\": 1, \"width\": 529, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2604.19570v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed RF-HiT: a rectified-flow segmentation framework composed of main flow model that follows an encoder-decoder structure, where neighborhood attention is applied at high-resolution levels, and global attention at the bottleneck. a Hierarchical Feature Encoder provides multi-scale features that are fused into corresponding main model levels via learnable linear interpolation (lerp) to guide velocity prediction.\", \"page\": 3, \"index\": 2, \"width\": 1076, \"height\": 504}]"
motivation: 现有的Transformer和扩散模型在处理医学图像分割时面临计算复杂度高和推理延迟大的瓶颈。
method: 提出一种结合修正流与沙漏型Transformer的高效架构，利用线性复杂度模块和多尺度分层编码器进行特征融合。
result: "在ACDC和BraTS 2021数据集上分别达到91.27%和87.40%的Dice分数，且仅需10.14 GFLOPs和3步推理。"
conclusion: RF-HiT在性能与效率之间取得了极佳平衡，是具有临床实时应用潜力的稳健医学图像分割基础模型。
---

## 摘要
精确的医学图像分割需要长程上下文推理和精确的边界描绘，但在这一任务中，现有的基于 Transformer 和扩散模型的范式经常受到二次计算复杂度和极高推理延迟的瓶颈限制。我们提出了 RF-HiT，一种修正流分层 Transformer (Rectified Flow Hierarchical Transformer)，它将沙漏型 Transformer 骨干网络与多尺度分层编码器相结合，用于解剖引导的特征调节。与之前的基于扩散的方法不同，RF-HiT 利用带有高效 Transformer 模块的修正流来实现线性复杂度，同时仅需少量的离散化步骤。该模型进一步通过可学习的插值融合跨分辨率的调节特征，从而以极小的计算开销实现有效的多尺度表示。结果表明，RF-HiT 实现了强大的效率与性能权衡，仅需 10.14 GFLOPs、13.6M 参数，且推理最少仅需三步。尽管设计紧凑，RF-HiT 在 ACDC 数据集上达到了 91.27% 的平均 Dice 系数，在 BraTS 2021 上达到了 87.40%，其性能与计算量大得多的架构相当甚至更优。这证明了其作为实时临床分割中稳健且计算高效的基础模型的巨大潜力。

## Abstract
Accurate medical image segmentation requires both long-range contextual reasoning and precise boundary delineation, a task where existing transformer- and diffusion-based paradigms are frequently bottlenecked by quadratic computational complexity and prohibitive inference latency. We propose RF-HiT, a Rectified Flow Hierarchical Transformer that integrates an hourglass transformer backbone with a multi-scale hierarchical encoder for anatomically guided feature conditioning. Unlike prior diffusion-based approaches, RF-HiT leverages rectified flow with efficient transformer blocks to achieve linear complexity while requiring only a few discretization steps. The model further fuses conditioning features across resolutions via learnable interpolation, enabling effective multi-scale representation with minimal computational overhead. As a result, RF-HiT achieves a strong efficiency-performance trade-off, requiring only 10.14 GFLOPs, 13.6M parameters, and inference in as few as three steps. Despite its compact design, RF-HiT attains 91.27% mean Dice on ACDC and 87.40% on BraTS 2021, achieving performance comparable to or exceeding that of significantly more intensive architectures. This demonstrates its strong potential as a robust, computationally efficient foundation for real-time clinical segmentation.

---

## 论文详细总结（自动生成）

以下是对论文《RF-HiT: Rectified Flow Hierarchical Transformer for General Medical Image Segmentation》的结构化深入总结：

### 1. 研究动机与核心问题
*   **核心问题**：医学图像分割需要同时具备**长程上下文建模能力**（理解全局解剖结构）和**精确的局部边界定位能力**。
*   **研究背景**：
    *   **CNN**：受限于感受野，难以捕捉长程依赖。
    *   **Transformer**：虽然擅长全局建模，但标准自注意力的计算复杂度随分辨率呈二次方增长（$O(n^2)$），处理高分辨率医学图像时开销巨大。
    *   **扩散模型 (Diffusion Models)**：虽然生成质量高且能提供不确定性估计，但推理时需要数百步迭代，速度极慢，难以满足临床实时需求。
*   **整体含义**：本文旨在开发一种既能保持 Transformer 全局建模优势，又能实现线性计算复杂度，且具备生成模型特性但推理极快的高效分割框架。

### 2. 方法论：RF-HiT 核心架构
RF-HiT 结合了**修正流 (Rectified Flow)** 生成范式与**沙漏型分层 Transformer (HDiT)** 骨干网络。
*   **修正流 (Rectified Flow)**：
    *   核心思想是学习一个常微分方程 (ODE)，将噪声分布平滑地传输到数据分布（分割掩码）。
    *   不同于扩散模型的随机路径，修正流学习的是**直线轨迹**（$X_t = tX_1 + (1-t)X_0$），这使得模型可以用极少的离散步数（如 1-3 步）实现高质量采样。
*   **双路径设计**：
    *   **主流量模型 (HFM)**：采用沙漏型结构。在高分辨率层使用**邻域注意力 (Neighborhood Attention)** 以降低复杂度，在低分辨率瓶颈层使用**全局自注意力**以捕捉上下文。
    *   **分层特征编码器 (HFE)**：独立于主模型，专门从原始图像中提取多尺度解剖特征。
*   **关键技术细节**：
    *   **可学习线性插值 (Lerp)**：通过可学习的标量参数 $\alpha$，将 HFE 提取的解剖特征与 HFM 的对应层特征进行融合，公式为 $\tilde{F} = (1-\alpha)F + \alpha C$。这种方式比简单的加法更具自适应性。
    *   **线性复杂度**：整体架构实现了对像素数 $n$ 的 $O(n)$ 线性空间复杂度。

### 3. 实验设计与 Benchmark
*   **数据集**：
    *   **ACDC**：心脏磁共振图像，涉及左心室、右心室和心肌三个类别。
    *   **BraTS 2021**：脑胶质瘤多模态 MRI 图像，涉及增强肿瘤 (ET)、肿瘤核心 (TC) 和全肿瘤 (WT)。
*   **对比方法**：
    *   **确定性模型**：TransUNet, SwinUnet, UNETR, nnFormer, MISSFormer 等。
    *   **生成式/扩散模型**：MedSegDiff-V2, DiffBTS, SegTransVAE 等。
*   **评价指标**：Dice 系数 (DSC) 和 95% 豪斯多夫距离 (HD95)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA TITAN V GPU**。
*   **训练参数**：ACDC 训练 1000 轮，BraTS 2021 训练 300 轮；Batch size 为 32；使用 AdamW 优化器。
*   **模型规模**：极其轻量化，仅 **13.6M 参数**，计算量为 **10.14 GFLOPs**。
*   **推理效率**：仅需 **3 步** Euler 求解器迭代即可完成推理。

### 5. 实验数量与充分性
*   **实验规模**：在两个具有代表性的不同解剖部位（心脏和大脑）数据集上进行了验证。
*   **消融实验**：
    1.  验证了 **HFE（分层特征编码器）** 的必要性：加入后 ACDC 的 Dice 从 90.69% 提升至 91.27%。
    2.  验证了 **融合机制**：证明了“可学习线性插值”优于简单的“特征加法”。
*   **充分性评价**：实验设计较为客观，对比了从经典 CNN 到最新 Transformer 及扩散模型的多种 SOTA 方法。虽然实验数据集数量（2个）不算极多，但涵盖了多类别和多模态场景，具有较强的说服力。

### 6. 主要结论与发现
*   **效率与性能的平衡**：RF-HiT 在参数量仅为 nnFormer 的 1/10、计算量远低于 MedSegDiff-V2 的情况下，达到了与之相当甚至更优的分割精度。
*   **修正流的优越性**：证明了修正流在医学图像任务中可以替代复杂的扩散模型，在极短的推理时间内（3步）保持生成模型的鲁棒性。
*   **解剖引导的重要性**：通过 HFE 显式注入多尺度图像特征，显著提升了边界定位的准确性。

### 7. 优点与亮点
*   **极高效率**：13.6M 参数和 10.14 GFLOPs 的配置使其非常适合部署在资源受限的临床实时环境中。
*   **架构创新**：巧妙地将修正流的直线传输特性与沙漏型 Transformer 的线性复杂度结合，解决了生成式分割模型“慢”的痛点。
*   **即插即用**：分层特征融合机制为如何将原始图像先验注入生成过程提供了一个高效的范式。

### 8. 不足与局限
*   **边界模糊风险**：可视化结果显示，在组织对比度极低或结构极其复杂的情况下，模型在解剖边界处仍存在轻微的误分类或过度分割。
*   **维度限制**：目前主要基于 2D 切片处理（虽然通过堆叠实现了 3D 评估），未充分利用 3D 空间连续性，相比纯 3D 架构（如 Swin UNETR）在某些复杂 3D 结构上可能稍逊一筹。
*   **超参数敏感性**：推理步数（虽然 3 步已足够好）和阈值选择可能需要针对不同数据集进行微调。

（完）
