---
title: "Neural Network Optimization Reimagined: Decoupled Techniques for Scratch and Fine-Tuning"
title_zh: 神经网络优化重构：针对从零训练与微调的解耦技术
authors: "Xin Ning, Qiankun Li, Xiaolong Huang, Qiupu Chen, Feng He, Weijun Li, Prayag Tiwari, Xinwang Liu"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.22838v1"
tags: ["query:dl"]
score: 9.0
evidence: 神经网络训练与微调的优化技术
tldr: 针对从零训练和微调两种不同的神经网络优化范式，本文提出了DualOpt优化方法。对于从零训练，引入实时逐层权重衰减以提升收敛与泛化性；对于微调，通过集成权重回退机制，确保下游模型与预训练模型的权重分布一致，有效缓解知识遗忘。实验证明该方法在图像分类、目标检测等多种视觉任务中达到了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22838v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the proposed DualOpt. The optimization is both finetuning with pre-trained weights and training from scratch with random initialization. The DualOpt integrates a weight rollback mechanism for finetuning and layer-wise weight decay for training from scratch, providing flexibility and efficiency for various tasks.\", \"page\": 1, \"index\": 1, \"width\": 475, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2604.22838v1/fig-002.webp\", \"caption\": \"Fig. 6. The t-SNE visualization of the feature distributions on the PACS test set using ViT-B and Adam. The extracted features are color-coded by class, and the accuracy is reported for each method. DualOpt achieves the best feature separation with compact intra-class distances and maximized inter-class distances, resulting in the highest accuracy, while other methods exhibit more overlap between feature clusters and lower accuracy.\", \"page\": 11, \"index\": 2, \"width\": 1076, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2604.22838v1/fig-003.webp\", \"caption\": \"TABLE II COMPARISON OF TRAINING FROM SCRATCH RESULTS ON VARIOUS TYPES OF CLASSIFICATION DATASETS (GENERAL, FINE-GRAINED, LONG-TAILED, CROSS-DOMAIN).\", \"page\": 7, \"index\": 3, \"width\": 1016, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.22838v1/fig-004.webp\", \"caption\": \"Fig. 3. Comparison of epochs and accuracy improvement between standard and DualOpt-enhanced optimizers across different datasets. The left panel shows results for Adam versus Adam-DualOpt, while the right panel displays comparisons for SGD versus SGD-DualOpt. These results highlight DualOpt’s ability to significantly reduce training epochs while improving model accuracy.\", \"page\": 10, \"index\": 4, \"width\": 1076, \"height\": 463}, {\"url\": \"assets/figures/arxiv/2604.22838v1/fig-005.webp\", \"caption\": \"TABLE III COMPARISON OF FINE-TUNING RESULTS ON VARIOUS TYPES OF CLASSIFICATION DATASETS (GENERAL, FINE-GRAINED, LONG-TAILED, CROSS-DOMAIN).\", \"page\": 8, \"index\": 5, \"width\": 1040, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.22838v1/fig-006.webp\", \"caption\": \"Fig. 2. Overview of the proposed DualOpt optimization framework. The framework addresses training from scratch with real-time layer-wise weight decay (top pathway), and fine-tuning with weight rollback and layer-wise penalty (bottom pathway). The weight rollback mechanism ensures alignment with pre-trained weights, while the layer-wise penalty adapts the learning intensity across layers based on depth, reducing knowledge forgetting. In addition, popular optimizers such as SGD and Adam are incorporated into DualOpt.\", \"page\": 4, \"index\": 6, \"width\": 968, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.22838v1/fig-007.webp\", \"caption\": \"TABLE VII COMPARISON OF METHOD EFFICIENCY ON BASE AND LARGE BACKBONES.\", \"page\": 9, \"index\": 7, \"width\": 870, \"height\": 202}]"
motivation: 现有优化器未能针对从零训练和微调这两种具有显著差异的训练场景进行差异化设计，难以满足不同任务的独特需求。
method: 提出了DualOpt框架，包含用于从零训练的实时逐层权重衰减技术，以及用于微调的权重回退机制，并支持跨层的动态回退调整。
result: 在图像分类、目标检测、语义分割和实例分割等多个视觉任务的广泛实验中，DualOpt均取得了最先进的性能表现。
conclusion: 通过解耦不同训练场景的优化策略，DualOpt能有效提升模型的泛化能力并解决微调过程中的知识遗忘问题，具有广泛的适用性。
---

## 摘要
随着大数据时代资源的积累和深度学习中预训练模型的兴起，针对各种任务优化神经网络通常涉及微调预训练模型与从零训练（training from scratch）的不同策略。然而，现有的优化器主要侧重于通过更新模型参数来降低损失函数，未能充分解决这两大范式的独特需求。在本文中，我们提出了 DualOpt，这是一种专门为这些不同训练场景量身定制的解耦优化技术新方法。对于从零训练，我们引入了实时逐层权重衰减，旨在通过契合权重更新和网络架构的特性来增强收敛性和泛化能力。更重要的是，针对微调，我们将权重回滚（weight rollback）集成到优化器中，在每个权重更新步骤中加入回滚项。这确保了上游和下游模型之间权重分布的一致性，有效缓解了知识遗忘并提升了微调性能。此外，我们将逐层权重衰减扩展到动态调整各层的回滚程度，以适应不同下游任务的多样化需求。在图像分类、目标检测、语义分割和实例分割等多种任务上的广泛实验证明了 DualOpt 的广泛适用性和最先进的性能。代码开源于 https://github.com/qklee-lz/OLOR-AAAI-2024。

## Abstract
With the accumulation of resources in the era of big data and the rise of pre-trained models in deep learning, optimizing neural networks for various tasks often involves different strategies for fine-tuning pre-trained models versus training from scratch. However, existing optimizers primarily focus on reducing the loss function by updating model parameters, without fully addressing the unique demands of these two major paradigms. In this paper, we propose DualOpt, a novel approach that decouples optimization techniques specifically tailored for these distinct training scenarios. For training from scratch, we introduce real-time layer-wise weight decay, designed to enhance both convergence and generalization by aligning with the characteristics of weight updates and network architecture. For more importantly fine-tuning, we integrate weight rollback with the optimizer, incorporating a rollback term into each weight update step. This ensures consistency in the weight distribution between upstream and downstream models, effectively mitigating knowledge forgetting and improving fine-tuning performance. Additionally, we extend the layer-wise weight decay to dynamically adjust the rollback levels across layers, adapting to the varying demands of different downstream tasks. Extensive experiments across diverse tasks, including image classification, object detection, semantic segmentation, and instance segmentation, demonstrate the broad applicability and state-of-the-art performance of DualOpt. Code is available at https://github.com/qklee-lz/OLOR-AAAI-2024.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DualOpt** 的新型优化框架，旨在解决神经网络在“从零训练（From Scratch）”和“微调（Fine-Tuning）”两种范式下对优化算法的不同需求。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的优化器（如 SGD, Adam）通常采用统一的参数更新策略，忽略了不同训练场景的独特性。
    *   **从零训练**：需要平衡收敛速度与泛化能力，防止深层网络过拟合。
    *   **微调**：面临“灾难性遗忘”问题，即在学习下游任务时容易丢失预训练模型中的通用知识。
*   **研究动机**：作者认为优化器应当根据训练阶段（是否有预训练权重）进行“解耦”设计，通过差异化的正则化手段来提升模型在特定场景下的表现。

### 2. 方法论：核心思想与关键技术
DualOpt 框架包含两个核心组件，分别针对两种场景：

*   **针对从零训练：实时逐层权重衰减（Real-Time Layer-Wise Weight Decay）**
    *   **核心思想**：修正了传统权重衰减在更新后可能导致权重反而增大的数学缺陷，改为“实时”衰减。
    *   **技术细节**：引入逐层衰减率 $\lambda_i = \lambda \cdot \frac{i}{n}$，其中 $i$ 是层索引，$n$ 是总层数。这意味着浅层（提取基础特征）衰减较小，深层（提取语义特征，易过拟合）衰减较大。
*   **针对微调：权重回滚机制（Weight Rollback Mechanism）**
    *   **核心思想**：在每次更新时加入一个“回滚项”，使当前权重向预训练初始权重 $\theta_0$ 靠拢。
    *   **技术细节**：
        *   更新公式形如：$\theta_{t+1} = (1-\lambda)(\theta_t - \eta g_t) + \lambda \theta_0$。当 $\lambda$ 趋近 1 时，模型强制保持预训练状态。
        *   **逐层惩罚衰减**：浅层（通用特征）应用更高的回滚强度，深层（任务相关特征）回滚强度较低，允许更多更新。
        *   引入幂指数 $\gamma$ 动态调整回滚速率，以适应不同下游任务的领域相似度。

### 3. 实验设计
*   **数据集**：涵盖 10 个主流视觉数据集，包括：
    *   **分类**：CIFAR-100, SVHN（通用）；CUB-200, Stanford Cars（细粒度）；Places-LT, IP102（长尾）；OfficeHome, PACS（跨域）。
    *   **下游任务**：COCO2017（检测与实例分割）、ADE20K（语义分割）。
    *   **大规模验证**：ImageNet-1K。
*   **Benchmark 与对比方法**：
    *   **基准优化器**：Adam, SGD。
    *   **微调对比方法**：Linear Probing, Full Fine-tuning, L2-SP, VPT（视觉提示微调）, LoRA。
*   **Backbones**：ResNet-50, Inception, ViT-B, Swin-B, ConvNeXt-B。

### 4. 资源与算力
*   **硬件设备**：
    *   从零训练实验：单张 NVIDIA GeForce RTX 4090 GPU。
    *   微调实验：两张 NVIDIA A5000 GPU (24GB 显存)。
*   **软件环境**：Python 3.8, PyTorch 2.0.0, Ubuntu 20.04。
*   **训练时长**：分类任务通常为 30-50 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 10 个数据集上的性能对比、大规模 ImageNet 训练、检测/分割任务迁移。
*   **消融实验**：针对逐层权重衰减策略、权重回滚组件、超参数（$\iota_1, \gamma$）的影响进行了独立验证。
*   **客观性**：对比了多种不同架构（CNN vs Transformer）和不同预训练范式（监督学习 vs MAE vs CLIP），证明了方法的通用性。实验设计遵循了标准的学术规范，对比基准涵盖了近年来的 SOTA 方法。

### 6. 主要结论与发现
*   **性能提升**：DualOpt 在所有测试任务中均达到了 SOTA 性能。例如在 PACS 跨域任务上，比 Full Fine-tuning 提升了约 6.59%。
*   **缓解遗忘**：知识遗忘测试证明，DualOpt 在提升下游任务准确率的同时，能显著保留模型在上游任务上的性能。
*   **收敛加速**：在从零训练场景下，DualOpt 不仅提升了最终精度，还显著减少了达到收敛所需的 epoch 数量。
*   **特征质量**：t-SNE 可视化显示 DualOpt 学习到的特征聚类更加紧凑，类间距离更大。

### 7. 优点（亮点）
*   **解耦视角**：首次系统性地从优化器底层逻辑出发，将从零训练和微调的正则化需求进行解耦。
*   **数学严谨性**：对实时权重衰减的必要性给出了理论证明，解决了传统方法可能导致的权重异常增长问题。
*   **极低开销**：相比 LoRA 或 VPT 等增加额外参数的方法，DualOpt 仅在优化器更新步骤增加极少量计算，推理阶段零额外开销。
*   **高通用性**：可以无缝集成到现有的 Adam 或 SGD 优化器中，支持各种主流网络架构。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文提供了指导建议，但权重回滚强度 $\iota_1$ 和幂指数 $\gamma$ 仍需根据下游任务与预训练任务的相似度进行手动微调。
*   **领域覆盖**：实验主要集中在计算机视觉（CV）领域，尚未在自然语言处理（NLP）的大语言模型（LLM）指令微调中进行大规模验证。
*   **理论深度**：虽然有数学证明，但对于“逐层衰减”的最优函数形式（目前是线性或幂函数）缺乏更深层的理论推导。

（完）
