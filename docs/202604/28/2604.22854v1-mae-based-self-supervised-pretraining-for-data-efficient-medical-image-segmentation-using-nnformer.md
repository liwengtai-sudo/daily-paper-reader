---
title: MAE-Based Self-Supervised Pretraining for Data-Efficient Medical Image Segmentation Using nnFormer
title_zh: 基于 MAE 的自监督预训练在利用 nnFormer 进行高效数据医学图像分割中的应用
authors: "R. M. Krishna Sureddi, T. Satyanarayana Murthy, Nomula Varsha Reddy, Adi Kanishka, Nalla Manvika Reddy"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.22854v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于医学图像分割的 Transformer 架构和自监督预训练
tldr: 针对医学图像分割中Transformer模型（如nnFormer）对大量标注数据依赖性强且易过拟合的问题，本文提出了一种基于掩码自编码器（MAE）的自监督预训练框架。该方法利用大量无标注医学影像进行预训练，通过重建被掩码的图像部分使编码器学习解剖结构特征，随后在少量标注数据上进行微调。实验证明，该方法显著提升了分割精度、收敛速度及在数据稀缺场景下的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22854v1/fig-001.webp\", \"caption\": \"Fig. 2. Architecture of Complete Pipeline\", \"page\": 2, \"index\": 1, \"width\": 396, \"height\": 317}]"
motivation: 旨在解决Transformer架构在医学图像分割中因高质量标注数据匮乏而导致的过拟合及训练不稳定问题。
method: 提出一种基于MAE的自监督预训练框架，通过在无标注数据上重建掩码图像块来预训练nnFormer编码器，随后在下游任务中进行微调。
result: 实验表明，该方法在Dice分数、收敛速度以及有限标注数据下的泛化性能方面均优于传统的全监督训练方案。
conclusion: 自监督学习与Transformer模型的结合是克服医学影像分析中数据短缺挑战并提升模型性能的有效途径。
---

## 摘要
包括 nnFormer 在内的 Transformer 架构通过捕获长程空间交互，在体积医学图像分割中展示了良好的结果。尽管这些模型性能优异，但它们需要大量的标注训练数据，且容易出现过拟合和训练不稳定的问题。这是一个严重的实际问题，因为获取由专家标注的医学图像不仅耗时而且昂贵。此外，全监督的传统训练流程未能利用临床中易于获取的大量未标注医学影像数据。我们通过引入基于掩码自编码器（MAE）的自监督预训练框架，提升了 nnFormer 的效率，从而解决了这些缺陷。在该方法中，模型在未标注的体积医学图像上进行预训练，以重建输入中随机掩码的部分。这使得编码器能够学习到有意义的解剖和结构表征。随后，编码器在下游分割任务的有标注数据集上进行进一步微调。实验结果表明，所提方法在 Dice 分数上获得了更高的分割性能，在微调过程中具有更快的收敛速度，并在有限标注数据的基础上表现出卓越的泛化能力。这些发现证实，自监督学习结合基于 Transformer 的分割模型是解决医学图像分析中数据短缺问题的有效方法。

## Abstract
Transformer architectures, including nnFormer,have demonstrated promising results in volumetric medical image segmentation by being able to capture long-range spatial interactions. Although they have high performance, these models need large quantities of labeled training data and are also likely to overfit and become training unstable. This is a serious practical problem because it is not only time-consuming but also expensive to obtain medical images that are annotated by experts. Moreover, fully supervised traditional training pipelines do not take advantage of the available large amounts of unlabeled medical imaging data that can be easily obtained in the clinics. We have solved these drawbacks by advancing the efficiency of the nnFormer with a self-supervised pretraining framework, which is based on the Masked Autoencoders (MAE). In this method, the model is pretrained on unlabeled volumetric medical images to reconstruct randomly masked parts of the input. This allows the encoder to learn meaningful anatomical and structural representations . The encoder is then further fine-tuned on a labeled dataset on the downstream segmentation task. Conducted Experiment shows that the offered method leads to a higher segmentation performance on the count of Dice score, a quicker convergence rate on the course of the fine-tuning procedure, and a superior generalization on the basis of limited labeled data . These findings validate that self-supervised learning combined with transformer-based segmentation models is an appropriate approach to the problem of data shortage in medical image analysis.

---

## 论文详细总结（自动生成）

这是一份关于论文《MAE-Based Self-Supervised Pretraining for Data-Efficient Medical Image Segmentation Using nnFormer》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：Transformer 架构（如 nnFormer）虽然在捕获医学图像的长程空间交互方面表现优异，但面临两大挑战：一是**高度依赖大规模专家标注数据**，而医学影像标注成本极高；二是**训练不稳定且易过拟合**，尤其是在小样本场景下。
*   **研究背景**：传统的全监督学习忽略了临床中大量易于获取的**无标注数据**。本文旨在通过自监督学习（SSL）挖掘无标注数据的价值，提升模型在有限标注数据下的分割精度和泛化能力。

### 2. 论文提出的方法论
该方法提出了一个两阶段的训练框架，将掩码自编码器（MAE）与 nnFormer 结合：
*   **第一阶段：MAE 自监督预训练**
    *   **核心思想**：通过“完形填空”的方式学习图像的内在结构。
    *   **技术细节**：将 3D 体积医学图像划分为非重叠的 3D 图像块（Patches），随机掩码掉大部分图像块。
    *   **流程**：仅将未掩码的块输入编码器，训练网络重建（预测）被掩码的部分。这迫使编码器在没有标签的情况下学习解剖结构、空间关系和纹理特征。
*   **第二阶段：有监督微调**
    *   **流程**：将预训练好的编码器权重迁移至 nnFormer 架构中，结合解码器在有标注的数据集上进行针对分割任务的微调。
    *   **作用**：预训练权重提供了优异的参数初始化，使模型能更快收敛并锁定关键解剖特征。

### 3. 实验设计
*   **数据集/场景**：主要针对**脑肿瘤分割（Brain Tumor Segmentation）**任务。
*   **Benchmark（基准）**：以原始的、全监督训练的 **nnFormer (Baseline)** 为核心基准。
*   **对比方法**：
    *   经典的 Transformer 变体：**TransUNet**、**Swin-UNETR**、**UNETR**。
    *   消融对比：对比了“从零训练”与“MAE 预训练后微调”的效果。
*   **评价指标**：Dice 相似系数（Dice Similarity Coefficient, DSC）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、显存大小、数量）以及具体的训练时长。这在一定程度上限制了对该方法计算效率的全面评估。

### 5. 实验数量与充分性
*   **实验规模**：论文对比了 5 种不同的模型架构，并提供了 Dice 分数的量化对比表。
*   **充分性评估**：
    *   **正面**：实验包含了在“有限标注数据”下的鲁棒性测试，验证了模型在数据稀缺时的泛化性能。
    *   **不足**：作为一篇 4 页的短论文，实验细节相对简略。缺乏针对不同掩码率（Masking Ratio）的消融实验，也未展示在多个不同器官数据集（如腹部 CT、心脏 MRI 等）上的通用性验证。

### 6. 主要结论与发现
*   **性能提升**：提出的 MAE + nnFormer 方法达到了 **88.7%** 的平均 Dice 分数，优于基准 nnFormer（86.4%）以及 Swin-UNETR（85.1%）等模型。
*   **收敛加速**：预训练编码器提供了更好的初始化点，使得模型在微调阶段比从零训练的模型收敛更快、更稳定。
*   **数据效率**：在减少标注数据量的情况下，该方法表现出更强的鲁棒性，证明了自监督预训练能有效缓解数据短缺问题。

### 7. 优点（亮点）
*   **架构解耦**：将表征学习与任务特定优化分离，无需修改 nnFormer 的原始分割架构即可实现性能增强。
*   **实用性强**：充分利用了临床中被浪费的无标注数据，具有很高的实际应用价值。
*   **即插即用**：这种基于 MAE 的预训练策略可以推广到其他 3D Transformer 架构中。

### 8. 不足与局限
*   **实验覆盖面有限**：仅在脑肿瘤分割上进行了验证，缺乏多中心、多模态数据的广泛测试。
*   **技术细节缺失**：未详细说明 3D 掩码的具体比例、Patch 大小等关键超参数设置。
*   **对比维度单一**：主要关注 Dice 分数，缺乏对推理延迟、内存占用等工业部署指标的分析。
*   **创新深度**：将 MAE 应用于 3D 医疗影像并非首创（文中也引用了相关前作），本文更多是针对 nnFormer 这一特定架构的工程优化和验证。

（完）
