---
title: "Learning to Rotate: Temporal and Semantic Rotary Encoding for Sequential Modeling"
title_zh: 学习旋转：用于序列建模的时间与语义旋转编码
authors: "Hailing Cheng, Daqi Sun, Xinyu Lu"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24717v1"
tags: ["query:dl"]
score: 9.0
evidence: Transformer注意力机制中可学习的旋转流形
tldr: 本研究针对Transformer中旋转位置编码（RoPE）结构固定且仅依赖离散索引的局限性，提出将旋转流形视为可学习且受信号调节的表达维度。通过引入SIREN-RoPE架构，利用正弦表示网络将连续时间戳、周期性模式及类别元数据映射至旋转空间。在生产级社交网络推荐任务中的实验表明，该方法在几乎不增加计算开销的情况下，显著提升了模型的排序与校准性能，为注意力机制开辟了新的表达维度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24717v1/fig-001.webp\", \"caption\": \"Table 2: Engagement prediction on the production social feed dataset. NE: lower is better; AUC: higher is better. Best results are bolded; second-best is underlined.\", \"page\": 6, \"index\": 1, \"width\": 690, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2604.24717v1/fig-002.webp\", \"caption\": \"Figure 3: Attention score of a SIREN-RoPE module (weights extracted from the production model) as the key timestamp is swept across four time spans, using a 128-dimensional unit vector for both query and key. Each panel overlaps two consecutive periods (day 1 vs. day 2; week 1 vs. week 2; month 1 vs. month 2; year 1 vs. year 2) to reveal repeating structure.\", \"page\": 11, \"index\": 2, \"width\": 829, \"height\": 594}, {\"url\": \"assets/figures/arxiv/2604.24717v1/fig-003.webp\", \"caption\": \"Table 3: Ablation study on the production social feed dataset across all six metric–task combinations. NE: lower is better; AUC: higher is better. Best per column in bold; second-best underlined. Groups separated by horizontal rules.\", \"page\": 7, \"index\": 3, \"width\": 786, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2604.24717v1/fig-004.webp\", \"caption\": \"Figure 2: Attention score between a query at position 0 and keys at ordinal positions 0–1023, computed with a 512-dimensional unit vector (q = k = 1/ √ 512) under four RoPE bases (104, 105, 106, 107). All bases produce a monotone global decay with small high-frequency oscillations whose period grows with the base.\", \"page\": 10, \"index\": 4, \"width\": 706, \"height\": 367}, {\"url\": \"assets/figures/arxiv/2604.24717v1/fig-005.webp\", \"caption\": \"Figure 5: 2D attention score heatmap as a function of key ordinal position (y-axis, 0–120) and key timestamp (x-axis) for four time spans. Color encodes attention magnitude (brighter = higher). The ordinal axis exhibits monotone decay irrespective of the time span; the temporal axis exhibits clear intra-day and intra-week oscillations in the short-span panels.\", \"page\": 13, \"index\": 5, \"width\": 829, \"height\": 593}, {\"url\": \"assets/figures/arxiv/2604.24717v1/fig-006.webp\", \"caption\": \"Figure 4: Year-long SIREN-RoPE attention in the time domain (top) and its FFT magnitude spectrum (bottom). Vertical dashed lines mark the weekly ( 17 cycles/day) and daily (1 cycle/day) frequencies. Both frequencies exhibit prominent peaks; sub-harmonics at 1 2 , 1 3 , . . . cycles/day are also visible.\", \"page\": 12, \"index\": 6, \"width\": 748, \"height\": 497}, {\"url\": \"assets/figures/arxiv/2604.24717v1/fig-007.webp\", \"caption\": \"Figure 1: Base model architecture (AttnMVP, Cheng [2026]). Item and action embeddings are maintained as separate streams. Item embeddings form the Query/Key projections in every Transformer layer while action embeddings are mixed into the Value stream under strict causal masking. A final action-pooling step aggregates historical action signals conditioned on item-level similarity before the prediction head.\", \"page\": 5, \"index\": 7, \"width\": 336, \"height\": 488}]"
motivation: 现有的RoPE通常采用手工设计的固定结构，忽略了旋转空间作为注意力机制中第二个表达维度的潜力。
method: 提出SIREN-RoPE，利用双分支正弦表示网络将时间戳、周期性信号和元数据等异构信号动态映射到旋转编码中。
result: 在大规模社交网络信息流推荐数据集上的实验证明，该方法在提升排序和校准精度的同时，计算开销几乎可以忽略。
conclusion: 旋转空间不应仅被视为简单的位置编码工具，而是一个能够显著增强注意力机制表达能力的、尚未被充分挖掘的独立维度。
---

## 摘要
每个 Transformer 架构都投入了巨大的容量在语义嵌入空间中学习丰富的表示——然而，旋转位置嵌入（RoPE）所作用的旋转流形一直被视为一种固定的、手工设计的结构，仅由离散的序数索引填充。我们认为，这个旋转空间是注意力机制中一个在很大程度上被忽视的第二表达维度，对其进行系统性探索可能会为基于注意力的架构开启一扇新的大门。与复数的类比具有启发性：正如引入与实轴正交且独立的虚轴解锁了曾经被认为不可能的新代数结构一样，将旋转流形视为一个可学习的、受信号调节的空间，开启了注意力机制中一个正交的自由度。在这种框架下，Token 嵌入编码了表示的语义（实部）成分——即 Token 的含义——而旋转则编码了其动态（虚部）成分——即它在时间、位置和上下文维度上如何与每一个其他 Token 相关联。我们提出了 SIREN-RoPE，这是该思想的一个具体实例化，它通过双分支正弦表示网络（SIREN）将异构信号（连续时间戳、周期性时间模式和类别型元数据）填充到旋转维度中。作为概念验证，我们在来自大型社交网络的生产级新闻推送数据集上，使用生成式推荐系统作为排序模型进行了评估，结果表明激活这一隐藏维度在校准和排序目标上均带来了持续的提升，且计算开销微乎其微。我们邀请社区不要将旋转空间仅仅视为一个已解决的位置编码细节，而是一个尚未开发的轴，其丰富的结构对注意力机制的重要性可能就像虚数单位对代数的重要性一样。

## Abstract
Every Transformer architecture dedicates enormous capacity to learning rich representations in semantic embedding space -- yet the rotation manifold acted upon by Rotary Positional Embeddings (RoPE) has been treated as a fixed, hand-crafted structure, populated only by discrete ordinal indices. We argue that this rotation space is a largely overlooked second dimension of expressivity in the attention mechanism, one whose systematic exploration may open a new door for attention-based architectures. The analogy to complex numbers is instructive: just as introducing the imaginary axis -- orthogonal to and independent of the real line -- unlocked new algebraic structure once believed impossible, treating the rotation manifold as a learnable, signal-conditioned space opens an orthogonal degree of freedom in attention. In this framing, the token embedding encodes the semantic (real) component of a representation -- what a token means -- while the rotation encodes its dynamic (imaginary) component -- how it relates to every other token across time, position, and context.   We introduce SIREN-RoPE, a concrete instantiation of this idea, which populates the rotation dimension with heterogeneous signals -- continuous timestamps, cyclical temporal patterns, and categorical metadata -- via a dual-branch Sinusoidal Representation Network (SIREN). As a proof of concept, we evaluate on a production-scale news feed dataset from a major social network using a generative recommender as the ranking model, demonstrating that activating this hidden dimension yields consistent improvements across calibration and ranking objectives with negligible computational overhead. We invite the community to view the rotation space not as a solved positional-encoding detail, but as an untapped axis whose rich structure may prove as consequential for attention as the imaginary unit proved for algebra.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Rotate: Temporal and Semantic Rotary Encoding for Sequential Modeling》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文指出，当前的 Transformer 架构在语义嵌入空间（Embedding Space）上投入了巨大容量，但对于**旋转位置编码（RoPE）**所作用的旋转流形，却一直将其视为一种固定的、由手工设计的离散序数索引填充的结构。

*   **研究动机**：作者认为旋转空间是注意力机制中被忽视的“第二表达维度”。
*   **类比复数**：正如虚轴的引入解锁了代数新结构，将旋转流形视为可学习、受信号调节的空间，能为注意力机制开启正交的自由度。
*   **语义 vs. 动态**：Token 嵌入编码“语义（实部）”——即 Token 是什么；旋转编码“动态（虚部）”——即 Token 在时间、位置和上下文中与其他 Token 的关系。在推荐系统中，时间间隔（如 7 分钟 vs 7 天）比单纯的序数偏移更重要。

### 2. 方法论：核心思想与技术细节
论文提出了 **SIREN-RoPE**，将旋转流形提升为“一等公民”的可学习空间。

*   **核心公式**：旋转角度 $\Theta_j$ 不再仅由位置 $p_i$ 决定，而是融合了时间与序数：
    $$\Theta_j(T_i, p_i) = f_\phi(T_i)_j \cdot \omega_{sj} + p_i \cdot \theta_j \cdot \lambda$$
    其中：
    *   $f_\phi(T_i)$：**双分支网络**。包含一个 **SIREN 周期分支**（使用 $\sin$ 激活函数捕捉日/周等周期模式）和一个 **DNN 非周期分支**（捕捉衰减趋势）。
    *   $\omega_{sj}$：可学习的每维度频率缩放。
    *   $\lambda$：可学习的标量门控，用于平衡序数位置的重要性。
*   **输入特征**：将原始 Unix 时间戳分解为 5 维向量，包括日周期（$\cos, \sin$）、周周期（$\cos, \sin$）和归一化的长期偏移量。
*   **兼容性**：该方法直接作用于旋转角度，与标准 RoPE 逻辑一致，完全兼容 `torch.compile` 和 FlashAttention。

### 3. 实验设计
*   **数据集与场景**：使用来自某大型社交网络（LinkedIn）的**生产级新闻推送（News Feed）数据集**，包含一整年的用户交互日志。
*   **任务目标**：评估三个参与度指标：互动（Contribution）、点赞（Like）和长停留（LongDwell）。
*   **对比方法（Baselines）**：
    1.  **Ordinal RoPE**：标准生产环境基准，仅使用离散序数。
    2.  **Timestamp-as-Feature**：将时间戳作为普通特征拼接到 Embedding 中，旋转仍使用序数 RoPE。
    3.  **TO-RoPE**：使用固定频率表但将时间戳作为索引的现有方法。
*   **消融实验**：测试了循环特征的必要性、SIREN 分支的作用以及将静态语义信号（如社交图谱属性）放入旋转维度的效果。

### 4. 资源与算力
*   **参数量**：SIREN-RoPE 仅增加了约 **0.2%** 的额外参数（主要来自小型 MLP）。
*   **计算开销**：在训练阶段，相比标准 RoPE 仅增加约 **1.4%** 的墙钟时间；在推理阶段增加约 **1.8%**。
*   **硬件细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长，但强调了其在生产级规模下的“微乎其微”的开销。

### 5. 实验数量与充分性
*   **实验规模**：在包含数亿级交互的生产数据集上进行验证，而非仅限于小型学术数据集。
*   **充分性**：
    *   涵盖了排序（AUC）和校准（NE）两个关键维度。
    *   通过 FFT（快速傅里叶变换）分析证明了模型确实自动学习到了 24 小时和 7 天的周期规律。
    *   **客观性**：通过控制变量法（Table 1）确保所有模型仅在时间戳的处理方式上有所不同，保证了对比的公平性。

### 6. 主要结论与发现
*   **性能提升**：SIREN-RoPE 在所有任务和指标上均优于所有基准模型，尤其在长停留（LongDwell）任务上提升最显著。
*   **旋转维度的独立性**：实验证明，将时间信号放入 Embedding 空间几乎无效，而放入旋转空间则效果显著，证明了旋转维度承载着与语义空间互补的独立信号。
*   **序数退化**：在引入 SIREN-RoPE 后，可学习门控 $\lambda$ 从 1.0 降至 0.044，表明模型发现**精确的时间动态比离散的先后顺序更具表达力**。

### 7. 优点
*   **理论优雅**：通过复数类比，为 RoPE 的改进提供了深刻的代数直觉。
*   **高效性**：以极小的计算代价（<2%）换取了生产环境下的显著增益。
*   **自动化**：SIREN 分支能够自动发现数据中的隐藏周期（如用户在周末的特殊行为），无需人工精细调参。

### 8. 不足与局限
*   **外推限制**：作为一种“内容感知”的编码，其对训练分布之外的时间间隔（OOD）的泛化能力仍有待进一步研究。
*   **应用范围**：目前主要在推荐系统（序列建模）中验证，在自然语言处理（NLP）等其他领域的普适性（如将旋转绑定到语法深度）尚需更多实验支撑。
*   **算力细节缺失**：缺乏具体的硬件环境描述，可能影响其他研究者评估其在不同硬件上的适配性。

（完）
