---
title: Decision-Aware Attention Propagation for Vision Transformer Explainability
title_zh: 视觉 Transformer 可解释性的决策感知注意力传播
authors: "Sehyeong Jo, Gangjae Jang, Haesol Park"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18094v1"
tags: ["query:dl"]
score: 9.0
evidence: 视觉Transformer的可解释性与注意力传播
tldr: 针对Vision Transformer（ViT）解释性不足的问题，本文提出决策感知注意力传播（DAP）方法。现有方法要么缺乏类别区分度，要么忽略了Transformer的层级注意力流。DAP通过将基于梯度的决策先验注入注意力传播过程，结合了结构化信息流与类别相关证据。实验证明，DAP在多种ViT模型上均能生成更精确、更具类别敏感性的归因图，显著提升了模型的可解释性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18094v1/fig-001.webp\", \"caption\": \"Figure 1: Overall pipeline of Decision-Aware Attention Propagation (DAP)\", \"page\": 2, \"index\": 1, \"width\": 821, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2604.18094v1/fig-002.webp\", \"caption\": \"Table 1: Comparison of the proposed method with attention-based XAI approaches across multiple ViT models under both balanced and non-balanced class settings\", \"page\": 7, \"index\": 2, \"width\": 827, \"height\": 529}, {\"url\": \"assets/figures/arxiv/2604.18094v1/fig-003.webp\", \"caption\": \"Table 2: Ablation on decision-prior injection under a balanced setting; variants differ only in how the prior is used in propagation.\", \"page\": 7, \"index\": 3, \"width\": 502, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.18094v1/fig-004.webp\", \"caption\": \"Figure A.1: Additional Layer-wise Attention Maps for Successful and Challenging Cases\", \"page\": 14, \"index\": 4, \"width\": 662, \"height\": 1132}, {\"url\": \"assets/figures/arxiv/2604.18094v1/fig-005.webp\", \"caption\": \"Table 3: Comparison of the proposed method with gradient-based XAI approaches across multiple ViT models under both balanced and non-balanced class settings\", \"page\": 8, \"index\": 5, \"width\": 830, \"height\": 529}, {\"url\": \"assets/figures/arxiv/2604.18094v1/fig-006.webp\", \"caption\": \"Figure 2: Layer-wise Attention Map Comparison Across Methods\", \"page\": 9, \"index\": 6, \"width\": 824, \"height\": 316}]"
motivation: 现有的注意力解释方法缺乏类别区分度，而梯度方法虽具类别相关性却未能充分利用Transformer的层级注意力传播机制。
method: 提出DAP方法，利用梯度定位估计Token重要性，并将其作为决策先验整合进层级注意力传播过程中。
result: 实验表明DAP在不同规模的ViT模型上均优于现有基准，生成的归因图在类别敏感性、紧凑性和忠实度上表现更佳。
conclusion: 将决策感知先验与注意力传播相结合是增强ViT模型可解释性的有效途径。
---

## 摘要
视觉 Transformer (ViT) 已成为计算机视觉领域的主流架构，但由于信息通过跨层和注意力头的复杂交互进行传播，其预测过程仍然难以解释。现有的基于注意力的解释方法提供了一种直观的信息流追踪方式。然而，它们主要依赖原始注意力权重，这不能显式地反映最终决策，且往往导致类别区分度有限的解释。相比之下，基于梯度的定位方法在突出类别特定证据方面更为有效，但它们未能充分利用 Transformer 的层级注意力传播机制。为了解决这一局限性，我们提出了决策感知注意力传播 (DAP)，这是一种将决策相关先验注入 Transformer 注意力传播的归因方法。通过基于梯度的定位来估计 Token 重要性，并将其整合到逐层注意力展开（attention rollout）中，该方法既捕捉了注意力的结构流，也捕捉了与最终预测最相关的证据。因此，与传统的基于注意力的方法相比，DAP 生成的归因图具有更高的类别敏感性、紧凑性和忠实度。在不同模型规模的 Vision Transformer 变体上进行的广泛实验表明，DAP 在定量指标和定性可视化方面均持续优于现有基准，这表明决策感知传播是提高 ViT 可解释性的有效方向。

## Abstract
Vision Transformers (ViTs) have become a dominant architecture in computer vision, yet their prediction process remains difficult to interpret because information is propagated through complex interactions across layers and attention heads. Existing attention based explanation methods provide an intuitive way to trace information flow. However, they rely mainly on raw attention weights, which do not explicitly reflect the final decision and often lead to explanations with limited class discriminability. In contrast, gradient based localization methods are more effective at highlighting class specific evidence, but they do not fully exploit the hierarchical attention propagation mechanism of transformers. To address this limitation, we propose Decision-Aware Attention Propagation (DAP), an attribution method that injects decision-relevant priors into transformer attention propagation. By estimating token importance through gradient based localization and integrating it into layer wise attention rollout, the method captures both the structural flow of attention and the evidence most relevant to the final prediction. Consequently, DAP produces attribution maps that are more class sensitive, compact, and faithful than those generated by conventional attention based methods. Extensive experiments across Vision Transformer variants of different model scales show that DAP consistently outperforms existing baselines in both quantitative metrics and qualitative visualizations, indicating that decision aware propagation is an effective direction for improving ViT interpretability.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **决策感知注意力传播 (Decision-Aware Attention Propagation, DAP)** 的新方法，旨在提升视觉 Transformer (ViT) 的可解释性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：Vision Transformer (ViT) 虽然性能强大，但其决策过程像个“黑盒”。现有的解释方法主要分为两类：
    *   **基于注意力的方法**（如 Attention Rollout）：能很好地追踪模型内部的结构化信息流，但往往缺乏类别区分度，生成的解释图比较模糊，无法区分特定类别的证据。
    *   **基于梯度的方法**（如 Grad-CAM）：能有效定位类别相关的区域，但忽略了 Transformer 逐层注意力传播的层级结构。
*   **核心问题**：如何结合注意力流的结构化优势与梯度的类别敏感性，生成既符合模型内部逻辑又具有高度类别区分度的解释图？

### 2. 方法论：核心思想与关键技术
DAP 的核心思想是将**基于梯度的决策先验**直接注入到 **Transformer 的注意力传播算子**中。
*   **决策先验估计**：首先利用 Grad-CAM 计算每个 Token 对目标预测类别的贡献度，并进行归一化处理，得到一个 Token 级别的决策先验向量 $d$。
*   **决策感知转换矩阵**：在每一层传播时，不直接使用原始注意力矩阵，而是通过决策先验对 Token 间的交互进行调制。公式上表现为：$\hat{T}_{ij} = \tilde{A}_{ij} \cdot d_i \cdot d_j$（其中 $\tilde{A}$ 是考虑了残差连接的注意力矩阵）。这种成对调制确保了只有当源 Token 和目标 Token 都与决策相关时，相关性才会强力传播。
*   **递归传播**：通过行归一化保持概率语义，然后递归地将这些决策感知的转换矩阵相乘，最终从 [CLS] Token 对应的行中提取出对输入 Patch 的归一化归一化得分。
*   **算法流程**：计算梯度先验 -> 提取各层注意力 -> 逐层注入先验并更新相关性矩阵 -> 生成最终热力图。

### 3. 实验设计
*   **数据集**：从 ImageNet-1K 训练集中随机采样，分为非平衡设置（800张）和类别平衡设置（200个类别，每类4张）。
*   **模型架构**：涵盖了不同规模的 ViT 变体，包括 ViT-Tiny (ViT-T), ViT-Small (ViT-S), ViT-Base (ViT-B), 和 ViT-Large (ViT-L)。
*   **对比方法**：
    *   **基于注意力类**：Attention Rollout (AR), Attribution Rollout (AttR), Chefer et al., GMAR。
    *   **基于梯度/定位类**：Grad-CAM, CDAM, AGCAM, LeGrad。
*   **评估指标**：
    *   扰动指标：Deletion (越低越好), Insertion (越高越好)。
    *   归因指标：类别敏感性 (CS)、Token 贡献一致性 (TCC)、注意力流稀疏度 (AFS)、层级决策对齐度 (LDA)。

### 4. 资源与算力
*   **硬件**：论文明确指出所有实验均在单台 **NVIDIA GeForce RTX 5060 Ti GPU** 上完成。
*   **软件**：基于 PyTorch 框架实现。
*   **训练/推理时长**：文中未明确提及生成单张解释图的具体耗时，但由于该方法属于后处理解释方法，不涉及模型重训练，通常计算开销在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 种不同规模的模型上进行了测试，并对比了 8 种主流基准方法。
*   **消融实验**：针对决策先验的注入方式（如仅目标端注入、仅输出端注入、成对注入等）进行了详细的消融研究。
*   **充分性评价**：实验设计较为全面，涵盖了定量指标、定性可视化、删除/插入曲线以及层级演化分析。通过在不同规模模型上的表现对比，客观地展示了该方法在大型模型上效果更显著的特性。

### 6. 主要结论与发现
*   **性能领先**：DAP 在大多数定量指标上（尤其是 Insertion, CS, TCC）优于现有的基于注意力的解释方法。
*   **规模效应**：模型规模越大（如 ViT-B, ViT-L），DAP 的优势越明显，这可能是因为大模型具有更丰富的表示和更结构化的注意力模式。
*   **传播一致性**：DAP 生成的解释在层级间演化更稳定，能够更早地锁定目标物体，且最终结果比传统方法更紧凑、更聚焦。

### 7. 优点
*   **结构化与语义的平衡**：成功将梯度信息整合进注意力流，解决了传统 Rollout 方法“类别盲”的问题。
*   **即插即用**：不需要修改模型架构或重新训练，适用于任何标准的 ViT 模型。
*   **理论严谨**：通过成对调制和行归一化，保持了 Transformer 内部注意力传播的数学语义。

### 8. 不足与局限
*   **对小模型效果有限**：在 ViT-Tiny 上的提升不明显，说明该方法依赖于模型本身已经具备较好的特征表示。
*   **数据集覆盖面**：实验主要集中在 ImageNet 上，尚未在医疗影像、遥感等更具挑战性或特定领域的视觉数据集上验证泛化性。
*   **依赖初始梯度质量**：DAP 使用 Grad-CAM 作为先验，如果模型本身的梯度信号存在噪声或失效，可能会影响最终的解释质量。

（完）
