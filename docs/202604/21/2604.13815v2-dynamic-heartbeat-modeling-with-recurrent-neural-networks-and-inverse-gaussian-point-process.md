---
title: Dynamic Heartbeat Modeling with Recurrent Neural Networks and Inverse Gaussian Point Process
title_zh: 基于循环神经网络和逆高斯点过程的动态心跳建模
authors: "Runwei Lin, Ying Wang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13815v2"
tags: ["query:dl"]
score: 9.0
evidence: 评估用于序列心跳建模的 GRU 和 LSTM 等 RNN 架构
tldr: 本研究探讨了将循环神经网络（RNN）与逆高斯点过程（IGP）结合，用于动态心率变异性（HRV）建模。针对IGP参数估计难的问题，评估了GRU、LSTM、S4和Mamba四种架构。结果表明，该方法能有效识别IGP时变参数，提升了R-R间期序列的概率建模精度，为复杂生理机制下的HRV分析提供了新框架。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13815v2/fig-001.webp\", \"caption\": \"Figure 1. The R–R sequence is encoded by a sequence model backbone and mapped to time-varying inverse Gaussian parameters. Separate branches are used to estimate the mean and the log-variance.\", \"page\": 2, \"index\": 1, \"width\": 465, \"height\": 428}, {\"url\": \"assets/figures/arxiv/2604.13815v2/fig-002.webp\", \"caption\": \"Table 2. Test results (Mean [SD]) of KS distance with the testing segment of 1800 beats. The last row reports the across-subject mean of subject-level mean KSD.\", \"page\": 3, \"index\": 2, \"width\": 525, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2604.13815v2/fig-003.webp\", \"caption\": \"Table 1. Test results (Mean [SD]) of KS distance with the testing segment of 600 beats. The last row reports the across-subject mean of subject-level mean KSD.\", \"page\": 3, \"index\": 3, \"width\": 525, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2604.13815v2/fig-004.webp\", \"caption\": \"Figure 2. A representative example of the testing results whose length including 1800 beats from comparing the predicted µt and the measured R–R interval series from subject 16483. Blue: Measured R-R; Orange: µt prediction. (a) GRU, (b) LSTM, (c) S4, and (d) Mamba.\", \"page\": 4, \"index\": 4, \"width\": 390, \"height\": 428}]"
motivation: 旨在解决逆高斯点过程在心率变异性建模中难以准确估计时变参数的挑战。
method: 评估了GRU、LSTM、S4和Mamba四种循环神经网络架构在IGP参数识别中的表现。
result: 实验证明结合神经序列模型与IGP框架能有效进行逐搏R-R间期建模，并通过KS统计验证了其准确性。
conclusion: 该研究为概率性HRV建模提供了灵活基础，有助于未来整合更复杂的生理机制和动态条件。
---

## 摘要
心率变异性（HRV）分析对于评估自主神经系统对心血管的调节具有重要意义。逆高斯过程（IGP）已被广泛用于逐搏 HRV 建模，因为它为心脏去极化过程提供了具有生理相关性的解释。基于 IGP 的心跳建模面临的一个关键挑战是时变参数的准确估计。在本研究中，我们探讨了循环神经网络（RNNs）是否可用于 IGP 参数识别，从而增强 R-R 间期动力学的概率建模。具体而言，我们使用 Kolmogorov-Smirnov 统计量评估了四种代表性的 RNN 架构，即 GRU、LSTM、结构化状态空间序列模型（S4）和 Mamba。结果表明，将神经序列模型与 IGP 框架相结合进行逐搏 R-R 序列建模是可行的。该方法为概率性 HRV 建模提供了灵活的基础，并为未来纳入更复杂的生理机制和动态条件奠定了基础。

## Abstract
Heart rate variability (HRV) analysis is important for the assessment of autonomic cardiovascular regulation. The inverse Gaussian process (IGP) has been widely used for beat-to-beat HRV modeling, as it gives a physiological relevant interpretation of heart depolarization process. A key challenge in IGP-based heartbeat modeling is the accurate estimation of time-varying parameters. In this study, we investigated whether recurrent neural networks (RNNs) can be used for IGP parameter identification and thereby enhance probabilistic modeling of R-R dynamics. Specifically, four representative RNN architectures, namely, GRU, LSTM, Structured State Space sequence model (S4), and Mamba, were evaluated using the Kolmogorov-Smirnov statistics. The results demonstrate the possibility of combining neural sequence models with the IGP framework for beat-wise R-R series modeling. This approach provides a flexible basis for probabilistic HRV modeling and for future incorporation of more complex physiological mechanisms and dynamic conditions.

---

## 论文详细总结（自动生成）

以下是对论文《Dynamic Heartbeat Modeling with Recurrent Neural Networks and Inverse Gaussian Point Process》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：心率变异性（HRV）是评估自主神经系统调节功能的关键指标。传统的逐搏（beat-to-beat）建模常采用**逆高斯点过程（IGP）**，因为它在生理上对应于窦房结去极化的“积分-阈值”过程，具有极强的可解释性。
*   **核心挑战**：IGP 模型的核心在于准确估计随时间变化的参数（均值 $\mu$ 和形状参数 $\lambda$）。传统方法多采用线性自回归或多项式扩展，难以捕捉 HRV 固有的高度非线性和个体差异。
*   **研究动机**：探索现代循环神经网络（RNN）架构是否能替代传统参数估计方法，在保持 IGP 生理可解释性的同时，提升对 R-R 间期动力学的建模精度和灵活性。

### 2. 方法论
*   **核心思想**：将 RNN 作为编码器学习心跳历史特征，并将其映射为 IGP 的时变参数，构建一个神经点过程框架。
*   **关键技术细节**：
    *   **概率模型**：假设下一个 R-R 间期 $x_{i+1}$ 服从逆高斯分布 $p(x_{i+1} | x_{1:i})$。
    *   **网络架构**：
        *   **输入层**：R-R 序列经过线性嵌入和层归一化（LayerNorm）。
        *   **骨干网络**：对比了四种架构：**GRU、LSTM、S4（结构化状态空间模型）和 Mamba**。采用残差连接处理隐藏状态。
        *   **预测头**：分为两个分支。**均值分支**通过线性层和 Shifted Softplus 激活函数输出 $\mu_t$（确保 $>0.3s$）；**方差分支**通过双层 MLP 预测 $\log \sigma^2_t$（用于计算 $\lambda_t$）。
    *   **损失函数**：最小化负对数似然（NLL），公式基于逆高斯分布的概率密度函数。
    *   **约束处理**：对 $\mu_t$ 设置 0.3s 的偏移量，对 $\log \sigma^2_t$ 进行截断处理以保证数值稳定性。

### 3. 实验设计
*   **数据集**：使用 **MIT-BIH 正常窦性心律数据库**，包含 18 名健康受试者（5 男 13 女），采样率为 128Hz。
*   **预处理**：Pan-Tompkins 算法识别 R 波，排除异常值（<0.3s 或 >2s）并进行三次埃尔米特多项式插值。
*   **Benchmark 与对比**：
    *   **模型对比**：横向对比了 GRU、LSTM、S4 和 Mamba。
    *   **评估指标**：采用 **Kolmogorov-Smirnov (KS) 距离**。基于时间重标度定理（Time-rescaling theorem），将拟合后的 CDF 转换为均匀分布，通过 KS 统计量衡量模型拟合优度（GOF）。
    *   **实验设置**：采用**留一受试者交叉验证（Leave-one-subject-out）**，确保测试集受试者未参与训练。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号或数量。
*   **训练细节**：模型维度固定为 64，单层架构。Mamba 和 S4 的状态维度设为 32。模型训练最多 2000 个 epoch，并采用早停（Early Stopping）策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 18 轮交叉验证（覆盖所有受试者）。
    *   测试了四种训练序列长度（100, 200, 300, 600 搏）和两种测试长度（600, 1800 搏）。
*   **充分性评价**：实验设计较为客观，通过留一法验证了模型的跨受试者泛化能力。对比了多种前沿序列模型（如 Mamba 和 S4），具有较好的时效性和公平性（固定了维度和层数）。但受试者样本量（18人）相对较小，且均为健康人群。

### 6. 主要结论与发现
*   **可行性验证**：RNN 能够有效识别 IGP 参数，性能与之前研究中报道的复杂非线性模型相当。
*   **架构表现**：**GRU、LSTM 和 Mamba** 的表现非常接近，而 **S4** 在该任务上的 KS 距离相对较大，表现略逊。
*   **序列长度影响**：训练长度为 600 搏时，模型获得的平均 KS 距离最低，拟合效果最好。
*   **拟合局限**：并非所有受试者的拟合结果都能通过 KS 阈值，说明 IGP 假设与某些复杂生理情况仍存在偏差。

### 7. 优点
*   **生理与数据驱动的结合**：既保留了 IGP 的生理语义（去极化过程），又利用了深度学习捕捉复杂非线性动力学的能力。
*   **概率化建模**：不仅预测心跳时间，还能提供不确定性（方差）估计，这对临床风险评估至关重要。
*   **架构前瞻性**：引入了 Mamba 等高性能线性序列模型，为长序列 HRV 分析提供了参考。

### 8. 不足与局限
*   **模型假设偏差**：IGP 假设可能无法完全覆盖所有生理调节机制，导致部分受试者拟合优度不达标。
*   **输入特征单一**：仅使用了 R-R 间期历史，未引入呼吸、血压等对心率有显著调制作用的多模态信号。
*   **数据噪声敏感**：尽管进行了预处理，但残留的异位搏动或检测错误仍会对基于点过程的模型产生较大干扰。
*   **人群覆盖面窄**：仅在健康人群中验证，尚未在心律失常或自主神经功能障碍（如糖尿病患者）人群中测试。

（完）
