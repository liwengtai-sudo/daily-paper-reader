---
title: "FD-NL2SQL: Feedback-Driven Clinical NL2SQL that Improves with Use"
title_zh: FD-NL2SQL：随使用而改进的反馈驱动型临床 NL2SQL
authors: "Suparno Roy Chowdhury, Tejas Anvekar, Manan Roy Choudhury, Muhammad Ali Khan, Kaneez Zahra Rubab Khakwani, Mohamad Bassam Sonbol, Irbaz Bin Riaz, Vivek Gupta"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15646v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于肿瘤试验库和生物标志物查询的临床 NL2SQL 助手
tldr: 针对临床医生在肿瘤试验数据库中进行复杂查询的需求，本文提出了FD-NL2SQL，一个反馈驱动的临床NL2SQL助手。该系统利用大语言模型进行问题分解、示例检索和SQL合成，并通过吸收临床医生的修改意见以及逻辑驱动的自动数据增强技术，实现了示例库的动态扩展与系统性能的持续进化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15646v1/fig-001.webp\", \"caption\": \"Figure 1: A clinician question is decomposed into schema-aligned predicate sub-questions; for each predicate, semantically similar expert-approved exemplars are retrieved; This guides schema-grounded SQL synthesizer. Users can edit and approve the final SQL to update the exemplar bank. To expand coverage with minimal annotation, approved SQL is augmented by a single atomic mutation (e.g., operator or column substitution) and retained only if it returns non-empty results. A second LLM back-translates augmented SQL into a NL-question & predicate sub-questions, new samples are added to the bank for continual improvement.\", \"page\": 1, \"index\": 1, \"width\": 434, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2604.15646v1/fig-002.webp\", \"caption\": \"Figure 2: FD-NL2SQL demo UI and feedback loop. Clinicians issue a natural-language query in the chat interface (right) and view executed results in the table view (left). The system shows the generated SQL, which an expert can accept, modify, or reject; accepted / edited queries are saved back to the exemplar bank to improve future retrieval and synthesis (autofill supports rapid refinement).\", \"page\": 3, \"index\": 2, \"width\": 946, \"height\": 645}, {\"url\": \"assets/figures/arxiv/2604.15646v1/fig-003.webp\", \"caption\": \"Table 1: Quantitative results across prompting strategies, finetuned model and FD-NL2SQL(Ours). We report CHRF, eEM, eF1, AST, and Conf; HM denotes the harmonic mean of {Conf, eF1, eEM}. Green marks the best score within a given strategy for each model, red marks the worst score within that strategy for each model, and underlines denote the best score overall across all models and strategies for a metric. Note gpt-5n depicts nano, and gpt-5m for mini variant\", \"page\": 5, \"index\": 3, \"width\": 458, \"height\": 479}]"
motivation: 临床医生在查询复杂的肿瘤试验数据库时面临SQL编写门槛高的问题，且现有系统缺乏随使用而持续优化的能力。
method: 采用模式感知的LLM进行问题分解和示例检索，并结合临床医生反馈及逻辑驱动的SQL自动变异技术来动态更新示例库。
result: 系统能够生成高质量的可执行SQL，并通过交互式界面支持临床医生的实时修正，从而实现系统性能的闭环提升。
conclusion: FD-NL2SQL证明了结合用户反馈与自动增强机制能有效解决临床领域NL2SQL系统的冷启动和持续改进难题。
---

## 摘要
临床医生在探索肿瘤试验库时，经常需要针对生物标志物、终点、干预措施和时间进行即时的多约束查询，但编写 SQL 需要具备数据库模式（schema）专业知识。我们演示了 FD-NL2SQL，这是一款针对基于 SQLite 的肿瘤数据库的反馈驱动型临床 NL2SQL 助手。给定一个自然语言问题，一个感知模式的大语言模型（LLM）将其分解为谓词级子问题，通过句子嵌入检索语义相似且经过专家验证的 NL2SQL 示例，并根据分解结果、检索到的示例和模式合成可执行的 SQL，同时进行后处理有效性检查。为了随使用而改进，FD-NL2SQL 整合了两种更新信号：(i) 临床医生对生成的 SQL 的编辑经批准后被添加到示例库中；(ii) 基于逻辑的轻量级 SQL 增强应用单个原子突变（例如运算符或列的更改），仅在返回非空结果时保留变体。第二个 LLM 为接受的变体生成相应的自然语言问题和谓词分解，从而在无需额外标注的情况下自动扩展示例库。演示界面展示了分解、检索、合成和执行结果，以支持交互式细化和持续改进。

## Abstract
Clinicians exploring oncology trial repositories often need ad-hoc, multi-constraint queries over biomarkers, endpoints, interventions, and time, yet writing SQL requires schema expertise. We demo FD-NL2SQL, a feedback-driven clinical NL2SQL assistant for SQLite-based oncology databases. Given a natural-language question, a schema-aware LLM decomposes it into predicate-level sub-questions, retrieves semantically similar expert-verified NL2SQL exemplars via sentence embeddings, and synthesizes executable SQL conditioned on the decomposition, retrieved exemplars, and schema, with post-processing validity checks. To improve with use, FD-NL2SQL incorporates two update signals: (i) clinician edits of generated SQL are approved and added to the exemplar bank; and (ii) lightweight logic-based SQL augmentation applies a single atomic mutation (e.g., operator or column change), retaining variants only if they return non-empty results. A second LLM generates the corresponding natural-language question and predicate decomposition for accepted variants, automatically expanding the exemplar bank without additional annotation. The demo interface exposes decomposition, retrieval, synthesis, and execution results to support interactive refinement and continuous improvement.

---

## 论文详细总结（自动生成）

这是一份关于论文《FD-NL2SQL: Feedback-Driven Clinical NL2SQL that Improves with Use》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
在肿瘤学研究中，临床医生需要频繁对临床试验数据库（如 ClinicalTrials.gov）进行多约束查询（涉及生物标志物、试验阶段、终点指标等）。然而，这些数据库的模式（Schema）极其复杂，临床医生通常缺乏编写 SQL 的专业技能，导致研究效率受阻。
现有的通用 NL2SQL 系统在处理高度专业化的医疗领域时存在以下痛点：
*   **领域知识缺失**：通用模型难以准确映射复杂的临床术语和约束。
*   **静态性**：系统无法从用户的交互和纠错中学习，性能无法随使用而进化。
*   **可靠性不足**：在医疗这种高风险领域，错误的 SQL 执行结果可能导致错误的科研决策。

### 2. 方法论：核心思想与技术细节
论文提出了 **FD-NL2SQL** 框架，其核心思想是**“分解-检索-合成”**的流水线，并结合**反馈驱动的自我进化机制**。

*   **谓词级分解（Predicate-level Decomposition）**：利用感知模式的 LLM 将复杂的自然语言问题拆解为多个原子化的子问题，每个子问题对应 SQL 中的一个 `WHERE` 谓词（如“年份 >= 2018”）。
*   **示例检索（Exemplar Retrieval）**：使用 Sentence-BERT 嵌入技术，从专家验证的示例库中检索与子问题语义最接近的 NL2SQL 对，为合成阶段提供结构模板。
*   **检索引导的 SQL 合成**：第二个 LLM 结合原始问题、分解后的谓词、检索到的示例以及数据库 Schema，合成最终的 SQL。系统包含后处理校验（如只读策略、Schema 存在性检查）。
*   **反馈与自动增强循环**：
    1.  **专家审批**：临床医生可修改并批准生成的 SQL，该对（问题, SQL）被存入示例库。
    2.  **逻辑驱动增强**：系统对批准的 SQL 进行“原子突变”（如修改运算符 `=` 为 `>=`，或更换同类型列），若变体执行结果非空，则保留。
    3.  **反向翻译（Back-Translation）**：利用 LLM 将增强后的 SQL 转化回自然语言问题和谓词分解，实现无需人工标注的示例库自动扩张。

### 3. 实验设计
*   **数据集/场景**：基于 Mayo Clinic 提供的 IOTOX 肿瘤临床试验数据库。
*   **种子集与基准测试**：
    *   **种子集**：500 个由肿瘤专家编写的真实科研查询。
    *   **扩展基准**：通过对种子集进行投影编辑、谓词删除、数值修改等原子变换，构建了包含 1500 个样本的测试集。
*   **对比方法**：
    *   **提示策略**：Zero-shot（零样本）、Few-shot（少样本）、Chain-of-Thought（思维链）。
    *   **模型底座**：Gemma3-27B、Qwen3-30B、GPT-5 nano/mini。
    *   **专用模型**：SQL-R1-14B（基于强化学习微调的推理模型）。
*   **评价指标**：eEM（执行精确匹配）、eF1（执行结果 F1 分数）、AST（抽象语法树相似度）、LLM Conf（模型置信度）。

### 4. 资源与算力
论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）或训练时长。由于该系统主要基于 LLM 的 Prompt Engineering（提示工程）和 API 调用，而非从头开始的大规模预训练，其算力消耗主要集中在推理阶段和嵌入向量的预计算上。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种不同的 LLM 底座和 4 种提示策略进行了交叉对比，实验组数较为充实。
*   **充分性与公平性**：
    *   实验涵盖了开源（Gemma, Qwen）与闭源（GPT-5）模型，具有代表性。
    *   通过程序化生成的变体进行压力测试，验证了系统在处理不同查询结构时的鲁棒性。
    *   **局限性**：实验主要集中在单表（IOTOX）操作，对于多表 Join 等更复杂的数据库操作涉及较少。

### 6. 主要结论与发现
*   **性能提升**：FD-NL2SQL 在所有模型底座上均显著优于传统的 Zero-shot 和 CoT 策略。
*   **分解的价值**：谓词级分解能有效减少约束条件的遗漏，使生成的 SQL 逻辑更严密。
*   **缩小模型差距**：通过分解和检索引导，较小的模型（如 GPT-5 nano）能够达到甚至超过大模型的基准表现，降低了部署成本。
*   **领域适配**：专门微调的 SQL-R1 在该领域表现不佳，证明了通用微调模型在缺乏特定领域 Schema 引导时难以直接应用。

### 7. 优点
*   **闭环进化**：引入了“专家审批 + 自动增强”机制，解决了医疗领域高质量标注数据稀缺的问题，实现了系统的持续优化。
*   **高透明度**：演示界面展示了中间分解步骤和检索到的示例，增强了临床医生对 AI 生成结果的可解释性和信任感。
*   **安全性**：通过只读策略和执行前校验，降低了对生产数据库的风险。

### 8. 不足与局限
*   **Schema 复杂度限制**：目前的基准测试主要针对单表，尚未充分验证其在具有数十个关联表的极复杂医疗数据库中的表现。
*   **增强偏差**：自动增强机制基于原子突变，可能导致示例库中高频模式过度代表，而覆盖不到复杂的嵌套查询或罕见的边缘情况。
*   **API 依赖**：置信度评分（Logprobs）依赖于特定模型 API 的开放程度，在不同供应商之间缺乏统一的衡量标准。
*   **缺乏用户研究**：论文主要侧重于离线指标评估，缺乏临床医生在实际工作流中长期使用该系统的定性反馈和生产环境下的安全性评估。

（完）
