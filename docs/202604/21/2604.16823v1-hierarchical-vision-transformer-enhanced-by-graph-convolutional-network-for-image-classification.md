---
title: Hierarchical Vision Transformer Enhanced by Graph Convolutional Network for Image Classification
title_zh: 基于图卷积网络增强的分层视觉 Transformer 图像分类研究
authors: Haibin Jiao
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16823v1"
tags: ["query:dl"]
score: 9.0
evidence: 结合图卷积网络增强的 Vision Transformer 用于图像分类
tldr: 针对Vision Transformer在Patch尺寸选择、空间结构捕捉及局部信息建模方面的局限，本文提出GCN-HViT模型。该模型结合了分层ViT与图卷积网络（GCN），通过分层结构融合不同尺度的Patch信息，利用GCN提取局部特征并作为2D位置编码，实现了全局关系与局部结构的协同建模，在多个数据集上达到了领先性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16823v1/fig-001.webp\", \"caption\": \"Figure 1. Model overview.\", \"page\": 3, \"index\": 1, \"width\": 1002, \"height\": 566}, {\"url\": \"assets/figures/arxiv/2604.16823v1/fig-002.webp\", \"caption\": \"Figure 2. Test accuracy (%) plotted against training epoch for models. All models within each plot were trained on the same type of GPU to ensure a fair comparison. Notably, in our experiments, we did not perform pre-training on the models and only trained the models for 30 epochs.\", \"page\": 5, \"index\": 2, \"width\": 1052, \"height\": 389}]"
motivation: 旨在解决ViT难以兼顾不同尺度Patch信息、1D位置编码空间表达不足以及缺乏局部结构建模能力的问题。
method: 提出一种结合分层ViT与GCN的架构，利用分层机制处理多尺度Patch，并借助GCN提取局部特征以增强2D空间位置表示。
result: 在三个真实世界数据集上的广泛实验证明，GCN-HViT模型达到了当前最先进的分类性能。
conclusion: 该研究通过融合GCN与分层ViT，有效提升了模型对图像全局关联与局部空间结构的综合表征能力。
---

## 摘要
视觉 Transformer (ViT) 通过引入自注意力机制为图像分类领域带来了新的突破，而图卷积网络 (GCN) 也已被提出并成功应用于数据表示与分析。然而，仍有一些关键挑战限制了它们的进一步发展：(1) ViT 选择的补丁 (patch) 大小对于准确预测至关重要，这引发了一个自然的问题：如何恰当地选择补丁大小，或者如何全面地结合小补丁和大补丁；(2) 虽然空间结构信息在视觉任务中非常重要，但一维 (1D) 位置嵌入无法更准确地捕捉补丁的空间结构信息；(3) GCN 能够捕捉图像节点之间的局部连接关系，但缺乏捕捉全局图结构信息的能力。相反，ViT 的自注意力机制可以描绘图像补丁之间的全局关系，但无法对图像的局部结构进行建模。为了克服这些局限性，我们提出了一种用于图像分类的图卷积网络增强分层视觉 Transformer (GCN-HViT)。具体而言，我们设计的分层 ViT 可以在每个层级的全局尺度上建模补丁间的信息交互，并在多个层级间建模小补丁与大补丁之间的层次关系。此外，所提出的 GCN 方法充当局部特征提取器，以获取每个图像补丁的局部表示，并将其作为每个补丁在二维 (2D) 空间中的二维位置嵌入。同时，它在每个层级的局部尺度上建模补丁间的信息交互。在 3 个真实世界数据集上的广泛实验表明，GCN-HViT 达到了最先进的性能。

## Abstract
Vision Transformer (ViT) has brought new breakthroughs to the field of image classification by introducing the self-attention mechanism and Graph Convolutional Networks(GCN) have been proposed and successfully applied in data representation and analysis. However, there are key challenges which limit their further development: (1) The patch size selected by ViT is crucial for accurate predictions, which raises a natural question: How to select the size of patches properly or how to comprehensively combine small patches and larger patches; (2) While the spatial structure information is important in vision tasks, the 1D position embeddings fails to capture the spatial structure information of patches more accurately; (3) The GCN can capture the local connectivity relationships between image nodes, but it lacks the ability to capture global graph structural information. On the contrary, the self-attention mechanism of ViT can draw the global relation on image patches, but it is unable to model the local structure of image. To overcome such limitations, we propose the Hierarchical Vision Transformer Enhanced by Graph Convolutional Network (GCN-HViT) for image classification. Specifically, the Hierarchical ViT we designed can model patch-wise information interactions on a global scale within each level and model hierarchical relationships between small patches and large patches across multiple levels. In addition, the proposed GCN method functions as a local feature extractor to obtain the local representation of each image patch which serves as a 2D position embedding of each patch in the 2D space. Meanwhile, it models patch-wise information interactions on a local scale within each level. Extensive experiments on 3 real-world datasets demonstrate that GCN-HViT achieves state-of-the-art performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Hierarchical Vision Transformer Enhanced by Graph Convolutional Network for Image Classification》（基于图卷积网络增强的分层视觉 Transformer 图像分类研究）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决 Vision Transformer (ViT) 在图像分类任务中的三个核心局限性：
*   **Patch 尺寸选择的矛盾：** 小 Patch 捕捉局部特征但计算开销大且缺乏全局感；大 Patch 包含信息多但容易平滑掉局部纹理。
*   **空间信息丢失：** ViT 采用的 1D 位置编码仅关注序列顺序，无法准确还原 2D 图像中像素间的高度空间结构关系。
*   **局部与全局建模的失衡：** GCN 擅长捕捉节点间的局部连接但缺乏全局视野；ViT 擅长全局自注意力建模但对局部结构的感知较弱。
**研究动机：** 结合分层结构与图卷积网络，构建一个既能处理多尺度 Patch 关系，又能通过 GCN 提取 2D 空间位置特征的综合模型。

### 2. 方法论
核心架构名为 **GCN-HViT**，主要包含两个关键组件：
*   **分层视觉 Transformer (Hierarchical ViT)：**
    *   **Level 1：** 将图像划分为 16 个小 Patch，通过 CNN 进行嵌入，并加入由 GCN 生成的 2D 位置编码，送入 Transformer 编码器。
    *   **桥接：** 将 Level 1 的输出通过 `Reshape` 操作还原回 2D 空间特征图。
    *   **Level 2：** 将特征图进一步聚合为 4 个大 Patch，再次进行嵌入和编码。这种结构模拟了人类视觉从微观到宏观的感知过程。
*   **图卷积网络 (GCN) 增强：**
    *   **核心思想：** 将图像 Patch 视为图中的节点，利用 Patch 间的空间邻接关系构建邻接矩阵（$A_{16}$ 和 $A_4$）。
    *   **2D 位置编码：** GCN 充当局部特征提取器，对 Patch 嵌入进行加权聚合，生成的局部表示直接作为 **2D 位置嵌入 (EPos)**，替代了 ViT 传统的 1D 学习编码。
    *   **交互建模：** 在每个层级内部，GCN 负责局部尺度的信息交互，而 Transformer 负责全局尺度的交互。

### 3. 实验设计
*   **数据集：**
    *   **Mnist：** 手写数字识别（28x28）。
    *   **FashionMnist：** 衣物分类（28x28）。
    *   **Quick Draw：** 手绘涂鸦分类（64x64）。
*   **Benchmark 与对比方法：**
    *   **ViT-4 / ViT-16：** 标准的 Vision Transformer（分别对应 4 和 16 个 Patch）。
    *   **HViT：** 仅使用分层结构但不含 GCN 的模型。
    *   **GCN-HViT-1：** 使用单向邻接矩阵（仅考虑右方和下方邻居）。
    *   **GCN-HViT-2：** 使用双向邻接矩阵。

### 4. 资源与算力
*   **GPU 型号：** 文中未明确指出具体的 GPU 型号（仅提到“相同类型的 GPU”以确保公平）。
*   **训练细节：** 模型**未进行预训练**，仅在目标数据集上训练了 **30 个 epoch**。
*   **算力说明：** 论文强调了通过分层结构和 GCN 优化，旨在提高预测效率并降低对超大 Patch 处理的依赖。

### 5. 实验数量与充分性
*   **实验组数：** 在 3 个数据集上分别对比了 5 种模型变体，并进行了消融实验（对比 ViT vs HViT vs GCN-HViT）。
*   **客观性：** 实验设置了统一的硬件环境和训练轮数，具有一定的客观性。
*   **充分性：** 实验验证了分层结构的有效性以及 GCN 引入 2D 位置信息的优越性。但由于数据集规模较小（如 MNIST 系列），在超大规模数据集（如 ImageNet）上的泛化能力尚待验证。

### 6. 主要结论与发现
*   **分层结构至关重要：** HViT 普遍优于单一尺度的 ViT，证明了结合大小 Patch 信息的必要性。
*   **GCN 提升显著：** 引入 GCN 后，模型在所有数据集上的表现均有提升，证明 GCN 提取的 2D 空间特征比 1D 位置编码更有效。
*   **邻接矩阵的影响：** 实验发现 **GCN-HViT-1（单向邻接）** 的表现优于 GCN-HViT-2（双向邻接），作者推测双向邻接矩阵可能引入了冗余或干扰信息。
*   **SOTA 性能：** GCN-HViT 在所选的三个数据集上均达到了最先进的分类准确率。

### 7. 优点
*   **创新的位置编码：** 巧妙地利用 GCN 提取 2D 空间结构特征来替代硬编码或简单的 1D 学习编码。
*   **局部与全局的优雅融合：** 通过 GCN 和 Transformer 的串联，解决了单一架构在特征提取上的片面性。
*   **结构简洁：** 模型原理清晰，分层逻辑符合视觉直觉，易于理解和复现。

### 8. 不足与局限
*   **数据集局限性：** 实验主要集中在低分辨率、相对简单的灰度图数据集（MNIST 等），缺乏在 ImageNet-1K 或更高分辨率彩色图像上的表现数据。
*   **超参数说明不足：** 论文未详细列出所有超参数（如学习率、优化器设置、GCN 层数等）的选择标准。
*   **层级深度有限：** 目前仅实现了两层分层，对于更复杂的图像，可能需要更多层级的抽象。
*   **邻接矩阵解释：** 对于为何单向邻接优于双向邻接，缺乏更深层次的理论证明或可视化分析。

（完）
