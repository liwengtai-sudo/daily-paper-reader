---
title: Stochastic Trust-Region Methods for Over-parameterized Models
title_zh: 过参数化模型的随机置信域方法
authors: "Aike Yang, Hao Wang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14017v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于模型优化的随机信任域方法
tldr: 针对过度参数化模型中随机优化方法对步长选择敏感的问题，本文提出了一种统一的随机置信域框架。该框架无需手动调节步长，并能自然扩展至等式约束问题。理论证明在强增长条件下，该方法在无约束和有约束场景下均具有优异的收敛复杂度。实验表明，该方法在深度学习和约束优化任务中表现稳定，且无需手动调度学习率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14017v1/fig-001.webp\", \"caption\": \"Figure 1. Comparison of training loss and test accuracy for SGD, Adam, SLS, and STR on CIFAR-10.\", \"page\": 21, \"index\": 1, \"width\": 850, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2604.14017v1/fig-002.webp\", \"caption\": \"Figure 3. Constrained orthogonal subspace fitting on synthetic spiked model data (d = 500, k = 10, n = 1000). Comparison of STR-P with projected SGD, Riemannian gradient descent, and stochastic augmented Lagrangian.\", \"page\": 23, \"index\": 2, \"width\": 850, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.14017v1/fig-003.webp\", \"caption\": \"Figure 2. Constrained orthogonal subspace fitting on synthetic spiked model data (d = 100, k = 5, n = 500). Comparison of STR-P with projected SGD, Riemannian gradient descent, and stochastic augmented Lagrangian.\", \"page\": 23, \"index\": 3, \"width\": 850, \"height\": 343}]"
motivation: 解决随机优化方法在过度参数化模型中对步长选择过于敏感且难以处理等式约束的问题。
method: 提出一种基于强增长条件的随机置信域框架，并针对等式约束引入了二次罚函数机制。
result: 理论上实现了优异的收敛复杂度，实验中在神经网络训练和子空间拟合任务上表现出极高的稳定性且无需调参。
conclusion: 该随机置信域方法有效消除了手动调参需求，为过度参数化模型的无约束和有约束优化提供了稳健的解决方案。
---

## 摘要
在插值型假设（如强增长条件）下，随机优化方法可以达到与全批量方法相当的收敛速度，但其性能（特别是 SGD）对步长选择高度敏感。为了解决这个问题，我们提出了一个统一的随机置信域框架，该框架消除了手动步长调优，并能自然地扩展到等式约束问题。对于无约束优化，我们开发了一种一阶随机置信域算法，并证明在强增长条件下，该算法在寻找 $\varepsilon$-驻点时，其迭代复杂度和随机一阶算子复杂度均为 $O(\varepsilon^{-2} \log(1/\varepsilon))$。对于等式约束问题，我们引入了一种基于二次惩罚项的随机置信域方法（惩罚参数为 $\mu$），并证明其在达到惩罚问题 $\varepsilon$-驻点（对应于原始约束问题的 $O(\varepsilon)$-近似 KKT 点）时的迭代和算子复杂度为 $O(\varepsilon^{-4} \log(1/\varepsilon))$。在深度神经网络训练和正交约束子空间拟合上的数值实验表明，所提方法在无需手动学习率调度的情况下，实现了与经过良好调优的随机基准方法相当的性能，同时表现出稳定的优化行为并能有效处理硬约束。

## Abstract
Under interpolation-type assumptions such as the strong growth condition, stochastic optimization methods can attain convergence rates comparable to full-batch methods, but their performance, particularly for SGD, remains highly sensitive to step-size selection. To address this issue, we propose a unified stochastic trust-region framework that eliminates manual step-size tuning and extends naturally to equality-constrained problems. For unconstrained optimization, we develop a first-order stochastic trust-region algorithm and show that, under the strong growth condition, it achieves an iteration and stochastic first-order oracle complexity of $O(\varepsilon^{-2} \log(1/\varepsilon))$ for finding an $\varepsilon$-stationary point. For equality-constrained problems, we introduce a quadratic-penalty-based stochastic trust-region method with penalty parameter $μ$, and establish an iteration and oracle complexity of $O(\varepsilon^{-4} \log(1/\varepsilon))$ to reach an $\varepsilon$-stationary point of the penalized problem, corresponding to an $O(\varepsilon)$-approximate KKT point of the original constrained problem. Numerical experiments on deep neural network training and orthogonally constrained subspace fitting demonstrate that the proposed methods achieve performance comparable to well-tuned stochastic baselines, while exhibiting stable optimization behavior and effectively handling hard constraints without manual learning-rate scheduling.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stochastic Trust-Region Methods for Over-parameterized Models》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随机梯度下降（SGD）及其变体是现代机器学习的核心，但其性能高度依赖于步长（学习率）的选择，通常需要繁琐的手动调优。
*   **背景**：在现代过参数化模型（如深度神经网络）中，模型往往能达到零训练误差，这被称为“插值”现象。在这种背景下，随机梯度满足“强增长条件”（Strong Growth Condition, SGC）。
*   **核心问题**：如何利用置信域（Trust-Region）方法的稳健性，在满足 SGC 的过参数化模型中，开发出一种无需手动调节步长、且能自然处理等式约束的统一随机优化框架。

### 2. 论文提出的方法论
该论文提出了一个统一的随机置信域（STR）框架，涵盖无约束和有约束两种场景：
*   **核心思想**：通过构建局部二次模型并动态调整“置信域半径”来控制步长，而不是预设学习率。
*   **无约束算法 (Algorithm 1)**：
    *   在每一步构建模型 $m_k(p) = g_k^\top p + \frac{1}{2}\|p\|^2$，其中 $g_k$ 是随机梯度。
    *   求解子问题得到试探步 $p_k$（通常是截断梯度方向）。
    *   计算实际下降与预测下降的比率 $r_k$。若 $r_k$ 足够大则接受该步并扩大半径，否则拒绝该步并缩小半径。
*   **有约束算法 (Algorithm 2)**：
    *   针对等式约束 $c(x)=0$，引入二次罚函数 $\phi(x) = f(x) + \frac{\mu}{2}\|c(x)\|^2$。
    *   将约束诱导的曲率信息整合进 Hessian 矩阵近似 $H_k = I + \mu \nabla c(x_k)^\top \nabla c(x_k)$。
    *   通过调节惩罚参数 $\mu$（设为 $1/\epsilon$）来平衡目标优化与约束满足。
*   **理论贡献**：证明了在 SGC 下，无约束 STR 的复杂度为 $O(\epsilon^{-2} \log(1/\epsilon))$；有约束 STR 达到 $\epsilon$-KKT 点的复杂度为 $O(\epsilon^{-4} \log(1/\epsilon))$。

### 3. 实验设计
*   **无约束场景**：
    *   **数据集**：CIFAR-10。
    *   **模型**：ResNet-20。
    *   **基准方法 (Benchmarks)**：手动调优的 SGD、Adam、以及随机线搜索方法 (SLS)。
*   **有约束场景**：
    *   **任务**：正交子空间拟合（Orthogonal Subspace Fitting），涉及 Stiefel 流形上的正交约束 $W^\top W = I$。
    *   **基准方法**：投影 SGD (Projected SGD)、黎曼梯度下降 (Riemannian GD)、随机增广拉格朗日法 (AugLag)。

### 4. 资源与算力
*   **文中说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、数量）或具体的训练总时长。
*   **推测**：鉴于使用了 ResNet-20 和 CIFAR-10，实验通常可以在单张消费级 GPU（如 RTX 3090 或 A100）上完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对图像分类做了多组对比实验，展示了训练损失和测试准确率随 Epoch 的变化。
    *   针对子空间拟合，分别在不同维度（$d=100$ 和 $d=500$）的合成数据集上进行了测试。
*   **充分性评价**：实验设计较为**标准且客观**。作者对所有基准方法都进行了网格搜索以确保对比的公平性，并提供了多次独立运行的均值和标准差。虽然数据集规模（CIFAR-10）不算极大，但足以验证置信域方法在非凸深度学习任务中的有效性。

### 6. 论文的主要结论与发现
*   **无需调参**：STR 方法在不需要手动设置学习率调度表的情况下，达到了与经过精细调优的 SGD 和 Adam 相当的性能。
*   **稳定性**：在训练过程中，STR 表现出非常稳定的收敛行为，尤其是在处理硬约束时，比传统的罚函数法更具鲁棒性。
*   **约束处理**：提出的罚函数 STR 方法能有效驱动约束违反度降至接近机器精度，同时保持较快的物标函数下降速度。

### 7. 优点
*   **理论与实践结合**：不仅提供了严谨的收敛复杂度证明，还在现代深度学习架构上验证了实用性。
*   **自适应性**：成功将确定性优化中成熟的置信域机制引入随机优化，解决了步长敏感性痛点。
*   **通用性**：框架统一，能够通过简单的罚函数扩展处理复杂的非线性等式约束。

### 8. 不足与局限
*   **计算开销**：虽然是一阶方法，但每一步需要计算 $r_k$（涉及额外的函数值评估），在某些计算代价极高的任务中可能会增加开销。
*   **约束类型限制**：目前主要集中在等式约束，对于不等式约束的处理尚未在文中详细讨论或实验。
*   **超参数依赖**：虽然消除了学习率调优，但置信域本身引入了半径更新系数（如 $\nu_1, \nu_2$）和接受阈值（$c_0, c_1, c_2$），尽管这些参数通常比学习率更鲁棒，但仍需设定。
*   **实验覆盖面**：未在大规模数据集（如 ImageNet）或超大规模语言模型上进行验证，其在高维极稀疏梯度下的表现尚待观察。

（完）
