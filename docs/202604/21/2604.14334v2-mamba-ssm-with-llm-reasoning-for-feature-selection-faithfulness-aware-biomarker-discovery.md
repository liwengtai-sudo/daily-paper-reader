---
title: "Mamba-SSM with LLM Reasoning for Feature Selection: Faithfulness-Aware Biomarker Discovery"
title_zh: 结合大语言模型推理与 Mamba-SSM 的特征选择：忠实度感知的生物标志物发现
authors: "Pushpa Kumar Balan, Aijing Feng"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14334v2"
tags: ["query:ai-pm"]
score: 10.0
evidence: 在癌症RNA-seq数据上使用Mamba-SSM和大模型推理进行生物标志物发现
tldr: 本研究针对深度序列模型在生物标志物发现中易受组织成分混杂因素干扰的问题，提出结合Mamba-SSM与大语言模型（LLM）推理的特征选择框架。通过Mamba提取梯度显著性基因，并利用DeepSeek-R1的思维链（CoT）进行过滤。实验表明，该方法在TCGA-BRCA数据集上仅用17个基因便实现了0.927的AUC，优于原始显著性集合及高方差基准，证明了LLM在去除混杂因素、提升预测性能方面的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14334v2/fig-001.webp\", \"caption\": \"Figure 1: End-to-end neuro-symbolic pipeline. High-dimensional TCGA-BRCA RNA-seq is processed by Mamba SSM to produce gradient saliency scores (top-50 candidate genes), then filtered by structured DeepSeek-R1 CoT reasoning to form a 17-gene BRCA-specific subset used in downstream comparison.\", \"page\": 2, \"index\": 1, \"width\": 790, \"height\": 373}, {\"url\": \"assets/figures/arxiv/2604.14334v2/fig-002.webp\", \"caption\": \"Figure 2: Performance Comparison: Accuracy, F1, and AUC metrics across experimental conditions. B3 utilizes 250x fewer genes than B1 but achieves higher predictive stability.\", \"page\": 7, \"index\": 2, \"width\": 706, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.14334v2/fig-003.webp\", \"caption\": \"Figure 3: Raw Gradient Saliency Heatmap for the top-50 genes in TCGA-BRCA samples, highlighting the noisy feature space prior to LLM intervention.\", \"page\": 8, \"index\": 3, \"width\": 706, \"height\": 1096}, {\"url\": \"assets/figures/arxiv/2604.14334v2/fig-004.webp\", \"caption\": \"Figure 4: Visualization of the Agentic Chain-of-Thought (CoT) process, mapping Mamba saliency to biological rationale.\", \"page\": 9, \"index\": 4, \"width\": 706, \"height\": 515}]"
motivation: 深度学习模型提取的生物标志物常包含降低分类性能的混杂因素，需探索LLM推理能否有效过滤这些干扰并提升下游任务表现。
method: 在乳腺癌RNA-seq数据上训练Mamba模型，提取梯度显著性前50个基因，并利用DeepSeek-R1的结构化思维链筛选出最终的17个基因。
result: LLM过滤后的17个基因集在测试集上达到0.927的AUC，显著优于5000个基因的基准（0.903）和未过滤的显著性集合（0.832）。
conclusion: 针对性的混杂因素去除可在不具备全面召回率的情况下显著提升预测性能，体现了LLM在特征选择中的选择性忠实度。
---

## 摘要
深度序列模型的梯度显著性（Gradient saliency）能高效地筛选出候选生物标志物，但生成的基因列表可能会受到组织成分混杂因素的污染，从而降低下游分类器的性能。我们研究了大语言模型（LLM）的思维链（CoT）推理是否能过滤这些混杂因素，以及推理质量是否与下游性能相关。我们在 TCGA-BRCA RNA-seq 数据集上训练了一个 Mamba SSM，并通过梯度显著性提取了前 50 个基因；DeepSeek-R1 使用结构化思维链对每个候选基因进行评估，最终生成了一个包含 17 个基因的集合。在留出测试集上，原始的 50 基因显著性集合（无 LLM）的表现逊于 5000 基因方差基准（AUC 0.832 对比 0.903），而经过 LLM 过滤的集合则超越了该基准（AUC 0.927），且使用的特征数量减少了 294 倍。忠实度审计（参考 COSMIC CGC、OncoKB、PAM50）显示，选出的 17 个基因中有 6 个（35.3%）是经过验证的 BRCA 生物标志物，而输入中存在的 16 个已知 BRCA 基因中有 10 个被遗漏——包括 FOXA1。下游性能与推理忠实度之间的这种分歧表明，在这种情境下存在“选择性忠实度”：有针对性地去除混杂因素可以在不具备全面召回率的情况下提高预测性能。

## Abstract
Gradient saliency from deep sequence models surfaces candidate biomarkers efficiently, but the resulting gene lists can be contaminated by tissue-composition confounders that degrade downstream classifiers. We study whether LLM chain-of-thought (CoT) reasoning can filter these confounders, and whether reasoning quality is associated with downstream performance. We train a Mamba SSM on TCGA-BRCA RNA-seq and extract the top-50 genes by gradient saliency; DeepSeek-R1 evaluates every candidate with structured CoT to produce a final 17-gene set. On the held-out test split, the raw 50-gene saliency set (no LLM) performs worse than a 5,000-gene variance baseline (AUC 0.832 vs. 0.903), while the LLM-filtered set surpasses it (AUC 0.927), using 294x fewer features. A faithfulness audit (COSMIC CGC, OncoKB, PAM50) shows that 6 of 17 selected genes (35.3%) are validated BRCA biomarkers, while 10 of 16 known BRCA genes present in the input were missed - including FOXA1. This divergence between downstream performance and reasoning faithfulness suggests selective faithfulness in this setting: targeted confounder removal can improve predictive performance without comprehensive recall.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何结合深度状态空间模型（Mamba-SSM）与大语言模型（LLM）的推理能力，来解决高维基因组数据中的特征选择和生物标志物发现问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在高维 RNA-seq 数据（>20,000 基因）中，深度学习模型虽然能通过梯度显著性（Gradient Saliency）识别重要特征，但这些特征往往包含大量“混杂因素”（如免疫浸润、组织纯度等），而非真正的疾病驱动基因。
*   **研究动机**：探索 LLM 的生物医学知识和思维链（CoT）推理是否能作为“生物过滤器”，剔除这些统计相关但生物学无关的噪声，从而提升模型的泛化能力和生物标志物发现的准确性。

### 2. 论文提出的方法论
该研究采用了一种**神经符号（Neuro-symbolic）流水线**，分为四个阶段：
1.  **Mamba-SSM 训练**：在 TCGA-BRCA 数据集上训练 Mamba 模型。利用其线性缩放特性处理长序列基因数据，将基因表达值嵌入 128 维空间，通过 Mamba 块捕捉基因间的长程依赖。
2.  **梯度显著性提取**：对训练好的模型进行反向传播，计算每个基因对分类损失的平均梯度幅值，筛选出前 50 个显著性最高的基因作为候选池。
3.  **结构化 CoT 推理过滤**：将这 50 个基因输入 **DeepSeek-R1 (7B)**。通过结构化提示词（Prompt），要求 LLM 依据 5 个拒绝准则（如：组织特异性混杂、无功能注释等）和 3 个保留准则进行评估，最终筛选出 17 个基因。
4.  **忠实度审计**：将 LLM 选择的基因与权威数据库（COSMIC CGC, OncoKB, PAM50）进行交叉比对，评估其生物学准确性。

### 3. 实验设计
*   **数据集**：TCGA-BRCA（乳腺癌）RNA-seq 数据，包含 1,095 个肿瘤样本和 113 个正常样本。
*   **Benchmark（基准）**：
    *   **B1 (方差基准)**：选取方差最大的 5,000 个基因。
    *   **B2 (原始显著性)**：仅使用 Mamba 提取的前 50 个显著性基因（无 LLM 过滤）。
    *   **B3 (LLM 过滤后)**：使用 LLM 筛选出的 17 个基因。
*   **对比指标**：准确率（Accuracy）、F1 分数、AUC。

### 4. 资源与算力
*   **算力支持**：论文提到获得了 Cohere Labs 的计算积分支持以及中央密苏里大学（UCM）的研究基金。
*   **具体配置**：LLM 部分使用了 **DeepSeek-R1 (7B)**，通过 **Ollama** 在本地运行（Temperature = 0.3）。
*   **训练细节**：Mamba 模型训练使用了 AdamW 优化器，学习率 $10^{-4}$，15 个 epoch，batch size 为 8。文中未明确给出具体的 GPU 型号和总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：主要针对 TCGA-BRCA 这一特定癌症类型进行了深入研究。
*   **充分性评价**：
    *   **优点**：对比了高维基准和原始显著性方法，证明了 LLM 过滤的有效性。
    *   **局限**：实验仅限于单一数据集（TCGA-BRCA）和单一随机种子划分，缺乏多中心数据验证或跨癌症类型的泛化性测试。未与 LASSO 或 ElasticNet 等传统特征选择方法进行直接对比。

### 6. 主要结论与发现
*   **性能提升**：LLM 过滤后的 17 基因集（B3）AUC 达到 **0.927**，优于 5,000 基因基准（0.903）和 50 基因原始显著性集（0.832）。
*   **选择性忠实度（Selective Faithfulness）**：LLM 虽然只召回了输入中 37.5% 的已知乳腺癌基因，但通过精准剔除混杂因素，依然显著提升了下游分类性能。
*   **LLM 的局限性**：LLM 存在“自信地犯错”现象。例如，它拒绝了乳腺癌关键驱动因子 **FOXA1**，且在推理过程中偶尔会产生幻觉（如引用无证据的信号通路）。

### 7. 优点
*   **高效性**：仅用 17 个基因（比基准减少 294 倍特征）就实现了更高的预测精度，极大地压缩了特征空间。
*   **可解释性**：通过 CoT 推理，为每个基因的留存或剔除提供了生物学解释，使黑盒模型的特征选择过程透明化。
*   **创新视角**：提出了“选择性忠实度”概念，即在生物标志物发现中，**精准剔除负样本**可能比**全面找回正样本**对模型性能更有利。

### 8. 不足与局限
*   **召回率低**：LLM 遗漏了超过 60% 的已知重要生物标志物，这在临床诊断应用中可能存在风险。
*   **幻觉风险**：LLM 在推理时仍会使用通用或虚假的生物学逻辑来强行解释其选择。
*   **评估单一**：目前仅在乳腺癌数据上验证，且缺乏对不同规模 LLM（如 70B 或更大模型）的对比研究。
*   **解析限制**：作者提到由于解析器问题，未能对所有基因的决策字符串进行全量分析。

（完）
