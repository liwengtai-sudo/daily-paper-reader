---
title: "SegMix:Shuffle-based Feedback Learning for Semantic Segmentation of Pathology Images"
title_zh: SegMix：基于打乱反馈学习的病理图像语义分割
authors: "Zhiling Yan, Sicheng Chen, Tianyi Zhang, Nan Ying, Yanli Lei, Guanglei Zhang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15777v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 病理图像语义分割用于疾病诊断
tldr: 针对病理图像语义分割中像素级标注获取困难的问题，本文提出SegMix框架。该方法利用弱监督学习，通过图像级标签生成伪掩码。核心创新在于引入基于课程学习启发的随机打乱反馈机制，通过对图像块进行自适应打乱并根据模型反馈调整策略，有效提升了CAM生成的伪标注质量。在三个数据集上的实验证明其性能优于现有主流方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15777v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed method. The model adaptively adjusts the schedulers in the left blue box based on the feedback from previous learning. The patch size scheduler and shuffle ratio scheduler regulate the patch size and shuffle ratio. Then, relation patches are in-place shuffled across the same batch, maintaining their relative positions the same in images. Lastly, the mixed and original images are fed into the Weekly Supervised Segmentation Network for twins’ steps training in the green boxes.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2604.15777v1/fig-002.webp\", \"caption\": \"Table 1: Dice score (%) and IoU (%) of methods for weakly supervised semantic segmentation. Ours w/o FL denotes the proposed method without the feedback learning. The best results are in bold.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 394}]"
motivation: 传统的像素级标注成本极高，而现有的基于CAM的弱监督方法在病理图像上生成的伪掩码范围有限，无法充分挖掘病理特征。
method: 提出一种基于打乱（Shuffle）的反馈学习方法，通过对病理图像进行块级打乱，并根据前期学习反馈动态调整打乱策略来优化伪掩码生成。
result: 在三个不同的病理图像数据集上进行的实验结果表明，该方法在生成高质量伪掩码和分割性能方面均优于当前最先进的技术。
conclusion: SegMix通过自适应打乱反馈机制有效解决了弱监督病理图像分割中伪标注不完整的问题，为低标注成本下的精准医疗诊断提供了新思路。
---

## 摘要
分割是计算病理学中的一项关键任务，因为它能识别受疾病或异常生长影响的区域，对于诊断和治疗至关重要。然而，获取高质量的像素级监督分割数据需要经验丰富的病理学家投入大量工作，这限制了深度学习的应用。为了克服这一挑战，将标注条件放宽至图像级分类标签，可以利用更多数据并启用更多应用场景。一种方法是利用类激活图（CAM），在仅有图像级标签的情况下生成用于语义分割的伪像素级标注。然而，该方法未能深入探索病理图像的本质特征，导致仅能识别出较小的区域，不足以生成伪掩码。在本文中，我们受课程学习启发，提出了一种新颖的基于打乱的反馈学习方法，用于生成更高质量的伪语义分割掩码。具体而言，我们对病理图像进行补丁级（patch-level）打乱，模型根据先前学习的反馈自适应地调整打乱策略。实验结果表明，我们提出的方法在三个不同的数据集上均优于现有最先进的方法。

## Abstract
Segmentation is a critical task in computational pathology, as it identifies areas affected by disease or abnormal growth and is essential for diagnosis and treatment. However, acquiring high-quality pixel-level supervised segmentation data requires significant workload demands from experienced pathologists, limiting the application of deep learning. To overcome this challenge, relaxing the label conditions to image-level classification labels allows for more data to be used and more scenarios to be enabled. One approach is to leverage Class Activation Map (CAM) to generate pseudo pixel-level annotations for semantic segmentation with only image-level labels. However, this method fails to thoroughly explore the essential characteristics of pathology images, thus identifying only small areas that are insufficient for pseudo masking. In this paper, we propose a novel shuffle-based feedback learning method inspired by curriculum learning to generate higher-quality pseudo-semantic segmentation masks. Specifically, we perform patch level shuffle of pathology images, with the model adaptively adjusting the shuffle strategy based on feedback from previous learning. Experimental results demonstrate that our proposed approach outperforms state-of-the-arts on three different datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《SegMix: Shuffle-based Feedback Learning for Semantic Segmentation of Pathology Images》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
病理图像的语义分割对于疾病诊断至关重要，但获取高质量的**像素级标注（Pixel-level annotations）**极度依赖资深病理学家的手工标注，成本高昂且难以大规模应用。
*   **核心问题**：现有的弱监督语义分割（WSSS）方法主要依赖图像级标签（Image-level labels）和类激活图（CAM）生成伪掩码，但这些方法多针对自然图像设计，未能充分考虑病理图像的局部特征（如细胞核比例）、全局特征（细胞排列）及多尺度实例关系，导致生成的伪掩码范围过小或不准确。
*   **研究目标**：提出一种能够深入挖掘病理图像多尺度特征的弱监督学习框架，以生成更高质量的伪分割掩码。

### 2. 方法论：核心思想与关键技术
论文提出了 **SegMix** 框架，其核心思想是结合**课程学习（Curriculum Learning）**和**反馈机制**，通过动态调整图像块（Patch）的打乱策略来增强模型对特征的提取能力。

*   **反馈学习模块（Feedback Learning Module）**：
    *   设计了两个调度器：**补丁大小调度器（Patch-size scheduler）**和**打乱比例调度器（Shuffle ratio scheduler）**。
    *   **反馈机制**：模型根据前一阶段的训练损失（Loss）自适应调整策略。若 Loss 低于阈值 $T$，说明模型已掌握当前难度，则减小补丁尺寸（更细粒度）并增加打乱比例（更高难度），实现从粗到细的学习。
*   **补丁打乱（Shuffle Patches）**：
    *   将图像划分为补丁，在同一 Batch 内随机选择特定比例的补丁进行“原位打乱”（In-place shuffle）。
    *   这种混合方式不仅引入了样本内的多尺度特征，还通过跨样本的补丁组合引入了样本间的关联信息。
*   **像素相关性模块（Pixel Correlation Module, PCM）**：
    *   利用余弦相似度衡量相邻像素间的关系，对原始 CAM 进行加权修正，以提高预测结果的一致性和平滑度。

### 3. 实验设计
*   **数据集**：
    1.  **ROSE**：胰腺癌细胞病理图像（1773张癌症，3315张正常）。
    2.  **WBC**：白细胞数据集（包含5类白细胞，共计约1.5万张图像）。
    3.  **MARS**：胃癌病理图像（1770张，包含正常、管状腺癌、粘液腺癌）。
*   **Benchmark 与对比方法**：
    *   对比了经典的 **CAM** 以及先进的弱监督方法 **SEAM** 和 **CPN**。
    *   评估指标采用语义分割标准的 **Dice Score (DSC)** 和 **IoU**。

### 4. 资源与算力
*   **硬件环境**：Ubuntu 服务器，配置为 Intel Xeon Platinum 8350C CPU。
*   **GPU 资源**：使用了 **2 块 NVIDIA RTX 3090 GPU**，但明确指出每次实验仅使用 **1 块 GPU**。
*   **软件环境**：PyTorch 1.10.0, CUDA 11.3, Python 3.8。
*   **训练细节**：优化器迭代 8 次（iterations），Batch Size 为 4，学习率在 0.00009 到 0.0001 之间。文中未明确提及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同类型的病理数据集上进行了验证，涵盖了细胞级和组织级的病理场景。
*   **消融实验**：
    *   对比了“有无反馈学习（FL）”的效果。
    *   对比了不同的打乱策略（如 Group 策略、Back 策略）。
*   **充分性评价**：实验设计较为充分，通过跨数据集的对比证明了算法的泛化性。消融实验有效地验证了反馈机制和打乱策略对性能提升的贡献。

### 6. 主要结论与发现
*   **性能领先**：SegMix 在所有三个数据集上均达到了 SOTA 性能。例如在 ROSE 数据集上，IoU 达到 50.2%，显著优于基准 CAM（35.6%）。
*   **反馈学习的有效性**：引入反馈学习模块后，DSC 和 IoU 分别提升了约 6.9% 和 4.8%，证明了自适应调整学习难度（从粗到细）的必要性。
*   **抗干扰能力**：可视化结果显示，SegMix 能够更好地排除病理图像中的背景噪声和无关像素干扰，生成的掩码更接近真实标注（Ground Truth）。

### 7. 优点（亮点）
*   **自适应性**：不同于固定参数的增强方法，SegMix 能根据模型学习状态动态调整策略，符合人类学习的认知规律（课程学习）。
*   **多尺度建模**：通过改变补丁大小，强制模型同时关注细胞级（局部）和组织级（全局）特征。
*   **弱监督高效性**：仅需图像级标签即可获得高质量的像素级分割效果，极大地降低了临床应用中的标注门槛。

### 8. 不足与局限
*   **超参数敏感性**：反馈机制依赖于预设的阈值 $T$ 和增长因子 $\alpha$，这些参数在不同数据集上的敏感度未被详细讨论。
*   **计算复杂度**：虽然单次训练仅需一块 GPU，但反馈机制可能增加训练收敛所需的迭代次数。
*   **维度限制**：目前研究集中在 2D 病理切片，尚未扩展到临床中同样重要的 3D 医疗影像数据（作者在结论中提到了这是未来的计划）。
*   **极端情况处理**：在消融实验中发现，如果反馈策略过于保守（如 Back 策略），模型可能会陷入过拟合或冗余学习。

（完）
