---
title: Transformer Based Machine Fault Detection From Audio Input
title_zh: 基于 Transformer 的音频输入机器故障检测
authors: Kiran Voderhobli Holla
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12733v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于音频故障检测的Transformer架构
tldr: 本研究探讨了利用 Sound AI 进行机器故障预测的新方法。传统上，CNN 被广泛用于分析音频频谱图，但其存在局部性和参数共享等归纳偏置。本文引入了在图像领域表现优异的 Transformer 架构（如 ViT），利用其较低的归纳偏置来处理声音数据。通过对比 Transformer 与 CNN 在机器故障检测任务中的表现及生成的嵌入向量，研究证明了 Transformer 驱动架构在音频分析中的有效性，为工业设备实时监控提供了更强大的技术路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12733v1/fig-001.webp\", \"caption\": \"Fig. 1. Popular techniques for fault detection using Sound AI\", \"page\": 1, \"index\": 1, \"width\": 506, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2604.12733v1/fig-002.webp\", \"caption\": \"TABLE III AREA UNDER CURVE\", \"page\": 6, \"index\": 2, \"width\": 540, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2604.12733v1/fig-003.webp\", \"caption\": \"Fig. 2. Spectrogram and Mel spectrogram for a normal sound sample of fan (top) and Valve (bottom)\", \"page\": 6, \"index\": 3, \"width\": 487, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.12733v1/fig-004.webp\", \"caption\": \"Fig. 3. Mel-Spectrograms for normal(left) and anomalous(right) sounds for slider (top) and Fan (bottom)\", \"page\": 6, \"index\": 4, \"width\": 479, \"height\": 508}, {\"url\": \"assets/figures/arxiv/2604.12733v1/fig-005.webp\", \"caption\": \"Fig. 4. Embedding visualization highlighting faulty sounds in a two-dimensional space using t-SNE\", \"page\": 7, \"index\": 5, \"width\": 487, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2604.12733v1/fig-006.webp\", \"caption\": \"TABLE I LIST OF OPERATIONS AND ANOMALOUS CONDITIONS\", \"page\": 3, \"index\": 6, \"width\": 527, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2604.12733v1/fig-007.webp\", \"caption\": \"Fig. 5. Attention patterns in the transformer heads across layers\", \"page\": 8, \"index\": 7, \"width\": 429, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.12733v1/fig-008.webp\", \"caption\": \"Fig. 6. Transformer and CNN explainability for an input audio after resizing to the original image shape\", \"page\": 8, \"index\": 8, \"width\": 500, \"height\": 500}]"
motivation: 旨在解决传统 CNN 在分析音频频谱图时因局部性偏置而存在的局限性，探索 Transformer 在声音 AI 领域的潜力。
method: 将基于 Transformer 的架构应用于机器运行声音生成的频谱图，并与传统的 CNN 模型在故障检测任务上进行对比实验。
result: 实验证明了 Transformer 架构在处理声音数据方面的有效性，并详细对比了其与 CNN 在特征嵌入生成上的差异。
conclusion: 由于归纳偏置较低，Transformer 架构在处理复杂的机器音频数据时表现出比 CNN 更好的性能潜力。
---

## 摘要
近年来，声音人工智能（Sound AI）越来越多地被用于预测机器故障。通过在目标机器上安装麦克风，可以从现场获取机器行为的实时数据。传统上，卷积神经网络（CNN）架构被用于分析由捕获的声音生成的语谱图图像，并预测机器是否按预期运行。尽管 CNN 架构具有局部性和参数共享等归纳偏置，而这些偏置可能并不完全适用于语谱图分析，但它们在经验上表现良好。随着 2020 年 Vision Transformer (ViT) 开启了基于 Transformer 的模型在图像处理领域的成功应用，人们对在声音人工智能领域利用这些模型产生了浓厚兴趣。由于基于 Transformer 的架构具有显著较低的归纳偏置，在数据充足的情况下，预计它们在语谱图分析方面的表现将优于 CNN。本文展示了 Transformer 驱动的架构在分析声音数据方面的有效性，并在机器故障检测这一特定任务上，将其生成的嵌入与 CNN 进行了比较。

## Abstract
In recent years, Sound AI is being increasingly used to predict machine failures. By attaching a microphone to the machine of interest, one can get real time data on machine behavior from the field. Traditionally, Convolutional Neural Net (CNN) architectures have been used to analyze spectrogram images generated from the sounds captured and predict if the machine is functioning as expected. CNN architectures seem to work well empirically even though they have biases like locality and parameter-sharing which may not be completely relevant for spectrogram analysis. With the successful application of transformer-based models in the field of image processing starting with Vision Transformer (ViT) in 2020, there has been significant interest in leveraging these in the field of Sound AI. Since transformer-based architectures have significantly lower inductive biases, they are expected to perform better than CNNs at spectrogram analysis given enough data. This paper demonstrates the effectiveness of transformer-driven architectures in analyzing Sound data and compares the embeddings they generate with CNNs on the specific task of machine fault detection.

---

## 论文详细总结（自动生成）

这篇论文对基于 Transformer 架构的机器故障音频检测进行了深入研究，并将其与传统的 CNN 架构进行了多维度的对比分析。以下是该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在工业 4.0 背景下，利用声音 AI 进行预测性维护具有低成本、非接触和提供先导指标（Lead Indicators）等优势。
*   **核心问题**：传统上使用 CNN 处理音频语谱图（Spectrogram），但 CNN 具有**局部性（Locality）**和**参数共享（Weight-sharing）**的归纳偏置。由于语谱图的纵轴代表频率，不同位置的相同模式含义不同，CNN 的这些偏置可能限制其性能。
*   **研究目标**：验证归纳偏置更低的 Transformer 架构（如 AST）在音频故障检测中是否优于 CNN，并分析其背后的特征提取机制。

### 2. 论文提出的方法论
*   **核心思想**：对比 Transformer（AST）与 CNN（ResNeXt）在监督学习（分类）和无监督学习（异常检测）两种范式下的表现。
*   **关键技术细节**：
    *   **预处理**：使用 Librosa 将 10 秒音频转换为 Log-Mel 语谱图。
    *   **模型选择**：
        *   **Transformer**：采用音频语谱图 Transformer (AST)，利用预训练的 ViT 权重初始化。
        *   **CNN**：采用 ResNeXt101_32x8d，参数量（约 87M）与 AST 相当，确保对比公平。
    *   **学习策略**：
        *   **监督学习**：在预训练模型后添加全连接层，采用**逐层学习率衰减（Layer-wise LR Decay）**和参数冻结技术进行高效微调。
        *   **无监督学习**：提取模型倒数第二层的嵌入向量（Embeddings），输入到**局部离群因子（LOF）**算法中进行异常识别。
    *   **损失函数**：监督学习使用二元交叉熵（BCE），无监督基准使用均方误差（MSE）。

### 3. 实验设计
*   **数据集**：使用 **MIMII 数据集**，包含四种工业机器（风扇、泵、阀门、滑轨）在真实工厂背景噪声下的正常与异常声音。
*   **Benchmark（基准）**：MIMII 官方提供的基于全连接层的自动编码器（Autoencoder）基准模型。
*   **对比方法**：
    1.  Autoencoder Baseline（无监督）。
    2.  CNN-Supervised（监督微调）。
    3.  AST-Supervised（监督微调）。
    4.  CNN-LOF（基于嵌入的无监督）。
    5.  AST-LOF（基于嵌入的无监督）。
*   **评价指标**：ROC 曲线下面积（AUC）。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验是在一台配有 **16GB 内存的个人 M1 MacBook** 上完成的。
*   **训练效率**：得益于参数高效的微调技术，每个训练周期（Epoch）耗时**少于 10 分钟**。这证明了该方法在低算力环境下的可行性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同类型的机器设备，并针对每种设备进行了监督和无监督两类实验。
*   **分析深度**：除了数值对比，作者还进行了 **t-SNE 嵌入可视化**、**Transformer 注意力图（Attention Maps）**分析以及 **CNN 的 Grad-CAM 可视化**。
*   **充分性评价**：实验设计较为全面，通过控制模型参数量（87M）实现了公平对比，且通过可视化手段解释了模型决策差异，具有较强的说服力。

### 6. 主要结论与发现
*   **性能优越性**：在监督学习下，AST 在所有机器类型上均优于 CNN；在无监督学习下，除风扇外，AST 的嵌入向量表现也优于 CNN。
*   **全局建模能力**：Transformer 的自注意力机制允许其从第一层起就捕捉全局上下文（长程依赖），而 CNN 的感受野是随层数逐渐扩大的。
*   **偏置影响**：CNN 的权重共享在语谱图上可能是有害的，因为它会同等对待不同频率位置的相同模式；而 Transformer 避开了这一限制，生成的特征簇（Clusters）在空间上更具辨识度。

### 7. 优点
*   **参数高效性**：提出了一种在普通笔记本电脑上即可微调大型 Transformer 模型的方法。
*   **可解释性**：首次在机器故障检测领域展示并对比了 AST 的内部注意力模式与 CNN 的激活图，揭示了 Transformer 关注区域更精准。
*   **实用性**：验证了即使在异常样本极少的情况下，通过 1 个 Epoch 的微调也能获得极高的检测精度。

### 8. 不足与局限
*   **数据不平衡**：尽管使用了 AUC 缓解，但异常样本稀缺仍是实际应用中的挑战。
*   **领域偏移（Domain Shift）**：论文提到环境变化可能导致模型性能下降，虽然提及了领域自适应技术，但未在实验中深入探讨。
*   **数据集单一**：实验主要集中在 MIMII 数据集上，未来需在更多样化的工业场景（如生物声学、医疗听诊）中验证泛化性。

（完）
