---
title: "MambaBack: Bridging Local Features and Global Contexts in Whole Slide Image Analysis"
title_zh: MambaBack：在全扫描切片图像分析中桥接局部特征与全局上下文
authors: "Sicheng Chen, Chad Wong, Tianyi Zhang, Enhui Chai, Zeyu Liu, Fei Xia"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15729v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 使用Mamba骨干网络进行全切片图像分析以诊断癌症
tldr: 本研究针对全切片图像（WSI）分析中Mamba模型存在的空间局部性破坏、局部特征建模不足及推理内存峰值高等挑战，提出了MambaBack混合架构。该方法引入希尔伯特采样以保持1D序列中的2D空间一致性，并设计了结合Gated CNN与BiMamba2的层次化结构，分别强化局部细胞特征提取与全局上下文聚合。此外，通过非对称分块设计显著降低了推理内存需求。实验证明，MambaBack在五个数据集上均优于现有SOTA方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15729v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of MambaBack. a. MIL pipeline with MambaBack. b. MambaBack structure. c. Heatmap visualization. d. Inference memory usage comparison.\", \"page\": 2, \"index\": 1, \"width\": 1068, \"height\": 802}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-002.webp\", \"caption\": \"Table 1: The performance comparison with SOTA methods.\", \"page\": 5, \"index\": 2, \"width\": 1064, \"height\": 359}, {\"url\": \"assets/figures/arxiv/2604.15729v1/fig-003.webp\", \"caption\": \"Figure 2: Analysis of WSI tile distributions and ablation studies on key components. a. Distribution of tile counts across datasets. b. Ablation study on sampling strategies on PANDA dataset. c. Ablation study on model architectures on PANDA dataset.\", \"page\": 5, \"index\": 3, \"width\": 1068, \"height\": 463}]"
motivation: 旨在解决Mamba在处理WSI时因1D展平导致的空间信息丢失、局部特征提取不充分以及边缘设备推理内存占用过高的问题。
method: 提出一种结合希尔伯特采样、1D Gated CNN局部提取器与BiMamba2全局聚合器的层次化混合架构，并采用非对称分块设计优化内存。
result: 在五个主流WSI数据集上的实验结果显示，MambaBack的性能全面超越了包括Transformer和纯Mamba在内的七种先进基准方法。
conclusion: MambaBack成功融合了局部与全局建模的优势，为计算病理学中高效、精准的大尺度图像分析提供了一种极具竞争力的解决方案。
---

## 摘要
全扫描切片图像（WSI）分析在计算病理学中至关重要，它通过整合不同放大倍率下的形态学和结构线索来实现癌症诊断。多实例学习（MIL）是 WSI 分析的标准框架。近期，Mamba 因其源自自然语言处理（NLP）的高效性和全局上下文建模能力，超越了 Transformer，成为 MIL 中极具前景的主干网络。然而，现有的基于 Mamba 的 MIL 方法面临三个关键挑战：(1) 1D 序列展平过程中 2D 空间局部性的破坏；(2) 细粒度局部细胞结构的建模效果欠佳；(3) 在资源受限的边缘设备上推理时内存峰值过高。诸如 MambaOut 等研究表明，Mamba 的 SSM 组件在局部特征提取方面是冗余的，而门控 CNN（Gated CNNs）已足够。意识到 WSI 分析既需要类似于自然图像的细粒度局部特征提取，又需要类似于 NLP 的全局上下文建模，我们提出了 MambaBack，这是一种融合了 Mamba 和 MambaOut 优势的新型混合架构。首先，我们提出了希尔伯特（Hilbert）采样策略，以保留 1D 序列中图块（tiles）的 2D 空间局部性，增强模型的空间感知能力。其次，我们设计了一个分层结构，包含一个基于 MambaOut 的 1D 门控 CNN 模块以捕获局部细胞特征，以及一个 BiMamba2 模块以聚合全局上下文，共同增强多尺度表示。最后，我们实现了一种非对称分块设计，允许在训练期间进行并行处理，并在推理期间进行分块流式累加，从而最大限度地降低部署时的峰值内存占用。在五个数据集上的实验结果表明，MambaBack 优于七种最先进的方法。源代码和数据集已公开。

## Abstract
Whole Slide Image (WSI) analysis is pivotal in computational pathology, enabling cancer diagnosis by integrating morphological and architectural cues across magnifications. Multiple Instance Learning (MIL) serves as the standard framework for WSI analysis. Recently, Mamba has become a promising backbone for MIL, overtaking Transformers due to its efficiency and global context modeling capabilities originating from Natural Language Processing (NLP). However, existing Mamba-based MIL approaches face three critical challenges: (1) disruption of 2D spatial locality during 1D sequence flattening; (2) sub-optimal modeling of fine-grained local cellular structures; and (3) high memory peaks during inference on resource-constrained edge devices. Studies like MambaOut reveal that Mamba's SSM component is redundant for local feature extraction, where Gated CNNs suffice. Recognizing that WSI analysis demands both fine-grained local feature extraction akin to natural images, and global context modeling akin to NLP, we propose MambaBack, a novel hybrid architecture that harmonizes the strengths of Mamba and MambaOut. First, we propose the Hilbert sampling strategy to preserve the 2D spatial locality of tiles within 1D sequences, enhancing the model's spatial perception. Second, we design a hierarchical structure comprising a 1D Gated CNN block based on MambaOut to capture local cellular features, and a BiMamba2 block to aggregate global context, jointly enhancing multi-scale representation. Finally, we implement an asymmetric chunking design, allowing parallel processing during training and chunking-streaming accumulation during inference, minimizing peak memory usage for deployment. Experimental results on five datasets demonstrate that MambaBack outperforms seven state-of-the-art methods. Source code and datasets are publicly available.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MambaBack**，一种专为全扫描切片图像（WSI）分析设计的新型混合架构。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
全扫描切片图像（WSI）具有极高的分辨率（十亿像素级），病理诊断需要同时关注微观的细胞形态（局部特征）和宏观的组织结构（全局上下文）。
*   **研究动机**：虽然 Mamba 模型在处理长序列 WSI 数据时比 Transformer 更高效，但仍面临三个挑战：
    1.  **空间局部性丢失**：将 2D 图像块展平为 1D 序列会破坏组织结构的 2D 空间关系。
    2.  **局部建模不足**：Mamba 的状态空间模型（SSM）在提取细粒度局部特征方面不如 CNN 高效。
    3.  **推理内存瓶颈**：在资源受限的临床边缘设备上，处理超长序列时内存峰值过高。
*   **核心目标**：通过结合 CNN 的局部提取能力和 Mamba 的全局建模能力，并引入空间感知采样，提升 WSI 分析的精度与部署效率。

### 2. 方法论：核心思想与技术细节
MambaBack 提出了三个核心技术模块：
*   **希尔伯特采样策略（Hilbert Sampling）**：
    *   利用希尔伯特空间填充曲线的特性，将 2D 坐标映射到 1D 索引。相比传统的行优先扫描，希尔伯特曲线能更好地将 2D 邻域内的图块在 1D 序列中聚集在一起，从而在序列化后最大限度保留空间连续性。
*   **层次化混合结构（Hierarchical Hybrid Structure）**：
    *   **局部阶段**：采用基于 MambaOut 的 **1D 门控 CNN（Gated CNN）** 模块。它通过深度卷积（DWConv）和门控机制捕获局部细胞特征，随后通过局部注意力池化将图块压缩为代表性标记（Token）。
    *   **全局阶段**：采用 **双向 Mamba2（BiMamba2）** 模块。它在压缩后的序列上进行前向和后向扫描，聚合全片的全局上下文信息。
*   **非对称分块设计（Asymmetric Chunking）**：
    *   **训练时**：全序列并行处理，利用 GPU 算力实现高吞吐量。
    *   **推理时**：采用“分块与累加”设计，将序列分为小块逐次处理并丢弃中间特征图，使内存占用保持在较低的常数水平，便于边缘设备部署。

### 3. 实验设计
*   **数据集**：涵盖 5 个公开数据集，共 17,828 张 WSI，涉及 4 种任务：
    *   **CAMELYON16/17**：乳腺癌淋巴结转移检测（二分类/多分类）。
    *   **PANDA**：前列腺癌 ISUP 分级（多分类）。
    *   **TCGA-NSCLC**：肺癌亚型分类。
    *   **TCGA-BRCA**：乳腺癌生存预测（生存分析任务）。
*   **对比方法（Benchmark）**：对比了 7 种最先进（SOTA）的方法，包括：
    *   传统 MIL：ABMIL, CLAM, DTFD-MIL。
    *   Transformer 类：TransMIL, Prov-GigaPath。
    *   Mamba/RNN 类：MambaMIL, PathRWKV。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA RTX 4090 GPU**。
*   **软件环境**：Python 3.12, PyTorch 2.9, CUDA 12.8。
*   **训练细节**：使用 AdamW 优化器，余弦退火调度器；特征提取使用预训练的 Prov-GigaPath 基础模型；训练时 Batch Size 为 8 或 64（视数据集而定）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同器官、不同任务的数据集上进行了验证，总样本量接近 1.8 万张切片，具有较强的统计说服力。
*   **消融实验**：
    *   对比了希尔伯特采样与随机、顺序、Z-order 采样的效果。
    *   对比了混合架构与纯 Mamba、纯 CNN 架构的性能差异。
    *   对比了不同模型在推理时的内存占用曲线。
*   **公平性**：所有方法均采用 5 折交叉验证，并报告了平均值和标准差，实验设计客观公平。

### 6. 主要结论与发现
*   **性能领先**：MambaBack 在所有 5 个数据集上的各项指标（AUC, Acc, F1, C-Index）均达到 SOTA 水平。
*   **空间感知的重要性**：希尔伯特采样显著优于其他序列化方法，证明了保留 2D 局部性对病理诊断的关键作用。
*   **混合架构的优越性**：实验证明在局部使用 Gated CNN 比纯 Mamba 效果更好，且能降低计算开销。
*   **部署友好**：非对称设计使 MambaBack 的推理内存占用远低于 Transformer 模型，甚至可与轻量级模型（如 CLAM）媲美。

### 7. 优点（亮点）
*   **逻辑清晰**：针对 Mamba 在视觉任务中的固有缺陷（空间丢失、局部冗余）提出了针对性的解决方案。
*   **采样创新**：引入希尔伯特曲线解决 WSI 非凸、不规则形状的序列化问题，比传统的重排序方法更具数学优雅性。
*   **实用性强**：非对称分块设计直接解决了 WSI 模型从实验室到临床边缘设备部署的痛点。

### 8. 不足与局限
*   **基础模型依赖**：模型性能高度依赖于第一阶段冻结的特征提取器（如 Prov-GigaPath），若基础模型无法捕捉某些特定形态，MambaBack 也难以弥补。
*   **超参数敏感性**：分块大小（Chunk Size）和采样长度等超参数可能需要针对不同分辨率或组织类型进行微调。
*   **计算复杂度**：虽然推理内存低，但双向 Mamba2 的引入在训练阶段仍需一定的计算资源。

（完）
