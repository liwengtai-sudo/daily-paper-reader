---
title: Post-hoc Self-explanation of CNNs
title_zh: 卷积神经网络的事后自解释
authors: "Ahcène Boubekki, Line H. Clemmensen"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28466v1"
tags: ["query:dl"]
score: 9.0
evidence: 卷积神经网络的后置自我解释
tldr: 本研究探讨了卷积神经网络（CNN）的事后自解释性。针对标准CNN内置原型无法准确表示数据的问题，提出将末端线性层替换为基于k-means的分类器。通过结合中间特征激活的空间一致性，生成基于概念的解释图和无梯度特征归因图。实验表明，使用浅层特征能在保持预测性能的同时，显著提升解释的语义保真度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28466v1/fig-001.webp\", \"caption\": \"Figure 2: UMAP projection of training and test (lower opacity) embeddings from the twenty sparrow classes of CUB-200. Left: classifier prototypes cj as crosses and rescaled to class norm as triangles. Right: KMEx prototypes.\", \"page\": 5, \"index\": 1, \"width\": 775, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2603.28466v1/fig-002.webp\", \"caption\": \"Figure 1: Interpretation process for B4 (left) and B234 (right) on a CUB-200 red-bellied woodpecker. Red and blue indicate higher and lower feature importance. Representative patches are the closest training examples to each prototype; border colors match the explanation map segments.\", \"page\": 5, \"index\": 2, \"width\": 722, \"height\": 287}]"
motivation: 标准CNN虽然在数学上可视为自解释模型，但其内置原型往往无法准确反映真实的数据分布。
method: 将CNN末端线性层替换为k-means分类器，并利用中间层特征激活的空间一致性生成概念解释图。
result: 在ResNet34上的实验显示，使用较浅层的特征激活（如B234）能在语义保真度与预测性能之间取得良好平衡。
conclusion: 通过改进分类器结构和利用多层特征激活，可以有效提升CNN的事后解释质量并增强其语义表达能力。
---

## 摘要
虽然标准的卷积神经网络（CNNs）在数学上可以被重新解释为自解释模型（SEMs），但其内置的原型本身并不能准确地表示数据。将最后的线性层替换为基于 $k$-means 的分类器可以在不损害性能的情况下解决这一局限性。本研究为分类器、编码器的最终输出（B4）以及中间特征激活的组合引入了一种通用的基于 $k$-means 的事后解释形式化方法。后一种方法利用卷积感受野的空间一致性来生成基于概念的解释图，并由无梯度特征归因图提供支持。对 ResNet34 的实证评估表明，使用更浅、压缩程度更低的特征激活（例如来自最后三个区块 B234 的激活）会导致语义保真度与预测性能轻微下降之间的权衡。

## Abstract
Although standard Convolutional Neural Networks (CNNs) can be mathematically reinterpreted as Self-Explainable Models (SEMs), their built-in prototypes do not on their own accurately represent the data. Replacing the final linear layer with a $k$-means-based classifier addresses this limitation without compromising performance. This work introduces a common formalization of $k$-means-based post-hoc explanations for the classifier, the encoder's final output (B4), and combinations of intermediate feature activations. The latter approach leverages the spatial consistency of convolutional receptive fields to generate concept-based explanation maps, which are supported by gradient-free feature attribution maps. Empirical evaluation with a ResNet34 shows that using shallower, less compressed feature activations, such as those from the last three blocks (B234), results in a trade-off between semantic fidelity and a slight reduction in predictive performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Post-hoc Self-explanation of CNNs》的结构化深入总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：标准的卷积神经网络（CNN）虽然在数学结构上可以被视为“自解释模型”（SEM），但其内置的分类器权重（即“原型”）在实际应用中往往无法准确代表数据分布，导致其解释缺乏语义代表性和直觉性。
*   **研究动机**：
    *   现有的自解释模型（如 ProtoPNet）通常需要复杂的联合训练方案，且往往会牺牲预测性能。
    *   CNN 的线性分类层虽然简单，但其权重向量（原型）通常远离数据中心，无法反映类内的多样性。
    *   旨在提出一种无需重新训练、基于事后（Post-hoc）处理的方法，使冻结的 CNN 能够生成高质量的语义解释。

### 2. 论文提出的方法论
核心思想是将 CNN 的末端线性层替换为基于 **$k$-means 聚类** 的分类器，并利用中间层特征生成解释。
*   **关键技术细节**：
    1.  **分类器重构（KMEx）**：在冻结的编码器后，对训练数据的特征向量进行类内 $k$-means 聚类，以聚类质心作为新的原型。
    2.  **多层特征融合**：提取编码器不同深度（如最后三个 Block，记为 B234）的特征激活，通过线性插值统一空间分辨率并进行归一化拼接。
    3.  **解释图生成**：将输入图像的每个空间位置（Patch）分配给最接近的聚类原型，生成离散的语义分割图（Explanation Map）。
    4.  **无梯度特征归因**：提出一种类 CAM 的方法，利用卷积感受野的空间一致性，将深层的分类贡献度反向映射到浅层分割块上，从而衡量每个语义概念对最终预测的重要性。

### 3. 实验设计
*   **数据集**：MNIST（手写数字）、STL10（通用物体识别）、CUB-200（细粒度鸟类分类）。
*   **Benchmark（基准）**：原始 ResNet34 模型。
*   **对比方法**：
    *   **CNN**：标准线性分类器。
    *   **KMEx**：基于 $k$-means 的事后解释方法。
    *   **ProtoPNet**：一种需要联合训练的典型自解释模型。
    *   **B4 / B234**：作者提出的不同深度的特征解释方案。

### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的训练或聚类耗时。由于该方法是事后（Post-hoc）处理且基于冻结模型，其主要计算开销在于对训练集特征进行 $k$-means 聚类。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单（MNIST）到复杂（CUB-200）的三个数据集，并针对 ResNet34 进行了深入分析。
*   **充分性**：
    *   进行了**原型对齐实验**：通过余弦相似度证明了 $k$-means 原型比原始线性权重更贴近数据分布。
    *   进行了**准确率对比**：验证了替换分类器后对性能的影响。
    *   进行了**可视化分析**：通过 UMAP 降维和解释图展示了方法的直观效果。
*   **评价**：实验设计较为客观，通过对比 B4（深层）和 B234（多层）展示了“解释分辨率”与“预测准确率”之间的权衡（Trade-off）。

### 6. 论文的主要结论与发现
*   **原型代表性**：原始 CNN 的分类器权重与数据分布的余弦相似度仅为 0.5 左右，而 $k$-means 原型接近 1.0，证明了事后聚类能更真实地反映数据特征。
*   **性能表现**：在 B4 层（编码器输出层）应用该方法，其准确率几乎可以无损地匹配原始 CNN，且优于需要重新训练的 ProtoPNet。
*   **深度权衡**：引入更浅层的特征（B234）可以显著提升解释图的细节和语义保真度，但在复杂任务（如 CUB-200）上会导致预测准确率下降（从 79.1% 降至 52.8%）。

### 7. 优点（亮点）
*   **无需重训**：直接应用于预训练好的模型，具有极高的通用性和灵活性。
*   **无梯度依赖**：特征归因不依赖梯度，避免了梯度噪声或饱和问题，解释结果更具空间一致性。
*   **数学完备性**：通过定理证明了 CNN 在机制上与原型自解释模型（SEM）的等价性。

### 8. 不足与局限
*   **扩展性问题**：$k$-means 聚类在处理超大规模数据集（如 ImageNet 全集）时可能面临计算瓶颈。
*   **浅层特征的性能损失**：在追求高分辨率解释时，浅层特征由于未经充分的非线性压缩，其分类能力明显弱于深层特征。
*   **超参数依赖**：每个类别的聚类数量 $K$ 需要手动设定，这可能影响解释的粒度和准确性。

（完）
