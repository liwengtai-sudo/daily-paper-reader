---
title: "SwinTextUNet: Integrating CLIP-Based Text Guidance into Swin Transformer U-Nets for Medical Image Segmentation"
title_zh: SwinTextUNet：将基于 CLIP 的文本引导集成到 Swin Transformer U-Net 中用于医学图像分割
authors: "Ashfak Yeafi, Parthaw Goswami, Md Khairul Islam, Ashifa Islam Shamme"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10000v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于医学图像分割的Swin Transformer
tldr: 本研究针对传统医疗图像分割模型在处理低对比度或模糊图像时的局限性，提出了SwinTextUNet框架。该模型创新性地将CLIP预训练的文本嵌入集成到Swin Transformer UNet架构中，通过交叉注意力和卷积融合机制实现语义文本引导与分层视觉特征的深度对齐。在QaTaCOV19数据集上的实验证明，该方法显著提升了分割的鲁棒性与准确性，为多模态医疗影像分析提供了高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10000v1/fig-001.webp\", \"caption\": \"Fig. 7. Representative qualitative segmentation results. Each triplet shows (a) original CXR, (b) ground-truth mask, and (c) SwinTextUNet prediction.\", \"page\": 5, \"index\": 1, \"width\": 370, \"height\": 527}, {\"url\": \"assets/figures/arxiv/2604.10000v1/fig-002.webp\", \"caption\": \"Fig. 4. Overall architecture of SwinTextUNet. The model integrates CLIP-based text embeddings with a hierarchical Swin U-Net through cross-attention and ConvFuse blocks.\", \"page\": 4, \"index\": 2, \"width\": 702, \"height\": 371}, {\"url\": \"assets/figures/arxiv/2604.10000v1/fig-003.webp\", \"caption\": \"Fig. 5. Representative samples from the QaTa-COV19 dataset across training, validation, and test sets. Each sample shows (a) chest X-ray, (b) ground-truth lesion mask, and (c) textual annotation provided by the dataset authors [23].\", \"page\": 4, \"index\": 3, \"width\": 474, \"height\": 452}]"
motivation: 传统分割模型仅依赖视觉特征，在面对医疗图像中低对比度或模糊的病灶模式时表现不佳。
method: 提出SwinTextUNet架构，利用交叉注意力和卷积融合技术将CLIP文本嵌入与Swin Transformer的视觉特征进行多尺度对齐。
result: "在QaTaCOV19数据集上，该模型实现了86.47%的Dice系数和78.2%的IoU，在性能与模型复杂度之间达到了最优平衡。"
conclusion: 视觉与语言信息的跨模态集成能显著增强医疗图像分割的精确度，为开发临床诊断工具提供了有力支持。
---

## 摘要
精确的医学图像分割是实现计算机辅助诊断和有效治疗规划的基础。仅依赖视觉特征的传统模型在面对模糊或低对比度模式时往往表现不佳。为了克服这些局限性，我们提出了 SwinTextUNet，这是一种多模态分割框架，它将对比语言-图像预训练（CLIP）衍生的文本嵌入集成到 Swin Transformer UNet 骨干网络中。通过集成交叉注意力和卷积融合，该模型有效地将语义文本引导与分层视觉表示对齐，从而增强了鲁棒性和准确性。我们在 QaTaCOV19 数据集上评估了我们的方法，其中所提出的四阶段变体在性能和复杂性之间达到了最佳平衡，Dice 和 IoU 分数分别达到 86.47% 和 78.2%。消融研究进一步验证了文本引导和多模态融合的重要性。这些研究结果强调了视觉-语言集成在推进医学图像分割和支持具有临床意义的诊断工具方面的潜力。

## Abstract
Precise medical image segmentation is fundamental for enabling computer aided diagnosis and effective treatment planning. Traditional models that rely solely on visual features often struggle when confronted with ambiguous or low contrast patterns. To overcome these limitations, we introduce SwinTextUNet, a multimodal segmentation framework that incorporates Contrastive Language Image Pretraining (CLIP), derived textual embeddings into a Swin Transformer UNet backbone. By integrating cross attention and convolutional fusion, the model effectively aligns semantic text guidance with hierarchical visual representations, enhancing robustness and accuracy. We evaluate our approach on the QaTaCOV19 dataset, where the proposed four stage variant achieves an optimal balance between performance and complexity, yielding Dice and IoU scores of 86.47% and 78.2%, respectively. Ablation studies further validate the importance of text guidance and multimodal fusion. These findings underscore the promise of vision language integration in advancing medical image segmentation and supporting clinically meaningful diagnostic tools.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SwinTextUNet: Integrating CLIP-Based Text Guidance into Swin Transformer U-Nets for Medical Image Segmentation》** 的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
医学图像分割（如病灶识别）对于临床诊断至关重要。然而，传统的卷积神经网络（CNN）或纯视觉 Transformer 模型（如 Swin-UNet）存在一个显著局限：**它们仅依赖视觉特征**。在面对低对比度、噪声干扰或边界模糊的医学图像时，仅靠像素信息往往难以实现精准分割。
本研究的动机在于：临床医生在诊断时不仅看图，还会参考放射报告等文本描述。因此，论文提出了 **SwinTextUNet**，旨在通过引入 **CLIP（对比语言-图像预训练）** 提取的文本语义引导，增强模型在复杂医学场景下的鲁棒性和分割精度。

### 2. 方法论
SwinTextUNet 结合了分层视觉表示和多模态语义对齐，核心架构包含以下四个关键部分：
*   **CLIP 文本编码器**：使用预训练且冻结的 CLIP 文本分支。将专家的临床描述（如“双肺感染，左上肺受累”）转化为高维语义嵌入（Text Embeddings）。
*   **Swin Transformer 骨干网络**：采用分层架构作为编码器，通过移动窗口自注意力（Shifted-Window Self-Attention）捕捉多尺度的全局和局部视觉特征。
*   **文本引导交叉注意力（Cross-Attention）**：这是核心对齐模块。在编码器的每个阶段，视觉 Token 作为 Query，文本 Token 作为 Key 和 Value，通过交叉注意力机制让文本语义“指导”视觉特征的关注区域。
*   **卷积融合模块（ConvFuse）**：在解码器阶段，该模块通过 3×3 卷积层融合来自跳跃连接（Skip Connection）的特征图和上采样特征，利用卷积的平移不变性来优化边界重建。
*   **损失函数**：采用混合损失函数 $L = \lambda_{dice} L_{Dice} + \lambda_{ce} L_{CE}$，平衡了区域重叠度与像素级分类准确性。

### 3. 实验设计
*   **数据集**：使用 **QaTa-COV19** 数据集。该数据集包含 9,258 张胸部 X 光片（CXR）及其对应的 COVID-19 病灶掩码，并配有由医学专家编写的文本标注（描述感染位置、数量和侧性）。
*   **Benchmark 与对比方法**：
    *   **传统 CNN**：U-Net, Attention U-Net。
    *   **多模态/Transformer 类**：CLIP（直接用于分割的变体）、LViT（语言引导的视觉 Transformer）。
*   **评估指标**：Dice 相似系数（Dice）和交并比（IoU）。

### 4. 资源与算力
*   **软件框架**：基于 PyTorch 实现。
*   **超参数**：使用 AdamW 优化器，学习率 $1 \times 10^{-4}$，权重衰减 $1 \times 10^{-2}$，采用带预热（Warmup）的余弦退火策略，Batch Size 为 8，训练 100 个 Epoch。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 NVIDIA A100 或 RTX 3090）及具体的训练总时长。

### 5. 实验数量与充分性
论文进行了多维度的实验验证：
*   **深度探索实验**：对比了 3 阶段、4 阶段和 5 阶段的架构，最终确定 4 阶段为性能与复杂度的平衡点。
*   **消融实验**：分别移除了“文本引导”、“ConvFuse 模块”和“交叉注意力机制”，结果显示移除文本引导会导致 Dice 分数大幅下降 7.3%，证明了多模态信息的关键作用。
*   **基准对比**：与 4 种主流模型进行了对比。
*   **充分性评价**：实验设计较为客观，涵盖了定量指标、定性可视化（预测图对比）和收敛曲线分析。但实验**仅限于单一数据集**（QaTa-COV19），缺乏在其他器官或模态（如 MRI、CT）上的泛化性验证。

### 6. 主要结论与发现
*   **多模态优势**：引入文本语义显著提升了分割性能，SwinTextUNet 在测试集上达到了 **86.47% 的 Dice** 和 **78.2% 的 IoU**，优于所有对比基准。
*   **架构优化**：4 阶段的 Swin 结构在参数量（57.6M）和精度之间达到了最优平衡。
*   **鲁棒性**：定性结果显示，在文本引导下，模型对多灶性感染和复杂边界的捕捉更加准确，产生的假阳性更少。

### 7. 优点
*   **创新性对齐**：成功将 CLIP 的零样本/弱监督语义能力引入到全监督的医学分割任务中。
*   **模块化设计**：Cross-Attention 与 ConvFuse 的结合既保留了 Transformer 的全局建模能力，又发挥了 CNN 在局部细节修复上的优势。
*   **临床相关性**：利用放射报告文本作为输入，符合实际临床诊断的工作流。

### 8. 不足与局限
*   **数据依赖性**：模型高度依赖高质量的文本标注。在实际临床中，并非所有影像都配有标准化的描述文本，这限制了其即时应用。
*   **泛化验证不足**：仅在 2D X 光片数据集上测试，未在 3D 医学影像（医学分割的主流领域）或多中心数据上验证。
*   **计算开销**：虽然比 5 阶段模型轻量，但 57.6M 的参数量对于某些嵌入式医疗设备而言仍然较大。

（完）
