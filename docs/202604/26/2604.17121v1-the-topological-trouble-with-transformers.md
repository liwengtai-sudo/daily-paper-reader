---
title: The Topological Trouble With Transformers
title_zh: Transformer 的拓扑困境
authors: "Michael C. Mozer, Shoaib Ahmed Siddiqui, Rosanne Liu"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17121v1"
tags: ["query:dl"]
score: 9.0
evidence: 分析Transformer架构在状态跟踪中的局限性
tldr: 本文探讨了Transformer在前馈架构下处理动态状态追踪的局限性，指出其必须通过增加深度来应对演化的状态，最终会导致深度耗尽。作者主张将研究重心从显式思维链转向基于循环架构的隐式激活态动力学，并提出了一套涵盖深度与步进维度的循环Transformer分类法，旨在通过增强状态空间模型等手段提升基础模型的状态处理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17121v1/fig-001.webp\", \"caption\": \"Figure 5: Unrolling a transformer. Each rectangle represents a transformer layer. The colored boxes represent distinct input tokens. The arrows indicate flow of activity. Note that the figure represents the output of the full transformer layer, which includes both the self-attention as well as the feedforward layer. The gray filled rectangles indicate blocks that can be replaced by the frozen KV cache as their values no longer change. The light grey unfilled rectangles represent input steps that are ignored at a given unrolling step. (a) A transformer with recurrence that allows activation from a deep layer to influence a shallow layer. (b) An unrolled depth-recurrent transformer. (c) An unrolled blockwise-recurrent transformer. (d) A transformer with potential attractor dynamics.\", \"page\": 6, \"index\": 1, \"width\": 829, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2604.17121v1/fig-002.webp\", \"caption\": \"Figure 6: Unrolling a latent-thought model, where the model feeds back its latent thoughts as input to the model for multiple auto-regressive steps before processing the next input token, which is marked with the blue color (e.g., Hao et al., 2025; Jolicoeur-Martineau, 2025; Galashov et al., 2025).\", \"page\": 7, \"index\": 2, \"width\": 826, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2604.17121v1/fig-003.webp\", \"caption\": \"Figure 7: Unrolling an SSM, where information from the previous input step at layer l flows directly to the next step in the horizontal direction (e.g., Allen-Zhu, 2025; Gu and Dao, 2024).\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 231}, {\"url\": \"assets/figures/arxiv/2604.17121v1/fig-004.webp\", \"caption\": \"Figure 1: (a) Schematic depiction of transformer decoder architecture with input steps along horizontal axis and blocks (layers) along vertical axis. Activation flows upwards, from shallow to deep layers. For three selected blocks outlined in color, the functional input connectivity of a causal model is shown by colored lines and shading of the input sources. (b) The propagation of state representations, indicated by the green rectangles. In cases where these representations depend on previous states and current inputs, the state tracking capability of a feedforward transformer is fundamentally limited by the depth of the model.\", \"page\": 3, \"index\": 4, \"width\": 752, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.17121v1/fig-005.webp\", \"caption\": \"Table 1: A taxonomy of recurrent transformer architectures, with selected examples. Note that ratio > 1 indicates processing multiple tokens per recurrence step, while ratio < 1 indicates multiple recurrence steps per token.\", \"page\": 8, \"index\": 5, \"width\": 827, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.17121v1/fig-006.webp\", \"caption\": \"Figure 2: Two examples from the literature: Li et al. (2025a) and Lindsey et al. (2025). The details of each Figure are not critical, but in each case, the upward flow of information to deeper layers is depicted.\", \"page\": 4, \"index\": 6, \"width\": 826, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2604.17121v1/fig-007.webp\", \"caption\": \"Figure 4: Unrolling a three-neuron recurrent neural net (left) into its equivalent feedforward network (right). Figure adapted from Rumelhart et al. (1986).\", \"page\": 5, \"index\": 7, \"width\": 756, \"height\": 315}, {\"url\": \"assets/figures/arxiv/2604.17121v1/fig-008.webp\", \"caption\": \"Figure 3: The depth of a state representation in a transformer can limit its utility for inference (adapted from Lepori et al., 2025). The input tokens are presented at the bottom, processed sequentially by the layers of the model. The symbols within the layers represent the model’s internal belief state. This highlights that despite the model converging to the correct belief (river bank) deep in the network at the “bank” token, this high-level disambiguation is inaccessible to the shallower layers of subsequent tokens. Consequently, when processing the final tokens, the model defaults to superficial associations (money bank → ATM) early in the stack, resulting in an incorrect prediction at the last token position.\", \"page\": 5, \"index\": 8, \"width\": 640, \"height\": 414}]"
motivation: Transformer的纯前馈架构在处理具有序列依赖的动态状态追踪时，会迫使状态表示不断向深层堆叠，导致计算效率低下且受限于模型深度。
method: 提出通过循环架构实现隐式激活态动力学，并根据循环轴（深度与步进）及输入与循环步数的比例对相关架构进行分类。
result: 分析表明现有的动态深度或显式思考方案在计算和内存上均不经济，而循环机制能更有效地维持随时间演化的环境状态。
conclusion: 未来基础模型的研究应聚焦于增强状态空间模型和粗粒度循环，以更好地将状态追踪能力整合进现代架构中。
---

## 摘要
Transformer 通过不断扩展的上下文历史来编码序列中的结构。然而，其纯前馈架构从根本上限制了动态状态追踪。状态追踪——即反映演变环境的隐变量的迭代更新——涉及前馈网络难以维持的固有序列依赖性。因此，随着每个新的输入步骤，前馈模型将演变的状态表示推向其层堆栈的更深处，导致浅层无法获取信息，并最终耗尽模型的深度。虽然这种深度限制可以通过动态深度模型以及将状态表示外部化的显式或隐式思考来规避，但这些解决方案在计算和内存上效率较低。在本文中，我们认为时间延伸认知需要将重点从显式思维轨迹转向通过循环架构实现的隐式激活态动力学。我们介绍了一种循环和连续思考 Transformer 架构的分类法，根据它们的循环轴（深度与步骤）以及输入 token 与循环步骤的比率进行分类。最后，我们概述了具有前景的研究方向，包括增强的状态空间模型和粗粒度循环，以更好地将状态追踪集成到现代基础模型中。

## Abstract
Transformers encode structure in sequences via an expanding contextual history. However, their purely feedforward architecture fundamentally limits dynamic state tracking. State tracking -- the iterative updating of latent variables reflecting an evolving environment -- involves inherently sequential dependencies that feedforward networks struggle to maintain. Consequently, feedforward models push evolving state representations deeper into their layer stack with each new input step, rendering information inaccessible in shallow layers and ultimately exhausting the model's depth. While this depth limit can be bypassed by dynamic depth models and by explicit or latent thinking that externalizes state representations, these solutions are computationally and memory inefficient. In this article, we argue that temporally extended cognition requires refocusing from explicit thought traces to implicit activation dynamics via recurrent architectures. We introduce a taxonomy of recurrent and continuous-thought transformer architectures, categorizing them by their recurrence axis (depth versus step) and their ratio of input tokens to recurrence steps. Finally, we outline promising research directions, including enhanced state-space models and coarse-grained recurrence, to better integrate state tracking into modern foundation models.

---

## 论文详细总结（自动生成）

这是一份关于 Google DeepMind 发表的论文《The Topological Trouble With Transformers》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：Transformer 的纯前馈（Feedforward）架构在处理**动态状态追踪（State Tracking）**时存在根本性的拓扑缺陷。
*   **研究动机**：人类认知被视为一个动态系统，状态随时间迭代更新（$s_t = f(s_{t-1}, x_t)$）。然而，Transformer 依赖注意力机制从静态上下文窗口检索信息，而非显式维护演化的状态。
*   **背景**：在前馈网络中，为了让当前状态 $s_t$ 依赖于前一状态 $s_{t-1}$，信息必须向更高层（更深层）传递。这意味着随着序列增长，状态表示会不断“向上”堆叠，最终导致浅层无法获取最新状态，且受限于模型的物理深度。

### 2. 论文提出的方法论
论文并未提出一个单一的新算法，而是提出了一套**架构分类法（Taxonomy）**和**转向循环动力学**的理论框架：
*   **核心思想**：主张将研究重心从“显式思维链”（如 CoT，通过输出 Token 外部化状态）转向“隐式激活态动力学”（通过循环架构在内部维持状态）。
*   **架构分类法**：根据两个维度对循环 Transformer 进行分类：
    1.  **循环轴（Recurrence Axis）**：
        *   **深度循环（Depth）**：如 Looped Transformer，在层间循环。
        *   **步进循环（Step）**：如 SSM（状态空间模型）、Mamba，在时间步间循环。
        *   **深度+步进循环**：如 Latent-thought 模型。
    2.  **输入 Token 与循环步数的比率**：
        *   **> 1**：多个 Token 对应一次循环（如块循环）。
        *   **= 1**：标准循环（如 RNN/SSM）。
        *   **< 1**：一个 Token 对应多次循环（如“思考”模型，在处理下一个 Token 前进行多次内部迭代）。

### 3. 实验设计与证据支持
由于本文属于**观点/综述性论文（Position Paper）**，其论证主要基于案例分析和对前人研究的综合：
*   **案例分析**：
    *   **Gemini 3 失败案例**：展示了模型在“猜数字”游戏中即便有“思维链”也无法维持一致的状态追踪。
    *   **歧义词消歧（Bank 案例）**：引用 Lepori 等人的研究，证明模型在深层才完成消歧，导致后续 Token 在浅层处理时仍基于错误假设，产生“翻车”现象。
*   **理论引用**：引用了 Merrill 和 Sabharwal 关于 Transformer 表达能力的证明，指出前馈模型识别正则语言需要 $\log n$ 层深度，这在处理长序列时会耗尽深度资源。
*   **对比对象**：对比了标准 Transformer、SSM（状态空间模型）、思维链（CoT）以及各种变体（如 Looped Transformer, Mamba, RWKV-7）。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的训练算力、GPU 型号或时长。
*   **原因**：本文侧重于架构分析和理论推导，而非发布一个需要大规模预训练的新模型。其引用的案例（如 Gemini 3）是基于已发布的商业/研究模型。

### 5. 实验数量与充分性
*   **实验规模**：本文不包含传统意义上的大规模消融实验。它通过逻辑推导、拓扑示意图（Figure 1 & 5）以及针对性的失败案例来支撑论点。
*   **客观性与公平性**：论证过程较为客观，承认了 Transformer 现有的成功（如通过上下文检索模拟状态追踪），但也清晰地指出了这种“捷径”在处理本质上属于序列依赖任务时的局限性。

### 6. 论文的主要结论与发现
1.  **深度瓶颈**：前馈 Transformer 将演化的状态推向深层，导致浅层信息缺失，最终受限于模型深度。
2.  **检索非追踪**：Transformer 的成功很大程度上是将状态追踪问题转化为了“工作记忆检索”问题，但这在计算上是不经济的。
3.  **循环的必要性**：要实现无限长的状态追踪和高效的推理，必须引入某种形式的循环（Recurrence），使信息能从深层回流到浅层。
4.  **未来方向**：看好增强型 SSM（如 RWKV-7）、粗粒度循环（按句子或“思想”块更新）以及利用残差连接带来的表示对齐。

### 7. 优点
*   **视角独特**：从拓扑结构（Topological）的角度解释了 Transformer 为什么会在多轮对话或复杂逻辑中失去连贯性。
*   **分类清晰**：提出的二维分类法为理解当前琳琅满目的“循环 Transformer”变体提供了统一的框架。
*   **直击痛点**：指出了当前大模型过度依赖显式 CoT（浪费 Token 和计算量）的本质原因。

### 8. 不足与局限
*   **缺乏实证数据**：作为观点论文，没有提供新架构在标准 Benchmark（如 MMLU, GSM8K）上的量化改进数据。
*   **训练难题**：虽然提倡循环架构，但文中也承认循环架构在并行训练上存在天然劣势，提出的“多阶段训练”等解决方案尚需实践验证。
*   **应用限制**：文中讨论的状态追踪主要集中在逻辑一致性和语言理解上，对于感知类任务（如图像识别）的适用性讨论较少。

（完）
