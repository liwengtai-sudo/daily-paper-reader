---
title: "Attend what matters: Leveraging vision foundational models for breast cancer classification using mammograms"
title_zh: 关注重点：利用视觉基础模型进行乳房 X 线摄影乳腺癌分类
authors: "Samyak Sanghvi, Piyush Miglani, Sarvesh Shashikumar, Kaustubh R Borgavi, Veenu Singla, Chetan Arora"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19350v1"
tags: ["query:ai-pm"]
score: 10.0
evidence: 利用乳腺钼靶和Vision Transformer进行乳腺癌分类
tldr: 针对乳腺癌筛查中视觉Transformer（ViT）难以处理高分辨率图像和小病灶的问题，本文提出一种新框架。通过目标检测模型引导RoI区域的Token缩减，结合对比学习增强细粒度判别力，并利用DINOv2预训练模型提取局部感知特征。实验证明该方法在多个公共乳腺钼靶数据集上优于现有基准，具有显著的临床应用潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19350v1/fig-001.webp\", \"caption\": \"Table 1. Performance comparison on the VinDR dataset. ViT-A and ViT-B correspond to DINOv2 [1] with a linear layer at input resolutions of 448×448 and 1024×1024 respectively. ViT-C demonstrates scores using a DeIT head instead of a linear layer at 448×448 input resolution.\", \"page\": 3, \"index\": 1, \"width\": 508, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2604.19350v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview. In our proposed method, we first use G-DINO in a zero-shot setting to localize semantically important Regions of Interest (RoIs) based on a clinically meaningful text prompt. The top-k RoIs, selected by bounding box confidence scores, are cropped and passed through a pretrained DINOv2 encoder to extract k feature vectors. These features are processed by a transformer module, enabling interaction among regions through self-attention. The global RoI (covering the full breast) is used as the final representation which is contextually enriched by fine-grained lesion-level RoIs, and is fed into a Multi-Layer Perceptron (MLP) for classification.\", \"page\": 3, \"index\": 2, \"width\": 1016, \"height\": 391}]"
motivation: 传统ViT在乳腺癌诊断中受限于高分辨率图像带来的Token冗余以及医学图像分类中极高的细粒度区分难度。
method: 结合基于RoI的Token缩减技术、RoI间的对比学习策略以及DINOv2预训练模型来增强局部特征提取和判别能力。
result: 在公开乳腺钼靶数据集上的实验结果表明，该方法在分类准确率和鲁棒性上均显著超越了现有的基准模型。
conclusion: 该框架通过精准定位关键区域和强化细粒度特征学习，有效提升了乳腺癌自动分类的性能，为大规模临床筛查提供了有力支持。
---

## 摘要
Vision Transformer (ViT) 已成为许多计算机视觉任务的首选架构，但其在计算机辅助诊断中的表现仍然有限。针对乳房 X 线摄影中的乳腺癌检测，我们确定了导致这一不足的两个主要原因。首先，医学图像具有高分辨率且异常区域微小，导致 token 数量过多，使得基于 softmax 的注意力机制难以定位并关注相关区域。其次，医学图像分类本质上是细粒度的，具有低类间差异和高类内差异，标准的交叉熵训练不足以应对。为了克服这些挑战，我们提出了一个包含三个关键组件的框架：(1) 使用目标检测模型引导注意力，进行基于感兴趣区域 (RoI) 的 token 缩减；(2) 在选定的 RoI 之间进行对比学习，通过基于难负样本 (hard-negative) 的训练来增强细粒度辨别能力；(3) 使用 DINOv2 预训练的 ViT，以捕获具有定位感知能力的细粒度特征，而非全局 CLIP 表示。在公开乳房 X 线摄影数据集上的实验表明，我们的方法优于现有的基准模型，证明了其在规模化乳腺癌筛查中的有效性和潜在临床应用价值。我们的代码已发布以供复现：https://aih-iitd.github.io/publications/attend-what-matters

## Abstract
Vision Transformers $(\texttt{ViT})$ have become the architecture of choice for many computer vision tasks, yet their performance in computer-aided diagnostics remains limited. Focusing on breast cancer detection from mammograms, we identify two main causes for this shortfall. First, medical images are high-resolution with small abnormalities, leading to an excessive number of tokens and making it difficult for the softmax-based attention to localize and attend to relevant regions. Second, medical image classification is inherently fine-grained, with low inter-class and high intra-class variability, where standard cross-entropy training is insufficient. To overcome these challenges, we propose a framework with three key components: (1) Region of interest $(\texttt{RoI})$ based token reduction using an object detection model to guide attention; (2) contrastive learning between selected $\texttt{RoI}$ to enhance fine-grained discrimination through hard-negative based training; and (3) a $\texttt{DINOv2}$ pretrained $\texttt{ViT}$ that captures localization-aware, fine-grained features instead of global $\texttt{CLIP}$ representations. Experiments on public mammography datasets demonstrate that our method achieves superior performance over existing baselines, establishing its effectiveness and potential clinical utility for large-scale breast cancer screening. Our code is available for reproducibility here: https://aih-iitd.github.io/publications/attend-what-matters

---

## 论文详细总结（自动生成）

这是一份关于论文《Attend what matters: Leveraging vision foundational models for breast cancer classification using mammograms》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**乳腺癌筛查（BCDM）**中视觉Transformer（ViT）表现受限的问题。研究者指出两个核心痛点：
*   **Token冗余与注意力分散**：乳腺钼靶图像分辨率极高（如4K），而病灶区域极小（仅占约0.1%）。直接输入ViT会导致Token数量巨大，Softmax注意力机制难以在海量背景中精准定位微小异常。
*   **细粒度分类挑战**：医学图像具有极高的类内差异和极低的类间差异，传统的交叉熵损失函数难以捕捉区分恶性与良性特征所需的细微判别信息。

### 2. 方法论：核心思想与关键技术
该研究提出了一种结合目标检测引导和对比学习的框架，核心流程如下：
*   **RoI引导的Token缩减**：使用预训练的 **Grounding DINO (G-DINO)** 模型，根据临床文本提示（如“肿块”、“钙化”）在全分辨率图像中提取前 $k$ 个感兴趣区域（RoI）。这有效过滤了背景噪声，将注意力集中在具有诊断潜力的区域。
*   **局部感知特征提取**：采用 **DINOv2** 预训练的ViT作为骨干网络。相比于侧重全局语义的CLIP，DINOv2在预训练阶段包含了大量定位感知任务，更擅长提取细粒度的局部特征。
*   **特征聚合与位置编码**：将提取的RoI嵌入向量序列输入Transformer块。引入**旋转位置编码（RoPE）**来隐式编码RoI之间的相对空间关系。
*   **损失函数设计**：
    *   **BCE Loss**：用于最终的二分类任务。
    *   **排斥性对比损失（Repulsive Contrastive Loss, RCL）**：计算不同RoI嵌入之间的余弦相似度平方和，旨在推开不同提案的特征分布，防止特征坍缩，并强制模型从“硬负样本”（即不含肿瘤但具有干扰性的RoI）中学习。

### 3. 实验设计
*   **数据集**：主要在公开的大规模乳腺钼靶数据集 **VinDR-Mammo** 上进行验证。
*   **Benchmark与对比方法**：
    *   **纯视觉模型**：ViT-A/B/C（基于DINOv2的不同配置）、MedVAE、TReg-SwinT、XFMamba。
    *   **图文多模态模型**：MMBCD、Mammo-CLIP（之前的SOTA）。
*   **评估指标**：AUC、F1-score 以及不同召回率水平下的表现（R@0.1等）。

### 4. 资源与算力
*   **文中说明**：论文**未明确提及**具体的GPU型号、数量或训练总时长。
*   **推测**：考虑到使用了DINOv2和G-DINO等大型基础模型，且处理的是高分辨率RoI，预计实验至少需要具备16GB以上显存的工业级GPU（如A100或RTX 3090/4090级别）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主流基准模型的全面对比（涵盖了最新的Mamba架构和多模态架构）。
    *   **消融实验**：详细测试了DINOv2、G-DINO、Anchor选择、RoPE和RCL损失函数对性能的贡献（见表2）。
*   **充分性评价**：实验设计较为客观。通过消融实验清晰地展示了每个组件的增益（如RCL提升了1%的AUC和F1）。虽然主要集中在VinDR数据集，但对比的对象包括了需要私有大规模数据预训练的SOTA模型（Mammo-CLIP），证明了该方法在仅使用公开数据时的优越性。

### 6. 主要结论与发现
*   **性能超越SOTA**：该方法在AUC上达到了86.6%，F1分数达到54.5%，相比之前的SOTA（Mammo-CLIP）在F1分数上实现了**4%的显著提升**，AUC提升了1%。
*   **局部特征的重要性**：实验证明，使用专注于局部特征的DINOv2比使用全局CLIP表示更适合医学细粒度分类。
*   **RoI策略的有效性**：通过G-DINO筛选RoI不仅降低了计算开销，还通过减少无关Token干扰直接提升了分类精度。

### 7. 优点（亮点）
*   **高效的Token管理**：巧妙利用目标检测模型作为预处理器，解决了高分辨率医学图像与Transformer计算复杂度之间的矛盾。
*   **无需私有数据**：在不依赖大型私有图文数据集预训练的情况下，超越了依赖私有数据的多模态模型，具有更好的可复现性和开放性。
*   **对比学习创新**：引入排斥性对比损失（RCL）来处理医学图像中的硬负样本问题，增强了模型的判别力。

### 8. 不足与局限
*   **依赖预训练检测器**：框架的性能在很大程度上取决于G-DINO提取RoI的质量。如果检测器漏掉微小病灶，后续分类器将无法弥补。
*   **计算流程复杂**：相比于端到端的分类网络，该框架包含检测、裁剪、特征提取、Transformer聚合等多个阶段，推理流水线较长。
*   **泛化性验证**：虽然在VinDR上表现优异，但未在更多样化的跨中心数据集上验证其对不同设备、不同人群的泛化鲁棒性。

（完）
