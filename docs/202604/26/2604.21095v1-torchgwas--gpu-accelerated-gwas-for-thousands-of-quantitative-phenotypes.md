---
title: "TorchGWAS : GPU-accelerated GWAS for thousands of quantitative phenotypes"
title_zh: TorchGWAS：针对数千种数量表型的 GPU 加速全基因组关联分析
authors: "Xingzhong Zhao, Ziqian Xie, Islam, Sheikh Muhammad Saiful, Tian Xia, Chen, Cheng, Degui Zhi"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.21095v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于生物信息学工作流的GPU加速GWAS
tldr: 针对现代生物信息学中数千个表型关联分析的计算瓶颈，本文开发了TorchGWAS框架。该工具利用GPU硬件加速，在处理相同基因型矩阵对应的大规模表型面板时表现卓越。通过Python和命令行接口，它支持多种输入格式并能自动调整协变量，相比传统工具实现了数百至上千倍的吞吐量提升，极大提高了大规模表型筛选的效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21095v1/fig-001.webp\", \"caption\": \"Figure 2. Validation and benchmarking of TorchGWAS. Left: Comparison of SNP association significance estimated by TorchGWAS and PLINK, showing near-perfect agreement for representative archived traits. Right: Runtime comparison of TorchGWAS and fastGWA for 2,048 and 20,480 phenotypes. Runtimes are shown on a log scale.\", \"page\": 3, \"index\": 1, \"width\": 620, \"height\": 253}]"
motivation: 传统的GWAS工具在处理由影像学或表示学习产生的成千上万个定量表型时，由于无法有效复用基因型矩阵，导致计算效率低下。
method: 开发了基于Python和PyTorch的TorchGWAS框架，利用GPU加速线性GWAS和多变量表型筛选，并支持多种基因型输入格式及内部协变量调整。
result: "在包含890万个标记和2.3万个样本的测试中，TorchGWAS在单块A100 GPU上处理20,480个表型仅需20分钟，较传统工具实现300至1700倍的吞吐量提升。"
conclusion: TorchGWAS为大规模定量表型的高通量GWAS筛选提供了高效实用的解决方案，显著降低了计算成本和时间。
---

## 摘要
动机：现代生物信息学工作流，特别是在影像学和表示学习领域，可以从单一队列中生成数千到数万个数量表型。在这种情况下，逐个性状进行全基因组关联分析（GWAS）会迅速成为计算瓶颈。虽然现有的 GWAS 工具对单个性状非常有效，但它们并未针对表型丰富的筛选工作流进行优化，而在这种工作流中，同一基因型矩阵会在大型表型面板中被重复使用。结果：我们提出了 TorchGWAS，这是一个通过硬件加速对大型表型面板进行高通量关联测试的框架。当前公开发布的版本为线性 GWAS 和多变量表型筛选提供了稳定的 Python 和命令行工作流，支持 NumPy、PLINK 和 BGEN 基因型输入，通过样本标识符对齐表型和协变量表，并在内部执行协变量调整。在包含 890 万个标记和 23,000 个样本的基准测试中，fastGWA 在 AMD EPYC 7763 64 核 CPU 上处理每个表型大约需要 100 秒，而 TorchGWAS 在单个 NVIDIA A100 GPU 上仅用 10 分钟就完成了 2,048 个表型，20 分钟完成了 20,480 个表型，这相当于表型吞吐量提高了约 300 到 1700 倍。因此，TorchGWAS 使得在需要高效评估数千个数量性状的表型丰富场景下，进行大规模 GWAS 筛选变得切实可行。可用性与实现：TorchGWAS 使用 Python 实现，并作为带有文档的源码仓库发布在 https://github.com/ZhiGroup/TorchGWAS。当前版本提供了命令行界面、打包的源代码、教程、基准测试脚本和示例工作流。

## Abstract
Motivation: Modern bioinformatics workflows, particularly in imaging and representation learning, can generate thousands to tens of thousands of quantitative phenotypes from a single cohort. In such settings, running genome-wide association analyses trait by trait rapidly becomes a computational bottleneck. While established GWAS tools are highly effective for individual traits, they are not optimized for phenotype-rich screening workflows in which the same genotype matrix is reused across a large phenotype panel. Results: We present TorchGWAS, a framework for high-throughput association testing of large phenotype panels through hardware acceleration. The current public release provides stable Python and command-line workflows for linear GWAS and multivariate phenotype screening, supports NumPy, PLINK, and BGEN genotype inputs, aligns phenotype and covariate tables by sample identifier, and performs covariate adjustment internally. In a benchmark with 8.9 million markers and 23,000 samples, fastGWA required approximately 100 second per phenotype on an AMD EPYC 7763 64-core CPU, whereas TorchGWAS completed 2,048 phenotypes in 10 minute and 20,480 phenotypes in 20 minutes on a single NVIDIA A100 GPU, corresponding to an approximately 300- to 1700-fold increase in phenotype throughput. TorchGWAS therefore makes large-scale GWAS screening practical in phenotype-rich settings where thousands of quantitative traits must be evaluated efficiently. Availability and implementation: TorchGWAS is implemented in Python and distributed as a documented source repository at https://github.com/ZhiGroup/TorchGWAS. The current release provides a command-line interface, packaged source code, tutorials, benchmark scripts, and example workflows.

---

## 论文详细总结（自动生成）

### TorchGWAS：针对大规模定量表型的高通量 GPU 加速框架

#### 1. 核心问题与整体含义（研究动机和背景）
在现代生物信息学研究中，尤其是影像遗传学（Imaging Genetics）和基于深度学习的表示学习领域，研究者往往能从同一批受试者中提取出数千甚至数万个定量表型（如脑部 MRI 的体素特征或学习到的特征向量）。传统的全基因组关联分析（GWAS）工具（如 fastGWA、PLINK）主要针对单个性状或少量性状设计，在处理这种“表型丰富”的场景时，需要对每个性状重复扫描巨大的基因型矩阵，导致计算效率低下，成为研究流程中的主要瓶颈。

#### 2. 论文提出的方法论
TorchGWAS 的核心思想是将关联测试重组为**批量矩阵运算**，通过复用基因型矩阵和预处理结果来最大化 GPU 的并行计算能力。
*   **核心流程**：
    1.  **预处理**：对输入的表型矩阵 $Y$ 进行均值中心化，并利用协变量空间的规范正交基 $Q$ 去除协变量效应（残差化），随后进行标准化。
    2.  **矩阵化关联核**：将基因型 $G$ 分批加载并标准化。核心计算公式为 $R = G_s Y_s / N$，即通过一次矩阵乘法直接获得所有基因型与所有表型之间的相关系数矩阵 $R$。
    3.  **统计量转换**：将相关系数 $R$ 转换为 t 统计量：$T = R \sqrt{\frac{N-2}{1-R^2}}$，进而计算 P 值。
*   **技术细节**：支持 PLINK (.bed)、BGEN 和 NumPy 格式输入；内部自动执行样本 ID 对齐和协变量调整；利用 PyTorch 框架实现硬件加速。

#### 3. 实验设计
*   **数据集**：使用了包含 890 万个遗传标记（Markers）和约 23,000 个样本的真实数据集。
*   **Benchmark（基准测试）**：
    *   **准确性验证**：将 TorchGWAS 的结果与 PLINK 的输出进行对比。
    *   **性能对比**：对比了传统工具 **fastGWA**（在多核 CPU 上运行）与 TorchGWAS（在 GPU 上运行）的处理耗时。
*   **测试场景**：分别测试了处理 2,048 个和 20,480 个定量表型的运行时间。

#### 4. 资源与算力
*   **GPU**：使用了**单块 NVIDIA A100 GPU**。
*   **CPU**：对比组 fastGWA 使用了 **AMD EPYC 7763 64 核处理器**。
*   **运行时长**：
    *   在 20,480 个表型的规模下，TorchGWAS 仅需约 **20 分钟**（1108 秒）。
    *   相比之下，fastGWA 处理每个表型约需 100 秒，若处理 20,480 个表型理论上需要数百小时。

#### 5. 实验数量与充分性
论文主要进行了两类核心实验：
1.  **一致性实验**：通过 Pearson 相关系数（r=0.999）证明了其数值计算的准确性。
2.  **扩展性实验**：展示了随着表型数量增加，运行时间的非线性增长趋势（即表型越多，分摊到每个表型的成本越低）。
*   **充分性评价**：实验设计直接针对其“高通量筛选”的定位，对比对象（fastGWA）具有代表性。虽然实验的数据集种类不算多，但对于证明工具的性能提升和数值正确性而言是充分且客观的。

#### 6. 论文的主要结论与发现
*   **吞吐量巨大提升**：在处理大规模表型面板时，TorchGWAS 相比传统 CPU 工具实现了 **300 到 1700 倍**的吞吐量提升。
*   **非线性扩展优势**：由于基因型读取和预处理开销被大量表型平摊，增加表型数量对总耗时的影响远低于线性增长。
*   **实用性**：TorchGWAS 成功将原本需要数周的计算任务缩短至分钟级，使大规模表型筛选变得切实可行。

#### 7. 优点
*   **极高的计算效率**：充分利用 GPU 的张量计算能力，解决了表型爆炸带来的计算瓶颈。
*   **易用性与集成性**：基于 Python 开发，提供命令行接口，兼容主流生物信息学文件格式（PLINK/BGEN）。
*   **流程优化**：内置了样本对齐、协变量处理等步骤，减少了用户手动预处理的工作量。

#### 8. 不足与局限
*   **模型局限性**：目前仅支持**线性回归模型**，尚未实现混合线性模型（LMM）或广义线性模型（GLM），无法直接处理复杂的亲缘关系校正（虽然支持样本剔除）。
*   **硬件依赖**：性能高度依赖于 GPU 显存大小和磁盘 I/O 带宽，在处理超大规模基因型数据时可能受限于显存。
*   **功能定位**：该工具定位为“第一轮快速筛选引擎”，而非最终的精细统计推断工具，对于需要复杂模型校正的场景仍需后续配合其他工具。

（完）
