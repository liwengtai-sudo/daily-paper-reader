---
title: Dynamic Heartbeat Modeling with Recurrent Neural Networks and Inverse Gaussian Point Process Modeling
title_zh: 基于循环神经网络与逆高斯点过程建模的动态心跳建模
authors: "Runwei Lin, Ying Wang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13815v1"
tags: ["query:dl"]
score: 9.0
evidence: 循环神经网络用于序列化心跳建模
tldr: 本研究针对心率变异性（HRV）分析中逆高斯过程（IGP）参数估计的难题，提出将循环神经网络（RNN）与IGP点过程模型相结合。通过评估GRU、LSTM、S4和Mamba四种架构在R-R间期序列建模中的表现，证明了神经序列模型能有效识别IGP的时变参数。该方法为概率性HRV建模提供了灵活框架，有助于未来整合更复杂的生理机制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13815v1/fig-001.webp\", \"caption\": \"Figure 1. The R–R sequence is encoded by a sequence model backbone and mapped to time-varying inverse Gaussian parameters. Separate branches are used to estimate the mean and the log-variance.\", \"page\": 2, \"index\": 1, \"width\": 465, \"height\": 428}, {\"url\": \"assets/figures/arxiv/2604.13815v1/fig-002.webp\", \"caption\": \"Table 2. Test results (Mean [SD]) of KS distance with the testing segment of 1800 beats. The last row reports the across-subject mean of subject-level mean KSD.\", \"page\": 3, \"index\": 2, \"width\": 525, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2604.13815v1/fig-003.webp\", \"caption\": \"Table 1. Test results (Mean [SD]) of KS distance with the testing segment of 600 beats. The last row reports the across-subject mean of subject-level mean KSD.\", \"page\": 3, \"index\": 3, \"width\": 525, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2604.13815v1/fig-004.webp\", \"caption\": \"Figure 2. A representative example of the testing results whose length including 1800 beats from comparing the predicted µt and the measured R–R interval series from subject 16483. Blue: Measured R-R; Orange: µt prediction. (a) GRU, (b) LSTM, (c) S4, and (d) Mamba.\", \"page\": 4, \"index\": 4, \"width\": 390, \"height\": 428}]"
motivation: 旨在解决逆高斯过程在心跳建模中难以准确估计时变参数的挑战，以提升心率变异性分析的准确性。
method: 将GRU、LSTM、S4和Mamba四种神经序列模型与IGP框架结合，用于动态识别心跳过程中的概率参数。
result: 实验通过KS统计量验证，证明了结合RNN的IGP模型能够有效捕捉R-R间期的动态变化并实现精确的概率建模。
conclusion: 该研究展示了深度学习与生理点过程模型结合的潜力，为未来研究复杂生理机制下的心率动态提供了灵活的基础。
---

## 摘要
心率变异性（HRV）分析对于评估自主神经系统对心血管的调节具有重要意义。逆高斯过程（IGP）已被广泛用于逐搏（beat-to-beat）HRV 建模，因为它为心脏去极化过程提供了具有生理相关性的解释。基于 IGP 的心跳建模面临的一个关键挑战是时变参数的准确估计。在本研究中，我们探讨了循环神经网络（RNNs）是否可用于 IGP 参数识别，从而增强 R-R 间期动力学的概率建模。具体而言，我们利用 Kolmogorov-Smirnov 统计量评估了四种代表性的 RNN 架构，即 GRU、LSTM、结构化状态空间序列模型（S4）和 Mamba。结果表明，将神经序列模型与 IGP 框架相结合进行逐搏 R-R 序列建模是可行的。该方法为概率性 HRV 建模提供了灵活的基础，并为未来整合更复杂的生理机制和动态条件奠定了基础。

## Abstract
Heart rate variability (HRV) analysis is important for the assessment of autonomic cardiovascular regulation. The inverse Gaussian process (IGP) has been widely used for beat-to-beat HRV modeling, as it gives a physiological relevant interpretation of heart depolarization process. A key challenge in IGP-based heartbeat modeling is the accurate estimation of time-varying parameters. In this study, we investigated whether recurrent neural networks (RNNs) can be used for IGP parameter identification and thereby enhance probabilistic modeling of R-R dynamics. Specifically, four representative RNN architectures, namely, GRU, LSTM, Structured State Space sequence model (S4), and Mamba, were evaluated using the Kolmogorov-Smirnov statistics. The results demonstrate the possibility of combining neural sequence models with the IGP framework for beat-wise R-R series modeling. This approach provides a flexible basis for probabilistic HRV modeling and for future incorporation of more complex physiological mechanisms and dynamic conditions.

---

## 论文详细总结（自动生成）

### 论文结构化总结：基于循环神经网络与逆高斯点过程建模的动态心跳建模

#### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何在心率变异性（HRV）分析中，更准确地估计**逆高斯点过程（IGP）**的时变参数。
*   **背景**：HRV 是评估自主神经调节的关键指标。IGP 模型因其物理意义（模拟窦房结去极化至阈值的过程）被广泛用于逐搏（beat-to-beat）建模。
*   **动机**：传统的 IGP 参数估计依赖于线性自回归或简单的非线性多项式扩展，难以捕捉 HRV 信号中高度非线性、非平稳以及个体差异巨大的复杂动力学特征。
*   **目标**：引入深度学习中的循环神经网络（RNN）架构，利用其强大的非线性序列建模能力，动态识别 IGP 参数，从而提升概率建模的精度。

#### 2. 论文提出的方法论
核心思想是将神经序列模型作为特征提取器，直接从历史 R-R 间期序列中推断出 IGP 的时变参数。
*   **核心流程**：
    1.  **输入层**：将 R-R 间期序列进行线性嵌入（Embedding）并进行层归一化（LayerNorm）。
    2.  **骨干网络**：采用 RNN 块（如 GRU、LSTM、S4 或 Mamba）处理嵌入序列，提取隐藏状态 $h_t$。
    3.  **双分支预测头**：
        *   **均值分支**：通过线性层和 Shifted Softplus 激活函数预测条件均值 $\mu_i$，确保其大于 0.3s（符合生理极限）。
        *   **方差分支**：预测对数方差 $\log \sigma_i^2$，进而推导出形状参数 $\lambda_i$。
    4.  **损失函数**：基于逆高斯分布的概率密度函数，构建**负对数似然（NLL）**损失函数，通过反向传播优化网络参数。
*   **关键公式**：利用 IGP 的条件分布 $p(x_{i+1} | x_{1:i})$ 来刻画下一个心跳发生的概率，并通过**时间重标度定理**进行模型评估。

#### 3. 实验设计
*   **数据集**：使用 **MIT-BIH Normal Sinus Rhythm Database**，包含 18 名健康受试者的长期心电图（ECG）数据。
*   **预处理**：Pan-Tompkins 算法识别 R 峰，排除异常值（<0.3s 或 >2s）并进行三次埃尔米特多项式插值。
*   **对比方法（Benchmark）**：对比了四种代表性的序列模型架构：**GRU、LSTM、S4（结构化状态空间模型）和 Mamba**。
*   **评估指标**：采用 **Kolmogorov-Smirnov (KS) 距离**。通过计算拟合后的累积分布函数（CDF）与均匀分布的偏差，评估模型对 R-R 动力学的拟合优度。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或数量。
*   **训练细节**：模型维度固定为 64，单层架构；训练最多 2000 个 epoch，采用早停（Early Stopping）机制；Mamba 和 S4 的状态维度设为 32。

#### 5. 实验数量与充分性
*   **实验规模**：采用了**留一受试者交叉验证（Leave-one-subject-out）**，共进行 18 轮实验，确保每个受试者都作为测试集。
*   **序列长度测试**：测试了四种训练长度（100, 200, 300, 600 拍）和两种测试长度（600, 1800 拍）。
*   **充分性评价**：实验设计较为客观、公平。通过跨受试者的平均 KS 距离和标准差来衡量泛化能力，涵盖了短时和长时的动态评估。

#### 6. 主要结论与发现
*   **可行性验证**：证明了将 RNN 与 IGP 框架结合进行逐搏建模是可行的，性能与以往研究相当。
*   **架构表现**：在测试中，**GRU、LSTM 和 Mamba** 的表现相近且较为稳健，而 **S4** 在该任务上的表现略逊一筹（KS 距离较高）。
*   **训练长度影响**：发现 600 拍的训练长度能产生最低的平均 KS 距离，说明较长的历史背景有助于参数估计。
*   **拟合局限**：并非所有受试者的结果都能通过 KS 显著性阈值，表明单一的 IGP 假设可能无法完全覆盖所有生理复杂性。

#### 7. 优点（亮点）
*   **生理可解释性与灵活性结合**：既保留了 IGP 模型在心脏去极化过程中的生理语义，又利用了深度学习处理非线性动力学的灵活性。
*   **不确定性量化**：通过专门的方差预测分支，模型能够提供心跳预测的不确定性度量。
*   **前瞻性架构**：引入了 Mamba 等最新的线性时间序列模型，探索了其在生物医学信号处理中的潜力。

#### 8. 不足与局限
*   **模型假设限制**：IGP 假设可能过于简化，无法完全捕捉受试者所有的生理调节机制（如呼吸性窦性心律失常）。
*   **输入维度单一**：目前仅使用 R-R 间期历史，未整合呼吸、血压等对心率有重要调节作用的多模态信号。
*   **数据质量敏感**：尽管进行了预处理，但残留的异位搏动或检测错误仍可能干扰模型性能。
*   **受试者范围**：仅在健康受试者数据上验证，尚未在心律失常或自主神经功能障碍的临床患者中测试。

（完）
