---
title: Geometry-Induced Long-Range Correlations in Recurrent Neural Network Quantum States
title_zh: 循环神经网络量子态中的几何诱导长程关联
authors: "Asif Bin Ayub, Amine Mohamed Aboussalah, Mohamed Hibat-Allah"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08661v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于长程相关的空洞循环神经网络
tldr: 针对标准循环神经网络（RNN）量子态难以捕捉长程关联的问题，本文提出了一种空洞循环神经网络（Dilated RNN）波函数。该方法通过引入空洞连接改变关联几何，使模型能以 O(N log N) 的计算复杂度捕捉长程依赖。研究证明了该架构能诱导幂律关联，并在临界伊辛模型和簇态上验证了其优于传统RNN的性能，为构建高效的长程关联感知自回归量子态提供了新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08661v1/fig-001.webp\", \"caption\": \"Figure 2. Illustration of the shortest path (in bold green arrows) between the last RNN cell at site xn and the first site x1 of (a) the vanilla RNN and (b) the dilated RNN given a system size N . For the vanilla RNN, the shortest path between sites separated by lag n is given by O(n), whereas for the dilated RNN, the shortest path has worst-case scaling O(log(n)).\", \"page\": 4, \"index\": 1, \"width\": 512, \"height\": 541}, {\"url\": \"assets/figures/arxiv/2604.08661v1/fig-002.webp\", \"caption\": \"Table I. Hyperparameters used to obtain the results reported in this paper.\", \"page\": 8, \"index\": 2, \"width\": 967, \"height\": 891}, {\"url\": \"assets/figures/arxiv/2604.08661v1/fig-003.webp\", \"caption\": \"Figure 1. Schematic representation of the RNN architectures used. (a) RNN wave function which sequentially generates the conditional probabilities pn and conditional phases ϕn by taking the one-hot encoded spin σn−1 and hidden vector hn−1 as inputs at the nth step. (b) A dilated RNN illustration for a spin configuration with length N = 5. For the nth site, the network uses the hidden vector h (l) n−2l−1 from the (n − 2l−1)th site at the same layer l, in addition to h (l−1) n from the preceding layer. If the dilation length is larger than the spin position n, a zero initialization h0 is used instead.\", \"page\": 2, \"index\": 3, \"width\": 512, \"height\": 326}]"
motivation: 标准RNN量子态受限于有限长度关联，难以处理具有长程依赖的量子态，而Transformer架构计算开销过大。
method: 引入空洞RNN波函数，通过空洞连接使循环单元直接访问远距离位点，从而注入长程归纳偏置。
result: 在1D临界伊辛模型中成功复现了幂律关联，并准确近似了传统RNN难以处理的1D簇态。
conclusion: 空洞连接是一种简单且高效的几何机制，能显著提升自回归神经网络量子态处理长程关联的能力。
---

## 摘要
基于自回归循环神经网络（RNN）波函数的神经网络量子态能够实现无马尔可夫链自相关的有效采样，但标准的 RNN 架构偏向于有限程关联，在处理具有长程依赖的量子态时可能会失效。一种常见的应对方案是采用 Transformer 式的自注意力机制，但这通常伴随着显著更高的计算和内存开销。在此，我们引入了空洞 RNN（dilated RNN）波函数，其中循环单元通过空洞连接访问远距离位点，在注入显式长程归纳偏置的同时，保持了优越的 $\mathcal{O}(N \log N)$ 前向传播缩放。我们通过解析证明，在简化的线性化和微扰设定下，空洞化改变了关联几何，并能诱导出幂律关联缩放。在数值模拟方面，对于一维临界横场伊辛模型，空洞 RNN 重现了预期的幂律连通两点关联，而传统的 RNN 拟设通常表现为指数衰减。我们进一步展示了空洞 RNN 能够精确逼近一维簇态（Cluster state），这是一个具有长程条件关联的范例，此前有报道称其对基于 RNN 的波函数具有挑战性。这些结果强调了空洞化是构建关联感知自回归神经网络量子态的一种简单几何机制。

## Abstract
Neural Quantum States based on autoregressive recurrent neural network (RNN) wave functions enable efficient sampling without Markov-chain autocorrelation, but standard RNN architectures are biased toward finite-length correlations and can fail on states with long-range dependencies. A common response is to adopt transformer-style self-attention, but this typically comes with substantially higher computational and memory overhead. Here we introduce dilated RNN wave functions, where recurrent units access distant sites through dilated connections, injecting an explicit long-range inductive bias while retaining a favorable $\mathcal{O}(N \log N)$ forward pass scaling. We show analytically that dilation changes the correlation geometry and can induce power-law correlation scaling in a simplified linearized and perturbative setting. Numerically, for the critical 1D transverse-field Ising model, dilated RNNs reproduce the expected power-law connected two-point correlations in contrast to the exponential decay typical of conventional RNN ansätze. We further show that the dilated RNN accurately approximates the one-dimensional Cluster state, a paradigmatic example with long-range conditional correlations that has previously been reported to be challenging for RNN-based wave functions. These results highlight dilation as a simple geometric mechanism for building correlation-aware autoregressive neural quantum states.

---

## 论文详细总结（自动生成）

这篇论文《Geometry-Induced Long-Range Correlations in Recurrent Neural Network Quantum States》探讨了如何通过改进循环神经网络（RNN）的架构，使其能够更有效地模拟具有长程关联的量子多体系统。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：神经网络量子态（NQS）中，自回归 RNN 因其采样效率高（线性缩放）而备受关注。然而，标准 RNN 架构存在“有限程关联偏置”，其内部信息传递路径随距离线性增长，导致关联函数通常呈指数衰减。这使得 RNN 在模拟临界量子态（具有幂律关联）或具有长程条件关联的态（如簇态）时表现不佳。
*   **核心问题**：如何在保持 RNN 计算效率的同时，引入能够捕捉长程依赖的归纳偏置？

### 2. 方法论
*   **核心思想**：引入**空洞循环神经网络（Dilated RNN）**。通过在多层 RNN 中设置跨步连接（Dilation），缩短序列中远距离位点之间的“有效几何距离”。
*   **关键技术细节**：
    *   **多层堆叠结构**：采用 $L$ 层 RNN 堆叠，每一层 $l$ 的空洞步长设定为 $s(l) = 2^{l-1}$。
    *   **连接机制**：第 $n$ 个位点的第 $l$ 层隐藏状态 $h_n^{(l)}$ 不仅接收前一层的输入 $h_n^{(l-1)}$，还接收同层中距离为 $s(l)$ 的前一状态 $h_{n-s(l)}^{(l)}$。
    *   **几何缩放**：这种设计将序列两端（1 到 $N$）的最短路径长度从 $O(N)$ 降低到了 $O(\log N)$。
    *   **计算复杂度**：前向传播的计算开销为 $O(N \log N)$，远低于 Transformer 的 $O(N^2)$。
*   **理论证明**：作者通过线性化 RNN 模型证明，空洞连接能使两点连通关联函数的下界从指数衰减转变为幂律形式 $C_n = \Omega(n^{-\alpha})$。

### 3. 实验设计
*   **实验场景**：
    1.  **一维横场伊辛模型（1D TFIM）**：在临界点 $g=1$ 处进行模拟，重点考察连通两点关联函数 $C(r)$。
    2.  **一维簇态（1D Cluster State）**：这是一个具有长程条件关联的非平凡量子态，已知对标准 RNN 极具挑战性。
*   **Benchmark（基准）**：
    *   TFIM：利用共形场论（CFT）预测的幂律衰减和临界指数 $\eta = 0.25$。
    *   簇态：精确的基态能量 $E_G$。
*   **对比方法**：单层标准 RNN（Vanilla RNN）以及不同层数的空洞 RNN。

### 4. 资源与算力
*   **算力来源**：论文提到使用了加拿大数字研究联盟（Digital Research Alliance of Canada）和滑铁卢大学数学系计算设施。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：TFIM 实验使用了 100,000 次迭代，隐藏单元维度 $d_h = 32$；簇态实验使用了 5,000 次迭代，隐藏单元维度 $d_h = 256$。

### 5. 实验数量与充分性
*   **实验组数**：针对 TFIM 进行了从 1 层到 7 层的消融实验，观察层数对关联函数拟合质量（$R^2$）和临界指数的影响。
*   **充分性**：实验设计较为充分，涵盖了 stoquastic（符号无忧）和非 stoquastic（复数波函数）两种情况。通过对临界指数的精确提取，有力地证明了架构改进的有效性。
*   **客观性**：对比了先前文献中提到的 RNN 难以处理的特定案例（簇态），证明了新架构的优越性。

### 6. 主要结论与发现
*   **关联特性的改变**：空洞架构从根本上改变了 NQS 的关联几何，使其能够自然地表达幂律关联。
*   **临界物理的捕捉**：空洞 RNN 成功复现了 1D TFIM 的临界行为，提取的指数 $\eta$ 与理论值高度吻合。
*   **训练稳定性提升**：在处理簇态时，空洞 RNN 不仅达到了极高的能量精度（相对误差 $10^{-5}$），而且训练过程比单层 RNN 更加平滑和稳定。

### 7. 优点
*   **高效性**：在不显著增加计算负担的前提下，解决了 RNN 处理长程关联的顽疾。
*   **理论与数值结合**：提供了清晰的线性化模型解析推导，为架构设计提供了物理依据。
*   **易于实现**：该方法是对现有 RNN 结构的简单几何修改，具有很强的通用性。

### 8. 不足与局限
*   **维度局限**：目前仅验证了一维系统，虽然文中提到可扩展至二维，但尚未给出具体的二维空洞模式设计和实验数据。
*   **优化器依赖**：实验主要基于一阶优化器（Adam），未探讨在二阶优化（如随机重整化 SR）下，空洞结构是否会带来额外的收敛优势或挑战。
*   **解析局限**：理论证明基于线性化假设，对于高度非线性的深度 RNN 内部动力学，该证明仅作为一种定性指导。

（完）
