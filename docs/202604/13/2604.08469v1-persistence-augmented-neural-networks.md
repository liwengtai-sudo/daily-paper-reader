---
title: Persistence-Augmented Neural Networks
title_zh: 持久性增强神经网络
authors: "Elena Xinyi Wang, Arnur Nigmetov, Dmitriy Morozov"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08469v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 利用拓扑数据分析进行组织病理学图像分类
tldr: 本文针对拓扑数据分析（TDA）在深度学习中难以保留局部几何结构的问题，提出了一种基于持久性的数据增强框架。该方法利用Morse-Smale复形编码局部梯度流区域及其层次演化，生成与CNN和GNN兼容的多尺度局部拓扑特征。该算法复杂度仅为O(n log n)，在病理图像分类和3D多孔材料回归任务中表现优于传统全局TDA描述符，为跨模态的可扩展拓扑学习提供了高效且具可解释性的新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08469v1/fig-001.webp\", \"caption\": \"Figure 3: Visualization of different levels of simplification\", \"page\": 9, \"index\": 1, \"width\": 650, \"height\": 529}, {\"url\": \"assets/figures/arxiv/2604.08469v1/fig-002.webp\", \"caption\": \"Figure 2: Construction of the hierarchical graph input for GNNs. Each layer represents a simplification level Vℓ, where nodes correspond to Morse–Smale regions defined by (m,M) pairs. Intra-level edges (dashed lines) connect adjacent regions, while inter-level edges (solid lines) indicate merge relationships as simplification progresses.\", \"page\": 7, \"index\": 2, \"width\": 590, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2604.08469v1/fig-003.webp\", \"caption\": \"Table 1: Classification performance on the histopathology dataset. Results are reported as mean accuracy / Cohen’s kappa with standard deviation over 5-fold cross-validation.\", \"page\": 10, \"index\": 3, \"width\": 802, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.08469v1/fig-004.webp\", \"caption\": \"Table 2: Regression performance on the porous material dataset. Results are reported as mean test R2 scores ± standard deviation over 5-fold cross-validation.\", \"page\": 10, \"index\": 4, \"width\": 868, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2604.08469v1/fig-005.webp\", \"caption\": \"Figure 1: Dual graph and simplification\", \"page\": 6, \"index\": 5, \"width\": 744, \"height\": 207}]"
motivation: 现有的TDA方法在集成到深度学习时往往侧重于全局特征总结，难以有效保留和利用数据的局部几何结构信息。
method: 提出一种基于Morse-Smale复形的持久性增强框架，通过编码局部梯度流区域及其层次演化来捕获多尺度、空间定位的拓扑特征。
result: 实验证明该方法在病理图像分类和3D材料回归任务中一致优于持久性图像等全局描述符，且通过剪枝层次结构可进一步优化内存使用。
conclusion: 局部结构化的拓扑增强能够显著提升深度学习模型在处理复杂几何数据时的性能，并具备良好的计算效率和跨模态适用性。
---

## 摘要
拓扑数据分析 (TDA) 提供了描述数据形状的工具，但将拓扑特征集成到深度学习流水线中仍然具有挑战性，特别是在保留局部几何结构而非进行全局总结时。我们提出了一种基于持久性的数据增强框架，该框架利用 Morse-Smale 复形对局部梯度流区域及其层次演化进行编码。这种表示方法与卷积神经网络和图神经网络均兼容，能够在多个尺度上保留空间局部的拓扑信息。重要的是，增强过程本身是高效的，计算复杂度为 $O(n \log n)$，使其适用于大规模数据集。我们在组织病理学图像分类和 3D 多孔材料回归任务上评估了我们的方法，结果表明它始终优于基准模型以及持久性图像 (persistence images) 和持久性景观 (persistence landscapes) 等全局 TDA 描述符。我们还表明，剪枝层次结构的底层可以减少内存使用，同时保持具有竞争力的性能。这些结果突显了局部、结构化拓扑增强在跨数据模态的可扩展和可解释学习方面的潜力。

## Abstract
Topological Data Analysis (TDA) provides tools to describe the shape of data, but integrating topological features into deep learning pipelines remains challenging, especially when preserving local geometric structure rather than summarizing it globally. We propose a persistence-based data augmentation framework that encodes local gradient flow regions and their hierarchical evolution using the Morse-Smale complex. This representation, compatible with both convolutional and graph neural networks, retains spatially localized topological information across multiple scales. Importantly, the augmentation procedure itself is efficient, with computational complexity $O(n \log n)$, making it practical for large datasets. We evaluate our method on histopathology image classification and 3D porous material regression, where it consistently outperforms baselines and global TDA descriptors such as persistence images and landscapes. We also show that pruning the base level of the hierarchy reduces memory usage while maintaining competitive performance. These results highlight the potential of local, structured topological augmentation for scalable and interpretable learning across data modalities.

---

## 论文详细总结（自动生成）

这是一份关于论文《Persistence-Augmented Neural Networks》的结构化深入总结：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
拓扑数据分析（TDA）在描述数据形状方面表现出色，但现有的集成方法（如持久性图像、持久性景观）主要提供**全局拓扑总结**，忽略了**局部几何结构**和空间位置信息。这导致模型虽然知道存在某种拓扑特征（如洞或峰值），却不知道它们在图像或图中的具体位置。
**核心问题：**
如何高效地提取并保留数据的局部拓扑结构，并将其以结构化的形式集成到卷积神经网络（CNN）和图神经网络（GNN）中，以提升模型在复杂科学数据上的表现。

### 2. 论文提出的方法论
**核心思想：**
利用 **Morse-Smale (MS) 复形** 将域（图像或图）划分为具有统一梯度流的局部区域，并通过**持久性简化（Persistence-based Simplification）**构建多尺度的层次化表示。

**关键技术细节：**
*   **MS 复形构建：** 将灰度图像或带权图视为标量场，通过追踪每个顶点的梯度路径（源自最小值，终于最大值），将具有相同“最小-最大值对”的顶点归为同一个单元（Cell）。
*   **对偶图表示：** 构建一个对偶图 $G=(V, E)$，其中每个节点代表一个 MS 区域，边代表区域间的空间相邻性。
*   **层次化简化：** 设定一系列持久性阈值 $\epsilon_1 < \epsilon_2 < \dots < \epsilon_k$。通过合并低持久性的临界点对，消除拓扑噪声，生成从精细到粗糙的多尺度拓扑结构。
*   **神经网络集成：**
    *   **CNN：** 将不同简化水平的拓扑区域标签编码为多通道图像输入。
    *   **GNN：** 构建一个层次图，不仅包含同层区域的相邻边，还包含跨层的“合并”边（子区域指向父区域），支持跨尺度的信息传递。

### 3. 实验设计
**数据集与场景：**
1.  **组织病理学图像分类：** 1,144 张 RGB 图像（转灰度），分为非肿瘤、坏死肿瘤和存活肿瘤三类。
2.  **3D 多孔材料回归：** 1,700 个 3D 体素样本，任务是根据距离变换场预测材料的渗透率（Permeability）。

**对比方法（Benchmark）：**
*   **原始基准：** 直接使用原始图像或体素输入的 CNN。
*   **全局 TDA 描述符：** CNN + 持久性图像（Persistence Images）、CNN + 持久性景观（Persistence Landscapes）。
*   **消融实验：** 针对 GNN，对比了包含完整层次结构与移除最底层（Base Layer）后的性能。

### 4. 资源与算力
**算力说明：**
论文**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
**算法效率：**
作者强调了该方法的计算效率：基础 MS 复形构建复杂度为 $O(n)$，包含 $k$ 层简化的完整流程复杂度为 $O(n \log n + kn)$。这表明该方法在处理大规模数据集时具有较好的实用性。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 2D 图像分类和 3D 体素回归两个截然不同的领域，证明了方法的通用性。
*   **客观性：** 采用了 **5 折交叉验证**，并使用网格搜索（Grid Search）优化学习率和权重衰减等超参数。
*   **公平性：** 为了隔离拓扑特征的贡献，作者特意使用了**极简的神经网络架构**（如仅 2 层卷积的 CNN），避免了复杂模型架构对实验结果的干扰。
*   **充分性：** 实验不仅对比了准确率，还引入了 Cohen’s Kappa 系数（针对分类）和 $R^2$ 分数（针对回归），评估维度较为全面。

### 6. 论文的主要结论与发现
*   **性能提升：** 持久性增强（Persistence Augmentation）在所有任务中均显著优于原始基准和全局 TDA 描述符。在病理图像分类中，准确率从 84% 提升至 95%。
*   **局部信息的重要性：** 实验结果证明，保留局部空间拓扑结构比单纯使用全局拓扑总结（PI/PL）更有效。
*   **层次结构的价值：** GNN 在处理多尺度拓扑演化方面表现优异，尤其是在复杂的 3D 回归任务中。
*   **效率与精度的权衡：** 移除层次结构的底层虽然会略微降低性能，但能显著减少内存占用，适合资源受限的场景。

### 7. 优点
*   **局部性保留：** 克服了传统 TDA 方法丢失空间定位信息的弊端。
*   **计算高效：** $O(n \log n)$ 的复杂度使其能够处理高分辨率图像和 3D 体素数据。
*   **架构无关性：** 增强框架可以无缝集成到现有的 CNN 和 GNN 流程中，无需设计复杂的自定义损失函数。
*   **可解释性：** MS 复形提供的区域划分具有明确的几何意义（如梯度流区域）。

### 8. 不足与局限
*   **内存开销：** 尽管计算快，但多通道图像和层次图会增加内存负担，尤其是在处理大规模 3D 数据时。
*   **超参数依赖：** 简化阈值 $\epsilon$ 的选择目前是手动的（固定比例），尚未实现端到端的自动学习。
*   **任务覆盖：** 目前仅验证了图/图像级别的分类和回归，尚未在节点级别（如分割、目标检测）任务上进行测试。
*   **算力细节缺失：** 缺乏具体的硬件运行环境和训练时间对比，难以直观评估实际部署成本。

（完）
