---
title: Region-Graph Optimal Transport Routing for Mixture-of-Experts Whole-Slide Image Classification
title_zh: 用于专家混合全扫描切片图像分类的区域图最优传输路由
authors: "Xin Tian, Jiuliu Lu, Ephraim Tsalik, Bart Wanders, Colleen Knoth, Julian Knight"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07298v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 计算病理学中全切片图像分类的混合专家模型
tldr: 针对全切片图像（WSI）分类中多实例学习（MIL）模型难以处理病理异质性的问题，本文提出ROAM框架。该方法将混合专家模型（MoE）引入MIL，通过容量受限的熵正则化最优传输（Sinkhorn）实现区域令牌到专家的平衡路由，并利用图正则化确保空间相邻区域路由的一致性。在多个基准测试中，ROAM展现了卓越的性能和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07298v1/fig-001.webp\", \"caption\": \"Fig. 1. Frozen patch embeddings are pooled into M spatial region tokens. A routing GNN on the region graph parameterises region-to-expert costs, and Sinkhorn optimal transport routes region mass to E experts under per-slide capacity constraints (with optional graph smoothing). Each expert performs gated-attention pooling over its routed regions, and expert outputs are fused for slide-level prediction.\", \"page\": 3, \"index\": 1, \"width\": 702, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2604.07298v1/fig-002.webp\", \"caption\": \"Table 2. Ablation study on NSCLC (LUAD vs. LUSC) using UNI2-h embeddings (mean±std over 5 folds). Internal evaluation is on TCGA (int.); external evaluation is on CPTAC (ext.).\", \"page\": 7, \"index\": 2, \"width\": 714, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2604.07298v1/fig-003.webp\", \"caption\": \"Table 1. WSI benchmark results with UNI2 patch encoders (mean±std over 5-folds). For NSCLC (LUAD/LUSC), we report internal AUC on TCGA (int.) and external AUC on CPTAC (ext.); for BRCA and CRC we report internal AUC; for PANDA we report internal QWK. Best per column in bold; second best underlined.\", \"page\": 7, \"index\": 3, \"width\": 725, \"height\": 264}]"
motivation: 现有的MIL聚合器采用单一路径处理所有实例，难以应对WSI的异质性，且传统MoE路由易出现专家负载不均的塌陷问题。
method: 提出ROAM框架，利用最优传输理论实现容量受限的专家分配，并结合空间图正则化引导相邻区域协同路由。
result: 在四个WSI基准数据集上表现优异，特别是在NSCLC跨中心泛化测试中达到了0.845的AUC。
conclusion: ROAM通过结合空间感知和平衡路由机制，有效提升了MoE在病理图像分类中的专家利用率和模型性能。
---

## 摘要
多实例学习（MIL）是计算病理学中十亿像素级全扫描切片图像（WSI）分类的主流框架。然而，目前的 MIL 聚合器通过共享路径路由所有实例，限制了它们针对每张切片中固有的病理异质性进行专门化处理的能力。专家混合（MoE）方法通过将实例划分到专门的专家子网络中提供了一种自然的解决方案；然而，无约束的 softmax 路由可能会导致严重的利用率不平衡，即一个或少数几个专家吸收了大部分路由权重，使混合模型退化为近乎单一路径的方案。为了解决这些局限性，我们提出了 ROAM（区域图最优传输专家混合），这是一种空间感知的 MoE-MIL 聚合器，它通过容量受限的熵正则化最优传输将区域 token 路由到专家池化器，从而在结构上促进了平衡的专家利用率。ROAM 作用于空间区域 token，这些 token 是通过将密集的 patch 包压缩为空间分箱单元而获得的，从而使路由与局部组织邻域对齐，并引入了两个关键机制：(i) 将区域到专家的分配表述为具有显式单张切片容量边际的熵正则化最优传输（Sinkhorn），在无需辅助负载均衡损失的情况下强制实现平衡的专家利用率；(ii) 图正则化的 Sinkhorn 迭代，在空间区域图上扩散路由分配，鼓励相邻区域一致地路由到相同的专家。在四个使用冻结基础模型 patch 嵌入的 WSI 基准测试中进行评估，ROAM 取得了与强 MIL 和 MoE 基线相当的性能，并在 NSCLC 泛化（TCGA-CPTAC）上达到了 0.845 ± 0.019 的外部 AUC。

## Abstract
Multiple Instance Learning (MIL) is the dominant framework for gigapixel whole-slide image (WSI) classification in computational pathology. However, current MIL aggregators route all instances through a shared pathway, constraining their capacity to specialise across the pathological heterogeneity inherent in each slide. Mixture-of-Experts (MoE) methods offer a natural remedy by partitioning instances across specialised expert subnetworks; yet unconstrained softmax routing may yield highly imbalanced utilisation, where one or a few experts absorb most routing mass, collapsing the mixture back to a near-single-pathway solution. To address these limitations, we propose ROAM (Region-graph OptimAl-transport Mixture-of-experts), a spatially aware MoE-MIL aggregator that routes region tokens to expert poolers via capacity-constrained entropic optimal transport, promoting balanced expert utilisation by construction. ROAM operates on spatial region tokens, obtained by compressing dense patch bags into spatially binned units that align routing with local tissue neighbourhoods and introduces two key mechanisms: (i) region-to-expert assignment formulated as entropic optimal transport (Sinkhorn) with explicit per slide capacity marginals, enforcing balanced expert utilisation without auxiliary load-balancing losses; and (ii) graph-regularised Sinkhorn iterations that diffuse routing assignments over the spatial region graph, encouraging neighbouring regions to coherently route to the same experts. Evaluated on four WSI benchmarks with frozen foundation-model patch embeddings, ROAM achieves performance competitive against strong MIL and MoE baselines, and on NSCLC generalisation (TCGA-CPTAC) reaches external AUC 0.845 +- 0.019.

---

## 论文详细总结（自动生成）

这是一份关于论文《Region-Graph Optimal Transport Routing for Mixture-of-Experts Whole-Slide Image Classification》的结构化深入分析总结：

### 1. 核心问题与研究背景
*   **研究动机**：全扫描切片图像（WSI）具有极高的空间异质性和形态复杂性。传统的多实例学习（MIL）聚合器（如 ABMIL、TransMIL）通常采用单一的共享路径处理所有图像块（patches），这限制了模型针对不同病理特征进行专门化处理的能力。
*   **核心问题**：虽然专家混合模型（MoE）可以提供专门化的子网络，但传统的 Softmax 路由机制容易导致“路由塌陷”（即少数专家承担了绝大部分任务，其他专家被闲置）。此外，现有的路由方法往往忽略了 WSI 中病理结构的**空间连续性**，导致相邻的相似组织被破碎地分配给不同专家。

### 2. 方法论：ROAM 框架
ROAM（Region-graph OptimAl-transport Mixture-of-experts）的核心思想是将路由问题转化为一个**带容量约束的最优传输（OT）问题**，并结合空间图正则化。
*   **区域令牌化（Region Tokenization）**：为了降低计算复杂度并捕获局部语境，ROAM 将密集的 patch 集合通过空间网格分箱（Grid Binning）压缩为 $M$ 个区域令牌（Region Tokens）。
*   **容量受限的最优传输路由**：
    *   将区域到专家的分配建模为熵正则化最优传输问题。
    *   **关键技术**：通过 Sinkhorn 算法求解，并显式设置专家的容量边际（Marginals）。这在结构上强制要求每个专家处理等量的区域质量，从而在不需要额外辅助损失函数的情况下实现了完美的负载均衡。
*   **图正则化 Sinkhorn 迭代**：
    *   在 Sinkhorn 迭代过程中交织一个轻量级的图扩散步骤。
    *   利用区域间的空间邻接图，鼓励地理位置相邻的区域路由到相同的专家，从而保证了病理诊断上的空间一致性。
*   **专家聚合与融合**：每个专家对其负责的区域进行门控注意力池化（Gated Attention Pooling），最后通过另一个门控机制融合所有专家的输出进行切片级预测。

### 3. 实验设计
*   **数据集与场景**：
    1.  **NSCLC（肺癌）**：TCGA 内部训练与验证，CPTAC 作为外部独立测试集（评估泛化性）。
    2.  **BRCA（乳腺癌）**：子型分类。
    3.  **CRC（结直肠癌）**：子型分类。
    4.  **PANDA（前列腺癌）**：六级 ISUP 评分（大规模多分类任务）。
*   **Benchmark 与对比方法**：
    *   **经典 MIL**：ABMIL, CLAM-SB/MB, DSMIL, TransMIL, ILRA。
    *   **MoE/OT 相关方法**：MAMMOTH, PAMoE, OTSurv。
    *   **特征提取**：所有方法统一使用冻结的病理基础模型 UNI2-h 嵌入。

### 4. 资源与算力
*   **硬件环境**：所有实验均在**单张 NVIDIA A100 40GB GPU** 上运行。
*   **训练细节**：使用 AdamW 优化器，最高训练 200 个 epoch，带有早停机制（patience=20）。
*   **效率**：由于引入了区域令牌化（将 $N$ 个 patch 压缩为 $M$ 个 region），路由计算量从 $O(NE)$ 降低到 $O(ME)$，显著提升了处理大尺寸 WSI 的效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了四个主流 WSI 基准数据集，样本量从几百张到上万张（PANDA 数据集超过 1 万张切片）。
*   **充分性**：
    *   采用了 5 折交叉验证并报告了标准差，确保了结果的统计可靠性。
    *   **外部验证**：特别强调了 TCGA 到 CPTAC 的跨中心泛化实验，这是衡量病理模型实用性的关键指标。
    *   **消融实验**：针对路由 GNN、图正则化、容量约束（对比 Softmax）等核心组件进行了详细的消融分析，证明了各模块的有效性。
*   **客观性**：在相同的特征编码器和实验协议下进行对比，实验设计较为公平。

### 6. 主要结论与发现
*   **性能领先**：ROAM 在多个任务上达到或超过了最先进的（SOTA）水平。在 NSCLC 外部泛化测试中，AUC 达到 0.845，且方差显著低于其他 MoE 基线。
*   **稳定性**：显式的容量约束比辅助损失函数更能有效防止路由塌陷，使专家学习更加稳定。
*   **空间一致性**：定性可视化表明，ROAM 生成的专家分配图具有明显的空间连通性，符合病理学中组织连续性的特征。

### 7. 优点与亮点
*   **结构化负载均衡**：利用最优传输的数学特性（边际约束）直接解决 MoE 的负载均衡问题，优雅且无需调参辅助损失。
*   **空间感知**：通过图正则化将空间先验引入路由过程，解决了传统 MoE 在处理图像数据时忽略位置信息的问题。
*   **泛化能力强**：在跨中心数据集上的表现证明了该方法在应对领域偏移（Domain Shift）时的鲁棒性。

### 8. 不足与局限
*   **依赖冻结编码器**：实验主要基于冻结的基础模型特征，尚未探讨端到端微调对路由机制的影响。
*   **固定区域划分**：目前采用固定的网格分箱进行区域令牌化，这种方式可能无法完美契合不规则的组织边界。
*   **超参数敏感性**：虽然减少了负载均衡的超参，但仍引入了如区域数量 $M$、图平滑强度 $\lambda_s$ 等新的超参数。

（完）
