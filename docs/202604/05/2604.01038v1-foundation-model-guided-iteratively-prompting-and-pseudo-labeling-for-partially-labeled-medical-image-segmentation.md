---
title: Foundation Model-guided Iteratively Prompting and Pseudo-Labeling for Partially Labeled Medical Image Segmentation
title_zh: 基础模型引导的迭代提示与伪标签方法用于部分标注医学图像分割
authors: "Qiaochu Zhao, Wei Wei, David Horowitz, Richard Bakst, Yading Yuan"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01038v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 基础模型引导的部分标记医学图像分割
tldr: 针对医疗影像分割中因标注成本高导致的标签缺失问题，本文提出IPnP框架。该框架通过可训练的专业分割网络与冻结的基础模型协同工作，采用迭代提示和伪标签生成策略，逐步恢复未标注器官的监督信息。在AMOS公开数据集和真实头颈癌临床数据集上的实验证明，该方法显著提升了部分标注数据的分割性能，表现接近全标注水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01038v1/fig-001.webp\", \"caption\": \"Fig. 2. Overview of the proposed IPnP framework.\", \"page\": 2, \"index\": 1, \"width\": 410, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2604.01038v1/fig-002.webp\", \"caption\": \"Table 1. Segmentation performance of IPnP and other methods on the AMOS dataset, reported in DSC (%) and HD95 (mm).\", \"page\": 4, \"index\": 2, \"width\": 1029, \"height\": 338}, {\"url\": \"assets/figures/arxiv/2604.01038v1/fig-003.webp\", \"caption\": \"Fig. 5. Segmentation performance of IPnP and other methods on HnN dataset, reported in DSC (%). The numbers in parentheses indicate the counts of labeled organs in the training, validation, and testing sets, respectively, for each organ.\", \"page\": 4, \"index\": 3, \"width\": 781, \"height\": 256}]"
motivation: 医疗影像标注的高昂成本和临床侧重点差异导致数据常呈现部分标注状态，严重影响了分割模型的训练效果。
method: 提出IPnP框架，通过基础模型引导的迭代提示与伪标签生成机制，实现专业分割网络与通用基础模型的协同进化。
result: 在模拟和真实的部分标注数据集上，IPnP均显著优于现有方法，且性能指标接近全标注的参考标准。
conclusion: 利用基础模型辅助迭代伪标签生成是解决医疗影像部分标注问题的有效途径，具有显著的临床实用性。
---

## 摘要
自动化医学图像分割在全标注数据上取得了显著进展。然而，受限于特定机构的临床优先级和高昂的人工标注成本，扫描图像通常仅标注了部分器官，导致了部分标注问题并降低了模型性能。为解决这一问题，我们提出了 IPnP，一个用于部分标注医学图像分割的迭代提示与伪标签框架。IPnP 通过可训练分割网络（专家）与冻结的基础模型（通用专家）之间的协作，为未标注器官迭代地生成并优化伪标签，从而逐步恢复全器官监督。在模拟部分标注设置的公开数据集 AMOS 上，IPnP 的分割性能始终优于现有方法，并接近全标注参考基准的性能。我们进一步在包含 210 名头颈癌患者的私有部分标注数据集上进行了评估，证明了该方法在真实临床场景中的有效性。

## Abstract
Automated medical image segmentation has achieved remarkable progress with fully labeled data. However, site-specific clinical priorities and the high cost of manual annotation often yield scans with only a subset of organs labeled, leading to the partially labeled problem that degrades performance. To address this issue, we propose IPnP, an Iteratively Prompting and Pseudo-labeling framework, for partially labeled medical image segmentation. IPnP iteratively generates and refines pseudo-labels for unlabeled organs through collaboration between a trainable segmentation network (specialist) and a frozen foundation model (generalist), progressively recovering full-organ supervision. On the public dataset AMOS with the simulated partial-label setting, IPnP consistently improves segmentation performance over prior methods and approaches the performance of the fully labeled reference. We further evaluate on a private, partially labeled dataset of 210 head-and-neck cancer patients and demonstrate our effectiveness in real-world clinical settings.

---

## 论文详细总结（自动生成）

### 论文总结：基础模型引导的迭代提示与伪标签方法用于部分标注医学图像分割

#### 1. 核心问题与整体含义（研究动机和背景）
在临床实践中，医学影像的完全标注（Full Annotation）成本极高且耗时。由于不同医疗机构的临床关注点不同，现有的数据集往往呈现“部分标注”状态（即每张图像只标注了部分感兴趣的器官）。
*   **研究动机**：传统方法在处理部分标注数据时，通常将未标注器官视为背景或直接忽略，这会导致监督信号缺失、解剖结构学习不完整以及性能下降。
*   **核心问题**：如何利用现有的医学影像基础模型（Foundation Models）的泛化能力，自动为未标注器官生成高质量的伪标签，并构建一个能够自我进化的训练框架。

#### 2. 方法论：核心思想与关键技术
论文提出了 **IPnP (Iteratively Prompting and Pseudo-labeling)** 框架，其核心思想是让一个“可训练的专家网络（Specialist）”与一个“冻结的通用基础模型（Generalist）”协同工作。

*   **核心流程**：
    1.  **初始训练**：专家网络（基于 nnU-Net）在原始的部分标注数据上进行初步训练，获得基础分割能力。
    2.  **交互式伪标签生成**：专家网络对未标注器官生成粗略预测，从中提取 3D 边界框（Box Prompts）。这些提示信息引导基础模型（nnInteractive）在零样本（Zero-shot）设置下生成更准确的伪标签。
    3.  **伪标签细化（Refinement）**：通过三种约束过滤噪声：
        *   **概率阈值**：剔除低置信度体素。
        *   **ROI 约束**：限制在提示框附近的区域，减少远端误报。
        *   **熵约束**：计算体素级熵值作为不确定性度量，仅在迭代中熵值降低时更新伪标签。
    4.  **迭代训练与体素级选择损失（VLS Loss）**：将细化后的伪标签加入训练。为了防止伪标签中的残留噪声干扰模型，引入 VLS 损失函数，该函数通过掩码机制，仅对预测与伪标签一致的“可靠体素”计算损失，而对真实标注体素保持全监督。

#### 3. 实验设计
*   **数据集**：
    1.  **AMOS 腹部数据集**：包含 15 个器官。通过随机移除标注，模拟了 67%（中等难度）和 33%（高难度）两种部分标注场景。
    2.  **私有头颈癌（HnN）数据集**：包含 210 名患者的 CT 影像，涉及 13 个 OAR（危及器官），属于真实的临床部分标注场景。
*   **Benchmark 与对比方法**：
    *   **全标注上限**：使用 100% 标签训练。
    *   **全监督下限**：将缺失标签视为背景。
    *   **部分监督基准**：忽略未标注通道（使用 Sigmoid 损失）。
    *   **SOTA 对比**：Trans-DoDNet（基于动态头的部分标注分割方法）。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA RTX 6000 Ada GPUs**。
*   **训练细节**：专家网络基于 nnUNetv2，使用 SGD 优化器，初始学习率 0.01（线性衰减），Batch Size 为 2，总计训练 1000 个 Epoch。
*   **时长**：文中未明确给出总训练时长，但考虑到迭代生成伪标签和 1000 Epoch 的深度训练，计算开销会高于常规单次训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 AMOS 数据集上进行了两种比例的模拟实验，并在 210 例真实临床数据上进行了验证，样本量较为充足。
*   **消融实验**：专门针对 **VLS 损失** 进行了消融分析，证明了其在抑制伪标签噪声方面的关键作用（例如在食管和肾上腺等挑战性器官上提升显著）。
*   **客观性**：对比了从简单基准到 SOTA 方法的多种路径，并提供了 DSC（Dice 系数）和 HD95（豪斯多夫距离）两种评价指标，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **性能优异**：IPnP 在所有部分标注设置下均显著优于现有方法。在 AMOS 33% 标注设置下，其 DSC 达到 83.12%，远超 Partial Supervision 的 67.19%。
*   **接近上限**：在 67% 标注情况下，IPnP 的表现（88.06% DSC）非常接近全标注的参考上限（89.21% DSC）。
*   **临床泛化性**：在头颈部数据集上，对于标注极少的器官（如左右眼仅 36 例标注），IPnP 提升巨大（DSC 从 80% 左右提升至 92% 以上），证明了基础模型在长尾/稀缺标注数据上的引导价值。

#### 7. 优点（亮点）
*   **协同进化**：巧妙结合了基础模型的通用泛化能力和特定任务网络的专业性，无需额外人工干预即可提升标签质量。
*   **鲁棒性强**：通过熵约束和 VLS 损失构建了多重“防错机制”，有效解决了伪标签学习中的错误传播问题。
*   **即插即用**：该框架逻辑清晰，可以扩展到其他基于提示的基础模型和分割架构中。

#### 8. 不足与局限
*   **计算成本**：迭代过程涉及多次伪标签生成和模型重训，相比于端到端的方法，推理和训练的时间成本更高。
*   **基础模型依赖**：伪标签的质量高度依赖于所选基础模型（如 nnInteractive）的初始性能，若基础模型在某些罕见病变上表现不佳，可能会限制上限。
*   **提示策略单一**：目前主要依赖 Box Prompts，未来可探索点提示（Point）或文本提示（Text）以增强交互精度。

（完）
