---
title: "NEMESIS: Noise-suppressed Efficient MAE with Enhanced Superpatch Integration Strategy"
title_zh: NEMESIS：具有增强型超块集成策略的噪声抑制高效 MAE
authors: "Kyeonghun Kim, Hyeonseok Jung, Youngung Han, Hyunsu Go, Eunseob Choi, Seongbin Park, Junsu Lim, Jiwon Yang, Sumin Lee, Insung Hwang, Ken Ying-Kai Liao, Nam-Joon Kim"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01612v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于体积CT影像诊断的自监督学习
tldr: 针对3D CT影像标注成本高及全量Transformer内存消耗大的挑战，本文提出NEMESIS自监督学习框架。该框架基于局部超块（superpatch）进行训练，引入噪声增强重建任务、双重掩码解剖Transformer块（MATB）以及跨尺度上下文聚合Token（NT）。实验表明，NEMESIS在显著降低计算开销的同时，在多器官分类任务及低标注场景下均取得了超越现有主流模型的性能，为3D医学影像提供了高效且鲁棒的基础模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01612v1/fig-001.webp\", \"caption\": \"Fig. 1. The overall architecture of the NEMESIS backbone. The input superpatch is divided into volume patches and processed through the patch embedding module. The framework utilizes dual-masking and MATB-based encoders and decoders to learn high-level anatomical representations in the latent space.\", \"page\": 2, \"index\": 1, \"width\": 965, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2604.01612v1/fig-002.webp\", \"caption\": \"Fig. 3. Detailed architecture of the 3D Adaptive Patch Embedding module, integrating Linear Projection (LP) and Superpatch-Embedder (SE) pathways.\", \"page\": 2, \"index\": 2, \"width\": 529, \"height\": 297}]"
motivation: 旨在解决3D CT影像自监督学习中全量体积计算开销过大以及传统掩码策略难以捕捉各向异性空间结构的问题。
method: 采用基于128x128x128超块的MAE架构，结合噪声增强重建、并行平面与轴向双重掩码策略以及跨尺度上下文聚合技术。
result: "在BTCV基准测试中，线性分类AUROC达到0.9633，超越了全微调的SuPreM，且计算量仅为全量体积基准的3%。"
conclusion: NEMESIS证明了通过局部超块设计和针对性掩码策略，可以在极低计算成本下实现高性能的3D医学影像表征学习。
---

## 摘要
体层 CT 成像是临床诊断的基础，但 3D 体数据的标注既昂贵又耗时，这促使了利用无标签数据进行自监督学习 (SSL) 的发展。然而，由于全卷 Transformer 的高内存开销以及 CT 数据各向异性的空间结构（传统掩码策略难以有效捕捉），将 SSL 应用于 3D CT 仍具挑战性。我们提出了 NEMESIS，这是一个在局部 128x128x128 超块 (superpatches) 上运行的掩码自编码器 (MAE) 框架，在保留解剖细节的同时实现了内存高效的训练。NEMESIS 引入了三个关键组件：(i) 作为预训练任务的噪声增强重建；(ii) 掩码解剖 Transformer 块 (MATB)，通过并行的平面级和轴级 Token 移除执行双重掩码；(iii) 用于跨尺度上下文聚合的 NEMESIS Token (NT)。在 BTCV 多器官分类基准测试中，采用冻结主干网络和线性分类器的 NEMESIS 达到了 0.9633 的平均 AUROC，超过了全微调的 SuPreM (0.9493) 和 VoCo (0.9387)。在仅有 10% 可用标注的低标签场景下，它仍保持了 0.9075 的 AUROC，展示了强大的标签效率。此外，基于超块的设计将单次前向传播的计算成本从全卷基线的 985.8 GFLOPs 降低至 31.0 GFLOPs，为 3D 医学影像提供了可扩展且稳健的基础。

## Abstract
Volumetric CT imaging is essential for clinical diagnosis, yet annotating 3D volumes is expensive and time-consuming, motivating self-supervised learning (SSL) from unlabeled data. However, applying SSL to 3D CT remains challenging due to the high memory cost of full-volume transformers and the anisotropic spatial structure of CT data, which is not well captured by conventional masking strategies. We propose NEMESIS, a masked autoencoder (MAE) framework that operates on local 128x128x128 superpatches, enabling memory-efficient training while preserving anatomical detail. NEMESIS introduces three key components: (i) noise-enhanced reconstruction as a pretext task, (ii) Masked Anatomical Transformer Blocks (MATB) that perform dual-masking through parallel plane-wise and axis-wise token removal, and (iii) NEMESIS Tokens (NT) for cross-scale context aggregation. On the BTCV multi-organ classification benchmark, NEMESIS with a frozen backbone and a linear classifier achieves a mean AUROC of 0.9633, surpassing fully fine-tuned SuPreM (0.9493) and VoCo (0.9387). Under a low-label regime with only 10% of available annotations, it retains an AUROC of 0.9075, demonstrating strong label efficiency. Furthermore, the superpatch-based design reduces computational cost to 31.0 GFLOPs per forward pass, compared to 985.8 GFLOPs for the full-volume baseline, providing a scalable and robust foundation for 3D medical imaging.

---

## 论文详细总结（自动生成）

以下是对论文《NEMESIS: Noise-suppressed Efficient MAE with Enhanced Superpatch Integration Strategy》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：3D CT 影像在临床中至关重要，但面临两大难题：一是**标注成本极高**，需要专家逐层勾画；二是**计算开销巨大**，全分辨率 3D 卷（如 512×512×400）生成的 Token 数量远超 Transformer 的处理能力（二次复杂度）。
*   **结构特性**：CT 数据具有显著的**各向异性**（轴向、冠状、矢状面结构特性不同），传统的均匀掩码策略无法有效捕捉这种空间结构偏差。
*   **研究目标**：开发一种既能显著降低内存/计算开销，又能针对 CT 结构特性进行高效表征学习的自监督学习（SSL）框架。

### 2. 方法论：核心思想与关键技术
NEMESIS 采用基于超块（Superpatch）的掩码自编码器（MAE）架构，核心组件包括：
*   **超块预训练（Superpatch Pretraining）**：不处理全卷，而是将 3D 卷划分为 $128^3$ 的局部超块进行随机采样训练。这在保留局部解剖细节的同时，极大降低了显存占用。
*   **3D 适配嵌入与 NEMESIS Token (NT)**：
    *   采用双路径嵌入：直接线性投影（LP）路径和基于自注意力块（SAB）的超块嵌入（SE）路径。
    *   引入 **NEMESIS Token (NT)** 作为辅助上下文锚点，用于聚合跨尺度的全局上下文信息。
*   **掩码解剖 Transformer 块 (MATB)**：
    *   **双重掩码策略**：并行执行“轴向流”（捕捉 Patch 内部上下文）和“平面流”（学习 Patch 间的层间关系）。
    *   这种设计专门针对 CT 的各向异性，通过不同维度的注意力机制建模结构归纳偏置。
*   **噪声增强重建**：在输入超块中加入高斯噪声，要求模型从可见 Token 中重建原始清晰图像，增强了特征的鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 TotalSegmentator 及多个公共 CT 数据集（HNSCC, FLARE23, TCIA-COVID19, LUNA16, BTCV），均为无标签数据。
    *   **下游任务**：BTCV 数据集（30 例腹部 CT），执行 8 种器官（主动脉、胆囊、脾脏、左右肾、肝、胃、胰腺）的超块级多标签分类。
*   **Benchmark 与对比方法**：
    *   对比了监督学习基线（ResNet3D-50）和 SOTA 自监督方法（SuPreM, VoCo）。
    *   评估协议包括：**冻结主干+线性分类器**（评估表征质量）和**全微调**。

### 4. 资源与算力
*   **计算效率**：论文明确给出了计算量对比。NEMESIS ($128^3$ 超块) 的单次前向传播计算量为 **31.0 GFLOPs**，相比全卷基线（985.8 GFLOPs）实现了 **32 倍的缩减**。
*   **硬件细节**：文中**未明确说明**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主干性能对比实验（AUROC 和 F1 分数）。
    *   **标签效率实验**：测试了在 10%、25%、50%、100% 标签比例下的表现。
    *   **消融实验**：对比了不同掩码策略（仅轴向 vs 仅平面）对重建质量（PSNR, SSIM）的影响，以及超参数（嵌入维度、掩码率）的敏感性分析。
*   **充分性评价**：实验设计较为客观，特别是“冻结主干”的对比有力证明了其预训练特征的判别力。但下游任务目前仅限于分类，尚未展示在分割任务上的表现。

### 6. 主要结论与发现
*   **性能超越 SOTA**：仅使用线性分类器的 NEMESIS（AUROC 0.9633）就超过了经过全微调的 SuPreM 和 VoCo。
*   **极高的标签效率**：在仅有 10% 标注数据时，AUROC 仍能达到 0.9075，接近全监督水平。
*   **结构建模的有效性**：平面掩码策略的重建质量（PSNR 27.75）显著优于轴向掩码（24.87），证明了捕捉 CT 层间结构的重要性。

### 7. 优点与亮点
*   **高效性**：通过超块设计解决了 3D Transformer 难以处理大尺度医学影像的痛点。
*   **针对性设计**：MATB 模块和双重掩码策略充分考虑了医学影像（尤其是 CT）的物理特性，而非盲目套用自然图像的 MAE。
*   **表征质量高**：线性探测结果极佳，说明模型真正学到了具有解剖学意义的通用特征。

### 8. 不足与局限
*   **任务覆盖有限**：目前主要验证了器官存在性分类任务，对于医学影像中更关键的**语义分割（Segmentation）**任务，文中仅提到作为未来研究方向，缺乏当前实验支撑。
*   **硬件描述缺失**：缺乏具体的训练环境和时间成本描述，难以评估其实际部署时的预训练门槛。
*   **各向异性泛化**：虽然针对 CT 优化，但对于其他模态（如各向同性的 MRI 或不同层厚的 CT）的泛化能力尚待验证。

（完）
