---
title: "MAESIL: Masked Autoencoder for Enhanced Self-supervised Medical Image Learning"
title_zh: MAESIL：用于增强型自监督医学图像学习的掩码自编码器
authors: "Kyeonghun Kim, Hyeonseok Jung, Youngung Han, Junsu Lim, YeonJu Jean, Seongbin Park, Eunseob Choi, Hyunsu Go, SeoYoung Ju, Seohyoung Park, Gyeongmin Kim, MinJu Kwon, KyungSeok Yuh, Soo Yong Kim, Ken Ying-Kai Liao, Nam-Joon Kim, Hyuk-Jae Lee"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00514v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 针对CT等3D医疗影像的自监督学习
tldr: 针对3D医学影像标注数据稀缺及现有自监督学习方法忽视空间连续性的问题，本文提出MAESIL框架。该方法引入“superpatch”概念，将3D体积划分为块状单元，并结合双重掩码策略的3D掩码自编码器，有效捕获轴向相干性和空间结构信息。实验证明，MAESIL在多个大型CT数据集上的重建性能显著优于传统自编码器模型，为3D医学影像预训练提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00514v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative reconstruction results of MAESIL across different anatomical views. The original input and reconstruction output are compared. From left to right: Axial, Coronal and Sagittal views.\", \"page\": 4, \"index\": 1, \"width\": 508, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.00514v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed MAESIL framework, which masks a high ratio of an input 3D superpatch, feeds only the visible portions to an encoder, and uses a decoder to reconstruct the original superpatch from the encoded information and learnable mask tokens.\", \"page\": 2, \"index\": 2, \"width\": 906, \"height\": 213}]"
motivation: 现有的医学影像自监督学习方法通常将3D扫描视为独立的2D切片，导致关键的轴向相干性和3D结构上下文信息丢失。
method: 提出一种基于“superpatch”块状单元的3D掩码自编码器框架，通过双重掩码策略在保持计算效率的同时学习全面的空间表示。
result: 在三大公开CT数据集上的实验结果显示，MAESIL在PSNR和SSIM等关键重建指标上均显著优于AE、VAE和VQ-VAE。
conclusion: MAESIL通过高效利用3D结构信息，为3D医学影像任务提供了一种鲁棒且实用的自监督预训练解决方案。
---

## 摘要
为三维（3D）医学影像（如计算机断层扫描，CT）训练深度学习模型，从根本上面临着标注数据稀缺的挑战。虽然在自然图像上进行预训练很常见，但这会导致显著的领域偏移，从而限制性能。在无标注医学数据上进行自监督学习（SSL）已成为一种强大的解决方案，但主流框架往往未能充分利用 CT 扫描固有的 3D 特性。这些方法通常将 3D 扫描视为一组独立的 2D 切片，这种方法从根本上丢弃了关键的轴向连贯性和 3D 结构上下文。为了解决这一局限性，我们提出了用于增强型自监督医学图像学习的自编码器（MAESIL），这是一种旨在高效捕获 3D 结构信息的新型自监督学习框架。其核心创新是“超级块”（superpatch），这是一种基于 3D 块的输入单元，平衡了 3D 上下文保留与计算效率。我们的框架将体数据划分为超级块，并采用带有双重掩码策略的 3D 掩码自编码器策略，以学习全面的空间表示。我们在三个不同的、大规模公开 CT 数据集上验证了我们的方法。实验结果表明，MAESIL 在 PSNR 和 SSIM 等关键重建指标上，相较于 AE、VAE 和 VQ-VAE 等现有方法表现出显著改进。这确立了 MAESIL 作为 3D 医学影像任务中一种稳健且实用的预训练解决方案。

## Abstract
Training deep learning models for three-dimensional (3D) medical imaging, such as Computed Tomography (CT), is fundamentally challenged by the scarcity of labeled data. While pre-training on natural images is common, it results in a significant domain shift, limiting performance. Self-Supervised Learning (SSL) on unlabeled medical data has emerged as a powerful solution, but prominent frameworks often fail to exploit the inherent 3D nature of CT scans. These methods typically process 3D scans as a collection of independent 2D slices, an approach that fundamentally discards critical axial coherence and the 3D structural context. To address this limitation, we propose the autoencoder for enhanced self-supervised medical image learning(MAESIL), a novel self-supervised learning framework designed to capture 3D structural information efficiently. The core innovation is the 'superpatch', a 3D chunk-based input unit that balances 3D context preservation with computational efficiency. Our framework partitions the volume into superpatches and employs a 3D masked autoencoder strategy with a dual-masking strategy to learn comprehensive spatial representations. We validated our approach on three diverse large-scale public CT datasets. Our experimental results show that MAESIL demonstrates significant improvements over existing methods such as AE, VAE and VQ-VAE in key reconstruction metrics such as PSNR and SSIM. This establishes MAESIL as a robust and practical pre-training solution for 3D medical imaging tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《MAESIL: Masked Autoencoder for Enhanced Self-supervised Medical Image Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：3D 医学影像（如 CT）深度学习面临两大难题：一是高质量标注数据极度稀缺，标注成本高昂；二是使用自然图像（如 ImageNet）预训练的模型存在严重的“领域偏移”（Domain Shift），在医学任务上表现受限。
*   **现有局限**：目前的自监督学习（SSL）框架（如 MedMAE）虽尝试利用医学数据，但大多将 3D CT 扫描简化为独立的 2D 切片处理。这种做法丢失了关键的**轴向相干性**（Axial Coherence）和 **3D 结构上下文**，导致模型无法理解解剖结构的连续性。
*   **研究目标**：开发一种既能保留 3D 空间信息，又能兼顾计算效率的自监督预训练框架。

### 2. 核心方法论
MAESIL 框架的核心在于将 3D 掩码自编码器（MAE）应用于特定的 3D 输入单元：
*   **Superpatch（超级块）概念**：为了平衡 3D 上下文和计算负载，模型不直接处理整个 512³ 的体积，而是将其划分为 64 个非重叠的 $128 \times 128 \times 128$ 的“超级块”。
*   **分级标记化（Tokenization）**：每个超级块进一步细分为 $8 \times 8 \times 8$ 的局部标准块（Standardized Patches），产生 4,096 个 Token，通过 3D 卷积层嵌入到潜空间。
*   **双重掩码策略（Dual-masking Strategy）**：
    1.  **平面内掩码（Plane-wise）**：在每个 2D 平面内随机丢弃 75% 的块。
    2.  **轴向掩码（Axis-wise）**：沿 S-I（上下）轴移除连续 50% 的切片块。
*   **编码器-解码器架构**：采用 Transformer 结构。编码器仅处理可见的 Token；解码器接收编码后的嵌入和可学习的掩码 Token，通过自注意力机制重建原始的 3D 超级块。

### 3. 实验设计
*   **数据集**：使用了三个大规模公开 CT 数据集，并将其合并为一个统一的数据集以增强泛化性：
    *   **BTCV**：腹部器官分割（30 例）。
    *   **LIDC-IDRI**：胸部 CT 扫描（1,018 例）。
    *   **TotalSegmentatorV2**：全身 104 个解剖结构（1,204 例），具有极高的多样性。
*   **Benchmark 与对比方法**：
    *   对比了标准的生成式基准模型：**AE**（自编码器）、**VAE**（变分自编码器）和 **VQ-VAE**（矢量量化变分自编码器）。
    *   评估指标：PSNR（峰值信噪比）、SSIM（结构相似性）和 LPIPS（感知相似度）。

### 4. 资源与算力
*   **文中说明**：论文**未明确说明**具体的 GPU 型号、数量、训练时长或内存消耗。
*   **技术细节提及**：提到了嵌入维度为 768，掩码比例为 75%，但缺乏硬件环境的详细描述。

### 5. 实验数量与充分性
*   **实验规模**：在合并后的超大规模数据集上进行了统一预训练，并在三个子数据集上分别进行了定量评估（PSNR 分解）。
*   **充分性评价**：
    *   **优点**：数据集覆盖了腹部、胸部和全身，样本量超过 2,200 例，具有较强的统计说服力。
    *   **不足**：缺乏针对下游任务（如分割、分类）的微调实验，目前仅停留在重建性能的对比；缺乏针对掩码比例或超级块大小的消融实验。

### 6. 主要结论与发现
*   **性能领先**：MAESIL 在所有指标上均显著优于基准模型。其 PSNR 达到 30.28（比 AE 高 5.05），SSIM 达到 0.98（接近完美重建），LPIPS 为 0.26（感知损失最低）。
*   **3D 上下文的重要性**：定性结果显示，MAESIL 能在轴向、冠状和矢状面同时实现高质量重建，证明其成功捕获了 3D 空间连续性。
*   **鲁棒性**：即使在 TotalSegmentator 这种极具挑战性的多样化数据集上，模型依然能保持稳健的重建能力。

### 7. 优点（亮点）
*   **创新的输入单元**：Superpatch 设计巧妙地解决了 3D 数据处理中“全量计算太重”与“2D 切片信息太碎”之间的矛盾。
*   **双重掩码机制**：通过轴向掩码强制模型学习层间关系，这比简单的随机掩码更符合医学影像的特性。
*   **跨领域泛化**：通过合并不同部位的数据集进行训练，提升了模型的通用表征能力。

### 8. 不足与局限
*   **重建伪影**：作者承认在重建输出中观察到固定模式的伪影（Patch-based 常见问题），这可能影响精细结构的还原。
*   **下游验证缺失**：论文主要关注重建质量（Pre-training 阶段），尚未展示该预训练权重在实际临床任务（如肿瘤检测）中的性能提升幅度。
*   **算力细节不明**：缺乏训练成本的量化信息，难以评估该方法在普通医疗研究机构中的可迁移性。

（完）
