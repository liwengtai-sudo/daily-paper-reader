---
title: Photonic convolutional neural network with pre-trained in-situ training
title_zh: 结合预训练与原位训练的光子卷积神经网络
authors: "Saurabh Ranjan, Sonika Thakral, Amit Sehgal"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02429v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于图像分类的光子卷积神经网络
tldr: "本研究提出了一种全光卷积神经网络（PCNN），旨在解决传统光子计算中频繁的光电转换能耗问题。该系统利用MZI网格、WDM池化和微环谐振器非线性单元，在光域内完成了MNIST图像分类。通过结合数字孪生离线预训练与SPSA原位微调的混合训练方法，实现了94%的准确率，且能效比传统GPU高出100至242倍，展现了极高的鲁棒性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02429v1/fig-001.webp\", \"caption\": \"Figure 2: Hardware PCNN confusion matrix on the full MNIST test set (10,000 images). Rows represent true digit classes and columns represent predicted classes. The strong diagonal indicates high per-class accuracy across all 10 digits.\", \"page\": 5, \"index\": 1, \"width\": 640, \"height\": 558}, {\"url\": \"assets/figures/arxiv/2604.02429v1/fig-002.webp\", \"caption\": \"Figure 1: Digital twin pre-training dynamics. (Left) Cross-entropy loss curve over 20 training epochs. (Right) Training and test accuracy curves, converging to 96.92% test accuracy in 20 epochs.\", \"page\": 3, \"index\": 2, \"width\": 976, \"height\": 298}]"
motivation: 针对现有光子计算架构中频繁的光电转换导致的能效瓶颈，探索全光域卷积神经网络的实现。
method: 采用MZI网格、波分复用池化及微环谐振器构建全光架构，并结合数字孪生预训练与原位SPSA算法进行混合训练。
result: "在MNIST数据集上达到94%的准确率，且在单次推理能效上比电子GPU提升了100至242倍。"
conclusion: 该全光卷积神经网络不仅显著提升了计算能效，还通过混合训练策略有效克服了物理参数训练与热串扰带来的挑战。
---

## 摘要
光子计算是一种具有巨大潜力克服电子冯·诺依曼架构能量瓶颈的计算范式。吞吐量和功耗是互补金属氧化物半导体（CMOS）芯片的根本限制，而卷积神经网络（CNN）正在变革机器学习、计算机视觉和其他基于图像的应用。在这项工作中，我们提出并验证了一种全光子卷积神经网络（PCNN），它完全在光域内执行 MNIST 图像分类，实现了 94% 的测试准确率。与依赖频繁光-电-光（O/E/O）转换的现有架构不同，我们的系统利用马赫-曾德尔干涉仪（MZI）网格、波分复用（WDM）池化以及基于微环谐振器的非线性来维持相干处理。最大池化单元完全在硅基光子学上实现，不需要光电或电转换。为了克服训练物理相移器参数的挑战，我们引入了一种混合训练方法，部署了一个数学上精确的可微数字孪生模型进行离位（ex-situ）反向传播，随后通过同时扰动随机逼近（SPSA）算法进行原位（in-situ）微调。我们的评估表明，该系统对热串扰具有显著的鲁棒性（在严重耦合下准确率仅下降 0.43%），并且在单幅图像推理的能效方面比最先进的电子 GPU 高出 100 到 242 倍。

## Abstract
Photonic computing is a computing paradigm which have great potential to overcome the energy bottlenecks of electronic von Neumann architecture. Throughput and power consumption are fundamental limitations of Complementary-metal-oxide-semiconductor (CMOS) chips, therefore convolutional neural network (CNN) is revolutionising machine learning, computer vision and other image based applications. In this work, we propose and validate a fully photonic convolutional neural network (PCNN) that performs MNIST image classification entirely in the optical domain, achieving 94 percent test accuracy. Unlike existing architectures that rely on frequent in-between conversions from optical to electrical and back to optical (O/E/O), our system maintains coherent processing utilizing Mach-Zehnder interferometer (MZI) meshes, wavelength-division multiplexed (WDM) pooling, and microring resonator-based nonlinearities. The max pooling unit is fully implemented on silicon photonics, which does not require opto-electrical or electrical conversions. To overcome the challenges of training physical phase shifter parameters, we introduce a hybrid training methodology deploying a mathematically exact differentiable digital twin for ex-situ backpropagation, followed by in-situ fine-tuning via Simultaneous Perturbation Stochastic Approximation (SPSA) algorithm. Our evaluation demonstrates significant robustness to thermal crosstalk (only 0.43 percent accuracy degradation at severe coupling) and achieves 100 to 242 times better energy efficiency than state-of-the-art electronic GPUs for single-image inference.

---

## 论文详细总结（自动生成）

这是一份关于论文《Photonic convolutional neural network with pre-trained in-situ training》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着人工智能对算力需求的指数级增长，传统基于 CMOS 的电子处理器（如 GPU、TPU）面临严重的能效瓶颈和延迟限制。光子计算因其高带宽、低能耗和光速处理能力成为替代方案。然而，现有的光子卷积神经网络（PCNN）大多是“混合型”的，即在卷积、池化或非线性激活环节需要频繁进行光电转换（O/E/O），这极大地增加了功耗和延迟。此外，物理光子芯片的参数训练（如相移器调节）极具挑战。本研究旨在实现一种**全光域处理**的 PCNN，并解决其在物理硬件上的训练难题。

### 2. 方法论：核心思想与关键技术
论文提出了一种全光子架构及配套的混合训练流程：
*   **全光架构组件**：
    *   **卷积层**：采用深度可分离卷积设计。利用 Clements 分解的 MZI 网格（CMXU）实现矩阵乘法。
    *   **全光池化**：利用多模干涉仪（MMI）、双稳态盘式谐振器和 GST 相变材料实现波分复用（WDM）最大池化，无需电信号介入。
    *   **非线性激活（NOFU）**：通过 32 个高 Q 值微环谐振器的载流子注入非线性实现。
*   **混合训练流程**：
    *   **离位（Ex-situ）预训练**：开发了一个基于 PyTorch 的**可微数字孪生模型**，精确模拟硬件的损耗、噪声和非线性。在 GPU 上利用反向传播训练出初始相位参数。
    *   **原位（In-situ）微调**：将预训练参数映射到硬件后，使用**同时扰动随机逼近（SPSA）算法**。该算法通过对所有参数同时施加随机扰动，仅需两次前向传播即可估计梯度，从而修正硬件噪声和热串扰。

### 3. 实验设计
*   **数据集**：标准的 MNIST 手写数字分类数据集（包含 10,000 张测试图像）。
*   **Benchmark（基准）**：对比了主流的电子 GPU 加速器，包括 NVIDIA T4、H100 和 A100。
*   **对比维度**：主要对比了推理准确率、鲁棒性（针对热串扰）、推理延迟以及单次推理能效（Energy per Inference）。

### 4. 资源与算力
*   **离位预训练**：使用了 NVIDIA RTX 3050 GPU (4GB VRAM)。
*   **硬件模拟与微调**：在 Intel i3 CPU (16GB RAM) 上进行硬件仿真测试。
*   **训练时长**：数字孪生模型在 20 个 Epoch 内收敛。
*   **芯片规格**：模拟了一个 18 × 18 $mm^2$ 的集成芯片，包含 2,132 个可调热光相移器。

### 5. 实验数量与充分性
*   **实验覆盖**：
    1.  数字孪生模型的收敛性实验。
    2.  理想硬件下的推理准确率测试。
    3.  **鲁棒性实验**：引入了严重的物理热串扰（耦合因子 0.1），测试性能跌落情况。
    4.  **消融/对比实验**：对比了 SPSA 微调前后的准确率变化。
    5.  **能效分析**：基于不同相移器技术（标准热光、MEMS 等）的性能预测。
*   **评价**：实验设计较为全面，涵盖了从算法模拟到物理非理想特性的讨论。但实验主要集中在 MNIST 这一相对简单的任务上，缺乏在更复杂数据集（如 CIFAR-10 或 ImageNet）上的验证。

### 6. 主要结论与发现
*   **准确率**：在 MNIST 上实现了 **94.23%** 的测试准确率。
*   **鲁棒性**：在严重热串扰下，准确率仅下降 0.43%；通过 SPSA 原位微调，性能可恢复至 94%。
*   **能效极高**：单次推理能耗仅为 12.4 μJ，比 NVIDIA A100 GPU 高出 **242 倍**。
*   **延迟极低**：端到端推理延迟约为 0.8 μs，主要受限于图像块的串行输入流。

### 7. 优点（亮点）
*   **全光链路**：实现了从卷积、池化到非线性的全光处理，消除了 O/E/O 转换的瓶颈。
*   **高保真数字孪生**：数字孪生与硬件模拟的误差小于 $10^{-15}$，使得预训练参数可以 1:1 直接映射，无需复杂的 SVD 分解。
*   **高效训练策略**：结合了离线反向传播的高效性和在线 SPSA 的抗噪性，解决了光子芯片难以直接计算梯度的痛点。

### 8. 不足与局限
*   **任务复杂度有限**：目前仅验证了 MNIST（28x28 灰度图），对于高分辨率、多通道的复杂图像任务，光子网格的规模扩展性（Scalability）尚待证明。
*   **串行瓶颈**：推理延迟主要受限于图像块的串行输入（Streaming），这限制了光子计算本应具备的超高吞吐量优势。
*   **硬件实现假设**：论文部分结果基于仿真和组件级功率追踪，实际大规模集成芯片的制造公差和长期稳定性仍需物理原型机验证。

（完）
