---
title: $p$-adic Character Neural Network
title_zh: $p$ 进特征标神经网络
authors: Tomoki Mihara
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29905v1"
tags: ["query:dl"]
score: 9.0
evidence: p-adic神经网络结构的新框架
tldr: 本文提出了一种新型的p-adic神经网络框架。不同于以往使用受精度超参数影响的特征函数族作为激活函数的方法，该研究采用p-进整数拓扑阿贝尔群上单一的单射p-进特征作为激活函数。作者证明了该框架下的p-进通用近似定理，并成功将网络训练问题转化为有限环上多项式方程的可行性问题，为p-adic深度学习提供了新的理论基础。
source: arxiv
selection_source: fresh_fetch
motivation: 旨在改进传统p-adic神经网络中激活函数构造复杂且依赖精度超参数的局限性。
method: 利用p-进整数环上的单个单射p-进特征作为神经网络的激活函数。
result: 证明了该新型p-adic神经网络的通用近似定理，并将其简化为模p幂次有限环上的多项式方程求解问题。
conclusion: 该研究简化了p-adic神经网络的数学结构，并建立了神经网络表示与代数几何计算之间的理论联系。
---

## 摘要
我们提出了一种新的 $p$ 进神经网络框架。与 S. Albeverio、A. Khrennikov 和 B. Tirrozi 最初提出的使用由精度超参数索引的特征函数族作为激活函数的 $p$ 进神经网络不同，我们使用定义在 $p$ 进整数拓扑阿贝尔群 $\mathbb{Z}_p$ 上的单个单射 $p$ 进特征标作为激活函数。我们证明了该形式 $p$ 进神经网络的 $p$ 进万能逼近定理，并将其归约为 $p$ 的幂次模有限整数环上的多项式方程的可解性问题。

## Abstract
We propose a new frame work of $p$-adic neural network. Unlike the original $p$-adic neural network by S.\ Albeverio, A.\ Khrennikov, and B.\ Tirrozi using a family of characteristic functions indexed by hyperparameters of precision as activation functions, we use a single injective $p$-adic character on the topological Abelian group $\mathbb{Z}_p$ of $p$-adic integers as an activation function. We prove the $p$-adic universal approximation theorem for this formulation of $p$-adic neural network, and reduce it to the feasibility problem of polynomial equations over the finite ring of integers modulo a power of $p$.

---

## 论文详细总结（自动生成）

这是一份关于论文《$p$-adic Character Neural Network》（$p$ 进特征标神经网络）的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究背景**：$p$ 进数（$p$-adic numbers）在现代数论中处于核心地位，近年来在计算机科学（如聚类分析、神经网络）中也展现出潜力。传统的 $p$ 进神经网络（由 Albeverio 等人提出）使用一系列受精度参数影响的“特征函数”（characteristic functions）作为激活函数。
*   **核心问题**：传统方法存在两个主要缺陷：
    1.  激活函数不统一，需要根据精度调整超参数。
    2.  仿射变换（$Ax+b$）在 $p$ 进域上不一定能保持空间的紧致性，导致无法直接利用 $p$ 进 Stone-Weierstrass 定理进行多项式一致逼近，限制了优化算法的发展。
*   **整体含义**：本文旨在提出一种全新的 $p$ 进神经网络框架，通过引入单一的“$p$ 进特征标”作为激活函数，解决上述数学一致性问题，并建立神经网络与代数几何计算之间的理论桥梁。

### 2. 论文提出的方法论
*   **核心思想**：使用定义在 $p$ 进整数环 $\mathbb{Z}_p$ 上的**单射 $p$ 进特征标** $\chi$（一种连续群同态 $\mathbb{Z}_p \to \mathbb{Q}_p^\times$）作为唯一的激活函数。
*   **关键技术细节**：
    *   **激活函数选择**：推荐使用 $p$ 进指数函数 $\exp_p(qx)$ 的缩放版本。作者证明了只要特征标是单射的（对于 $p=2$ 需排除特定情况），就能保证网络的表达能力。
    *   **网络构造**：定义为 $f(x) = C\chi(Ax + b)$，其中权重 $A$ 和偏置 $b$ 被限制在 $\mathbb{Z}_p$ 内，而输出系数 $C$ 在 $\mathbb{Q}_p$ 中。这种限制确保了变换后的空间依然落在紧致的 $\mathbb{Z}_p$ 内。
    *   **万能逼近定理**：作者利用 $p$ 进 Stone-Weierstrass 定理证明了：对于任何定义在 $\mathbb{Z}_p^n$ 上的连续函数，都可以通过这种特征标神经网络以任意精度逼近。
*   **算法流程**：
    1.  利用 Mahler 级数或泰勒展开式高效计算特征标。
    2.  将神经网络的训练（最小化误差）转化为在有限环 $\mathbb{Z}/p^E\mathbb{Z}$ 上求解多项式方程组的可行性问题。
    3.  提出使用**数字动态规划（Digit Dynamic Programming）**算法，按 $p$ 的幂次逐级诱导求解。

### 3. 实验设计
*   **实验性质**：**本文是一篇纯理论数学与算法分析论文**。
*   **数据集/场景**：文中**未包含**任何基于真实世界数据集（如 MNIST 或 CIFAR）的数值实验。
*   **Benchmark 与对比方法**：作者在理论层面对比了 Albeverio 的原始模型、N'guessan 的 van der Put 基模型以及 Zubarev 的多项式回归模型。对比的重点在于激活函数的数量（本文仅需 1 个，其他方法需要无穷多个或随精度增加）以及数学性质的优劣。

### 4. 资源与算力
*   **算力说明**：由于本文侧重于数学证明和算法复杂度的理论推导，文中**未提及**任何具体的硬件资源（如 GPU 型号、数量）或实际训练时长。
*   **复杂度分析**：作者提供了算法的时间和空间复杂度分析（以 $t(r)$ 和 $s(r)$ 表示算术操作的复杂度），重点讨论了在模 $p^E$ 意义下的计算开销。

### 5. 实验数量与充分性
*   **实验充分性评价**：从**数学证明**的角度看，论文非常充分。它严谨地证明了万能逼近定理，并给出了完整的算法伪代码（算法 1-6）。
*   **实证充分性评价**：从**机器学习工程**的角度看，缺乏数值实验是其主要不足。论文没有展示该网络在处理具体分类或回归任务时的收敛速度、鲁棒性或泛化能力。

### 6. 论文的主要结论与发现
1.  **理论可行性**：证明了基于单一 $p$ 进特征标的神经网络具有万能逼近性质。
2.  **计算转化**：成功将 $p$ 进神经网络的优化问题简化为模 $p^E$ 环上的多项式方程求解问题。
3.  **算法路径**：确定了通过数字动态规划和 Gröbner 基方法解决该类网络训练问题的技术路线。

### 7. 优点
*   **数学优雅性**：相比之前需要无穷多个激活函数的方法，本文仅用一个特征标就实现了万能逼近，结构更简洁。
*   **理论完备性**：解决了 $p$ 进空间中仿射变换不保紧致性的长期问题，使得 Stone-Weierstrass 定理的应用变得合法。
*   **跨学科连接**：将深度学习的训练问题与数论中的多项式方程求解（如 Hensel 引理的推广）联系起来。

### 8. 不足与局限
*   **缺乏实证验证**：没有在实际计算机上运行算法，无法评估其在噪声数据下的表现。
*   **计算复杂度风险**：虽然理论上可行，但随着维度 $D$ 和精度 $E$ 的增加，多项式方程组的可行性问题（尤其是 APX-complete 类问题）可能会遭遇维度灾难。
*   **应用范围限制**：目前仅讨论了单层隐藏层的结构，尚未扩展到深层架构（Deep Architecture）的理论分析。

（完）
