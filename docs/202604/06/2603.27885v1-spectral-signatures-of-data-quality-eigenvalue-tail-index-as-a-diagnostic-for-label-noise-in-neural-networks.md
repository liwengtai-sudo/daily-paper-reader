---
title: "Spectral Signatures of Data Quality: Eigenvalue Tail Index as a Diagnostic for Label Noise in Neural Networks"
title_zh: 数据质量的谱特征：特征值尾部指数作为神经网络标签噪声的诊断指标
authors: Matthew Loftus
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27885v1"
tags: ["query:dl"]
score: 8.0
evidence: 神经网络权重矩阵的光谱特性用于预测测试准确性和标签噪声
tldr: 本研究探讨了神经网络权重矩阵的谱特性与测试准确率之间的关系。研究发现，瓶颈层特征值分布的尾部指数 alpha 在预测由标签噪声引起的数据质量下降方面具有极高的准确性（R^2=0.984），远超传统指标。尽管它在超参数变化下的泛化预测能力有限，但作为数据质量诊断工具表现出色，能有效识别 CIFAR-10N 中的真实人类标注错误，为理解模型内部表征与数据质量的关系提供了新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.27885v1/fig-001.webp\", \"caption\": \"FIG. 1. Spectral evolution during training, plotted against training loss (right = untrained, left = converged). Real labels (blue) and shuffled labels (red) show clearly separated trajectories for all bulk observables but identical ⟨r⟩.\", \"page\": 4, \"index\": 1, \"width\": 514, \"height\": 283}]"
motivation: 旨在研究神经网络权重矩阵的谱特性是否可以作为预测模型泛化能力和评估训练数据质量的可靠指标。
method: 重点分析了瓶颈层权重矩阵特征值分布的尾部指数 alpha，并在多种架构和数据集上对比了其在不同噪声水平和超参数配置下的表现。
result: 尾部指数 alpha 在检测标签噪声方面表现卓越（R^2=0.984），但在超参数变化场景下预测能力较弱，且成功识别了现实中的人类标注错误。
conclusion: 尾部指数 alpha 并非通用的泛化预测器，而是一个强大的数据质量诊断工具，能够灵敏地捕捉训练集的退化情况。
---

## 摘要
我们研究了神经网络权重矩阵的谱性质是否能够预测测试准确率。在受控的标签噪声变化下，网络瓶颈层特征值分布的尾部指数 alpha 预测测试准确率的留一法（LOO）R^2 达到 0.984（包含 21 个噪声水平，每个水平 3 个随机种子），远超所有基准指标：表现最好的传统指标（最优层的 Frobenius 范数）的 LOO R^2 仅为 0.149。这一关系在三种架构（MLP、CNN、ResNet-18）和两个数据集（MNIST、CIFAR-10）中均成立。然而，在固定数据质量下的超参数变化（180 种配置，涵盖不同的宽度、深度、学习率和权重衰减）中，所有谱指标和传统指标的预测能力都很弱（R^2 < 0.25），简单的基准指标（全局 L_2 范数，LOO R^2 = 0.219）略优于谱指标（尾部指数 alpha，LOO R^2 = 0.167）。因此，我们将尾部指数定位为一种数据质量诊断工具：它是标签损坏和训练集退化的强力检测器，而非通用的泛化预测指标。一个在合成噪声上校准的噪声检测器成功识别了 CIFAR-10N 中的真实人类标注错误（检测到 9% 的噪声，误差为 3%）。我们确定信息处理瓶颈层是该特征的发生地，并将这些观察结果与尖峰随机矩阵模型中的 BBP 相变联系起来。我们还报告了一个负面结果：由于 Wishart 普适性，能级间距比 <r> 对于权重矩阵不具有信息量。

## Abstract
We investigate whether spectral properties of neural network weight matrices can predict test accuracy. Under controlled label noise variation, the tail index alpha of the eigenvalue distribution at the network's bottleneck layer predicts test accuracy with leave-one-out R^2 = 0.984 (21 noise levels, 3 seeds per level), far exceeding all baselines: the best conventional metric (Frobenius norm of the optimal layer) achieves LOO R^2 = 0.149. This relationship holds across three architectures (MLP, CNN, ResNet-18) and two datasets (MNIST, CIFAR-10). However, under hyperparameter variation at fixed data quality (180 configurations varying width, depth, learning rate, and weight decay), all spectral and conventional measures are weak predictors (R^2 < 0.25), with simple baselines (global L_2 norm, LOO R^2 = 0.219) slightly outperforming spectral measures (tail alpha, LOO R^2 = 0.167). We therefore frame the tail index as a data quality diagnostic: a powerful detector of label corruption and training set degradation, rather than a universal generalization predictor. A noise detector calibrated on synthetic noise successfully identifies real human annotation errors in CIFAR-10N (9% noise detected with 3% error). We identify the information-processing bottleneck layer as the locus of this signature and connect the observations to the BBP phase transition in spiked random matrix models. We also report a negative result: the level spacing ratio <r> is uninformative for weight matrices due to Wishart universality.