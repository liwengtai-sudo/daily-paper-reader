---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中太字节级生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于TB级生物数据的微批次加载器
tldr: 针对生物大数据规模超出内存限制导致的训练瓶颈，本文推出了annbatch。它是专为anndata设计的原生小批量加载器，支持直接对磁盘上的大规模数据集进行核外训练。该工具在单细胞、影像和基因组学任务中表现卓越，将数据加载效率提升了10倍，使TB级数据的训练时间从数天缩短至数小时，且完美兼容scverse生态系统，为生物AI的规模化应用提供了关键基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01949v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-003.webp\", \"caption\": \"\", \"page\": 18, \"index\": 3, \"width\": 1280, \"height\": 960}]"
motivation: 生物数据集规模的急剧增长使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。
method: 开发了原生于anndata的加载器annbatch，通过支持磁盘数据的核外训练来解决内存限制问题。
result: 在多项生物学基准测试中，该工具将加载吞吐量提升了最高一个数量级，显著缩短了训练周期。
conclusion: annbatch为大规模生物AI提供了高效的数据加载方案，实现了在不放弃标准数据格式的前提下处理TB级数据。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集测定，以及在既定计算生态系统内的下游分析。在此，我们介绍了 annbatch，这是一个 anndata 原生的迷你批次（mini-batch）加载器，能够直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序的基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短到几小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能（AI）建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够使用日益庞大且多样化的数据集。

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

### 论文总结：AnnBatch —— 实现 anndata 中太字节级生物数据的训练

#### 1. 论文的核心问题与整体含义
随着生物技术的发展，生物数据集（如单细胞转录组、全基因组测序、高分辨率影像）的规模已达到太字节（TB）级别，远超系统内存容量。在训练深度学习模型时，**瓶颈已从模型计算转移到了数据加载（I/O）**。
*   **研究动机**：现有的高性能数据加载框架（如 NVIDIA Merlin）通常需要转换文件格式，这破坏了生物信息学社区广泛使用的 `anndata` 标准及 `scverse` 生态系统的兼容性。
*   **核心目标**：开发一种既能保持 `anndata` 原生兼容性，又能实现磁盘数据高速核外（out-of-core）训练的迷你批次加载器。

#### 2. 论文提出的方法论
AnnBatch 引入了一个端到端的高性能框架，其核心思想是通过**预洗牌（Pre-shuffling）**将随机访问转化为**伪随机块访问（Pseudo-random chunked access）**。
*   **预洗牌器（Preshuffler）**：
    *   针对磁盘上的 `anndata` 文件，采用延迟加载（Lazy loading）技术。
    *   将数据划分为用户定义的连续块，在内存中进行局部洗牌后重新写入磁盘（基于 Zarr 格式）。
    *   这种方式保证了训练时的随机性，同时允许加载器读取大块连续数据以利用顺序 I/O。
*   **高性能加载器（Data Loader）**：
    *   **底层优化**：利用 Rust 编写的 `zarrs-python` 桥接库，实现高效的 Zarr 数组索引。
    *   **关键技术**：采用 Linux 的 **Direct I/O** 功能绕过内核缓存；使用**固定内存（Pinned Memory）**加速数据向 GPU 的传输；支持异步 GPU 加速洗牌。
    *   **框架无关性**：不绑定特定的深度学习框架（如 PyTorch 或 TensorFlow）。

#### 3. 实验设计
论文在三个具有代表性的生物学场景中进行了基准测试：
*   **单细胞转录组学**：使用 **Tahoe-100M** 数据集（约 1 亿个细胞），对比 `scDataset` 和 `MappedCollection`。
*   **全基因组测序（WGS）**：使用 **1000 Genomes GRCh38** 数据集（扩展至 50 万样本），涵盖稀疏（罕见变异）和稠密（常见变异）两种模式，对比 `numpy memmap`。
*   **单细胞显微影像**：处理 128x128x5 的图像裁剪块，对比传统的 `h5ad` 随机索引加载。
*   **Benchmark 指标**：每秒处理样本数（samples/sec）、单轮训练（Epoch）耗时、磁盘占用及预洗牌开销。

#### 4. 资源与算力
论文在多种硬件环境下验证了性能：
*   **云端实例**：AWS `ml.g4dn.4xlarge`（配备 NVIDIA T4 GPU，受限于 125MB/s 的磁盘带宽）。
*   **高性能计算（HPC）**：de.NBI 节点（T4 GPU，更高 I/O 带宽）和 LRZ AI 分区（配备 **NVIDIA H100 GPU**）。
*   **训练时长示例**：在 Tahoe-100M 数据集上，AnnBatch 运行一个 Epoch 仅需 **17 分钟**，而对比方法 `MappedCollection` 需要 **21 小时**。

#### 5. 实验数量与充分性
*   **实验覆盖面**：论文针对不同数据特征（高样本量、高特征维度、大单样本载荷）设计了多组实验，涵盖了目前生物 AI 的主流方向。
*   **消融与对比**：对比了 Zarr 与 HDF5 格式的性能差异，并分析了预洗牌开销的摊销情况。
*   **客观性**：作者明确指出了在不同磁盘带宽（AWS vs HPC）下的性能差异，并讨论了操作系统缓存对小规模数据集基准测试的干扰，确保了在大规模数据场景下的评估是客观且符合实际的。

#### 6. 主要结论与发现
*   **吞吐量提升**：AnnBatch 的加载速度比现有工具快 **10 到 40 倍**。
*   **训练效率**：将 TB 级数据的训练时间从“天”缩短至“小时”。
*   **预洗牌价值**：虽然预洗牌有数小时的初始开销，但在第一个训练 Epoch 内即可通过节省的 I/O 时间实现成本回收（摊销）。
*   **格式建议**：论文强烈建议社区从 HDF5 (`.h5ad`) 转向 Zarr 格式，以获得更优的 I/O 性能。

#### 7. 优点
*   **生态兼容性**：完全集成于 `scverse`，输入输出均为标准的 `anndata`，无需改变用户现有的分析工作流。
*   **技术先进性**：引入了 Direct I/O 和 Rust 后端，解决了 Python 在处理大规模并发 I/O 时的性能瓶颈。
*   **通用性**：不仅适用于单细胞数据，还能无缝扩展至基因组学和影像学。

#### 8. 不足与局限
*   **采样限制**：由于采用连续块读取，目前难以直接实现复杂的**加权采样（Weighted Sampling）**，需在写入磁盘前预先处理。
*   **多模态支持**：目前尚未直接支持复杂的多模态数据结构（尽管可以通过扩展实现）。
*   **预洗牌开销**：对于仅需训练 1-2 个 Epoch 的极小模型，预洗牌的时间成本可能超过其带来的收益。
*   **硬件依赖**：Direct I/O 等特性主要在 Linux 环境下表现最佳，且性能高度依赖于底层磁盘的物理带宽。

（完）
