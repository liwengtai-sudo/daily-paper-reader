---
title: Memory-efficient optimization of implicit neural representations for CT reconstruction
title_zh: 用于 CT 重建的隐式神经表示的显存高效优化
authors: "Mahrokh Najaf, Gregory Ongie"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09884v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于 CT 重建的隐式神经表示优化
tldr: 隐式神经表示（INR）为CT重建提供了高效的参数化模型，但标准自动微分在模拟射线投影时会消耗海量显存，限制了其在3D成像中的应用。本文提出一种内存高效的随机梯度近似方法，通过将梯度分解为雅可比-向量积并进行随机子采样，实现了显存占用与梯度精度之间的权衡。实验证明，该方法在显著降低显存的同时，能保持与标准方法相当的重建质量，并成功应用于3D锥束CT的稀疏视图重建。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09884v1/fig-001.webp\", \"caption\": \"Fig. 2. Top row (2D FORBILD): Image MSE plots for INR training with FFNs, SIREN, and hash encoding, comparing auto-differentiation and gradient approximation (for both LS and FLS). Bottom row (3D Walnut): Image MSE plots for INR training with FFNs, SIREN, and hash encoding using gradient approximation (for both LS and FLS).\", \"page\": 3, \"index\": 1, \"width\": 836, \"height\": 474}, {\"url\": \"assets/figures/arxiv/2604.09884v1/fig-002.webp\", \"caption\": \"Fig. 1. 2D FORBILD sparse-view reconstructions obtained with conjugate gradient least squares (CGLS) and filtered backprojection (FBP), alongside reconstructions produced by training an INR using FFNs, SIREN, and Hash Encoding architectures. All images are shown on the scale [0.0200,0.0220] mm−1.\", \"page\": 3, \"index\": 2, \"width\": 758, \"height\": 517}]"
motivation: INR在CT重建中因模拟射线投影需要大量评估，导致GPU显存占用过高，限制了其在3D高分辨率成像中的应用。
method: 提出一种基于雅可比-向量积分解的随机梯度近似方法，通过对投影射线上的采样点进行随机子采样来降低显存需求。
result: 实验表明该方法在大幅减少显存消耗的情况下，其收敛行为和重建均方误差与标准INR训练方法相当。
conclusion: 该方法有效解决了INR在CT重建中的显存瓶颈，为显存受限环境下的3D及稀疏视图CT重建提供了高效的优化方案。
---

## 摘要
隐式神经表示 (INRs) 为 CT 重建提供了一种参数高效且完全可微的图像模型。然而，由于模拟射线投影需要大量的 INR 求值，使用标准自动微分技术优化用于 CT 重建的 INR 可能会消耗极高的 GPU 显存，在 3D 成像中尤为明显。为了解决这一问题，我们提出了一种显存高效的随机梯度近似方法，该方法基于将梯度分解为适用于随机子采样的雅可比-向量积 (Jacobian-vector product)。这种近似方法允许用户在 GPU 显存占用与梯度近似精度之间进行权衡。我们在合成 2D 数据上的实验表明，与标准的 INR 训练相比，梯度近似使用的 GPU 显存要少得多，同时在收敛行为和均方误差方面能产生相当的重建结果。最后，我们证明了所提方法能够在稀疏视图设置下实现显存高效的 3D 锥束 CT 重建。

## Abstract
Implicit neural representations (INRs) provide a parameter-efficient and fully differentiable image model for CT reconstruction. However, optimizing INRs for CT reconstruction using standard auto-differentiation techniques can be prohibitively GPU memory-intensive, especially in 3D imaging, due to the large number of INR evaluations needed to simulate ray projections. To address this issue, we propose a memory-efficient stochastic gradient approximation based on decomposing the gradient into a Jacobian-vector product that is amenable to stochastic subsampling. This approximation allows the user to trade-off between GPU memory usage and gradient approximation accuracy. Our experiments on synthetic 2D data demonstrate that gradient approximation uses far less GPU memory than standard INR training, while yielding reconstructions that are comparable in convergence behavior and mean squared error. Finally, we demonstrate that the proposed approach allows for memory-efficient 3D cone beam CT reconstruction in a sparse-view setting.

---

## 论文详细总结（自动生成）

这篇论文针对隐式神经表示（INR）在 CT 重建中的显存瓶颈问题提出了创新的优化方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在 CT 重建中，使用隐式神经表示（INR）作为图像模型具有参数高效和隐式正则化的优势。然而，传统的优化方法依赖于**端到端自动微分（Auto-differentiation）**。由于 CT 前向投影涉及大量射线积分采样，自动微分需要存储海量的中间计算状态，导致 GPU 显存占用随分辨率和维度（尤其是 3D）急剧膨胀，限制了高分辨率 3D 图像的重建。
*   **研究动机**：开发一种能够解耦投影算子与网络梯度计算的方法，以在有限的硬件资源（如单张消费级 GPU）上实现大规模 3D CT 重建。

### 2. 方法论
*   **核心思想**：利用链式法则将损失函数的梯度分解为**雅可比-向量积（Jacobian-vector product）**形式，并引入**随机子采样（Stochastic Subsampling）**来近似梯度。
*   **关键技术细节**：
    1.  **梯度分解**：将梯度表示为 $v = P^\top r$（残差的后向投影）与网络评估算子导数的乘积。
    2.  **解耦计算**：首先在不跟踪梯度的前提下，利用高效的 CT 算子计算前向投影和后向投影，得到权重向量 $v$。
    3.  **随机近似**：不再对全量像素/体素点求导，而是随机抽取空间坐标子集，构造一个“虚拟损失函数”（Virtual Loss）。
    4.  **算法流程**：
        *   计算当前 INR 的正向投影并求出残差。
        *   通过后向投影计算权重向量 $v$（此过程不产生梯度图）。
        *   随机采样坐标索引子集 $I$。
        *   仅针对子集 $I$ 计算虚拟损失 $\tilde{L}$，并通过自动微分更新 INR 参数 $\theta$。

### 3. 实验设计
*   **数据集/场景**：
    *   **2D 合成数据**：FORBILD 幻像，采用 180 个稀疏视图。
    *   **3D 真实数据**：Walnut（胡桃木）数据集，120 个稀疏视图，重建规模为 $501^3$ 体素。
*   **Benchmark（基准）**：
    *   **FBP/FDK**：传统的滤波反投影算法。
    *   **CGLS**：共轭梯度最小二乘法（像素/体素空间重建）。
*   **对比方法**：
    *   对比了三种 INR 架构：**FFNs**（傅里叶特征网络）、**SIREN**（正弦激活网络）、**Hash Encoding**（多分辨率哈希编码）。
    *   对比了两种损失函数：**LS**（最小二乘）和 **FLS**（滤波最小二乘）。
    *   对比了**自动微分（Auto-diff）**与**提出的梯度近似（Grad-approx）**在显存和精度上的差异。

### 4. 资源与算力
*   **硬件设备**：单张 **NVIDIA GeForce RTX 4090 GPU (24 GB RAM)**。
*   **算力表现**：
    *   在 2D 实验中，自动微分需 2.6GB-3.4GB 显存，而该方法仅需约 0.5GB-0.7GB。
    *   在 3D 实验中，标准自动微分因显存不足无法运行（Out of Memory），而该方法在 $501^3$ 分辨率下仅占用 6.3GB-8.4GB 显存。
*   **训练时长**：2D 实验运行 5000 次迭代，3D 实验运行 1000 次迭代（具体小时数未说明，但强调了显存的节省）。

### 5. 实验数量与充分性
*   **实验组数**：论文涵盖了 2D 和 3D 场景，测试了 3 种主流 INR 架构和 2 种损失函数，并提供了收敛曲线（MSE 随迭代次数的变化）。
*   **充分性与客观性**：实验设计较为充分。通过 2D 实验证明了近似梯度与全量梯度在收敛性上高度一致；通过 3D 实验证明了该方法在处理大规模真实数据时的可行性。对比了多种架构，增强了结论的普适性。

### 6. 主要结论与发现
*   **显存效率**：提出的梯度近似方法显著降低了峰值显存占用，使 3D 锥束 CT 重建在普通显卡上成为可能。
*   **重建质量**：尽管使用了随机子采样近似梯度，但其收敛行为和最终的均方误差（MSE）与标准自动微分方法基本持平，甚至在某些配置下表现更优。
*   **架构兼容性**：该方法对 FFNs、SIREN 和 Hash Encoding 等不同 INR 架构均有效。

### 7. 优点
*   **极高的显存效率**：成功解决了 INR 在 CT 领域应用的最大障碍。
*   **数学简洁性**：通过雅可比分解将复杂的投影算子移出自动微分计算图，逻辑清晰且易于实现。
*   **灵活性**：允许用户通过调整子采样比例（Batch Size）在显存占用和梯度精度之间进行权衡。

### 8. 不足与局限
*   **计算开销权衡**：虽然显存降低了，但随机梯度可能引入噪声，在某些极端稀疏采样下可能需要更多迭代次数才能收敛（文中未深入讨论采样率对收敛时间的具体影响）。
*   **超参数敏感性**：对于不同复杂度的物体，如何选择最优的子采样比例和学习率仍需经验调试。
*   **应用范围**：目前主要展示了稀疏视图下的表现，对于极高噪声或超大规模工业检测场景的鲁棒性有待进一步验证。

（完）
