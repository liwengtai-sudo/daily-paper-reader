---
title: "TTE-CAM: Built-in Class Activation Maps for Test-Time Explainability in Pretrained Black-Box CNNs"
title_zh: TTE-CAM：预训练黑盒 CNN 中用于测试时可解释性的内置类激活映射
authors: "Kerol Djoumessi, Philipp Berens"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26885v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 使用CNN进行医学图像分析的可解释性
tldr: 针对医疗影像分析中CNN模型透明度不足的问题，本文提出TTE-CAM框架。该方法通过将预训练黑盒CNN的分类层替换为基于卷积的结构，并利用原始权重初始化，在不损失预测性能的前提下，将黑盒模型转化为具有内置可解释性的模型。TTE-CAM在保持高精度的同时，提供了比传统事后解释方法更具忠实度的解释结果，为临床应用提供了可靠的决策支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.26885v1/fig-001.webp\", \"caption\": \"Figure 1: Qualitative explanations comparison. The first column shows a DR fundus with clinical annotations (green markers) and a pneumonia CXR with groundtruth bounding boxes (green). Columns 2-6 show post-hoc saliency maps; the last column shows TTE-CAM explanations. CXR scores indicate activation precision.\", \"page\": 2, \"index\": 1, \"width\": 897, \"height\": 261}]"
motivation: 现有的事后解释方法不够忠实，而原生可解释架构往往会牺牲模型的预测性能。
method: 通过将预训练CNN的分类头替换为卷积层并利用原权重初始化，使模型在推理阶段具备内置的类激活映射能力。
result: 该方法在保留原始黑盒模型预测精度的同时，生成了在定性和定量上均优于或等同于事后解释方法的忠实解释。
conclusion: TTE-CAM成功弥合了模型性能与可解释性之间的鸿沟，为预训练黑盒模型在医疗等高风险领域的应用提供了有效的解释方案。
---

## 摘要
卷积神经网络（CNN）在医学图像分析中取得了最先进的性能，但其不透明性限制了其在关键临床场景中的应用。现有方法面临着一个根本性的权衡：事后（post-hoc）方法提供的是不忠实的近似解释，而固有可解释架构虽然具有忠实性，但往往会牺牲预测性能。我们引入了 TTE-CAM，这是一个测试时框架，通过使用基于卷积的分类头替换（由原始权重初始化）将预训练的黑盒 CNN 转换为自解释模型，从而弥补了这一差距。所得模型在保留黑盒预测性能的同时，提供了在定性和定量上均可与事后方法相媲美的内置忠实解释。代码可在 https://github.com/kdjoumessi/Test-Time-Explainability 获取。

## Abstract
Convolutional neural networks (CNNs) achieve state-of-the-art performance in medical image analysis yet remain opaque, limiting adoption in high-stakes clinical settings. Existing approaches face a fundamental trade-off: post-hoc methods provide unfaithful approximate explanations, while inherently interpretable architectures are faithful but often sacrifice predictive performance. We introduce TTE-CAM, a test-time framework that bridges this gap by converting pretrained black-box CNNs into self-explainable models via a convolution-based replacement of their classification head, initialized from the original weights. The resulting model preserves black-box predictive performance while delivering built-in faithful explanations competitive with post-hoc methods, both qualitatively and quantitatively. The code is available at https://github.com/kdjoumessi/Test-Time-Explainability

---

## 论文详细总结（自动生成）

这是一份关于论文《TTE-CAM: Built-in Class Activation Maps for Test-Time Explainability in Pretrained Black-Box CNNs》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：卷积神经网络（CNN）在医疗影像分析中表现卓越，但其“黑盒”性质限制了临床信任。
*   **核心痛点**：现有的可解释性方案存在两难困境：
    1.  **事后解释（Post-hoc）**：如 Grad-CAM，生成的显著图仅是近似，并不直接决定模型输出，存在“不忠实”风险。
    2.  **原生可解释架构**：虽然解释与预测一致，但通常需要复杂的重新训练，且往往以牺牲预测精度为代价。
*   **研究目标**：提出一种名为 **TTE-CAM** 的框架，旨在不进行重新训练、不损失精度的前提下，将预训练好的黑盒 CNN 转化为具有“内置”且“忠实”解释能力的模型。

### 2. 论文提出的方法论
*   **核心思想**：利用数学上的等价性，将 CNN 末端的全连接层（FCL）重构为 $1 \times 1$ 卷积层，从而将类激活映射（CAM）直接嵌入到模型的推理流程中。
*   **关键技术细节**：
    1.  **架构重构**：移除预训练模型中的全局平均池化（GAP）层和全连接层（FCL）。
    2.  **权重转移**：引入一个 $1 \times 1$ 卷积层，其卷积核权重直接从原 FCL 的权重中提取并初始化。由于 $1 \times 1$ 卷积在数学上等效于全连接操作，这种转换是无损的。
    3.  **推理流程**：特征图先经过 $1 \times 1$ 卷积生成“内置 CAM”，随后对这些 CAM 进行空间平均得到类别得分，最后通过 Softmax 输出预测。
*   **机械忠实性（Mechanistic Faithfulness）**：在这种结构下，最终的预测结果完全由 CAM 的空间平均值决定，因此解释图与预测逻辑在机械结构上是完全统一的。

### 3. 实验设计
*   **数据集**：
    1.  **Kaggle 眼底图数据集**：用于糖尿病视网膜病变（DR）的二分类。
    2.  **RSNA 胸部 X 光（CXR）数据集**：用于肺炎检测。
*   **Benchmark（基准模型）**：使用在上述数据集上预训练好的 **ResNet-50**。
*   **对比方法**：
    *   **无梯度方法**：CAM, ScoreCAM。
    *   **基于梯度方法**：GradCAM, LayerCAM。
    *   **基于反向传播方法**：Integrated Gradients (IG)。
*   **评估指标**：
    *   **预测性能**：准确率（Acc）、AUC。
    *   **解释质量**：Top-k 灵敏度（Top-k sensitivity）、Top-k 定位精度、激活精确度（Activation precision）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **算力需求分析**：由于 TTE-CAM 是一种“测试时（Test-time）”方法，它直接操作预训练权重而无需重新训练，因此其部署和转换过程对算力的需求极低，远低于需要多次前向传播的事后解释方法（如 ScoreCAM）。

### 5. 实验数量与充分性
*   **实验规模**：在两个具有代表性的医疗影像任务上进行了验证，并使用了临床专家标注的病灶区域作为金标准。
*   **充分性评价**：
    *   **对比全面**：涵盖了从经典的 CAM 到先进的 ScoreCAM 等五种主流方法。
    *   **客观性**：通过定量指标（如 Top-k 灵敏度）验证了模型在转换后预测精度完全没有下降（100% 保留），这证明了该方法的有效性。
    *   **局限性**：实验主要集中在 ResNet 架构上，对于其他复杂架构的泛化性实验相对较少。

### 6. 论文的主要结论与发现
*   **性能无损**：TTE-CAM 成功实现了在不改变预测输出（Acc 和 AUC 保持不变）的情况下，赋予黑盒模型内置解释能力。
*   **解释质量**：TTE-CAM 生成的解释图在定性和定量上均与主流事后解释方法具有竞争力，且在“忠实度”上更胜一筹。
*   **效率提升**：相比于需要多次前向传播或计算梯度的 post-hoc 方法，TTE-CAM 仅需一次前向传播即可同时获得预测和解释，显著降低了推理开销。

### 7. 优点
*   **即插即用**：无需重新训练模型，非常适合对已有的高性能医疗 AI 模型进行快速解释性增强。
*   **绝对忠实**：解决了“解释与预测脱节”的问题，预测结果直接源自对解释图的聚合。
*   **计算高效**：单次前向传播即可生成 CAM，适合对实时性要求高的临床场景。

### 8. 不足与局限
*   **分辨率瓶颈**：由于依赖于 CNN 深层的低分辨率特征图，生成的解释图较为粗糙，对于微小病灶（如早期 DR 的微动脉瘤）的精细定位能力有限。
*   **架构依赖**：该方法要求分类头的输入必须与特征图通道数匹配，目前主要适用于 ResNet 和 DenseNet，对于包含多层全连接层的 VGG 等老旧架构或某些 Vision Transformer 变体，适用性受限。
*   **任务范围**：目前仅在分类任务上验证，尚未探讨其在分割或检测任务中的表现。

（完）
