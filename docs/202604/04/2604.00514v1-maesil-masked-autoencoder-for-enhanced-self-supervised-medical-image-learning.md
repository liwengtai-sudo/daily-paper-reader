---
title: "MAESIL: Masked Autoencoder for Enhanced Self-supervised Medical Image Learning"
title_zh: MAESIL：用于增强型自监督医学图像学习的掩码自编码器
authors: "Kyeonghun Kim, Hyeonseok Jung, Youngung Han, Junsu Lim, YeonJu Jean, Seongbin Park, Eunseob Choi, Hyunsu Go, SeoYoung Ju, Seohyoung Park, Gyeongmin Kim, MinJu Kwon, KyungSeok Yuh, Soo Yong Kim, Ken Ying-Kai Liao, Nam-Joon Kim, Hyuk-Jae Lee"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00514v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 针对CT扫描等3D医学影像的自监督学习
tldr: 针对3D医疗影像标注数据稀缺及现有自监督学习方法忽视空间连续性的问题，本文提出MAESIL框架。该方法创新性地引入“超块（superpatch）”作为3D输入单元，结合3D掩码自编码器与双重掩码策略，有效捕捉CT扫描的轴向相干性和三维结构信息。在三大公开数据集上的实验证明，MAESIL在图像重建质量上显著优于AE、VAE等传统模型，为3D医疗影像预训练提供了高效且稳健的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00514v1/fig-001.webp\", \"caption\": \"Fig. 3. Qualitative reconstruction results of MAESIL across different anatomical views. The original input and reconstruction output are compared. From left to right: Axial, Coronal and Sagittal views.\", \"page\": 4, \"index\": 1, \"width\": 508, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.00514v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed MAESIL framework, which masks a high ratio of an input 3D superpatch, feeds only the visible portions to an encoder, and uses a decoder to reconstruct the original superpatch from the encoded information and learnable mask tokens.\", \"page\": 2, \"index\": 2, \"width\": 906, \"height\": 213}]"
motivation: 现有的自监督学习方法常将3D CT扫描视为独立的2D切片处理，导致关键的轴向相干性和三维结构上下文信息丢失。
method: 提出一种基于“超块”输入单元的3D掩码自编码器框架，利用双重掩码策略在平衡计算效率的同时学习全面的空间特征表示。
result: 在三个大规模公共CT数据集上，MAESIL在PSNR和SSIM等关键重建指标上均显著优于AE、VAE和VQ-VAE等主流模型。
conclusion: MAESIL通过高效利用3D结构信息，确立了其作为3D医疗影像任务中一种强大且实用的自监督预训练解决方案的地位。
---

## 摘要
针对计算机断层扫描（CT）等三维（3D）医学影像训练深度学习模型，面临着标注数据稀缺的根本挑战。虽然在自然图像上进行预训练很常见，但这会导致显著的领域偏移，从而限制性能。在无标注医学数据上进行自监督学习（SSL）已成为一种强大的解决方案，但主流框架往往未能充分利用 CT 扫描固有的 3D 特性。这些方法通常将 3D 扫描视为一组独立的 2D 切片，这种方法从根本上丢弃了关键的轴向连贯性和 3D 结构上下文。为了解决这一局限性，我们提出了用于增强型自监督医学图像学习的自编码器（MAESIL），这是一种旨在高效捕获 3D 结构信息的新型自监督学习框架。其核心创新是“超级块”（superpatch），这是一种基于 3D 块的输入单元，在保留 3D 上下文与计算效率之间取得了平衡。我们的框架将体数据划分为超级块，并采用带有双重掩码策略的 3D 掩码自编码器策略，以学习全面的空间表示。我们在三个不同的、大规模公开 CT 数据集上验证了我们的方法。实验结果表明，MAESIL 在 PSNR 和 SSIM 等关键重建指标上，相较于 AE、VAE 和 VQ-VAE 等现有方法表现出显著改进。这确立了 MAESIL 作为 3D 医学影像任务中一种稳健且实用的预训练解决方案。

## Abstract
Training deep learning models for three-dimensional (3D) medical imaging, such as Computed Tomography (CT), is fundamentally challenged by the scarcity of labeled data. While pre-training on natural images is common, it results in a significant domain shift, limiting performance. Self-Supervised Learning (SSL) on unlabeled medical data has emerged as a powerful solution, but prominent frameworks often fail to exploit the inherent 3D nature of CT scans. These methods typically process 3D scans as a collection of independent 2D slices, an approach that fundamentally discards critical axial coherence and the 3D structural context. To address this limitation, we propose the autoencoder for enhanced self-supervised medical image learning(MAESIL), a novel self-supervised learning framework designed to capture 3D structural information efficiently. The core innovation is the 'superpatch', a 3D chunk-based input unit that balances 3D context preservation with computational efficiency. Our framework partitions the volume into superpatches and employs a 3D masked autoencoder strategy with a dual-masking strategy to learn comprehensive spatial representations. We validated our approach on three diverse large-scale public CT datasets. Our experimental results show that MAESIL demonstrates significant improvements over existing methods such as AE, VAE and VQ-VAE in key reconstruction metrics such as PSNR and SSIM. This establishes MAESIL as a robust and practical pre-training solution for 3D medical imaging tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《MAESIL: Masked Autoencoder for Enhanced Self-supervised Medical Image Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在3D医学影像（如CT）领域，深度学习面临标注数据稀缺和“领域偏移”（Domain Shift，即使用自然图像预训练模型效果不佳）的双重挑战。
*   **研究动机**：现有的自监督学习（SSL）框架（如MedMAE）虽然尝试利用医学数据，但通常将3D CT扫描简化为一系列独立的2D切片处理。这种做法丢弃了关键的**轴向相干性（Axial Coherence）**和**3D结构上下文**，导致模型无法理解解剖结构的连续性。
*   **整体含义**：本文旨在开发一种既能保留3D空间信息，又能兼顾计算效率的自监督预训练框架，为3D医疗影像任务提供更强大的特征提取基础。

### 2. 核心方法论
*   **核心思想**：引入一种名为**“超级块”（Superpatch）**的3D输入单元，并结合改进的掩码自编码器（MAE）策略进行预训练。
*   **关键技术细节**：
    *   **超级块划分**：将512×512×512的原始CT体数据划分为64个非重叠的“超级块”（每个大小为128×128×128）。这种设计在全局3D上下文和局部计算负载之间取得了平衡。
    *   **标准化补丁化（Tokenization）**：每个超级块进一步细分为8×8×8的局部小补丁（共4096个Token），通过3D卷积层嵌入到潜空间。
    *   **双重掩码策略（Dual-masking Strategy）**：
        1.  **平面内掩码**：在每个2D平面内随机丢弃75%的补丁。
        2.  **轴向掩码**：沿上下（S-I）轴随机移除连续50%的切片块。这种极高比例且具有结构性的掩码迫使模型必须通过极少的可见信息推断复杂的3D结构。
    *   **编码器-解码器架构**：采用Transformer架构。编码器仅处理可见Token；解码器结合编码嵌入和可学习的掩码Token，通过自注意力机制重建原始超级块。

### 3. 实验设计
*   **数据集**：使用了三个大规模公共CT数据集的组合，以增强模型的泛化能力：
    *   **BTCV**：腹部器官分割数据（30例）。
    *   **LIDC-IDRI**：胸部CT扫描数据（1018例）。
    *   **TotalSegmentatorV2**：全身多器官分割数据（1204例，包含104个解剖结构）。
*   **Benchmark与对比方法**：
    *   对比了标准的生成式基准模型：**AE（自编码器）**、**VAE（变分自编码器）**和**VQ-VAE（矢量量化变分自编码器）**。
    *   评估指标：PSNR（峰值信噪比）、SSIM（结构相似性）和LPIPS（感知相似度）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的GPU型号、数量以及训练所需的总时长。
*   **实现细节**：提到了嵌入维度为768，掩码比例高达75%，这暗示了实验对显存和计算能力有一定要求，但缺乏具体的硬件环境描述。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在统一的大规模数据集上进行了综合对比实验。
    *   针对三个子数据集分别进行了PSNR性能测试（消融/验证性质）。
    *   展示了轴向、冠状面和矢状面三个视角的定性重建结果。
*   **充分性评价**：实验在数据集多样性上表现优秀（涵盖腹部、胸部、全身），对比基准涵盖了主流的生成模型。然而，**缺乏下游任务（如分类、分割）的微调实验**，仅通过重建质量来证明预训练效果，在SSL论文中略显单薄。

### 6. 主要结论与发现
*   **性能领先**：MAESIL在所有指标上均显著优于对比模型。其PSNR达到30.28（AE为25.23），SSIM接近完美（0.98），LPIPS最低（0.26）。
*   **3D理解力**：定性结果显示，MAESIL能高保真地重建脊椎和软组织，证明其成功学习到了跨轴的3D上下文信息。
*   **鲁棒性**：即使在TotalSegmentator这种极具挑战性的多样化数据集上，模型依然能保持稳健的重建能力。

### 7. 优点（亮点）
*   **创新的输入表征**：提出的“超级块”概念有效解决了3D数据处理中“信息丢失”与“计算爆炸”的矛盾。
*   **强悍的掩码策略**：双重掩码（平面+轴向）设计极大地提升了重建任务的难度，从而迫使模型学习更深层的解剖结构特征。
*   **跨领域泛化**：通过混合多个不同部位的数据集进行训练，增强了模型的通用性。

### 8. 不足与局限
*   **缺乏下游验证**：论文主要关注重建质量（Pretext Task），未展示该预训练模型在实际临床任务（如肿瘤检测、器官分割）中的性能提升幅度。
*   **重建伪影**：作者承认在重建输出中存在固定的模式伪影（Fixed-pattern artifacts），这与解码器的上采样过程有关。
*   **算力信息缺失**：未提供训练成本参考，难以评估该方法在普通医疗研究机构中的可迁移性。

（完）
