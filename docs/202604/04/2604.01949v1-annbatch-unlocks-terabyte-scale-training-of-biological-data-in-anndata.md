---
title: annbatch unlocks terabyte-scale training of biological data in anndata
title_zh: annbatch 实现了 anndata 中太字节规模生物数据的训练
authors: "Ilan Gold, Felix Fischer, Lucas Arnoldt, F. Alexander Wolf, Fabian J. Theis"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01949v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于TB级生物数据训练的生物信息学工具
tldr: 随着生物数据集规模突破内存限制，数据加载成为机器学习训练的主要瓶颈。本文推出 annbatch，这是一款原生支持 anndata 的小批量加载器，支持直接在磁盘数据集上进行核外训练。它在单细胞、显微镜和全基因组测序等任务中将加载吞吐量提升了一个数量级，将训练时间从数天缩短至数小时，且完全兼容 scverse 生态，为大规模生物 AI 提供了实用的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01949v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 684, \"height\": 684}, {\"url\": \"assets/figures/arxiv/2604.01949v1/fig-003.webp\", \"caption\": \"\", \"page\": 18, \"index\": 3, \"width\": 1280, \"height\": 960}]"
motivation: 生物数据集规模已超出系统内存，导致数据访问而非模型计算成为机器学习训练的主要瓶颈。
method: 开发了原生于 anndata 的 mini-batch 加载器 annbatch，支持直接对存储在磁盘上的数据集进行高效的核外训练。
result: 在多项生物学基准测试中，annbatch 将加载吞吐量提升了高达一个数量级，并将训练时间从数天缩短至数小时。
conclusion: annbatch 为大规模生物 AI 提供了高效的数据加载方案，在保持与标准生物数据格式兼容的同时实现了 TB 级数据的可扩展训练。
---

## 摘要
生物数据集的规模现在通常超过系统内存，使得数据访问而非模型计算成为训练机器学习模型的主要瓶颈。这一瓶颈在生物学领域尤为突出，因为广泛使用的社区数据格式必须支持异构元数据、稀疏和密集检测，以及在既定计算生态系统内的下游分析。在此，我们介绍了 annbatch，这是一种原生于 anndata 的小批量加载器，能够直接在基于磁盘的数据集上进行核外（out-of-core）训练。在单细胞转录组学、显微镜成像和全基因组测序的基准测试中，annbatch 将加载吞吐量提高了一个数量级，并将训练时间从几天缩短到几小时，同时保持与 scverse 生态系统的完全兼容。Annbatch 为可扩展的生物人工智能建立了实用的数据加载基础设施，使得在不放弃标准生物数据格式的情况下，能够使用日益庞大且多样化的数据集。

## Abstract
The scale of biological datasets now routinely exceeds system memory, making data access rather than model computation the primary bottleneck in training machine-learning models. This bottleneck is particularly acute in biology, where widely used community data formats must support heterogeneous metadata, sparse and dense assays, and downstream analysis within established computational ecosystems. Here we present annbatch, a mini-batch loader native to anndata that enables out-of-core training directly on disk-backed datasets. Across single-cell transcriptomics, microscopy and whole-genome sequencing benchmarks, annbatch increases loading throughput by up to an order of magnitude and shortens training from days to hours, while remaining fully compatible with the scverse ecosystem. Annbatch establishes a practical data-loading infrastructure for scalable biological AI, allowing increasingly large and diverse datasets to be used without abandoning standard biological data formats. Github: https://github.com/scverse/annbatch

---

## 论文详细总结（自动生成）

这是一份关于论文《annbatch unlocks terabyte-scale training of biological data in anndata》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在生物信息学领域，数据集规模（如单细胞转录组、全基因组测序）已达到太字节（TB）级别，远超系统内存容量。目前的瓶颈已从“模型计算”转移到了“数据加载（I/O）”。
*   **背景**：现有的高性能数据加载框架（如 NVIDIA Merlin, WebDataset）通常需要转换文件格式，导致与生物学界主流的 `anndata` 格式及 `scverse` 生态系统脱节。而原生支持 `anndata` 的现有工具（如 `scDataset`）在处理大规模数据时速度极慢，无法使 GPU 达到饱和。
*   **整体含义**：本文推出了 **annbatch**，这是一个原生集成于 `anndata` 的高性能小批量（mini-batch）加载器，旨在不改变数据格式的前提下，解决大规模生物 AI 训练中的 I/O 瓶颈。

### 2. 核心方法论
*   **核心思想**：通过“预洗牌（Pre-shuffling）”和“伪随机块访问（Pseudo-random chunked access）”相结合，将随机磁盘访问转变为高效的顺序读取。
*   **关键技术细节**：
    *   **预洗牌器（Preshuffler）**：在磁盘上对 `anndata` 文件进行分块读取、内存洗牌并重新写入为 Zarr 格式。这保证了后续加载时可以连续读取大块数据，同时维持训练所需的随机性。
    *   **高性能加载器**：利用 `zarrs-python`（基于 Rust 的后端）实现极速 I/O。
    *   **优化技术**：采用自定义非均匀切片索引、直接 I/O（Direct I/O，绕过 OS 缓存）、读取至固定内存（Pinned Memory）以及 GPU 加速洗牌。
    *   **生态兼容性**：输入输出均为标准的 `anndata` 对象，完全兼容 `scanpy` 等社区工具。

### 3. 实验设计
*   **数据集与场景**：
    1.  **单细胞转录组（scRNA-seq）**：使用 Tahoe100M 数据集（约 1 亿个细胞）。
    2.  **单细胞显微镜图像**：128x128x5 像素的裁剪图像。
    3.  **全基因组测序（WGS）**：基于 1000 Genomes 项目，扩展至 50 万样本，涵盖稀疏（罕见变异）和密集（常见变异）特征。
*   **对比方法（Baselines）**：
    *   `scDataset`
    *   `MappedCollection`（Lamin Labs 开发）
    *   `numpy memmap`（针对密集数据）
    *   `h5ad` 原生格式读取。

### 4. 资源与算力
*   **GPU/硬件型号**：
    *   主要基准测试在 **AWS ml.g4dn.4xlarge** 实例（配备 NVIDIA T4 GPU）上运行，以模拟标准云环境。
    *   高性能测试在 **NVIDIA HGX-H100** 单元和德国 de.NBI 集群（T4 GPU）上运行。
    *   部分扩展性测试使用了 **Intel Xeon Gold 6142M** CPU。
*   **训练时长/效率**：
    *   在 Tahoe100M 数据集上，annbatch 跑完一个 epoch 仅需 **17 分钟**（GPU 加速），而对比方法 `MappedCollection` 需要 **21 小时**。
    *   预洗牌过程（不需 GPU）耗时约 5.5 小时，但在第一个训练 epoch 内即可通过节省的 I/O 时间收回成本。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1 亿细胞的转录组到 50 万样本的全基因组，实验维度跨越了样本量大、特征维度高、单样本载荷大三种典型生物学场景。
*   **充分性与客观性**：
    *   作者在不同硬件环境（AWS vs. 本地 HPC）下进行了重复实验，证明了性能提升的稳定性。
    *   进行了消融/对比实验，如 Zarr vs. h5ad 格式对比、进程数扩展性分析。
    *   **公平性**：特别指出了 OS 缓存对小规模数据测试的干扰，并坚持在超出内存的大规模数据集上进行测试以确保结果真实。

### 6. 主要结论与发现
*   **吞吐量提升**：annbatch 的加载速度比现有工具快 **10 到 40 倍**。
*   **训练加速**：将原本需要数天的训练任务缩短至数小时。
*   **格式优势**：采用 Zarr 后端配合 Rust 桥接，显著优于传统的 HDF5 (h5ad) 格式。
*   **分布式支持**：在多 GPU 环境下，annbatch 能够提供足够的 I/O 吞吐量以维持 H100 等高端显卡的计算饱和，避免了分布式训练中的 I/O 瓶颈。

### 7. 优点
*   **无缝集成**：无需转换数据格式即可获得极致性能，保护了现有的生物信息学工作流。
*   **多模态通用性**：不仅适用于测序数据，也适用于图像和基因组稀疏矩阵。
*   **工程质量高**：引入了 Rust 后端和 Direct I/O 等底层优化，技术栈先进。
*   **开源贡献**：提供了易用的 API 和完善的文档。

### 8. 不足与局限
*   **采样策略限制**：由于采用连续读取模式，目前难以实现复杂的加权采样（Weighted Sampling），需要通过特定的磁盘写入顺序来变通。
*   **多模态支持**：目前尚未直接支持原生多模态数据的同步加载（虽可扩展，但非当前核心）。
*   **预洗牌开销**：虽然长期看是划算的，但对于仅训练 1 个 epoch 的极小任务，预洗牌的时间成本可能显得较高。
*   **硬件依赖**：在磁盘 I/O 极慢（如低端云存储）的环境下，GPU 加速的效果会受到物理限制。

（完）
