---
title: "LoGSAM: Parameter-Efficient Cross-Modal Grounding for MRI Segmentation"
title_zh: LoGSAM：用于 MRI 分割的参数高效跨模态定位
authors: "Mohammad Robaitul Islam Bhuiyan, Sheethal Bhat, Melika Qahqaie, Tri-Thien Nguyen, Paula Andrea Pérez Toro, Tomas Arias Vergara, Andreas Maier"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17576v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于脑肿瘤MRI分割的深度学习与跨模态定位技术
tldr: "本研究提出LoGSAM框架，旨在解决MRI脑肿瘤分割中任务特定模型对标注数据依赖性强的问题。该框架通过Whisper模型将放射科医生语音转化为文本，利用临床NLP提取提示词，并结合LoRA微调的Grounding DINO进行跨模态定位，最后利用生成的边界框引导冻结的MedSAM进行分割。该方法仅需更新5%的参数，在BRISC 2025数据集上达到80.32%的Dice得分，实现了高效的语音到分割流程。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.17576v1/fig-001.webp\", \"caption\": \"Table 4. LoRA ablation on GDINO fine-tuning. Reports the effect of LoRA rank size (32, 64, 128) and LoRA injection location at rank=64 on the trainable parameters and detection performance.\", \"page\": 8, \"index\": 1, \"width\": 696, \"height\": 229}, {\"url\": \"assets/figures/arxiv/2603.17576v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative results at different stages of the proposed detection–segmentation pipeline for two samples of each of the three tumor types.\", \"page\": 8, \"index\": 2, \"width\": 725, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2603.17576v1/fig-003.webp\", \"caption\": \"Fig. 1. Overview of the proposed speech-guided detection-to-segmentation pipeline for brain tumor analysis. The speech signal s(t) is transcribed and translated using Whisper [3] ASR to obtain an English transcript y = fASR(s(t)). The transcript is processed by spaCy [19] + negspaCy [18] to extract a tumor class cue and negation information, which are converted into a text prompt p = g(c, n). In parallel, the T1-weighted brain MRI I ∈ RH×W is provided to a LoRA-augmented GDINO localizer, which combines I and p to predict bounding boxes B = fGDINO,θ(I, p). Each predicted box bi ∈ B is then used as a prompt for MedSAM to generate a pixel-wise mask, yielding the final tumor segmentation output.\", \"page\": 4, \"index\": 3, \"width\": 725, \"height\": 183}]"
motivation: 针对现有MRI分割模型过度依赖标注数据且缺乏跨模态交互能力的问题，探索利用基础模型实现高效的临床语音驱动分割。
method: 结合Whisper语音识别、临床NLP提取提示词、LoRA微调的Grounding DINO目标检测以及冻结的MedSAM模型，构建了一个模块化的跨模态分割流水线。
result: "在BRISC 2025挑战赛中取得80.32%的Dice得分，并在真实放射科医生德语语音测试中实现了91.7%的病例级准确率。"
conclusion: 证明了通过极少量的参数更新，利用预训练基础模型构建模块化、语音驱动的医学图像分割系统是切实可行的。
---

## 摘要
使用磁共振成像 (MRI) 对脑肿瘤进行精确的定位和勾画，对于制定治疗方案和指导手术决策至关重要。然而，大多数现有方法依赖于特定任务的有监督模型，并受限于标注数据的稀缺性。为了解决这一问题，我们提出了 LoGSAM，这是一个参数高效、检测驱动的框架，它将放射科医生的口述转化为文本提示，用于基于基础模型的定位和分割。放射科医生的语音首先使用预训练的 Whisper ASR 模型进行转录和翻译，随后通过具有否定感知能力的临床自然语言处理 (NLP) 提取肿瘤特定的文本提示。这些提示通过经过 LoRA 适配的视觉语言检测模型 Grounding DINO (GDINO) 引导以文本为条件的肿瘤定位。LoRA 适配仅更新 5% 的模型参数，从而在保留预训练跨模态知识的同时，实现了计算高效的领域自适应。预测的边界框被用作 MedSAM 的提示，在无需任何额外微调的情况下生成像素级肿瘤掩码。在冻结的 MedSAM 上以 LoGSAM 衍生的先验为条件，在 BRISC 2025 数据集上达到了 80.32% 的最先进 Dice 分数。此外，我们使用资深放射科医生的德语口述在 12 例未见过的 MRI 扫描上评估了整个流程，实现了 91.7% 的病例级准确率。这些结果突显了通过智能利用预训练基础模型并进行极少参数更新，构建模块化“语音到分割”流程的可行性。

## Abstract
Precise localization and delineation of brain tumors using Magnetic Resonance Imaging (MRI) are essential for planning therapy and guiding surgical decisions. However, most existing approaches rely on task-specific supervised models and are constrained by the limited availability of annotated data. To address this, we propose LoGSAM, a parameter-efficient, detection-driven framework that transforms radiologist dictation into text prompts for foundation-model-based localization and segmentation. Radiologist speech is first transcribed and translated using a pretrained Whisper ASR model, followed by negation-aware clinical NLP to extract tumor-specific textual prompts. These prompts guide text-conditioned tumor localization via a LoRA-adapted vision-language detection model, Grounding DINO (GDINO). The LoRA adaptation updates using 5% of the model parameters, thereby enabling computationally efficient domain adaptation while preserving pretrained cross-modal knowledge. The predicted bounding boxes are used as prompts for MedSAM to generate pixel-level tumor masks without any additional fine-tuning. Conditioning the frozen MedSAM on LoGSAM-derived priors yields a state-of-the-art dice score of 80.32% on BRISC 2025. In addition, we evaluate the full pipeline using German dictations from a board-certified radiologist on 12 unseen MRI scans, achieving 91.7% case-level accuracy. These results highlight the feasibility of constructing a modular, speech-to-segmentation pipeline by intelligently leveraging pretrained foundation models with minimal parameter updates.

---

## 论文详细总结（自动生成）

### 论文总结：LoGSAM - 用于 MRI 分割的参数高效跨模态定位框架

#### 1. 核心问题与整体含义（研究动机和背景）
脑肿瘤（如胶质瘤、脑膜瘤等）的精确分割对临床治疗至关重要。然而，现有的深度学习分割模型面临两大挑战：
*   **数据依赖性强**：主流的监督学习模型需要大量昂贵的像素级标注数据。
*   **临床工作流脱节**：放射科医生在日常工作中通常通过口述报告进行诊断，而现有的自动化工具难以直接利用这些丰富的语音/文本信息作为监督信号。
**LoGSAM** 的研究动机在于利用预训练的基础模型（Foundation Models），构建一个“语音到分割”的自动化流水线，通过极少量的参数微调，实现从放射科医生口述到精确肿瘤掩码的转化。

#### 2. 核心思想与方法论
LoGSAM 采用了一种**检测驱动、参数高效**的跨模态框架，主要包含三个模块：
*   **语音到提示词生成（Speech-to-Prompt）**：
    *   使用 **Whisper (large-v3)** 模型将放射科医生的语音（如德语）转录并翻译为英文文本。
    *   利用 **spaCy** 和 **negspaCy** 进行临床自然语言处理，提取肿瘤类别（如胶质瘤）并识别否定词（如“无肿瘤”），生成结构化的文本提示词。
*   **LoRA 增强的 Grounding DINO (GDINO)**：
    *   核心定位模块。在预训练的 GDINO 视觉语言模型中引入 **LoRA（低秩自适应）** 技术。
    *   仅对注意力层、前馈网络和文本编码器的部分层进行微调（更新参数 < 5%），使其能够根据文本提示在 MRI 图像中预测肿瘤的边界框（Bounding Box）。
*   **MedSAM 分割**：
    *   将 GDINO 预测的边界框作为提示（Prompt）输入到 **MedSAM** 模型中。
    *   MedSAM 在完全冻结（无需微调）的状态下，根据边界框生成最终的像素级分割掩码。

#### 3. 实验设计
*   **数据集**：
    *   **BRISC 2025**：包含 6,000 张对比增强 T1 加权 MRI 扫描，涵盖胶质瘤、脑膜瘤、垂体瘤和健康样本。
    *   **Kaggle MRI (BBOX)**：作为分布外（OOD）测试集，包含 5,249 张图像，用于验证模型的泛化能力。
    *   **真实临床数据**：由资深放射科医生对 12 例未见过的 MRI 扫描进行德语口述标注。
*   **对比方法与基准**：
    *   对比了全参数微调的 GDINO 基准。
    *   对比了 SOTA 分割模型（如 Trust-Refined U-Net Ensemble）。
    *   在检测任务上参考了 YOLOv11 的表现。

#### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA A100 GPU (40GB RAM)**。
*   **训练细节**：训练时长为 125 个 epoch，使用 AdamW 优化器，学习率为 $2 \times 10^{-4}$，采用了 OneCycle 学习率调度策略。
*   **参数量**：LoRA 适配仅涉及约 4.96% 的可训练参数。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括在 BRISC 数据集上的性能测试、在 Kaggle 数据集上的跨域泛化测试，以及针对真实语音输入的端到端测试。
*   **消融实验**：针对 LoRA 的秩（Rank $r=32, 64, 128$）以及 LoRA 注入的位置（视觉编码器、解码器、特征增强器等）进行了详细的消融研究。
*   **评价**：实验设计较为充分，涵盖了从算法性能到临床应用场景的闭环验证，对比实验客观地展示了参数高效微调与全量微调之间的权衡。

#### 6. 主要结论与发现
*   **性能优异**：LoGSAM 在 BRISC 2025 上达到了 **80.32% 的 Dice 分数**，优于现有的 U-Net 集成方法，且达到了全量微调基准 98.6% 的性能。
*   **参数高效**：仅通过更新 **< 5%** 的参数，即可实现从通用领域到医学 MRI 领域的有效迁移。
*   **泛化性强**：在 OOD 数据集和真实放射科医生语音测试中表现稳健，语音到分割的病例级准确率达到 **91.7%**。
*   **LoRA 配置**：实验发现 $r=64$ 是性能与泛化性的最佳平衡点，过高的秩会导致过拟合。

#### 7. 优点（亮点）
*   **模块化设计**：巧妙地串联了 ASR、NLP、检测和分割四个模块，实现了从非结构化语音到结构化掩码的端到端处理。
*   **临床实用性**：直接对接放射科医生的口述习惯，降低了自动化工具的使用门槛。
*   **计算高效**：利用 LoRA 避免了对大型基础模型进行全量微调，极大地降低了算力需求。

#### 8. 不足与局限
*   **维度限制**：目前主要针对 **2D MRI 切片**进行处理，尚未扩展到 3D 空间的一致性分割。
*   **语音依赖性**：系统性能受限于 ASR 的准确率，如文中提到的“glioblastoma”被误识为“glioplaston”导致失败。
*   **模态单一**：目前仅验证了 T1 加权序列，未探讨多模态 MRI（如 T2, FLAIR）融合的效果。
*   **样本量**：真实临床语音测试的样本量（12 例）相对较小，需更大规模的临床验证。

（完）
