---
title: Adversarial Domain Adaptation Enables Knowledge Transfer Across Heterogeneous RNA-Seq Datasets
title_zh: 对抗性领域自适应实现异构 RNA-Seq 数据集间的知识迁移
authors: "Kevin Dradjat, Massinissa Hamidi, Blaise Hanczar"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08062v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 个性化医疗中RNA-seq数据的领域自适应
tldr: 针对RNA-seq数据在表型预测中面临的小样本和异构性挑战，本研究提出了一种基于对抗性领域自适应的深度学习框架。该方法通过在大型通用数据集（如TCGA、GTEx）和小型目标数据集之间构建领域不变的潜在空间，实现了有效的知识迁移。实验证明，该框架在癌症和组织类型分类任务中显著提升了模型在数据稀缺场景下的泛化能力和预测准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.08062v1/fig-001.webp\", \"caption\": \"Fig. 1: Conceptual illustration of domain adaptation process.\", \"page\": 2, \"index\": 1, \"width\": 496, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.08062v1/fig-002.webp\", \"caption\": \"Fig. 2: Composition of the domain adaptation pipeline. E, C and D denote, respectively, the encoder, the phenotype classifier and the domain discriminator. The unsupervised variant is defined by only the solid arrows, whereas we add the dotted arrows for the supervised variant. The discriminator loss can either be the Wasserstein distance or cross-entropy.\", \"page\": 3, \"index\": 2, \"width\": 1057, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2603.08062v1/fig-003.webp\", \"caption\": \"Fig. 4: Performances of each adaptation methods by considering the TCGA dataset as target (a) and GTEx dataset as target (b).\", \"page\": 5, \"index\": 3, \"width\": 901, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2603.08062v1/fig-004.webp\", \"caption\": \"Table 1. Accuracy on the TCGA/GTEx target test set after each adaptation method when considering the entire source and target training sets.\", \"page\": 5, \"index\": 4, \"width\": 1057, \"height\": 183}]"
motivation: 解决RNA-seq数据因预处理差异和样本量有限导致的深度学习模型过拟合及跨数据集泛化能力差的问题。
method: 采用对抗性领域自适应框架，通过联合优化分类目标和领域对齐目标，学习跨数据集的领域不变特征表示。
result: 在TCGA、ARCHS4和GTEx等大型转录组数据集上的实验表明，该方法在低数据量情况下显著优于非自适应基准模型。
conclusion: 领域自适应是实现转录组学数据高效知识迁移的有力策略，能有效提升受限数据条件下的表型预测鲁棒性。
---

## 摘要
从 RNA 测序 (RNA-seq) 数据中进行准确的表型预测对于诊断、生物标志物发现和个性化医疗至关重要。深度学习模型已展现出超越传统机器学习方法的强大潜力，但其性能依赖于大规模且标注良好的数据集。在转录组学中，此类数据集通常有限，导致模型出现过拟合且泛化能力较差。从更大、更通用的数据集中进行知识迁移可以缓解这一问题。然而，由于异构的预处理流程和目标表型的差异，跨 RNA-seq 数据集的信息迁移仍然具有挑战性。在本研究中，我们提出了一种基于深度学习的领域自适应框架，能够实现从大型通用数据集到小型数据集的有效知识迁移，用于癌症类型分类。该方法通过联合优化分类和领域对齐目标，学习一个领域不变的潜空间。为了确保在数据稀缺场景下的稳定训练和鲁棒性，该框架采用对抗性方法并结合适当的正则化进行训练。研究探索了监督和无监督两种变体，分别利用有标签或无标签的目标样本。该框架在三个大规模转录组数据集（TCGA、ARCHS4、GTEx）上进行了评估，以衡量其跨队列迁移知识的能力。实验结果表明，与非自适应基准模型相比，该方法在癌症和组织类型分类准确率上取得了持续提升，尤其是在低数据量场景下。总之，这项工作强调了领域自适应是转录组学中一种高效的数据迁移策略，能够在受限的数据条件下实现鲁棒的表型预测。

## Abstract
Accurate phenotype prediction from RNA sequencing (RNA-seq) data is essential for diagnosis, biomarker discovery, and personalized medicine. Deep learning models have demonstrated strong potential to outperform classical machine learning approaches, but their performance relies on large, well-annotated datasets. In transcriptomics, such datasets are frequently limited, leading to over-fitting and poor generalization. Knowledge transfer from larger, more general datasets can alleviate this issue. However, transferring information across RNA-seq datasets remains challenging due to heterogeneous preprocessing pipelines and differences in target phenotypes. In this study, we propose a deep learning-based domain adaptation framework that enables effective knowledge transfer from a large general dataset to a smaller one for cancer type classification. The method learns a domain-invariant latent space by jointly optimizing classification and domain alignment objectives. To ensure stable training and robustness in data-scarce scenarios, the framework is trained with an adversarial approach with appropriate regularization. Both supervised and unsupervised approach variants are explored, leveraging labeled or unlabeled target samples. The framework is evaluated on three large-scale transcriptomic datasets (TCGA, ARCHS4, GTEx) to assess its ability to transfer knowledge across cohorts. Experimental results demonstrate consistent improvements in cancer and tissue type classification accuracy compared to non-adaptive baselines, particularly in low-data scenarios. Overall, this work highlights domain adaptation as a powerful strategy for data-efficient knowledge transfer in transcriptomics, enabling robust phenotype prediction under constrained data conditions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adversarial Domain Adaptation Enables Knowledge Transfer Across Heterogeneous RNA-Seq Datasets》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：利用 RNA-seq 数据进行表型预测（如癌症诊断）对个性化医疗至关重要。虽然深度学习模型表现优异，但其高度依赖大规模标注数据。在实际应用中，转录组数据集往往规模较小、高度异构（存在批次效应和生物学差异），导致模型容易过拟合且跨研究泛化能力差。
*   **核心问题**：如何有效地将知识从大规模、通用的 RNA-seq 数据集（源域）迁移到小型、特定且分布不同的数据集（目标域）上，以提高在数据稀缺场景下的预测性能。

### 2. 方法论
*   **核心思想**：采用**对抗性领域自适应（Adversarial Domain Adaptation）**框架。通过在潜在空间中对齐源域和目标域的特征分布，学习“领域不变（Domain-invariant）”的特征表示，从而消除技术噪声（批次效应）和非目标生物差异。
*   **架构组成**：
    *   **编码器 (E)**：将输入基因表达数据投影到低维潜在空间。
    *   **分类器 (C)**：基于潜在表示预测表型标签。
    *   **判别器 (D)**：尝试区分特征来自于源域还是目标域。
*   **关键技术细节**：
    *   **联合优化**：目标函数为 $\min_{E,C} \max_{D} L_{cls}(E, C) + \lambda L_{dom}(E, D)$。
    *   **两种变体**：
        1.  **无监督 DA**：目标域无标签，仅靠对抗损失对齐分布。
        2.  **监督 DA**：目标域有少量标签，分类器同时在源域和目标域上训练，增强类一致性。
    *   **损失函数选择**：除了传统的 DANN（交叉熵损失），还引入了基于 **Wasserstein 距离**（带梯度惩罚）的判别器，以提供更平滑的梯度和更稳定的训练。

### 3. 实验设计
*   **数据集**：
    *   **ARCHS4**（源域）：包含超过 5 万个样本的通用组织数据集。
    *   **TCGA**（目标域）：泛癌数据集，用于癌症类型分类。
    *   **GTEx**（目标域）：健康组织数据集，用于组织类型分类。
*   **评估场景**：
    1.  **全量数据对齐**：观察潜在空间的分布情况。
    2.  **低目标数据制度**：模拟稀有疾病场景，目标域样本比例从 1% 增加到 20%。
    3.  **低源数据制度**：测试源域数据量受限时模型的鲁棒性。
*   **对比基准 (Benchmarks)**：
    *   **Target-only**：仅在目标域训练的 MLP。
    *   **Supervised no adaptation**：源域+目标域联合训练，但不进行领域对齐。
    *   **统计学批次校正方法**：ComBat 和 limma（生物信息学中最常用的线性校正工具）。

### 4. 资源与算力
*   **算力说明**：论文提到计算资源由 *Institut Français de Bioinformatique* 提供。
*   **具体参数**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了训练参数：200 个 epoch，batch size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了两个主要的迁移任务（ARCHS4 $\rightarrow$ TCGA 和 ARCHS4 $\rightarrow$ GTEx）。
    *   针对每个任务，对比了 8 种不同的方法/变体。
    *   在低数据制度实验中，每个比例点都进行了 5 次随机初始化迭代以评估方差。
*   **充分性评价**：实验设计较为充分。不仅评估了分类准确率，还通过 UMAP 可视化直观展示了特征对齐效果。通过改变源域和目标域的数据比例，深入探讨了模型在极端数据稀缺情况下的表现，具有较强的说服力。

### 6. 主要结论与发现
*   **领域对齐的必要性**：简单的合并数据集（无自适应的监督学习）在数据量大时效果尚可，但在小样本下表现不佳，甚至不如 Target-only。
*   **对抗学习的优越性**：提出的监督 DA 方法（尤其是 DANN 和 Wasserstein 变体）在所有目标数据比例下均显著优于 ComBat 和 limma，证明了深度学习捕捉非线性领域偏移的能力。
*   **数据效率**：在目标域仅有约 100 个样本（1%）时，DA 框架能大幅提升准确率，显示出极高的知识迁移效率。
*   **生物学相似性影响**：ARCHS4 到 GTEx（均为健康组织）的迁移效果优于到 TCGA（癌症），说明生物学背景的邻近性有助于领域自适应。

### 7. 优点
*   **鲁棒性设计**：引入 Wasserstein 距离解决了对抗训练中常见的梯度消失和不稳定问题。
*   **实用性强**：同时提供了监督和无监督方案，适应实际研究中标签获取难度不同的情况。
*   **超越传统工具**：证明了在处理复杂的非线性批次效应时，深度学习 DA 比传统的线性统计方法（ComBat/limma）更有效。

### 8. 不足与局限
*   **任务单一**：实验主要集中在分类任务（癌症/组织类型），未探讨回归任务（如生存预测或药物反应预测）。
*   **超参数敏感性**：对抗网络对超参数（如 $\lambda$）通常较为敏感，文中未详细讨论在不同数据集上如何自动调整这些参数。
*   **生物学解释性**：虽然实现了特征对齐，但对于哪些基因在对齐过程中起到了关键作用，缺乏深入的生物学解释。
*   **硬件信息缺失**：未提供算力消耗数据，不便于其他研究者评估复现成本。

（完）
