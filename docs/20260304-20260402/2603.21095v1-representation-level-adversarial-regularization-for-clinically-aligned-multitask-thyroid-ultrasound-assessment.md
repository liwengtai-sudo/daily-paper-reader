---
title: Representation-Level Adversarial Regularization for Clinically Aligned Multitask Thyroid Ultrasound Assessment
title_zh: 面向临床对齐的多任务甲状腺超声评估的表示层对抗正则化
authors: "Dina Salama, Mohamed Mahmoud, Nourhan Bayasi, David Liu, Ilker Hacihaliloglu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21095v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于风险类别预测的多任务甲状腺超声评估
tldr: 本研究针对甲状腺超声评估中放射科医生标注不一致的问题，提出了一种临床引导的多任务学习框架。该框架同步预测结节掩码与TI-RADS风险等级，并利用对齐的放射组学特征引导分类。为解决多任务间的梯度竞争，引入了表示级对抗正则化（RLAR），通过在潜空间中惩罚任务间对抗方向的过度对齐来优化共享表示。实验证明，该方法在提升风险分层准确性的同时保持了高质量分割，为临床辅助诊断提供了更稳健的工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.21095v1/fig-001.webp\", \"caption\": \"Table 5. Clinical-guidance feature ablation (Classification). Each row reports the change in performance relative to the full clinically guided model (all features enabled). Values are mean ± std over 3 folds; negative values indicate a performance drop. Bold denotes the largest degradation and Underline denotes the second-largest degradation within each dataset/metric column.\", \"page\": 8, \"index\": 1, \"width\": 723, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2603.21095v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed clinically guided multitask framework. A shared encoder jointly performs nodule segmentation and TI-RADS classification influenced by clinical guidance, while RLAR regularizes task interaction in the shared representation.\", \"page\": 3, \"index\": 2, \"width\": 720, \"height\": 371}]"
motivation: 旨在解决甲状腺超声评估中因标注者差异导致的分割与风险分级任务间的监督不一致及梯度竞争问题。
method: 提出一种结合临床放射组学引导的多任务框架，并引入表示级对抗正则化（RLAR）来显式控制任务间的梯度冲突。
result: 在公开TI-RADS数据集上，该方法在保持分割精度的同时显著提升了风险分层性能，优于现有的多任务学习基准。
conclusion: 通过结合临床先验知识与对抗性梯度正则化，该研究有效提升了多任务甲状腺超声评估模型的鲁棒性与准确性。
---

## 摘要
甲状腺超声是评估甲状腺结节并确定是否需要进行活检的一线检查手段。在常规报告中，放射科医生会生成两个耦合的输出：用于测量的结节轮廓和基于超声标准的 TI-RADS 风险分级。然而，不同阅片者的勾画风格和风险分级各不相同，导致了不一致的监督，这可能会降低标准学习流程的性能。在本文中，我们通过一个临床引导的多任务框架来处理这一工作流程，该框架在单个模型中联合预测结节掩码和 TI-RADS 类别。为了使风险预测基于具有临床意义的证据，我们在训练过程中使用紧凑的、与 TI-RADS 对齐的影像组学目标来引导分类嵌入，同时保留互补的深度特征以维持判别性能。然而，在标注者存在差异的情况下，朴素的多任务优化往往会失败，这并非因为任务之间不相关，而是因为它们的梯度在共享表示中存在竞争。为了使这种竞争显式化且可控，我们引入了 RLAR，一种表示层对抗梯度正则化器。RLAR 并非执行参数级的梯度手术，而是利用每个任务在潜空间中的归一化对抗方向作为任务敏感性的几何探测器，并对任务特定对抗方向之间过度的角度对齐进行惩罚。在一个公开的 TI-RADS 数据集上，与单任务训练和传统多任务基准模型相比，我们带有 RLAR 的临床引导多任务模型在保持分割质量的同时，持续提升了风险分层性能。代码和预训练模型将会发布。

## Abstract
Thyroid ultrasound is the first-line exam for assessing thyroid nodules and determining whether biopsy is warranted. In routine reporting, radiologists produce two coupled outputs: a nodule contour for measurement and a TI-RADS risk category based on sonographic criteria. Yet both contouring style and risk grading vary across readers, creating inconsistent supervision that can degrade standard learning pipelines. In this paper, we address this workflow with a clinically guided multitask framework that jointly predicts the nodule mask and TI-RADS category within a single model. To ground risk prediction in clinically meaningful evidence, we guide the classification embedding using a compact TI-RADS aligned radiomics target during training, while preserving complementary deep features for discriminative performance. However, under annotator variability, naive multitask optimization often fails not because the tasks are unrelated, but because their gradients compete within the shared representation. To make this competition explicit and controllable, we introduce RLAR, a representation-level adversarial gradient regularizer. Rather than performing parameter-level gradient surgery, RLAR uses each task's normalized adversarial direction in latent space as a geometric probe of task sensitivity and penalizes excessive angular alignment between task-specific adversarial directions. On a public TI-RADS dataset, our clinically guided multitask model with RLAR consistently improves risk stratification while maintaining segmentation quality compared to single-task training and conventional multitask baselines. Code and pretrained models will be released.

---

## 论文详细总结（自动生成）

### 论文结构化总结：面向临床对齐的多任务甲状腺超声评估的表示层对抗正则化

#### 1. 核心问题与研究动机
甲状腺超声评估在临床中包含两个耦合任务：**结节勾画（分割）**和**TI-RADS风险分级（分类）**。目前深度学习在该领域面临两大挑战：
*   **标注不一致性**：不同放射科医生对同一结节的轮廓勾画和风险评分存在主观差异，导致监督信号不稳定。
*   **多任务梯度冲突**：在共享编码器的多任务学习中，分割和分类任务的梯度往往在表示空间中相互竞争（负迁移），导致模型性能下降。
*   **缺乏临床可解释性**：纯数据驱动的分类模型往往忽略了临床医生关注的形态学特征（如边缘、回声等）。

#### 2. 核心方法论
论文提出了一种临床引导的多任务框架，核心思想是将临床先验知识与一种新型的梯度正则化技术相结合：
*   **临床引导的架构**：在共享编码器的基础上，将分类嵌入空间划分为两部分：一部分用于编码**TI-RADS对齐的影像组学特征**（如形状、边缘锐度、纹理等13个指标），另一部分保留深度非线性特征。
*   **影像组学蒸馏**：在训练阶段，利用真值掩码计算放射组学特征作为回归目标（$L_{clin}$），引导模型学习临床相关的描述符。推理时无需计算这些特征，保持了部署的简便性。
*   **表示层对抗正则化 (RLAR)**：
    *   **核心逻辑**：不直接在参数空间做梯度手术，而是在共享的瓶颈表示层（Bottleneck）进行干预。
    *   **技术细节**：为每个任务计算一个归一化的**对抗方向**（即在表示空间中使损失函数增加最快的方向），将其作为任务敏感性的几何探测器。
    *   **正则化项**：计算不同任务对抗方向之间的余弦相似度，并惩罚过度的角度对齐（$L_{RLAR}$），从而鼓励任务间形成互补而非冲突的表示。

#### 3. 实验设计
*   **数据集**：
    *   **ThyroidXL**：大规模专家标注数据集（11,545张图像），用于3折交叉验证。
    *   **Stanford AIMI**：外部测试集（15,093帧电影剪辑图像），用于评估模型的零样本（Zero-shot）泛化能力。
*   **基准模型 (Benchmarks)**：
    *   **单任务模型**：EfficientNet-B7, ConvNeXt。
    *   **多任务基准**：Vanilla Multitask（朴素多任务）、Rep-MTL（基于表示的多任务学习）。
*   **对比维度**：分类性能（精确率、召回率、F1）、分割性能（Dice、IoU、HD95）。

#### 4. 资源与算力
*   **硬件**：单台 16 GB GPU 服务器，配备 16 核 CPU。
*   **软件**：PyTorch 2.9.1, CUDA 12.8。
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 8，训练 500 个 Epoch（含 250 个训练步和 50 个验证步）。使用了混合精度训练。

#### 5. 实验数量与充分性
*   **实验规模**：进行了跨数据集的验证（内部 CV + 外部零样本测试），涵盖了分类和分割两大任务。
*   **消融实验**：
    *   **RLAR 作用位置**：对比了在瓶颈层、末端编码层、中间编码层等不同位置应用正则化的效果。
    *   **特征重要性分析**：通过“留一法”分析了 13 种临床引导特征对最终预测的贡献度。
*   **评价**：实验设计较为充分，特别是引入外部电影剪辑数据集（Cine-clip）进行零样本测试，极大地增强了结果的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升**：RLAR 在 ThyroidXL 上显著提升了风险分层性能，同时保持了与 nnU-Net 相当的分割精度。
*   **鲁棒性增强**：在外部数据集 AIMI 上，RLAR 表现出更强的召回率（减少漏诊）和更低的边界距离误差（HD95），证明了其在处理领域偏移时的稳健性。
*   **优化稳定性**：相比于朴素多任务学习，RLAR 显著降低了不同折叠实验间的性能方差，使训练过程更加稳定。

#### 7. 优点与亮点
*   **临床对齐**：通过影像组学蒸馏将 TI-RADS 标准融入深度学习，使模型决策更符合医学逻辑。
*   **几何视角**：RLAR 提供了一种新颖的视角来处理多任务冲突，即通过潜空间的对抗敏感性方向来衡量任务干扰，比传统的参数级梯度投影更高效。
*   **无额外开销**：临床引导和 RLAR 仅在训练阶段存在，推理阶段不增加任何计算负担或输入要求。

#### 8. 不足与局限
*   **数据集分布不均**：实验中提到的两个数据集在 TI-RADS 类别分布上存在严重失衡（如 TR4 占主导），虽然使用了加权交叉熵，但极端少数类别的表现仍有提升空间。
*   **超参数敏感性**：RLAR 的效果可能依赖于正则化权重 $\lambda_{adv}$ 的选择，文中未详细讨论该参数在不同场景下的敏感度。
*   **应用限制**：目前仅针对 2D 超声图像，未充分利用电影剪辑（Cine）中的时序信息。

（完）
