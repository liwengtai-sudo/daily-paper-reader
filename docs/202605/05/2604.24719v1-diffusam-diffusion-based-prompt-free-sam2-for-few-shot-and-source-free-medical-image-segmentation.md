---
title: "DiffuSAM: Diffusion-Based Prompt-Free SAM2 for Few-Shot and Source-Free Medical Image Segmentation"
title_zh: DiffuSAM：用于少样本和无源医疗图像分割的基于扩散模型的无提示 SAM2
authors: "Tal Grossman, Noa Cahan, Lev Ayzenberg, Hayit Greenspan"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24719v1"
tags: ["query:ai-pm"]
score: 9.0
evidence: 用于癌症检测任务的基于扩散的医学图像分割
tldr: 针对SAM2在医疗影像中依赖专家提示且域迁移能力有限的问题，本文提出DiffuSAM。该方法通过轻量级扩散先验从冻结的SAM2特征中合成掩码嵌入，实现了无需人工提示的自动分割。通过引入空间一致性约束并应用于少样本和无源域自适应场景，DiffuSAM在CT和MRI数据集上表现出色，显著提升了医疗分割的效率与准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.24719v1/fig-001.webp\", \"caption\": \"Fig. 3: Diffusion inference steps. Top: CT; Bottom: MRI.\", \"page\": 3, \"index\": 1, \"width\": 486, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.24719v1/fig-002.webp\", \"caption\": \"Table 1: Few-shot settings: (a) model requirements and (b) Dice results per organ on BTCV dataset.\", \"page\": 4, \"index\": 2, \"width\": 502, \"height\": 401}, {\"url\": \"assets/figures/arxiv/2604.24719v1/fig-003.webp\", \"caption\": \"Fig. 1: An outline of our proposed method. (a) In our proposed network, we add a diffusion prior (in Green) to generate memory embeddings from the image embeddings, thereby removing the need for a user prompt (in Red). During training, the SAM2 components all remain frozen. (b) Diffusion Prior model. During training, a diffusion model takes a memory embedding to which noise has been added, the matching image embedding, an embedding of the current time step, and a label. The system learns to use the image embedding to remove the noise in successive time steps. In inference, it generates a memory embedding by starting with pure noise and an image embedding and removing noise iteratively according to that embedding.\", \"page\": 2, \"index\": 3, \"width\": 477, \"height\": 513}]"
motivation: 旨在解决SAM2在医疗影像应用中需要复杂专家提示以及在自然图像训练后难以直接迁移到医学领域的问题。
method: 提出一种基于扩散先验的框架，从SAM2图像特征中生成掩码嵌入并输入解码器，同时利用相邻切片的空间信息增强一致性。
result: 在BTCV和CHAOS数据集的少样本及无源无监督域自适应实验中，该方法取得了具有竞争力的分割精度且训练推理高效。
conclusion: DiffuSAM成功将扩散模型与SAM2结合，为医疗影像提供了一种无需提示、高效且泛化能力强的自动分割方案。
---

## 摘要
Segment Anything Model (SAM) 和 SAM2 等分割模型在提示驱动下表现出强大的零样本性能。然而，它们在自然图像上的训练限制了其向医疗数据的领域迁移。因此，准确的分割通常需要大量的微调和专家设计的提示。我们提出了 DiffuSAM，这是一种针对无提示医疗图像分割的 SAM2 扩散适配版本。我们的框架通过轻量级扩散先验，从现成的冻结 SAM2 图像特征中合成与 SAM2 兼容的类分割掩码嵌入。生成的嵌入被集成到 SAM2 的掩码解码器中以产生准确的分割，从而消除了对用户提示的需求。该扩散先验进一步以先前分割的切片为条件，从而增强了跨体数据的空间一致性。在 BTCV 和 CHAOS 数据集的 CT 和 MRI 图像上，在无源无监督领域自适应 (SF-UDA) 和少样本设置下进行的评估表明，DiffuSAM 以高效的训练和推理实现了具有竞争力的性能。代码可根据要求向通讯作者索取。

## Abstract
Segmentation models such as Segment Anything Model (SAM) and SAM2 achieve strong prompt-driven zero-shot performance. However, their training on natural images limits domain transfer to medical data. Consequently, accurate segmentation typically requires extensive fine-tuning and expert-designed prompts. We propose DiffuSAM, a diffusion-based adaptation of SAM2 for prompt-free medical image segmentation. Our framework synthesizes SAM2-compatible segmentation mask-like embeddings via a lightweight diffusion-prior from off-the-shelf frozen SAM2 image features. The generated embeddings are integrated into SAM2's mask decoder to produce accurate segmentations, thereby eliminating the need for user prompts. The diffusion prior is further conditioned on previously segmented slices, enforcing spatial consistency across volumes. Evaluated on the BTCV and CHAOS datasets for CT and MRI under Source-Free Unsupervised Domain Adaptation (SF-UDA) and Few-Shot settings, DiffuSAM achieves competitive performance with efficient training and inference. Code is available upon request from the corresponding author.

---

## 论文详细总结（自动生成）

这是一份关于论文《DiffuSAM: Diffusion-Based Prompt-Free SAM2 for Few-Shot and Source-Free Medical Image Segmentation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何克服 SAM2（Segment Anything Model 2）在医疗影像应用中的两大瓶颈：**领域偏移（Domain Shift）**和**提示依赖（Prompt Dependency）**。
*   **背景**：SAM2 虽然在自然图像上表现卓越，但医疗影像（CT/MRI）在纹理、对比度和解剖结构上差异巨大。此外，临床工作流中手动提供点、框提示（Prompts）效率低下且依赖专家经验。
*   **整体含义**：本文旨在开发一种无需人工提示、能够快速适应少样本（Few-shot）和无源域自适应（SF-UDA）场景的自动医疗图像分割框架。

### 2. 方法论：DiffuSAM 框架
*   **核心思想**：利用**扩散模型（Diffusion Model）**作为先验，从冻结的 SAM2 图像特征中学习并生成“记忆嵌入（Memory Embeddings）”，从而替代人工提示来引导 SAM2 的解码器。
*   **关键技术细节**：
    *   **扩散先验（Diffusion Prior）**：训练一个轻量级的 2D UNet 扩散模型。在训练阶段，它学习将加噪的记忆嵌入还原为真实的 SAM2 记忆嵌入，条件是冻结的 SAM2 图像编码器输出和类别标签。
    *   **无提示推理**：推理时，模型从纯噪声开始，结合当前切片的图像特征，通过迭代去噪生成记忆嵌入，直接输入 SAM2 的掩码解码器。
    *   **体积一致性（Volumetric Consistency）**：为了保证 3D 序列的连续性，扩散先验会参考相邻切片的分割预测。通过记忆编码器将相邻切片信息融合，确保解剖结构的平滑过渡。
    *   **损失函数**：结合了先验损失（$L_{prior}$，去噪误差）和分割损失（$L_{seg}$，解码后的掩码与真值的差异）。

### 3. 实验设计
*   **数据集**：
    *   **BTCV**：腹部 CT 数据集（30 个病例），用于少样本学习。
    *   **CHAOS**：T2-SPIR MRI 数据集（20 个病例），用于跨模态（CT $\rightarrow$ MRI）的无源域自适应（SF-UDA）测试。
*   **任务目标**：分割脾脏、右肾、左肾和肝脏。
*   **对比方法（Benchmarks）**：
    *   **监督学习**：UNet, UNETR。
    *   **基础模型/变体**：原始 SAM, SAM2（带随机点提示）, FATE-SAM（少样本适配版）。
    *   **SF-UDA 基准**：DPL, DFG。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A5000 GPU**。
*   **显存占用**：训练和推理均**小于 5 GB**，体现了极高的轻量化特性。
*   **训练参数**：迭代 7800 次，Batch Size 为 4，使用 Adam 优化器。
*   **推理效率**：推理时仅需 **2 步扩散迭代**即可生成高质量嵌入，保证了速度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **少样本实验**：在 BTCV 上使用 10% 数据训练，90% 测试。
    *   **跨模态实验**：CT 训练，MRI 测试（SF-UDA）。
    *   **消融实验**：对比了有无“3D 体积一致性”模块的效果。
*   **评价**：实验设计较为客观，涵盖了医疗影像中最具挑战性的少样本和跨模态场景。通过 t-SNE 可视化展示了扩散模型学习嵌入分布的过程，增强了结果的可解释性。

### 6. 主要结论与发现
*   **性能优异**：在少样本设置下，DiffuSAM 的平均 Dice 分数达到 **87.24%**，优于需要提示的 SAM2（44.36%）和强基准 FATE-SAM。
*   **跨模态能力**：在 SF-UDA 场景下，Dice 分数达到 **85.2%**，与当前最先进的 DFG 方法持平，远超传统无源自适应方法。
*   **空间约束的重要性**：引入相邻切片条件（3D 约束）后，Dice 分数在两种设置下均提升了约 5%，证明了医疗影像中空间上下文的关键作用。

### 7. 优点
*   **无需提示（Prompt-Free）**：彻底解决了 SAM 类模型在医疗领域依赖人工交互的痛点。
*   **轻量且高效**：对算力要求极低（<5GB 显存），且推理步数极少（2步），适合临床部署。
*   **即插即用**：保持 SAM2 核心权重冻结，仅训练轻量级先验，保留了基础模型的泛化能力。

### 8. 不足与局限
*   **上限限制**：作者承认其表现可能略逊于在海量医疗数据上进行全量微调的 MedSAM（因为 MedSAM 使用了更强的领域特定监督）。
*   **实验覆盖面**：目前仅在腹部器官上验证，尚未在更复杂的病灶（如微小肿瘤）或更多模态（如超声、病理）上测试。
*   **扩散步数权衡**：虽然 2 步推理很快，但可能在极端复杂的解剖结构下牺牲了一定的生成精度。

（完）
