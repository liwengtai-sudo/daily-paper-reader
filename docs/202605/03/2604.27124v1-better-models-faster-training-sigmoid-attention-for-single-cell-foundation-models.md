---
title: "Better Models, Faster Training: Sigmoid Attention for single-cell Foundation Models"
title_zh: 更优的模型，更快的训练：用于单细胞基础模型的 Sigmoid 注意力机制
authors: "Vijay Sadashivaiah, Georgios Dasoulas, Judith Mueller, Soumya Ghosh"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27124v1"
tags: ["query:dl"]
score: 9.0
evidence: 用于单细胞生物学基础模型的Sigmoid注意力机制
tldr: "本研究针对单细胞基础模型训练中的稳定性与效率问题，提出使用Sigmoid注意力机制替代传统的Softmax。通过理论分析证明了Sigmoid具有全局有界导数和对角雅可比结构，能有效缓解训练不稳定性。实验表明，该方法在六个单细胞数据集上显著提升了细胞类型分离度，训练速度提升10%，且在大规模长序列压力测试中表现出极高的稳定性。此外，团队开源了高效的TritonSigmoid内核，为生物基础模型提供了更优的架构选择。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.27124v1/fig-001.webp\", \"caption\": \"Table 1: Pairwise MMD for Heart OFT dataset at 4K context. Each row shows one cell-type pair comparison. Values show MMD statistic with 95% bootstrap confidence interval [lower, upper] from 1000 bootstrap resamples. Best (highest) MMD per pair shown in bold. Sigmoid achieves higher MMD on all 28 pairs, with mean improvement of 25.0%.\", \"page\": 11, \"index\": 1, \"width\": 772, \"height\": 669}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-002.webp\", \"caption\": \"Figure 8: Kernel execution time comparison. GPU kernel latency (milliseconds) across head dimensions (64, 128), padding levels (0%, 25%), and forward/backward passes. Rows 1–2: no padding. Rows 3–4: 25% padding, FlashSigmoid unavailable (no padding support). Standard Attention excluded (60–85% slower than optimized kernels). TritonSigmoid achieves lowest latency in most configurations, particularly with padding. Shaded regions: 99% confidence intervals. Context lengths: 512–16,384 tokens. H100 GPU, BF16 precision.\", \"page\": 25, \"index\": 2, \"width\": 863, \"height\": 1040}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-003.webp\", \"caption\": \"Figure 1: Sequence length distribution in CellxGene pretraining dataset. The histogram (left y-axis, gray bars) shows the distribution of sequence lengths across cells in the dataset, revealing high variability characteristic of biological data. The cumulative distribution function (right y-axis, black line) indicates the fraction of sequences below each length. Vertical dashed lines mark common context length thresholds (2,048, 4,096, 8,192, and 16,384 tokens) and percentage of cells fully covered at each. Unlike text data where multiple sequences can be packed together, biological sequences must be processed individually with padding, making padding-aware kernel implementations essential. The jagged distribution demonstrates why efficient padding support is critical for biological foundation models.\", \"page\": 2, \"index\": 3, \"width\": 817, \"height\": 328}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-004.webp\", \"caption\": \"Figure 4: Training convergence and validation performance across datasets. Left: Training loss over full CellxGene dataset for four models (2K/4K context × sigmoid/softmax). All models converge smoothly with sigmoid 4K achieving the lowest final loss. Right: Six subplots showing validation loss per dataset. Each subplot displays four measurements: softmax (red) and sigmoid (blue) at 2K (circles) and 4K (squares) contexts. Two consistent patterns emerge: (1) Sigmoid systematically outperforms softmax across all datasets and both context lengths, and (2) longer context (4K) yields lower loss than shorter context (2K) for both attention mechanisms, demonstrating that extended context windows improve modeling of gene expression patterns.\", \"page\": 10, \"index\": 4, \"width\": 963, \"height\": 399}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-005.webp\", \"caption\": \"Figure 5: SCIB biological conservation metrics at 4K context. Scatter plots comparing sigmoid (y-axis) vs softmax (x-axis) across six datasets. Each panel shows a different metric: Leiden NMI (clustering agreement with ground-truth labels), Leiden ARI (pairwise clustering accuracy), Silhouette label (cell-type cohesion), and aggregate Bio Conservation (average of the three components). Points above the diagonal favor sigmoid. Sigmoid achieves perfect dominance on cell-type cohesion (6/6 datasets) and wins overall on 4/6 datasets for aggregate biological conservation, demonstrating superior preservation of biological structure compared to softmax.\", \"page\": 10, \"index\": 5, \"width\": 967, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-006.webp\", \"caption\": \"Table 2: Model architecture specifications. All experiments use 160M parameter models with these architectural choices.\", \"page\": 18, \"index\": 6, \"width\": 682, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-007.webp\", \"caption\": \"Figure 2: TFLOPS comparison across attention implementations. Performance across head dimensions (64, 128), padding levels (0%, 25%), and forward/backward passes. Rows 1–2: no padding, all implementations compared. Rows 3–4: 25% padding, FlashSigmoid unavailable (no padding support). TritonSigmoid matches or exceeds FlashSigmoid without padding and outperforms FlashAttention-2 across all configurations. Error bars: 99% confidence intervals. Context lengths: 512–16,384 tokens. H100 GPU, BF16 precision.\", \"page\": 8, \"index\": 7, \"width\": 863, \"height\": 1037}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-008.webp\", \"caption\": \"Table 4: Evaluation datasets. Six single-cell RNA-seq datasets from CellxGene covering diverse tissues, ages, and conditions.\", \"page\": 19, \"index\": 8, \"width\": 872, \"height\": 444}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-009.webp\", \"caption\": \"Figure 6: Softmax divergence vs sigmoid stability under stress-test conditions. Comparing training dynamics for identical 160M parameter models (8K context, no gradient clipping). Left: Training loss. Softmax initially learns (10→3 over 40K steps) but diverges catastrophically at step 55.6K (loss→10+). Sigmoid maintains stable monotonic decrease throughout 80K steps. Middle: Global gradient norm (log scale). Softmax explodes from ∼100 to 1.6× 106 (four orders of magnitude) at divergence. Sigmoid remains bounded (10–100 range) throughout. Right: Absolute maximum attention score in layer 0 (log scale). Softmax scores grow exponentially to 2× 108, while sigmoid scores remain controlled (1–5 range).\", \"page\": 12, \"index\": 9, \"width\": 955, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-010.webp\", \"caption\": \"Figure 3: End-to-end training compute cost. Projected GPU hours to process 131.6M samples across four model sizes (160M, 400M, 600M, 1.4B) and three context lengths (2K, 4K, 8K), derived from measured throughput on 16 H100 GPUs (batch size 32; 2 samples/GPU). Sigmoid is consistently faster than softmax, with the gap increasing at longer contexts (e.g., 1.4B model: 2.1% at 2K, 4.0% at 4K, 7.5% at 8K), reflecting the quadratic scaling of attention with sequence length. Error bars show 95% confidence intervals.\", \"page\": 9, \"index\": 10, \"width\": 966, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2604.27124v1/fig-011.webp\", \"caption\": \"Figure 7: UMAP visualization for Heart OFT dataset at 4K context. Left: Softmax attention. Right: Sigmoid attention. We see that sigmoid model derived embeddings cluster better. For instance, endothelial cells (purple), are clustered closer by sigmoid, while softmax has two separate clusters.\", \"page\": 20, \"index\": 11, \"width\": 968, \"height\": 310}]"
motivation: 传统的Softmax注意力机制在训练大规模生物基础模型时存在梯度不稳定、计算开销大以及长序列处理困难等局限性。
method: 提出将Sigmoid注意力作为Softmax的直接替代方案，并开发了高性能的TritonSigmoid GPU内核以支持高效计算。
result: "实验显示Sigmoid注意力使细胞类型分离度提高25%，训练速度提升10%，且在无梯度裁剪的高压测试下仍能保持训练稳定。"
conclusion: Sigmoid注意力在理论上更具稳定性，在实践中表现更优，是构建高性能生物基础模型的理想选择。
---

## 摘要
训练稳定的生物基础模型需要重新思考注意力机制：我们发现，将 sigmoid 注意力作为 softmax 注意力的直接替代方案，能够：a) 产生更好的学习表示：在六个不同的单细胞数据集上，sigmoid 实现了高出 25% 的细胞类型分离度、更好的细胞类型内聚性指标以及更低的验证损失；b) 训练速度更快：采用 sigmoid 注意力的模型比对应的 softmax 模型训练速度快达 10%；c) 训练更稳定：通过消除 softmax 注意力中固有的不稳定源。我们证明了 sigmoid 注意力具有全局有界的导数（≤ 0.25），而 softmax 则不然；此外，与 softmax 的稠密耦合相比，sigmoid 具有对角雅可比矩阵结构，这些共同有助于缓解训练不稳定性。在对 160M 参数的双向注意力模型进行的压力测试中（在 8K token 序列上训练且不使用梯度裁剪），softmax 发生了灾难性的发散，梯度爆炸了四个数量级，而 sigmoid 保持稳定。最后，我们实现并开源了 TritonSigmoid，这是一个高效的 GPU 内核，在 H100 GPU 上达到了 515 TFLOPS，性能优于 FlashAttention-2 和 FlashSigmoid，并提供原生填充（padding）支持，这对于生物序列至关重要。我们的结果证明了 sigmoid 注意力在生物基础模型中既有理论依据，又具有实证优越性。代码可在 https://github.com/MSDLLCpapers/triton-sigmoid 获取。

## Abstract
Training stable biological foundation models requires rethinking attention mechanisms: we find that using sigmoid attention as a drop in replacement for softmax attention a) produces better learned representations: on six diverse single-cell datasets, sigmoid achieves 25% higher cell-type separation, better cell-type cohesion metrics, and lower validation loss, b) faster training, models with sigmoid attention train up to 10% faster than their softmax counterparts, and c) more stable training by eliminating inherent sources of instability in softmax attention. We establish that sigmoid attention has globally bounded derivatives ($\leq 0.25$) as opposed to softmax, and a diagonal Jacobian structure in contrast with softmax's dense coupling, which together help alleviate training instabilities. In stress tests on 160M-parameter bidirectional attention models trained without gradient clipping on 8K-token sequences, softmax diverges catastrophically, with gradients exploding by four orders of magnitude, while sigmoid remains stable. Finally, we implement and open-source TritonSigmoid, an efficient GPU kernel that achieves 515 TFLOPS on H100 GPUs, outperforming both FlashAttention-2 and FlashSigmoid, with native padding support, which is essential for biological sequences. Our results establish sigmoid attention as both theoretically grounded and empirically superior for biological foundation models. Code is available at https://github.com/MSDLLCpapers/triton-sigmoid

---

## 论文详细总结（自动生成）

这篇论文针对单细胞生物学基础模型（scFMs）的训练效率与稳定性问题，提出了一种基于 Sigmoid 注意力机制的替代方案，并开发了高性能的计算内核。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：单细胞基础模型通常沿用自然语言处理中的 Softmax 注意力机制，但在生物学领域面临三大挑战：
    1.  **生物学不匹配**：Softmax 是竞争性的（权重和为1），而基因调控往往是非竞争性的，多个转录因子可同时独立作用。
    2.  **训练不稳定性**：在处理长基因序列（4K-16K tokens）时，Softmax 的 Lipschitz 常数随得分呈指数增长，易导致梯度爆炸和训练崩溃。
    3.  **计算效率低下**：生物数据具有极高的“锯齿状”特征（不同细胞表达基因数差异巨大），现有高效内核（如 FlashAttention）对 Padding（填充）支持不足，导致大量无效计算。
*   **核心目标**：通过 Sigmoid 注意力提升模型表示能力、训练稳定性和长序列处理速度。

### 2. 论文提出的方法论
*   **核心思想**：将注意力机制中的 Softmax 替换为逐元素的 Sigmoid 函数。
*   **技术细节**：
    *   **公式定义**：$SigmoidAttn(X) = \sigma(\frac{QK^\top}{\sqrt{d}} + b)V$。其中偏置项 $b$ 设为 $-\log(n)$（$n$ 为序列长度），以在经验上逼近 Softmax 的缩放效果。
    *   **理论优势**：Sigmoid 的导数全局有界（$\le 0.25$），且其雅可比矩阵（Jacobian）是平滑的对角结构，消除了 Token 间的梯度耦合，从根本上抑制了梯度爆炸。
    *   **TritonSigmoid 内核**：
        *   **原生填充支持**：引入分块稀疏计算，自动识别并跳过全填充（Padded）的数据块。
        *   **算子融合**：利用 Triton 语言实现，将缩放、偏置、Sigmoid 和加权求和融合进单个 GPU 内核。
        *   **反向传播优化**：将反向传播分解为两个内核，避免原子操作，并重新计算激活值以节省显存。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 CellxGene 数据库（2024年6月快照），包含 **1.316 亿个细胞**。
    *   **验证集**：6 个独立的单细胞数据集，涵盖青少年大脑、印尼 PBMC、健康结肠、肺部排斥反应、心脏流出道（Heart OFT）和衰老 PBMC。
*   **Benchmark 与对比方法**：
    *   **模型对比**：Softmax 注意力（使用 PyTorch FlexAttention 实现）。
    *   **内核对比**：标准 PyTorch Attention、FlashAttention-2、FlashSigmoid。
*   **评估指标**：验证损失（Validation Loss）、scIB 生物保护指标（NMI, ARI, Silhouette）、最大平均差异（MMD）以及训练吞吐量。

### 4. 资源与算力
*   **硬件环境**：主要使用 **NVIDIA H100 80GB SXM5 GPU**。
*   **模型规模**：涵盖 160M、400M、600M 和 **1.4B** 参数四种规格。
*   **训练时长**：
    *   1.4B 模型在 8K 上下文下训练需约 **8000-8600 GPU 小时**。
    *   160M 模型在 2K/4K 上下文下训练至完全收敛，分别耗时约 650-900 GPU 小时。
*   **精度**：使用 BF16 混合精度训练。

### 5. 实验数量与充分性
*   **实验规模**：预训练样本量达亿级，验证集跨越多种组织和生理状态，具有极强的代表性。
*   **消融与压力测试**：
    *   针对 2K、4K、8K 不同上下文长度进行了对比。
    *   **压力测试**：在 8K 长度且**不使用梯度裁剪**的极端条件下对比 Softmax 和 Sigmoid 的稳定性。
*   **客观性**：实验涵盖了从内核级算力测试到端到端训练时间，再到下游生物学表征质量的全方位评估，对比基准（如 FlashAttention-2）均为行业顶尖水平，实验设计客观公正。

### 6. 主要结论与发现
*   **表示质量更优**：Sigmoid 在所有 6 个验证集上的损失均低于 Softmax，细胞类型分离度（MMD）平均提升 **25%**，生物结构保护得分更高。
*   **训练速度更快**：端到端训练提速达 **10%**。TritonSigmoid 内核在 H100 上达到 **515 TFLOPS**，优于 FlashAttention-2（361 TFLOPS）。
*   **极致稳定性**：在压力测试中，Softmax 在 5.5 万步左右发生灾难性发散（梯度爆炸 4 个数量级），而 Sigmoid 始终保持单调收敛且梯度平稳。
*   **长序列友好**：随着上下文长度增加，Sigmoid 的效率优势更加明显。

### 7. 优点
*   **理论与工程结合**：不仅提供了严谨的雅可比矩阵稳定性证明，还开发了高性能的开源 Triton 内核。
*   **解决行业痛点**：针对生物序列“锯齿状”分布设计的 Padding 优化，极大地减少了计算浪费。
*   **即插即用**：作为 Softmax 的直接替代品，无需复杂的超参数调整即可提升现有模型性能。

### 8. 不足与局限
*   **模态局限性**：实验主要集中在单细胞 RNA-seq 数据，尚未验证其在空间转录组或多模态单细胞数据中的表现。
*   **模型架构单一**：主要基于双向 Transformer 编码器，未深入探讨在生成式（Decoder-only）生物模型中的表现。
*   **硬件依赖**：高性能内核针对 H100 等现代 GPU 优化，在旧型号硬件上的加速效果可能受限。

（完）
